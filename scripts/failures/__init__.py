"""FAILURES aggregation tooling (Phase 6 Plan 09 + Phase 10 promotion).

Phase 6 ships ONLY the dry-run aggregator (aggregate_patterns.py). Real
SKILL.md.candidate creation + 7-day staged rollout state machine is Phase 10
(deferred per D-13). The dry-run aggregator is SAFE to run repeatedly; no
side effects beyond stdout / --output JSON.
"""
