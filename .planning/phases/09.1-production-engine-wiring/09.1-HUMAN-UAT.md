---
status: partial
phase: 09.1-production-engine-wiring
source: [09.1-VERIFICATION.md, 09.1-VALIDATION.md §Manual-Only Verifications]
started: 2026-04-20T00:00:00Z
updated: 2026-04-20T12:00:00Z
---

## Current Test

[대표님 외출 복귀 후 수동 검증 2건 대기 (YOLO 세션 #24 architecture fix 적용 후 축소: 3→2)]

## Architecture Fix Note (2026-04-20 세션 #24)

최초 Phase 9.1 Plan 05 는 `anthropic` Python SDK (usage-based billing) 로 구현했으나 **대표님 Claude Code Max 요금제 활용 원칙에 위배**. 동일 세션 내에서 **Claude Code CLI (`claude --print --append-system-prompt --json-schema`) subprocess 경로로 전면 재작성**. 결과: `ANTHROPIC_API_KEY` 자체가 불필요 (원 UAT #1 의 제거 근거).

Memory: `project_claude_code_max_no_api_key.md` — 향후 모든 세션 자동 소급 적용.

## Tests

### 1. Live smoke clip.mp4 품질 평가 (REQ-091-07, SC4) — **Kling 2.6 Pro 재생성 필요**
expected: `output/phase091_smoke/clip.mp4` 재생 → 다음 3항목 주관 평가:
  a) 캐릭터 consistency — `output/phase091_smoke/scene.png` (Nano Banana 생성) 과 clip.mp4 의 인물이 동일인물로 인식되는가
  b) 복합 action 표현 — "커피잔을 입으로 가져가는 부드러운 motion + 양손 유지 + 자연스러운 미소" (Template A 기준) 이 5초 내에 드러나는가
  c) 기존 Runway Gen-3a Turbo 버전 (세션 #24 전반부 $0.29 smoke, task ID `ac2823c6-71df-48fd-a6c7-59d016616899`) 과 비교한 품질 차이 (컵 크기 안정성, 위치 정확도, limb duplication 부재)
procedure:
```bash
# 선행 조건: KLING_API_KEY 또는 FAL_KEY + GOOGLE_API_KEY 환경 설정
cd C:\Users\PC\Desktop\naberal_group\studios\shorts
python scripts/smoke/phase091_stage2_to_4.py --live
# 예상 비용: $0.04 (Nano Banana) + $0.35 (Kling 2.6 Pro 5s) = $0.39, cap $1.00
# 산출물: output/phase091_smoke/{scene.png, clip.mp4, manifest.json}
explorer C:\Users\PC\Desktop\naberal_group\studios\shorts\output\phase091_smoke\
```
평가: `clip.mp4` 재생 + `scene.png` 동시 open 비교.
result: [pending — 대표님 주관 평가 필수, Kling 2.6 Pro 버전 재생성 후]
notes:
  - **스택 변경 (2026-04-20 세션 #24 후반 최종)**: Runway Gen-3a Turbo → **Kling 2.6 Pro primary** 전환. smoke CLI 는 박제 batch 에서 Template A (27단어 3원칙) + Kling 2.6 Pro default 로 리팩터 완료.
  - 정밀/세세한 motion (얼굴 micro / 손가락 / 머리카락) 실패 시: `python scripts/smoke/phase091_stage2_to_4.py --live --use-veo` 로 Veo 3.1 Fast fallback 수동 실행 ($0.04 + $0.50 = $0.54, VEO_API_KEY 또는 FAL_KEY 필요).
  - 품질 만족 시 Phase 10 진입 OK. 불만족 시 Template B/C 시도 또는 Veo fallback 검증 후 Phase 10 batch window 에서 threshold 조정.
  - 기존 Runway 버전 clip.mp4 는 덮어쓰기 됨 — 필요 시 사전에 `clip.mp4` 를 `clip_runway_gen3a.mp4` 로 rename 권장.

### 2. TTS Primary (Typecast) + Fallback (ElevenLabs) voice 검증 (REQ-091-06) — 세션 #26 재정의 (2-a + 2-b)

**재정의 배경 (세션 #26 2026-04-20)**: 대표님 확인 "현재 운영중인 주 체널은 타입캐스트다". 원 UAT #2 가 ElevenLabs 만 타겟으로 잡혀 있어 **주 채널 Typecast 검증 누락**. 세션 #26 에서 Typecast primary + ElevenLabs fallback 2단계 검증으로 재정의. Memory: `project_tts_stack_typecast`.

**Import 자산 (세션 #26 포팅)**:
- `config/voice-presets.json` (611 lines) — 11 채널 × Typecast voice_id + ElevenLabs voice_id + emotion 매핑
- `config/channels.yaml` (693 lines) — 채널 metadata + TTS emotion_map

#### 2-a. Typecast primary voice resolution (주 채널 검증)

expected: `TYPECAST_API_KEY` 유효 + `config/voice-presets.json` 의 Typecast voice_name (예: Morgan, Changsu, Sojin 등) 이 Typecast 계정에서 `client.voices_v2()` 로 resolve 됨.

procedure:
```bash
cd C:\Users\PC\Desktop\naberal_group\studios\shorts
# 샘플 1개 채널 (incidents=Morgan) voice resolution 확인
python -c "
import os, json
from scripts.orchestrator.api.typecast import TypecastAdapter
adapter = TypecastAdapter()
with open('config/voice-presets.json') as f:
    presets = json.load(f)
morgan_name = presets['incidents']['typecast']['voice_name']  # 'Morgan'
print(f'Resolving Typecast voice: {morgan_name}')
# adapter 내부 _typecast_voice_cache 에 voice_id 로딩되는지 확인
"
```
result: [pending — 대표님 계정 상태 의존]
notes:
- 주 채널 Typecast 가 실패하면 Circuit Breaker open → ElevenLabs fallback (2-b 검증 필수).
- Fish Audio Tier 1 은 dead code 로 확증됨 (모든 `reference_id = "PENDING_VOICE_SELECTION"`, memory `reference_shorts_naberal_voice_setup`). shorts_studio 는 3-tier 로 단순화 권장 (D091-DEF-02 추가).

#### 2-b. ElevenLabs fallback Korean voice (폴백 경로 검증)

expected: 대표님 ElevenLabs 계정에 `labels.language == "ko"` 또는 한국어 voice 1개 이상 존재하여 `discover_korean_default_voice()` 가 voice_id 반환 → 또는 `.env` 에 `ELEVENLABS_DEFAULT_VOICE_ID=xxx` 수동 설정

procedure:
```bash
# 옵션 A: API 조회
python -c "from scripts.orchestrator.voice_discovery import discover_korean_default_voice; print(discover_korean_default_voice())"
# 옵션 B: 계정 voices 수동 확인 → ELEVENLABS_DEFAULT_VOICE_ID .env 추가
```
result: [pending — 대표님 계정 상태 의존]
notes:
- `discover_korean_default_voice` 는 `GET /v1/voices` + `labels.language="ko"` 필터.
- 한국어 voice 미등록 시 `VoiceDiscoveryError` Korean ("대표님 ElevenLabs 계정에 한국어 voice 없음...").
- shorts_naberal voice-presets.json 기준 한국 채널 ElevenLabs 는 모두 `PLACEHOLDER_PENDING` 상태 — 실제 fallback 호출 시 failure path 진입 가능. Typecast primary 가 안정적이면 실 production 에서 거의 도달 안 함.
- voice_discovery.py 를 Typecast 도 지원하도록 확장하는 건 Phase 10 batch window (D091-DEF-02 #8 신규).

## Summary

total: 2
passed: 0
issues: 0
pending: 2
skipped: 0
blocked: 0

## Gaps

(대표님 귀환 후 실행 결과 실패 시 D091-UAT-XX-fail 엔트리 append. Phase 10 첫 batch patch window 에서 일괄 해소.)

## 대표님 승인 경로

- 2 항목 모두 `result: passed` → `09.1-VERIFICATION.md status: passed` 재-flip 가능
- 어느 하나 `failed` → 작은 decimal phase (예: 9.2 prompt/voice tuning) 또는 Phase 10 첫 batch 이관
- `approved` 선언 시 Phase 10 Sustained Operations 진입 허용

## 원 UAT #1 제거 이력

**(제거됨) ANTHROPIC_API_KEY 실 Claude SDK 호출** — 2026-04-20 세션 #24 architecture fix 로 `anthropic` SDK 경로 자체를 삭제하고 Claude Code CLI subprocess 로 교체. Claude Code Max 구독 인증이 `claude` CLI 를 통해 자동 적용되므로 **API key 추가 불필요**. HUMAN-UAT 를 3건 → 2건으로 축소.

---

*Phase 9.1 구조적 완료 2026-04-20: 8/8 plans + 7/7 SC automated PASS + 15/15 decisions 반영 + 42/42 Phase 9.1 isolated tests + $0.29 실 smoke MP4 생성 + architecture fix (Claude Max 요금제 정합). 본 UAT 는 "Phase 10 진입 전 마지막 인간 확인" — Phase 9 HUMAN-UAT 와 동일 패턴.*
