---
status: partial
phase: 09.1-production-engine-wiring
source: [09.1-VERIFICATION.md, 09.1-VALIDATION.md §Manual-Only Verifications]
started: 2026-04-20T00:00:00Z
updated: 2026-04-20T00:00:00Z
---

## Current Test

[대표님 외출 복귀 후 수동 검증 3건 대기 (YOLO 세션 #24 자동 완결)]

## Tests

### 1. ANTHROPIC_API_KEY 실 Claude SDK 호출 (REQ-091-01)
expected: `.env` 에 `ANTHROPIC_API_KEY=sk-ant-...` 추가 → `ClaudeAgentProducerInvoker().invoke(...)` 또는 간단 ping script 실행 시 `anthropic.Anthropic` client 가 auth 통과 → producer system prompt 로 JSON 응답 수신
procedure:
```bash
# 1. .env 편집: ANTHROPIC_API_KEY=sk-ant-api03-...
# 2. ping (scripts/smoke/claude_sdk_ping.py 는 미작성 — Phase 10 에서 작성 또는 간단 REPL 테스트)
python -c "from scripts.orchestrator.invokers import ClaudeAgentProducerInvoker; inv=ClaudeAgentProducerInvoker(); print(inv.invoke('trend-collector', 'TREND', {'session_id':'test'}))"
```
result: [pending — 대표님 .env 추가 필수, AI 대행 불가]
notes: 현재 Phase 9.1 code 는 key 미설정 시 graceful Korean ValueError raise (NotImplementedError 아님). mock path (MagicMock 주입) 는 Wave 2 test 통과로 검증됨. 실 API 호출 검증은 대표님 key 추가 후 1회면 충분.

### 2. Live smoke clip.mp4 품질 평가 (REQ-091-07, SC4)
expected: `output/phase091_smoke/clip.mp4` (1.87 MB, 5초, Runway Gen-3a Turbo) 재생 → 다음 3항목 주관 평가:
  a) 캐릭터 consistency — `output/phase091_smoke/scene.png` (Nano Banana 생성) 과 clip.mp4 의 인물이 동일인물로 인식되는가
  b) 복합 action 표현 — "커피잔 들어 입으로 가져가기 + 미소 + 머리카락 바람" 이 5초 내에 드러나는가
  c) 세션 #24 초반 channel_art 직접 테스트 결과 (kling_20260420_101904.mp4 + runway_20260420_102114.mp4) 와 비교한 stage chain 품질
procedure: `explorer C:\Users\PC\Desktop\naberal_group\studios\shorts\output\phase091_smoke\` → `clip.mp4` 재생 + `scene.png` 동시 open 비교
result: [pending — 대표님 주관 평가 필수]
notes: 비용 $0.29, wall-time 49초, Runway task ID `ac2823c6-71df-48fd-a6c7-59d016616899`. smoke CLI `python scripts/smoke/phase091_stage2_to_4.py --live` 재실행 시 새 ID + 신규 $0.29. 품질 만족 시 Phase 10 진입 OK, 불만족 시 Nano Banana prompt 또는 Runway prompt tuning 후속 phase.

### 3. ElevenLabs Korean voice 계정 상태 (REQ-091-06)
expected: 대표님 ElevenLabs 계정에 `labels.language == "ko"` 또는 한국어 voice 1개 이상 존재하여 `discover_korean_default_voice()` 가 voice_id 반환 → 또는 `.env` 에 `ELEVENLABS_DEFAULT_VOICE_ID=xxx` 수동 설정
procedure:
```bash
# 옵션 A: API 조회
python -c "from scripts.orchestrator.voice_discovery import discover_korean_default_voice; print(discover_korean_default_voice())"
# 옵션 B: 계정 voices 수동 확인 → ELEVENLABS_DEFAULT_VOICE_ID .env 추가
```
result: [pending — 대표님 계정 상태 의존]
notes: discover_korean_default_voice 는 `GET /v1/voices` + labels.language="ko" 필터. 한국어 voice 미등록 시 `VoiceDiscoveryError` Korean ("대표님 ElevenLabs 계정에 한국어 voice 없음..."). 해결: (1) ElevenLabs 계정에서 한국어 voice add, (2) 또는 환경변수로 수동 지정.

## Summary

total: 3
passed: 0
issues: 0
pending: 3
skipped: 0
blocked: 0

## Gaps

(대표님 귀환 후 실행 결과 실패 시 D091-UAT-XX-fail 엔트리 append. Phase 10 첫 batch patch window 에서 일괄 해소.)

## 대표님 승인 경로

- 3 항목 모두 `result: passed` → `09.1-VERIFICATION.md status: passed` 재-flip 가능
- 어느 하나 `failed` → 작은 decimal phase (예: 9.2 prompt/voice tuning) 또는 Phase 10 첫 batch 이관
- `approved` 선언 시 Phase 10 Sustained Operations 진입 허용 (ANTHROPIC_API_KEY 는 Phase 10 첫 실 사용 시 필수, 그 전까지는 mock 모드)

---

*Phase 9.1 구조적 완료 2026-04-20: 8/8 plans + 7/7 SC automated PASS + 15/15 decisions 반영 + 41/41 Phase 9.1 isolated tests + $0.29 실 smoke MP4 생성. 본 UAT 는 "Phase 10 진입 전 마지막 인간 확인" — Phase 9 HUMAN-UAT 와 동일 패턴.*
