# Phase 9.1: Production Engine Wiring - Discussion Log

> **Audit trail only.** Do not use as input to planning/research/execution agents.

**Date:** 2026-04-20 (세션 #24)
**Mode:** `--auto` (YOLO 6세션 연속, 대표님 외출 중 자동 처리 지시)
**Areas discussed:** Agent SDK wiring / Nano Banana adapter / 캐릭터 레지스트리 / Ken-Burns 로컬 / VALID_RATIOS 매핑 / ElevenLabs voice_id / 통합 smoke test

---

## Area 1: producer_invoker / supervisor_invoker wiring 전략

| Option | Description | Selected |
|--------|-------------|----------|
| Claude Agent SDK (Recommended) | `anthropic` python package 직접 호출, production 환경 독립, 토큰 직접 제어 | ✓ |
| Claude Code CLI subprocess | `claude` CLI 를 subprocess 로 호출, CC runtime 의존 | |
| Task tool wrapper | shorts_pipeline.py 내부에서 Task API 래핑, nesting 깊어짐 | |

**Notes:** D-01 → D-03 로 구체화. Anthropic SDK + `model.messages.create` + system prompt from `.claude/agents/producers/{name}/AGENT.md` + JSON response enforce.

---

## Area 2: Nano Banana Pro adapter 설계

| Option | Description | Selected |
|--------|-------------|----------|
| google-genai SDK (Recommended) | shorts_naberal 검증된 방식, `client.models.generate_content` | ✓ |
| REST API 직접 | 의존성 최소이나 auth 자체 구현 부담 | |
| Vertex AI 우회 | Google Cloud 별도 설정 부담 | |

**Notes:** D-04 → D-06. shorts_naberal `_nanobanana_from_script_v3.py` 의 `build_hyperreal_prompt` + safety fallback 로직 포팅.

---

## Area 3: 캐릭터 레지스트리 구조

| Option | Description | Selected |
|--------|-------------|----------|
| 파일시스템 + JSON manifest (Recommended) | shorts_naberal 규약 상속 + 경량 metadata | ✓ |
| SQLite DB | overkill, 단일 사용자 단일 채널 | |
| JSON only | metadata 확장성 부족 | |

**Notes:** D-07 → D-09. `assets/characters/` + `registry.json` + `CharacterRegistry` util. smoke test 용 `channel_profile_ref.png` 1개만 9.1 에서 시드.

---

## Area 4: Ken-Burns FFmpeg 로컬 구현

| Option | Description | Selected |
|--------|-------------|----------|
| subprocess ffmpeg (Recommended) | 의존성 최소, shorts_naberal 방식, Python stdlib only | ✓ |
| ffmpeg-python binding | 추가 package 의존 | |
| Shotstack 유지 | 드리프트 지속, 클라우드 비용 | |

**Notes:** D-10 → D-11. `scripts/orchestrator/api/ken_burns.py` 신설, Shotstack.create_ken_burns_clip() deprecated 마킹만 (완전 제거는 Phase 10 이후).

---

## Area 5: RunwayI2VAdapter VALID_RATIOS_BY_MODEL

| Option | Description | Selected |
|--------|-------------|----------|
| dict 상수 + auto-select (Recommended) | 단순, model 교체 시 ratio 자동 조정 | ✓ |
| Adapter subclass per model | 코드 중복 많음 | |
| 하드코드 유지 | 드리프트 지속 | |

**Notes:** D-12. 2026-04-20 실측된 `gen3a_turbo: ["16:9","9:16","768:1280","1280:768"]` + `gen4.5: ["720:1280"]` 시드.

---

## Area 6: ElevenLabs voice_id 교체

| Option | Description | Selected |
|--------|-------------|----------|
| shorts_naberal grep → 실 voice_id (Recommended) | 대표님 검증된 voice 재사용 | ✓ |
| ElevenLabs API `GET /v1/voices` 첫 유효 voice | shorts_naberal 미발견 시 fallback | ✓ (secondary) |
| 새 voice 등록 | Phase 10 이후 대표님 직접 | |

**Notes:** D-13. 1순위 grep, 2순위 API query.

---

## Area 7: 통합 smoke test 범위

| Option | Description | Selected |
|--------|-------------|----------|
| Stage 2+4 (Nano Banana → Runway) (Recommended) | 4-stage chain 의 핵심 연쇄, 대표님 품질 체감 가능 | ✓ |
| 전체 orchestrator.run() | 13-gate 완주, Phase 10 소관, 9.1 범위 초과 | |
| Stage 4 단독 | 이미 세션 #24 에서 검증 | |

**Notes:** D-14 → D-15. 비용 상한 $1.00. smoke test 는 "대표님이 실 영상 품질 보는 것" 이라는 원래 세션 #24 의도 충족.

---

## Claude's Discretion

- Claude Agent SDK model 선택 (producer=Sonnet, supervisor=Opus)
- `CharacterRegistry` pydantic schema 세부
- Ken-Burns pan/zoom 속도 / zoom factor
- smoke test Nano Banana prompt 구체 문구
- 테스트 디렉토리 구조

## Deferred Ideas

- Remotion v4 composition integration (Phase 10 이후)
- Shotstack 완전 제거 (Phase 10 이후)
- NLM 대본 자동 수신 (Phase 10 이후)
- Agent SDK 고급 기능 (Tool use, batch)
- producer/supervisor prompt tuning (Phase 10 batch window)
- 캐릭터 레지스트리 variation selection algorithm (Phase 10 이후)
- 다중 채널 지원
- Bedrock/Vertex 백엔드 전환

---

*Mode note: `--auto` 로 모든 옵션에서 Recommended 자동 선택. 대표님 외출 복귀 시 09.1-CONTEXT.md 재-discuss 요청 가능.*
