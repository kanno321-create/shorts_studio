---
phase: 09.1-production-engine-wiring
plan: "06"
subsystem: smoke-test-harness
tags: [smoke, stage2-to-4, nano-banana, runway-gen3a-turbo, cost-cap, hook-hygiene, req-091-07, wave-3, live-api-verified]
requires:
  - .planning/phases/09.1-production-engine-wiring/09.1-00-SUMMARY.md  # foundation (test stubs + character seed)
  - .planning/phases/09.1-production-engine-wiring/09.1-01-SUMMARY.md  # NanoBananaAdapter
  - .planning/phases/09.1-production-engine-wiring/09.1-02-SUMMARY.md  # CharacterRegistry
  - .planning/phases/09.1-production-engine-wiring/09.1-04-SUMMARY.md  # Runway VALID_RATIOS_BY_MODEL
  - .planning/phases/09.1-production-engine-wiring/09.1-05-SUMMARY.md  # invokers + pipeline wiring
provides:
  - scripts/smoke/phase091_stage2_to_4.py CLI (argparse --dry-run default / --live)
  - Stage 2->4 end-to-end live verification (CharacterRegistry -> NanoBanana -> Runway)
  - $1.00 cost cap circuit-breaker pattern (_check_cost_cap before each API call)
  - output/phase091_smoke/{scene.png,clip.mp4,manifest.json} artefact layout
  - test_hook_hygiene.py RED -> GREEN flip (all 6 Wave 1-3 target files clean)
  - .planning/phases/09.1-production-engine-wiring/deferred-items.md (Runway ratio drift)
affects:
  - tests/phase091/test_hook_hygiene.py (no source changes; flipped to GREEN by virtue of phase091_stage2_to_4.py existing + all 6 targets being hygienic)
tech-stack:
  added:
    - (none — reuses Wave 1 NanoBananaAdapter + RunwayI2VAdapter + CharacterRegistry)
  patterns:
    - "argparse mutually-exclusive --dry-run / --live with --dry-run as implicit default (no flag = dry-run)"
    - "lazy adapter import inside _run_live: google-genai + runwayml imports only under --live, so --dry-run works on systems without the SDKs installed"
    - "cost-cap circuit breaker: _check_cost_cap(total_usd) raises Korean RuntimeError BEFORE the next API call (spends $0.04 + $0.25 = $0.29, aborts before more on overshoot)"
    - "Windows cp949 stdout.reconfigure guard with explicit stderr log on failure (Hook 3종-compatible, multi-statement except body — not silent except)"
    - "manifest.json captures: mode, timestamp, anchor_path, scene_path, clip_path, scene_prompt, motion_prompt, per-stage cost, total_usd, cost_cap_usd, wall_time_seconds"
    - "dry-run path uses shutil.copy(anchor) -> scene_placeholder.png + zero-byte clip_placeholder.mp4 (wiring proof without cost)"
key-files:
  created:
    - scripts/smoke/phase091_stage2_to_4.py (282 lines)
    - .planning/phases/09.1-production-engine-wiring/deferred-items.md
    - .planning/phases/09.1-production-engine-wiring/09.1-06-SUMMARY.md
  modified:
    - (none — test_hook_hygiene.py source untouched; flipped GREEN via target-file existence)
decisions:
  - "Executor-initiated --live run (not deferred to 대표님 manual path): the orchestrator prompt explicitly lists '--live exits 0, scene.jpg + video.mp4 생성' as a hard success criterion; 대표님 external context 'video quality 보고 싶다' per 세션 #24; both API keys present in .env; cost envelope $0.29 < $1.00 cap; YOLO mode per feedback_yolo_no_questions."
  - "Rule 1 auto-fix at Stage 4: Runway API rejected adapter-advertised '9:16' ratio with HTTP 400 'must be one of: 768:1280, 1280:768'. Inline fix hardcodes ratio='768:1280' in the smoke CLI. The deeper VALID_RATIOS_BY_MODEL drift (scripts/orchestrator/api/runway_i2v.py §50-53 lists '16:9' and '9:16' for gen3a_turbo) is logged in deferred-items.md for Plan 07 / Phase 10 because touching the adapter constant ripples into tests/phase091/test_runway_ratios.py matchers — out of scope for Plan 06."
  - "test_hook_hygiene.py source NOT modified: the Wave 0 RED stub was written to this exact shape already (3 tests scanning skip_gates / TODO(next-session) / ast silent except:pass across 6 target files). The stub flips GREEN automatically once phase091_stage2_to_4.py exists AND is hygienic — no Edit needed on the test file. Plan directive was 'if the stub matches this shape, only verify it passes'."
  - "Windows cp949 console encoding: sys.stdout.reconfigure('utf-8') guard replicates the Phase 8 smoke_test.py pattern. Without it, the Korean 완료 + em-dash combination in the summary print() crashes with UnicodeEncodeError on default Windows consoles."
metrics:
  duration: "~40 min (12:30 -> 13:10 KST, including one live retry after ratio 400)"
  tasks: 2
  commits: 2  # feat + fix (SUMMARY + state in final commit)
  files_created: 3
  files_modified: 0  # test_hook_hygiene.py: no source change required
  lines_added_smoke: 282
  tests_green_hook_hygiene: "3/3"
  phase091_full_regression: "41/41"
  dry_run_wall_time_s: "<0.1"
  live_run_wall_time_s: 49.2
  live_run_total_cost_usd: 0.29
  live_run_cost_cap_usd: 1.00
  live_run_cost_headroom_usd: 0.71
completed: "2026-04-20T13:10:00+09:00"
---

# Phase 9.1 Plan 06: Stage 2→4 Smoke Test Summary

**One-liner:** End-to-end live-verified Nano Banana Pro scene generation + Runway Gen-3a Turbo 5-second I2V clip via a Hook 3종-clean CLI with a $1.00 cost-cap circuit breaker — REAL API run succeeded, $0.29 actual spend, 49.2 s wall time.

---

## Deliverables

### scripts/smoke/phase091_stage2_to_4.py (282 lines, new)

- **argparse mutex** `--dry-run` (implicit default) vs `--live` — passing both exits 2.
- **--dry-run path** (SDK-free):
  1. `CharacterRegistry().load().get_reference_path('channel_profile')` → anchor Path
  2. `shutil.copy(anchor, output_dir/'scene_placeholder.png')`
  3. zero-byte `output_dir/'clip_placeholder.mp4'`
  4. `manifest.json` with `mode=dry-run`, `total_usd=0.0`
- **--live path** (cost-capped):
  1. `_require_env(('GOOGLE_API_KEY', 'RUNWAY_API_KEY'))` — Korean ValueError on miss
  2. Lazy imports of `NanoBananaAdapter` + `RunwayI2VAdapter`
  3. Stage 2: `nanobanana.generate_scene(SCENE_PROMPT, output_path=scene.png)` ($0.04)
  4. `_check_cost_cap(total_usd)` — Korean RuntimeError if > $1.00
  5. Size sanity: scene.png ≥ 1 KB or Korean RuntimeError
  6. Stage 4: `runway.image_to_video(MOTION_PROMPT, anchor_frame=scene.png, duration=5)` ($0.25)
  7. `_check_cost_cap(total_usd)` (again, guards any future stage insertion)
  8. Size sanity: clip.mp4 ≥ 100 KB or Korean RuntimeError
  9. `shutil.move(raw_clip → output_dir/'clip.mp4')` — consolidate under smoke dir
  10. `manifest.json` with full cost breakdown + wall time

### tests/phase091/test_hook_hygiene.py (no source change; flipped GREEN)

The Wave 0 RED stub was already written to the final shape per the plan directive. With all 6 Wave 1-3 target files now existing AND hygienic, the three assertions (skip_gates / TODO(next-session) / AST silent except:pass) flip GREEN on their own. No Edit needed.

Target files scanned (0 hits each):

| File | Plan | Result |
| ---- | ---- | ------ |
| scripts/orchestrator/invokers.py | 05 | Clean |
| scripts/orchestrator/api/nanobanana.py | 01 | Clean |
| scripts/orchestrator/api/ken_burns.py | 03 | Clean |
| scripts/orchestrator/character_registry.py | 02 | Clean |
| scripts/orchestrator/voice_discovery.py | 04 | Clean |
| scripts/smoke/phase091_stage2_to_4.py | 06 | Clean |

---

## Live API Run Result (대표님 재생 가능)

```
Stage 2 — Nano Banana Pro generateContent ... HTTP 200
  scene.png = 657.6 KB
  cost = $0.04

Stage 4 — Runway Gen-3a Turbo image_to_video ... HTTP 200
  task ac2823c6-71df-48fd-a6c7-59d016616899
  3 GET polls to task status → completed
  MP4 download from CloudFront (144b0613-5d57-...).mp4
  clip.mp4 = 1831.7 KB (≈ 1.87 MB)
  cost = $0.25

TOTAL: $0.29 / cap $1.00 / headroom $0.71
WALL: 49.2 s
EXIT: 0
```

**Artefacts (대표님 재생 경로):**

| Artefact | Path | Size |
| -------- | ---- | ---- |
| Scene anchor (Nano Banana output) | `output/phase091_smoke/scene.png` | 673 KB |
| Video clip (Runway output) | `output/phase091_smoke/clip.mp4` | 1,875,628 bytes |
| Manifest | `output/phase091_smoke/manifest.json` | 804 bytes |

### manifest.json (live run)

```json
{
  "mode": "live",
  "timestamp": 1776656062,
  "anchor_path": "assets\\characters\\channel_profile_ref.png",
  "scene_path": "output\\phase091_smoke\\scene.png",
  "clip_path": "output\\phase091_smoke\\clip.mp4",
  "scene_prompt": "Reference the character in the attached image. Generate a hyperrealistic 9:16 vertical photograph of the SAME character in a modern Korean studio: warm natural lighting from left, holding a white ceramic coffee cup, warm smile looking slightly off-camera, shallow depth of field. Shot on ARRI Alexa Mini, film grain. No AI-generated look.",
  "motion_prompt": "character lifts coffee cup toward camera, warm studio lighting",
  "nanobanana_cost_usd": 0.04,
  "runway_cost_usd": 0.25,
  "total_usd": 0.29,
  "cost_cap_usd": 1.0,
  "wall_time_seconds": 49.2
}
```

### 대표님 재생 명령 (shell)

```bash
start output\phase091_smoke\clip.mp4    # Windows — opens default player
# or, to re-run the live smoke:
set -a && source .env && set +a
python scripts/smoke/phase091_stage2_to_4.py --live
```

**Character consistency:** subjective — 대표님 직접 확인 권장. Stage 2 anchor was the shorts_naberal `profile_kr_bright.png` seed. Stage 4 motion (`character lifts coffee cup toward camera, warm studio lighting`) applied to the Stage 2 scene.

---

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Runway gen3a_turbo ratio '9:16' rejected at API**

- **Found during:** Task 1 `--live` run (Stage 4)
- **Issue:** `scripts/orchestrator/api/runway_i2v.py` §50-53 `VALID_RATIOS_BY_MODEL['gen3a_turbo']` advertises `['16:9', '9:16', '768:1280', '1280:768']` but the live I2V endpoint at `api.dev.runwayml.com/v1/image_to_video` returns HTTP 400 `'ratio must be one of: 768:1280, 1280:768.'` for the two string variants.
- **Fix:** Smoke CLI hardcodes `RunwayI2VAdapter(model='gen3a_turbo', ratio='768:1280')` with an inline comment referencing `deferred-items.md`.
- **Files modified:** `scripts/smoke/phase091_stage2_to_4.py` (+5 lines comment + 1-line literal swap)
- **Commit:** `dc82255`
- **Deferred:** `scripts/orchestrator/api/runway_i2v.py` drift logged in `deferred-items.md` for Plan 07 / Phase 10 (touches `tests/phase091/test_runway_ratios.py` matchers — out of Plan 06 scope).

### Authentication Gates

None — both `GOOGLE_API_KEY` and `RUNWAY_API_KEY` were present in `.env` at plan start, and `set -a && source .env && set +a` loaded them into the live-run environment.

---

## Verification Checkpoint

| Criterion | Result |
| --------- | ------ |
| scripts/smoke/phase091_stage2_to_4.py exists | 282 lines |
| `python scripts/smoke/phase091_stage2_to_4.py --dry-run` | exit 0, manifest mode=dry-run, total_usd=0.0 |
| `python scripts/smoke/phase091_stage2_to_4.py --dry-run --live` | exit 2 (argparse mutex) |
| `python scripts/smoke/phase091_stage2_to_4.py --live` | exit 0, scene.png 657.6 KB, clip.mp4 1.87 MB, total $0.29, cap $1.00 |
| `pytest tests/phase091/test_hook_hygiene.py -x -v` | 3/3 GREEN |
| `pytest tests/phase091/ -v` full regression | 41/41 GREEN |
| Cost cap ($1.00) enforced before next call | `_check_cost_cap` called after Stage 2 + Stage 4 (RuntimeError guard) |
| Korean errors at every raise | ValueError (missing key), FileNotFoundError (missing anchor), RuntimeError (overshoot / undersized artefacts) |
| Hook 3종 (skip_gates / TODO(next-session) / silent except:pass) | 0 hits across all 6 target files |

---

## Wave Dependency for Plan 07

Plan 07 (Phase 9.1 Wave 4 gate aggregator) consumes the following SC from this plan:

- **SC4:** live smoke MP4 existence → satisfied (`output/phase091_smoke/clip.mp4` = 1.87 MB, cost $0.29 recorded in manifest)
- **SC (Hook 3종):** test_hook_hygiene.py GREEN → satisfied (3/3 pass)

Plan 07 should also consume `deferred-items.md` to decide whether the Runway ratio drift is closed in 09.1 (adapter constant fix + test_runway_ratios.py matcher update) or deferred to Phase 10.

---

## Next Steps (대표님 action items)

1. **재생 확인:** `output/phase091_smoke/clip.mp4` 를 기본 미디어 플레이어로 열어서 character consistency + 동작 품질 평가.
2. **Runway billing 검증:** Runway 대시보드에서 task `ac2823c6-71df-48fd-a6c7-59d016616899` 의 실 과금이 $0.25 ± 소액 범위인지 확인.
3. **Google AI Studio billing 검증:** nano-banana-pro-preview 실 과금 $0.04 ± 소액 확인.
4. **Plan 07 진행 결정:** Plan 07 phase-gate aggregator 가 smoke MP4 존재 + 비용 기록을 흡수하여 Phase 9.1 최종 gate 를 닫을 수 있음. YOLO 연속 시 자동 진행.

## Self-Check: PASSED

- [x] scripts/smoke/phase091_stage2_to_4.py created (282 lines, verified)
- [x] output/phase091_smoke/scene.png exists (673 KB, gitignored)
- [x] output/phase091_smoke/clip.mp4 exists (1.87 MB, gitignored)
- [x] output/phase091_smoke/manifest.json exists (804 bytes, gitignored)
- [x] deferred-items.md created and tracked
- [x] SUMMARY.md created (this file)
- [x] Commit 17e733a (feat smoke CLI) exists in git log
- [x] Commit dc82255 (fix ratio + deferred-items) exists in git log
- [x] 41/41 phase091 tests GREEN after all changes
