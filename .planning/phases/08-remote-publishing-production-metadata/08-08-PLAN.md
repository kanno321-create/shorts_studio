---
plan: 08-08
phase: 8
wave: 7
depends_on: [08-01, 08-02, 08-03, 08-04, 08-05, 08-06, 08-07]
files_modified:
  - .planning/phases/08-remote-publishing-production-metadata/08-TRACEABILITY.md
  - .planning/phases/08-remote-publishing-production-metadata/08-VALIDATION.md
  - tests/phase08/test_traceability_matrix.py
files_read_first:
  - .planning/phases/08-remote-publishing-production-metadata/08-CONTEXT.md
  - .planning/phases/08-remote-publishing-production-metadata/08-RESEARCH.md
  - .planning/phases/08-remote-publishing-production-metadata/08-VALIDATION.md
  - .planning/phases/07-integration-test/07-TRACEABILITY.md
  - .planning/phases/07-integration-test/07-VALIDATION.md
  - .planning/phases/06-wiki-notebooklm-integration-failures-reservoir/06-TRACEABILITY.md
  - tests/phase07/test_traceability_matrix.py
autonomous: true
requirements: [PUB-01, PUB-02, PUB-03, PUB-04, PUB-05, REMOTE-01, REMOTE-02, REMOTE-03]
tasks_addressed: [8-08-01, 8-08-02]
success_criteria: [SC1, SC2, SC3, SC4, SC5, SC6]
estimated_commits: 2
parallel_boundary: sequential (phase final gate)

must_haves:
  truths:
    - "08-TRACEABILITY.md exists with 8-REQ (PUB-01..05 + REMOTE-01..03) x source x test x SC matrix mirroring 07-TRACEABILITY.md format"
    - "08-TRACEABILITY.md documents 3 anchor tests: ANCHOR A (AI disclosure AST) + ANCHOR B (endScreen grep 0) + ANCHOR C (selenium grep 0)"
    - "08-TRACEABILITY.md documents 2 field-name corrections applied: Pitfall 6 syntheticMedia → containsSyntheticMedia + Pitfall 7 end_screen_subscribe_cta dropped"
    - "08-VALIDATION.md frontmatter flipped: status=complete + nyquist_compliant=true + wave_0_complete=true + completed=<date>"
    - "All 24 rows in 08-VALIDATION.md Per-Task Verification Map transitioned from ⬜ pending to ✅ green"
    - "Validation Sign-Off all 6 checkboxes [x]"
    - "test_traceability_matrix.py asserts all 8 REQ IDs + all 6 SC references present in 08-TRACEABILITY.md"
  artifacts:
    - path: ".planning/phases/08-remote-publishing-production-metadata/08-TRACEABILITY.md"
      provides: "8-REQ × source × test × SC matrix (Phase 7 format)"
      min_lines: 80
    - path: ".planning/phases/08-remote-publishing-production-metadata/08-VALIDATION.md"
      provides: "Frontmatter flipped to complete + 24 rows green + Completion Summary appended"
      contains: "nyquist_compliant: true"
    - path: "tests/phase08/test_traceability_matrix.py"
      provides: "Automated REQ/SC coverage guard"
      min_lines: 70
  key_links:
    - from: ".planning/phases/08-remote-publishing-production-metadata/08-TRACEABILITY.md"
      to: "tests/phase08/test_traceability_matrix.py"
      via: "Coverage matrix file referenced by test for REQ ID + SC presence"
      pattern: "08-TRACEABILITY\\.md"
    - from: ".planning/phases/08-remote-publishing-production-metadata/08-VALIDATION.md"
      to: "scripts/validate/phase08_acceptance.py"
      via: "All 6 SC proven green before frontmatter flip"
      pattern: "ALL_PASS"
---

<objective>
Wave 7 PHASE GATE — Close Phase 8. Author `08-TRACEABILITY.md` (8-REQ × source × test × SC matrix mirroring Phase 7 Plan 07-08 format, including 3 anchor tests + 2 Pitfall 6/7 corrections), ship `test_traceability_matrix.py` asserting REQ/SC coverage automation, then flip `08-VALIDATION.md` frontmatter to `status=complete / nyquist_compliant=true / wave_0_complete=true / completed=<ISO date>` with all 24 rows green + Completion Summary appended. This plan ships documentation + verification artifacts only — no new production code.

Purpose: Phase 8 officially shippable. `/gsd:verify-work 08` runs against flipped VALIDATION.md. Direct analogue of Phase 7 Plan 07-08.

Output: 1 traceability matrix + 1 orphan-guard test + 1 VALIDATION flip + 1 ROADMAP.md + REQUIREMENTS.md flip (in commit message scope).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-remote-publishing-production-metadata/08-CONTEXT.md
@.planning/phases/08-remote-publishing-production-metadata/08-RESEARCH.md
@.planning/phases/08-remote-publishing-production-metadata/08-VALIDATION.md
@.planning/phases/07-integration-test/07-TRACEABILITY.md
@.planning/phases/07-integration-test/07-VALIDATION.md
@tests/phase07/test_traceability_matrix.py

<interfaces>
## 08-TRACEABILITY.md structure (mirror 07-TRACEABILITY.md)

Sections in order:
1. Header + metadata (phase, gathered date, requirements count, author)
2. REQ × Source × Test × SC matrix table (8 rows)
3. SC Aggregation table (6 rows referencing requirements)
4. Plan → REQ → Commit audit trail (8 plans: 08-01..08-08)
5. Research Corrections applied section (Pitfall 6 + Pitfall 7 + 3 anchor tests documented)
6. Phase 8 shipped signature line

Example row format:
| REQ-ID | Source File(s) | Test File(s) | SC |
|--------|----------------|--------------|----|
| PUB-01 | scripts/publisher/ai_disclosure.py (build_status_block hardcoded True) | tests/phase08/test_ai_disclosure_anchor.py (ANCHOR A: AST + grep 0 False hits) | SC3 |
| PUB-02 | scripts/publisher/youtube_uploader.py + scripts/publisher/oauth.py | tests/phase08/test_uploader_mocked_e2e.py + test_oauth_installed_flow.py + test_no_selenium_anchor.py (ANCHOR C) | SC3, SC6 |
| PUB-03 | scripts/publisher/publish_lock.py + scripts/publisher/kst_window.py | tests/phase08/test_publish_lock_48h.py + test_kst_window_weekday.py + test_kst_window_weekend.py | SC4 |
| PUB-04 | scripts/publisher/production_metadata.py | tests/phase08/test_production_metadata_schema.py + test_metadata_html_comment.py | SC5 |
| PUB-05 | scripts/publisher/youtube_uploader.py (publish funnel block) | tests/phase08/test_pinned_comment.py + test_endscreen_nonexistent_anchor.py (ANCHOR B) | SC6 |
| REMOTE-01 | scripts/publisher/github_remote.py (create_private_repo) | tests/phase08/test_github_remote_create.py | SC1 |
| REMOTE-02 | scripts/publisher/github_remote.py (push_to_remote) | tests/phase08/test_github_push_main.py | SC1 |
| REMOTE-03 | scripts/publisher/github_remote.py (add_harness_submodule) | tests/phase08/test_submodule_add.py | SC2 |

## SC Aggregation table

| SC | Description | Requirements | Status |
|----|-------------|--------------|--------|
| SC1 | GitHub mirror + main pushed | REMOTE-01, REMOTE-02 | PASS (08-07 SC1 green) |
| SC2 | naberal_harness submodule | REMOTE-03 | PASS (08-07 SC2 green) |
| SC3 | AI disclosure + Selenium zero | PUB-01, PUB-02 | PASS (08-07 SC3 green) |
| SC4 | 48h+ lock + KST window | PUB-03 | PASS (08-07 SC4 green) |
| SC5 | production_metadata 4-field | PUB-04 | PASS (08-07 SC5 green) |
| SC6 | Pinned comment + funnel | PUB-05 | PASS (08-07 SC6 green) |

## Research Corrections applied section (Required body content)

- **Pitfall 6 correction**: Phase 4 publisher AGENT.md uses custom key `ai_disclosure.syntheticMedia=True`; the canonical YouTube Data API v3 field is `status.containsSyntheticMedia` (2024-10-30). Applied at `scripts/publisher/ai_disclosure.py:build_status_block` + translated at `scripts/publisher/youtube_uploader.py:build_insert_body`. ANCHOR A test locks this against regression.
- **Pitfall 7 correction**: Phase 4 publisher AGENT.md references `end_screen_subscribe_cta: true` + CONTEXT D-09 mentioned `captions.insert`. end-screen is NOT supported by Data API v3 (YouTube Studio UI only). Replaced with description CTA + pinned comment. ANCHOR B test locks this.
- **ANCHOR C**: Redundant Hook-layer enforcement — zero `selenium`/`webdriver`/`playwright` imports in `scripts/publisher/**/*.py`. grep + AST verified.
</interfaces>
</context>

<tasks>

<task id="8-08-01">
  <action>
Create `.planning/phases/08-remote-publishing-production-metadata/08-TRACEABILITY.md` (~120 lines). Structure EXACTLY following Phase 7 Plan 07-08 canonical format. Required content:

**Header:**
```markdown
---
phase: 8
slug: remote-publishing-production-metadata
gathered: <EXECUTION_DATE>   # use datetime.utcnow().strftime('%Y-%m-%d')
requirements_count: 8
success_criteria_count: 6
---

# Phase 8 — Traceability Matrix

**Phase:** Remote + Publishing + Production Metadata
**Plans:** 8 (08-01 through 08-08)
**Requirements:** PUB-01..05 + REMOTE-01..03 (8 total)
**Success Criteria:** SC1..SC6 (6 total)
**Anchors:** 3 permanent (AI disclosure AST + endScreen grep 0 + selenium grep 0)
**Research Corrections:** 2 applied (Pitfall 6 + Pitfall 7)

## REQ × Source × Test × SC Matrix
```

Then include the 8-row requirement table from `<interfaces>` above (column alignment exactly as shown).

Then the SC Aggregation table (6 rows).

Then the Plan → REQ → Commit audit trail:
```markdown
## Plan → REQ → Commit Audit Trail

| Plan | Wave | Objective | Requirements | Commit |
|------|------|-----------|--------------|--------|
| 08-01 | 0 | Wave 0 FOUNDATION — tests/phase08/ scaffold + mocks + publisher namespace | (infra — unblocks all) | TBD |
| 08-02 | 1 | REMOTE — github_remote.py + _git_askpass.sh + .gitignore | REMOTE-01, REMOTE-02, REMOTE-03 | TBD |
| 08-03 | 2 | OAUTH — oauth.py InstalledAppFlow + refresh roundtrip | PUB-02 | TBD |
| 08-04 | 3 | LOCK+WINDOW+DISCLOSURE — publish_lock + kst_window + ai_disclosure + ANCHOR A | PUB-01, PUB-03 | TBD |
| 08-05 | 4 | METADATA+FUNNEL+UPLOADER — production_metadata + youtube_uploader + ANCHOR B + ANCHOR C | PUB-02, PUB-04, PUB-05 | TBD |
| 08-06 | 5 | SMOKE TEST — unlisted upload + 30s wait + delete cleanup (USER APPROVAL) | PUB-01..05 | TBD |
| 08-07 | 6 | E2E + REGRESSION — phase08_acceptance + 986 regression + full chain mocked | ALL | TBD |
| 08-08 | 7 | PHASE GATE — TRACEABILITY + VALIDATION flip | ALL | TBD |
```

Then the Research Corrections section body from `<interfaces>` above (3 bullet points).

Then signature line:
```markdown
---

**Phase 8 shipped:** <EXECUTION_DATE> — 8/8 REQs complete · 6/6 SC PASS · 3 anchors permanent · 986 + Phase 8 regression green.
```

Create `tests/phase08/test_traceability_matrix.py` (~90 lines):

```python
"""Wave 7 — orphan guard asserting 08-TRACEABILITY.md REQ + SC coverage."""
from __future__ import annotations

from pathlib import Path

import pytest


_TRACE = (Path(__file__).resolve().parents[2]
          / ".planning" / "phases" / "08-remote-publishing-production-metadata"
          / "08-TRACEABILITY.md")


@pytest.fixture(scope="module")
def trace_text() -> str:
    assert _TRACE.exists(), f"{_TRACE} missing"
    return _TRACE.read_text(encoding="utf-8")


@pytest.mark.parametrize("req_id", [
    "PUB-01", "PUB-02", "PUB-03", "PUB-04", "PUB-05",
    "REMOTE-01", "REMOTE-02", "REMOTE-03",
])
def test_each_requirement_appears_in_traceability(trace_text, req_id):
    assert f"| {req_id} " in trace_text or f"| **{req_id}** " in trace_text, (
        f"REQ {req_id} not found as table row header in 08-TRACEABILITY.md"
    )


@pytest.mark.parametrize("sc_id", ["SC1", "SC2", "SC3", "SC4", "SC5", "SC6"])
def test_each_sc_appears_in_traceability(trace_text, sc_id):
    assert sc_id in trace_text, (
        f"SC {sc_id} not referenced anywhere in 08-TRACEABILITY.md"
    )


def test_three_anchors_documented(trace_text):
    assert "ANCHOR A" in trace_text
    assert "ANCHOR B" in trace_text
    assert "ANCHOR C" in trace_text


def test_pitfall_corrections_documented(trace_text):
    assert "Pitfall 6" in trace_text
    assert "Pitfall 7" in trace_text


def test_eight_plan_rows_in_audit_trail(trace_text):
    import re
    rows = re.findall(r"^\| 08-0[1-8] \|", trace_text, flags=re.MULTILINE)
    assert len(rows) == 8, f"Expected 8 plan rows, found {len(rows)}"


def test_correction_keys_containssynthetic_vs_custom(trace_text):
    """Both names must be documented for transparency."""
    assert "containsSyntheticMedia" in trace_text
    assert "syntheticMedia" in trace_text


def test_end_screen_marked_as_unsupported(trace_text):
    assert "end-screen" in trace_text.lower() or "end_screen" in trace_text


def test_no_orphan_plan_numbers(trace_text):
    """No plan 08-09 or 08-10 — Phase 8 is 8 plans exactly."""
    import re
    orphans = re.findall(r"08-(09|10|11|12)", trace_text)
    assert not orphans, f"Unexpected plan number references: {orphans}"
```
  </action>
  <read_first>
    - .planning/phases/07-integration-test/07-TRACEABILITY.md (canonical template)
    - tests/phase07/test_traceability_matrix.py (orphan-guard template)
    - .planning/phases/08-remote-publishing-production-metadata/08-VALIDATION.md (per-task map for SC mapping)
    - All 7 prior plan SUMMARYs (if they exist on disk when Plan 08-08 runs — commit hashes pulled from there)
  </read_first>
  <acceptance_criteria>
    - `test -f .planning/phases/08-remote-publishing-production-metadata/08-TRACEABILITY.md` exits 0
    - `grep -c "PUB-01\|PUB-02\|PUB-03\|PUB-04\|PUB-05\|REMOTE-01\|REMOTE-02\|REMOTE-03" .planning/phases/08-remote-publishing-production-metadata/08-TRACEABILITY.md` outputs `>= 8`
    - `grep -c "SC1\|SC2\|SC3\|SC4\|SC5\|SC6" .planning/phases/08-remote-publishing-production-metadata/08-TRACEABILITY.md` outputs `>= 6`
    - `grep -c "ANCHOR A\|ANCHOR B\|ANCHOR C" .planning/phases/08-remote-publishing-production-metadata/08-TRACEABILITY.md` outputs `>= 3`
    - `grep -c "Pitfall 6\|Pitfall 7" .planning/phases/08-remote-publishing-production-metadata/08-TRACEABILITY.md` outputs `>= 2`
    - `grep -cE "^\| 08-0[1-8] \|" .planning/phases/08-remote-publishing-production-metadata/08-TRACEABILITY.md` outputs `8`
    - `python -m pytest tests/phase08/test_traceability_matrix.py -q --no-cov` exits 0
  </acceptance_criteria>
  <automated>python -m pytest tests/phase08/test_traceability_matrix.py -q --no-cov</automated>
  <task_type>impl</task_type>
</task>

<task id="8-08-02">
  <action>
Flip `.planning/phases/08-remote-publishing-production-metadata/08-VALIDATION.md` frontmatter from:
```yaml
status: draft
nyquist_compliant: false
wave_0_complete: false
```
to:
```yaml
status: complete
nyquist_compliant: true
wave_0_complete: true
completed: <ISO_DATE>   # e.g. 2026-04-19
```

Transition ALL 24 rows in the Per-Task Verification Map table from `⬜ pending` to `✅ green`. File Exists column for all rows: `✅ shipped` (or keep existing if row was green earlier).

Check all 6 boxes in Validation Sign-Off section (flip `- [ ]` → `- [x]`).

Set Approval line: `**Approval:** complete 2026-04-19`.

Append a Completion Summary section at end of file:
```markdown
---

## Completion Summary

**Phase 8 officially shippable:** 2026-04-19

### Success Criteria Status
- [x] SC1: GitHub mirror — REMOTE-01/02 (Plan 08-02)
- [x] SC2: naberal_harness submodule — REMOTE-03 (Plan 08-02)
- [x] SC3: AI disclosure anchor + Selenium zero — PUB-01/02 (Plan 08-04 ANCHOR A + Plan 08-05 ANCHOR C)
- [x] SC4: 48h+ lock + KST window — PUB-03 (Plan 08-04)
- [x] SC5: production_metadata 4-field — PUB-04 (Plan 08-05)
- [x] SC6: Pinned comment + funnel — PUB-05 (Plan 08-05 + ANCHOR B)

### Requirement Status
- [x] PUB-01, PUB-02, PUB-03, PUB-04, PUB-05, REMOTE-01, REMOTE-02, REMOTE-03 (8/8)

### Anchors Permanent
- ANCHOR A — AI disclosure AST + containsSyntheticMedia grep (tests/phase08/test_ai_disclosure_anchor.py)
- ANCHOR B — endScreen + captions.insert + end_screen_subscribe_cta grep (tests/phase08/test_endscreen_nonexistent_anchor.py)
- ANCHOR C — selenium + webdriver + playwright import grep (tests/phase08/test_no_selenium_anchor.py)

### Research Corrections Applied
- Pitfall 6: syntheticMedia → canonical containsSyntheticMedia (ai_disclosure.py + youtube_uploader.py)
- Pitfall 7: end_screen_subscribe_cta dropped (description CTA + pinned comment replacement per D-09)

### Test Suite Status
- Phase 4: 244/244 preserved
- Phase 5: 329/329 preserved
- Phase 6: 236/236 preserved
- Phase 7: 177/177 preserved
- Phase 8: <COUNT> tests green (exact count from pytest output)
- Combined regression: 986/986 + Phase 8 = <TOTAL>/<TOTAL> green

### Smoke Test Evidence
- Wave 5 Plan 08-06 executed post-approval
- Video ID: <first 8 chars> (full id in 08-06-SMOKE-EVIDENCE.md)
- cleanup-complete confirmed via videos.delete

### Phase 8 Ready for
- `/gsd:verify-work 8` — verifier audit
- `/gsd:plan-phase 9` — next phase (Documentation + KPI Dashboard + Taste Gate)
```

After VALIDATION.md flip, update `.planning/ROADMAP.md`:
- Phase 8 line `- [ ]` → `- [x] **Phase 8: Remote + Publishing + Production Metadata** — ...`
- Progress Table Phase 8 row: `0/TBD Not started` → `8/8 ✅ Complete | 2026-04-19`

Also update `.planning/REQUIREMENTS.md`:
- Lines 116-122 (PUB-01..05): `[ ]` → `[x]` each
- Lines 163-167 (REMOTE-01..03): `[ ]` → `[x]` each

Final verification:
```
python -m pytest tests/phase04 tests/phase05 tests/phase06 tests/phase07 tests/phase08 -q --no-cov
python scripts/validate/phase08_acceptance.py
python -m pytest tests/phase08/test_traceability_matrix.py -q --no-cov
grep -c "nyquist_compliant: true" .planning/phases/08-remote-publishing-production-metadata/08-VALIDATION.md
grep -c "status: complete" .planning/phases/08-remote-publishing-production-metadata/08-VALIDATION.md
```

All MUST exit 0 / output expected counts.
  </action>
  <read_first>
    - .planning/phases/08-remote-publishing-production-metadata/08-VALIDATION.md (before flip — frontmatter + 24 rows)
    - .planning/phases/07-integration-test/07-VALIDATION.md (Phase 7 Completion Summary format template)
    - .planning/phases/06-wiki-notebooklm-integration-failures-reservoir/06-VALIDATION.md (Phase 6 flip format)
    - .planning/ROADMAP.md (Phase 8 row + Progress Table)
    - .planning/REQUIREMENTS.md (PUB-01..05 + REMOTE-01..03 checkbox positions)
  </read_first>
  <acceptance_criteria>
    - `grep -c "^status: complete$" .planning/phases/08-remote-publishing-production-metadata/08-VALIDATION.md` outputs `1`
    - `grep -c "^nyquist_compliant: true$" .planning/phases/08-remote-publishing-production-metadata/08-VALIDATION.md` outputs `1`
    - `grep -c "^wave_0_complete: true$" .planning/phases/08-remote-publishing-production-metadata/08-VALIDATION.md` outputs `1`
    - `grep -c "^completed: 2" .planning/phases/08-remote-publishing-production-metadata/08-VALIDATION.md` outputs `1`
    - `grep -c "⬜ pending" .planning/phases/08-remote-publishing-production-metadata/08-VALIDATION.md` outputs `0` (all rows flipped)
    - `grep -c "✅ green\|✅ shipped" .planning/phases/08-remote-publishing-production-metadata/08-VALIDATION.md` outputs `>= 24`
    - `grep -cE "^- \[x\]" .planning/phases/08-remote-publishing-production-metadata/08-VALIDATION.md` outputs `>= 6` (6 Sign-Off checkboxes)
    - `grep -c "## Completion Summary" .planning/phases/08-remote-publishing-production-metadata/08-VALIDATION.md` outputs `1`
    - `grep -c "\- \[x\] \*\*Phase 8" .planning/ROADMAP.md` outputs `1`
    - `grep -cE "\- \[x\] \*\*PUB-0[1-5]\*\*" .planning/REQUIREMENTS.md` outputs `5`
    - `grep -cE "\- \[x\] \*\*REMOTE-0[1-3]\*\*" .planning/REQUIREMENTS.md` outputs `3`
    - `python -m pytest tests/phase04 tests/phase05 tests/phase06 tests/phase07 tests/phase08 -q --no-cov` exits 0
    - `python scripts/validate/phase08_acceptance.py` exits 0 + stdout contains `ALL_PASS`
    - `python -m pytest tests/phase08/test_traceability_matrix.py -q --no-cov` exits 0
  </acceptance_criteria>
  <automated>python -m pytest tests/phase08 -q --no-cov && python scripts/validate/phase08_acceptance.py</automated>
  <task_type>impl</task_type>
</task>

</tasks>

<verification>
1. `08-TRACEABILITY.md` exists with 8-REQ matrix + 6-SC aggregation + 8-plan audit + 3 anchors + 2 corrections documented.
2. `test_traceability_matrix.py` locks the above as automation guard.
3. `08-VALIDATION.md` frontmatter flipped: status=complete / nyquist_compliant=true / wave_0_complete=true / completed=<date>.
4. All 24 per-task rows ✅ green; Sign-Off 6/6 [x]; Completion Summary appended.
5. `.planning/ROADMAP.md` Phase 8 [x] + Progress Table updated.
6. `.planning/REQUIREMENTS.md` PUB-01..05 + REMOTE-01..03 all [x].
7. Full regression sweep + phase08_acceptance + traceability test all green.
</verification>

<success_criteria>
Plan 08-08 is COMPLETE when:
- 08-TRACEABILITY.md shipped with all required sections + 3 anchor documentation + 2 correction documentation.
- 08-VALIDATION.md frontmatter flipped + all rows green + Sign-Off checked + Completion Summary appended.
- ROADMAP.md + REQUIREMENTS.md reflect Phase 8 completion.
- test_traceability_matrix.py green (automation lock).
- phase08_acceptance.py still exits 0 post-flip.
- Phase 8 officially shippable for `/gsd:verify-work 8`.
</success_criteria>

<output>
Create `.planning/phases/08-remote-publishing-production-metadata/08-08-SUMMARY.md` documenting:
- 08-TRACEABILITY.md structure (8 REQ rows + 6 SC rows + 8 plan audit + 3 anchors + 2 corrections)
- 08-VALIDATION.md frontmatter flip: status=complete, nyquist_compliant=true, wave_0_complete=true, completed=<ISO_DATE>
- 24 rows transitioned ⬜→✅; Sign-Off 6/6 [x]; Completion Summary appended
- ROADMAP.md Phase 8 [x] + Progress Table updated
- REQUIREMENTS.md PUB-01..05 + REMOTE-01..03 [x]
- Full regression sweep + phase08_acceptance + traceability tests all green
- Commit hashes (2 atomic commits: traceability ship + VALIDATION flip)
- Phase 8 officially shippable — next: `/gsd:verify-work 8` then `/gsd:plan-phase 9`
</output>
