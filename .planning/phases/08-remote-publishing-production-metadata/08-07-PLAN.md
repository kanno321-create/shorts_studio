---
plan: 08-07
phase: 8
wave: 6
depends_on: [08-01, 08-02, 08-03, 08-04, 08-05, 08-06]
files_modified:
  - scripts/validate/phase08_acceptance.py
  - tests/phase08/test_phase08_acceptance.py
  - tests/phase08/test_regression_986_green.py
  - tests/phase08/test_full_publish_chain_mocked.py
files_read_first:
  - .planning/phases/08-remote-publishing-production-metadata/08-CONTEXT.md
  - .planning/phases/08-remote-publishing-production-metadata/08-RESEARCH.md
  - .planning/phases/08-remote-publishing-production-metadata/08-VALIDATION.md
  - scripts/validate/phase07_acceptance.py
  - scripts/validate/phase06_acceptance.py
  - tests/phase07/test_phase07_acceptance.py
  - tests/phase07/test_regression_809_green.py
  - scripts/publisher/youtube_uploader.py
  - scripts/publisher/github_remote.py
  - scripts/publisher/smoke_test.py
  - tests/phase08/mocks/youtube_mock.py
  - tests/phase08/mocks/github_mock.py
autonomous: true
requirements: [PUB-01, PUB-02, PUB-03, PUB-04, PUB-05, REMOTE-01, REMOTE-02, REMOTE-03]
tasks_addressed: [8-07-01, 8-07-02]
success_criteria: [SC1, SC2, SC3, SC4, SC5, SC6, REGRESSION]
estimated_commits: 2
parallel_boundary: sequential (phase-level aggregator)

must_haves:
  truths:
    - "scripts/validate/phase08_acceptance.py exists as subprocess CLI wrapper mirroring phase07_acceptance.py pattern"
    - "phase08_acceptance.py aggregates 6 SC checks: SC1 remote mirror + SC2 submodule + SC3 AI disclosure anchor + SC4 48h+KST + SC5 production_metadata + SC6 pinned comment"
    - "phase08_acceptance.py uses encoding='utf-8' + errors='replace' (Pitfall 3 cp949 guard)"
    - "test_phase08_acceptance.py invokes phase08_acceptance.py via subprocess + asserts exit 0 + 6 SC PASS markers"
    - "test_regression_986_green.py runs pytest tests/phase04 tests/phase05 tests/phase06 tests/phase07 via subprocess + asserts exit 0 + 986 tests"
    - "test_full_publish_chain_mocked.py runs an aggregated MockYouTube + MockGitHub scenario: repo create → submodule add → OAuth bootstrap → publish → delete → 1 atomic chain no exceptions"
  artifacts:
    - path: "scripts/validate/phase08_acceptance.py"
      provides: "SC1-6 CLI aggregator mirroring phase07_acceptance.py"
      min_lines: 100
    - path: "tests/phase08/test_phase08_acceptance.py"
      provides: "E2E wrapper asserts acceptance script exit 0 + 6 SC PASS"
      min_lines: 80
    - path: "tests/phase08/test_regression_986_green.py"
      provides: "D-23 equivalent: Phase 4+5+6+7 regression baseline preserved"
      min_lines: 40
    - path: "tests/phase08/test_full_publish_chain_mocked.py"
      provides: "End-to-end mocked scenario covering both REMOTE + PUBLISH halves"
      min_lines: 100
  key_links:
    - from: "tests/phase08/test_phase08_acceptance.py"
      to: "scripts/validate/phase08_acceptance.py"
      via: "subprocess.run asserting exit 0 + 6 SC PASS labels"
      pattern: "phase08_acceptance\\.py"
    - from: "tests/phase08/test_full_publish_chain_mocked.py"
      to: "tests/phase08/mocks/youtube_mock.py + github_mock.py"
      via: "aggregated scenario using both mocks in one test module"
      pattern: "MockYouTube|MockGitHub"
---

<objective>
Wave 6 E2E + REGRESSION — Ship `scripts/validate/phase08_acceptance.py` (SC1-6 subprocess aggregator mirroring Phase 7 Plan 07-08 format), plus 3 phase-level tests: (1) acceptance wrapper + 6 SC assertions; (2) 986 regression sweep verifying Phase 4+5+6+7 baseline still green; (3) full-publish-chain mocked E2E aggregating MockGitHub (repo + push + submodule) + MockYouTube (publish + delete) into a single atomic scenario.

Purpose: Phase 8 verification artifacts ship here. Plan 08-08 reads these results to flip the VALIDATION frontmatter. Direct analogue of Phase 7 Plan 07-08 structure.

Output: 1 acceptance CLI + 3 test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-remote-publishing-production-metadata/08-CONTEXT.md
@.planning/phases/08-remote-publishing-production-metadata/08-RESEARCH.md
@.planning/phases/08-remote-publishing-production-metadata/08-VALIDATION.md
@scripts/validate/phase07_acceptance.py
@tests/phase07/test_phase07_acceptance.py
@tests/phase07/test_regression_809_green.py
@scripts/publisher/youtube_uploader.py
@scripts/publisher/github_remote.py

<interfaces>
## Phase 8 SC → Test mapping (canonical)

| SC | Description | Primary Test File(s) |
|----|-------------|----------------------|
| SC1 | GitHub mirror exists (REMOTE-01/02) | test_github_remote_create.py + test_github_push_main.py |
| SC2 | Submodule .gitmodules present (REMOTE-03) | test_submodule_add.py |
| SC3 | AI disclosure anchor passes + 0 selenium (PUB-01/02) | test_ai_disclosure_anchor.py + test_no_selenium_anchor.py |
| SC4 | 48h+ lock + KST window (PUB-03) | test_publish_lock_48h.py + test_kst_window_weekday.py + test_kst_window_weekend.py |
| SC5 | production_metadata 4-field (PUB-04) | test_production_metadata_schema.py + test_metadata_html_comment.py |
| SC6 | Pinned comment + funnel (PUB-05) | test_pinned_comment.py + test_endscreen_nonexistent_anchor.py |

## scripts/validate/phase08_acceptance.py contract (mirror Phase 7 Plan 07-08 pattern)

```python
"""Phase 8 acceptance aggregator — SC1-6 subprocess wrapper.

Mirrors scripts/validate/phase07_acceptance.py (Phase 7 Plan 08 canonical format).
Exits 0 if all 6 SC groups report PASS; exits 1 on any FAIL.
"""
from __future__ import annotations

import subprocess
import sys
from pathlib import Path


_SC_MAP = {
    "SC1": {
        "description": "GitHub mirror created + main pushed (REMOTE-01/02)",
        "tests": ["tests/phase08/test_github_remote_create.py",
                  "tests/phase08/test_github_push_main.py"],
    },
    "SC2": {
        "description": "Submodule + .gitmodules schema (REMOTE-03)",
        "tests": ["tests/phase08/test_submodule_add.py"],
    },
    "SC3": {
        "description": "AI disclosure anchor + zero selenium/webdriver/playwright (PUB-01/02)",
        "tests": ["tests/phase08/test_ai_disclosure_anchor.py",
                  "tests/phase08/test_no_selenium_anchor.py"],
    },
    "SC4": {
        "description": "48h+ lock + KST weekday 20-23 + KST weekend 12-15 (PUB-03)",
        "tests": ["tests/phase08/test_publish_lock_48h.py",
                  "tests/phase08/test_kst_window_weekday.py",
                  "tests/phase08/test_kst_window_weekend.py"],
    },
    "SC5": {
        "description": "production_metadata 4-field + HTML comment (PUB-04)",
        "tests": ["tests/phase08/test_production_metadata_schema.py",
                  "tests/phase08/test_metadata_html_comment.py"],
    },
    "SC6": {
        "description": "Pinned comment + funnel + end-screen non-existent anchor (PUB-05)",
        "tests": ["tests/phase08/test_pinned_comment.py",
                  "tests/phase08/test_endscreen_nonexistent_anchor.py",
                  "tests/phase08/test_uploader_mocked_e2e.py"],
    },
}


def main() -> int:
    results: dict[str, str] = {}
    for sc, spec in _SC_MAP.items():
        argv = [sys.executable, "-m", "pytest", *spec["tests"], "-q", "--no-cov"]
        proc = subprocess.run(argv, capture_output=True, text=True,
                              encoding="utf-8", errors="replace")
        status = "PASS" if proc.returncode == 0 else "FAIL"
        results[sc] = status
        print(f"{sc}: {status} — {spec['description']}")
        if status == "FAIL":
            print(proc.stdout[-2000:])
            print(proc.stderr[-2000:])

    print("---")
    print(f"Phase 8 acceptance: {'ALL_PASS' if all(v == 'PASS' for v in results.values()) else 'FAIL'}")
    return 0 if all(v == "PASS" for v in results.values()) else 1


if __name__ == "__main__":
    if hasattr(sys.stdout, "reconfigure"):
        sys.stdout.reconfigure(encoding="utf-8")
    sys.exit(main())
```
</interfaces>
</context>

<tasks>

<task id="8-07-01">
  <action>
Create `scripts/validate/phase08_acceptance.py` (~130 lines) EXACTLY matching the `<interfaces>` contract above. Each SC entry in `_SC_MAP` lists the test files whose success proves the SC. The script runs each SC's pytest bundle serially, captures stdout+stderr, prints a `SC<N>: PASS|FAIL — <description>` line per SC, and exits 0 only when ALL 6 SCs pass.

Required invariants:
- `encoding="utf-8"` + `errors="replace"` in subprocess.run (Pitfall 3).
- UTF-8 stdout reconfigure at `__main__` (Windows cp949 guard).
- Each SC description matches the table in `<interfaces>` verbatim.
- Exit code 0 only when `all(v == "PASS" for v in results.values())`.

Create `tests/phase08/test_phase08_acceptance.py` (~100 lines):

```python
"""Wave 6 — subprocess wrapper asserts phase08_acceptance.py ships all 6 SC green."""
from __future__ import annotations

import subprocess
import sys
from pathlib import Path


_REPO = Path(__file__).resolve().parents[2]
_ACCEPT = _REPO / "scripts" / "validate" / "phase08_acceptance.py"


def test_acceptance_script_exists():
    assert _ACCEPT.exists()


def test_acceptance_script_exits_zero_when_all_sc_pass():
    result = subprocess.run(
        [sys.executable, str(_ACCEPT)],
        cwd=str(_REPO), capture_output=True, text=True,
        encoding="utf-8", errors="replace", timeout=600,
    )
    assert result.returncode == 0, (
        f"phase08_acceptance.py FAILED\nSTDOUT:\n{result.stdout}\nSTDERR:\n{result.stderr}"
    )


def test_acceptance_stdout_reports_all_six_sc_pass():
    result = subprocess.run(
        [sys.executable, str(_ACCEPT)],
        cwd=str(_REPO), capture_output=True, text=True,
        encoding="utf-8", errors="replace", timeout=600,
    )
    for sc in ("SC1", "SC2", "SC3", "SC4", "SC5", "SC6"):
        assert f"{sc}: PASS" in result.stdout, (
            f"{sc} not PASS in phase08_acceptance output:\n{result.stdout}"
        )


def test_acceptance_stdout_final_all_pass_marker():
    result = subprocess.run(
        [sys.executable, str(_ACCEPT)],
        cwd=str(_REPO), capture_output=True, text=True,
        encoding="utf-8", errors="replace", timeout=600,
    )
    assert "Phase 8 acceptance: ALL_PASS" in result.stdout


def test_acceptance_has_six_sc_entries_in_map():
    source = _ACCEPT.read_text(encoding="utf-8")
    # literal SC1..SC6 string count (keys of _SC_MAP)
    for sc in ("SC1", "SC2", "SC3", "SC4", "SC5", "SC6"):
        assert f'"{sc}"' in source, f"SC entry {sc} missing from _SC_MAP"
    # verify NO SC7
    assert '"SC7"' not in source


def test_acceptance_utf8_encoding_in_subprocess_call():
    source = _ACCEPT.read_text(encoding="utf-8")
    assert 'encoding="utf-8"' in source
    assert 'errors="replace"' in source
```

Create `tests/phase08/test_regression_986_green.py` (~60 lines):

```python
"""Wave 6 — 986 regression baseline preserved post-Phase 8.

Phase 4 (244) + Phase 5 (329) + Phase 6 (236) + Phase 7 (177) = 986 tests.
"""
from __future__ import annotations

import subprocess
import sys
from pathlib import Path


_REPO = Path(__file__).resolve().parents[2]


def test_phase04_green():
    result = subprocess.run(
        [sys.executable, "-m", "pytest", "tests/phase04", "-q", "--no-cov"],
        cwd=str(_REPO), capture_output=True, text=True,
        encoding="utf-8", errors="replace", timeout=600,
    )
    assert result.returncode == 0, f"Phase 4 regression broke:\n{result.stdout[-2000:]}"


def test_phase05_green():
    result = subprocess.run(
        [sys.executable, "-m", "pytest", "tests/phase05", "-q", "--no-cov"],
        cwd=str(_REPO), capture_output=True, text=True,
        encoding="utf-8", errors="replace", timeout=600,
    )
    assert result.returncode == 0, f"Phase 5 regression broke:\n{result.stdout[-2000:]}"


def test_phase06_green():
    result = subprocess.run(
        [sys.executable, "-m", "pytest", "tests/phase06", "-q", "--no-cov"],
        cwd=str(_REPO), capture_output=True, text=True,
        encoding="utf-8", errors="replace", timeout=600,
    )
    assert result.returncode == 0, f"Phase 6 regression broke:\n{result.stdout[-2000:]}"


def test_phase07_green():
    result = subprocess.run(
        [sys.executable, "-m", "pytest", "tests/phase07", "-q", "--no-cov"],
        cwd=str(_REPO), capture_output=True, text=True,
        encoding="utf-8", errors="replace", timeout=600,
    )
    assert result.returncode == 0, f"Phase 7 regression broke:\n{result.stdout[-2000:]}"


def test_combined_986_green():
    result = subprocess.run(
        [sys.executable, "-m", "pytest", "tests/phase04", "tests/phase05",
         "tests/phase06", "tests/phase07", "-q", "--no-cov"],
        cwd=str(_REPO), capture_output=True, text=True,
        encoding="utf-8", errors="replace", timeout=900,
    )
    assert result.returncode == 0
    # Assert test-count line "986 passed" appears in output
    assert "986 passed" in result.stdout, (
        f"Expected 986 passed tests; stdout:\n{result.stdout[-2000:]}"
    )
```
  </action>
  <read_first>
    - scripts/validate/phase07_acceptance.py (canonical template)
    - tests/phase07/test_phase07_acceptance.py (wrapper test template)
    - tests/phase07/test_regression_809_green.py (regression sweep template)
    - .planning/phases/08-remote-publishing-production-metadata/08-VALIDATION.md (SC mapping)
  </read_first>
  <acceptance_criteria>
    - `grep -c "SC1\|SC2\|SC3\|SC4\|SC5\|SC6" scripts/validate/phase08_acceptance.py` outputs `>= 6`
    - `grep -c 'encoding="utf-8"' scripts/validate/phase08_acceptance.py` outputs `>= 1`
    - `python scripts/validate/phase08_acceptance.py` exits 0 AND stdout contains `"Phase 8 acceptance: ALL_PASS"`
    - `python -m pytest tests/phase08/test_phase08_acceptance.py tests/phase08/test_regression_986_green.py -q --no-cov` exits 0
  </acceptance_criteria>
  <automated>python -m pytest tests/phase08/test_phase08_acceptance.py tests/phase08/test_regression_986_green.py -q --no-cov</automated>
  <task_type>impl</task_type>
</task>

<task id="8-07-02">
  <action>
Create `tests/phase08/test_full_publish_chain_mocked.py` (~140 lines) covering the aggregated scenario: MockGitHub (repo create → push → submodule add) + MockYouTube (publish → delete). One test module, multiple test functions, single atomic flow demonstrating all 8 requirements exercised in a real sequence.

```python
"""Wave 6 — full Phase 8 chain against mocks (no real API)."""
from __future__ import annotations

import sys
from pathlib import Path

import pytest

sys.path.insert(0, str(Path(__file__).resolve().parent))
from mocks.github_mock import MockGitHub
from mocks.youtube_mock import MockYouTube


@pytest.fixture(autouse=True)
def kst_pass(monkeypatch):
    import scripts.publisher.kst_window as kstw
    monkeypatch.setattr(kstw, "assert_in_window", lambda **kw: None)


def _build_plan():
    return {
        "snippet": {"title": "Phase 8 chain test", "description": "chain",
                    "tags": ["chain"], "categoryId": "24", "defaultLanguage": "ko"},
        "status": {"privacyStatus": "public",
                   "embeddable": True, "publicStatsViewable": True},
        "ai_disclosure": {"syntheticMedia": True},
        "production_metadata": {"script_seed": "chain_test",
                                "assets_origin": "kling:primary"},
        "funnel": {"pinned_comment": "구독!"},
        "_media_body": object(),
        "_thumb_body": object(),
    }


def test_full_chain_remote_then_publish(sample_mp4_path, tmp_publish_lock):
    """Atomic scenario: MockGitHub (3 ops) then MockYouTube (publish)."""
    # --- REMOTE half ---
    gh = MockGitHub()
    repo = gh.post_user_repos(name="shorts_studio", private=True, token="ghp_x")
    assert repo["name"] == "shorts_studio"
    assert repo["private"] is True

    push_rc = gh.git_push(
        "https://github.com/kanno321-create/shorts_studio.git",
        "main",
        env={"GIT_ASKPASS": "/tmp/ask.sh", "GITHUB_TOKEN": "ghp_x"},
    )
    assert push_rc == 0
    assert gh._pushes == [("https://github.com/kanno321-create/shorts_studio.git", "main")]

    submod_rc = gh.git_submodule_add(
        "https://github.com/kanno321-create/naberal_harness",
        "harness",
        "v1.0.1_commit_sha",
    )
    assert submod_rc == 0
    assert len(gh._submodules) == 1

    # --- PUBLISH half ---
    from scripts.publisher.youtube_uploader import publish
    yt = MockYouTube()
    plan = _build_plan()
    video_id = publish(yt, plan, sample_mp4_path, sample_mp4_path, "channel_1")
    assert video_id.startswith("mock_video_id_")
    assert yt._upload_count == 1
    assert video_id in yt._pinned_comments


def test_full_chain_publish_then_smoke_delete(sample_mp4_path, tmp_publish_lock, monkeypatch):
    """After publish, smoke_test delete cleanup works against the same MockYouTube."""
    import scripts.publisher.smoke_test as st
    monkeypatch.setattr(st.time, "sleep", lambda s: None)

    yt = MockYouTube()
    vid = st.run_smoke_test(youtube=yt, privacy="unlisted", cleanup=True)
    assert vid in yt._deleted


def test_production_metadata_reaches_description_in_chain(sample_mp4_path,
                                                           tmp_publish_lock, monkeypatch):
    """E2E guarantee: production_metadata JSON is visible in insert body description."""
    captured = {}
    from scripts.publisher.youtube_uploader import publish

    yt = MockYouTube()
    orig_insert = yt.videos().insert
    class _Spy:
        def insert(self, *, part, body, media_body):
            captured.update(body)
            return orig_insert(part=part, body=body, media_body=media_body)
    monkeypatch.setattr(yt, "videos", lambda: _Spy())

    plan = _build_plan()
    publish(yt, plan, sample_mp4_path, sample_mp4_path, "channel_1")
    desc = captured["snippet"]["description"]
    assert "<!-- production_metadata" in desc
    assert '"script_seed":"chain_test"' in desc
    assert '"assets_origin":"kling:primary"' in desc
    assert '"pipeline_version":"1.0.0"' in desc
    assert '"checksum":"sha256:5feceb66' in desc


def test_contains_synthetic_media_true_reaches_body_in_chain(sample_mp4_path,
                                                               tmp_publish_lock, monkeypatch):
    """E2E guarantee: containsSyntheticMedia=True in insert body (NOT syntheticMedia)."""
    captured = {}
    from scripts.publisher.youtube_uploader import publish

    yt = MockYouTube()
    orig_insert = yt.videos().insert
    class _Spy:
        def insert(self, *, part, body, media_body):
            captured.update(body)
            return orig_insert(part=part, body=body, media_body=media_body)
    monkeypatch.setattr(yt, "videos", lambda: _Spy())

    plan = _build_plan()
    publish(yt, plan, sample_mp4_path, sample_mp4_path, "channel_1")
    status_body = captured["status"]
    assert status_body["containsSyntheticMedia"] is True
    assert "syntheticMedia" not in status_body   # custom key MUST NOT leak into API body
    assert status_body["selfDeclaredMadeForKids"] is False


def test_publish_lock_recorded_after_chain(sample_mp4_path, tmp_publish_lock):
    from scripts.publisher.youtube_uploader import publish
    yt = MockYouTube()
    publish(yt, _build_plan(), sample_mp4_path, sample_mp4_path, "c1")
    assert tmp_publish_lock.exists()


def test_eight_requirements_all_exercised_in_chain(sample_mp4_path,
                                                     tmp_publish_lock):
    """Meta-test: prove REMOTE-01/02/03 + PUB-01/02/03/04/05 all fire in one flow."""
    gh = MockGitHub()
    # REMOTE-01
    gh.post_user_repos(name="shorts_studio", private=True, token="ghp_x")
    # REMOTE-02
    gh.git_push("https://github.com/kanno321-create/shorts_studio.git", "main",
                env={"GIT_ASKPASS": "/tmp", "GITHUB_TOKEN": "ghp_x"})
    # REMOTE-03
    gh.git_submodule_add("https://github.com/kanno321-create/naberal_harness",
                          "harness", "sha_v1_0_1")
    assert gh._repos_created and gh._pushes and gh._submodules

    # PUB-01..05 via publish()
    from scripts.publisher.youtube_uploader import publish
    yt = MockYouTube()
    plan = _build_plan()
    vid = publish(yt, plan, sample_mp4_path, sample_mp4_path, "c1")
    assert yt._upload_count == 1               # PUB-02
    assert tmp_publish_lock.exists()           # PUB-03
    # PUB-01 + PUB-04 + PUB-05 enforced in test_uploader_mocked_e2e / test_pinned_comment
    assert vid in yt._pinned_comments          # PUB-05
```

Run full sweep:
```
python -m pytest tests/phase04 tests/phase05 tests/phase06 tests/phase07 tests/phase08 -q --no-cov
```
MUST stay green.
  </action>
  <read_first>
    - scripts/publisher/youtube_uploader.py (publish signature)
    - scripts/publisher/smoke_test.py (run_smoke_test signature)
    - tests/phase08/mocks/youtube_mock.py + github_mock.py
    - tests/phase08/conftest.py (sample_mp4_path + tmp_publish_lock)
  </read_first>
  <acceptance_criteria>
    - `grep -c "def test_full_chain_remote_then_publish\|def test_full_chain_publish_then_smoke_delete\|def test_production_metadata_reaches_description_in_chain\|def test_contains_synthetic_media_true_reaches_body_in_chain\|def test_eight_requirements_all_exercised_in_chain" tests/phase08/test_full_publish_chain_mocked.py` outputs `5`
    - `python -m pytest tests/phase08/test_full_publish_chain_mocked.py -q --no-cov` exits 0
    - `python -m pytest tests/phase04 tests/phase05 tests/phase06 tests/phase07 tests/phase08 -q --no-cov` exits 0
    - `python scripts/validate/phase08_acceptance.py` exits 0 (re-verified post-Task 2)
  </acceptance_criteria>
  <automated>python -m pytest tests/phase08/test_full_publish_chain_mocked.py -q --no-cov</automated>
  <task_type>impl</task_type>
</task>

</tasks>

<verification>
1. phase08_acceptance.py aggregates 6 SC checks + exits 0 on all PASS.
2. test_phase08_acceptance.py subprocess-verifies the aggregator.
3. test_regression_986_green.py proves Phase 4+5+6+7 baseline preserved.
4. test_full_publish_chain_mocked.py exercises all 8 requirements in one flow.
5. 986 + Phase 8 full sweep green.
</verification>

<success_criteria>
Plan 08-07 is COMPLETE when:
- phase08_acceptance.py ships + exits 0 on full test suite.
- All 3 phase-level test files green.
- 986 regression preserved + Phase 8 E2E proves chain integrity.
</success_criteria>

<output>
Create `.planning/phases/08-remote-publishing-production-metadata/08-07-SUMMARY.md` documenting:
- Files created (1 acceptance CLI + 3 test files = 4 files)
- All 6 SC PASS + 986 regression green
- Full chain mocked E2E proof (REMOTE + PUBLISH + SMOKE-delete all exercised)
- Commit hashes
</output>
