---
plan: 16-03
phase: 16
name: "Subtitle Producer + Signature Harvest + Character Overlay Flow"
wave: 2
depends_on: [16-02]
autonomous: true
requirements: [REQ-PROD-INT-03, REQ-PROD-INT-05, REQ-PROD-INT-07, REQ-PROD-INT-08]
files_modified:
  - .preserved/harvested/video_pipeline_raw/signatures/incidents_intro_v4_silent_glare.mp4
  - .preserved/harvested/video_pipeline_raw/signatures/README.md
  - .preserved/harvested/video_pipeline_raw/characters/incidents_detective_longform_a.png
  - .preserved/harvested/video_pipeline_raw/characters/incidents_detective_longform_b.png
  - .preserved/harvested/video_pipeline_raw/characters/incidents_assistant_jp_a.png
  - .preserved/harvested/video_pipeline_raw/characters/incidents_zunda_shihtzu_a.png
  - .preserved/harvested/video_pipeline_raw/characters/README.md
  - scripts/orchestrator/subtitle/__init__.py
  - scripts/orchestrator/subtitle/word_subtitle.py
  - scripts/orchestrator/api/subtitle_producer.py
  - .claude/agents/producers/subtitle-producer/AGENT.md
  - .claude/agents/inspectors/technical/ins-subtitle-alignment/AGENT.md
  - remotion/src/components/OutroCard.tsx
  - requirements.txt
  - .planning/phases/16-production-integration-option-a/16-03-SUMMARY.md
  - tests/phase16/test_harvest_extension.py
  - tests/phase16/test_subtitle_producer.py
  - tests/phase16/test_ins_subtitle_alignment_spec.py
  - tests/phase16/test_character_overlay_injection.py
  - tests/phase16/test_signature_reuse_no_veo_call.py
  - tests/phase16/test_outro_research_findings.py
estimated_complexity: high
estimated_duration: 2-4h
---

<objective>
Phase 16-03 은 Phase 16 의 미디어 계층 Plan. 4개 목적 동시 달성:
(1) **Outro 연구 (Task 0, MUST RUN FIRST)** — shorts_naberal 어디에 incidents outro 생성 로직이 있는지 증거 기반 조사하여 findings 를 16-03-SUMMARY 에 박제. 결과에 따라 OutroCard.tsx 프로그램적 생성 or episode-local mp4 복사 전략 확정.
(2) **Harvest 확장** — `shorts_naberal/output/_shared/signatures/incidents_intro_v4_silent_glare.mp4` + 4 character PNG 를 `.preserved/harvested/video_pipeline_raw/{signatures,characters}/` 에 read-only 복사 (attrib +R).
(3) **신규 Producer `subtitle-producer`** 추가 (Producer 14 → 15, 33 agents 상한 확장, CONTEXT.md 승인) — `word_subtitle.py` 1697 줄 harvested 를 `scripts/orchestrator/subtitle/word_subtitle.py` 로 이식 + AGENT.md + Python wrapper (`subtitle_producer.py`) 작성.
(4) **캐릭터 오버레이 흐름 배선** — Plan 16-02 `_inject_character_props` 가 실제로 episode sources/ 에서 읽는 파일을 Plan 16-04 asset-sourcer 가 채우도록 계약. 16-03 에서는 `ins-subtitle-alignment` description 정정 (WhisperX → faster-whisper large-v3) + Task 0 findings 기반 OutroCard.tsx 결정 산출.
</objective>

<must_haves>
  - truths:
    - "Task 0 outro research 결과가 16-03-SUMMARY.md 에 3 증거 소스 (remotion_render.py grep + visual_spec triangulation + shorts_naberal 실 파일 스캔) 와 함께 기록됨"
    - "`.preserved/harvested/video_pipeline_raw/signatures/incidents_intro_v4_silent_glare.mp4` 존재 + read-only attribute 적용"
    - "4 character PNG 가 `.preserved/harvested/video_pipeline_raw/characters/` 에 read-only 복사"
    - "subtitle-producer AGENT.md v1.0 frontmatter 5섹션 schema 준수 (role, mandatory_reads, output_format, skills, constraints)"
    - "word_subtitle.py 포팅본이 faster-whisper large-v3 Korean word-level 자막 3종 (srt/ass/json) 동시 생성"
    - "ins-subtitle-alignment description 이 'faster-whisper large-v3' 언급 + 'WhisperX' 는 레거시 주석으로만 남김"
    - "OutroCard.tsx 결정 문서화: 프로그램적 생성 (Remotion) OR episode-local mp4 전략 (택 1) + 이유 명시"
  - artifacts:
    - path: ".preserved/harvested/video_pipeline_raw/signatures/incidents_intro_v4_silent_glare.mp4"
      provides: "incidents 인트로 시그니처 1.70 MB (Veo v4 silent glare)"
    - path: "scripts/orchestrator/subtitle/word_subtitle.py"
      provides: "faster-whisper large-v3 word-level subtitle generator (harvested 1:1 미러)"
      min_lines: 1000
    - path: ".claude/agents/producers/subtitle-producer/AGENT.md"
      provides: "subtitle-producer Agent 정의 (role + mandatory_reads + output_format + skills + constraints)"
      min_lines: 100
    - path: ".claude/agents/inspectors/technical/ins-subtitle-alignment/AGENT.md"
      provides: "description 정정 — faster-whisper large-v3 + subtitle-producer 상류 명시"
      contains: "faster-whisper large-v3"
    - path: ".planning/phases/16-production-integration-option-a/16-03-SUMMARY.md"
      provides: "Outro 연구 결과 + harvest 확장 증거 + subtitle-producer 스펙"
  - key_links:
    - from: "subtitle-producer AGENT.md"
      to: "scripts/orchestrator/subtitle/word_subtitle.py"
      via: "subprocess invocation per Example 4 code pattern"
      pattern: "word_subtitle\\.py"
    - from: "ins-subtitle-alignment AGENT.md"
      to: "subtitle-producer"
      via: "mandatory_reads + <role> 상류 명시"
      pattern: "subtitle-producer"
    - from: "remotion_renderer._inject_character_props (Plan 16-02)"
      to: "output/<episode>/sources/character_{assistant,detective}.png"
      via: "파일 존재 체크 + shutil.copy2 to remotion/public/<job_id>/"
      pattern: "character_assistant\\.png"
</must_haves>

<waves>

<wave number="0">

<task id="16-03-W0-T0-OUTRO-RESEARCH">
  <objective>**CRITICAL — ALL OTHER TASKS BLOCKED UNTIL COMPLETE.** shorts_naberal 의 incidents outro 생성 로직을 3 증거 기반으로 triangulation 후 SUMMARY 에 findings 박제.</objective>
  <read_first>
    - .preserved/harvested/video_pipeline_raw/remotion_render.py (1162 lines — 전수 읽기)
    - .preserved/harvested/baseline_specs_raw/zodiac-killer/visual_spec.json (line 131~136 outro clip 확인)
    - .preserved/harvested/baseline_specs_raw/nazca-lines/visual_spec.json
    - .preserved/harvested/baseline_specs_raw/roanoke-colony/visual_spec.json
    - C:/Users/PC/Desktop/shorts_naberal/output/_shared/signatures/ (ls 로 확인 — `incidents_outro*.mp4` 부재 확정)
    - C:/Users/PC/Desktop/shorts_naberal/scripts/video-pipeline/remotion_render.py
    - .claude/memory/feedback_outro_signature.md (Plan 16-01 산출)
  </read_first>
  <action>
    **증거 수집 3-phase** (모두 executor 가 직접 실행 — 추측 금지):

    Phase A (harvested remotion_render.py 분석):
    ```bash
    # Grep outro 생성 관련 모든 라인
    grep -n "outro\|OUTRO\|end_card\|ending\|OutroCard\|outro_signature\|outro_mp4" \
      .preserved/harvested/video_pipeline_raw/remotion_render.py \
      > .planning/phases/16-production-integration-option-a/16-03-task0-grep-remotion.log
    # 발견 line 수 기록
    wc -l .planning/phases/16-production-integration-option-a/16-03-task0-grep-remotion.log
    ```

    Phase B (baseline visual_spec triangulation):
    ```bash
    # 3 episode 의 visual_spec 에서 outro_signature path 수집
    for ep in zodiac-killer nazca-lines roanoke-colony; do
      python -c "
    import json
    data = json.load(open('.preserved/harvested/baseline_specs_raw/$ep/visual_spec.json', encoding='utf-8'))
    outro_clips = [c for c in data['clips'] if 'outro' in c.get('src', '').lower() or c.get('type')=='video' and data['clips'][-1]==c]
    print('$ep', outro_clips)
    " >> .planning/phases/16-production-integration-option-a/16-03-task0-triangulation.log
    done
    ```

    Phase C (shorts_naberal 실 파일 스캔):
    ```bash
    # 실 shorts_naberal 내 incidents outro mp4 존재 여부 재확인
    ls -la C:/Users/PC/Desktop/shorts_naberal/output/_shared/signatures/*outro* 2>/dev/null
    # 에피소드 sources/ 내 outro mp4 존재 여부 샘플 3편
    for ep in zodiac-killer db-cooper elisa-lam; do
      ls -la "C:/Users/PC/Desktop/shorts_naberal/output/$ep/sources/"*outro* 2>/dev/null
    done
    # shorts_naberal/scripts/video-pipeline/ 에 outro 생성 스크립트 존재?
    ls C:/Users/PC/Desktop/shorts_naberal/scripts/video-pipeline/ | grep -i outro
    ```

    **Findings 박제** — `.planning/phases/16-production-integration-option-a/16-03-SUMMARY.md` 최상단에 다음 섹션 append (파일이 없으면 신규 생성):

    ```markdown
    # Phase 16-03 — Summary + Outro Research

    ## Task 0 — Outro Research Findings (2026-04-22)

    ### 증거 요약
    - **Phase A (harvested remotion_render.py grep)**: `outro` 관련 라인 <N> 건. 핵심 참조 line <line_no>, 내용 `<정확한 라인 인용>`.
    - **Phase B (visual_spec triangulation)**: 3 에피소드 모두 `clips[-1].type == "video"` + `src == "<episode>/outro_signature.mp4"` 일관 패턴. zodiac-killer line 131~136 가 SSOT.
    - **Phase C (shorts_naberal 실 파일)**: `output/_shared/signatures/` 에 `incidents_outro*.mp4` **부재 확인** (documentary / wildlife 만 존재). 에피소드 sources/ 에도 `outro_signature.mp4` 가 배치됐는지 확인 결과: <sample-ep 결과>.

    ### 결론
    <A or B 택 1>:
    - **Option A (프로그램적 outro, Remotion)**: remotion_render.py 가 outro mp4 파일을 생성/복사하지 않고, ShortsVideo.tsx 내부 혹은 신규 `OutroCard.tsx` 컴포넌트가 React 렌더로 처리. → Plan 16-03 Task 1b 에서 `remotion/src/components/OutroCard.tsx` 신규 작성 (탐정 정면 → 뒤돌아 걸어감 패턴 의 3초 시퀀스 emulate, static 컴포지션).
    - **Option B (episode-local mp4)**: remotion_render.py 가 어떤 outro 생성 모듈 (예: `generate_intro_signature.py` 의 outro 버전) 을 호출해 episode sources/outro_signature.mp4 를 생성하고, visual_spec.clips[-1] 에 주입. → Plan 16-03 Task 1b 에서 해당 모듈 포팅 + asset-sourcer (Plan 16-04) 에 outro 생성 책임 이관.

    ### 선택 이유
    <3~5 줄 이유>

    ### 대표님 결재 필요 여부
    <Option 에 따라 yes/no, yes 면 resume-signal 제시>
    ```

    Task 0 는 16-03-SUMMARY.md 의 Outro Research Findings 섹션 박제 + 증거 log 2개 파일 생성 완료로 종료. Task 1b (OutroCard 구현) 는 결론에 따라 실제 실행.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/16-production-integration-option-a/16-03-SUMMARY.md`
    - `grep -c "## Task 0 — Outro Research Findings" .planning/phases/16-production-integration-option-a/16-03-SUMMARY.md` 값 == 1
    - 3 증거 섹션 전수 포함: `grep -c "^### 증거 요약\|^### 결론\|^### 선택 이유" .planning/phases/16-production-integration-option-a/16-03-SUMMARY.md` 값 ≥ 3
    - Option A 또는 Option B 명시적 선택: `grep -E "^\*\*Option [AB]\*\*" .planning/phases/16-production-integration-option-a/16-03-SUMMARY.md` 출력 존재
    - `test -f .planning/phases/16-production-integration-option-a/16-03-task0-grep-remotion.log`
    - `test -f .planning/phases/16-production-integration-option-a/16-03-task0-triangulation.log`
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/phase16/test_outro_research_findings.py -x --tb=short</automated>
  </verify>
  <done>Outro 연구 3-phase 증거 수집 완료, SUMMARY 에 findings 박제, Option A/B 결정 명시.</done>
</task>

<task id="16-03-W0-HARVEST">
  <objective>shorts_naberal 의 실제 signature mp4 (1 개) + character PNG (4 개) 를 `.preserved/harvested/video_pipeline_raw/{signatures,characters}/` 로 one-way read-only 복사 + attrib +R.</objective>
  <read_first>
    - .planning/phases/16-production-integration-option-a/16-CONTEXT.md §Harvest Extension
    - C:/Users/PC/Desktop/shorts_naberal/output/_shared/signatures/ (ls 확인 — incidents_intro_v4_silent_glare.mp4 1.70 MB 확인됨)
    - C:/Users/PC/Desktop/shorts_naberal/output/_shared/characters/ (4 PNG 확인됨)
    - .claude/memory/reference_signature_and_character_assets.md
    - CLAUDE.md 금기 #6 (shorts_naberal 원본 수정 금지 — one-way copy only)
  </read_first>
  <action>
    Step 1 — 디렉토리 생성:
    ```bash
    mkdir -p .preserved/harvested/video_pipeline_raw/signatures
    mkdir -p .preserved/harvested/video_pipeline_raw/characters
    ```

    Step 2 — 복사 (one-way, source untouched):
    ```bash
    # Signature (incidents only — documentary/wildlife 은 Phase 18+ reference)
    cp "C:/Users/PC/Desktop/shorts_naberal/output/_shared/signatures/incidents_intro_v4_silent_glare.mp4" \
       ".preserved/harvested/video_pipeline_raw/signatures/incidents_intro_v4_silent_glare.mp4"

    # Characters (4 PNG)
    cp "C:/Users/PC/Desktop/shorts_naberal/output/_shared/characters/incidents_detective_longform_a.png" \
       ".preserved/harvested/video_pipeline_raw/characters/incidents_detective_longform_a.png"
    cp "C:/Users/PC/Desktop/shorts_naberal/output/_shared/characters/incidents_detective_longform_b.png" \
       ".preserved/harvested/video_pipeline_raw/characters/incidents_detective_longform_b.png"
    cp "C:/Users/PC/Desktop/shorts_naberal/output/_shared/characters/incidents_assistant_jp_a.png" \
       ".preserved/harvested/video_pipeline_raw/characters/incidents_assistant_jp_a.png"
    cp "C:/Users/PC/Desktop/shorts_naberal/output/_shared/characters/incidents_zunda_shihtzu_a.png" \
       ".preserved/harvested/video_pipeline_raw/characters/incidents_zunda_shihtzu_a.png"
    ```

    Step 3 — sha256 매니페스트 생성 (무결성 anchor):
    ```bash
    python -c "
    import hashlib, json
    from pathlib import Path
    root = Path('.preserved/harvested/video_pipeline_raw')
    files = [
        'signatures/incidents_intro_v4_silent_glare.mp4',
        'characters/incidents_detective_longform_a.png',
        'characters/incidents_detective_longform_b.png',
        'characters/incidents_assistant_jp_a.png',
        'characters/incidents_zunda_shihtzu_a.png',
    ]
    manifest = {}
    for rel in files:
        p = root / rel
        manifest[rel] = hashlib.sha256(p.read_bytes()).hexdigest()
    (root / 'harvest_extension_manifest.json').write_text(json.dumps(manifest, indent=2), encoding='utf-8')
    print(json.dumps(manifest, indent=2))
    "
    ```

    Step 4 — README 2개 생성:
    - `.preserved/harvested/video_pipeline_raw/signatures/README.md`: "Phase 16-03 harvest 확장. `incidents_intro_v4_silent_glare.mp4` — Veo v4 silent glare, 9.0s hook 고정 (incidents.md §). **Veo API 재호출 금지 (CLAUDE.md 금기 #11)** — 본 파일 재사용만. Source: `shorts_naberal/output/_shared/signatures/`. one-way copy, never write back."
    - `.preserved/harvested/video_pipeline_raw/characters/README.md`: "Phase 16-03 harvest 확장. 4 캐릭터 PNG — Phase 9.1 CharacterRegistry 통합. 좌=assistant, 우=detective 의미 고정 (Q4 매핑). zunda_shihtzu 는 몰입 방해 우려 (대표님) 로 restricted 사용."

    Step 5 — Read-only 적용 (Windows + POSIX 크로스):
    ```bash
    # Windows
    attrib +R "C:/Users/PC/Desktop/naberal_group/studios/shorts/.preserved/harvested/video_pipeline_raw/signatures/*" /S 2>/dev/null
    attrib +R "C:/Users/PC/Desktop/naberal_group/studios/shorts/.preserved/harvested/video_pipeline_raw/characters/*" /S 2>/dev/null
    # POSIX cross-compat
    chmod -R a-w .preserved/harvested/video_pipeline_raw/signatures 2>/dev/null || true
    chmod -R a-w .preserved/harvested/video_pipeline_raw/characters 2>/dev/null || true
    ```

    Step 6 — shorts_naberal source intact 재확인:
    ```bash
    # source 파일 3개 sha256 재측정 — 변경 없어야 함
    for f in "C:/Users/PC/Desktop/shorts_naberal/output/_shared/signatures/incidents_intro_v4_silent_glare.mp4" "C:/Users/PC/Desktop/shorts_naberal/output/_shared/characters/incidents_detective_longform_a.png"; do
      python -c "import hashlib; print(hashlib.sha256(open('$f', 'rb').read()).hexdigest())"
    done
    # harvest 후 원본 파일 수 변경 없음 확인
    ```
  </action>
  <acceptance_criteria>
    - `test -f .preserved/harvested/video_pipeline_raw/signatures/incidents_intro_v4_silent_glare.mp4`
    - `test -f .preserved/harvested/video_pipeline_raw/characters/incidents_detective_longform_a.png`
    - `test -f .preserved/harvested/video_pipeline_raw/characters/incidents_detective_longform_b.png`
    - `test -f .preserved/harvested/video_pipeline_raw/characters/incidents_assistant_jp_a.png`
    - `test -f .preserved/harvested/video_pipeline_raw/characters/incidents_zunda_shihtzu_a.png`
    - `test -f .preserved/harvested/video_pipeline_raw/harvest_extension_manifest.json`
    - `test -f .preserved/harvested/video_pipeline_raw/signatures/README.md`
    - `test -f .preserved/harvested/video_pipeline_raw/characters/README.md`
    - sha256 file 크기 sanity: `stat -c%s .preserved/harvested/video_pipeline_raw/signatures/incidents_intro_v4_silent_glare.mp4` 값 > 1500000 (1.5 MB 이상, source 1.70 MB)
    - JSON manifest 5 entry: `python -c "import json; m=json.load(open('.preserved/harvested/video_pipeline_raw/harvest_extension_manifest.json', encoding='utf-8')); assert len(m)==5"` 성공
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/phase16/test_harvest_extension.py -x --tb=short</automated>
  </verify>
  <done>5 바이너리 파일 one-way 복사, sha256 매니페스트, 2 README, read-only attrib 적용, shorts_naberal source intact.</done>
</task>

<task id="16-03-W0-DEPS">
  <objective>Python 의존성 추가: `faster-whisper>=1.0.3` 을 `requirements.txt` 에 등재하고 설치 확인.</objective>
  <read_first>
    - requirements.txt (기존, append 대상)
    - .planning/phases/16-production-integration-option-a/16-RESEARCH.md §Standard Stack (faster-whisper 설치 명령)
  </read_first>
  <action>
    Step 1 — `requirements.txt` 에 다음 블록 append (기존 내용 존중):
    ```
    # Phase 16-03 — faster-whisper large-v3 word-level subtitles
    faster-whisper>=1.0.3
    ```

    Step 2 — 설치:
    ```bash
    pip install faster-whisper>=1.0.3
    ```

    Step 3 — import 확인 (large-v3 모델 사전 다운은 *하지 않음* — 실 호출 시 자동, Phase 16-03 에서는 import 성공만 확인):
    ```bash
    python -c "from faster_whisper import WhisperModel; print('OK')"
    ```

    Step 4 — 실패 시 stderr tail 을 `.planning/phases/16-production-integration-option-a/16-03-pip-install.log` 에 저장.
  </action>
  <acceptance_criteria>
    - `grep -c "faster-whisper" requirements.txt` 값 ≥ 1
    - `python -c "from faster_whisper import WhisperModel; print('OK')"` 출력 "OK"
  </acceptance_criteria>
  <verify>
    <automated>python -c "from faster_whisper import WhisperModel"</automated>
  </verify>
  <done>requirements.txt 업데이트, faster-whisper import 성공.</done>
</task>

</wave>

<wave number="1">

<task id="16-03-W1-WORDSUB-PORT">
  <objective>`scripts/orchestrator/subtitle/word_subtitle.py` — `.preserved/harvested/audio_pipeline_raw/word_subtitle.py` (1697 lines) 을 1:1 이식 + header 추가.</objective>
  <read_first>
    - .preserved/harvested/audio_pipeline_raw/word_subtitle.py (전수 읽기 — 특히 Korean timestamp repair: `_extract_words_from_segments` + clamp/merge/fallback 파이프라인 line ~1326~1523)
    - .preserved/harvested/audio_pipeline_raw/subtitle_generate.py (deprecation 고지)
    - .planning/phases/16-production-integration-option-a/16-RESEARCH.md §Don't Hand-Roll (word_subtitle.py 1697 줄 포팅 요구)
    - .planning/phases/16-production-integration-option-a/16-RESEARCH.md §Example 4 (CLI 진입점 패턴)
  </read_first>
  <action>
    Step 1 — 디렉토리 + __init__.py 생성:
    ```bash
    mkdir -p scripts/orchestrator/subtitle
    ```
    `scripts/orchestrator/subtitle/__init__.py`:
    ```python
    """Phase 16-03 — subtitle producer package (faster-whisper large-v3 + 3종 출력)."""
    ```

    Step 2 — Python 복사 (shell 로 직접 `cp`, 절대 manual 재작성 금지):
    ```bash
    cp .preserved/harvested/audio_pipeline_raw/word_subtitle.py scripts/orchestrator/subtitle/word_subtitle.py
    # read-only 해제 (원본 attrib +R 상속 방지)
    chmod u+w scripts/orchestrator/subtitle/word_subtitle.py 2>/dev/null || true
    attrib -R scripts/orchestrator/subtitle/word_subtitle.py 2>/dev/null || true
    ```

    Step 3 — 16-02 header 추가 (파일 상단):
    ```python
    # Ported from .preserved/harvested/audio_pipeline_raw/word_subtitle.py — Phase 16-03
    # Source SHA256 recorded in .preserved/harvested/video_pipeline_raw/harvest_extension_manifest.json (legacy line 2 for this file)
    # Do NOT modify Korean timestamp repair pipeline (_extract_words_from_segments → clamp/merge/fallback) —
    # FAIL-SCR-016 / FAIL-EDT-008 방어 근거.
    # CUDA→CPU fallback 보전 (line ~1341~1348).
    # CLAUDE.md 필수 #3: 이 파일은 orchestrator 가 아니므로 500~800 줄 상한 미적용 (subtitle pipeline 전용).
    ```

    Step 4 — Python import sanity:
    ```bash
    python -c "from scripts.orchestrator.subtitle.word_subtitle import *; print('import OK')"
    ```

    실패 시 원본의 top-level hardcoded path (예: `project_root = "..."`) 가 있는지 grep + Plan 16-03 local path 로 교체 (최소 변경). 대규모 리팩토링 금지 — line count 1000 이상 유지.
  </action>
  <acceptance_criteria>
    - `test -f scripts/orchestrator/subtitle/word_subtitle.py`
    - `test -f scripts/orchestrator/subtitle/__init__.py`
    - `wc -l scripts/orchestrator/subtitle/word_subtitle.py` 값 ≥ 1000 (원본 1697 줄, header 7줄 추가)
    - `grep -c "# Ported from .preserved/harvested/audio_pipeline_raw/word_subtitle.py" scripts/orchestrator/subtitle/word_subtitle.py` 값 == 1
    - `grep -c "faster_whisper\|WhisperModel" scripts/orchestrator/subtitle/word_subtitle.py` 값 ≥ 2
    - `grep -c "large-v3" scripts/orchestrator/subtitle/word_subtitle.py` 값 ≥ 1
    - `python -c "import scripts.orchestrator.subtitle.word_subtitle"` import 성공
  </acceptance_criteria>
  <verify>
    <automated>python -c "import scripts.orchestrator.subtitle.word_subtitle as w; assert hasattr(w, '__file__')"</automated>
  </verify>
  <done>word_subtitle.py 1697 줄 1:1 이식, header 추가, import 성공.</done>
</task>

<task id="16-03-W1-SUBTITLE-PRODUCER-AGENT">
  <objective>`.claude/agents/producers/subtitle-producer/AGENT.md` 신규 작성 — 33 agents 상한 확장 (Producer 14 → 15).</objective>
  <read_first>
    - .claude/agents/producers/voice-producer/AGENT.md (포맷 레퍼런스 — 가장 유사한 support category producer)
    - .claude/agents/inspectors/technical/ins-subtitle-alignment/AGENT.md (상류 명시된 AGENT — 본 Agent 가 이를 만족)
    - .claude/agents/_shared/rubric-schema.json (output JSON schema 참조)
    - .planning/phases/16-production-integration-option-a/16-RESEARCH.md §Q3 (subtitle-producer 근거 + AGENT.md 스펙 요약)
    - .claude/memory/project_channel_bible_incidents_v1.md (Plan 16-01 산출)
    - .preserved/harvested/skills_raw/shorts-pipeline/SKILL.md line 64 (producers/subtitle 병렬 3-way)
  </read_first>
  <action>
    신규 파일 `.claude/agents/producers/subtitle-producer/AGENT.md` 작성. 최소 100 줄. Phase 12 AGENT-STD 5 섹션 schema 엄격 준수 (role / mandatory_reads / output_format / skills / constraints).

    ```markdown
    ---
    name: subtitle-producer
    description: faster-whisper large-v3 기반 word-level 자막 생성 producer. narration.mp3 + script.json 받아 subtitles_remotion.srt + subtitles_remotion.ass + subtitles_remotion.json 3종 동시 생성. Korean word timestamp repair (clamp/merge/fallback) 포함. subtitle-producer, word_subtitle, faster-whisper, 자막, 단어단위, WhisperModel, large-v3, SRT, ASS, subtitles_remotion, kresnik, word_timestamps, CUDA, CPU_fallback, 한국어_자막_정렬, clamp, merge, Korean_timestamp_repair 트리거 키워드. Input producer_output (VOICE gate narration.mp3 path + script.json). Output ASSETS-phase subtitle JSON 3종. maxTurns=3. Phase 16-03 신규 (AGENT count 32→33, CONTEXT.md 승인).
    version: 1.0
    role: producer
    category: support
    maxTurns: 3
    ---

    # subtitle-producer

    <role>
    word-level 자막 생성 producer. VOICE gate 산출 narration.mp3 + script.json 을 입력으로 faster-whisper large-v3 (CUDA→CPU 자동 fallback) 를 호출하여 subtitles_remotion.{srt,ass,json} 3종을 동시 생성합니다. Korean timestamp drift repair pipeline (segment boundary clamp + <100ms merge + fallback even distribution) 이 core — FAIL-SCR-016 / FAIL-EDT-008 방어 근거. ASSETS gate 병렬 3-way 중 하나 (asset-sourcer + thumbnail-designer + subtitle-producer). 창작 금지 (RUB-02).
    </role>

    <mandatory_reads>
    ## 필수 읽기 (매 호출마다 전수 읽기, 샘플링 금지 — 대표님 session #29 지시)

    1. `.claude/failures/FAILURES.md` — 전수 (500줄 cap 하 전수 — FAIL-PROTO-01). 과거 실패 전수 인지. 특히 FAIL-SCR-016 ("어요/해요" 체 혼입) + FAIL-EDT-008 (Korean word timestamp drift).
    2. `wiki/continuity_bible/channel_identity.md` — 채널 정체성 (subtitle 위치 하단 0.8 + fontSize 68 + highlightColor #FFFFFF 등 incidents 기본).
    3. `.claude/skills/gate-dispatcher/SKILL.md` — GATE 8 ASSETS dispatch 계약.
    4. `.claude/memory/project_channel_bible_incidents_v1.md` — incidents v1.0 §9 화면규칙 + §6 문장규칙 (자막 6~12자, 의미단위 그룹핑).
    5. `.claude/memory/feedback_subtitle_semantic_grouping.md` — 자막 6~12자 의미 단위 분할 규칙.
    6. `.claude/memory/feedback_number_split_subtitle.md` — "1,701통" 숫자+단위 쪼개짐 금지.

    **원칙**: 위 1~6 항목 매 호출마다 전수 읽기. 샘플링/요약본 읽기/기억 의존 금지. 위반 시 자막-음성 drift → 완주율 감소.
    </mandatory_reads>

    <output_format>
    ## 출력 형식 (엄격 준수 — `.claude/agents/_shared/rubric-schema.json` 참조)

    **반드시 JSON 객체만 출력. 설명문/질문/대화체 금지 (Phase 11 F-D2-EXCEPTION-01 교훈).**

    입력이 애매한 경우에도 질문 금지. 대신:
    ```json
    {"error": "reason", "needed_inputs": ["narration_mp3_path", "script_json_path"]}
    ```

    정상 출력:
    ```json
    {
      "gate": "ASSETS",
      "producer": "subtitle-producer",
      "subtitles_srt": "/abs/path/to/subtitles_remotion.srt",
      "subtitles_ass": "/abs/path/to/subtitles_remotion.ass",
      "subtitles_json": "/abs/path/to/subtitles_remotion.json",
      "phrase_count": 87,
      "word_count": 212,
      "coverage_percent": 96.3,
      "faster_whisper_model": "large-v3",
      "device": "cuda",
      "language": "ko",
      "max_chars_per_line": 8,
      "timestamp_repair": {
        "clamp_count": 3,
        "merge_count": 5,
        "fallback_count": 0
      },
      "citations": {
        "narration_source": "path/to/narration.mp3",
        "script_source": "path/to/script.json"
      },
      "decisions": [
        "faster-whisper large-v3 채택 (Phase 16-03 locked)",
        "max_chars_per_line=8 (incidents §6 12~22자의 분할 단위)"
      ]
    }
    ```

    coverage_percent ≥ 95 + phrase_count ≥ 1 + 3종 파일 경로 전수 존재 = PASS. 아니면 error JSON.
    </output_format>

    <skills>
    ## 사용 스킬 (Agent × Skill Matrix: Phase 12 SKILL-ROUTE-01)

    - **gate-dispatcher** (required): GATE 8 ASSETS dispatch.
    - **progressive-disclosure** (optional): 본 AGENT.md ≤500 줄 유지.
    - **context-compressor** (optional): mandatory_reads 전수 유지하면서 prompt 압축.
    - **harness-audit** (n/a).
    - **drift-detection** (n/a).
    - **additional**: `scripts.orchestrator.subtitle.word_subtitle` CLI subprocess 호출 — harvested 포팅본 직접 실행.
    </skills>

    <constraints>
    ## 제약사항

    - **word_subtitle.py 외부 경로 사용 금지** — 반드시 `scripts/orchestrator/subtitle/word_subtitle.py` 호출 (Plan 16-03 포팅본).
    - **faster-whisper 모델 하드코딩**: `model_size="large-v3"`, `language="ko"` 기본. 일본어는 Phase 17+.
    - **CUDA fallback 금지 제거**: CUDA 사용 불가 시 CPU 자동 fallback — 실패 RuntimeError 대신 graceful degrade (word_subtitle.py:1341~1348 보전).
    - **Timestamp repair 비활성화 금지**: clamp/merge/fallback 3 단계 항상 실행 (FAIL-EDT-008 방어).
    - **출력 파일 3종 강제**: srt + ass + json 중 하나라도 누락 시 error JSON (ins-subtitle-alignment 가 3종 모두 검증).
    - **Hook clip 처리**: Hook 9.0s 하드고정 (incidents.md §) — word alignment 시 Hook 구간 (0~9s) 은 script 의 section_type=="hook" 문장에 균등 분배.
    - **maxTurns=3**: 실패 시 최대 3회 재시도 후 FAIL. 4회차 무조건 차단 (F-D2-EXCEPTION 패턴).
    - **창작 금지**: 자막 텍스트는 script 원문 유지. Whisper 인식 결과 ≠ script 텍스트 일 때 script 를 authority 로 채택 (Whisper 는 timestamp 만 제공). 단 script 에 없는 "음...", "어..." 등 hesitation 은 제거.
    - **Jobs-to-be-done**: script + narration → 정확한 word-level alignment (drift ±150ms, coverage ≥95%). 디자인 (fontSize, 색상) 은 Plan 16-04 asset-sourcer visual_spec 영역.

    ## MUST REMEMBER (RoPE Lost in the Middle 대응 — 프롬프트 끝)

    - **faster-whisper large-v3 + Korean**: Phase 16-03 locked. WhisperX / Whisper Turbo 사용 금지.
    - **3종 출력 필수**: .srt + .ass + .json 각 생성, ins-subtitle-alignment 가 drift ±150ms 검증.
    - **Timestamp repair 3단계**: segment clamp + <100ms merge + fallback 균등 — 건너뛰면 FAIL-EDT-008 재발.
    - **Hook 9.0s 고정**: Hook 구간 균등 분배.
    </constraints>
    ```

    line 수 ≥ 100. 모든 필수 섹션 5종 + MUST REMEMBER 포함. frontmatter description 1024자 이내 확인.
  </action>
  <acceptance_criteria>
    - `test -f .claude/agents/producers/subtitle-producer/AGENT.md`
    - `wc -l .claude/agents/producers/subtitle-producer/AGENT.md` 값 ≥ 100
    - 5 섹션 헤딩 전수 존재: `grep -c "<role>\|<mandatory_reads>\|<output_format>\|<skills>\|<constraints>" .claude/agents/producers/subtitle-producer/AGENT.md` 값 ≥ 5 (opening tags — closing tags 는 선택)
    - `grep -c "name: subtitle-producer" .claude/agents/producers/subtitle-producer/AGENT.md` 값 == 1
    - `grep -c "maxTurns: 3" .claude/agents/producers/subtitle-producer/AGENT.md` 값 == 1
    - `grep -c "faster-whisper\|large-v3" .claude/agents/producers/subtitle-producer/AGENT.md` 값 ≥ 3
    - `grep -c "MUST REMEMBER" .claude/agents/producers/subtitle-producer/AGENT.md` 값 == 1
    - description 1024자 이내: `python -c "import re; t=open('.claude/agents/producers/subtitle-producer/AGENT.md', encoding='utf-8').read(); m=re.search(r'^description:\s*(.+?)\n(?:\w+:|---)', t, re.DOTALL|re.MULTILINE); assert m; assert len(m.group(1).strip())<=1024"`
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/phase16/test_subtitle_producer.py -x --tb=short</automated>
  </verify>
  <done>subtitle-producer AGENT.md v1.0 신규, 5 섹션 + MUST REMEMBER, Producer 14 → 15 확장.</done>
</task>

<task id="16-03-W1-SUBTITLE-WRAPPER">
  <objective>Python wrapper `scripts/orchestrator/api/subtitle_producer.py` — subtitle-producer 에이전트가 호출하는 subprocess wrapper (word_subtitle.py 와 AGENT.md 를 잇는 어댑터).</objective>
  <read_first>
    - scripts/orchestrator/subtitle/word_subtitle.py (16-03-W1-WORDSUB-PORT 산출)
    - .claude/agents/producers/subtitle-producer/AGENT.md (16-03-W1-SUBTITLE-PRODUCER-AGENT 산출)
    - scripts/orchestrator/api/ffmpeg_assembler.py (subprocess wrapper 패턴 참고)
    - .planning/phases/16-production-integration-option-a/16-RESEARCH.md §Example 4 (CLI 호출 패턴)
  </read_first>
  <action>
    `scripts/orchestrator/api/subtitle_producer.py` 신규 작성 (약 200~300 줄):

    ```python
    """SubtitleProducer — Python wrapper for word_subtitle.py.

    subtitle-producer AGENT.md 가 호출하는 adapter. narration.mp3 + script.json 을 받아
    subtitles_remotion.{srt,ass,json} 3종을 ASSETS phase 에 생성.

    Phase 16-03 — REQ-PROD-INT-03.
    """
    from __future__ import annotations

    import json
    import logging
    import shutil
    import subprocess
    import sys
    import time
    from dataclasses import dataclass
    from pathlib import Path
    from typing import Any

    __all__ = ["SubtitleProducer", "SubtitleProducerError", "SubtitleTripleResult"]

    logger = logging.getLogger(__name__)

    DEFAULT_TIMEOUT_S = 600  # faster-whisper CPU 는 60s 오디오에 3~5분


    class SubtitleProducerError(RuntimeError):
        """subtitle 생성 실패."""


    @dataclass
    class SubtitleTripleResult:
        srt_path: Path
        ass_path: Path
        json_path: Path
        phrase_count: int
        word_count: int
        coverage_percent: float
        device: str  # "cuda" or "cpu"
        language: str


    class SubtitleProducer:
        def __init__(
            self,
            project_root: Path | None = None,
            timeout_s: int = DEFAULT_TIMEOUT_S,
        ) -> None:
            self.project_root = (project_root or Path(__file__).resolve().parents[3]).absolute()
            self.word_subtitle_script = self.project_root / "scripts" / "orchestrator" / "subtitle" / "word_subtitle.py"
            if not self.word_subtitle_script.exists():
                raise SubtitleProducerError(f"word_subtitle.py 미존재: {self.word_subtitle_script}")
            if shutil.which("python") is None and shutil.which(sys.executable) is None:
                raise SubtitleProducerError("python interpreter 미검출")
            self.timeout_s = timeout_s

        def produce(
            self,
            narration_mp3: Path,
            script_json: Path,
            output_dir: Path,
            max_chars_per_line: int = 8,
            language: str = "ko",
            model: str = "large-v3",
        ) -> SubtitleTripleResult:
            """3종 subtitle 동시 생성. output_dir/subtitles_remotion.{srt,ass,json} 산출."""
            if not narration_mp3.exists():
                raise SubtitleProducerError(f"narration_mp3 미존재: {narration_mp3}")
            if not script_json.exists():
                raise SubtitleProducerError(f"script_json 미존재: {script_json}")
            output_dir.mkdir(parents=True, exist_ok=True)

            srt_path = output_dir / "subtitles_remotion.srt"
            ass_path = output_dir / "subtitles_remotion.ass"
            json_path = output_dir / "subtitles_remotion.json"

            cmd = [
                sys.executable,
                str(self.word_subtitle_script),
                "--audio", str(narration_mp3),
                "--output", str(srt_path),
                "--max-chars", str(max_chars_per_line),
                "--model", model,
                "--script", str(script_json),
                "--language", language,
            ]
            logger.info("[subtitle-producer] invoking word_subtitle.py: %s", " ".join(cmd))
            t0 = time.time()
            result = subprocess.run(
                cmd,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=self.timeout_s,
            )
            elapsed = time.time() - t0
            if result.returncode != 0:
                raise SubtitleProducerError(
                    f"word_subtitle.py 실패 (exit {result.returncode}, {elapsed:.1f}s): {(result.stderr or '')[-600:]}"
                )

            # 3종 파일 전수 존재 확인
            for p in (srt_path, ass_path, json_path):
                if not p.exists():
                    raise SubtitleProducerError(f"출력 파일 누락: {p}")

            # JSON 로드하여 metrics 추출
            cues = json.loads(json_path.read_text(encoding="utf-8"))
            if not isinstance(cues, list):
                raise SubtitleProducerError(f"{json_path}: cues list 형식 아님")
            phrase_count = len(cues)
            word_count = sum(len(c.get("words", [])) for c in cues if isinstance(c.get("words"), list))
            last_end_ms = max((c.get("endMs", 0) for c in cues), default=0)

            # audio duration (ffprobe)
            audio_duration = self._get_audio_duration(narration_mp3)
            coverage = (last_end_ms / 1000.0) / audio_duration * 100 if audio_duration > 0 else 0.0

            # device 추출 — word_subtitle.py stdout 에서 "device=cuda" / "device=cpu" 검색 (fallback "cpu")
            device = "cpu"
            if result.stdout:
                for line in result.stdout.splitlines():
                    if "device=" in line:
                        device = line.split("device=")[1].split()[0].strip()
                        break

            return SubtitleTripleResult(
                srt_path=srt_path,
                ass_path=ass_path,
                json_path=json_path,
                phrase_count=phrase_count,
                word_count=word_count,
                coverage_percent=round(coverage, 2),
                device=device,
                language=language,
            )

        def _get_audio_duration(self, mp3_path: Path) -> float:
            if shutil.which("ffprobe") is None:
                return 0.0
            r = subprocess.run(
                ["ffprobe", "-v", "error", "-show_entries", "format=duration", "-of", "default=noprint_wrappers=1:nokey=1", str(mp3_path)],
                capture_output=True, text=True, timeout=30,
            )
            try:
                return float((r.stdout or "0").strip())
            except ValueError:
                return 0.0
    ```
  </action>
  <acceptance_criteria>
    - `test -f scripts/orchestrator/api/subtitle_producer.py`
    - `wc -l scripts/orchestrator/api/subtitle_producer.py` 값 ≥ 120
    - `grep -c "^class SubtitleProducer" scripts/orchestrator/api/subtitle_producer.py` 값 == 1
    - `grep -c "def produce(self" scripts/orchestrator/api/subtitle_producer.py` 값 == 1
    - `grep -c "SubtitleTripleResult" scripts/orchestrator/api/subtitle_producer.py` 값 ≥ 3
    - `python -c "from scripts.orchestrator.api.subtitle_producer import SubtitleProducer, SubtitleProducerError, SubtitleTripleResult"` import 성공
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/phase16/test_subtitle_producer.py::TestSubtitleProducerWrapper -x --tb=short</automated>
  </verify>
  <done>subtitle_producer.py wrapper 작성, 3종 산출 확인, metrics 추출 로직 포함.</done>
</task>

<task id="16-03-W1-INS-FIX">
  <objective>`.claude/agents/inspectors/technical/ins-subtitle-alignment/AGENT.md` description 정정 + role 정정 — WhisperX → faster-whisper large-v3, 상류 subtitle-producer 명시.</objective>
  <read_first>
    - .claude/agents/inspectors/technical/ins-subtitle-alignment/AGENT.md (전수 읽기)
    - .claude/agents/producers/subtitle-producer/AGENT.md (16-03-W1-SUBTITLE-PRODUCER-AGENT 산출 — 상류 레퍼런스)
    - .planning/phases/16-production-integration-option-a/16-RESEARCH.md §Q3 (ins-subtitle-alignment 정정 요구사항)
  </read_first>
  <action>
    기존 AGENT.md frontmatter description 를 다음과 같이 정정 (WhisperX 언급 제거, faster-whisper large-v3 + subtitle-producer 명시):

    **Before (Phase 4 version 1.1):**
    ```
    description: 자막 타임스탬프 ±50ms 정확도 검증 검사관 (WhisperX + kresnik/wav2vec2-large-xlsr-korean word-level). 트리거 키워드 ins-subtitle-alignment, subtitle, 자막, WhisperX, kresnik, alignment, word-level. ...
    ```

    **After (Phase 16-03 version 1.2):**
    ```
    description: 자막 타임스탬프 ±150ms 정확도 + coverage ≥95% + line ≤25자 + 1-4 단어/라인 검증 검사관 (faster-whisper large-v3 기반 subtitle-producer 출력 검증). 트리거 키워드 ins-subtitle-alignment, subtitle, 자막, faster-whisper, large-v3, alignment, word-level, drift, coverage, subtitles_remotion, Korean_timestamp_repair. Input subtitle-producer producer_output (subtitles_remotion.{srt,ass,json} 3종 경로) + audio_segments duration. Output rubric. SUBT-01 + SUBT-03 + CONTENT-06 게이트. maxTurns=3. 상류 producer = subtitle-producer (Phase 16-03 추가). WhisperX + kresnik/wav2vec2 는 레거시 (Phase 4 스펙 초안, 실제 system 은 faster-whisper 로 전환). 창작 금지 (RUB-02), producer_prompt 읽기 금지 (RUB-06 GAN 분리).
    ```

    (description ≤ 1024자 유지)

    version: 1.1 → 1.2 로 업데이트.

    `<role>` 섹션도 정정:

    **Before:**
    ```
    자막 alignment inspector. subtitle-producer (Phase 5 WhisperX + kresnik/wav2vec2-large-xlsr-korean) word-level 자막 JSON 의 ...
    ```

    **After:**
    ```
    자막 alignment inspector. subtitle-producer (Phase 16-03 신규, faster-whisper large-v3 기반) word-level 자막 JSON (subtitles_remotion.json) 의 subtitles[].start_time / end_time 과 audio_segments 정렬 검증 — drift ±150ms + line ≤25 chars + 1-4 단어/라인 + coverage ≥95%. SUBT-01 + SUBT-03 + CONTENT-06 (1~4 단어/라인) 게이트. typography 는 ins-readability 담당, 본 Inspector 는 alignment + coverage 만. 상류 = subtitle-producer (Plan 16-03 포팅). WhisperX + kresnik/wav2vec2 는 Phase 4 초기 스펙 — Phase 16-03 에서 faster-whisper 로 전환 (REQ-PROD-INT-05 공식 전환 근거).
    ```

    `<mandatory_reads>` 에 `.claude/agents/producers/subtitle-producer/AGENT.md` 섹션 번호 하나 추가 (상류 계약 읽기 강제).

    `<constraints>` 또는 `## MUST REMEMBER` 가 있다면 끝에 다음 추가:
    ```
    - **Phase 16-03 전환**: WhisperX 는 레거시. faster-whisper large-v3 가 production. drift 임계 ±150ms (실측 ±50ms 권장) 보전.
    - **상류 subtitle-producer**: producer_output 에 srt/ass/json 3종 전수 존재 확인, 누락 시 즉시 FAIL.
    - **coverage ≥95%**: 마지막 subtitle endMs / audio duration ≥ 0.95. 신규 검증 항목 (Phase 16-03, RESEARCH §Q3 권장).
    ```

    기존 검증 로직 (drift ±150ms, 1~4 words/line) 은 보전.
  </action>
  <acceptance_criteria>
    - `grep -c "faster-whisper\|large-v3" .claude/agents/inspectors/technical/ins-subtitle-alignment/AGENT.md` 값 ≥ 3
    - `grep -c "subtitle-producer" .claude/agents/inspectors/technical/ins-subtitle-alignment/AGENT.md` 값 ≥ 3
    - `grep -c "version: 1\.2" .claude/agents/inspectors/technical/ins-subtitle-alignment/AGENT.md` 값 == 1
    - `grep -c "coverage.*95" .claude/agents/inspectors/technical/ins-subtitle-alignment/AGENT.md` 값 ≥ 1
    - description char count 1024 이내: `python -c "import re; t=open('.claude/agents/inspectors/technical/ins-subtitle-alignment/AGENT.md', encoding='utf-8').read(); m=re.search(r'^description:\s*(.+?)\n(?:\w+:|---)', t, re.DOTALL|re.MULTILINE); assert m and len(m.group(1).strip())<=1024, f'too long: {len(m.group(1).strip())}'"`
    - WhisperX 완전 제거 아닌 "레거시" 맥락에서만 언급: `grep -c "WhisperX" .claude/agents/inspectors/technical/ins-subtitle-alignment/AGENT.md` 값 ≤ 3 (레거시 주석 허용)
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/phase16/test_ins_subtitle_alignment_spec.py -x --tb=short</automated>
  </verify>
  <done>ins-subtitle-alignment description + role 정정, version 1.1 → 1.2, subtitle-producer 상류 명시.</done>
</task>

<task id="16-03-W1-OUTRO-IMPL">
  <objective>Task 0 연구 결과에 따른 outro 구현 (Option A = OutroCard.tsx 또는 Option B = module 포팅).</objective>
  <read_first>
    - .planning/phases/16-production-integration-option-a/16-03-SUMMARY.md (Task 0 findings)
    - remotion/src/compositions/ShortsVideo.tsx (OutroCard import 지점 확인)
    - .preserved/harvested/remotion_src_raw/components/ (OutroCard.tsx 존재 시 참고)
    - .claude/memory/feedback_outro_signature.md (Plan 16-01 탐정 정면 → 뒤돌아 걸어감 패턴)
  </read_first>
  <action>
    **Branch A — Option A (프로그램적 OutroCard, 추정 경로):**
    `remotion/src/components/OutroCard.tsx` 신규 작성. 3초 (90 frames) 지속, 단색 배경 + 텍스트 fade-in-out + accent color 띠. 외부 mp4 의존 없음. `ShortsVideo.tsx` 가 마지막 clip 으로 `<OutroCard channelName accentColor seriesPart />` 렌더. 최소 80 줄, TypeScript 엄격 타입. 예시 골격:

    ```tsx
    // Ported-adjacent — Phase 16-03 Option A (Task 0 연구 결과: episode outro mp4 부재).
    import {AbsoluteFill, interpolate, spring, useCurrentFrame, useVideoConfig} from 'remotion';
    import React from 'react';

    export interface OutroCardProps {
      channelName: string;
      accentColor: string;
      seriesPart?: number | null;
      seriesTotal?: number | null;
    }

    export const OUTRO_DURATION_FRAMES = 90;  // 3s @ 30fps — incidents.md §7 마지막 2-5초

    export const OutroCard: React.FC<OutroCardProps> = ({channelName, accentColor, seriesPart, seriesTotal}) => {
      const frame = useCurrentFrame();
      const {fps} = useVideoConfig();
      const opacity = interpolate(frame, [0, 10, OUTRO_DURATION_FRAMES - 15, OUTRO_DURATION_FRAMES], [0, 1, 1, 0.4]);
      // 탐정 정면 → 뒤돌아 걸어감 패턴 emulate: 캐릭터 오버레이는 TopBar 가 담당, OutroCard 는 text + accent stripe 만
      const textY = spring({frame, fps, config: {damping: 14}, from: 40, to: 0});
      return (
        <AbsoluteFill style={{backgroundColor: '#0A0A0A', justifyContent: 'center', alignItems: 'center'}}>
          <div style={{
            color: accentColor,
            fontFamily: 'BlackHanSans',
            fontSize: 120,
            opacity,
            transform: `translateY(${textY}px)`,
            letterSpacing: 4,
            textAlign: 'center',
          }}>{channelName}</div>
          {seriesPart && seriesTotal && (
            <div style={{
              marginTop: 48,
              color: '#FFFFFF',
              fontFamily: 'DoHyeon',
              fontSize: 64,
              opacity,
              letterSpacing: 2,
            }}>Part {seriesPart} / {seriesTotal}</div>
          )}
          <div style={{
            position: 'absolute',
            bottom: 200,
            left: '10%',
            right: '10%',
            height: 8,
            backgroundColor: accentColor,
            opacity: opacity * 0.7,
          }} />
        </AbsoluteFill>
      );
    };
    ```

    `ShortsVideo.tsx` 수정: visual_spec.clips 마지막 entry 가 `type: "video"` + `src` 에 "outro" 포함 + 파일 부재 시, OutroCard 로 자동 폴백. 실구현은 props-schema 확장 또는 Python (remotion_renderer) 측에서 clipDesign 마지막에 `{"type": "outro_card"}` sentinel 삽입 중 택 1. Plan 16-03 에서는 ShortsVideo.tsx 최소 수정 (OutroCard import + type==="outro_card" branch 추가).

    **Branch B — Option B (episode-local mp4 포팅):**
    Task 0 findings 가 "shorts_naberal 이 outro 생성 모듈 존재" 를 증명하면 해당 모듈을 `.preserved/harvested/` 에서 찾아 `scripts/orchestrator/` 로 포팅. 이는 Plan 16-03 scope 에서 이탈 가능성 높으므로 **대표님 결재 필요** — Option B 선택 시 Plan 16-03 진행을 중단하고 결재 요청.

    **기본값 Option A** (증거 상 가장 가능성 높음 — shorts_naberal output/_shared/signatures/ 에 incidents_outro*.mp4 **부재** 확정, baseline visual_spec 의 outro_signature.mp4 path 는 episode-local 이나 실 파일 존재 여부 미확인).

    Task 0 결론이 Option B 인 경우 본 Task 를 **Option B 분기로 재계획**. 대표님 결재 대기 상태로 Plan 16-03 wave 1 종결.
  </action>
  <acceptance_criteria>
    **Option A 선택 시:**
    - `test -f remotion/src/components/OutroCard.tsx`
    - `wc -l remotion/src/components/OutroCard.tsx` 값 ≥ 60
    - `grep -c "export const OutroCard" remotion/src/components/OutroCard.tsx` 값 == 1
    - `grep -c "OUTRO_DURATION_FRAMES" remotion/src/components/OutroCard.tsx` 값 ≥ 1
    - `grep -c "OutroCard" remotion/src/compositions/ShortsVideo.tsx` 값 ≥ 1 (import 추가)

    **Option B 선택 시:**
    - `.planning/phases/16-production-integration-option-a/16-03-SUMMARY.md` 에 "Option B 결재 대기" 섹션 추가 + Task 중단 명시
  </acceptance_criteria>
  <verify>
    <automated>cd remotion && npx tsc --noEmit 2>&1 | grep -E "error TS" | wc -l | xargs -I{} test {} -eq 0</automated>
  </verify>
  <done>Task 0 결과에 따라 OutroCard.tsx 작성 (Option A) 또는 결재 대기 (Option B) — 산출물 일관.</done>
</task>

</wave>

<wave number="2">

<task id="16-03-W2-TESTS">
  <objective>Phase 16-03 전용 테스트 6종 작성.</objective>
  <read_first>
    - tests/phase16/conftest.py
    - 모든 16-03 W0/W1 산출물
  </read_first>
  <action>
    6개 테스트 파일 작성:

    1. **`tests/phase16/test_outro_research_findings.py`** (≥30 줄) — SUMMARY.md 내 "Task 0 — Outro Research Findings" 섹션 존재 + 3 증거 sub-heading + Option A/B 결정 + evidence log 2 file 존재.

    2. **`tests/phase16/test_harvest_extension.py`** (≥60 줄) — 5 바이너리 파일 전수 존재 + 각 파일 size > 0 + harvest_extension_manifest.json 5 entries + 2 README + shorts_naberal source sha256 변경 없음 (주: source 는 read-only 접근만, 테스트 skip if shorts_naberal 경로 없음).

    3. **`tests/phase16/test_subtitle_producer.py`** (≥100 줄) — `TestSubtitleProducerAgent` class: AGENT.md frontmatter 검증 (name, version, maxTurns, description char count) + `<role>` / `<mandatory_reads>` / `<output_format>` / `<skills>` / `<constraints>` 5 섹션 존재. `TestSubtitleProducerWrapper` class: SubtitleProducer 생성자 (project_root / timeout_s) + produce() method 호출 (subprocess mock) + 3 output file path 생성 + metrics 추출.

    4. **`tests/phase16/test_ins_subtitle_alignment_spec.py`** (≥50 줄) — description + role 에 "faster-whisper" 언급 / "large-v3" 언급 / "subtitle-producer" 언급 / version "1.2" / WhisperX 는 레거시 컨텍스트만 (absolute count ≤ 3).

    5. **`tests/phase16/test_character_overlay_injection.py`** (≥60 줄) — Plan 16-02 `_inject_character_props` 동작 검증 — tmp_path 에 sources/character_{assistant,detective}.png 배치 후 호출 → remotion/public/<job_id>/ 로 복사 + props 에 characterLeftSrc/characterRightSrc 주입. 좌=assistant, 우=detective 의미 고정 검증 (Q4 매핑).

    6. **`tests/phase16/test_signature_reuse_no_veo_call.py`** (≥40 줄) — `.preserved/harvested/video_pipeline_raw/signatures/incidents_intro_v4_silent_glare.mp4` 존재 + 전체 `scripts/orchestrator/` 및 `remotion/` 에 VeoClient / veo_i2v.generate / `import .*veo` 호출 0건 (CLAUDE.md 금기 #11 Veo API 재호출 금지 검증).

    모든 테스트는 conftest fixtures 활용 — 외부 subprocess/파일시스템 mock 으로 hermetic 실행.
  </action>
  <acceptance_criteria>
    - 6 테스트 파일 전수 존재
    - 각 파일 라인수 ≥ 30
    - 6 파일 합계 `pytest --collect-only -q` 테스트 수 ≥ 25
    - 전수 PASS: `pytest tests/phase16/test_outro_research_findings.py tests/phase16/test_harvest_extension.py tests/phase16/test_subtitle_producer.py tests/phase16/test_ins_subtitle_alignment_spec.py tests/phase16/test_character_overlay_injection.py tests/phase16/test_signature_reuse_no_veo_call.py -x` exit 0
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/phase16/ -x --tb=short</automated>
  </verify>
  <done>6 테스트 파일 각 ≥30 줄, 25+ 테스트 green, 16-03 전수 요구사항 검증.</done>
</task>

</wave>

</waves>

<verification>
  Plan 16-03 전체 완료 조건:
  1. `16-03-SUMMARY.md` 의 Task 0 Outro Research Findings 섹션 박제 (3 증거 + Option A/B 결정)
  2. `.preserved/harvested/video_pipeline_raw/{signatures,characters}/` 5 바이너리 파일 + manifest + README + read-only attrib
  3. `.claude/agents/producers/subtitle-producer/AGENT.md` v1.0 신규 (Producer 14 → 15, 33 agents 상한 확장)
  4. `scripts/orchestrator/subtitle/word_subtitle.py` 1697 줄 이식 + header
  5. `scripts/orchestrator/api/subtitle_producer.py` Python wrapper 작성
  6. `ins-subtitle-alignment` description/role v1.2 정정 (faster-whisper large-v3)
  7. OutroCard.tsx 신규 (Option A) 또는 Option B 결재 대기
  8. `pytest tests/phase16/ -x` exit 0
  9. `pytest tests/phase04 tests/phase05 tests/phase06 tests/phase07 --tb=line -q` 986+ regression preserved
  10. `grep -rn "VeoClient\|veo_i2v\.generate" scripts/orchestrator/subtitle/ scripts/orchestrator/api/subtitle_producer.py remotion/src/` 값 == 0 (Veo 금지)
</verification>

<deviations_allowed>
  - **Rule 1 (typo):** agent description 문구 / 주석 오타 / 로그 메시지 수정. `feedback_*` 메모리 파일명 정확성 검증.
  - **Rule 2 (minor variant):** word_subtitle.py 포팅 시 원본의 하드코딩된 shorts_naberal path 를 project_root 기준 상대경로로 교체 허용 (대규모 리팩토링 금지 — line count 1000 유지). AGENT.md 의 skills 컬럼 리스트 조정 허용 (5 공용 skill 중 일부 required → optional). SubtitleProducer wrapper 에 추가 helper method 허용 (단 produce() 시그니처 유지).
  - **Rule 3 (blocker):** Task 0 연구 결과 Option B (episode-local mp4 포팅) 선택 시 Plan 16-03 Wave 1 OUTRO-IMPL Task 중단 + 대표님 결재 요청. 중단 시 16-03-SUMMARY.md 에 명시적 "blocker" 섹션 추가. 다른 Task (harvest, subtitle-producer, ins-subtitle-alignment) 는 계속 진행 가능.
</deviations_allowed>
