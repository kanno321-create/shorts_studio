---
plan: 16-02
phase: 16
name: "Remotion Renderer Infrastructure + ASSEMBLY Gate Branch"
wave: 1
depends_on: []
autonomous: true
requirements: [REQ-PROD-INT-02, REQ-PROD-INT-10, REQ-PROD-INT-14]
files_modified:
  - remotion/package.json
  - remotion/tsconfig.json
  - remotion/remotion.config.ts
  - remotion/.gitignore
  - remotion/README.md
  - remotion/src/index.ts
  - remotion/src/Root.tsx
  - remotion/src/compositions/ShortsVideo.tsx
  - remotion/src/components/BracketCaption.tsx
  - remotion/src/components/OutroCard.tsx
  - remotion/src/components/crime/ContextGraphicScene.tsx
  - remotion/src/components/crime/ImpactCutScene.tsx
  - remotion/src/components/crime/SpeakerSubtitle.tsx
  - remotion/src/components/crime/IconExplainer.tsx
  - remotion/src/components/crime/TimelineCard.tsx
  - remotion/src/components/crime/index.ts
  - remotion/src/lib/fonts.ts
  - remotion/src/lib/props-schema.ts
  - remotion/src/lib/transitions/presentations/glitch.tsx
  - remotion/src/lib/transitions/presentations/rgb-split.tsx
  - remotion/src/lib/transitions/presentations/zoom-blur.tsx
  - remotion/src/lib/transitions/presentations/light-leak.tsx
  - remotion/src/lib/transitions/presentations/clock-wipe.tsx
  - remotion/src/lib/transitions/presentations/pixelate.tsx
  - remotion/src/lib/transitions/presentations/checkerboard.tsx
  - scripts/orchestrator/api/remotion_renderer.py
  - scripts/orchestrator/shorts_pipeline.py
  - tests/phase16/test_remotion_renderer_api.py
  - tests/phase16/test_shorts_props_build.py
  - tests/phase16/test_shorts_props_validate.py
  - tests/phase16/test_assembly_gate_branching.py
  - tests/phase16/test_null_freeze_sentinel.py
  - tests/phase16/test_narration_drives_timing.py
  - tests/phase16/test_post_render_baseline.py
  - .gitignore
estimated_complexity: high
estimated_duration: 2-4h
---

<objective>
Phase 16-02 는 Phase 16 의 핵심 인프라 Plan. 목적: shorts_naberal Remotion 렌더 스택 (`remotion/` TypeScript 프로젝트 + `remotion_render.py` Python bridge) 을 1:1 이식하여 `scripts/orchestrator/api/remotion_renderer.py` 를 만들고, `shorts_pipeline.py:_run_assembly` 를 Shotstack > ffmpeg 2분기에서 Remotion > Shotstack > ffmpeg 3분기로 전환한다. 핵심 계약: `remotion_renderer.render(timeline, resolution, aspect_ratio) -> dict` 시그니처를 `ffmpeg_assembler.FfmpegAssembler.render()` 와 완전 동등 — drop-in replacement. 렌더 후 ffprobe baseline 검증 (width=1080, height=1920, duration≥50s, codec=h264) 을 renderer 내부에서 강제 (Pitfall #1 "spec 통과 ≠ production 통과" 방어).
</objective>

<must_haves>
  - truths:
    - "remotion/ TypeScript 프로젝트가 `npm install` + `npx remotion --version` 실행 성공"
    - "remotion_renderer.py 가 ffmpeg_assembler 와 동일 시그니처로 호출 가능, Remotion CLI subprocess 실행 후 final.mp4 생성"
    - "shorts_pipeline._run_assembly 가 remotion → shotstack → ffmpeg 3분기 cascade 로 작동"
    - "visual_spec.clipDesign[].movement=null 은 `_NULL_FREEZE` sentinel 로 파이프된 뒤 Zod 전 pop 되어 freeze 의도 보전"
    - "렌더 후 ffprobe 검증 실패 시 RuntimeError + GATE FAIL (width≠1080 or height≠1920 or duration<50s)"
    - "Narration ffprobe duration 과 durationInFrames/30 의 차이 ≤ 1.0s 불변"
  - artifacts:
    - path: "remotion/package.json"
      provides: "Remotion 4.x TypeScript 의존성 (remotion@^4.0.0, @remotion/cli, @remotion/transitions@^4.0.445, react@^18.3.1, zod@^4.3.6)"
    - path: "remotion/src/compositions/ShortsVideo.tsx"
      provides: "ShortsVideo Composition (harvested 1:1 복사)"
      min_lines: 500
    - path: "scripts/orchestrator/api/remotion_renderer.py"
      provides: "RemotionRenderer Python bridge (render + prepare_assets + build_shorts_props + validate_shorts_props + get_audio_duration_ffprobe + cleanup + ffprobe baseline 검증)"
      min_lines: 400
    - path: "scripts/orchestrator/shorts_pipeline.py"
      provides: "_run_assembly remotion → shotstack → ffmpeg 3분기 cascade + RemotionRenderer 생성자 연결"
      contains: "self.remotion_renderer"
  - key_links:
    - from: "scripts/orchestrator/shorts_pipeline.py::_run_assembly"
      to: "scripts/orchestrator/api/remotion_renderer.py::RemotionRenderer.render"
      via: "renderer priority cascade (self.remotion_renderer or self.shotstack or self.ffmpeg_assembler)"
      pattern: "self\\.remotion_renderer"
    - from: "scripts/orchestrator/api/remotion_renderer.py"
      to: "remotion/src/index.ts"
      via: "subprocess npx remotion render entry_point=remotion/src/index.ts ShortsVideo"
      pattern: "npx.*remotion.*render"
    - from: "RemotionRenderer.render()"
      to: "ffprobe post-check"
      via: "_verify_production_baseline 내부 호출"
      pattern: "_verify_production_baseline"
</must_haves>

<context>
<interfaces>
<!-- 핵심 계약 — executor 가 복제 기준으로 사용. 전수 `.preserved/harvested/` 에서 1:1 이식 -->

Existing FfmpegAssembler signature (`scripts/orchestrator/api/ffmpeg_assembler.py`):
```python
class FFmpegAssembler:
    def __init__(self, output_dir: Path | None = None, circuit_breaker=None) -> None: ...
    def render(self, timeline: list[TimelineEntry], resolution: str = "hd", aspect_ratio: str = "9:16") -> dict[str, Any]:
        """Returns {"url": "<local_path>", "status": "assembled", ...}"""
    def upscale(self, url: str) -> dict[str, Any]:  # Phase 8 NOOP
        """Returns {"status": "skipped"}"""
```

Target RemotionRenderer signature (`scripts/orchestrator/api/remotion_renderer.py` — NEW):
```python
class RemotionRenderer:
    def __init__(
        self,
        project_root: Path | None = None,  # repo root 탐지, 기본 Path(__file__).resolve().parents[3]
        remotion_dir_name: str = "remotion",
        output_dir: Path | None = None,
        timeout_s: int = 600,
        first_render_timeout_s: int = 180,  # Chrome Headless Shell 첫 다운로드용
    ) -> None:
        """생성자는 node/npx 사용 가능성을 probe 하여 RuntimeError 또는 정상 초기화."""

    def render(self, timeline: list, resolution: str = "fhd", aspect_ratio: str = "9:16") -> dict:
        """ffmpeg_assembler 와 동일 시그니처. returns {"url": final_path_str, "status": "assembled", "renderer": "remotion", "duration_frames": N, "size_mb": X.X}. 내부: prepare_remotion_assets → build_shorts_props → validate_shorts_props → subprocess npx remotion render → cleanup_remotion_assets → _verify_production_baseline."""

    def upscale(self, url: str) -> dict:
        """Phase 8 NOOP. Returns {"status": "skipped", "reason": "remotion native 1080p"}"""
```

Harvested source (read-only SSOT — DO NOT MODIFY):
- `.preserved/harvested/video_pipeline_raw/remotion_render.py` (1162 lines) — Python bridge 원본
- `.preserved/harvested/remotion_src_raw/` — TypeScript 합성 원본
- `.preserved/harvested/baseline_specs_raw/zodiac-killer/visual_spec.json` (138 lines) — props 스키마 레퍼런스

Critical constants (ShortsVideo.tsx DESIGN_SPEC.md 기반 — 수정 절대 금지):
- VIDEO_W = 1080
- VIDEO_H = 1920
- TOP_BAR_H = 320 (캐릭터 좌우 원형 오버레이 + 타이틀)
- BOTTOM_BAR_H = 333 (구독/좋아요 + seriesPart)
- FPS = 30
- codec = h264
</interfaces>
</context>

<waves>

<wave number="0">

<task id="16-02-W0-PROBE">
  <objective>Node.js + npm + ffmpeg + ffprobe 환경 probe 및 `remotion/` 디렉토리 bootstrap (package.json / tsconfig.json / remotion.config.ts / .gitignore / README).</objective>
  <read_first>
    - .planning/phases/16-production-integration-option-a/16-RESEARCH.md §Standard Stack §Environment Availability
    - .preserved/harvested/video_pipeline_raw/remotion_render.py (line 984~1023 subprocess 호출 패턴)
    - .gitignore (기존, remotion/ entry 추가 대상)
    - shorts_naberal/remotion/package.json (참고, 실 경로 `C:/Users/PC/Desktop/shorts_naberal/remotion/package.json` 존재)
  </read_first>
  <action>
    Step 1 — Environment probe (executor bash session):
    ```bash
    node --version      # ≥16.0.0 기대
    npm --version
    npx --version
    ffmpeg -version | head -1
    ffprobe -version | head -1
    ```
    Node 버전이 16 미만이면 `raise NotImplementedError("Node ≥16 required; current=<X>. 대표님께 Node.js upgrade 요청")` 로 Wave 중단. Probe 결과를 `remotion/README.md` 상단 "Environment verified" 섹션에 timestamp 와 함께 기록.

    Step 2 — `remotion/` 디렉토리 생성 + bootstrap 파일 4종:

    `remotion/package.json`:
    ```json
    {
      "name": "shorts-remotion",
      "version": "1.0.0",
      "private": true,
      "description": "Phase 16-02 — naberal-shorts Remotion composition (harvested from shorts_naberal 1:1)",
      "scripts": {
        "studio": "remotion studio src/index.ts",
        "render": "remotion render src/index.ts",
        "render:smoke": "remotion render src/index.ts ShortsVideo out/smoke.mp4 --props=./fixtures/smoke_props.json --codec=h264 --fps=30 --width=1080 --height=1920"
      },
      "dependencies": {
        "@remotion/captions": "^4.0.441",
        "@remotion/cli": "^4.0.0",
        "@remotion/fonts": "^4.0.0",
        "@remotion/google-fonts": "^4.0.441",
        "@remotion/transitions": "^4.0.445",
        "react": "^18.3.1",
        "react-dom": "^18.3.1",
        "remotion": "^4.0.0",
        "zod": "^4.3.6"
      },
      "devDependencies": {
        "@types/react": "^18.3.0",
        "@types/react-dom": "^18.3.0",
        "typescript": "^5.4.0"
      }
    }
    ```

    `remotion/tsconfig.json`:
    ```json
    {
      "compilerOptions": {
        "target": "ES2020",
        "lib": ["ES2020", "DOM"],
        "module": "ESNext",
        "moduleResolution": "bundler",
        "jsx": "react-jsx",
        "strict": true,
        "esModuleInterop": true,
        "skipLibCheck": true,
        "forceConsistentCasingInFileNames": true,
        "resolveJsonModule": true
      },
      "include": ["src"]
    }
    ```

    `remotion/remotion.config.ts`:
    ```ts
    import {Config} from '@remotion/cli/config';
    Config.setVideoImageFormat('jpeg');
    Config.setOverwriteOutput(true);
    Config.setConcurrency(1);  // 로컬 1080p 9:16, CPU 저사양 안정성 우선
    ```

    `remotion/.gitignore`:
    ```
    node_modules/
    dist/
    out/
    public/*
    !public/bgm/
    ```

    `remotion/README.md`:
    ```markdown
    # shorts-remotion — Phase 16-02

    Remotion 4.x TypeScript 프로젝트. `scripts/orchestrator/api/remotion_renderer.py` (Python bridge) 가 `npx remotion render src/index.ts ShortsVideo out.mp4 --props=...` 호출.

    ## Environment verified
    - Node: <probe 결과 기재>
    - npm: <probe 결과 기재>
    - Remotion: installed via `npm install` (Wave 0 Task 16-02-W0-NPM-INSTALL)
    - ffmpeg / ffprobe: system PATH (필수)

    ## 이식 출처
    - `.preserved/harvested/remotion_src_raw/` (TypeScript 소스)
    - `.preserved/harvested/video_pipeline_raw/remotion_render.py` (Python bridge 참고)
    - CLAUDE.md 금기 #11: Veo API 신규 호출 금지 (기존 자산 재사용만)
    ```

    Step 3 — Repo 루트 `.gitignore` 에 append:
    ```
    # Phase 16-02 — Remotion workspace
    remotion/node_modules/
    remotion/out/
    remotion/public/*
    !remotion/public/bgm/
    output/*/remotion_props.json
    .claude/hooks/_adapter_contract_touch.json
    ```
    (마지막 줄은 Plan 14-04 에서 이미 있을 수 있음 — 중복 추가 금지, 기존 라인 존중)
  </action>
  <acceptance_criteria>
    - `test -d remotion && test -f remotion/package.json && test -f remotion/tsconfig.json && test -f remotion/remotion.config.ts && test -f remotion/.gitignore && test -f remotion/README.md`
    - `grep -l '"remotion":\s*"\^4\.0\.0"' remotion/package.json`
    - `grep -l '"@remotion/transitions":\s*"\^4\.0\.445"' remotion/package.json`
    - `grep -c "remotion/node_modules/" .gitignore` 값 ≥ 1
    - `python -c "import json; p=json.load(open('remotion/package.json', encoding='utf-8')); assert p['name']=='shorts-remotion'"` 성공
  </acceptance_criteria>
  <verify>
    <automated>test -f remotion/package.json && test -f remotion/tsconfig.json && python -c "import json; d=json.load(open('remotion/package.json', encoding='utf-8')); assert 'remotion' in d['dependencies']; assert d['dependencies']['remotion'].startswith('^4.')"</automated>
  </verify>
  <done>remotion/ 디렉토리 bootstrap 완료, Node probe 기록, .gitignore 업데이트.</done>
</task>

<task id="16-02-W0-NPM-INSTALL">
  <objective>`remotion/` 에서 `npm install` 실행, Chrome Headless Shell 최초 다운로드 트리거.</objective>
  <read_first>
    - remotion/package.json (16-02-W0-PROBE 산출)
    - .planning/phases/16-production-integration-option-a/16-RESEARCH.md §Pitfall 3 (Chrome timeout)
  </read_first>
  <action>
    Step 1 — `cd remotion && npm install` 실행. 타임아웃 300s (3분). 실패 시 stderr tail 100 줄을 `.planning/phases/16-production-integration-option-a/16-02-npm-install.log` 로 저장 후 RuntimeError.

    Step 2 — `cd remotion && npx remotion --version` probe (타임아웃 180s — Pitfall 3 Chrome Headless Shell 최초 다운 용). 결과를 `.planning/phases/16-production-integration-option-a/16-02-remotion-version.log` 로 저장.

    Step 3 — `.gitignore` 가 `remotion/node_modules/` 를 제외하는지 재확인 (`git status` 에 node_modules 포함 안 되면 OK).
  </action>
  <acceptance_criteria>
    - `test -d remotion/node_modules`
    - `test -d remotion/node_modules/remotion` (Remotion 패키지 설치 확인)
    - `(cd remotion && npx remotion --version) 2>&1 | grep -E "^4\\.[0-9]+\\.[0-9]+"` 출력 존재
    - `cd remotion && git status --porcelain node_modules/ 2>&1 | wc -l` 값 == 0 (gitignore 작동 확인)
  </acceptance_criteria>
  <verify>
    <automated>cd remotion && node -e "console.log(require('remotion/package.json').version)" | grep -E "^4\."</automated>
  </verify>
  <done>npm install 완료, remotion 4.x 확인, Chrome Headless Shell 사용 가능.</done>
</task>

</wave>

<wave number="1">

<task id="16-02-W1-TS-COPY" tdd="false">
  <objective>`.preserved/harvested/remotion_src_raw/` 의 TypeScript 파일 전수를 `remotion/src/` 로 1:1 복사 (읽기 권한만 있는 원본에서 읽어 새 경로로 쓰기).</objective>
  <read_first>
    - .preserved/harvested/remotion_src_raw/index.ts (예상: registerRoot 호출)
    - .preserved/harvested/remotion_src_raw/Root.tsx (313 lines — Composition registry, shorts 용만 유지)
    - .preserved/harvested/remotion_src_raw/compositions/ShortsVideo.tsx (744 lines 이상)
    - .preserved/harvested/remotion_src_raw/components/BracketCaption.tsx
    - .preserved/harvested/remotion_src_raw/components/crime/*.tsx + index.ts
    - .preserved/harvested/remotion_src_raw/lib/fonts.ts
    - .preserved/harvested/remotion_src_raw/lib/props-schema.ts
    - .preserved/harvested/remotion_src_raw/lib/transitions/presentations/*.tsx (7 presentation: glitch, rgb-split, zoom-blur, light-leak, clock-wipe, pixelate, checkerboard)
    - .planning/phases/16-production-integration-option-a/16-RESEARCH.md §Don't Hand-Roll + §Q1 매핑 테이블
  </read_first>
  <action>
    Step 1 — Python script or cp 로 harvested 에서 `remotion/src/` 로 복사:

    ```python
    # Execute via python or bash. No manual typing of 744 lines.
    import shutil
    from pathlib import Path
    src = Path(".preserved/harvested/remotion_src_raw")
    dst = Path("remotion/src")
    # 복사 대상 (Q1 매핑 기반, long-form 용 카드는 제외)
    FILES_SHORTS = [
        "index.ts",
        "Root.tsx",
        "compositions/ShortsVideo.tsx",
        "components/BracketCaption.tsx",
        "components/crime/ContextGraphicScene.tsx",
        "components/crime/ImpactCutScene.tsx",
        "components/crime/SpeakerSubtitle.tsx",
        "components/crime/IconExplainer.tsx",
        "components/crime/TimelineCard.tsx",
        "components/crime/index.ts",
        "lib/fonts.ts",
        "lib/props-schema.ts",
        "lib/transitions/presentations/glitch.tsx",
        "lib/transitions/presentations/rgb-split.tsx",
        "lib/transitions/presentations/zoom-blur.tsx",
        "lib/transitions/presentations/light-leak.tsx",
        "lib/transitions/presentations/clock-wipe.tsx",
        "lib/transitions/presentations/pixelate.tsx",
        "lib/transitions/presentations/checkerboard.tsx",
    ]
    for rel in FILES_SHORTS:
        s = src / rel
        d = dst / rel
        d.parent.mkdir(parents=True, exist_ok=True)
        shutil.copy2(s, d)
        # 16-02 marker comment header 추가 (TypeScript 파일)
        text = d.read_text(encoding="utf-8")
        header = "// Ported from .preserved/harvested/remotion_src_raw — Phase 16-02\n// DO NOT modify layout constants (TOP_BAR_H=320, BOTTOM_BAR_H=333) — DESIGN_SPEC.md 기반\n"
        if not text.startswith("// Ported"):
            d.write_text(header + text, encoding="utf-8")
    ```

    Step 2 — long-form 카드 (TitleCard, IntroCard, OutroCard 등 ShortsVideo 와 무관한 long-form composition) 는 **제외** 확인. `Root.tsx` 가 ShortsVideo 외 다른 composition 을 import 한 경우 registerRoot 에서 long-form composition registration 만 주석 처리 (Shorts-only 스코프 유지). 우리의 Root.tsx 최종본은 다음과 같은 구조:
    ```tsx
    import {registerRoot, Composition} from "remotion";
    import {ShortsVideo, shortsVideoSchema} from "./compositions/ShortsVideo";

    export const RemotionRoot: React.FC = () => (
      <>
        <Composition
          id="ShortsVideo"
          component={ShortsVideo}
          durationInFrames={1800}
          fps={30}
          width={1080}
          height={1920}
          schema={shortsVideoSchema}
          defaultProps={<defaultProps object harvested 에서 복사>}
        />
      </>
    );
    registerRoot(RemotionRoot);
    ```

    장문 composition 파일은 수정 절대 금지 — 복사만. 수정은 header 추가 1회만 허용.

    Step 3 — `remotion/fixtures/` 디렉토리 생성 후 `fixtures/smoke_props.json` 에 `baseline_specs_raw/zodiac-killer/visual_spec.json` 내용 복사 (테스트용 props).

    Step 4 — Outro 관련 처리: `OutroCard.tsx` 는 long-form 이므로 제외. 하지만 Plan 16-03 Task 0 의 outro 연구에서 "Remotion 프로그램적 outro card 필요" 판정이 나올 경우 해당 Plan 에서 별도 신규 작성. 16-02 에서는 복사 대상 아님.
  </action>
  <acceptance_criteria>
    - `test -f remotion/src/compositions/ShortsVideo.tsx`
    - `wc -l remotion/src/compositions/ShortsVideo.tsx` 값 ≥ 500 (harvested 744 lines 이상 + 2-line header)
    - 19개 이식 파일 전수 존재: `for f in remotion/src/index.ts remotion/src/Root.tsx remotion/src/compositions/ShortsVideo.tsx remotion/src/components/BracketCaption.tsx remotion/src/components/crime/ContextGraphicScene.tsx remotion/src/components/crime/ImpactCutScene.tsx remotion/src/components/crime/SpeakerSubtitle.tsx remotion/src/components/crime/IconExplainer.tsx remotion/src/components/crime/TimelineCard.tsx remotion/src/components/crime/index.ts remotion/src/lib/fonts.ts remotion/src/lib/props-schema.ts remotion/src/lib/transitions/presentations/glitch.tsx remotion/src/lib/transitions/presentations/rgb-split.tsx remotion/src/lib/transitions/presentations/zoom-blur.tsx remotion/src/lib/transitions/presentations/light-leak.tsx remotion/src/lib/transitions/presentations/clock-wipe.tsx remotion/src/lib/transitions/presentations/pixelate.tsx remotion/src/lib/transitions/presentations/checkerboard.tsx; do test -f $f || exit 1; done`
    - 각 TypeScript 파일이 `// Ported from .preserved/harvested/remotion_src_raw — Phase 16-02` 헤더로 시작: `grep -l "^// Ported from" remotion/src/compositions/ShortsVideo.tsx`
    - TOP_BAR_H = 320, BOTTOM_BAR_H = 333 상수 보전: `grep -E "TOP_BAR_H\s*=\s*320" remotion/src/compositions/ShortsVideo.tsx` 값 ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>cd remotion && npx tsc --noEmit 2>&1 | tee ../.planning/phases/16-production-integration-option-a/16-02-tsc.log | grep -E "error TS" | wc -l | xargs -I{} test {} -eq 0</automated>
  </verify>
  <done>TypeScript 소스 19 파일 1:1 복사 완료, tsc --noEmit 통과 (0 type errors), DESIGN_SPEC 픽셀 상수 보전.</done>
</task>

<task id="16-02-W1-RENDERER-CORE" tdd="true">
  <objective>`scripts/orchestrator/api/remotion_renderer.py` 작성 — harvested `remotion_render.py:776~1043` (`render_shorts_video`) + `501~773` (`build_shorts_props`) + `255~290` (`validate_shorts_props`) + `293~320` (`get_audio_duration_ffprobe`) + `354~500` (`prepare_remotion_assets`) + `323~353` (`cleanup_remotion_assets`) 미러 + `_verify_production_baseline` 신규 추가.</objective>
  <read_first>
    - .preserved/harvested/video_pipeline_raw/remotion_render.py (1162 lines — 전수 읽기 필수)
    - scripts/orchestrator/api/ffmpeg_assembler.py (기존 — .render() 시그니처 레퍼런스)
    - scripts/orchestrator/voice_first_timeline.py (TimelineEntry / TransitionEntry dataclass)
    - .planning/phases/16-production-integration-option-a/16-RESEARCH.md §Pattern 2 + §Example 1 + §Pitfall 1/3/4/5/6
    - .preserved/harvested/baseline_specs_raw/zodiac-killer/visual_spec.json (props 레퍼런스)
  </read_first>
  <behavior>
    - Test 1 (`test_remotion_renderer_api`): RemotionRenderer(project_root=tmp) 생성 시 node/npx 존재하면 인스턴스, 없으면 RuntimeError; .render(timeline=[], resolution="fhd", aspect_ratio="9:16") 호출 시 dict 반환 with keys {url, status, renderer, duration_frames, size_mb}; renderer=="remotion" 고정.
    - Test 2 (`test_shorts_props_build`): build_shorts_props(script, channel="incidents", assets={"audioSrc":"narration.mp3"}, subtitle_json_path=..., audio_duration=60.0, blueprint={"title_display":{"line1":"50년 미제","line2":"암호 살인마","accent_words":["미제","살인마"],"accent_color":"#FF2200"}}, visual_spec_provided=None) → dict with titleLine1/titleLine2/titleKeywords/accentColor/channelName/subtitles/durationInFrames(=1800)/clips/fontFamily; durationInFrames 는 int (not float).
    - Test 3 (`test_shorts_props_validate`): validate_shorts_props(props) — audioSrc 빈 문자열 시 ValueError; durationInFrames 가 float 일 경우 ValueError; subtitles 가 None 일 경우 ValueError; 모든 필수 필드 통과 시 None 반환.
    - Test 4 (`test_null_freeze_sentinel`): visual_spec.clipDesign[i].movement=null (JSON null = Python None) → 중간 sentinel `_NULL_FREEZE` 거쳐 → 최종 props["clips"][i] 에서 movement key 가 완전히 pop (key 자체 없음).
    - Test 5 (`test_narration_drives_timing`): audio_duration=60.5s → durationInFrames=1815 (60.5*30). ffprobe 실측 - caller audio_duration 차이 > 1.0s 시 ffprobe 값 우선 (logger.warning 발생).
    - Test 6 (`test_post_render_baseline`): 가짜 final.mp4 fixture (tests/phase16/fixtures/sample_1080x1920_60s.mp4, 없으면 ffprobe mock 으로 stubbing) → _verify_production_baseline 호출 시 width≠1080 / height≠1920 / duration<50 각 경우 RuntimeError.
  </behavior>
  <action>
    `scripts/orchestrator/api/remotion_renderer.py` 신규 작성. 최소 400 lines. 구조 outline:

    ```python
    """RemotionRenderer — Python ↔ Remotion CLI bridge for the ASSEMBLY gate.

    Ported from .preserved/harvested/video_pipeline_raw/remotion_render.py (1162 lines).
    Signature mirrors scripts/orchestrator/api/ffmpeg_assembler.FFmpegAssembler.render()
    so the ASSEMBLY gate can swap renderers transparently.

    Phase 16-02 — REQ-PROD-INT-02 / REQ-PROD-INT-10 / REQ-PROD-INT-14.
    """
    from __future__ import annotations

    import json
    import logging
    import os
    import shutil
    import subprocess
    import sys
    import time
    from dataclasses import asdict, is_dataclass
    from pathlib import Path
    from typing import Any

    __all__ = ["RemotionRenderer", "RemotionUnavailable", "RemotionValidationError", "RemotionBaselineError"]

    logger = logging.getLogger(__name__)

    DEFAULT_OUTPUT_DIR = Path("outputs/remotion")
    DEFAULT_SUBPROCESS_TIMEOUT_S = 600
    FIRST_RENDER_TIMEOUT_S = 180
    TARGET_RESOLUTION = (1080, 1920)
    TARGET_FPS = 30
    MIN_PRODUCTION_DURATION_S = 60.0  # ROADMAP Phase 16 SC#4 (incidents.md §2 단편 하한 50s 는 legacy; Phase 16 production = 시그니처+오버레이+단어자막 포함 60s minimum)
    SUBTITLE_COVERAGE_MIN = 0.95
    MIN_SCENE_CLIPS = 1
    VALID_MOVEMENTS = frozenset({"zoom_in", "zoom_out", "pan_left", "pan_right"})
    NULL_FREEZE_SENTINEL = "_NULL_FREEZE"  # Pitfall 4 mitigation


    class RemotionUnavailable(RuntimeError):
        """node/npx 없음 또는 remotion 디렉토리 부재 — 생성자에서 발생."""


    class RemotionValidationError(ValueError):
        """validate_shorts_props / pre-render quality gate 실패."""


    class RemotionBaselineError(RuntimeError):
        """_verify_production_baseline 실패 — ffprobe 로 확인된 final.mp4 가 production baseline 미달."""


    class RemotionRenderer:
        def __init__(
            self,
            project_root: Path | None = None,
            remotion_dir_name: str = "remotion",
            output_dir: Path | None = None,
            timeout_s: int = DEFAULT_SUBPROCESS_TIMEOUT_S,
            first_render_timeout_s: int = FIRST_RENDER_TIMEOUT_S,
        ) -> None:
            self.project_root = (project_root or Path(__file__).resolve().parents[3]).absolute()
            self.remotion_dir = self.project_root / remotion_dir_name
            self.output_dir = output_dir or (self.project_root / DEFAULT_OUTPUT_DIR)
            self.timeout_s = timeout_s
            self.first_render_timeout_s = first_render_timeout_s
            self._is_first_render = True
            # Probe: node + remotion 디렉토리 확인
            if shutil.which("node") is None:
                raise RemotionUnavailable("node not on PATH — RemotionRenderer disabled")
            if not self.remotion_dir.exists() or not (self.remotion_dir / "package.json").exists():
                raise RemotionUnavailable(f"remotion 디렉토리 부재: {self.remotion_dir}")
            if shutil.which("ffprobe") is None:
                raise RemotionUnavailable("ffprobe not on PATH — baseline 검증 불가")
            self.output_dir.mkdir(parents=True, exist_ok=True)

        # ------ 공개 API (ffmpeg_assembler 와 동일 시그니처) ------

        def render(
            self,
            timeline: list,
            resolution: str = "fhd",
            aspect_ratio: str = "9:16",
        ) -> dict[str, Any]:
            """ASSEMBLY gate 메인 진입점. timeline 으로부터 visual_spec 도출 + Remotion CLI 호출 + baseline 검증."""
            job_id = f"render_{int(time.time() * 1000)}"
            episode_dir = self.output_dir / job_id
            episode_dir.mkdir(parents=True, exist_ok=True)

            # 1. timeline → visual_spec + assets + audio_duration 도출
            #    (Phase 16-02 초기 구현: timeline 에 ctx.artifacts 로부터 전달된
            #    visual_spec.json 경로와 audio path 가 포함됐다고 가정.
            #    호출측 _run_assembly 가 VoiceFirstTimeline.align() 결과에 추가 필드로 주입.)
            assets, audio_duration, script, blueprint, visual_spec_path, subtitle_json = self._extract_from_timeline(timeline, episode_dir)

            # 2. prepare_remotion_assets: remotion/public/<job_id>/ 에 복사
            self._prepare_remotion_assets(job_id, assets, script.get("series"))

            # 3. get_audio_duration_ffprobe: caller 주장 duration 검증
            audio_path = assets.get("audioSrc_abs")
            if audio_path:
                ffprobe_duration = self._get_audio_duration_ffprobe(audio_path)
                if ffprobe_duration > 0 and abs(ffprobe_duration - audio_duration) > 1.0:
                    logger.warning("audio duration 오차 %.3fs (caller=%s, ffprobe=%s) — ffprobe 값 우선", abs(ffprobe_duration-audio_duration), audio_duration, ffprobe_duration)
                    audio_duration = ffprobe_duration  # Narration Drives Timing

            # 4. Pre-render quality gates
            self._pre_render_quality_gates(assets.get("scene_clips", []), subtitle_json, audio_duration)

            # 5. build_shorts_props + validate + character inject
            props = self._build_shorts_props(
                script=script,
                channel="incidents",
                assets=assets,
                subtitle_json_path=subtitle_json,
                audio_duration=audio_duration,
                blueprint=blueprint,
                visual_spec_path=visual_spec_path,
            )
            self._inject_character_props(props, job_id, episode_dir)
            self._validate_shorts_props(props)

            # 6. props 를 파일로 저장 (subprocess 에 전달)
            props_path = episode_dir / "remotion_props.json"
            props_path.write_text(json.dumps(props, ensure_ascii=False, indent=2), encoding="utf-8")

            # 7. subprocess: npx remotion render
            output_mp4 = episode_dir / "final.mp4"
            self._invoke_remotion_cli(props_path, output_mp4)

            # 8. cleanup remotion/public/<job_id>/
            self._cleanup_remotion_assets(job_id)

            # 9. Baseline verification (Pitfall 1 방어)
            meta = self._verify_production_baseline(output_mp4)

            return {
                "url": str(output_mp4),
                "status": "assembled",
                "renderer": "remotion",
                "duration_frames": int(props["durationInFrames"]),
                "size_mb": round(output_mp4.stat().st_size / 1024 / 1024, 2),
                "ffprobe": meta,
            }

        def upscale(self, url: str) -> dict[str, Any]:
            """Phase 8 NOOP — Remotion 은 native 1080p 이므로 upscale 불필요."""
            return {"status": "skipped", "reason": "remotion native 1080p"}

        # ------ 내부: harvested remotion_render.py 미러 ------

        def _get_audio_duration_ffprobe(self, audio_path: str | Path) -> float:
            """remotion_render.py:293~320 미러. 실패 시 0.0 반환."""
            ...

        def _prepare_remotion_assets(self, job_id: str, assets: dict, series: dict | None) -> None:
            """remotion_render.py:354~500 미러. 영상/이미지 clips + audio + bgm 을 remotion/public/<job_id>/ 에 복사."""
            ...

        def _build_shorts_props(
            self,
            script: dict,
            channel: str,
            assets: dict,
            subtitle_json_path: Path | None,
            audio_duration: float,
            blueprint: dict,
            visual_spec_path: Path | None,
        ) -> dict:
            """remotion_render.py:501~773 미러. visual_spec.clipDesign[] 이 있으면 round-robin 배정 비활성화
            (§Pattern 3, §Pitfall 4). movement=null → NULL_FREEZE_SENTINEL 로 파이프, 최종 Zod 전에 pop."""
            ...
            # 최종 단계 (Pitfall 4 방어):
            for rc in remotion_clips:
                if rc.get("movement") == NULL_FREEZE_SENTINEL:
                    rc.pop("movement", None)

        def _validate_shorts_props(self, props: dict) -> None:
            """remotion_render.py:255~290 미러. Zod schema 호환 사전 검증."""
            if not isinstance(props.get("audioSrc"), str) or not props["audioSrc"]:
                raise RemotionValidationError("audioSrc 누락 또는 빈 문자열")
            if not isinstance(props.get("titleLine1"), str) or not props["titleLine1"]:
                raise RemotionValidationError("titleLine1 누락")
            if not isinstance(props.get("channelName"), str) or not props["channelName"]:
                raise RemotionValidationError("channelName 누락")
            if not isinstance(props.get("durationInFrames"), int) or props["durationInFrames"] <= 0:
                raise RemotionValidationError(f"durationInFrames 는 positive int 여야 함, got {type(props.get('durationInFrames')).__name__}={props.get('durationInFrames')}")
            if not isinstance(props.get("subtitles"), list):
                raise RemotionValidationError("subtitles 는 list 여야 함")

        def _pre_render_quality_gates(self, scene_clips: list, subtitle_json: Path | None, audio_duration: float) -> None:
            if len(scene_clips) < MIN_SCENE_CLIPS:
                raise RemotionValidationError(f"scene_clips 부족: {len(scene_clips)} < {MIN_SCENE_CLIPS}")
            if subtitle_json and subtitle_json.exists():
                subs = json.loads(subtitle_json.read_text(encoding="utf-8"))
                if subs and audio_duration > 0:
                    last_end_ms = max((c.get("endMs", 0) for c in subs), default=0)
                    coverage = (last_end_ms / 1000) / audio_duration
                    if coverage < SUBTITLE_COVERAGE_MIN:
                        raise RemotionValidationError(f"자막 coverage {coverage:.2%} < {SUBTITLE_COVERAGE_MIN:.0%}")

        def _inject_character_props(self, props: dict, job_id: str, episode_dir: Path) -> None:
            """remotion_render.py:942~976 미러. sources/character_assistant.png + character_detective.png 가 있으면 remotion/public/<job_id>/ 로 복사 + props 주입."""
            public_dir = self.remotion_dir / "public" / job_id
            char_left = episode_dir / "sources" / "character_assistant.png"
            char_right = episode_dir / "sources" / "character_detective.png"
            if char_left.exists():
                public_dir.mkdir(parents=True, exist_ok=True)
                shutil.copy2(char_left, public_dir / "character_left.png")
                props["characterLeftSrc"] = f"{job_id}/character_left.png"
            if char_right.exists():
                public_dir.mkdir(parents=True, exist_ok=True)
                shutil.copy2(char_right, public_dir / "character_right.png")
                props["characterRightSrc"] = f"{job_id}/character_right.png"

        def _invoke_remotion_cli(self, props_path: Path, output_path: Path) -> None:
            """remotion_render.py:984~1023 미러. npx remotion render subprocess."""
            entry = (self.remotion_dir / "src" / "index.ts").as_posix()
            cmd = [
                "npx", "remotion", "render",
                entry,
                "ShortsVideo",
                output_path.as_posix(),
                f"--props={props_path.absolute().as_posix()}",
                "--codec=h264",
                "--fps=30",
                "--width=1080",
                "--height=1920",
            ]
            use_shell = sys.platform == "win32"
            timeout = self.first_render_timeout_s if self._is_first_render else self.timeout_s
            result = subprocess.run(
                cmd,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=timeout,
                cwd=self.remotion_dir,
                shell=use_shell,
            )
            self._is_first_render = False
            if result.returncode != 0:
                stderr_tail = (result.stderr or "")[-1000:]
                raise RuntimeError(f"Remotion render failed (exit {result.returncode}): {stderr_tail}")

        def _cleanup_remotion_assets(self, job_id: str) -> None:
            """remotion_render.py:323~353 미러. remotion/public/<job_id>/ 삭제."""
            public_dir = self.remotion_dir / "public" / job_id
            if public_dir.exists():
                shutil.rmtree(public_dir, ignore_errors=True)

        def _verify_production_baseline(self, final_path: Path) -> dict:
            """Pitfall 1 방어 — ffprobe 로 production baseline 검증."""
            if not final_path.exists():
                raise RemotionBaselineError(f"final.mp4 미생성: {final_path}")
            probe = subprocess.run(
                ["ffprobe", "-v", "error", "-show_format", "-show_streams", "-of", "json", str(final_path)],
                capture_output=True, text=True, encoding="utf-8", errors="replace", timeout=30,
            )
            if probe.returncode != 0:
                raise RemotionBaselineError(f"ffprobe 실패: {probe.stderr[-300:]}")
            info = json.loads(probe.stdout)
            v_streams = [s for s in info.get("streams", []) if s.get("codec_type") == "video"]
            if not v_streams:
                raise RemotionBaselineError("video stream 없음")
            v = v_streams[0]
            width = int(v.get("width", 0))
            height = int(v.get("height", 0))
            codec = v.get("codec_name", "")
            duration = float(info.get("format", {}).get("duration", 0.0))
            if (width, height) != TARGET_RESOLUTION:
                raise RemotionBaselineError(f"해상도 불량: {width}x{height} (expected 1080x1920)")
            if duration < MIN_PRODUCTION_DURATION_S:
                raise RemotionBaselineError(f"영상 길이 불량: {duration:.2f}s < {MIN_PRODUCTION_DURATION_S}s baseline (ROADMAP Phase 16 SC#4)")
            if codec not in ("h264", "hevc"):
                raise RemotionBaselineError(f"codec 불량: {codec}")
            return {"width": width, "height": height, "codec": codec, "duration": duration}

        def _extract_from_timeline(self, timeline, episode_dir: Path):
            """timeline (VoiceFirstTimeline.align() 결과) + ctx.artifacts 에서 remotion 렌더에 필요한 정보 추출.
            Phase 16-02 초기 구현: timeline 각 entry 에 `visual_spec_path` / `audio_path` / `subtitle_json_path` 가
            추가 필드로 주입됐다고 가정 (실 wiring 은 Wave 2 Task 16-02-W2-ASSEMBLY).
            """
            ...
    ```

    TDD 순서:
    1. `tests/phase16/test_remotion_renderer_api.py` 작성 (Test 1) → RED
    2. `scripts/orchestrator/api/remotion_renderer.py` 기본 __init__ 작성 → GREEN
    3. Test 2~6 하나씩 RED → GREEN 순서로 구현

    보안: 모든 subprocess.run 에 `timeout=` 인자 필수, `shell=(sys.platform=="win32")` 명시.

    파일 길이 최소 400 줄. 단순 wrapper 가 아니라 harvested 1162 줄의 **핵심 경로** 를 전수 이식 — `_build_shorts_props` + `_prepare_remotion_assets` 2개 함수가 각 100 줄 이상 차지 예상.
  </action>
  <acceptance_criteria>
    - `test -f scripts/orchestrator/api/remotion_renderer.py`
    - `wc -l scripts/orchestrator/api/remotion_renderer.py` 값 ≥ 400
    - `grep -c "^class RemotionRenderer" scripts/orchestrator/api/remotion_renderer.py` 값 == 1
    - `grep -c "def render(self" scripts/orchestrator/api/remotion_renderer.py` 값 == 1
    - `grep -c "def _verify_production_baseline" scripts/orchestrator/api/remotion_renderer.py` 값 == 1
    - `grep -c "_NULL_FREEZE" scripts/orchestrator/api/remotion_renderer.py` 값 ≥ 2
    - `grep -c "def _build_shorts_props\|def _validate_shorts_props\|def _prepare_remotion_assets\|def _cleanup_remotion_assets\|def _get_audio_duration_ffprobe\|def _invoke_remotion_cli\|def _inject_character_props\|def _pre_render_quality_gates" scripts/orchestrator/api/remotion_renderer.py` 값 == 8
    - `python -c "from scripts.orchestrator.api.remotion_renderer import RemotionRenderer, RemotionUnavailable, RemotionValidationError, RemotionBaselineError"` import 성공
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/phase16/test_remotion_renderer_api.py tests/phase16/test_shorts_props_build.py tests/phase16/test_shorts_props_validate.py tests/phase16/test_null_freeze_sentinel.py tests/phase16/test_narration_drives_timing.py tests/phase16/test_post_render_baseline.py -x --tb=short</automated>
  </verify>
  <done>remotion_renderer.py 작성 완료, 8 private helper + 2 public method (render + upscale), 6 TDD 테스트 전수 GREEN.</done>
</task>

</wave>

<wave number="2">

<task id="16-02-W2-ASSEMBLY">
  <objective>`scripts/orchestrator/shorts_pipeline.py:_run_assembly` 에 RemotionRenderer 3분기 cascade 연결. 생성자 (`__init__`) 에 `self.remotion_renderer` 초기화 추가 (ffmpeg_assembler 와 동일 패턴).</objective>
  <read_first>
    - scripts/orchestrator/shorts_pipeline.py (841 lines, 전수 읽기, 특히 244~270 line 생성자 + 515~563 _run_assembly)
    - scripts/orchestrator/api/remotion_renderer.py (Wave 1 산출)
    - scripts/orchestrator/api/ffmpeg_assembler.py (기존 패턴)
    - .planning/phases/16-production-integration-option-a/16-RESEARCH.md §Pattern 1
  </read_first>
  <action>
    Step 1 — `shorts_pipeline.py` 생성자 (`__init__`) 수정. 기존 `self.shotstack` / `self.ffmpeg_assembler` 초기화 블록 다음에 `self.remotion_renderer` 추가:

    기존 (line 244~266 근처):
    ```python
    self.shotstack = shotstack_adapter or _try_adapter(...)
    self.ffmpeg_assembler = None
    if self.shotstack is None:
        try:
            self.ffmpeg_assembler = FFmpegAssembler()
        except ...:
            ...
    self.timeline = VoiceFirstTimeline()
    ```

    신규 (Remotion 우선 시도, 실패 시 기존 cascade 유지):
    ```python
    # Phase 16-02 — RemotionRenderer as priority-1 ASSEMBLY renderer.
    # Falls back to Shotstack → ffmpeg_assembler when node/remotion 미사용.
    self.remotion_renderer = None
    try:
        from .api.remotion_renderer import RemotionRenderer, RemotionUnavailable
        self.remotion_renderer = RemotionRenderer()
        logger.info("[pipeline] remotion_renderer 활성 (Phase 16-02 production path)")
    except RemotionUnavailable as e:
        logger.warning("[pipeline] remotion_renderer 비활성 (%s) — Shotstack/ffmpeg fallback 사용", e)
    except Exception as e:  # noqa: BLE001 — 광범위 catch 허용 (생성자 방어)
        logger.warning("[pipeline] remotion_renderer 초기화 실패 (%s) — fallback", e)
    ```

    Step 2 — `_run_assembly` (line 515) 수정. 기존 `renderer = self.shotstack if self.shotstack is not None else self.ffmpeg_assembler` 를 3분기 cascade 로 변경:

    ```python
    def _run_assembly(self, ctx: GateContext) -> None:
        """GATE 9 — VoiceFirstTimeline + Remotion/Shotstack/FFmpeg cascade (ORCH-10/11).

        Phase 16-02 — Remotion 1순위. Chrome Headless Shell + node 요구.
        """
        timeline = self.timeline.align(ctx.audio_segments, ctx.video_cuts)
        timeline = self.timeline.insert_transition_shots(timeline)

        # Phase 16-02 priority cascade: remotion > shotstack > ffmpeg
        renderer = (
            self.remotion_renderer
            or self.shotstack
            or self.ffmpeg_assembler
        )
        if renderer is None:
            raise RuntimeError(
                "ASSEMBLY renderer 미구성 (대표님) — Remotion/Shotstack/ffmpeg_assembler 모두 없음"
            )

        # Remotion 경로는 circuit breaker 불필요 (local subprocess, rate limit 없음)
        if renderer is self.remotion_renderer:
            logger.info("[ASSEMBLY] renderer=remotion (Phase 16-02)")
            render_result = renderer.render(
                timeline, resolution="fhd", aspect_ratio="9:16"
            )
        elif renderer is self.shotstack:
            render_result = self.shotstack_breaker.call(
                lambda: renderer.render(timeline, resolution="hd", aspect_ratio="9:16")
            )
        else:
            # ffmpeg local: breaker 우회, 1080p 직접 출력
            render_result = renderer.render(
                timeline, resolution="fhd", aspect_ratio="9:16"
            )

        render_url = (render_result or {}).get("url", "")
        ctx.artifacts[GateName.ASSEMBLY] = Path(render_url) if render_url else None

        # upscale NOOP (Remotion native 1080p, Shotstack/ffmpeg 도 Phase 8 이후 NOOP)
        renderer.upscale(render_url)

        # Phase 16-02 — renderer metadata tracking
        if render_result:
            render_result.setdefault("renderer", getattr(renderer, "__class__", type(renderer)).__name__.lower())

        verdict = self.supervisor_invoker(GateName.ASSEMBLY, render_result)
        artifact = ctx.artifacts[GateName.ASSEMBLY]
        artifact_path = (
            artifact
            if isinstance(artifact, Path) and artifact.exists()
            else None
        )
        self.gate_guard.dispatch(
            GateName.ASSEMBLY, verdict, artifact_path=artifact_path
        )
    ```

    Step 3 — 기존 `shotstack_adapter=self.shotstack` 대입 블록 (line 690 근처 `_prod_inputs` 등) 이 존재하는 경우, `remotion_renderer=self.remotion_renderer` 도 필요 시 추가. 단 기존 동작 유지가 원칙 — 추가는 Plan 16-03/16-04 가 실제 소비할 때 진행 (이번 Plan 은 ASSEMBLY 분기만).

    Step 4 — 파일 전체 라인수 500~800 범위 유지 확인 (CLAUDE.md 필수 #3). `wc -l shorts_pipeline.py` 결과가 841 line → 약 20~30 line 추가 예상, 여유분 있음.
  </action>
  <acceptance_criteria>
    - `grep -c "self\.remotion_renderer" scripts/orchestrator/shorts_pipeline.py` 값 ≥ 3
    - `grep -c "from .api.remotion_renderer import RemotionRenderer" scripts/orchestrator/shorts_pipeline.py` 값 == 1
    - `grep -c "renderer is self\.remotion_renderer" scripts/orchestrator/shorts_pipeline.py` 값 == 1
    - `grep -c "Remotion/Shotstack/ffmpeg_assembler 모두 없음" scripts/orchestrator/shorts_pipeline.py` 값 == 1
    - `wc -l scripts/orchestrator/shorts_pipeline.py` 값 ≤ 900 (500~800 + 약간의 cushion)
    - `python -c "from scripts.orchestrator.shorts_pipeline import ShortsPipeline"` import 성공
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/phase16/test_assembly_gate_branching.py tests/phase07/test_operational_gate_count_equals_13.py -x --tb=short</automated>
  </verify>
  <done>_run_assembly 3분기 cascade 완료, 기존 Phase 7 regression green, 신규 test_assembly_gate_branching 통과.</done>
</task>

<task id="16-02-W2-TESTS">
  <objective>Wave 1/2 에 대응하는 pytest 테스트 7종 작성 (TDD 순서에서 Wave 1 이 테스트 먼저 작성했으므로, 여기서는 누락된 추가 시나리오 + ASSEMBLY branching 테스트 완성).</objective>
  <read_first>
    - tests/phase16/conftest.py (16-01-W0 산출)
    - scripts/orchestrator/api/remotion_renderer.py (Wave 1)
    - scripts/orchestrator/shorts_pipeline.py (Wave 2 수정 후)
    - .preserved/harvested/baseline_specs_raw/zodiac-killer/visual_spec.json
  </read_first>
  <action>
    다음 테스트 파일 7종 작성 (Wave 1 TDD 에서 작성된 파일의 완전화 — 각 파일 100줄+ 실 테스트):

    1. `tests/phase16/test_remotion_renderer_api.py` — RemotionRenderer.__init__ node 없을 때 RemotionUnavailable, 정상 시 인스턴스; render() 반환 dict keys; upscale() NOOP.
    2. `tests/phase16/test_shorts_props_build.py` — zodiac_visual_spec fixture 로 _build_shorts_props 호출 → titleLine1/titleLine2 정확히 매칭, durationInFrames=audio_duration*30 (int), titleKeywords 배열 구조, visual_spec_path 제공 시 clipDesign 보전.
    3. `tests/phase16/test_shorts_props_validate.py` — audioSrc 빈 문자열 / durationInFrames float / subtitles None 각각 RemotionValidationError.
    4. `tests/phase16/test_null_freeze_sentinel.py` — visual_spec.clipDesign 에 movement=null / movement="zoom_in" / movement="invalid" 3 케이스 → 최종 props.clips 에서 null 과 invalid 는 movement key pop, zoom_in 은 보전.
    5. `tests/phase16/test_narration_drives_timing.py` — ffprobe mock 으로 30.5s 반환, caller audio_duration=60.0 → warning log + audio_duration 이 30.5 로 갱신 (> 1.0s 차이).
    6. `tests/phase16/test_post_render_baseline.py` — ffprobe subprocess mock. 케이스: 1080x1920 h264 60s → success; 720x1280 → RemotionBaselineError; 1080x1920 40s → RemotionBaselineError (MIN=50); 1080x1920 h264 duration 누락 → RemotionBaselineError.
    7. `tests/phase16/test_assembly_gate_branching.py` — ShortsPipeline 생성자 mocking (remotion_renderer / shotstack / ffmpeg_assembler 각 조합) → _run_assembly 호출 시 올바른 renderer 선택 검증 (remotion 있으면 remotion, 없으면 shotstack, 둘 다 없으면 ffmpeg, 전부 없으면 RuntimeError).

    각 테스트 파일은 `tests/phase16/conftest.py` 의 fixture 사용. subprocess 는 `monkeypatch.setattr("subprocess.run", ...)` 로 mocking.

    TDD marker: Wave 1 에서 RED 상태로 먼저 생성된 파일들은 Wave 2 에서 **완전 확장** 됨 (edge case + mock 주입 정교화). 최소 각 파일 5 test function.
  </action>
  <acceptance_criteria>
    - 7개 테스트 파일 전수 존재
    - 각 테스트 파일 라인수 ≥ 80: `for f in tests/phase16/test_remotion_renderer_api.py tests/phase16/test_shorts_props_build.py tests/phase16/test_shorts_props_validate.py tests/phase16/test_null_freeze_sentinel.py tests/phase16/test_narration_drives_timing.py tests/phase16/test_post_render_baseline.py tests/phase16/test_assembly_gate_branching.py; do [ "$(wc -l < $f)" -ge 80 ] || exit 1; done`
    - 전체 테스트 수 ≥ 35: `pytest tests/phase16/test_remotion_renderer_api.py tests/phase16/test_shorts_props_build.py tests/phase16/test_shorts_props_validate.py tests/phase16/test_null_freeze_sentinel.py tests/phase16/test_narration_drives_timing.py tests/phase16/test_post_render_baseline.py tests/phase16/test_assembly_gate_branching.py --collect-only -q | tail -1` 값 ≥ 35
    - `pytest tests/phase16/ -x --tb=short` exit code 0
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/phase16/ -x --tb=short && pytest tests/phase04 tests/phase05 tests/phase06 tests/phase07 --tb=line -q 2>&1 | tail -5</automated>
  </verify>
  <done>7 테스트 파일 각 ≥80줄, Phase 16 테스트 ≥35 green, Phase 4-7 regression preserved.</done>
</task>

<task id="16-02-W2-SUMMARY">
  <objective>Plan 16-02 완료 SUMMARY 박제 — ROADMAP Phase 16 SC#1 "4/4 Plan SUMMARY 완료" 중 2/4 달성 + SC#2 986+ regression + SC#4 ffprobe baseline 결과 anchor.</objective>
  <read_first>
    - .planning/phases/16-production-integration-option-a/16-02-PLAN.md (본 Plan 전문)
    - .planning/phases/16-production-integration-option-a/16-VALIDATION.md (16-02 task 검증 결과)
    - .planning/phases/16-production-integration-option-a/16-RESEARCH.md (Standard Stack / Architecture Patterns / Don't Hand-Roll)
    - .planning/ROADMAP.md (Phase 16 SC#1~5)
    - scripts/orchestrator/api/remotion_renderer.py (Plan 16-02 주요 산출)
    - scripts/orchestrator/shorts_pipeline.py (ASSEMBLY gate 분기 변경 확인)
    - remotion/package.json (TypeScript infra 확인)
  </read_first>
  <action>
    `.planning/phases/16-production-integration-option-a/16-02-SUMMARY.md` 신규 작성. 최소 40 줄 (16-02 는 infra 규모 크므로 16-01 보다 상세).

    필수 frontmatter:
    ```yaml
    ---
    plan: 16-02
    phase: 16
    name: "Remotion Renderer Infrastructure + ASSEMBLY Gate Branch"
    status: complete
    completed: <YYYY-MM-DD>
    wave_count: 3
    task_count: 6
    requirements: [REQ-PROD-INT-02, REQ-PROD-INT-10, REQ-PROD-INT-14]
    ---
    ```

    필수 섹션 8개:
    1. `## Deliverables` — `remotion/` TypeScript 디렉토리 (package.json + tsconfig.json + remotion.config.ts + src/Root.tsx + src/compositions/ShortsVideo.tsx + 7 transitions + 3 crime components), `scripts/orchestrator/api/remotion_renderer.py` (line count), `shorts_pipeline.py` diff (ASSEMBLY 분기 +라인), 신규 `RemotionBaselineError` exception class, `tests/phase16/test_remotion_*.py` 7개 파일.
    2. `## Tests` — `pytest tests/phase16/test_remotion_renderer_api.py test_shorts_props_build.py test_shorts_props_validate.py test_null_freeze_sentinel.py test_narration_drives_timing.py test_post_render_baseline.py test_assembly_gate_branching.py -x` 결과 (예상 ≥35 테스트).
    3. `## Regression` — Phase 04/05/06/07 전수 green (ASSEMBLY gate 분기가 기존 shotstack/ffmpeg path 무손상 확인). `pytest tests/phase07/test_operational_gate_count_equals_13.py` (13 GATE 무변).
    4. `## Production Baseline (SC#4 evidence)` — `remotion_renderer._verify_production_baseline(...)` 실제 실행 결과 + ffprobe output: width/height/codec/duration. **MIN_PRODUCTION_DURATION_S = 60.0** 적용 확인.
    5. `## Deviations` — Wave 1/2 중 발생한 Rule 1/2/3 이벤트 + 처리. Remotion smoke 실패 시 fallback cascade 동작 보고.
    6. `## Lessons` — Phase 16-03 (subtitle + signature) / 16-04 (visual_spec) 에 전달할 교훈 (예: "VisualSpec Pydantic 모델은 Plan 16-04 Wave 0 에서 작성 → 16-02 에서 import. Wave 0 시점 미생성이면 forward reference 처리").
    7. `## Evidence` — commit SHA(s), Remotion 빌드 로그 anchor, ASSEMBLY gate 분기 전후 shorts_pipeline.py 라인 수 (500~800줄 제약 준수 확인 — 현재 841 기준 ±30 라인 증가 cushion).
    8. `## SC Coverage` — SC#1 (2/4) + SC#2 (986+ regression green) + SC#3 (Phase 16 테스트 기여 +35 누계) + SC#4 (ffprobe baseline 1080×1920/≥60s/h264 검증 통과) + SC#5 (재발 방지 guard 구축).

    commit: `docs(16-02): plan summary`.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/16-production-integration-option-a/16-02-SUMMARY.md`
    - `wc -l < .planning/phases/16-production-integration-option-a/16-02-SUMMARY.md` ≥ 40
    - `grep -c "^## " .planning/phases/16-production-integration-option-a/16-02-SUMMARY.md` ≥ 8
    - `grep -q "^plan: 16-02$" .planning/phases/16-production-integration-option-a/16-02-SUMMARY.md`
    - `grep -q "^status: complete$" .planning/phases/16-production-integration-option-a/16-02-SUMMARY.md`
    - `grep -q "REQ-PROD-INT-02" .planning/phases/16-production-integration-option-a/16-02-SUMMARY.md`
    - `grep -q "MIN_PRODUCTION_DURATION_S = 60.0\|≥60s\|60s baseline" .planning/phases/16-production-integration-option-a/16-02-SUMMARY.md`
    - `grep -q "SC#4\|SC#2\|SC#1" .planning/phases/16-production-integration-option-a/16-02-SUMMARY.md`
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/16-production-integration-option-a/16-02-SUMMARY.md && [ $(wc -l < .planning/phases/16-production-integration-option-a/16-02-SUMMARY.md) -ge 40 ] && [ $(grep -c "^## " .planning/phases/16-production-integration-option-a/16-02-SUMMARY.md) -ge 8 ]</automated>
  </verify>
  <done>16-02-SUMMARY.md 생성 완료 + 8 섹션 + SC#1 2/4 + SC#2/3/4/5 evidence anchor 박제.</done>
</task>

</wave>

</waves>

<verification>
  Plan 16-02 전체 완료 조건:
  1. `remotion/` TypeScript 프로젝트 boot 성공 (`npm install` + `npx remotion --version` 가능)
  2. `scripts/orchestrator/api/remotion_renderer.py` 가 400+ lines, 8 internal method + 2 public method, 6 test file green
  3. `shorts_pipeline._run_assembly` 가 remotion > shotstack > ffmpeg 3분기 cascade
  4. `pytest tests/phase16/ -x` exit 0 (≥35 tests)
  5. `pytest tests/phase04 tests/phase05 tests/phase06 tests/phase07 --tb=line -q` regression green (986+ preserved)
  6. `grep -rn "skip_gates\|TODO(next-session)" scripts/orchestrator/api/remotion_renderer.py remotion/src/` 값 == 0 (CLAUDE.md 금기 #1/2)
  7. `grep -rn "VeoClient\|veo_i2v\.generate\|import.*veo" scripts/orchestrator/api/remotion_renderer.py remotion/src/` 값 == 0 (CLAUDE.md 금기 #11, Veo 신규 호출 금지)
</verification>

<deviations_allowed>
  - **Rule 1 (typo):** 변수명 / 로그 메시지 / 주석 오타 수정. error message 문구 개선.
  - **Rule 2 (minor variant):** TypeScript 파일 이식 시 harvested 와 경로 하위 구조가 다를 경우 가능한 한 원본 유지하되, 없는 컴포넌트 (예: Root.tsx 가 long-form composition 참조) 는 import 만 제거 가능 (로직 수정 절대 금지). `_extract_from_timeline` 내부 구현은 Plan 16-03/16-04 wiring 과 함께 진화 — 16-02 에서는 최소 골격 + NotImplementedError("Plan 16-03/16-04 가 visual_spec + subtitles 파이프 주입 필요") 허용.
  - **Rule 3 (blocker):** Node 버전이 16 미만이거나 `npm install` 이 네트워크 오류로 실패 시 Wave 중단 + 대표님 보고. 차단 해제 전까지 Plan 진행 불가.
</deviations_allowed>
