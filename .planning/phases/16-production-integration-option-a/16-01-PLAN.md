---
plan: 16-01
phase: 16
name: "Channel Bible Imprint + Production Feedback Memory Mapping"
wave: 1
depends_on: []
autonomous: true
requirements: [REQ-PROD-INT-01]
files_modified:
  - .claude/memory/project_channel_bible_incidents_v1.md
  - .claude/memory/project_channel_bible_wildlife_ref.md
  - .claude/memory/project_channel_bible_humor_ref.md
  - .claude/memory/project_channel_bible_politics_ref.md
  - .claude/memory/project_channel_bible_trend_ref.md
  - .claude/memory/project_channel_bible_documentary_ref.md
  - .claude/memory/feedback_script_tone_seupnida.md
  - .claude/memory/feedback_duo_natural_dialogue.md
  - .claude/memory/feedback_subtitle_semantic_grouping.md
  - .claude/memory/feedback_video_clip_priority.md
  - .claude/memory/feedback_outro_signature.md
  - .claude/memory/feedback_series_ending_tiers.md
  - .claude/memory/feedback_detective_exit_cta.md
  - .claude/memory/feedback_watson_cta_pool.md
  - .claude/memory/feedback_dramatization_allowed.md
  - .claude/memory/feedback_info_source_distinction.md
  - .claude/memory/feedback_veo_supplementary_only.md
  - .claude/memory/feedback_number_split_subtitle.md
  - .claude/memory/MEMORY.md
  - tests/phase16/__init__.py
  - tests/phase16/conftest.py
  - tests/phase16/test_channel_bible_memory.py
  - tests/phase16/test_feedback_memories_mapped.py
estimated_complexity: low
estimated_duration: 1-2h
---

<objective>
Phase 16-01 은 코드 수정 0, 순수 텍스트 박제 Plan. 목적: shorts_naberal 에서 확인된 incidents 채널 바이블 v1.0 을 `.claude/memory/project_channel_bible_incidents_v1.md` 로 영구 박제하여 모든 Producer/Inspector 에이전트가 `<mandatory_reads>` 로 참조할 수 있도록 하며, incidents.md 각주에서 언급되는 12개 `feedback_*` 메모리를 생성/갱신해 FAIL-SCR-011 / FAIL-SCR-016 / FAIL-SCR-004 등 반복 실패를 원천 차단한다. 타 5개 채널바이블(wildlife / humor / politics / trend / documentary)은 reference-only 요약 메모리로만 박제(Phase 16 범위 — 렌더링 통합은 Phase 18+). MEMORY.md 인덱스에 전 항목 등재.
</objective>

<must_haves>
  - truths:
    - "`.claude/memory/project_channel_bible_incidents_v1.md` 존재 + incidents 10개 규칙 섹션 (타겟/영상길이/목표/톤/금지어/문장규칙/구조/근거규칙/화면규칙/CTA규칙) 모두 포함"
    - "12개 `feedback_*.md` 메모리 존재 및 각 파일에 `.preserved/harvested/theme_bible_raw/incidents.md` 근거 섹션 라인 cross-reference 포함"
    - "5개 채널바이블 reference 메모리 (`project_channel_bible_{wildlife,humor,politics,trend,documentary}_ref.md`) 존재 + 각 파일 첫 줄 `status: reference_only_phase_16` 선언"
    - "MEMORY.md 인덱스에 신규 18개 엔트리 (incidents v1 + 5 refs + 12 feedbacks) 등재"
  - artifacts:
    - path: ".claude/memory/project_channel_bible_incidents_v1.md"
      provides: "incidents 채널 v1.0 SSOT (10 규칙 섹션 + FAIL-SCR 매핑 + feedback 각주)"
      min_lines: 60
    - path: ".claude/memory/MEMORY.md"
      provides: "18 신규 엔트리 인덱스"
      contains: "feedback_script_tone_seupnida"
    - path: "tests/phase16/test_channel_bible_memory.py"
      provides: "memory 파일 존재 + 필수 섹션 키워드 grep 검증"
    - path: "tests/phase16/test_feedback_memories_mapped.py"
      provides: "12 feedback 메모리 파일 전수 존재 검증"
  - key_links:
    - from: "Producer/Inspector AGENT.md"
      to: ".claude/memory/project_channel_bible_incidents_v1.md"
      via: "<mandatory_reads> 참조 (Phase 16 이후 순차 추가, 16-01 은 박제만)"
      pattern: "project_channel_bible_incidents_v1.md"
    - from: "scripter AGENT.md (향후 개별 업데이트)"
      to: "feedback_script_tone_seupnida"
      via: "FAIL-SCR-011 / FAIL-SCR-016 방지용 mandatory_reads"
      pattern: "feedback_script_tone_seupnida"
</must_haves>

<waves>

<wave number="0">

<task id="16-01-W0-TESTS">
  <objective>tests/phase16/ 인프라 스캐폴드 생성 (__init__.py + conftest.py).</objective>
  <read_first>
    - tests/phase091/conftest.py (기존 패턴)
    - tests/phase091/__init__.py
    - tests/phase14/conftest.py (있으면)
    - .planning/phases/16-production-integration-option-a/16-VALIDATION.md
  </read_first>
  <action>
    `tests/phase16/__init__.py` 을 빈 파일로 생성 (package marker).

    `tests/phase16/conftest.py` 생성. 최소 3개 fixture:
    ```python
    # tests/phase16/conftest.py
    """Phase 16 — shared pytest fixtures for Remotion / faster-whisper / visual_spec tests."""
    from __future__ import annotations

    from pathlib import Path
    import json
    import pytest

    REPO_ROOT = Path(__file__).resolve().parents[2]
    HARVEST_ROOT = REPO_ROOT / ".preserved" / "harvested"


    @pytest.fixture(scope="session")
    def harvest_root() -> Path:
        assert HARVEST_ROOT.exists(), ".preserved/harvested/ 미존재 — Phase 3 HARVEST 재실행 필요"
        return HARVEST_ROOT


    @pytest.fixture(scope="session")
    def zodiac_visual_spec(harvest_root: Path) -> dict:
        """zodiac-killer baseline visual_spec.json 로드 (read-only)."""
        path = harvest_root / "baseline_specs_raw" / "zodiac-killer" / "visual_spec.json"
        assert path.exists(), f"baseline visual_spec 미존재: {path}"
        return json.loads(path.read_text(encoding="utf-8"))


    @pytest.fixture(scope="session")
    def memory_dir() -> Path:
        return REPO_ROOT / ".claude" / "memory"
    ```

    둘 다 UTF-8 인코딩 (Windows cp949 안전).
  </action>
  <acceptance_criteria>
    - `test -f tests/phase16/__init__.py` (파일 존재)
    - `test -f tests/phase16/conftest.py`
    - `grep -c "zodiac_visual_spec" tests/phase16/conftest.py` >= 1
    - `python -c "import tests.phase16.conftest"` 실행 성공 (syntax 검증)
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/phase16/ --collect-only 2>&1 | grep -E "no tests ran|collected 0 items" </automated>
  </verify>
  <done>tests/phase16/ 디렉토리 존재, __init__.py + conftest.py 작성, pytest 가 디렉토리를 인식하나 test 함수는 아직 없음.</done>
</task>

</wave>

<wave number="1">

<task id="16-01-W1-INCIDENTS-BIBLE">
  <objective>incidents 채널바이블 v1.0 을 `.claude/memory/project_channel_bible_incidents_v1.md` 로 박제.</objective>
  <read_first>
    - .preserved/harvested/theme_bible_raw/incidents.md (SSOT 원본)
    - .preserved/harvested/skills_raw/channel-incidents/SKILL.md (duo 대화 규칙 + CTA signature + Hook 시그니처 Veo 재사용 패턴)
    - .claude/memory/MEMORY.md (인덱스 등재 패턴)
    - .claude/memory/project_tts_stack_typecast.md (기존 project_ 메모리 포맷 참조)
  </read_first>
  <action>
    `.claude/memory/project_channel_bible_incidents_v1.md` 신규 작성. 구조 (UTF-8):

    ```markdown
    ---
    memory_id: project_channel_bible_incidents_v1
    source: .preserved/harvested/theme_bible_raw/incidents.md
    version: v1.0
    status: production_active
    imprinted_session: 33
    imprinted_date: 2026-04-22
    phase: 16-01
    channel: incidents
    drafted_by: 나베랄
    approved_by: 대표님
    approved_date: 2026-04-17
    ---

    # incidents (사건기록부) 채널바이블 v1.0 — 박제본

    > shorts_naberal `.preserved/harvested/theme_bible_raw/incidents.md` (v1.0, 승인됨) 의 박제 사본.
    > 원본 수정 금지. 본 메모리만 참조.

    ## 1. 타겟
    30~50대 남성 중심. 범죄·미스터리·실화 관심.
    영화 《살인의 추억》《세븐》 톤, 팟캐스트 《그것이 알고싶다》 청취층.

    ## 2. 영상 길이
    - 단편 50~60초
    - 시리즈편 (Part N) 90~120초
    - 상한 120초 (DESIGN_BIBLE 기준)

    ## 3. 목표
    감정 1개 (불쾌한 긴장감 or 인간에 대한 공허함) + 정보 1개 (시청자가 영상 종료 후 기억하는 디테일 1개) — **둘 다 1개만**.

    ## 4. 톤
    탐정 1인칭 독백 + 왓슨(조수) 시청자 대리 질문. 딱딱하지만 몰입감 있게. 감정 과잉 금지 (울먹임·감탄사 X). 냉정한 사실 제시 + 문장 말미 짧은 인간적 반응 허용. 종결어미 "습니다/입니다/였죠" 만 (FAIL-SCR-011 방지 → `feedback_script_tone_seupnida`).

    ## 5. 금지어 (블랙리스트)
    - "여러분", "놀랍게도", "결론부터 말하면", "~해보세요"
    - "충격적인", "소름 끼치는", "엄청난", "역대급", "믿을 수 없는"
    - "다음과 같다", "그것은 바로" (체언종결)
    - "되었다", "되어 있었다" (수동태 3연속 금지, Q2 번역체 패턴)
    - 체언종결 2연속 금지 ("공포였다. 침묵이었다." — 금지)
    - 3글자 이상 핵심 구절 반복 금지 (FAIL-SCR-006)
    - "다시" / "또" 과거 참조 앞 첫 설정 없이 금지

    ## 6. 문장규칙
    평균 길이 12~22자. 20자+ 문장에 쉼표 ≥2개 (호흡 마크). Hook ≤ 50자. 한 컷 = 한 호흡. 한 문장에 메시지 1개. 리듬: std ≥ 30자 (짧은-긴-중 교차). 20자 이하 드라마 라인 ≥ 1회/영상.

    ## 7. 구조 (4단계 고정)
    - **Hook (0-5초)**: 탐정 현장 도착. 질문/반전/금지된 진실/오해 깨기 중 1개로 첫 문장.
    - **갈등·오해 (5-30초)**: 시청자 선입견과 다른 실체. 왓슨 첫 질문 → 탐정 답변 (`feedback_duo_natural_dialogue`).
    - **핵심 3포인트 (30-90초)**: 증거 / 프로파일링 / 현장 관찰. 각 포인트 숫자·비교·사례 ≥ 1 개.
    - **반전·정리 (마지막 2-5초)**: 인간적 공허 + 다음 편 예고형 CTA.

    ## 8. 근거규칙
    숫자·비교·사례 ≥ 1 (한 편). 실화 기반 — 핵심 사실 엄격, 장면 디테일·호흡은 각색 허용 (`feedback_dramatization_allowed`). "~라고 합니다" 전달형 vs "탐정이 직접 본 것" 구분 (`feedback_info_source_distinction`).

    ## 9. 화면규칙
    매 컷 보여줄 것 1개 명시 (자막/그래픽/현장 이미지/Veo 재현). Veo 보조용만, 실제 이미지 크롤링 최우선 (`feedback_veo_supplementary_only`). 자막 6~12자, 동사 종결, 의미단위 그룹핑 (`feedback_subtitle_semantic_grouping`). 숫자 쪼개짐 금지 ("1,701통" 한 단어 유지 → `feedback_number_split_subtitle`). 영상:이미지 ≥ 30% (`feedback_video_clip_priority`). 텍스트 스크린샷 자동 제거.

    ## 10. CTA규칙
    구독 강요 금지. 시리즈 끝맺음 3단계 (`feedback_series_ending_tiers`):
    - Part 1: 시그니처 끝 문구 ("놓지 않았습니다" 등)
    - Part 2: 독백형 여운
    - Part 3: 경각심 + 다음 에피소드 예고

    탐정 퇴장 문구 풀 10개 랜덤 선택, 과공손 "뵙겠습니다" 금지 (`feedback_detective_exit_cta`). 왓슨 CTA 풀 10개 (`feedback_watson_cta_pool`). 엔딩 시그니처: 탐정 정면 → 뒤돌아 걸어감 (`feedback_outro_signature`).

    ---

    ## FAIL-SCR 매핑 (복제 포함 — 원본 §FAIL-SCR 매핑 유지)

    | 실패 ID | 원인 | 바이블 방지 |
    |--------|------|----------|
    | FAIL-SCR-011 | 국어책 낭독체, Hook 과속 | §4 종결어미 + §6 문장규칙 |
    | FAIL-SCR-006 | 핵심 구절 반복 | §5 금지어 |
    | FAIL-SCR-004 | 듀오 흐름 끊김, CTA 누락 | §7 구조 + §10 CTA |
    | FAIL-SCR-016 | "어요/해요" 체 혼입 | §4 종결어미 "습니다/입니다/였죠" 만 |

    ## 관련 feedback 메모리 (12건)

    - feedback_script_tone_seupnida (§4)
    - feedback_duo_natural_dialogue (§7)
    - feedback_subtitle_semantic_grouping (§9)
    - feedback_video_clip_priority (§9)
    - feedback_outro_signature (§10)
    - feedback_series_ending_tiers (§10)
    - feedback_detective_exit_cta (§10)
    - feedback_watson_cta_pool (§10)
    - feedback_dramatization_allowed (§8)
    - feedback_info_source_distinction (§8)
    - feedback_veo_supplementary_only (§9)
    - feedback_number_split_subtitle (§9)

    ## Hook signature (channel-incidents SKILL §)
    - Hook 클립 길이 하드 고정: **9.0s**
    - 인트로 시그니처 파일: `.preserved/harvested/video_pipeline_raw/signatures/incidents_intro_v4_silent_glare.mp4` (Plan 16-03 Task 1 harvest 확장 후 배치)

    ## 재사용 안내
    모든 Producer / Inspector AGENT.md 는 본 메모리를 `<mandatory_reads>` 에 추가해야 합니다 (Phase 16 에서는 박제만 수행, 에이전트 mandatory_reads 업데이트는 Phase 17+ 개별 진행).
    ```

    완성 후 60 줄 이상. UTF-8 인코딩. 본 Plan 은 텍스트 박제 only — 에이전트 AGENT.md 수정은 Phase 17+.
  </action>
  <acceptance_criteria>
    - `test -f .claude/memory/project_channel_bible_incidents_v1.md`
    - `grep -c "^## " .claude/memory/project_channel_bible_incidents_v1.md` 값 ≥ 10 (10 섹션 헤딩)
    - `grep -c "feedback_" .claude/memory/project_channel_bible_incidents_v1.md` 값 ≥ 12 (12 feedback 언급)
    - `grep -l "FAIL-SCR-011" .claude/memory/project_channel_bible_incidents_v1.md` (FAIL-SCR 매핑)
    - `grep -l "status: production_active" .claude/memory/project_channel_bible_incidents_v1.md`
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/phase16/test_channel_bible_memory.py -x --tb=short</automated>
  </verify>
  <done>incidents v1.0 박제 완료, 10 규칙 섹션 + FAIL-SCR 매핑 + 12 feedback 참조 + Hook signature 9.0s 고정.</done>
</task>

<task id="16-01-W1-REFS-5CHANNELS">
  <objective>타 5개 채널바이블 (wildlife / humor / politics / trend / documentary) reference-only 요약 메모리 5개 생성.</objective>
  <read_first>
    - .preserved/harvested/theme_bible_raw/wildlife.md
    - .preserved/harvested/theme_bible_raw/humor.md
    - .preserved/harvested/theme_bible_raw/politics.md
    - .preserved/harvested/theme_bible_raw/trend.md
    - .preserved/harvested/theme_bible_raw/documentary.md
    - .claude/memory/project_channel_bible_incidents_v1.md (16-01-W1-INCIDENTS-BIBLE 산출 — 포맷 참조)
  </read_first>
  <action>
    각 채널별로 `.claude/memory/project_channel_bible_{channel}_ref.md` 5개 생성. 각 파일 구조 (UTF-8, 약 20~40 줄):

    ```markdown
    ---
    memory_id: project_channel_bible_<channel>_ref
    source: .preserved/harvested/theme_bible_raw/<channel>.md
    status: reference_only_phase_16
    imprinted_session: 33
    imprinted_date: 2026-04-22
    phase: 16-01
    channel: <channel>
    ---

    # <channel> 채널바이블 — Reference Only (Phase 16)

    > shorts_naberal `.preserved/harvested/theme_bible_raw/<channel>.md` 의 요약 박제.
    > **Phase 16 에서는 렌더링 통합 없음** — incidents 만 production_active.
    > 본 메모리는 Phase 18+ 활성화 시점에 확장 재작성 예정.

    ## 타겟 / 영상 길이 / 톤 (원본 발췌)
    <원본 §1~3 요약 3~5줄>

    ## 핵심 금지어 (원본 §5 발췌)
    <원본 blacklist 5~8 항목>

    ## 구조 (원본 §7 요약)
    <4단계 이름만 bullet list>

    ## Phase 16 활성화 여부
    - 렌더링 통합: ❌ (Phase 18+)
    - 에이전트 mandatory_reads 등재: ❌
    - MEMORY.md 인덱스 등재: ✅
    - reference 전용 박제 범위만 유효

    ## 원본 참조
    전수 내용은 `.preserved/harvested/theme_bible_raw/<channel>.md` 참조 (read-only, 수정 금지).
    ```

    5개 모두 동일 구조. 각 ref 메모리는 20줄 이상 실콘텐츠 포함 — placeholder/빈 파일 금지 (CLAUDE.md 금기 #10).
  </action>
  <acceptance_criteria>
    - 5개 파일 전수 존재: `test -f .claude/memory/project_channel_bible_wildlife_ref.md && test -f .claude/memory/project_channel_bible_humor_ref.md && test -f .claude/memory/project_channel_bible_politics_ref.md && test -f .claude/memory/project_channel_bible_trend_ref.md && test -f .claude/memory/project_channel_bible_documentary_ref.md`
    - 각 파일 라인 수 ≥ 20: `for f in .claude/memory/project_channel_bible_{wildlife,humor,politics,trend,documentary}_ref.md; do [ "$(wc -l < $f)" -ge 20 ] || exit 1; done`
    - 각 파일에 `status: reference_only_phase_16` 포함: `grep -c "reference_only_phase_16" .claude/memory/project_channel_bible_*_ref.md` 값 == 5
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/phase16/test_channel_bible_memory.py::test_five_reference_channel_bibles_exist -x --tb=short</automated>
  </verify>
  <done>5개 ref 메모리 각 파일 ≥20 줄, reference_only_phase_16 상태 명시, 원본 경로 참조.</done>
</task>

<task id="16-01-W1-FEEDBACK-12">
  <objective>incidents.md 각주에서 언급되는 12개 feedback_*.md 메모리 생성/갱신.</objective>
  <read_first>
    - .preserved/harvested/theme_bible_raw/incidents.md (§4 + §7 + §8 + §9 + §10 + FAIL-SCR 매핑)
    - .preserved/harvested/skills_raw/channel-incidents/SKILL.md (CTA signature + 탐정 퇴장 문구 풀)
    - .claude/memory/feedback_i2v_prompt_principles.md (기존 feedback 메모리 포맷)
    - .claude/memory/feedback_no_mockup_no_empty_files.md (기존 feedback 메모리 포맷)
  </read_first>
  <action>
    12개 feedback 메모리 각각 생성 또는 갱신. 모두 `.claude/memory/feedback_<key>.md` 경로. 각 파일 구조 (UTF-8, 15~30 줄):

    ```markdown
    ---
    memory_id: feedback_<key>
    category: feedback
    status: active
    imprinted_session: 33
    imprinted_date: 2026-04-22
    phase: 16-01
    channel: incidents
    source_refs:
      - .preserved/harvested/theme_bible_raw/incidents.md §<N>
      - .preserved/harvested/skills_raw/channel-incidents/SKILL.md (해당 섹션)
    failure_mapping:
      - FAIL-SCR-<NNN> (해당 시)
    ---

    # feedback: <제목>

    ## 규칙
    <1~3 문장 규칙 요약>

    ## 예시 — 준수
    <Good 예시 1건>

    ## 예시 — 위반
    <Bad 예시 1건>

    ## 근거
    <incidents.md 해당 섹션 ≥1 문장 인용>
    ```

    각 key 별 내용 스펙 (각 파일 실 콘텐츠 ≥ 15줄):

    1. **feedback_script_tone_seupnida**: 종결어미 "습니다 / 입니다 / 였죠" 만 허용. "어요/해요/요" 혼입 금지 (FAIL-SCR-016). 예시 Good: "그는 그날 밤 돌아오지 않았습니다." / Bad: "그는 그날 밤 돌아오지 않았어요."
    2. **feedback_duo_natural_dialogue**: 왓슨 질문 키워드 ≥ 60% 가 탐정 답변 첫 문장에 포함 (FAIL-SCR-004 방지). 예시 Good: (왓슨) "범인은 누구였죠?" → (탐정) "범인은 밝혀지지 않았습니다." / Bad: (왓슨) "범인은 누구였죠?" → (탐정) "사건 당일 비가 왔습니다." (키워드 0%).
    3. **feedback_subtitle_semantic_grouping**: 자막 6~12자, 동사 종결, 의미 단위 분할. 예시 Good: "편지 1,701통이 배달됐습니다." (1줄) / Bad: "편지" / "1,701통이" / "배달됐" / "습니다" (4줄 낱개 분할).
    4. **feedback_video_clip_priority**: 영상:이미지 ≥ 30% 비율. 60초 영상 기준 영상 클립 총 18초 이상. 순수 스틸 배열 금지.
    5. **feedback_outro_signature**: 엔딩 시그니처 — 탐정 정면 → 뒤돌아 걸어감 패턴. Phase 16 에서는 incidents_outro_signature.mp4 파일 부재 (Plan 16-03 Task 0 연구 대상). 임시로 Remotion 프로그램적 outro card 로 대체 가능.
    6. **feedback_series_ending_tiers**: Part 1/2/3 CTA 차등. Part 1 = 시그니처 끝 문구 ("놓지 않았습니다"). Part 2 = 독백형 여운. Part 3 = 경각심 + 다음 편 예고.
    7. **feedback_detective_exit_cta**: 10 pool 탐정 퇴장 문구 랜덤 선택. 과공손 "뵙겠습니다" 금지. 예시 pool ≥ 5개 나열.
    8. **feedback_watson_cta_pool**: 10 pool 왓슨 CTA. 예시 pool ≥ 5개 나열.
    9. **feedback_dramatization_allowed**: 실화 기반 — 핵심 사실 엄격, 장면 디테일·호흡은 각색 허용. 예시: 범행 시각 (엄격) vs 범인 발자국 소리 묘사 (각색).
    10. **feedback_info_source_distinction**: "~라고 합니다" (전달형) vs "탐정이 직접 본 것" (직관형) 구분. 예시 Good: "현장 사진에는 피가 없었습니다." (직관) / Bad: "경찰은 피가 없었다고 발표했습니다." 를 직관처럼 쓰기 (혼동).
    11. **feedback_veo_supplementary_only**: Veo/I2V 보조용만, 실제 이미지 crawling 최우선. Veo 씬 ≤ 2 per 쇼츠. incidents §9 원칙 + CLAUDE.md 금기 #11 보강.
    12. **feedback_number_split_subtitle**: "1,701통" 같은 숫자 + 단위는 한 단어 유지. 자막 분할 시 쪼개짐 금지. 예시 Good: "편지 1,701통" (1단어) / Bad: "1,701" + "통" (2단어 분할).

    기존 파일이 있을 경우 덮어쓰기 — memory_id frontmatter + status: active 일관 유지.
  </action>
  <acceptance_criteria>
    - 12개 파일 전수 존재: `for k in feedback_script_tone_seupnida feedback_duo_natural_dialogue feedback_subtitle_semantic_grouping feedback_video_clip_priority feedback_outro_signature feedback_series_ending_tiers feedback_detective_exit_cta feedback_watson_cta_pool feedback_dramatization_allowed feedback_info_source_distinction feedback_veo_supplementary_only feedback_number_split_subtitle; do test -f .claude/memory/$k.md || exit 1; done`
    - 각 파일 라인 수 ≥ 15
    - 각 파일에 `memory_id: feedback_` + `status: active` + `phase: 16-01` 포함: `grep -lc "status: active" .claude/memory/feedback_*.md` 값 ≥ 12
    - 각 파일에 `source_refs:` 섹션 포함
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/phase16/test_feedback_memories_mapped.py -x --tb=short</automated>
  </verify>
  <done>12개 feedback 메모리 각 파일 ≥15 줄, incidents.md 근거 섹션 명시, status: active.</done>
</task>

<task id="16-01-W1-MEMORY-INDEX">
  <objective>MEMORY.md 인덱스에 신규 18개 엔트리 등재.</objective>
  <read_first>
    - .claude/memory/MEMORY.md (기존 인덱스 — 2 entries, 기존 형식)
    - .claude/memory/project_channel_bible_incidents_v1.md (16-01-W1-INCIDENTS-BIBLE 산출)
    - (16-01-W1-REFS-5CHANNELS + 16-01-W1-FEEDBACK-12 산출물 모두)
  </read_first>
  <action>
    `.claude/memory/MEMORY.md` 끝에 `## Phase 16-01 Imprinted (2026-04-22)` 섹션 추가. 형식 (기존 엔트리와 동일 bullet + relative link + 1줄 설명):

    ```markdown

    ## Phase 16-01 Imprinted (2026-04-22)

    ### Channel Bibles (6)
    - [incidents v1.0 (production_active)](project_channel_bible_incidents_v1.md) — 사건기록부 v1.0 SSOT 박제. Phase 16 production-active 채널 (렌더링 통합). 10 규칙 섹션 + FAIL-SCR 매핑 + 12 feedback 참조.
    - [wildlife (reference_only)](project_channel_bible_wildlife_ref.md) — 야생 채널 reference-only (Phase 18+).
    - [humor (reference_only)](project_channel_bible_humor_ref.md) — 유머 채널 reference-only (Phase 18+).
    - [politics (reference_only)](project_channel_bible_politics_ref.md) — 시사 채널 reference-only (Phase 18+).
    - [trend (reference_only)](project_channel_bible_trend_ref.md) — 트렌드 채널 reference-only (Phase 18+).
    - [documentary (reference_only)](project_channel_bible_documentary_ref.md) — 다큐 채널 reference-only (Phase 18+).

    ### Script / Dialogue feedbacks (4)
    - [feedback_script_tone_seupnida](feedback_script_tone_seupnida.md) — 종결어미 "습니다/입니다/였죠" 만 (FAIL-SCR-016 방어).
    - [feedback_duo_natural_dialogue](feedback_duo_natural_dialogue.md) — 왓슨 키워드 ≥60% 탐정 답변 첫 문장 포함 (FAIL-SCR-004 방어).
    - [feedback_dramatization_allowed](feedback_dramatization_allowed.md) — 핵심 사실 엄격, 장면 디테일 각색 허용.
    - [feedback_info_source_distinction](feedback_info_source_distinction.md) — "~라고 합니다" vs "직관" 구분.

    ### Subtitle feedbacks (2)
    - [feedback_subtitle_semantic_grouping](feedback_subtitle_semantic_grouping.md) — 자막 6~12자, 동사 종결.
    - [feedback_number_split_subtitle](feedback_number_split_subtitle.md) — "1,701통" 숫자+단위 한 단어 유지.

    ### Visual feedbacks (2)
    - [feedback_video_clip_priority](feedback_video_clip_priority.md) — 영상:이미지 ≥ 30%.
    - [feedback_veo_supplementary_only](feedback_veo_supplementary_only.md) — Veo/I2V 보조용, 실제 이미지 최우선 (금기 #11 보강).

    ### Outro / CTA feedbacks (4)
    - [feedback_outro_signature](feedback_outro_signature.md) — 탐정 정면 → 뒤돌아 걸어감 패턴.
    - [feedback_series_ending_tiers](feedback_series_ending_tiers.md) — Part 1/2/3 CTA 차등.
    - [feedback_detective_exit_cta](feedback_detective_exit_cta.md) — 10 pool 탐정 퇴장 문구, "뵙겠습니다" 금지.
    - [feedback_watson_cta_pool](feedback_watson_cta_pool.md) — 10 pool 왓슨 CTA.
    ```

    기존 MEMORY.md 내용은 보존. 끝에 append only — 중간 수정 금지.
  </action>
  <acceptance_criteria>
    - `grep -c "Phase 16-01 Imprinted" .claude/memory/MEMORY.md` 값 == 1
    - `grep -c "project_channel_bible_incidents_v1.md" .claude/memory/MEMORY.md` 값 ≥ 1
    - 12 feedback 중 전수 MEMORY.md 에 링크: `for k in feedback_script_tone_seupnida feedback_duo_natural_dialogue feedback_subtitle_semantic_grouping feedback_video_clip_priority feedback_outro_signature feedback_series_ending_tiers feedback_detective_exit_cta feedback_watson_cta_pool feedback_dramatization_allowed feedback_info_source_distinction feedback_veo_supplementary_only feedback_number_split_subtitle; do grep -l "$k.md" .claude/memory/MEMORY.md || exit 1; done`
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/phase16/test_channel_bible_memory.py::test_memory_index_updated -x --tb=short</automated>
  </verify>
  <done>MEMORY.md 에 Phase 16-01 섹션 append-only, 18 신규 링크 전수 등재.</done>
</task>

</wave>

<wave number="2">

<task id="16-01-W2-TESTS">
  <objective>16-01 산출물 전수 검증 테스트 2개 작성.</objective>
  <read_first>
    - tests/phase16/conftest.py (16-01-W0-TESTS 산출)
    - .claude/memory/project_channel_bible_incidents_v1.md
    - .claude/memory/MEMORY.md
  </read_first>
  <action>
    `tests/phase16/test_channel_bible_memory.py` 신규 작성:

    ```python
    """Phase 16-01 — channel bible memory imprinting 검증.

    REQ-PROD-INT-01 담당.
    """
    from __future__ import annotations

    from pathlib import Path
    import pytest

    MEMORY_DIR = Path(__file__).resolve().parents[2] / ".claude" / "memory"


    def test_incidents_bible_memory_exists():
        path = MEMORY_DIR / "project_channel_bible_incidents_v1.md"
        assert path.exists(), f"incidents v1.0 박제 메모리 미존재: {path}"


    def test_incidents_bible_has_ten_sections():
        """incidents.md 10 규칙 섹션 (§1~§10) 모두 박제되었는지 검증."""
        text = (MEMORY_DIR / "project_channel_bible_incidents_v1.md").read_text(encoding="utf-8")
        section_headings = [ln for ln in text.splitlines() if ln.startswith("## ")]
        # 10 규칙 섹션 + FAIL-SCR 매핑 + 관련 feedback + Hook signature 등
        assert len(section_headings) >= 10, f"섹션 수 부족: {len(section_headings)}"


    def test_incidents_bible_references_12_feedbacks():
        text = (MEMORY_DIR / "project_channel_bible_incidents_v1.md").read_text(encoding="utf-8")
        expected = [
            "feedback_script_tone_seupnida",
            "feedback_duo_natural_dialogue",
            "feedback_subtitle_semantic_grouping",
            "feedback_video_clip_priority",
            "feedback_outro_signature",
            "feedback_series_ending_tiers",
            "feedback_detective_exit_cta",
            "feedback_watson_cta_pool",
            "feedback_dramatization_allowed",
            "feedback_info_source_distinction",
            "feedback_veo_supplementary_only",
            "feedback_number_split_subtitle",
        ]
        missing = [k for k in expected if k not in text]
        assert not missing, f"feedback 미참조: {missing}"


    def test_incidents_bible_declares_production_active():
        text = (MEMORY_DIR / "project_channel_bible_incidents_v1.md").read_text(encoding="utf-8")
        assert "status: production_active" in text


    def test_incidents_bible_fail_scr_mapping_present():
        text = (MEMORY_DIR / "project_channel_bible_incidents_v1.md").read_text(encoding="utf-8")
        for code in ("FAIL-SCR-011", "FAIL-SCR-016", "FAIL-SCR-004", "FAIL-SCR-006"):
            assert code in text, f"FAIL-SCR code 누락: {code}"


    def test_five_reference_channel_bibles_exist():
        for ch in ("wildlife", "humor", "politics", "trend", "documentary"):
            p = MEMORY_DIR / f"project_channel_bible_{ch}_ref.md"
            assert p.exists(), f"ref 메모리 미존재: {p}"
            text = p.read_text(encoding="utf-8")
            assert "status: reference_only_phase_16" in text
            assert len(text.splitlines()) >= 20, f"{ch}: 실 콘텐츠 20줄 미달"


    def test_memory_index_updated():
        text = (MEMORY_DIR / "MEMORY.md").read_text(encoding="utf-8")
        assert "Phase 16-01 Imprinted" in text
        assert "project_channel_bible_incidents_v1.md" in text
        # 5 ref + 12 feedback 전수 링크
        for ref in ("wildlife", "humor", "politics", "trend", "documentary"):
            assert f"project_channel_bible_{ref}_ref.md" in text
    ```

    `tests/phase16/test_feedback_memories_mapped.py` 신규 작성:

    ```python
    """Phase 16-01 — feedback 메모리 12종 전수 박제 검증."""
    from __future__ import annotations

    from pathlib import Path
    import pytest

    MEMORY_DIR = Path(__file__).resolve().parents[2] / ".claude" / "memory"

    EXPECTED_FEEDBACKS = [
        "feedback_script_tone_seupnida",
        "feedback_duo_natural_dialogue",
        "feedback_subtitle_semantic_grouping",
        "feedback_video_clip_priority",
        "feedback_outro_signature",
        "feedback_series_ending_tiers",
        "feedback_detective_exit_cta",
        "feedback_watson_cta_pool",
        "feedback_dramatization_allowed",
        "feedback_info_source_distinction",
        "feedback_veo_supplementary_only",
        "feedback_number_split_subtitle",
    ]


    @pytest.mark.parametrize("key", EXPECTED_FEEDBACKS)
    def test_feedback_memory_exists(key: str):
        p = MEMORY_DIR / f"{key}.md"
        assert p.exists(), f"feedback 메모리 미존재: {p}"


    @pytest.mark.parametrize("key", EXPECTED_FEEDBACKS)
    def test_feedback_memory_has_frontmatter(key: str):
        text = (MEMORY_DIR / f"{key}.md").read_text(encoding="utf-8")
        assert text.startswith("---\n"), f"{key}: frontmatter 누락"
        assert f"memory_id: {key}" in text
        assert "status: active" in text
        assert "phase: 16-01" in text


    @pytest.mark.parametrize("key", EXPECTED_FEEDBACKS)
    def test_feedback_memory_minimum_content(key: str):
        text = (MEMORY_DIR / f"{key}.md").read_text(encoding="utf-8")
        lines = text.splitlines()
        assert len(lines) >= 15, f"{key}: 실 콘텐츠 15줄 미달 ({len(lines)})"


    @pytest.mark.parametrize("key", EXPECTED_FEEDBACKS)
    def test_feedback_memory_cites_incidents(key: str):
        text = (MEMORY_DIR / f"{key}.md").read_text(encoding="utf-8")
        assert "incidents" in text, f"{key}: incidents 참조 누락"
    ```

    두 테스트 모두 순수 파일 시스템 기반 — 외부 의존 없음.
  </action>
  <acceptance_criteria>
    - `test -f tests/phase16/test_channel_bible_memory.py`
    - `test -f tests/phase16/test_feedback_memories_mapped.py`
    - `pytest tests/phase16/test_channel_bible_memory.py tests/phase16/test_feedback_memories_mapped.py -x` exit code 0
    - 테스트 수: `pytest tests/phase16/test_channel_bible_memory.py tests/phase16/test_feedback_memories_mapped.py --collect-only -q` 값 ≥ 7 + 48 (12×4 parametrize) = 55
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/phase16/test_channel_bible_memory.py tests/phase16/test_feedback_memories_mapped.py -x --tb=short</automated>
  </verify>
  <done>테스트 2종 작성 완료, 모두 pass, 전체 regression (기존 986) 영향 없음.</done>
</task>

<task id="16-01-W2-SUMMARY">
  <objective>Plan 16-01 완료 SUMMARY 박제 — ROADMAP Phase 16 SC#1 "4/4 Plan SUMMARY 완료" 중 1/4 달성.</objective>
  <read_first>
    - .planning/phases/16-production-integration-option-a/16-01-PLAN.md (본 Plan 전문)
    - .planning/phases/16-production-integration-option-a/16-VALIDATION.md (16-01 task 검증 결과)
    - .planning/phases/16-production-integration-option-a/16-RESEARCH.md (Q1-Q4 authoritative 근거)
    - .planning/ROADMAP.md (Phase 16 SC#1~5 ground truth)
    - .claude/memory/project_channel_bible_incidents_v1.md (Plan 16-01 주요 산출물)
    - .claude/memory/MEMORY.md (인덱스 갱신 확인)
  </read_first>
  <action>
    `.planning/phases/16-production-integration-option-a/16-01-SUMMARY.md` 신규 작성. 최소 30 줄, 실 콘텐츠만 (placeholder 금지).

    필수 frontmatter:
    ```yaml
    ---
    plan: 16-01
    phase: 16
    name: "Channel Bible Imprint + 12 Feedback Memory Mapping"
    status: complete
    completed: <YYYY-MM-DD>  # 실제 완료 날짜
    wave_count: 3
    task_count: 6
    requirements: [REQ-PROD-INT-01]
    ---
    ```

    필수 섹션 7개 (각 최소 2 bullet):
    1. `## Deliverables` — 생성/수정 파일 목록 (path + line count). 최소 항목: `project_channel_bible_incidents_v1.md` / 5 `project_channel_bible_{wildlife,humor,politics,trend,documentary}_ref.md` / 12 `feedback_*.md` 박제 또는 확인 / `MEMORY.md` 인덱스 갱신.
    2. `## Tests` — `pytest tests/phase16/test_channel_bible_memory.py tests/phase16/test_feedback_memories_mapped.py -x` 결과 (passed/total, 예상 ≥55 테스트).
    3. `## Regression` — `pytest tests/phase04 tests/phase05 tests/phase06 tests/phase07 --tb=line -q` 결과 (986+ green 여부).
    4. `## Deviations` — Rule 1/2/3 발생 건수 + 각 case 상세. 발생 없을 시 "None" + "16-01 은 pure text imprint 이므로 structural deviation 불가" 명시.
    5. `## Lessons` — Phase 16-02/03/04 에 전달할 교훈 3+ bullet (예: "incidents.md v1.0 10 규칙 중 §7 hook signature 는 Plan 16-03 의 intro_signature 복사 로직에 직접 입력", "feedback_outro_signature 내용은 Plan 16-03 Task 0 outro research 에 직접 참조").
    6. `## Evidence` — commit SHA(s), 박제 전후 MEMORY.md 인덱스 line count diff, 각 feedback 메모리 파일 sha256.
    7. `## SC Coverage` — SC#1 (본 SUMMARY 로 1/4 달성) + SC#3 (Phase 16 테스트 ≥55 기여) 명시.

    작성 후 `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(16-01): plan summary"` 로 commit.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/16-production-integration-option-a/16-01-SUMMARY.md`
    - `wc -l < .planning/phases/16-production-integration-option-a/16-01-SUMMARY.md` ≥ 30
    - `grep -c "^## " .planning/phases/16-production-integration-option-a/16-01-SUMMARY.md` ≥ 7
    - `grep -q "^plan: 16-01$" .planning/phases/16-production-integration-option-a/16-01-SUMMARY.md` (frontmatter)
    - `grep -q "^status: complete$" .planning/phases/16-production-integration-option-a/16-01-SUMMARY.md`
    - `grep -q "REQ-PROD-INT-01" .planning/phases/16-production-integration-option-a/16-01-SUMMARY.md`
    - `grep -q "## Deliverables\|## Tests\|## Regression\|## Deviations\|## Lessons\|## Evidence\|## SC Coverage" .planning/phases/16-production-integration-option-a/16-01-SUMMARY.md`
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/16-production-integration-option-a/16-01-SUMMARY.md && [ $(wc -l < .planning/phases/16-production-integration-option-a/16-01-SUMMARY.md) -ge 30 ] && [ $(grep -c "^## " .planning/phases/16-production-integration-option-a/16-01-SUMMARY.md) -ge 7 ]</automated>
  </verify>
  <done>16-01-SUMMARY.md 생성 완료 + 7 섹션 전수 실 콘텐츠 + ROADMAP SC#1 1/4 달성 박제.</done>
</task>

</wave>

</waves>

<verification>
  Plan 16-01 전체 완료 조건:
  1. `pytest tests/phase16/test_channel_bible_memory.py tests/phase16/test_feedback_memories_mapped.py -x` exit 0
  2. `pytest tests/ -n auto` regression 전수 PASS (986+ 기존 기준, 신규 테스트 추가만 허용)
  3. `grep -c "Phase 16-01 Imprinted" .claude/memory/MEMORY.md` == 1
  4. 모든 박제 메모리 파일이 `status: (production_active|reference_only_phase_16|active)` frontmatter 포함
</verification>

<deviations_allowed>
  - **Rule 1 (typo):** feedback 메모리 내 예시 문장의 구두점/맞춤법 교정, 메모리 id 등 식별자 오타 수정.
  - **Rule 2 (minor variant):** frontmatter 추가 필드 (예: `related_memories: [...]`) 로 풍부화 가능, 섹션 헤딩 표기 `### 예시 — 준수` → `### ✅ 예시` 변경 가능. 단 `memory_id` / `status` / `phase` 3종 필드는 고정.
  - **Rule 3 (blocker):** incidents.md 에 아직 없는 feedback 키 (예: 새로운 FAIL-SCR 발견) 발견 시 대표님께 보고 후 13번째 feedback 추가 진행 결정. Plan 자체 변경 없이 append.
</deviations_allowed>
