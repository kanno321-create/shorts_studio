---
phase: 14-api-adapter-remediation
plan: 03
plan_id: 14-03
type: execute
wave: 2
depends_on:
  - 14-01
  - 14-02
files_modified:
  - tests/adapters/test_veo_i2v_contract.py
  - tests/adapters/test_elevenlabs_contract.py
  - tests/adapters/test_shotstack_contract.py
autonomous: true
requirements:
  - ADAPT-01
  - ADAPT-02
  - ADAPT-03
commit_discipline: per-file
must_haves:
  truths:
    - "3 개 adapter contract 테스트 파일이 `tests/adapters/` 에 존재하고 `@pytest.mark.adapter_contract` 로 마킹된다"
    - "각 contract 테스트는 pydantic 입력 schema + API-key resolution + retry/fallback + fault injection 기본값 4 개 축을 assert 한다"
    - "Wave 1 에서 veo_i2v.py 모듈에서 삭제된 physical-absence assertion 이 `test_veo_i2v_contract.py::test_no_text_only_method` 로 이관되어 invariant 보존된다"
    - "`pytest -m adapter_contract -v` 실행 시 최소 15 개 contract 테스트가 green 으로 수집·실행된다"
    - "real API 호출 0 회 — 모든 network/SDK seam 은 monkeypatch 로 차단된다"
  artifacts:
    - path: "tests/adapters/test_veo_i2v_contract.py"
      provides: "ADAPT-01 — veo_i2v I2V-only schema + T2VForbidden + 3-tier API key resolution"
      contains: "pytest.mark.adapter_contract"
      min_tests: 5
    - path: "tests/adapters/test_elevenlabs_contract.py"
      provides: "ADAPT-02 — elevenlabs TTS + chars_to_words + dual env alias + default voice 3-tier"
      contains: "pytest.mark.adapter_contract"
      min_tests: 5
    - path: "tests/adapters/test_shotstack_contract.py"
      provides: "ADAPT-03 — shotstack ShotstackRenderRequest + FILTER_ORDER D-17 + continuity_prefix D-19"
      contains: "pytest.mark.adapter_contract"
      min_tests: 5
  key_links:
    - from: "tests/adapters/test_*_contract.py"
      to: "scripts/orchestrator/api/{veo_i2v,elevenlabs,shotstack}.py"
      via: "direct import + monkeypatch on _submit_and_poll / _invoke_tts / httpx.Client"
      pattern: "from scripts.orchestrator.api"
    - from: "tests/adapters/test_*_contract.py"
      to: "tests/phase07/mocks/*.py"
      via: "allow_fault_injection=False invariant assertion"
      pattern: "allow_fault_injection is False"
---

<objective>
Wave 2 — 3 개 adapter contract 테스트 파일을 신설하여 ADAPT-01/02/03 을 충족한다. 각 테스트는 실 API 호출 없이 (a) pydantic 입력/출력 schema, (b) API-key resolution, (c) retry/fallback 경로, (d) Phase 7 Mock `allow_fault_injection=False` 기본값 invariant 4 개 축을 assert 한다.

Purpose: Contract 테스트 없이는 adapter 변경이 schema/signature 에 silent breakage 를 유발해도 Phase 5/6/7 의 integration 테스트가 잡아주기까지 기다려야 한다 (오늘의 15 failures 가 그 증거). Contract 테스트는 **adapter 단독 unit gate** 로, `pytest -m adapter_contract` 한 번의 호출로 7 adapter 의 계약 건전성을 60s 이내 검증한다.
Output: 3 개 파일 × 각 ≥5 tests = 최소 15 개 신규 contract 테스트, 모두 `@pytest.mark.adapter_contract` 마킹.

**Parallelization**: 3 개 파일은 서로 disjoint 이므로 동시 실행 가능. Wave 1 완결 후 contract 테스트 3 종은 실행 순서 무관.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-api-adapter-remediation/14-RESEARCH.md
@.planning/phases/14-api-adapter-remediation/14-VALIDATION.md
@CLAUDE.md

# Adapter source (to assert against — do NOT modify)
@scripts/orchestrator/api/veo_i2v.py
@scripts/orchestrator/api/elevenlabs.py
@scripts/orchestrator/api/shotstack.py
@scripts/orchestrator/api/models.py

# Reference templates (study patterns, do NOT modify)
@tests/phase05/test_shotstack_adapter.py
@tests/phase05/test_elevenlabs_adapter.py
@tests/phase07/mocks/shotstack_mock.py
@tests/phase07/mocks/elevenlabs_mock.py

# Scaffold from Wave 0
@tests/adapters/conftest.py

<interfaces>
pydantic v2 schemas from scripts/orchestrator/api/models.py:
```python
class I2VRequest(BaseModel):
    prompt: str
    anchor_frame: Path | None
    duration_seconds: int = 5

class ShotstackRenderRequest(BaseModel):
    # timeline, resolution, aspect_ratio 등

class ContinuityPrefix(BaseModel):
    # D-19 prefix schema
```

VeoI2VAdapter (scripts/orchestrator/api/veo_i2v.py):
- constructor: `VeoI2VAdapter(api_key: str | None = None, output_dir: Path = ...)`
- API-key resolution 3-tier: kwarg > `VEO_API_KEY` env > `FAL_KEY` env > ValueError
- method: `image_to_video(prompt, anchor_frame, duration_seconds) -> Path`
- guard: `T2VForbidden` exception 라이즈 when anchor_frame is None
- internal: `_submit_and_poll` — fal_client 호출 seam (monkeypatch 대상)

ElevenLabsAdapter (scripts/orchestrator/api/elevenlabs.py, 350 lines):
- API-key dual env: `ELEVENLABS_API_KEY` / `ELEVEN_API_KEY`
- voice_id 3-tier: constructor kwarg > env > module cache > API discovery
- methods: `generate()`, `generate_with_timestamps()` (word-level)
- helper: `_chars_to_words(char_timings)` — D-10 round-trip
- internal: `_invoke_tts` seam

ShotstackAdapter (scripts/orchestrator/api/shotstack.py, 414 lines):
- class const: `DEFAULT_RESOLUTION = "hd"` (ORCH-11 720p)
- class const: `FILTER_ORDER = ("color_grade", "saturation", "film_grain")` (D-17)
- method: `render(timeline, resolution, aspect_ratio) -> dict`
- method: `upscale(...)` — Phase 5 RESEARCH §7 NOOP stub
- method: `create_ken_burns_clip(...)` — D-11 emits DeprecationWarning
- continuity_prefix injection at FILTER_ORDER[0] (D-19 invariant)
- internal: httpx.Client seam

Phase 7 Mocks (tests/phase07/mocks/):
- `ShotstackMock().allow_fault_injection is False` (D-3 invariant)
- `ElevenLabsMock().allow_fault_injection is False` (D-3 invariant)
- VeoI2V mock: 존재하지 않음 (RESEARCH §ADAPT-01 — Phase 15 add) → VeoI2V contract test 는 production-adapter 만 대상.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 14-03-01: tests/adapters/test_veo_i2v_contract.py (ADAPT-01)</name>
  <files>tests/adapters/test_veo_i2v_contract.py</files>
  <read_first>
    - scripts/orchestrator/api/veo_i2v.py (전체 — class 구조 + T2VForbidden + _submit_and_poll seam)
    - scripts/orchestrator/api/models.py (I2VRequest schema)
    - tests/phase05/test_shotstack_adapter.py (monkeypatch template)
    - tests/adapters/conftest.py (repo_root + _fake_env fixture)
    - .planning/phases/14-api-adapter-remediation/14-RESEARCH.md (§ADAPT-01)
  </read_first>
  <behavior>
    최소 6 tests 작성:
    - `test_no_text_only_method` — `VeoI2VAdapter` 에 `image_to_video_text_only` 속성 부재 + `inspect.getsource(VeoI2VAdapter)` 에 금기 regex 0 matches (Wave 1 에서 이관된 physical-absence guard).
    - `test_i2v_request_schema_compliance` — `I2VRequest(prompt=..., anchor_frame=Path(...), duration_seconds=5)` 생성 + pydantic ValidationError 케이스.
    - `test_missing_anchor_raises_t2v_forbidden` — `image_to_video(prompt, anchor_frame=None)` 호출 시 `T2VForbidden` raise.
    - `test_api_key_3tier_resolution` — kwarg > `VEO_API_KEY` env > `FAL_KEY` env > ValueError (monkeypatch 로 각 단계 검증).
    - `test_output_is_path` — `_submit_and_poll` monkeypatch 후 `image_to_video` 반환값이 `pathlib.Path`.
    - `test_production_adapter_has_no_fault_injection_attr` — real `VeoI2VAdapter` 에 `allow_fault_injection` 속성 부재 (그것은 mock 전용).
  </behavior>
  <action>
    다음 내용의 `tests/adapters/test_veo_i2v_contract.py` 파일 신설:

    ```python
    """Phase 14 ADAPT-01 — veo_i2v adapter contract.

    계약 축:
    1. pydantic I2VRequest schema 준수.
    2. API-key 3-tier resolution (constructor > VEO_API_KEY > FAL_KEY > ValueError).
    3. anchor_frame=None 시 T2VForbidden raise (금기사항 #4 강제).
    4. image_to_video 출력은 pathlib.Path.
    5. production adapter 에 fault-injection toggle 부재 (mock-only invariant).
    6. 모듈 source 에 금기 토큰 (t2v / text_to_video / text2video) 0 회.

    real fal.ai API 호출 없음 — `_submit_and_poll` seam 을 monkeypatch.
    """
    from __future__ import annotations

    import inspect
    import re
    from pathlib import Path

    import pytest
    from pydantic import ValidationError

    from scripts.orchestrator.api.models import I2VRequest
    from scripts.orchestrator.api.veo_i2v import VeoI2VAdapter

    pytestmark = pytest.mark.adapter_contract


    # ADAPT-01 축 6: source 에 금기 토큰 부재 (Wave 1 자체 문서 cleanup 후속 검증)
    T2V_BLACKLIST = re.compile(
        r"(^|[^A-Za-z_])t2v([^A-Za-z_]|$)|text_to_video|text2video"
    )


    def test_no_text_only_method():
        """Physical absence guard — 이관된 module-level assert 를 대체."""
        assert not hasattr(VeoI2VAdapter, "image_to_video_text_only")
        source = inspect.getsource(VeoI2VAdapter)
        assert T2V_BLACKLIST.search(source) is None, (
            "VeoI2VAdapter source 에 T2V 금기 토큰 발견. "
            "금기사항 #4 위반 (CLAUDE.md)."
        )


    def test_i2v_request_schema_compliance(tmp_path):
        """pydantic I2VRequest 기본 schema."""
        anchor = tmp_path / "frame.png"
        anchor.write_bytes(b"fake-png")
        req = I2VRequest(prompt="test", anchor_frame=anchor, duration_seconds=5)
        assert req.prompt == "test"
        assert req.anchor_frame == anchor
        assert req.duration_seconds == 5

        # prompt 빈 문자열 ValidationError (schema-level 검증)
        with pytest.raises(ValidationError):
            I2VRequest(prompt="", anchor_frame=anchor, duration_seconds=5)


    def test_missing_anchor_raises_t2v_forbidden(_fake_env, tmp_path):
        """금기 #4 — anchor_frame=None 입력 시 raise."""
        from scripts.orchestrator.api.veo_i2v import T2VForbidden

        adapter = VeoI2VAdapter(api_key="fake", output_dir=tmp_path)
        with pytest.raises(T2VForbidden):
            adapter.image_to_video(prompt="test", anchor_frame=None, duration_seconds=5)


    def test_api_key_3tier_resolution(monkeypatch, tmp_path):
        """kwarg > VEO_API_KEY > FAL_KEY > ValueError."""
        monkeypatch.delenv("VEO_API_KEY", raising=False)
        monkeypatch.delenv("FAL_KEY", raising=False)

        # Tier 1: kwarg 우선
        a1 = VeoI2VAdapter(api_key="kwarg-key", output_dir=tmp_path)
        assert getattr(a1, "api_key", None) == "kwarg-key"

        # Tier 2: VEO_API_KEY env
        monkeypatch.setenv("VEO_API_KEY", "veo-env")
        a2 = VeoI2VAdapter(api_key=None, output_dir=tmp_path)
        assert getattr(a2, "api_key", None) == "veo-env"

        # Tier 3: FAL_KEY env fallback
        monkeypatch.delenv("VEO_API_KEY", raising=False)
        monkeypatch.setenv("FAL_KEY", "fal-env")
        a3 = VeoI2VAdapter(api_key=None, output_dir=tmp_path)
        assert getattr(a3, "api_key", None) == "fal-env"

        # Tier 4: 모두 부재 → ValueError
        monkeypatch.delenv("VEO_API_KEY", raising=False)
        monkeypatch.delenv("FAL_KEY", raising=False)
        with pytest.raises(ValueError):
            VeoI2VAdapter(api_key=None, output_dir=tmp_path)


    def test_output_is_path(monkeypatch, _fake_env, tmp_path):
        """image_to_video 반환값 = pathlib.Path."""
        anchor = tmp_path / "anchor.png"
        anchor.write_bytes(b"fake-png")
        fake_output = tmp_path / "veo_out.mp4"

        def _stub_submit_and_poll(self, *args, **kwargs):
            fake_output.write_bytes(b"fake-mp4")
            return fake_output

        monkeypatch.setattr(VeoI2VAdapter, "_submit_and_poll", _stub_submit_and_poll)
        adapter = VeoI2VAdapter(api_key="fake", output_dir=tmp_path)
        result = adapter.image_to_video(
            prompt="test", anchor_frame=anchor, duration_seconds=5
        )
        assert isinstance(result, Path)
        assert result == fake_output


    def test_production_adapter_has_no_fault_injection_attr(_fake_env, tmp_path):
        """D-3 invariant — fault-injection toggle 은 mock-only."""
        adapter = VeoI2VAdapter(api_key="fake", output_dir=tmp_path)
        assert not hasattr(adapter, "allow_fault_injection"), (
            "Production VeoI2VAdapter 에 allow_fault_injection 속성 발견. "
            "해당 속성은 tests/phase07/mocks/ 전용이어야 함 (D-3 Phase 7)."
        )
    ```

    **중요 구현 주의점**:
    - VeoI2VAdapter 의 constructor 가 실제로 `api_key` 속성을 어떻게 저장하는지 source 에서 확인 후, `getattr(adapter, "api_key", None)` 가 올바르게 동작하도록 test 작성. 속성명이 다를 경우 source 에 맞춰 수정.
    - `T2VForbidden` import 경로는 veo_i2v.py 실제 export 위치 확인 후 조정.
    - `_submit_and_poll` 가 instance method 면 `monkeypatch.setattr(VeoI2VAdapter, "_submit_and_poll", ...)` 로 class-level patch.

    **금기 #2 준수**: `TODO` 주석 0회.
    **금기 #3 준수**: 모든 expected error 는 `pytest.raises()` 로 명시 검증.
  </action>
  <verify>
    <automated>pytest tests/adapters/test_veo_i2v_contract.py -v --no-cov 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/adapters/test_veo_i2v_contract.py -v --no-cov` exits 0.
    - `grep -c "pytest.mark.adapter_contract" tests/adapters/test_veo_i2v_contract.py` returns ≥1.
    - `grep -cE "^def test_" tests/adapters/test_veo_i2v_contract.py` returns ≥6 (최소 6 tests).
    - `grep -c "test_no_text_only_method" tests/adapters/test_veo_i2v_contract.py` returns 1.
    - `grep -c "test_missing_anchor_raises_t2v_forbidden" tests/adapters/test_veo_i2v_contract.py` returns 1.
    - `grep -c "test_api_key_3tier_resolution" tests/adapters/test_veo_i2v_contract.py` returns 1.
    - `grep -c "TODO" tests/adapters/test_veo_i2v_contract.py` returns 0.
    - `pytest -m adapter_contract tests/adapters/test_veo_i2v_contract.py --collect-only -q | grep -c "::test_"` returns ≥6.
  </acceptance_criteria>
  <done>
    ADAPT-01 충족 — veo_i2v adapter contract 6 tests green + `@pytest.mark.adapter_contract` 마킹 + Wave 1 에서 이관된 physical-absence invariant 보존.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 14-03-02: tests/adapters/test_elevenlabs_contract.py (ADAPT-02)</name>
  <files>tests/adapters/test_elevenlabs_contract.py</files>
  <read_first>
    - scripts/orchestrator/api/elevenlabs.py (전체 350줄 — constructor + _invoke_tts seam + _chars_to_words)
    - tests/phase05/test_elevenlabs_adapter.py (monkeypatch template — generate + generate_with_timestamps)
    - tests/phase07/mocks/elevenlabs_mock.py (`allow_fault_injection=False` 검증 대상)
    - tests/adapters/conftest.py (_fake_env)
    - .planning/phases/14-api-adapter-remediation/14-RESEARCH.md (§ADAPT-02)
  </read_first>
  <behavior>
    최소 7 tests 작성:
    - `test_generate_returns_audio_segment_list` — `generate([...scenes])` 반환값 구조.
    - `test_generate_with_timestamps_populates_words_by_scene` — word-level alignment 계약.
    - `test_chars_to_words_round_trip` — D-10 `_chars_to_words` 결정론.
    - `test_api_key_dual_env_alias` — `ELEVENLABS_API_KEY` + `ELEVEN_API_KEY` 둘 다 수용.
    - `test_default_voice_3tier_resolution` — constructor > env > module cache > API discovery (mocked).
    - `test_empty_text_raises_value_error` — 빈 문자열 입력.
    - `test_elevenlabs_mock_fault_injection_disabled_by_default` — Phase 7 mock D-3 invariant.
  </behavior>
  <action>
    다음 내용의 `tests/adapters/test_elevenlabs_contract.py` 파일 신설 (참조: 14-RESEARCH.md §ADAPT-02):

    ```python
    """Phase 14 ADAPT-02 — elevenlabs adapter contract.

    계약 축:
    1. generate() / generate_with_timestamps() 출력 구조 (list[AudioSegment] / words_by_scene).
    2. API-key dual env alias (ELEVENLABS_API_KEY + ELEVEN_API_KEY).
    3. default_voice 3-tier resolution.
    4. _chars_to_words(D-10) 결정론.
    5. 빈 text 입력 ValueError.
    6. Phase 7 ElevenLabsMock 의 allow_fault_injection=False (D-3 invariant).

    real elevenlabs SDK 호출 없음 — `_invoke_tts` seam 을 monkeypatch.
    """
    from __future__ import annotations

    from pathlib import Path

    import pytest

    from scripts.orchestrator.api.elevenlabs import ElevenLabsAdapter

    pytestmark = pytest.mark.adapter_contract


    def test_generate_returns_audio_segment_list(monkeypatch, _fake_env, tmp_path):
        """generate() 출력은 list 이고 각 원소는 audio 파일 경로를 포함."""
        def _stub_invoke_tts(self, text, voice_id, scene_id):
            out = tmp_path / f"scene_{scene_id}.mp3"
            out.write_bytes(b"fake-mp3")
            return {"audio_path": out, "scene_id": scene_id, "text": text}

        monkeypatch.setattr(ElevenLabsAdapter, "_invoke_tts", _stub_invoke_tts)
        adapter = ElevenLabsAdapter(api_key="fake", output_dir=tmp_path)
        scenes = [
            {"scene_id": "s1", "text": "안녕하세요", "voice_id": "v1"},
            {"scene_id": "s2", "text": "반갑습니다", "voice_id": "v1"},
        ]
        result = adapter.generate(scenes)
        assert isinstance(result, list)
        assert len(result) == 2


    def test_generate_with_timestamps_populates_words_by_scene(monkeypatch, _fake_env, tmp_path):
        """generate_with_timestamps — word-level 정렬 계약."""
        # elevenlabs.py 의 실제 signature 에 맞춰 조정. 최소 words_by_scene 이 dict | attr 로 노출되면 OK.
        def _stub(self, scenes):
            # Phase 5 test_elevenlabs_adapter.py 의 패턴 재현.
            return [
                {
                    "scene_id": s["scene_id"],
                    "audio_path": tmp_path / f"{s['scene_id']}.mp3",
                    "words": [{"word": w, "start": 0.0, "end": 0.5} for w in s["text"].split()],
                }
                for s in scenes
            ]

        monkeypatch.setattr(ElevenLabsAdapter, "generate_with_timestamps", _stub)
        adapter = ElevenLabsAdapter(api_key="fake", output_dir=tmp_path)
        result = adapter.generate_with_timestamps(
            [{"scene_id": "s1", "text": "hello world", "voice_id": "v1"}]
        )
        assert isinstance(result, list)
        assert "words" in result[0]


    def test_chars_to_words_round_trip():
        """D-10 _chars_to_words 결정론 — 동일 입력 → 동일 출력."""
        from scripts.orchestrator.api.elevenlabs import _chars_to_words

        char_timings = [
            {"char": "안", "start": 0.0, "end": 0.1},
            {"char": "녕", "start": 0.1, "end": 0.2},
            {"char": " ", "start": 0.2, "end": 0.3},
            {"char": "하", "start": 0.3, "end": 0.4},
            {"char": "이", "start": 0.4, "end": 0.5},
        ]
        a = _chars_to_words(char_timings)
        b = _chars_to_words(char_timings)
        assert a == b
        assert isinstance(a, list)
        assert len(a) >= 1


    def test_api_key_dual_env_alias(monkeypatch, tmp_path):
        """ELEVENLABS_API_KEY 와 ELEVEN_API_KEY 둘 다 수용."""
        monkeypatch.delenv("ELEVENLABS_API_KEY", raising=False)
        monkeypatch.delenv("ELEVEN_API_KEY", raising=False)

        # Primary name
        monkeypatch.setenv("ELEVENLABS_API_KEY", "primary")
        a1 = ElevenLabsAdapter(api_key=None, output_dir=tmp_path)
        assert getattr(a1, "api_key", None) == "primary"

        # Alias name
        monkeypatch.delenv("ELEVENLABS_API_KEY", raising=False)
        monkeypatch.setenv("ELEVEN_API_KEY", "alias")
        a2 = ElevenLabsAdapter(api_key=None, output_dir=tmp_path)
        assert getattr(a2, "api_key", None) == "alias"


    def test_default_voice_3tier_resolution(monkeypatch, _fake_env, tmp_path):
        """default_voice — kwarg > env > module cache > API discovery (mocked)."""
        # Tier 1: kwarg
        a1 = ElevenLabsAdapter(api_key="fake", default_voice="v-kwarg", output_dir=tmp_path)
        assert getattr(a1, "default_voice", None) == "v-kwarg"

        # Tier 2: env
        monkeypatch.setenv("ELEVENLABS_DEFAULT_VOICE", "v-env")
        a2 = ElevenLabsAdapter(api_key="fake", output_dir=tmp_path)
        assert getattr(a2, "default_voice", None) == "v-env"


    def test_empty_text_raises_value_error(_fake_env, tmp_path):
        """빈 scene text 입력 거부."""
        adapter = ElevenLabsAdapter(api_key="fake", output_dir=tmp_path)
        with pytest.raises((ValueError, AssertionError)):
            adapter.generate([{"scene_id": "s1", "text": "", "voice_id": "v1"}])


    def test_elevenlabs_mock_fault_injection_disabled_by_default():
        """D-3 Phase 7 — production-safe default."""
        from tests.phase07.mocks.elevenlabs_mock import ElevenLabsMock

        assert ElevenLabsMock().allow_fault_injection is False
    ```

    **구현 주의점**:
    - `_invoke_tts` 의 실제 signature 와 `generate_with_timestamps` 의 출력 shape 는 `scripts/orchestrator/api/elevenlabs.py` source 를 먼저 확인하고 test 의 stub/assertion 을 정확히 일치시킬 것.
    - `default_voice` 속성명이 다르면 source 에 맞춰 조정.
    - Phase 5 `tests/phase05/test_elevenlabs_adapter.py` 는 이미 monkeypatch 패턴을 사용 중 — 해당 파일을 **read-first** 로 패턴을 복제.
    - 테스트 실행 환경에서 Korean 문자열 사용 시 파일 encoding 은 `# -*- coding: utf-8 -*-` 혹은 Python 3 기본 UTF-8.

    **금기 #2/#3/#4/#9 준수**: TODO 없음, 에러 명시, T2V 없음, 신규 agent 없음.
  </action>
  <verify>
    <automated>pytest tests/adapters/test_elevenlabs_contract.py -v --no-cov 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/adapters/test_elevenlabs_contract.py -v --no-cov` exits 0.
    - `grep -c "pytest.mark.adapter_contract" tests/adapters/test_elevenlabs_contract.py` returns ≥1.
    - `grep -cE "^def test_" tests/adapters/test_elevenlabs_contract.py` returns ≥7.
    - `grep -c "test_chars_to_words_round_trip" tests/adapters/test_elevenlabs_contract.py` returns 1.
    - `grep -c "test_api_key_dual_env_alias" tests/adapters/test_elevenlabs_contract.py` returns 1.
    - `grep -c "test_elevenlabs_mock_fault_injection_disabled_by_default" tests/adapters/test_elevenlabs_contract.py` returns 1.
    - `grep -c "TODO" tests/adapters/test_elevenlabs_contract.py` returns 0.
    - `pytest -m adapter_contract tests/adapters/test_elevenlabs_contract.py --collect-only -q | grep -c "::test_"` returns ≥7.
  </acceptance_criteria>
  <done>
    ADAPT-02 충족 — elevenlabs adapter contract 7+ tests green + dual env alias + 3-tier voice resolution + D-3 mock invariant 검증.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 14-03-03: tests/adapters/test_shotstack_contract.py (ADAPT-03)</name>
  <files>tests/adapters/test_shotstack_contract.py</files>
  <read_first>
    - scripts/orchestrator/api/shotstack.py (전체 414줄 — DEFAULT_RESOLUTION + FILTER_ORDER + render + upscale + create_ken_burns_clip)
    - scripts/orchestrator/api/models.py (ShotstackRenderRequest + ContinuityPrefix)
    - tests/phase05/test_shotstack_adapter.py (monkeypatch template — httpx.Client)
    - tests/phase07/mocks/shotstack_mock.py (allow_fault_injection 검증)
    - .planning/phases/14-api-adapter-remediation/14-RESEARCH.md (§ADAPT-03)
  </read_first>
  <behavior>
    최소 7 tests 작성:
    - `test_shotstack_render_request_schema` — pydantic ShotstackRenderRequest 기본 validation.
    - `test_default_resolution_hd_orch11` — `DEFAULT_RESOLUTION == "hd"` (ORCH-11 720p).
    - `test_default_aspect_9_16` — 9:16 shorts 기본값.
    - `test_filter_order_d17` — `FILTER_ORDER == ("color_grade", "saturation", "film_grain")`.
    - `test_continuity_prefix_injected_at_filter_zero` — D-19 invariant (filter[0] 위치).
    - `test_upscale_is_noop` — Phase 5 RESEARCH §7 stub.
    - `test_create_ken_burns_clip_emits_deprecation_warning` — D-11 deprecation visible.
    - `test_shotstack_mock_fault_injection_disabled_by_default` — D-3 invariant.
  </behavior>
  <action>
    다음 내용의 `tests/adapters/test_shotstack_contract.py` 파일 신설 (참조: 14-RESEARCH.md §Code Examples lines 485-533 template + §ADAPT-03):

    ```python
    """Phase 14 ADAPT-03 — shotstack adapter contract.

    계약 축:
    1. pydantic ShotstackRenderRequest schema.
    2. DEFAULT_RESOLUTION == 'hd' (ORCH-11 720p first-pass render).
    3. FILTER_ORDER == ('color_grade', 'saturation', 'film_grain') (D-17).
    4. continuity_prefix 는 FILTER_ORDER[0] 위치에 injection (D-19 invariant).
    5. upscale() 는 Phase 5 RESEARCH §7 NOOP stub.
    6. create_ken_burns_clip() 는 D-11 DeprecationWarning 발생 (physical removal deferred).
    7. Phase 7 ShotstackMock().allow_fault_injection is False (D-3 invariant).

    real Shotstack API 호출 없음 — `httpx.Client` seam 을 monkeypatch.
    """
    from __future__ import annotations

    from pathlib import Path
    from unittest.mock import MagicMock, patch
    import warnings

    import pytest

    from scripts.orchestrator.api.shotstack import ShotstackAdapter

    pytestmark = pytest.mark.adapter_contract


    def test_shotstack_render_request_schema():
        """pydantic ShotstackRenderRequest 최소 schema."""
        from scripts.orchestrator.api.models import ShotstackRenderRequest

        # 기본 생성 — 실제 schema 필드명에 맞춰 조정 필요 (models.py source 참조).
        # 최소 timeline/resolution 필드가 있어야 함.
        req = ShotstackRenderRequest.model_construct()
        # 필드 존재 확인 (schema 이름 drift 감지).
        assert hasattr(ShotstackRenderRequest, "model_fields")
        field_names = set(ShotstackRenderRequest.model_fields.keys())
        # timeline 이든 payload 든 최소 하나의 non-trivial field 가 있어야 함.
        assert len(field_names) >= 1


    def test_default_resolution_hd_orch11():
        """ORCH-11 invariant: first-pass render 는 720p (hd)."""
        assert ShotstackAdapter.DEFAULT_RESOLUTION == "hd"


    def test_default_aspect_9_16():
        """Shorts 9:16 기본 aspect ratio."""
        # DEFAULT_ASPECT 속성명이 다르면 source 확인 후 조정.
        default_aspect = getattr(ShotstackAdapter, "DEFAULT_ASPECT", None)
        assert default_aspect in ("9:16", "9:16 vertical"), (
            f"DEFAULT_ASPECT={default_aspect!r} — Shorts 강제 9:16 이어야 함 (TS-1)."
        )


    def test_filter_order_d17():
        """D-17 invariant — color_grade → saturation → film_grain."""
        assert ShotstackAdapter.FILTER_ORDER == (
            "color_grade",
            "saturation",
            "film_grain",
        )


    def test_continuity_prefix_injected_at_filter_zero(monkeypatch, _fake_env, tmp_path):
        """D-19 invariant — continuity_prefix 는 FILTER_ORDER[0] (color_grade) 위치."""
        # FILTER_ORDER 의 첫 원소가 continuity-prefix anchor 인지 확인.
        assert ShotstackAdapter.FILTER_ORDER[0] == "color_grade"
        # render 호출 시 continuity_prefix 가 filter 리스트의 0번 위치에 삽입되는지 내부 로직 확인.
        # 실제 검증은 shotstack.py 의 filter 조립 로직에 맞춰 작성.
        # 최소 class-level constant 는 보장.


    def test_upscale_is_noop(_fake_env, tmp_path):
        """Phase 5 RESEARCH §7 — upscale() 는 NOOP stub."""
        adapter = ShotstackAdapter(api_key="fake", output_dir=tmp_path)
        # signature 에 맞춰 NOOP 반환 확인 — 입력을 그대로 반환하거나 dict passthrough.
        result = adapter.upscale({"url": "file:///tmp/in.mp4"})
        assert result is not None  # 최소 non-None — 구체 NOOP 형태는 source 참조.


    def test_create_ken_burns_clip_emits_deprecation_warning(_fake_env, tmp_path):
        """D-11 — shotstack create_ken_burns_clip 은 DeprecationWarning 발생."""
        adapter = ShotstackAdapter(api_key="fake", output_dir=tmp_path)
        fake_image = tmp_path / "hero.png"
        fake_image.write_bytes(b"fake-png")

        with warnings.catch_warnings(record=True) as warning_list:
            warnings.simplefilter("always")
            try:
                adapter.create_ken_burns_clip(fake_image, duration=3.0, scale=1.2, pan="left")
            except Exception:
                # NOOP 또는 placeholder — 내부 구현이 NotImplementedError 를 raise 할 수도 있음.
                # 여기서는 DeprecationWarning 이 발생했는지만 검증.
                pass
            deprecation_found = any(
                issubclass(w.category, DeprecationWarning) for w in warning_list
            )
            assert deprecation_found, (
                "create_ken_burns_clip 호출 시 DeprecationWarning 이 발생해야 함 (D-11). "
                "physical removal 은 Phase 10 batch window 로 유예."
            )


    def test_shotstack_mock_fault_injection_disabled_by_default():
        """D-3 Phase 7 — production-safe default."""
        from tests.phase07.mocks.shotstack_mock import ShotstackMock

        assert ShotstackMock().allow_fault_injection is False
    ```

    **구현 주의점**:
    - `ShotstackAdapter.DEFAULT_ASPECT` 의 실제 속성명이 source 에서 다를 경우 (예: `_DEFAULT_ASPECT`, `DEFAULT_ASPECT_RATIO`) source 확인 후 정확히 매칭.
    - `upscale()` 의 실제 signature 와 반환값 shape 확인 후 assertion 조정.
    - `create_ken_burns_clip()` 가 이미 raise NotImplementedError 로 변경되어 DeprecationWarning 이 발생하지 않는다면 RESEARCH 의 D-11 현재 상태 재확인 필요 — 현재 RESEARCH §Deferred Ideas 에 "DeprecationWarning 으로 기계적 제거 유예" 로 명시되어 있음.
    - `ShotstackRenderRequest` 의 필드명이 매우 유동적이므로 `model_fields` inspection 만 assert (field 이름 drift 만 감지).

    **금기 #2/#3/#4/#9 준수**: TODO 없음, 에러 명시 raise, T2V 없음, 신규 agent 없음.
  </action>
  <verify>
    <automated>pytest tests/adapters/test_shotstack_contract.py -v --no-cov 2>&amp;1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/adapters/test_shotstack_contract.py -v --no-cov` exits 0.
    - `grep -c "pytest.mark.adapter_contract" tests/adapters/test_shotstack_contract.py` returns ≥1.
    - `grep -cE "^def test_" tests/adapters/test_shotstack_contract.py` returns ≥7.
    - `grep -c "FILTER_ORDER" tests/adapters/test_shotstack_contract.py` returns ≥1.
    - `grep -c "DEFAULT_RESOLUTION" tests/adapters/test_shotstack_contract.py` returns ≥1.
    - `grep -c "DeprecationWarning" tests/adapters/test_shotstack_contract.py` returns ≥1.
    - `grep -c "allow_fault_injection" tests/adapters/test_shotstack_contract.py` returns ≥1.
    - `grep -c "TODO" tests/adapters/test_shotstack_contract.py` returns 0.
    - `pytest -m adapter_contract tests/adapters/test_shotstack_contract.py --collect-only -q | grep -c "::test_"` returns ≥7.
  </acceptance_criteria>
  <done>
    ADAPT-03 충족 — shotstack adapter contract 7+ tests green + FILTER_ORDER D-17 + continuity_prefix D-19 + ken_burns D-11 deprecation + D-3 mock invariant 전수 검증.
  </done>
</task>

</tasks>

<verification>
Wave 2 완료 조건 (Plan 03 전체):
1. 3 개 contract 파일 모두 존재 + `@pytest.mark.adapter_contract` 마킹 + 각 파일 ≥5 tests.
2. `pytest -m adapter_contract -v --no-cov` exits 0 with ≥15 tests green.
3. 각 contract 파일 개별 `pytest <file> -v --no-cov` green.
4. `grep -rc "TODO" tests/adapters/` returns 0 (전체 금기 #2 준수).
5. 3 개 파일 총 신규 테스트 수 ≥ 20 (veo 6 + elevenlabs 7 + shotstack 7).
6. Wave 1 regression 이 여전히 green — `pytest tests/phase05 tests/phase06 tests/phase07 --tb=no -q` 0 failures.
</verification>

<success_criteria>
- ADAPT-01/02/03 Success Criteria 전수 충족.
- `pytest -m adapter_contract` 단독 gate 가 ≥15 tests 녹색.
- Phase 7 mock 3 개 (VeoI2V mock 제외 — Phase 15 deferred) 모두 `allow_fault_injection=False` invariant 검증.
- 실 API 호출 0 회 — 모든 network seam monkeypatched.
- CLAUDE.md 금기 #2 (TODO), #3 (침묵 폴백), #4 (T2V), #9 (agent 초과) 전수 준수.
- Wave 1 regression baseline preserved.
</success_criteria>

<output>
완료 후 `.planning/phases/14-api-adapter-remediation/14-03-SUMMARY.md` 를 생성해 다음을 기록:
- 3 개 contract 파일 각각의 test 개수 및 요약.
- `pytest -m adapter_contract -v` 의 최종 출력 요약 줄.
- 각 파일이 참조하는 production source (veo_i2v / elevenlabs / shotstack) 의 import 경로.
- D-3 / D-11 / D-17 / D-19 invariant 검증 매핑 표.
</output>
