---
phase: 14-api-adapter-remediation
plan: 02
plan_id: 14-02
type: execute
wave: 1
depends_on:
  - 14-01
files_modified:
  - scripts/orchestrator/api/veo_i2v.py
  - tests/phase05/test_kling_adapter.py
  - tests/phase05/test_line_count.py
  - tests/phase06/test_moc_linkage.py
  - tests/phase06/test_notebooklm_wrapper.py
autonomous: true
requirements:
  - ADAPT-04
commit_discipline: per-atom
# Clarifying note (Warning 5 resolution):
# 'per-atom' disambiguation: "per-atom" 은 "file-level disjointness" 를 의미합니다.
# 단일 task 내 여러 file 수정은 허용되지 않으나 (각 task = 1 file edit = 1 commit),
# Task 14-02-03 은 유일한 예외: line_count 테스트 invariant (caps dict) 가 elevenlabs
# 와 shotstack line 을 공유하므로 같은 file (tests/phase05/test_line_count.py) 에서
# 두 라인을 같은 commit 으로 수정합니다. 이는 단일 test invariant 의 두 knob 조정이므로
# atomic.
must_haves:
  truths:
    - "15 pre-existing pytest failures 가 모두 green 으로 전환된다 (phase05/06/07)"
    - "veo_i2v.py 자체 문서 comment 가 더 이상 T2V blacklist regex 에 매칭되지 않는다 (금기 #4 self-documentation 해소)"
    - "runway adapter 테스트가 Phase 9.1 D-12 rename (gen3_alpha_turbo → gen4.5) 를 반영한다"
    - "elevenlabs.py (350줄) 와 shotstack.py (414줄) 의 line-count 테스트가 Phase 9.1 D-11/D-13 의도된 성장을 수용한다"
    - "MOC.md `status: partial` (Phase 9.1 정당 승격) 이 허용되면서도 `ready|complete` 로의 drift 는 여전히 차단된다"
    - "NotebookLM wrapper 의 default skill path 가 Phase 9 D09-DEF-02 migration (shorts_naberal → secondjob_naberal) 를 반영한다"
  artifacts:
    - path: "scripts/orchestrator/api/veo_i2v.py"
      provides: "T2V-forbidden I2V adapter with blacklist-safe self-documentation"
      contains: "class VeoI2VAdapter"
    - path: "tests/phase05/test_kling_adapter.py"
      provides: "Runway gen4.5 assertion (Phase 9.1 D-12 aligned)"
      contains: "gen4.5"
    - path: "tests/phase05/test_line_count.py"
      provides: "Line caps raised — elevenlabs 360 / shotstack 420"
      contains: "elevenlabs.py"
    - path: "tests/phase06/test_moc_linkage.py"
      provides: "MOC status invariant — scaffold OR partial accepted, ready/complete blocked"
    - path: "tests/phase06/test_notebooklm_wrapper.py"
      provides: "Default skill path — secondjob_naberal install"
      contains: "secondjob_naberal"
  key_links:
    - from: "scripts/orchestrator/api/veo_i2v.py"
      to: "tests/phase05/test_blacklist_grep.py"
      via: "self-documentation rewrite (no t2v/text_to_video tokens in source)"
      pattern: "T2VForbidden|text-only"
    - from: "tests/phase05/test_line_count.py"
      to: "scripts/orchestrator/api/{elevenlabs,shotstack}.py"
      via: "soft cap alignment with Phase 9.1 D-11/D-13 intentional growth"
---

<objective>
15 pre-existing pytest failures 전수 청산. Phase 9.1 측정 baseline (15 failed / 727 passed / 660s wall) 을 **6 atomic edit** 으로 전량 녹색 전환한다. 9 건 aggregator cascade 는 구조적으로 자동 green 이 되며 phase05/06/07 sweep 이 742/742 passed 로 회복된다. 실 adapter 코드 수정은 veo_i2v.py 자체 문서 한 블록 (lines 179-185, Research §Bucket A2 A-4 에 따름) 뿐이고, 나머지 5 건은 test-to-code synchronization.

Purpose: Phase 7 기준 986 regression baseline 을 복원하지 못하면 ADAPT-04 Success Criteria #1 이 실패하고 Wave 4 phase gate 가 차단된다. 본 wave 완료 후 `pytest tests/phase05 tests/phase06 tests/phase07` 이 0 failures 를 반환해야 Wave 2 (contract tests) 가 clean baseline 위에 안착한다.
Output: veo_i2v.py self-documentation rewrite + 4 test file 편집 (runway model rename / line caps raise / MOC status regex / notebooklm path migration).

**Parallelization**: 6 atoms 는 서로 disjoint file 이므로 **동시 실행 안전**. Claude 가 순차 실행하더라도 각 edit 독립 commit 하면 bisect 가능.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-api-adapter-remediation/14-RESEARCH.md
@.planning/phases/14-api-adapter-remediation/14-VALIDATION.md
@CLAUDE.md

# Source files (to be modified)
@scripts/orchestrator/api/veo_i2v.py
@tests/phase05/test_kling_adapter.py
@tests/phase05/test_line_count.py
@tests/phase06/test_moc_linkage.py
@tests/phase06/test_notebooklm_wrapper.py

# Reference — blacklist regex pattern (to study, do NOT modify)
@tests/phase05/test_blacklist_grep.py

<interfaces>
Current state evidence (from 14-RESEARCH.md — pytest 2026-04-21 measured):

1. `tests/phase05/test_kling_adapter.py:205` currently:
   ```python
   assert kwargs["model"] == "gen3_alpha_turbo"  # D-16
   ```
   Actual runtime returns `"gen4.5"` (Phase 9.1 D-12 rename — Runway Gen-3a Turbo failed complex-limb-motion in 세션 #24).

2. `tests/phase05/test_line_count.py:63-70` caps dict:
   ```python
   caps = {
       "models.py": 180,
       "kling_i2v.py": 260,
       "runway_i2v.py": 240,
       "typecast.py": 400,
       "elevenlabs.py": 340,  # ← breach: actual 350
       "shotstack.py": 400,   # ← breach: actual 414
   }
   ```

3. `tests/phase06/test_moc_linkage.py:46`:
   ```python
   assert "status: scaffold" in text, f"{moc}: MOC status MUST stay scaffold (D-17 structural)"
   ```
   MOC.md 실제 값은 `status: partial` (Phase 9.1 정당 승격).

4. `tests/phase06/test_notebooklm_wrapper.py:49-53`:
   ```python
   def test_default_skill_path_is_the_2026_install():
       assert str(DEFAULT_SKILL_PATH).replace("\\", "/") == (
           "C:/Users/PC/Desktop/shorts_naberal/.claude/skills/notebooklm"
       )
   ```
   실제 DEFAULT_SKILL_PATH 는 `secondjob_naberal/.claude/skills/notebooklm` (Phase 9 D09-DEF-02 migration).

5. `scripts/orchestrator/api/veo_i2v.py:179-185` (Research §Bucket A2 A-4 lines pinned):
   ```python
   # Physical T2V guard — same pattern as kling_i2v.py.
   # 이 모듈에 image_to_video 만 존재하고 text_to_video/t2v 는 절대 정의되지 않음을
   # 테스트 모듈 + pre_tool_use.py deprecated_patterns.json 이 3중 검증.
   assert not hasattr(VeoI2VAdapter, "text_to_video"), (
       "VIDEO-01 위반: VeoI2VAdapter 에 text_to_video 가 정의됨. "
       "D-13 anchor_frame 필수 규율 위반."
   )
   ```
   이 self-documentation 이 `test_blacklist_grep.py::test_no_t2v_in_orchestrator` 의 regex `(^|[^A-Za-z_])t2v([^A-Za-z_]|$)|text_to_video|text2video` 에 매칭되어 실패.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 14-02-01: veo_i2v.py self-documentation rewrite (T2V blacklist unblock)</name>
  <files>scripts/orchestrator/api/veo_i2v.py</files>
  <read_first>
    - scripts/orchestrator/api/veo_i2v.py (lines 170-185 전체)
    - tests/phase05/test_blacklist_grep.py (regex 정의 — lines 60-89)
    - .planning/phases/14-api-adapter-remediation/14-RESEARCH.md (§Bucket A2 A-4)
    - CLAUDE.md (금기사항 #4 — T2V / text_to_video / t2v 절대 금지)
  </read_first>
  <behavior>
    - Test: `pytest tests/phase05/test_blacklist_grep.py::test_no_t2v_in_orchestrator -x` exits 0.
    - Test: `py -3.11 -c "from scripts.orchestrator.api.veo_i2v import VeoI2VAdapter; assert not hasattr(VeoI2VAdapter, 'image_to_video_text_only')"` exits 0 (physical absence invariant 유지).
    - Test: `py -3.11 -c "from scripts.orchestrator.api import veo_i2v"` exits 0 (import 무결성).
    - 금기 regex `(^|[^A-Za-z_])t2v([^A-Za-z_]|$)|text_to_video|text2video` 가 veo_i2v.py 에 0회 매칭.
  </behavior>
  <action>
    **Pre-step (필수 — line 경계 확정)**: 편집 전에 **반드시** 다음 명령으로 실제 라인 boundary 를 확정:
    ```
    cat -n scripts/orchestrator/api/veo_i2v.py | sed -n '175,190p'
    ```
    Research §Bucket A2 A-4 는 해당 블록이 **라인 179-185** 에 위치함을 pin 함 (line 178 은 blank). 위 명령 출력에서 라인 번호가 Research 와 일치함을 검증 후 진행.

    `scripts/orchestrator/api/veo_i2v.py` 파일 **line 179-185** (module footer 전체 — T2V guard comment + assert 블록) 를 다음과 같이 **교체**:

    **BEFORE** (lines 179-185 현행 — blank line 178 은 유지):
    ```python
    # Physical T2V guard — same pattern as kling_i2v.py.
    # 이 모듈에 image_to_video 만 존재하고 text_to_video/t2v 는 절대 정의되지 않음을
    # 테스트 모듈 + pre_tool_use.py deprecated_patterns.json 이 3중 검증.
    assert not hasattr(VeoI2VAdapter, "text_to_video"), (
        "VIDEO-01 위반: VeoI2VAdapter 에 text_to_video 가 정의됨. "
        "D-13 anchor_frame 필수 규율 위반."
    )
    ```

    **AFTER** (교체 내용 — 라인 178 blank 보존, 179부터 주석만):
    ```python
    # Physical image-only guard — VIDEO-01 anchor_frame enforcement.
    # 본 모듈은 image_to_video 메서드 1 종만 공개합니다. 반대 극 (text-only
    # 입력에서 영상을 생성하는 메서드) 의 물리적 부재 검증은 repo blacklist
    # grep (tests/phase05/test_blacklist_grep.py) + pre_tool_use.py
    # deprecated_patterns.json + Phase 14 contract 테스트
    # (tests/adapters/test_veo_i2v_contract.py::test_no_text_only_method) 가
    # 3 중 수행합니다. 본 모듈의 source 에는 금기 토큰을 남기지 않습니다.
    ```

    즉, **module-level assert 블록 전체를 삭제**하고 주석만 남긴다. 금기 토큰 (`t2v`, `text_to_video`, `text2video`) 이 veo_i2v.py source 에 한 번도 나타나지 않아야 한다.

    **삭제된 assert** 의 역할 (VeoI2VAdapter 가 `text_to_video` 메서드를 가지지 않음) 은 Wave 2 Plan 03 의 `tests/adapters/test_veo_i2v_contract.py::test_no_text_only_method` 로 이관된다 (RESEARCH §ADAPT-01 "Option (b) preferred — contract test adopts the guard").

    **금기 #4 준수**: 수정 후 `grep -E "(^|[^A-Za-z_])t2v([^A-Za-z_]|$)|text_to_video|text2video" scripts/orchestrator/api/veo_i2v.py` 가 0 matches 를 반환해야 한다.
    **금기 #3 준수**: assert 블록 삭제로 인한 런타임 검증 공백은 contract 테스트 + blacklist grep + pre_tool_use hook 3중 방어로 보완 (주석에 명시).
  </action>
  <verify>
    <automated>pytest tests/phase05/test_blacklist_grep.py::test_no_t2v_in_orchestrator -x --no-cov -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/phase05/test_blacklist_grep.py::test_no_t2v_in_orchestrator -x --no-cov` exits 0.
    - `grep -cE "(^|[^A-Za-z_])t2v([^A-Za-z_]|$)|text_to_video|text2video" scripts/orchestrator/api/veo_i2v.py` returns 0.
    - `py -3.11 -c "from scripts.orchestrator.api.veo_i2v import VeoI2VAdapter"` exits 0 (import 무결성).
    - `py -3.11 -c "from scripts.orchestrator.api.veo_i2v import VeoI2VAdapter; assert not hasattr(VeoI2VAdapter, 'image_to_video_text_only')"` exits 0.
    - veo_i2v.py 전체 라인 수가 기존 185줄에서 감소 (185 → 약 178-180줄 예상, 최대 185 유지).
  </acceptance_criteria>
  <done>
    veo_i2v.py 의 self-documentation 에서 금기 토큰이 제거되고 blacklist grep 테스트가 green. VeoI2VAdapter 클래스 자체 기능 (image_to_video 메서드, T2VForbidden 예외) 은 변경 없음.
  </done>
</task>

<task type="auto">
  <name>Task 14-02-02: Runway model assertion update (D-12 gen4.5 alignment)</name>
  <files>tests/phase05/test_kling_adapter.py</files>
  <read_first>
    - tests/phase05/test_kling_adapter.py (lines 195-215 전후)
    - scripts/orchestrator/api/runway_i2v.py (lines 40-56 — DEFAULT_MODEL 확인)
    - .planning/phases/14-api-adapter-remediation/14-RESEARCH.md (§Bucket A A-1)
  </read_first>
  <action>
    `tests/phase05/test_kling_adapter.py` line 205 (함수 `test_runway_valid_call_returns_path` 내) 변경:

    **BEFORE** (line 205 현행):
    ```python
        assert kwargs["model"] == "gen3_alpha_turbo"  # D-16
    ```

    **AFTER**:
    ```python
        assert kwargs["model"] == "gen4.5"  # Phase 9.1 D-12 — Runway Gen-3a Turbo failed complex-limb-motion in 세션 #24
    ```

    주석에서 D-16 → Phase 9.1 D-12 로 authority reference 를 업데이트 (RESEARCH §Bucket A A-1 근거). line 206 (`kwargs["ratio"] == "720:1280"`) 과 line 207 (`kwargs["duration"] == 6`) 은 그대로 유지.

    **금기 #2 준수**: TODO 주석 금지, 즉시 완결.
  </action>
  <verify>
    <automated>pytest tests/phase05/test_kling_adapter.py::test_runway_valid_call_returns_path -x --no-cov -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/phase05/test_kling_adapter.py::test_runway_valid_call_returns_path -x --no-cov` exits 0.
    - `grep -c "gen4.5" tests/phase05/test_kling_adapter.py` returns ≥1.
    - `grep -c "gen3_alpha_turbo" tests/phase05/test_kling_adapter.py` returns 0.
    - `grep -c "720:1280" tests/phase05/test_kling_adapter.py` returns ≥1 (기존 ratio assertion 보존).
  </acceptance_criteria>
  <done>
    Runway adapter 테스트가 Phase 9.1 D-12 model rename 을 반영하여 `gen4.5` 로 pass.
  </done>
</task>

<task type="auto">
  <name>Task 14-02-03: Line-count soft caps raise (elevenlabs 340→360, shotstack 400→420)</name>
  <files>tests/phase05/test_line_count.py</files>
  <read_first>
    - tests/phase05/test_line_count.py (lines 58-80 전체)
    - scripts/orchestrator/api/elevenlabs.py (현재 350줄 — Phase 9.1 D-13 3-tier voice_id)
    - scripts/orchestrator/api/shotstack.py (현재 414줄 — Phase 9.1 D-11 DeprecationWarning + D-19 continuity_prefix)
    - .planning/phases/14-api-adapter-remediation/14-RESEARCH.md (§Bucket A A-2 + §ADAPT-02/03)
  </read_first>
  <action>
    `tests/phase05/test_line_count.py` lines 63-70 의 caps dict 를 수정:

    **BEFORE** (lines 63-70 현행):
    ```python
        caps = {
            "models.py": 180,
            "kling_i2v.py": 260,
            "runway_i2v.py": 240,
            "typecast.py": 400,
            "elevenlabs.py": 340,
            "shotstack.py": 400,
        }
    ```

    **AFTER**:
    ```python
        caps = {
            "models.py": 180,
            "kling_i2v.py": 260,
            "runway_i2v.py": 240,
            "typecast.py": 400,
            # Phase 9.1 D-13 — 3-tier voice_id resolution (constructor > env > module cache) intentionally grew elevenlabs.py.
            # Phase 14 RESEARCH §Bucket A A-2 authority: raise cap 340 → 360.
            "elevenlabs.py": 360,
            # Phase 9.1 D-11 — ken_burns DeprecationWarning + D-19 continuity_prefix injection at filter[0] intentionally grew shotstack.py.
            # Phase 14 RESEARCH §Bucket A A-2 authority: raise cap 400 → 420.
            "shotstack.py": 420,
        }
    ```

    주석을 통해 이 변경의 authority (Phase 9.1 D-11/D-13 + Phase 14 RESEARCH) 를 기록하여 향후 drift 감사 시 trail 확보.

    **Commit-discipline note (Warning 5)**: 이 task 는 `commit_discipline: per-atom` 의 유일한 예외 — 단일 파일 (test_line_count.py) 의 단일 caps dict invariant 의 두 knob (elevenlabs line + shotstack line) 이 atomic 하게 함께 조정됨. Frontmatter 주석 참조.

    **필수 #8 준수**: 증거 기반 — 주석에 출처 명시.
  </action>
  <verify>
    <automated>pytest tests/phase05/test_line_count.py::test_api_adapters_under_soft_caps -x --no-cov -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/phase05/test_line_count.py::test_api_adapters_under_soft_caps -x --no-cov` exits 0.
    - `grep -c "elevenlabs.py.*360" tests/phase05/test_line_count.py` returns ≥1.
    - `grep -c "shotstack.py.*420" tests/phase05/test_line_count.py` returns ≥1.
    - `grep -c "Phase 9.1 D-13" tests/phase05/test_line_count.py` returns ≥1 (authority reference).
    - `grep -c "Phase 14 RESEARCH" tests/phase05/test_line_count.py` returns ≥2 (authority reference for both caps).
  </acceptance_criteria>
  <done>
    Line-count 테스트가 Phase 9.1 의도된 adapter 성장을 수용하면서 향후 drift 감시는 유지 (360/420 은 최신 caps, 더 이상 성장은 차단). 'per-atom' commit 규율 예외로서 단일 caps dict invariant 의 두 knob 를 단일 commit 으로 조정 — frontmatter disambiguation 주석 참조.
  </done>
</task>

<task type="auto">
  <name>Task 14-02-04: MOC status invariant relaxation (scaffold|partial; ready blocked)</name>
  <files>tests/phase06/test_moc_linkage.py</files>
  <read_first>
    - tests/phase06/test_moc_linkage.py (lines 35-50 전체)
    - wiki/render/MOC.md (frontmatter 확인 — status 필드)
    - .planning/phases/14-api-adapter-remediation/14-RESEARCH.md (§Bucket B B-1 + §Risks R2)
  </read_first>
  <action>
    `tests/phase06/test_moc_linkage.py` 의 `test_moc_frontmatter_unchanged_scaffold` 함수 (lines 41-46) 를 수정:

    **BEFORE** (현행):
    ```python
    def test_moc_frontmatter_unchanged_scaffold(repo_root: Path):
        """Sanity: MOC frontmatter status stays scaffold (not promoted to ready)."""
        for moc_rel, _ in MOC_CHECK_EXPECTATIONS:
            moc = repo_root / "wiki" / moc_rel
            text = moc.read_text(encoding="utf-8")
            assert "status: scaffold" in text, f"{moc}: MOC status MUST stay scaffold (D-17 structural)"
    ```

    **AFTER**:
    ```python
    def test_moc_frontmatter_unchanged_scaffold(repo_root: Path):
        """MOC frontmatter status — scaffold OR partial allowed, ready/complete blocked.

        D-17 invariant 은 "MOC-as-TOC 는 ready 로 승격되지 않는다" 이며, Phase 9.1
        에서 production-bible-driven update 가 `scaffold → partial` 로의 정당 승격을
        발생시켰다. 따라서 본 테스트는 (scaffold|partial) 를 허용하고 (ready|complete)
        만 drift 로 차단한다. 출처: Phase 14 RESEARCH §Bucket B B-1.
        """
        import re

        for moc_rel, _ in MOC_CHECK_EXPECTATIONS:
            moc = repo_root / "wiki" / moc_rel
            text = moc.read_text(encoding="utf-8")
            assert re.search(r"^status:\s*(scaffold|partial)\b", text, re.MULTILINE), (
                f"{moc}: MOC status MUST be 'scaffold' or 'partial' (D-17 structural, "
                "Phase 9.1 legitimate progression). Actual text head: "
                f"{text[:200]!r}"
            )
            assert not re.search(r"^status:\s*(ready|complete)\b", text, re.MULTILINE), (
                f"{moc}: MOC status MUST NOT be 'ready' or 'complete' (D-17 invariant). "
                "MOC-as-TOC 는 영구 half-baked 상태 유지."
            )
    ```

    두 assert 로 (a) 허용 상태 `scaffold|partial`, (b) 금지 상태 `ready|complete` 를 양방향 검증. regex `^status:\s*(...)\b` 로 line-anchored + word-boundary 매칭하여 문자열 포함 매칭보다 엄격.

    **Canonical regex (Warning 4 alignment)**: `^status:\s*(scaffold|partial)\b` 는 본 테스트와 Plan 04 Task 14-04-03 acceptance 에서 동일하게 재사용되는 표준 regex. 두 plan 사이 일관성 유지 필수.

    **금기 #3 준수**: try-except 금지, regex 실패는 AssertionError 로 명시 전파.
    **RESEARCH R2 mitigation**: invariant tightening — `ready|complete` drift 는 여전히 차단.
  </action>
  <verify>
    <automated>pytest tests/phase06/test_moc_linkage.py::test_moc_frontmatter_unchanged_scaffold -x --no-cov -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/phase06/test_moc_linkage.py::test_moc_frontmatter_unchanged_scaffold -x --no-cov` exits 0.
    - `grep -c "scaffold|partial" tests/phase06/test_moc_linkage.py` returns ≥1.
    - `grep -c "ready|complete" tests/phase06/test_moc_linkage.py` returns ≥1 (양방향 검증).
    - `grep -c "Phase 14 RESEARCH" tests/phase06/test_moc_linkage.py` returns ≥1 (authority).
    - `grep -c "import re" tests/phase06/test_moc_linkage.py` returns ≥1.
  </acceptance_criteria>
  <done>
    MOC.md `status: partial` 이 허용되며 `ready|complete` drift 는 여전히 차단. Phase 9.1 정당 승격 을 수용하면서 D-17 spirit (MOC = half-baked TOC) 는 보존. Canonical regex `^status:\s*(scaffold|partial)\b` 가 Plan 04 Task 14-04-03 과 동일하게 재사용 가능.
  </done>
</task>

<task type="auto">
  <name>Task 14-02-05: NotebookLM default skill path migration (D09-DEF-02)</name>
  <files>tests/phase06/test_notebooklm_wrapper.py</files>
  <read_first>
    - tests/phase06/test_notebooklm_wrapper.py (lines 45-60 전체)
    - scripts/notebooklm/query.py (DEFAULT_SKILL_PATH 정의 — line 44 부근)
    - scripts/notebooklm/__init__.py (lines 7-10 — migration rationale 주석)
    - .planning/phases/14-api-adapter-remediation/14-RESEARCH.md (§Bucket B B-2)
  </read_first>
  <action>
    `tests/phase06/test_notebooklm_wrapper.py` 의 `test_default_skill_path_is_the_2026_install` 함수 (lines 49-53) 를 수정:

    **BEFORE** (현행):
    ```python
    def test_default_skill_path_is_the_2026_install():
        """D-7 hardcoded fallback value matches the 2026-04-19 install location."""
        assert str(DEFAULT_SKILL_PATH).replace("\\", "/") == (
            "C:/Users/PC/Desktop/shorts_naberal/.claude/skills/notebooklm"
        )
    ```

    **AFTER**:
    ```python
    def test_default_skill_path_is_the_2026_install():
        """D-7 hardcoded fallback value — Phase 9 D09-DEF-02 migrated to secondjob_naberal install.

        Migration authority: scripts/notebooklm/__init__.py lines 7-10 (5-notebook registry
        consolidation 2026-04-19). Previous path was shorts_naberal; current is secondjob_naberal.
        Source: Phase 14 RESEARCH §Bucket B B-2.
        """
        assert str(DEFAULT_SKILL_PATH).replace("\\", "/") == (
            "C:/Users/PC/Desktop/secondjob_naberal/.claude/skills/notebooklm"
        )
    ```

    Expected string 만 교체. 주석에 migration authority 명시.

    **금기 #6 준수**: `shorts_naberal` 원본 디렉토리 수정 금지 — 본 변경은 `studios/shorts/tests/` 내부 테스트 파일의 문자열 교체일 뿐, 실제 `shorts_naberal/` 디렉토리는 건드리지 않음.
    **필수 #8 준수**: 증거 기반 — __init__.py migration 주석을 authority 로 참조.
  </action>
  <verify>
    <automated>pytest tests/phase06/test_notebooklm_wrapper.py::test_default_skill_path_is_the_2026_install -x --no-cov -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/phase06/test_notebooklm_wrapper.py::test_default_skill_path_is_the_2026_install -x --no-cov` exits 0.
    - `grep -c "secondjob_naberal/.claude/skills/notebooklm" tests/phase06/test_notebooklm_wrapper.py` returns ≥1.
    - `grep -c "shorts_naberal/.claude/skills/notebooklm" tests/phase06/test_notebooklm_wrapper.py` returns 0.
    - `grep -c "D09-DEF-02" tests/phase06/test_notebooklm_wrapper.py` returns ≥1 (authority reference).
  </acceptance_criteria>
  <done>
    Default skill path 테스트가 Phase 9 D09-DEF-02 migration 을 반영. 실제 `secondjob_naberal/.claude/skills/notebooklm` 경로와 일치.
  </done>
</task>

<task type="auto">
  <name>Task 14-02-06: Wave 1 regression sweep — 15→0 failures</name>
  <files>.planning/phases/14-api-adapter-remediation/14-02-wave1-sweep.log</files>
  <read_first>
    - .planning/phases/14-api-adapter-remediation/14-RESEARCH.md (§Bucket A + B + C 전체 — 15 failures 분류)
    - .planning/phases/14-api-adapter-remediation/14-VALIDATION.md (§Sampling Rate — "After every plan wave")
  </read_first>
  <action>
    Task 14-02-01 ~ 14-02-05 가 모두 완료된 후 다음 명령을 실행하여 Wave 1 종료 gate 를 검증하고 산출물을 log 로 anchoring:

    ```
    pytest tests/phase05 tests/phase06 tests/phase07 --tb=short -q --no-cov 2>&1 | tee .planning/phases/14-api-adapter-remediation/14-02-wave1-sweep.log
    ```

    **기대 결과**: `0 failed` (15 pre-existing → 0). 만약 1건이라도 실패하면:
    1. 실패 test nodeid 를 로그에서 추출.
    2. 해당 실패가 14-RESEARCH.md §15 Failures Inventory 의 15건 중 하나인지 대조.
    3. 아니라면 **신규 failure** → Wave 2 진입 금지, diagnosis 후 추가 atomic fix 필요.
    4. 맞다면 Task 14-02-01~05 중 어느 edit 이 누락/오적용 되었는지 재점검.

    **Windows cp949 주의 (RESEARCH R5 mitigation)**: pytest 실행 환경에서 Korean 출력이 mojibake 가 되지 않도록 `PYTHONIOENCODING=utf-8` 필요 시 주입:
    ```
    PYTHONIOENCODING=utf-8 pytest tests/phase05 tests/phase06 tests/phase07 --tb=short -q --no-cov 2>&1 | tee .planning/phases/14-api-adapter-remediation/14-02-wave1-sweep.log
    ```

    **금기 #3 준수**: try-except 로 실패 은폐 금지. log 는 있는 그대로 anchoring.
    **필수 #8 준수**: 증거 기반 보고 — log 파일이 Wave 1 완료의 primary evidence.
  </action>
  <verify>
    <automated>bash -c 'test -f .planning/phases/14-api-adapter-remediation/14-02-wave1-sweep.log && grep -cE "^[0-9]+ passed" .planning/phases/14-api-adapter-remediation/14-02-wave1-sweep.log && [ "$(grep -cE "^[0-9]+ failed" .planning/phases/14-api-adapter-remediation/14-02-wave1-sweep.log)" = "0" ]'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/14-api-adapter-remediation/14-02-wave1-sweep.log` returns 0.
    - `grep -cE "^[0-9]+ passed" .planning/phases/14-api-adapter-remediation/14-02-wave1-sweep.log` returns ≥1 (pytest 요약 줄 존재).
    - POSIX-safe 실패 검사: `[ "$(grep -cE '[0-9]+ failed' .planning/phases/14-api-adapter-remediation/14-02-wave1-sweep.log)" = "0" ]` returns 0 (실패 요약 부재 — Warning 3: `! grep` bash-specific 회피).
    - 로그 요약 줄이 Phase 7 기준 `742 passed` 이상 (986 regression preserved + 기존 15 건 flip).
    - 로그에 `tests/phase05` + `tests/phase06` + `tests/phase07` 3 phase 모두 실행 흔적 포함.
  </acceptance_criteria>
  <done>
    Phase 5/6/7 regression sweep 이 0 failures 로 완료되고 로그가 anchoring 되어 Wave 2 진입을 허가한다. ADAPT-04 Success Criteria #1 충족. Verify 명령은 POSIX-safe 양식 (`[ "$(...)" = "0" ]`) 으로 `! grep` bash-only 문법 회피.
  </done>
</task>

</tasks>

<verification>
Wave 1 완료 조건 (Plan 02 전체):
1. 6 atomic edit 전수 적용 (veo_i2v.py + 4 test files + regression sweep).
2. `pytest tests/phase05 tests/phase06 tests/phase07 --tb=short -q --no-cov` exits 0 with "0 failed".
3. 각 atom 별 pinned pytest 명령 (Task 14-02-01~05) 이 개별 green.
4. `14-02-wave1-sweep.log` 이 evidence 로 저장됨.
5. veo_i2v.py 에 금기 토큰 (`t2v`, `text_to_video`, `text2video`) 0회 등장.
6. Phase 9.1 measured baseline 15 failed → 0 failed (15 건 전수 청산).
</verification>

<success_criteria>
- ADAPT-04 Success Criteria #1 충족: `pytest tests/phase05 tests/phase06 tests/phase07` 0 failures.
- 6 atomic fix 모두 disjoint file, 각 fix 별 독립 verify 명령 green.
- CLAUDE.md 금기사항 #2 (TODO 금지), #3 (침묵 폴백 금지), #4 (T2V 금지), #6 (shorts_naberal 원본 보존) 전수 준수.
- RESEARCH R1 (pytest.ini 도입), R2 (MOC invariant), R5 (cp949) mitigation 적용.
- Wave 2 Plan 03 이 clean baseline 위에 contract 테스트를 생성할 수 있는 상태.
</success_criteria>

<output>
완료 후 `.planning/phases/14-api-adapter-remediation/14-02-SUMMARY.md` 를 생성해 다음을 기록:
- 6 atomic edit 각각의 before/after 요약 (file:line 기반).
- `14-02-wave1-sweep.log` 의 최종 요약 줄 (`XXX passed, 0 failed in YYYs`).
- Phase 9.1 baseline 대비 delta: 15 failed → 0 failed (15 resolved, 0 regressed).
- veo_i2v.py 라인 수 변화 (185 → 현재).
</output>
</output>
