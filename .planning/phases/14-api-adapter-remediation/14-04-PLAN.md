---
phase: 14-api-adapter-remediation
plan: 04
plan_id: 14-04
type: execute
wave: 3
depends_on:
  - 14-01
  - 14-02
  - 14-03
files_modified:
  - wiki/render/adapter_contracts.md
  - wiki/render/MOC.md
  - tests/adapters/test_adapter_contracts_doc.py
  - .claude/hooks/pre_tool_use.py
  - .gitignore
autonomous: true
requirements:
  - ADAPT-05
  - ADAPT-06
commit_discipline: per-artifact
must_haves:
  truths:
    - "`wiki/render/adapter_contracts.md` 파일이 존재하고 7 adapter × 5 column 매트릭스 + mock↔real delta + retry/fallback rails + 금기 #4 I2V-only 주석을 포함한다"
    - "whisperx row 는 'NOT YET IMPLEMENTED — Phase 15+ stub' 으로 명시되어 drift 감사 시 spec 불일치가 발생하지 않는다"
    - "구조적 validator 테스트가 doc 의 7-row 매트릭스 + frontmatter + 필수 섹션을 검증한다"
    - "wiki/render/MOC.md 의 링크/체크박스가 adapter_contracts.md 를 포함하도록 업데이트된다"
    - "ADAPT-06c 선택 Hook 은 Option B (warn-only) 로 pre_tool_use.py 에 통합되어 adapter 파일 수정 시 contract 테스트 touch 여부를 경고만 하며 작업을 차단하지 않는다"
    - "`.gitignore` 가 `.claude/hooks/_adapter_contract_touch.json` 을 포함하여 session-scope ephemeral tracking 파일이 커밋되지 않는다"
  artifacts:
    - path: "wiki/render/adapter_contracts.md"
      provides: "ADAPT-05 — 7 adapter 입력/출력 schema + retry/fallback + fault injection 지원 여부 + mock↔real delta"
      contains: "adapter_contract"
      min_sections: 4
    - path: "tests/adapters/test_adapter_contracts_doc.py"
      provides: "ADAPT-05 구조적 validator — 문서 존재 + frontmatter + 7 row 매트릭스"
      contains: "pytest.mark.adapter_contract"
    - path: ".claude/hooks/pre_tool_use.py"
      provides: "ADAPT-06c warn-only adapter-contract drift 경고 (Option B)"
      contains: "adapter_contract"
    - path: ".gitignore"
      provides: "Session-scope tracking file exclusion"
      contains: "_adapter_contract_touch"
  key_links:
    - from: "wiki/render/adapter_contracts.md"
      to: "tests/adapters/test_*_contract.py"
      via: "명시적 cross-reference 섹션 (어떤 contract 테스트 파일이 어떤 adapter 를 커버하는지)"
    - from: "wiki/render/MOC.md"
      to: "wiki/render/adapter_contracts.md"
      via: "TOC 링크 체크박스"
      pattern: "adapter_contracts.md"
    - from: ".claude/hooks/pre_tool_use.py"
      to: "scripts/orchestrator/api/*.py + tests/adapters/test_*_contract.py"
      via: "편집 대상이 adapter 이면 contract 테스트 동반 편집 여부 검사 (warn-only)"
    - from: ".gitignore"
      to: ".claude/hooks/_adapter_contract_touch.json"
      via: "pattern 행 추가 (session-scope ephemeral)"
---

<objective>
Wave 3 — (a) `wiki/render/adapter_contracts.md` 신설하여 7 adapter 의 schema + retry/fallback + fault injection 계약을 단일 문서로 anchoring (ADAPT-05), (b) 해당 doc 을 검증하는 구조적 validator 테스트 추가, (c) wiki/render/MOC.md 링크 업데이트, (d) ADAPT-06c 의 선택 Hook 을 **Option B (warn-only)** 방식으로 `.claude/hooks/pre_tool_use.py` 에 통합하여 향후 adapter 수정 시 contract 테스트 동반 편집 여부를 경고한다. (e) `.gitignore` 에 session-scope tracking 파일 exclude 항목 추가.

Purpose: Phase 14 의 기술적 핵심 (contract 테스트) 은 Wave 2 에 끝났지만, **drift 재발 방지** 는 문서 + Hook 없이 완성되지 않는다. 문서 없으면 향후 adapter 가 새로 추가되어도 계약이 코드화되기 전에 또 drift 가 발생하고, Hook 없으면 adapter 파일만 수정하고 contract 테스트를 빠뜨리는 패턴이 재발한다. RESEARCH §ADAPT-06c 에서 Option B (warn-only) 가 Option A (defer) 보다 선호되는 이유는 "15 lines Python 으로 구현 가능 + 작업 차단 없음" 이며, 본 plan 은 이를 그대로 채택한다.
Output: 1 문서 파일 + 1 structural validator 테스트 + 1 MOC 업데이트 + 1 Hook 확장 + 1 .gitignore entry.

**Parallelization**: 5 tasks 중 문서 / 테스트 / MOC 는 disjoint, Hook 확장은 `.gitignore` 수정과 한 task 로 묶임 (논리적 single concern — tracking file wiring).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-api-adapter-remediation/14-RESEARCH.md
@.planning/phases/14-api-adapter-remediation/14-VALIDATION.md
@CLAUDE.md

# Existing wiki/render/ ecosystem (natural home for adapter_contracts.md)
@wiki/render/MOC.md
@wiki/render/remotion_kling_stack.md
@wiki/render/i2v_prompt_engineering.md

# Adapter source (document against — do NOT modify)
@scripts/orchestrator/api/kling_i2v.py
@scripts/orchestrator/api/runway_i2v.py
@scripts/orchestrator/api/veo_i2v.py
@scripts/orchestrator/api/typecast.py
@scripts/orchestrator/api/elevenlabs.py
@scripts/orchestrator/api/shotstack.py
@scripts/orchestrator/api/ken_burns.py
@scripts/orchestrator/api/nanobanana.py

# Hook (to extend warn-only)
@.claude/hooks/pre_tool_use.py

# .gitignore (to extend)
@.gitignore

# Phase 7 mocks (document signatures)
@tests/phase07/mocks/kling_mock.py
@tests/phase07/mocks/runway_mock.py
@tests/phase07/mocks/typecast_mock.py
@tests/phase07/mocks/elevenlabs_mock.py
@tests/phase07/mocks/shotstack_mock.py
</context>

<tasks>

<task type="auto">
  <name>Task 14-04-01: wiki/render/adapter_contracts.md — 7 adapter 계약 문서</name>
  <files>wiki/render/adapter_contracts.md</files>
  <read_first>
    - scripts/orchestrator/api/{kling_i2v,runway_i2v,veo_i2v,typecast,elevenlabs,shotstack,ken_burns,nanobanana}.py (전부 — 각 adapter 의 실제 signature + class const 확인)
    - tests/phase07/mocks/{kling,runway,typecast,elevenlabs,shotstack}_mock.py (mock signature + allow_fault_injection 기본값)
    - wiki/render/MOC.md (frontmatter + 기존 entries)
    - wiki/render/remotion_kling_stack.md (frontmatter 양식 참조)
    - .planning/phases/14-api-adapter-remediation/14-RESEARCH.md (§ADAPT-05 lines 216-265, §Adapter File Locations 114-128, §Mock↔Real Contract Deltas)
  </read_first>
  <action>
    `wiki/render/adapter_contracts.md` 를 신설. 다음 구조를 엄격히 따르되, 각 셀은 adapter source 에서 실측한 값을 채운다:

    ```markdown
    ---
    category: render
    status: ready
    tags: [contract, adapter, phase14]
    updated: 2026-04-21
    owner: Phase 14 API Adapter Remediation
    ---

    # Adapter Contracts — Phase 14 Baseline

    > naberal-shorts-studio 의 모든 외부 API adapter 의 입력 schema + 출력 schema
    > + retry/fallback 규칙 + fault injection 지원 여부 + mock↔real 계약 차이를
    > 단일 지점에 anchoring 한다. Phase 5/6/7 integration 테스트와 Phase 14
    > contract 테스트 (`tests/adapters/test_*_contract.py`) 의 상위 개념 문서.

    ## Adapter Registry

    | Adapter | Purpose | Input Schema | Output Schema | Retry/Fallback | Fault Injection |
    |---------|---------|--------------|---------------|----------------|-----------------|
    | kling_i2v | Primary I2V (Kling 2.6 Pro via fal.ai) | `I2VRequest(prompt, anchor_frame REQUIRED, duration_seconds)` | `Path` (MP4) | CircuitBreaker 3회/300s → veo_i2v fallback | `KlingMock(fault_mode="circuit_3x"\|"runway_failover")` |
    | runway_i2v | HELD backup I2V (Gen-4.5, off production path) | `I2VRequest` | `Path` | — (deferred-items.md §2.1) | `RunwayMock` |
    | veo_i2v | Fallback I2V (Veo 3.1 Fast via fal.ai, precision motion failover) | `I2VRequest` (anchor REQUIRED; `T2VForbidden` raise 시 None) | `Path` | Called when Kling CircuitBreaker OPEN | No dedicated mock (Phase 15 add) |
    | typecast | Primary TTS (Korean) | scenes `list[dict]` (scene_id, text, voice_id, emotion) | `list[AudioSegment]` | ElevenLabs fallback on failure | `TypecastMock(allow_fault_injection=False)` |
    | elevenlabs | Fallback TTS + word-level timestamps (D-10 `_chars_to_words`) | scenes `list[dict]` | `list[AudioSegment]` + `words_by_scene` | Tier-3 EdgeTTS (not yet implemented) | `ElevenLabsMock(allow_fault_injection=False)` |
    | shotstack | Assembly/720p render + continuity_prefix (D-19) | `ShotstackRenderRequest` | `dict` with `response.url` | KenBurnsLocal fallback for THUMBNAIL gate | `ShotstackMock(allow_fault_injection=False)` |
    | whisperx | Subtitle alignment — **NOT YET IMPLEMENTED (Phase 15+ stub)** | audio_path + text (계획) | word-level timings (계획) | 현재 경로 = ElevenLabs `_chars_to_words` D-10 | — |

    ## Mock ↔ Real Contract Deltas

    | Adapter | Real signature | Mock signature | Delta rationale |
    |---------|---------------|----------------|-----------------|
    | kling_i2v | `image_to_video(prompt: str, anchor_frame: Path\|None, duration_seconds: int = 5) → Path` | `image_to_video(*args, **kwargs) → Path` | Mock 은 호출 convention 불문 수용 (pipeline kwargs + legacy positional 양쪽) |
    | typecast | `generate(scenes: list[dict]) → list[AudioSegment]` | `generate(*args, **kwargs) → list[dict]` | Mock 은 D-18 Phase 7 unit contract 에 따라 list[dict] 반환; pipeline 은 `VoiceFirstTimeline.align → []` 로 patch |
    | elevenlabs | `generate_with_timestamps(scenes) → list[AudioSegment] + words_by_scene attr` | `generate_with_timestamps(*args, **kwargs) → list[dict]` (word dicts) | Mock 은 attr-on-class 패턴을 dict-of-dicts 로 단순화 |
    | shotstack | `render(timeline, resolution, aspect_ratio) → dict` | `render(payload=None, *args, **kwargs) → dict` | Mock 은 v1/v2 envelope shape 양쪽 흡수 |

    ## Retry / Fallback Rails

    1. **I2V chain**: `kling_i2v` → (CircuitBreaker 3회/300s OPEN) → `veo_i2v`. `runway_i2v` 는 off-path (deferred-items.md §2.1).
    2. **TTS chain**: `typecast` → `elevenlabs` (explicit raise 또는 empty output 시). Tier-3 EdgeTTS 는 아직 미구현.
    3. **Render chain**: `shotstack` → (DeprecationWarning-guarded ken_burns path) → `ken_burns_local`. THUMBNAIL gate 전용 (Phase 7 RESEARCH Correction 3).
    4. **Subtitle alignment chain**: 현재 `elevenlabs._chars_to_words` (D-10). whisperx 도입 시 Tier-0 primary + elevenlabs Tier-1 fallback 으로 재설계 예정 (Phase 15+).

    ## Production-Safe Defaults (D-3 Phase 7 Invariant)

    - 모든 Phase 7 Mock 은 `allow_fault_injection=False` 로 기본값 선언.
    - Real adapter 는 `allow_fault_injection` 속성을 **가지지 않는다** — production 코드에는 fault-injection toggle 이 없다.
    - Contract 테스트는 `tests/adapters/test_*_contract.py::test_*_mock_fault_injection_disabled_by_default` 로 전수 검증.

    ## 금기사항 강제 (CLAUDE.md)

    - **금기 #4** T2V / text_to_video / t2v: kling / runway / veo_i2v 3 종 I2V adapter 는 모두 anchor_frame 필수.
      `veo_i2v` 는 `anchor_frame=None` 입력 시 `T2VForbidden` raise.
    - **금기 #3** try-except 침묵 폴백: 모든 adapter 는 실패 시 명시적 raise; retry 는 CircuitBreaker 레이어에서만 허용.
    - **금기 #5** Selenium 업로드: shotstack/publisher 는 YouTube Data API v3 공식 경로만 사용.

    ## Contract 테스트 Cross-Reference

    | Adapter | Contract 테스트 파일 | Phase | Test 개수 |
    |---------|---------------------|-------|----------|
    | veo_i2v | `tests/adapters/test_veo_i2v_contract.py` | 14 (Plan 03 Task 14-03-01) | ≥ 6 |
    | elevenlabs | `tests/adapters/test_elevenlabs_contract.py` | 14 (Plan 03 Task 14-03-02) | ≥ 7 |
    | shotstack | `tests/adapters/test_shotstack_contract.py` | 14 (Plan 03 Task 14-03-03) | ≥ 7 |
    | kling_i2v | (Phase 5 `tests/phase05/test_kling_adapter.py` — unit) + 추후 contract 이관 | 5 / Phase 15 후보 | n/a |
    | runway_i2v | (Phase 5 `tests/phase05/test_kling_adapter.py::test_runway_*`) + deferred 제거 후 정리 | 5 / Phase 10 batch | n/a |
    | typecast | (Phase 5 unit + Phase 7 mock) + 추후 contract 이관 | 5 / Phase 15 후보 | n/a |
    | whisperx | **NOT YET IMPLEMENTED** | — | — |

    ## Phase 13 Smoke (real API) 경계

    - 본 문서의 contract 테스트는 **schema + signature + retry 로직** 만 mock 기반으로 검증.
    - **실 API 회귀** (network error, 429 rate limit, 실 payload 파싱) 는 Phase 13 SMOKE-01~06 범위.
    - 두 layer 는 상호 보완 — contract 은 빠르고 (~ 30s), smoke 는 느리고 비싼 (실 API, 과금).

    ## 갱신 기록

    - **2026-04-21** Phase 14 Plan 04 Task 14-04-01 신설.
    ```

    **구현 주의점**:
    - adapter 실제 signature (예: `kling_i2v.image_to_video` 의 keyword arg 이름) 는 source 를 직접 읽고 문서화. 본 template 은 RESEARCH 의 기대치이며 실측과 다르면 실측 우선.
    - "whisperx" 행의 "NOT YET IMPLEMENTED" 라벨은 RESEARCH §Open Question #2 의 recommended Option A (stub row 유지) 에 따른다.
    - frontmatter `status: ready` 는 본 문서가 신설 직후 production-ready 한 계약 문서이기 때문 (Phase 9.1 MOC 처럼 scaffold/partial 로 제약 받지 않음 — MOC 와는 다른 category).
    - MOC 가 포괄하는 범위 (scaffold TOC) 가 아니라 **세부 contract 문서** 이므로 status=ready 적법.
    - 필수 #7 한국어 존댓말 baseline — 본 문서는 prose 가 적으므로 기술 용어 혼용 허용.
  </action>
  <verify>
    <automated>test -f wiki/render/adapter_contracts.md &amp;&amp; grep -c "^## " wiki/render/adapter_contracts.md &amp;&amp; grep -c "whisperx" wiki/render/adapter_contracts.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f wiki/render/adapter_contracts.md` returns 0.
    - `grep -cE "^## [A-Za-z]" wiki/render/adapter_contracts.md` returns ≥4 (Adapter Registry / Mock↔Real Deltas / Retry-Fallback / Production-Safe Defaults).
    - `grep -c "kling_i2v" wiki/render/adapter_contracts.md` returns ≥2 (Registry + Retry chain).
    - `grep -c "runway_i2v" wiki/render/adapter_contracts.md` returns ≥1.
    - `grep -c "veo_i2v" wiki/render/adapter_contracts.md` returns ≥2.
    - `grep -c "typecast" wiki/render/adapter_contracts.md` returns ≥2.
    - `grep -c "elevenlabs" wiki/render/adapter_contracts.md` returns ≥2.
    - `grep -c "shotstack" wiki/render/adapter_contracts.md` returns ≥2.
    - `grep -c "whisperx" wiki/render/adapter_contracts.md` returns ≥2 (stub + retry chain remark).
    - `grep -c "NOT YET IMPLEMENTED" wiki/render/adapter_contracts.md` returns ≥1.
    - `grep -c "allow_fault_injection" wiki/render/adapter_contracts.md` returns ≥2.
    - `grep -cE "^category:\s*render" wiki/render/adapter_contracts.md` returns 1 (frontmatter).
    - `grep -cE "^status:\s*ready" wiki/render/adapter_contracts.md` returns 1.
    - `grep -c "Phase 14" wiki/render/adapter_contracts.md` returns ≥1.
    - `grep -c "금기" wiki/render/adapter_contracts.md` returns ≥1 (CLAUDE.md 준수 명시).
  </acceptance_criteria>
  <done>
    `wiki/render/adapter_contracts.md` 가 7 adapter 전수 계약을 단일 지점에 anchoring 하고, 향후 adapter 변경 시 본 문서가 sync 를 강제하는 single-source-of-truth 역할을 한다. ADAPT-05 충족.
  </done>
</task>

<task type="auto">
  <name>Task 14-04-02: Structural validator test for adapter_contracts.md</name>
  <files>tests/adapters/test_adapter_contracts_doc.py</files>
  <read_first>
    - wiki/render/adapter_contracts.md (Task 14-04-01 산출물)
    - tests/adapters/conftest.py (repo_root fixture)
    - .planning/phases/14-api-adapter-remediation/14-RESEARCH.md (§Wave 0 Gaps — test_adapter_contracts_doc.py)
    - .planning/phases/14-api-adapter-remediation/14-VALIDATION.md (§Per-Task Verification Map row 14-04-01)
  </read_first>
  <action>
    `tests/adapters/test_adapter_contracts_doc.py` 를 다음 내용으로 신설:

    ```python
    """Phase 14 ADAPT-05 structural validator — adapter_contracts.md 문서 계약.

    검증 축:
    1. 파일 존재.
    2. frontmatter 필수 키 (category: render, status: ready, updated).
    3. Adapter Registry 섹션에 7 adapter (kling_i2v, runway_i2v, veo_i2v, typecast,
       elevenlabs, shotstack, whisperx) 전부 행 존재.
    4. 최소 4 개 섹션 헤더 (Adapter Registry, Mock↔Real Deltas, Retry/Fallback,
       Production-Safe Defaults).
    5. whisperx 행에 'NOT YET IMPLEMENTED' 명시.
    6. Contract 테스트 cross-reference 섹션 존재.
    """
    from __future__ import annotations

    import re
    from pathlib import Path

    import pytest

    pytestmark = pytest.mark.adapter_contract


    ADAPTER_NAMES = (
        "kling_i2v",
        "runway_i2v",
        "veo_i2v",
        "typecast",
        "elevenlabs",
        "shotstack",
        "whisperx",
    )


    @pytest.fixture(scope="module")
    def doc_text(repo_root: Path) -> str:
        path = repo_root / "wiki" / "render" / "adapter_contracts.md"
        assert path.exists(), f"ADAPT-05 문서 부재: {path}"
        return path.read_text(encoding="utf-8")


    def test_frontmatter_required_keys(doc_text: str):
        """frontmatter 필수 키 존재."""
        head = doc_text.split("---", 2)
        assert len(head) >= 3, "frontmatter 구분자 `---` 부재 또는 비정상."
        fm = head[1]
        assert re.search(r"^category:\s*render\b", fm, re.MULTILINE)
        assert re.search(r"^status:\s*ready\b", fm, re.MULTILINE)
        assert re.search(r"^updated:\s*\d{4}-\d{2}-\d{2}", fm, re.MULTILINE)


    def test_all_seven_adapters_listed(doc_text: str):
        """7 adapter 이름이 모두 문서에 등장."""
        for name in ADAPTER_NAMES:
            assert name in doc_text, f"adapter '{name}' 누락 — ADAPT-05 spec 위반."


    def test_min_four_section_headers(doc_text: str):
        """최소 4 개 주요 섹션 헤더 존재."""
        required_headers = (
            "Adapter Registry",
            "Mock",
            "Retry",
            "Production-Safe Defaults",
        )
        for header in required_headers:
            # `## Header` 패턴 (유연하게 일부 substring 매칭)
            assert re.search(rf"^##\s+.*{re.escape(header)}", doc_text, re.MULTILINE), (
                f"필수 섹션 '{header}' 부재."
            )


    def test_whisperx_stub_labeled(doc_text: str):
        """whisperx 행에 NOT YET IMPLEMENTED 라벨 명시."""
        assert "NOT YET IMPLEMENTED" in doc_text, (
            "whisperx stub 라벨 'NOT YET IMPLEMENTED' 부재 — Phase 15+ 계약 명확화 요구."
        )


    def test_contract_cross_reference_present(doc_text: str):
        """3 개 contract 테스트 파일 경로가 문서 내 참조됨."""
        for f in (
            "tests/adapters/test_veo_i2v_contract.py",
            "tests/adapters/test_elevenlabs_contract.py",
            "tests/adapters/test_shotstack_contract.py",
        ):
            assert f in doc_text, f"contract 테스트 교차 참조 누락: {f}"


    def test_forbid_reference_present(doc_text: str):
        """CLAUDE.md 금기사항 (#3 침묵 폴백, #4 T2V) 준수 명시."""
        # 본 문서 자체가 adapter 가 금기사항을 어떻게 지키는지 설명해야 함.
        assert "금기" in doc_text or "forbid" in doc_text.lower(), (
            "CLAUDE.md 금기사항 cross-reference 부재."
        )
    ```

    **구현 주의점**:
    - `repo_root` fixture 는 Wave 0 Plan 01 의 `tests/adapters/conftest.py` 에서 export 된 것을 재사용.
    - frontmatter parsing 은 minimal regex — 완전한 YAML parser 는 오버헤드.
    - 섹션 매칭은 substring 기반으로 유연하게 (e.g., "Mock" 이 "Mock ↔ Real Contract Deltas" 에 매칭되도록).

    **금기 #2 준수**: TODO 없음.
    **금기 #3 준수**: assert 로 명시 실패.
  </action>
  <verify>
    <automated>pytest tests/adapters/test_adapter_contracts_doc.py -v --no-cov 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/adapters/test_adapter_contracts_doc.py -v --no-cov` exits 0.
    - `grep -c "pytest.mark.adapter_contract" tests/adapters/test_adapter_contracts_doc.py` returns ≥1.
    - `grep -cE "^def test_" tests/adapters/test_adapter_contracts_doc.py` returns ≥6.
    - `grep -c "ADAPTER_NAMES" tests/adapters/test_adapter_contracts_doc.py` returns ≥1.
    - `grep -c "whisperx" tests/adapters/test_adapter_contracts_doc.py` returns ≥1.
    - `grep -c "TODO" tests/adapters/test_adapter_contracts_doc.py` returns 0.
  </acceptance_criteria>
  <done>
    adapter_contracts.md 의 구조 (frontmatter + 7 adapter 행 + 필수 섹션 + stub 라벨 + cross-reference) 가 자동 검증되어, 향후 drift 를 구조적으로 차단한다.
  </done>
</task>

<task type="auto">
  <name>Task 14-04-03: wiki/render/MOC.md — adapter_contracts.md TOC 추가</name>
  <files>wiki/render/MOC.md</files>
  <read_first>
    - wiki/render/MOC.md (frontmatter + 현재 entries)
    - .planning/phases/14-api-adapter-remediation/14-RESEARCH.md (§Bucket B B-1 — MOC.md status 결정 이력)
    - tests/phase06/test_moc_linkage.py (Plan 02 Task 14-02-04 canonical regex — `^status:\s*(scaffold|partial)\b`)
  </read_first>
  <action>
    `wiki/render/MOC.md` 를 열고 기존 구조를 보존하면서 다음을 수행:

    1. **body 에 신규 entry 추가** (기존 remotion_kling_stack.md / i2v_prompt_engineering.md 엔트리 인근):
       ```markdown
       - [x] [adapter_contracts.md](./adapter_contracts.md) — 7 adapter 입력/출력 schema + retry/fallback + fault injection 계약 (Phase 14 ADAPT-05)
       ```
       체크박스는 `[x]` — Phase 14 에서 생성 완료되었으므로 체크 상태.

    2. **frontmatter 보존**: `status` 필드는 기존 값 (`scaffold` 또는 `partial`) 을 **변경하지 않음**. Wave 1 Task 14-02-04 에서 테스트가 `scaffold|partial` 양쪽 허용으로 수정되었으므로 현재 상태 유지 OK. MOC-as-TOC 의 D-17 invariant 존중 (`ready|complete` 로 승격 금지).

       Canonical regex (Plan 02 Task 14-02-04 과 **완전 동일**): `^status:\s*(scaffold|partial)\b` (word-boundary 포함). Plan 02 와 Plan 04 의 regex 가 1:1 일치해야 drift 감사 도구 도구가 일관된 판정을 내린다.

    3. **"갱신 기록" 섹션** 이 존재하면 2026-04-21 entry 추가:
       ```markdown
       - **2026-04-21** Phase 14 Plan 04 — adapter_contracts.md 신설 + TOC entry.
       ```

    **금기 #4/#6 준수**: `shorts_naberal/` 원본 수정 없음. 본 파일은 `studios/shorts/wiki/render/MOC.md`.
    **Task 14-02-04 와의 일관성**: MOC 상태 변경은 absolute 금지 — Wave 1 에서 테스트가 양방향 허용으로 수정되었다 해도 MOC 자체는 한번 결정된 상태를 유지해야 함.
  </action>
  <verify>
    <automated>test -f wiki/render/MOC.md &amp;&amp; grep -c "adapter_contracts.md" wiki/render/MOC.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "adapter_contracts.md" wiki/render/MOC.md` returns ≥1.
    - `grep -cE "^- \[x\].*adapter_contracts\.md" wiki/render/MOC.md` returns 1 (체크 완료 상태).
    - `grep -c "Phase 14" wiki/render/MOC.md` returns ≥1.
    - Canonical regex (Plan 02 Task 14-02-04 과 1:1 일치): Python check
      `python -c "import re, sys; t = open('wiki/render/MOC.md', encoding='utf-8').read(); sys.exit(0 if re.search(r'^status:\s*(scaffold|partial)\b', t, re.MULTILINE) else 1)"` exits 0 (기존 상태 보존, word-boundary `\b` 포함하여 `scaffoldish|partially` 같은 prefix drift 도 차단).
    - Python check `python -c "import re, sys; t = open('wiki/render/MOC.md', encoding='utf-8').read(); sys.exit(1 if re.search(r'^status:\s*(ready|complete)\b', t, re.MULTILINE) else 0)"` exits 0 (D-17 invariant — ready/complete drift 차단).
    - Wave 1 에서 수정된 `tests/phase06/test_moc_linkage.py` 가 여전히 green — `pytest tests/phase06/test_moc_linkage.py -x --no-cov` exits 0.
  </acceptance_criteria>
  <done>
    MOC 가 adapter_contracts.md 를 TOC 에 포함하면서 D-17 invariant (scaffold|partial 유지) 보존. Plan 02 Task 14-02-04 의 canonical regex `^status:\s*(scaffold|partial)\b` 와 1:1 일치 — word-boundary `\b` 포함. Phase 6 MOC linkage 테스트 여전히 green.
  </done>
</task>

<task type="auto">
  <name>Task 14-04-04: pre_tool_use.py warn-only Hook 확장 + .gitignore wiring (ADAPT-06c Option B)</name>
  <files>
    .claude/hooks/pre_tool_use.py
    .gitignore
  </files>
  <read_first>
    - .claude/hooks/pre_tool_use.py (전체 — 기존 8 regex 차단 구조)
    - .gitignore (현재 내용 확인)
    - .planning/phases/14-api-adapter-remediation/14-RESEARCH.md (§ADAPT-06 Option B lines 286-292, §Claude's Discretion Hook extension)
    - CLAUDE.md (필수사항 #1 Hook 3종 활성, 금기사항 #1 skip_gates 금지 — Hook 자체는 차단형이면 안 됨, warn-only 이어야 함)
  </read_first>
  <action>
    **Step 1 — Hook 확장**:

    `.claude/hooks/pre_tool_use.py` 의 기존 차단 로직을 **보존** 한 채로, 다음 **warn-only** 체크를 추가:

    **규칙**:
    - Tool 호출이 `Edit`/`Write`/`MultiEdit` 이고 `file_path` 가 `scripts/orchestrator/api/*.py` 패턴에 매칭되면,
    - 동일 tool 호출 session 내에서 `tests/adapters/test_*_contract.py` 파일이 touch (Edit/Write) 된 흔적이 없으면,
    - **차단하지 않고 stderr 에 경고만 출력** (`print(..., file=sys.stderr)`).

    Python 구현 (기존 pre_tool_use.py body 에 추가):

    ```python
    # ============================================================
    # Phase 14 ADAPT-06c — warn-only adapter contract drift check
    # ============================================================
    # 규칙: scripts/orchestrator/api/*.py 파일을 Edit/Write/MultiEdit 로 수정하는
    # 경우 대응 tests/adapters/test_*_contract.py 가 동반 편집되었는지 검사.
    # 동반 편집이 없어도 차단하지 않고 stderr 에 경고만 출력 (Option B, RESEARCH
    # §ADAPT-06 recommendation).
    #
    # 이 Hook 은 Phase 14 Plan 04 Task 14-04-04 에서 도입. 필수사항 #1 (Hook 3종
    # 활성) 을 훼손하지 않으면서 adapter_contract drift 재발 방지.

    import os
    import re as _re
    import sys as _sys
    import json as _json
    from pathlib import Path as _Path

    _ADAPTER_PATTERN = _re.compile(
        r"scripts[/\\]orchestrator[/\\]api[/\\][A-Za-z_0-9]+\.py$"
    )
    _CONTRACT_DIR = _re.compile(r"tests[/\\]adapters[/\\]test_[A-Za-z_0-9]+_contract\.py$")
    _TRACK_FILE = _Path(".claude/hooks/_adapter_contract_touch.json")


    def _record_contract_touch(file_path: str) -> None:
        """세션 scope 에서 contract 파일 touch 를 기록."""
        _TRACK_FILE.parent.mkdir(parents=True, exist_ok=True)
        try:
            data = _json.loads(_TRACK_FILE.read_text(encoding="utf-8")) if _TRACK_FILE.exists() else {}
        except _json.JSONDecodeError:
            data = {}
        pid = str(os.getppid())
        data.setdefault(pid, [])
        if file_path not in data[pid]:
            data[pid].append(file_path)
        _TRACK_FILE.write_text(_json.dumps(data), encoding="utf-8")


    def _contract_touched_this_session() -> bool:
        if not _TRACK_FILE.exists():
            return False
        try:
            data = _json.loads(_TRACK_FILE.read_text(encoding="utf-8"))
        except _json.JSONDecodeError:
            return False
        pid = str(os.getppid())
        return bool(data.get(pid))


    def check_adapter_contract_drift(tool_name: str, tool_input: dict) -> None:
        """Warn-only — adapter 파일 수정 시 contract 테스트 동반 편집 여부 경고.

        차단하지 않음 (CLAUDE.md 필수사항 #1 Hook 3종 활성 준수 + 작업 흐름 보존).
        """
        if tool_name not in ("Edit", "Write", "MultiEdit"):
            return
        file_path = tool_input.get("file_path") or ""
        norm = file_path.replace("\\", "/")

        if _CONTRACT_DIR.search(norm):
            _record_contract_touch(norm)
            return

        if _ADAPTER_PATTERN.search(norm):
            if not _contract_touched_this_session():
                print(
                    f"[Phase 14 ADAPT-06c warn] adapter 파일 수정 감지: {file_path}\n"
                    f"  → 대응 tests/adapters/test_*_contract.py 를 함께 수정하세요.\n"
                    f"  (경고만 — 작업은 차단되지 않습니다.)",
                    file=_sys.stderr,
                )
    ```

    그리고 pre_tool_use.py 의 **기존 메인 호출부** (Edit/Write/MultiEdit 이 처리되는 지점) 에서 기존 차단 로직이 끝난 뒤 `check_adapter_contract_drift(tool_name, tool_input)` 를 호출하도록 endpoint 한 줄 추가.

    **Step 2 — `.gitignore` wiring** (Warning 1 resolution):

    `.gitignore` 를 repo root 에서 열고, `.claude/hooks/_adapter_contract_touch.json` 행이 이미 존재하는지 먼저 확인:
    ```
    grep -c '_adapter_contract_touch' .gitignore
    ```

    부재 시 (count=0), 파일 끝에 다음 블록 append:
    ```
    # Phase 14 ADAPT-06c — warn-only Hook session-scope tracking file
    .claude/hooks/_adapter_contract_touch.json
    ```

    Verify with: `grep -c '_adapter_contract_touch' .gitignore` returns ≥1.

    **중요 구현 주의점**:
    - **기존 pre_tool_use.py 의 8 regex 차단 로직은 절대 변경하지 않음** — CLAUDE.md 필수사항 #1 + F-CTX-01 재발 방지.
    - warn-only Hook 이 raise/exit 하면 안 됨 — `print(..., file=sys.stderr)` 만.
    - 기존 Hook 의 import 구문을 건드리지 않기 위해 신규 import 는 `_` prefix 로 namespace 충돌 회피.
    - `.gitignore` entry 는 session-scope ephemeral tracking file 이 절대 git 에 커밋되지 않도록 보장.

    **금기 #1 준수**: skip_gates 회피 없음, 본 Hook 은 차단형이 아니라 경고형.
    **금기 #3 준수**: 모든 실패 (JSONDecodeError 등) 는 명시적으로 처리 후 fall-through — 단 이 특정 Hook 은 warn-only 이므로 실패를 silently 무시해도 작업을 막지 않는 설계.
  </action>
  <verify>
    <automated>grep -c "ADAPT-06c" .claude/hooks/pre_tool_use.py &amp;&amp; grep -c "check_adapter_contract_drift" .claude/hooks/pre_tool_use.py &amp;&amp; grep -c "_adapter_contract_touch" .gitignore &amp;&amp; py -3.11 -c "import importlib.util, sys; spec = importlib.util.spec_from_file_location('h', '.claude/hooks/pre_tool_use.py'); m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m); print('hook loaded OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ADAPT-06c" .claude/hooks/pre_tool_use.py` returns >=1.
    - `grep -c "check_adapter_contract_drift" .claude/hooks/pre_tool_use.py` returns >=1.
    - `grep -c "warn-only" .claude/hooks/pre_tool_use.py` returns >=1 (경고형 라벨 명시).
    - Hook module import smoke test: `py -3.11 -c "import importlib.util; spec = importlib.util.spec_from_file_location('h', '.claude/hooks/pre_tool_use.py'); m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m)"` exits 0 (import 무결성).
    - 기존 8 regex 차단 로직의 regex 문자열은 모두 보존 (Hook 의 diff 에서 기존 regex 블록 줄 변경이 0).
    - **`.gitignore` wiring (Warning 1)**: `grep -c '_adapter_contract_touch' .gitignore` returns ≥1 — session-scope ephemeral tracking file 이 git 커밋에서 제외됨.
    - Dry-run Hook 호출: `python .claude/hooks/pre_tool_use.py --dry-run` exits 0 (Hook 의 dry-run 플래그가 지원되면 — 미지원 시 import smoke test 로 대체).
  </acceptance_criteria>
  <done>
    Adapter 파일을 수정하면서 contract 테스트를 touch 하지 않는 경우 stderr 경고가 출력되어 drift 재발 가능성을 낮춘다. 작업은 차단되지 않으며 기존 8 regex 차단 로직은 손상 없이 보존. `.gitignore` 에 `.claude/hooks/_adapter_contract_touch.json` 이 명시되어 session-scope tracking file 이 git 에서 제외됨.
  </done>
</task>

</tasks>

<verification>
Wave 3 완료 조건 (Plan 04 전체):
1. `wiki/render/adapter_contracts.md` 존재 + structural validator 테스트 green.
2. `wiki/render/MOC.md` 가 adapter_contracts.md 를 포함 + D-17 invariant (scaffold|partial) 보존 + Plan 02 canonical regex 와 1:1 일치 + Phase 6 MOC linkage 테스트 여전히 green.
3. `pre_tool_use.py` 에 warn-only ADAPT-06c Hook 통합 + 기존 차단 로직 손상 없음 + import smoke test green.
4. `.gitignore` 에 `_adapter_contract_touch` 행 존재 (Warning 1 closure).
5. `pytest tests/adapters/ -v --no-cov` 가 Wave 2 tests + doc validator 모두 green (≥ 20 tests).
6. `pytest tests/phase05 tests/phase06 tests/phase07 --tb=no -q` 여전히 0 failures.
</verification>

<success_criteria>
- ADAPT-05 Success Criteria 충족: `wiki/render/adapter_contracts.md` 가 7 adapter × 5 column + mock↔real delta + retry/fallback + fault injection 전수 문서화.
- ADAPT-06c Option B 채택: warn-only Hook 통합 + .gitignore wiring.
- 문서 structural validator 가 drift 재발을 자동 감지.
- MOC D-17 invariant 보존 (scaffold|partial) + Plan 02 canonical regex `^status:\s*(scaffold|partial)\b` 와 1:1 일치.
- CLAUDE.md 금기 #1 (skip_gates 회피 없음), #4 (T2V 없음), #6 (shorts_naberal 원본 보존), 필수 #1 (Hook 3종 활성) 전수 준수.
</success_criteria>

<output>
완료 후 `.planning/phases/14-api-adapter-remediation/14-04-SUMMARY.md` 를 생성해 다음을 기록:
- `wiki/render/adapter_contracts.md` 최종 섹션 헤더 목록.
- structural validator 테스트 개수 및 pass 상태.
- MOC.md 내 adapter_contracts.md entry 위치 (line number).
- pre_tool_use.py 에 추가된 라인 수 및 기존 8 regex 블록 diff 검증 (touched=0).
- `.gitignore` 에 추가된 엔트리 (1 라인 + 주석 1 라인 = total 2 라인 추가).
</output>
</output>
