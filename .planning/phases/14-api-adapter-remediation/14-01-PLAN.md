---
phase: 14-api-adapter-remediation
plan: 01
plan_id: 14-01
type: execute
wave: 0
depends_on: []
files_modified:
  - pytest.ini
  - tests/adapters/__init__.py
  - tests/adapters/conftest.py
autonomous: true
requirements:
  - ADAPT-01
  - ADAPT-02
  - ADAPT-03
  - ADAPT-06
commit_discipline: atomic
must_haves:
  truths:
    - "`pytest.ini` 파일이 repo root 에 존재하고 `adapter_contract` marker 가 등록되어 있다"
    - "기존 informal marker (asyncio 등) 가 모두 pytest.ini 에 등록되어 `--strict-markers` 가 기존 테스트를 깨뜨리지 않는다"
    - "`tests/adapters/` 가 Python 패키지로 존재한다 (`__init__.py` 생성 완료)"
    - "`tests/adapters/conftest.py` 가 Phase 5/7 fixture (`repo_root`, `_fake_env`, Phase 7 Mock classes) 를 재사용할 수 있다"
    - "`pytest --collect-only -m adapter_contract` 가 에러 없이 실행된다 (현재 0건이어도 OK)"
  artifacts:
    - path: "pytest.ini"
      provides: "adapter_contract marker registration + --strict-markers + testpaths"
      contains: "adapter_contract"
    - path: "tests/adapters/__init__.py"
      provides: "Python package marker"
    - path: "tests/adapters/conftest.py"
      provides: "Shared fixtures for contract tests (repo_root, _fake_env, mock factories)"
  key_links:
    - from: "pytest.ini"
      to: "tests/adapters/test_*_contract.py"
      via: "adapter_contract marker"
      pattern: "pytest -m adapter_contract"
    - from: "tests/adapters/conftest.py"
      to: "tests/phase07/mocks/*.py"
      via: "import 경로"
      pattern: "from tests.phase07.mocks.* import"
---

<objective>
Wave 0 Foundation. pytest.ini 를 repo root 에 신설하여 `adapter_contract` marker 를 등록하고, `tests/adapters/` 패키지를 scaffold 한다. 동시에 **기존 tests/ 내 informal marker 전수 사전 감사** (RESEARCH R1 mitigation) 를 수행해 `--strict-markers` 활성화가 기존 green suite 를 회귀시키지 않도록 보장한다.

Purpose: Wave 1~4 모든 후속 작업의 전제. pytest.ini 없이는 `@pytest.mark.adapter_contract` 가 미등록 warning 또는 --strict-markers 하에 error 로 수집되지 않고, tests/adapters/ 패키지 없이는 contract 테스트를 import 할 수 없다.
Output: pytest.ini (10-15줄) + tests/adapters/__init__.py (빈 파일) + tests/adapters/conftest.py (Phase 5/7 fixture re-export).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-api-adapter-remediation/14-RESEARCH.md
@.planning/phases/14-api-adapter-remediation/14-VALIDATION.md
@CLAUDE.md

# Reference fixtures (read-only study)
@tests/phase05/conftest.py
@tests/phase07/conftest.py

<interfaces>
Phase 7 Mocks (re-exportable from tests/adapters/conftest.py):

From tests/phase07/mocks/kling_mock.py:
```python
class KlingMock:
    def __init__(self, allow_fault_injection: bool = False, fault_mode: str | None = None) -> None
    def image_to_video(self, *args, **kwargs) -> Path
```
From tests/phase07/mocks/runway_mock.py, typecast_mock.py, elevenlabs_mock.py, shotstack_mock.py: 동일 패턴 (D-3 Phase 7 invariant — `allow_fault_injection=False` 기본).

Existing phase05 fixtures (tests/phase05/conftest.py):
- `repo_root` — pathlib.Path 루트
- `_fake_env` — monkeypatch 기반 env wipe fixture
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 14-01-01: Pre-grep informal marker audit (R1 mitigation)</name>
  <files>.planning/phases/14-api-adapter-remediation/14-01-marker-audit.log</files>
  <read_first>
    - .planning/phases/14-api-adapter-remediation/14-RESEARCH.md (§Risks R1 pytest.ini introduction side effects)
    - .planning/phases/14-api-adapter-remediation/14-VALIDATION.md (§Wave 0 Requirements — pre-grep line)
  </read_first>
  <action>
    1. 다음 명령을 repo root 에서 실행하고 stdout/stderr 를 `.planning/phases/14-api-adapter-remediation/14-01-marker-audit.log` 로 리다이렉트:
       ```
       grep -rE "@pytest\.mark\.([A-Za-z_][A-Za-z0-9_]*)" tests/ scripts/ 2>&1 | sort -u
       ```
    2. 로그에서 발견된 marker 중 built-in (skip, skipif, xfail, parametrize, usefixtures, filterwarnings) 과 추후 등록할 `adapter_contract` 를 제외한 나머지를 수집.
    3. 수집된 marker 목록을 로그 파일 끝에 `### MARKERS_TO_REGISTER` 섹션으로 append. 예: `asyncio`, `slow`, `integration` 등이 있다면 Task 14-01-02 의 pytest.ini `markers =` 블록에 반드시 포함해야 함.
    4. 수집 결과가 0건인 경우에도 로그 파일은 생성하되 `### MARKERS_TO_REGISTER\n(none — only built-in markers found)` 로 명시.
    CLAUDE.md 금기사항 #3 (try-except 침묵 폴백 금지) — grep 실패 시 예외를 삼키지 말고 `raise` 혹은 명시적 로깅.
  </action>
  <verify>
    <automated>test -f .planning/phases/14-api-adapter-remediation/14-01-marker-audit.log &amp;&amp; grep -c "MARKERS_TO_REGISTER" .planning/phases/14-api-adapter-remediation/14-01-marker-audit.log</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/14-api-adapter-remediation/14-01-marker-audit.log` returns 0 (파일 존재).
    - `grep -c "MARKERS_TO_REGISTER" <로그>` returns 1 (정확히 1회 — 섹션 헤더 있음).
    - 로그 내 built-in marker 외 발견된 모든 marker 가 섹션에 목록화되어 있다.
  </acceptance_criteria>
  <done>
    Informal marker 전수 목록이 log 파일로 물리 anchoring 되어 Task 14-01-02 가 `markers =` 블록을 정확히 작성할 수 있다.
  </done>
</task>

<task type="auto">
  <name>Task 14-01-02: Create pytest.ini at repo root with adapter_contract marker</name>
  <files>pytest.ini</files>
  <read_first>
    - .planning/phases/14-api-adapter-remediation/14-01-marker-audit.log (Task 14-01-01 산출물 — MARKERS_TO_REGISTER 섹션)
    - .planning/phases/14-api-adapter-remediation/14-RESEARCH.md (§ADAPT-06 + §pytest.ini template lines 537-544)
    - CLAUDE.md (필수사항 #5 — STRUCTURE.md Whitelist 에서 repo root 설정 파일 허용 여부 확인)
  </read_first>
  <action>
    Repo root (`c:/Users/PC/Desktop/naberal_group/studios/shorts/pytest.ini`) 에 다음 내용의 파일 신설 (Task 14-01-01 로그의 MARKERS_TO_REGISTER 섹션 내용을 `markers =` 블록에 **반드시** 추가):

    ```ini
    [pytest]
    markers =
        adapter_contract: Phase 14 adapter contract tests (tests/adapters/) — run with -m adapter_contract
        # [Task 14-01-01 로그에서 발견된 각 informal marker 를 여기 한 줄씩 추가]
        # 예시: asyncio: async test marker (pytest-asyncio)
        # 예시: slow: slow-running test (skip with -m "not slow")

    testpaths = tests

    addopts = --strict-markers
    ```

    구체 규칙:
    - 헤더 `[pytest]` 그대로.
    - `adapter_contract` 는 **항상 첫 번째** marker 로 배치.
    - Task 14-01-01 로그의 MARKERS_TO_REGISTER 섹션 내용이 `(none — only built-in markers found)` 면 adapter_contract 한 줄만 등록.
    - `testpaths = tests` 로 지정하여 conftest 탐색 범위 명시.
    - `addopts = --strict-markers` 활성 (미등록 marker 는 collection error 발생하므로 drift 방지).

    **금기 #6 방어**: `shorts_naberal/` 디렉토리 수정 금지 — pytest.ini 는 `studios/shorts/` 프로젝트 루트에만 배치.
  </action>
  <verify>
    <automated>test -f pytest.ini &amp;&amp; grep -c "adapter_contract" pytest.ini &amp;&amp; grep -c "strict-markers" pytest.ini</automated>
  </verify>
  <acceptance_criteria>
    - `test -f pytest.ini` returns 0 (repo root 존재).
    - `grep -c "adapter_contract" pytest.ini` returns ≥1.
    - `grep -c "strict-markers" pytest.ini` returns ≥1.
    - `grep -c "testpaths = tests" pytest.ini` returns 1.
    - `pytest --markers 2>&amp;1 | grep -c "@pytest.mark.adapter_contract"` returns 1 (marker 등록 확인).
    - `pytest --collect-only tests/phase05 tests/phase06 tests/phase07 2>&amp;1 | grep -ci "unknown mark\|PytestUnknownMarkWarning" ` returns 0 (--strict-markers 하에 기존 테스트가 미등록 marker 로 깨지지 않음; R1 mitigation).
  </acceptance_criteria>
  <done>
    pytest.ini 가 존재하고 `pytest --markers` 출력에 adapter_contract 가 표시되며, 기존 phase05/06/07 test collection 이 --strict-markers 하에 에러 없이 완료된다.
  </done>
</task>

<task type="auto">
  <name>Task 14-01-03: Scaffold tests/adapters/ package (__init__.py + conftest.py)</name>
  <files>
    tests/adapters/__init__.py
    tests/adapters/conftest.py
  </files>
  <read_first>
    - tests/phase05/conftest.py (repo_root fixture pattern)
    - tests/phase07/conftest.py (Mock import pattern + _fake_env fixture)
    - .planning/phases/14-api-adapter-remediation/14-RESEARCH.md (§Wave 0 Gaps, §Environment Availability)
  </read_first>
  <action>
    **파일 1**: `tests/adapters/__init__.py` — 완전히 빈 파일 (Python 패키지 marker).

    **파일 2**: `tests/adapters/conftest.py` — 다음 내용 작성 (Phase 5/7 fixture 재사용, 신규 정의 최소화):

    ```python
    """Phase 14 — tests/adapters/ 공용 fixture.

    Contract 테스트 3종 (veo_i2v / elevenlabs / shotstack) 이 Phase 5 conftest
    의 `repo_root` + Phase 7 mocks 를 공통적으로 의존하므로 본 conftest 에서
    re-export 한다. Nothing here touches production code — monkeypatch only.
    """
    from __future__ import annotations

    from pathlib import Path
    import sys

    import pytest

    # Phase 5/7 fixture 재사용을 위한 PYTHONPATH 보강.
    REPO_ROOT = Path(__file__).resolve().parents[2]
    if str(REPO_ROOT) not in sys.path:
        sys.path.insert(0, str(REPO_ROOT))


    @pytest.fixture(scope="session")
    def repo_root() -> Path:
        """Phase 14 root — mirrors tests/phase05/conftest.py."""
        return REPO_ROOT


    @pytest.fixture
    def _fake_env(monkeypatch):
        """Phase 5 precedent — wipe every known adapter env key, populate fakes.

        Contract 테스트는 모두 monkeypatch 경유로 env 를 설정하므로 이 fixture
        는 baseline wipe + 공통 fake 값을 주입한다.
        """
        for key in (
            "KLING_API_KEY",
            "RUNWAY_API_KEY",
            "VEO_API_KEY",
            "FAL_KEY",
            "TYPECAST_API_KEY",
            "ELEVENLABS_API_KEY",
            "ELEVEN_API_KEY",
            "SHOTSTACK_API_KEY",
        ):
            monkeypatch.delenv(key, raising=False)
        monkeypatch.setenv("KLING_API_KEY", "fake-kling")
        monkeypatch.setenv("VEO_API_KEY", "fake-veo")
        monkeypatch.setenv("TYPECAST_API_KEY", "fake-tc")
        monkeypatch.setenv("ELEVENLABS_API_KEY", "fake-el")
        monkeypatch.setenv("SHOTSTACK_API_KEY", "fake-ss")
    ```

    **금기 #2 방어**: `TODO(next-session)` 주석 금지. 미완 로직은 NotImplementedError 로 명시.
    **금기 #3 방어**: try-except 침묵 폴백 금지. PYTHONPATH 조작 실패 시 즉시 raise.
    **필수 #7 방어**: 한국어 docstring 존댓말 baseline 유지.
  </action>
  <verify>
    <automated>test -f tests/adapters/__init__.py &amp;&amp; test -f tests/adapters/conftest.py &amp;&amp; py -3.11 -c "import tests.adapters" &amp;&amp; pytest --collect-only -m adapter_contract -q 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/adapters/__init__.py` returns 0 (파일 존재, 빈 파일 OK).
    - `test -f tests/adapters/conftest.py` returns 0.
    - `py -3.11 -c "import tests.adapters"` exits 0 (package import 가능).
    - `grep -c "def repo_root" tests/adapters/conftest.py` returns ≥1.
    - `grep -c "def _fake_env" tests/adapters/conftest.py` returns ≥1.
    - `pytest --collect-only -m adapter_contract -q` exits 0 (collection 에러 0건 — 현재 0 tests 수집되어도 OK, pytest.ini marker 등록 검증).
    - `grep -c "TODO" tests/adapters/conftest.py` returns 0 (금기 #2 준수).
  </acceptance_criteria>
  <done>
    tests/adapters/ 가 Python 패키지로 존재하고 conftest.py 가 repo_root + _fake_env fixture 를 export 하며, `pytest -m adapter_contract --collect-only` 가 clean exit 0 으로 완료된다. Wave 2 contract 테스트 3종이 이 scaffold 에 바로 안착 가능한 상태.
  </done>
</task>

</tasks>

<verification>
Wave 0 완료 조건 (Plan 01 전체):
1. `test -f pytest.ini` 성공 + `pytest --markers | grep adapter_contract` 1회 이상 매칭.
2. `test -d tests/adapters` 성공 + `__init__.py` + `conftest.py` 존재.
3. `pytest --collect-only tests/phase05 tests/phase06 tests/phase07 -q` 에서 --strict-markers 로 인한 collection error 0 (R1 mitigation).
4. `pytest --collect-only -m adapter_contract` 가 exit 0 (marker 등록 검증).
5. `.planning/phases/14-api-adapter-remediation/14-01-marker-audit.log` 존재 (사전 감사 증거).
</verification>

<success_criteria>
- pytest.ini 가 repo root 에 배치되어 `adapter_contract` + 기존 informal marker (발견 시) 전수 등록.
- tests/adapters/ 가 importable Python 패키지.
- conftest.py 가 Phase 5/7 fixture 를 충실히 재사용 (신규 API 최소화).
- Wave 1~4 후속 plan 이 `pytest -m adapter_contract` 를 조건 없이 호출 가능.
- CLAUDE.md 금기사항 #2, #3, #6 + 필수사항 #5 준수.
</success_criteria>

<output>
완료 후 `.planning/phases/14-api-adapter-remediation/14-01-SUMMARY.md` 를 생성해 다음을 기록:
- pytest.ini 최종 내용 (markers 목록 전수).
- Task 14-01-01 marker audit 결과 요약.
- `pytest --markers` 출력에서 adapter_contract 확인 증거.
- `pytest --collect-only` --strict-markers 하에 0 error 증거.
</output>
