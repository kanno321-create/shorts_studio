---
phase: 02-domain-definition
plan: 04
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - C:/Users/PC/Desktop/naberal_group/studios/shorts/CLAUDE.md
autonomous: true
requirements: [INFRA-02]
requirements_addressed: [INFRA-02]
gap_closure: false

must_haves:
  truths:
    - "studios/shorts/CLAUDE.md contains zero `TODO:` placeholder text"
    - "Line 7 typo `vv1.0` corrected to `v1.0.1`"
    - "Line 3 DOMAIN_GOAL replaced with outcome-shaped 1-sentence goal"
    - "Line 38-42 PIPELINE_FLOW replaced with 12-GATE state machine diagram + Phase 5 TBD note"
    - "Line 44-45 DOMAIN_ABSOLUTE_RULES replaced with 8 Hard Constraint bullets"
    - "Line 64-68 replaced with `주 3~4편 + YPP 궤도` + TBD note + 5 Korean triggers (single merged Edit)"
    - "GSD auto-managed blocks (line 109+) remain UNCHANGED"
  artifacts:
    - path: "C:/Users/PC/Desktop/naberal_group/studios/shorts/CLAUDE.md"
      provides: "Domain-scope declaration — shorts 니치, 주 3~4편 페이스, 절대 규칙 8개, trigger phrases"
      contains: "shorts_naberal 원본 수정 금지"
  key_links:
    - from: "CLAUDE.md DOMAIN_ABSOLUTE_RULES"
      to: "pre_tool_use.py regex 차단 rules"
      via: "skip_gates=True 금지 / TODO(next-session) 금지 선언"
      pattern: "skip_gates=True 금지.*TODO\\(next-session\\) 금지"
    - from: "CLAUDE.md PIPELINE_FLOW"
      to: "Phase 5 scripts/orchestrator/shorts_pipeline.py"
      via: "12 GATE 상태머신 명칭 선언 + Phase 5 확정 노트"
      pattern: "IDLE.*TREND.*COMPLETE"
---

<objective>
studios/shorts/CLAUDE.md의 5개 TODO placeholder + line 7 오타 (`vv1.0` → `v1.0.1`)를 **line-exact 수술**로 치환한다.
D2-D 결정: D-1~D-10 확정 사항만 반영. Phase 4~5 미결정 수치는 `TBD (Phase X)` 주석 유지.

**Purpose:** Phase 2 INFRA-02 SC#3 "CLAUDE.md의 {{TODO}} 5종이 모두 치환되어 'shorts_naberal 니치 승계 + 주 3~4편 + YPP 진입'이 문서로 선언되어 있다" 달성.
**Output:** studios/shorts/CLAUDE.md. **5 Edit operations covering 6 edit sites** (the last Edit merges Line 64-66 and Line 68 into one semantic block). GSD 자동 블록(line 109+)은 절대 건드리지 않는다 (OQ-3 resolved).
**File not yet committed** — consolidated commit in Plan 06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/PC/Desktop/naberal_group/studios/shorts/.planning/phases/02-domain-definition/02-CONTEXT.md
@C:/Users/PC/Desktop/naberal_group/studios/shorts/.planning/phases/02-domain-definition/02-RESEARCH.md
@C:/Users/PC/Desktop/naberal_group/studios/shorts/.planning/phases/02-domain-definition/02-VALIDATION.md
@C:/Users/PC/Desktop/naberal_group/studios/shorts/CLAUDE.md

<interfaces>
<!-- All 6 Before/After pairs are in RESEARCH.md § CLAUDE.md TODO 5 Replacement (line 464-625) verbatim. -->
<!-- RESEARCH confirmed CLAUDE.md is 409 lines total. Lines 3, 7, 38-42, 44-45, 64-66, 68 are the ONLY edit sites. -->
<!-- GSD auto-managed blocks line 109-409 MUST remain untouched. -->
<!-- Expected outcome: `grep -E "TODO:|TODO\(next-session\)" CLAUDE.md` returns 0 matches (VALIDATION.md row 2-W3-01). -->

Current state of edit targets (confirmed via Read):
- Line 3: `TODO: 도메인 목표 1문장 작성`
- Line 7: ``**Layer 1**: `naberal_harness` vv1.0``  (note: "vv1.0" double-v typo)
- Line 38-42: Pipeline section with ```TODO: 파이프라인 다이어그램 작성```
- Line 44-45: `### 도메인 절대 규칙` + `- TODO: 도메인 절대 규칙 작성`
- Line 64: `## 하네스: shorts-hive (e.g., shorts-hive)`
- Line 66: `**목표**: TODO: 하네스 목표 작성`
- Line 68: `**트리거**: "shorts 돌려", "shorts 시작"` (placeholder level)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply 5 Edit operations covering 6 edit sites on CLAUDE.md (4 TODO replacements + 1 typo fix + 1 merged block for Line 64-68)</name>
  <read_first>
    - C:/Users/PC/Desktop/naberal_group/studios/shorts/CLAUDE.md (reconfirm line numbers — file is 409 lines, edit sites are all in lines 3-68, GSD blocks start line 109)
    - C:/Users/PC/Desktop/naberal_group/studios/shorts/.planning/phases/02-domain-definition/02-RESEARCH.md § CLAUDE.md TODO 5 Replacement (line 464-625 verbatim Before/After)
    - C:/Users/PC/Desktop/naberal_group/studios/shorts/.planning/phases/02-domain-definition/02-CONTEXT.md § D2-D (치환 매핑 5개, Phase 4~5 TBD 명시)
  </read_first>
  <files>C:/Users/PC/Desktop/naberal_group/studios/shorts/CLAUDE.md</files>
  <action>
    Use the Edit tool **exactly 5 times**. Each edit uses exact old_string/new_string from RESEARCH.md § CLAUDE.md TODO 5 Replacement. **Do NOT use a single MultiEdit** — edits intersect in line ranges (e.g., Line 64-66 and Line 68 are adjacent), so sequential Edit is safer for string uniqueness.

    **Edit count summary:** 5 Edit calls → 6 semantic edit sites (Line 3, Line 7, Line 38-42, Line 44-45, Line 64-66 + Line 68 merged). The 5th Edit covers both the hive-goal block and the trigger line in one old_string/new_string pair.

    **EDIT 1 — Line 3 DOMAIN_GOAL:**
    - old_string:
      ```
      # shorts — AI 영상 제작

      TODO: 도메인 목표 1문장 작성
      ```
    - new_string:
      ```
      # shorts — AI 영상 제작

      AI 에이전트 팀이 자율 제작하는 주 3~4편 YouTube Shorts로 대표님의 기존 채널을 YPP 궤도에 올리는 스튜디오. **Core Value = 외부 수익 발생** (기술 성공 ≠ 비즈니스 성공).
      ```

    **EDIT 2 — Line 7 typo fix (`vv1.0` → `v1.0.1`):**
    - old_string:
      ```
      **Layer 1**: `naberal_harness` vv1.0
      ```
    - new_string:
      ```
      **Layer 1**: `naberal_harness` v1.0.1
      ```

    **Note for executor (EDIT 3):** EDIT 3 replaces a fenced markdown code block. The old_string STARTS at the opening ``` of the existing code fence on Line 38 and ENDS at the closing ``` on Line 42. The new_string preserves the same fence structure (opening ``` → diagram content → closing ```) then adds two blockquote lines (> ...) outside the fence. Do not introduce additional indentation or trailing blank lines beyond what is shown.

    **EDIT 3 — Line 38-42 PIPELINE_FLOW (inside fenced code block):**
    - old_string:
      ```
      ## Pipeline → 도메인 오케스트레이터 참조

      ```
      TODO: 파이프라인 다이어그램 작성
      ```
      ```
    - new_string:
      ```
      ## Pipeline → 도메인 오케스트레이터 참조

      ```
      IDLE
        → TREND → NICHE → RESEARCH_NLM → BLUEPRINT
        → SCRIPT → POLISH → VOICE → ASSETS
        → ASSEMBLY → THUMBNAIL → METADATA
        → UPLOAD → MONITOR → COMPLETE
      ```

      > 위 GATE 이름·개수는 **대표 후보, Phase 5 Orchestrator v2 작성 시 최종 확정** (D-7 state machine, 500~800줄 구현).
      > 실 오케스트레이터 구현: `scripts/orchestrator/shorts_pipeline.py` (Phase 5)
      ```

    **EDIT 4 — Line 44-45 DOMAIN_ABSOLUTE_RULES:**
    - old_string:
      ```
      ### 도메인 절대 규칙
      - TODO: 도메인 절대 규칙 작성
      ```
    - new_string:
      ```
      ### 도메인 절대 규칙

      1. **`skip_gates=True` 금지** — `pre_tool_use.py` regex 차단 (CONFLICT_MAP A-6 재발 방지)
      2. **`TODO(next-session)` 금지** — `pre_tool_use.py` regex 차단 (A-5 재발 방지)
      3. **try-except 침묵 폴백 금지** — 명시적 `raise` + GATE 기록 필수
      4. **T2V 금지 — I2V only** — Anchor Frame 강제 (NotebookLM T1)
      5. **Selenium 업로드 영구 금지** — YouTube Data API v3 공식만 (AF-8)
      6. **`shorts_naberal` 원본 수정 금지** — Harvest는 `.preserved/harvested/`에 읽기 전용 복사만 (Phase 3, chmod -w)
      7. **K-pop 트렌드 음원 직접 사용 금지** — KOMCA + Content ID strike 위험 (AF-13). 하이브리드 오디오: 트렌딩 3~5초 → royalty-free crossfade (T11)
      8. **주 3~4편 페이스 준수** — 일일 업로드 = 봇 패턴 + Inauthentic Content 직격 (AF-1, AF-11). 48시간+ 랜덤 간격 + 한국 피크 시간 (평일 20-23 / 주말 12-15 KST)
      ```

    **EDIT 5 — Line 64-68 merged block (hive header + hive goal + TBD note + triggers):**
    This single Edit replaces TWO adjacent semantic sites (Line 64-66 and Line 68) with one combined new_string. The `**트리거**:` line is included within this Edit's new_string. Do NOT issue a separate 6th Edit for line 68.
    - old_string:
      ```
      ## 하네스: shorts-hive (e.g., shorts-hive)

      **목표**: TODO: 하네스 목표 작성

      **트리거**: "shorts 돌려", "shorts 시작"
      ```
    - new_string:
      ```
      ## 하네스: shorts-hive

      **목표**: 주 3~4편 자동 영상 제작 + YPP 진입 궤도(1000구독 + 10M views/년) 확보. Core Value = 외부 YouTube 광고 수익 발생.

      > **TBD (Phase 4 Agent Team Design)**: 에이전트 개수·이름 최종 확정 (현재 추정: Producer 11명 + Inspector 17명 + Supervisor 1명 = 29명, 범위 12~20 재조정 예정)

      **트리거**: "쇼츠 돌려" / "영상 뽑아" / "shorts 파이프라인" / "YouTube 업로드" / "쇼츠 시작"
      ```

    **DO NOT modify any other lines.** In particular:
    - Identity section (line 16-27) — unchanged
    - Session Init (line 29-35) — unchanged
    - Skill Routing table (line 49-55) — TBD, Phase 4 fills
    - 공용 하네스 스킬 (line 56-61) — unchanged
    - 하네스 변경 이력 table (line 74-78) — unchanged (2026-04-18 scaffold row stays)
    - Context Tiers (line 82-93) — unchanged
    - 운영 원칙 (line 97-104) — unchanged
    - GSD blocks (line 109-409) — ABSOLUTELY UNCHANGED

    **Post-edit grep sanity** (run manually after all edits):
    ```bash
    grep -E "TODO:|TODO\(next-session\)" "C:/Users/PC/Desktop/naberal_group/studios/shorts/CLAUDE.md" | grep -vE "금지|regex 차단"
    # Expected: 0 matches (only the "TODO(next-session) 금지" rule reference remains, which is intentional documentation of the ban)
    ```

    Note: the grep filter `| grep -vE "금지|regex 차단"` excludes our own rule bullets that explicitly mention `TODO(next-session)` as a banned pattern (EDIT 4 rule #2). Those are documentation OF the ban, not actual TODOs.

    Precise verification is the VALIDATION.md row 2-W3-01 command: `grep -E "TODO:|TODO\(next-session\)" studios/shorts/CLAUDE.md | grep -vE "^[[:space:]]*<!--"` should return 0 — but we also need to exclude the rule-documentation lines. A stricter check: `grep -c "^TODO:" studios/shorts/CLAUDE.md` should return 0 (no line STARTS with `TODO:`).
  </action>
  <verify>
    <automated>grep -c "^TODO:" "C:/Users/PC/Desktop/naberal_group/studios/shorts/CLAUDE.md" | grep -q "^0$" && grep -c "vv1.0" "C:/Users/PC/Desktop/naberal_group/studios/shorts/CLAUDE.md" | grep -q "^0$" && grep -c "skip_gates=True 금지" "C:/Users/PC/Desktop/naberal_group/studios/shorts/CLAUDE.md" | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^TODO:" C:/Users/PC/Desktop/naberal_group/studios/shorts/CLAUDE.md` returns `0` (no line starts with `TODO:`)
    - `grep -cE "TODO: 도메인|TODO: 파이프라인|TODO: 하네스" C:/Users/PC/Desktop/naberal_group/studios/shorts/CLAUDE.md` returns `0`
    - `grep -c "vv1.0" C:/Users/PC/Desktop/naberal_group/studios/shorts/CLAUDE.md` returns `0`
    - `grep -c "v1.0.1" C:/Users/PC/Desktop/naberal_group/studios/shorts/CLAUDE.md` returns ≥ `1`
    - `grep -c "skip_gates=True 금지" C:/Users/PC/Desktop/naberal_group/studios/shorts/CLAUDE.md` returns `1` (rule #1)
    - `grep -c "TODO(next-session) 금지" C:/Users/PC/Desktop/naberal_group/studios/shorts/CLAUDE.md` returns `1` (rule #2 — documentation of the ban)
    - `grep -c "T2V 금지" C:/Users/PC/Desktop/naberal_group/studios/shorts/CLAUDE.md` returns `1` (rule #4)
    - `grep -c "Selenium 업로드 영구 금지" C:/Users/PC/Desktop/naberal_group/studios/shorts/CLAUDE.md` returns `1` (rule #5)
    - `grep -c "shorts_naberal.* 원본 수정 금지" C:/Users/PC/Desktop/naberal_group/studios/shorts/CLAUDE.md` returns `1` (rule #6)
    - `grep -c "K-pop 트렌드 음원 직접 사용 금지" C:/Users/PC/Desktop/naberal_group/studios/shorts/CLAUDE.md` returns `1` (rule #7)
    - `grep -c "주 3~4편 페이스 준수" C:/Users/PC/Desktop/naberal_group/studios/shorts/CLAUDE.md` returns `1` (rule #8)
    - `grep -c "IDLE" C:/Users/PC/Desktop/naberal_group/studios/shorts/CLAUDE.md` returns ≥ `1` (PIPELINE_FLOW)
    - `grep -c "RESEARCH_NLM" C:/Users/PC/Desktop/naberal_group/studios/shorts/CLAUDE.md` returns `1`
    - `grep -c "YPP 진입 궤도" C:/Users/PC/Desktop/naberal_group/studios/shorts/CLAUDE.md` returns ≥ `1`
    - `grep -c "쇼츠 돌려" C:/Users/PC/Desktop/naberal_group/studios/shorts/CLAUDE.md` returns `1` (Korean trigger phrase)
    - `grep -c "TBD (Phase 4 Agent Team Design)" C:/Users/PC/Desktop/naberal_group/studios/shorts/CLAUDE.md` returns `1`
    - `grep -c "TBD (Phase 5)" C:/Users/PC/Desktop/naberal_group/studios/shorts/CLAUDE.md` OR similar Phase 5 marker in PIPELINE_FLOW returns ≥ `1`
    - GSD blocks preserved: `grep -c "<!-- GSD:project-start source:PROJECT.md -->" C:/Users/PC/Desktop/naberal_group/studios/shorts/CLAUDE.md` returns `1`
    - GSD blocks preserved: `grep -c "<!-- GSD:profile-end -->" C:/Users/PC/Desktop/naberal_group/studios/shorts/CLAUDE.md` returns `1`
    - File line count approximately preserved (grew by ~15-20 lines from the expanded domain rules): `wc -l C:/Users/PC/Desktop/naberal_group/studios/shorts/CLAUDE.md` returns between 415 and 440
  </acceptance_criteria>
  <done>CLAUDE.md contains zero unfilled TODO placeholders, typo corrected, 5 Edit operations applied covering 6 semantic edit sites per D2-D (D-1~D-10 reflected, Phase 4-5 marked TBD). GSD auto-managed blocks untouched. File ready for studio repo commit in Plan 06.</done>
</task>

</tasks>

<verification>
VALIDATION.md rows 2-W3-01, 2-W3-02, 2-W3-03:
- `grep -E "TODO:|TODO\(next-session\)" studios/shorts/CLAUDE.md | grep -vE "^[[:space:]]*<!--"` → 0 unfilled TODOs (only rule-doc lines remain, those state `X 금지`)
- `grep -c "vv1.0" studios/shorts/CLAUDE.md` → 0; `grep -c "v1.0.1" studios/shorts/CLAUDE.md` → ≥ 1
- `grep -c "skip_gates=True 금지" studios/shorts/CLAUDE.md` → 1
</verification>

<success_criteria>
1. All 5 TODO placeholders replaced with concrete domain-scope content (via 5 Edit operations covering 6 sites).
2. Line 7 typo fixed (`vv1.0` → `v1.0.1`).
3. 8 Hard Constraint bullets present in DOMAIN_ABSOLUTE_RULES section.
4. 12-GATE pipeline diagram present with Phase 5 TBD note.
5. hive goal declares 주 3~4편 + YPP, triggers declare 5 Korean phrases.
6. GSD auto-managed blocks (line 109+) byte-identical to pre-edit state.
7. File ready for commit in Plan 06 (studio consolidated commit).

**Phase 2 ROADMAP SC#3 달성 — domain scope 문서로 선언됨.**
</success_criteria>

<output>
After completion, create `.planning/phases/02-domain-definition/02-04-SUMMARY.md` with:
- 5 Edit operations applied to CLAUDE.md covering 6 edit sites (4 TODO replacements + 1 typo fix + 1 merged Line 64-68 block)
- Before/after line counts
- Confirmation GSD blocks untouched
- File pending commit in Plan 06
</output>
</output>
