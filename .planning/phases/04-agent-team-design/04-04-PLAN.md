---
phase: 04-agent-team-design
plan: 04
type: execute
wave: 1
depends_on: [01]
files_modified:
  - .claude/agents/inspectors/style/ins-tone-brand/AGENT.md
  - .claude/agents/inspectors/style/ins-readability/AGENT.md
  - .claude/agents/inspectors/style/ins-thumbnail-hook/AGENT.md
  - tests/phase04/test_inspector_style.py
autonomous: true
requirements:
  - AGENT-04
  - AGENT-07
  - AGENT-08
  - AGENT-09
  - RUB-01
  - RUB-02
  - RUB-04
  - RUB-05
  - RUB-06
  - CONTENT-06

must_haves:
  truths:
    - "3 style inspectors exist under .claude/agents/inspectors/style/{name}/"
    - "ins-tone-brand maxTurns=5 (RUB-05 exception for 채널바이블 대조 + 톤 샘플)"
    - "ins-readability + ins-thumbnail-hook maxTurns=3"
    - "ins-readability prompt body contains '24' AND '32' AND '1' AND '4' (24~32pt + 1~4 단어/라인 — CONTENT-06)"
    - "ins-tone-brand prompt body references '.preserved/harvested/theme_bible_raw/' (채널바이블 10줄 바이블)"
  artifacts:
    - path: ".claude/agents/inspectors/style/ins-tone-brand/AGENT.md"
      provides: "채널바이블 tone 정합 + 탐정-조수 duo dialogue 톤 일관성 + 금지어 검출"
      max_lines: 500
    - path: ".claude/agents/inspectors/style/ins-readability/AGENT.md"
      provides: "한국어 자막 burn-in 스펙 (24~32pt, 1~4 단어/라인, 중앙 정렬) 검증 (CONTENT-06)"
      max_lines: 500
    - path: ".claude/agents/inspectors/style/ins-thumbnail-hook/AGENT.md"
      provides: "thumbnail 텍스트 가독성 + 색상 대비 + CTR 패턴 검증"
      max_lines: 500
  key_links:
    - from: "ins-tone-brand/AGENT.md"
      to: ".preserved/harvested/theme_bible_raw/"
      via: "References section (읽기 전용 참조, 10줄 바이블 10 필드)"
      pattern: "theme_bible_raw"
    - from: "ins-readability/AGENT.md"
      to: "CONTENT-06 자막 스펙"
      via: "Prompt body hardcodes 24-32pt + 1-4 단어/라인"
      pattern: "24.*32.*pt|24~32"
---

<objective>
Wave 1c parallel. Create 3 Style Inspector AGENT.md files. Style category guards channel identity + thumbnail + subtitle readability.

Purpose:
- ins-tone-brand: 채널바이블 대조 (하오체 탐정 + 해요체 조수 + 금지어) — maxTurns=5 (RUB-05 exception)
- ins-readability: 자막 24~32pt + 1~4 단어/라인 (CONTENT-06)
- ins-thumbnail-hook: thumbnail CTR 패턴

Output: 3 AGENT.md files + 1 pytest file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-agent-team-design/04-CONTEXT.md
@.planning/phases/04-agent-team-design/04-RESEARCH.md
@.claude/agents/_shared/agent-template.md
@.claude/agents/_shared/rubric-schema.json

<interfaces>
RESEARCH.md §3.9 line 1087: ins-tone-brand maxTurns=5 (채널바이블 대조 + 톤 샘플 LLM 판정)
RESEARCH.md §5.6 lines 1193-1196: ins-readability 폰트 Pretendard/Noto Sans KR, 24~32pt, 1~4 단어/라인
Phase 3 harvested: .preserved/harvested/theme_bible_raw/ 7 channel bibles (읽기 전용, attrib +R)
Phase 2 02-HARVEST_SCOPE.md defines 10줄 바이블 10 필드: 타겟/길이/목표/톤/금지어/문장규칙/구조/근거규칙/화면규칙/CTA규칙
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 3 Style Inspector AGENT.md files</name>
  <files>
    .claude/agents/inspectors/style/ins-tone-brand/AGENT.md
    .claude/agents/inspectors/style/ins-readability/AGENT.md
    .claude/agents/inspectors/style/ins-thumbnail-hook/AGENT.md
  </files>
  <action>
Use agent-template.md Inspector variant.

**ins-tone-brand/AGENT.md** (~200 lines, maxTurns=5):
```yaml
  ---
name: ins-tone-brand
description: 채널바이블 기반 톤 정합 검증 (탐정 하오체 + 조수 해요체 + 금지어 + 톤 드리프트). 트리거 키워드 ins-tone-brand, 채널바이블, tone, 톤, 브랜드. Input: scripter 대본 + niche-classifier 산출 bible ref. Output: rubric. maxTurns=5 (RUB-05 exception, 채널바이블 대조 필요). 창작 금지 (RUB-02). ≤1024자.
version: 1.0
role: inspector
category: style
maxTurns: 5
  ---
```
- Purpose: 채널바이블 10 필드 (특히 톤/금지어/문장규칙) 대조
- References section MUST include: `.preserved/harvested/theme_bible_raw/{niche}.md` (읽기 참조, attrib +R)
- LogicQA sub_qs:
  - q1: scripter 대본의 tone이 bible의 톤 필드와 일치?
  - q2: bible 금지어 모두 부재?
  - q3: 문장 길이/구조가 bible 문장규칙 준수?
  - q4: 탐정 하오체 + 조수 해요체 일관성 (ins-korean-naturalness와 다른 축: 톤 드리프트)?
  - q5: 채널별 "한 줄 아이덴티티"가 hook + CTA에 반영?

**ins-readability/AGENT.md** (~150 lines, maxTurns=3):
```yaml
  ---
name: ins-readability
description: 한국어 자막 가독성 검증 (폰트 24~32pt, 1~4 단어/라인, 중앙 정렬, Pretendard/Noto Sans KR). 트리거 키워드 ins-readability, 자막, 가독성, burn-in, 폰트. Input: 자막 JSON (subtitle segments). Output: rubric. CONTENT-06 게이트. maxTurns=3. ≤1024자.
version: 1.0
role: inspector
category: style
maxTurns: 3
  ---
```
- Purpose: CONTENT-06 한국어 자막 burn-in (24~32pt, 1~4 단어/라인, 중앙) 검증
- Prompt body MUST contain strings: "24", "32", "1", "4", "Pretendard", "중앙"
- LogicQA sub_qs:
  - q1: 폰트 크기 24~32pt 범위?
  - q2: 줄당 단어 수 1~4 범위?
  - q3: 중앙 정렬?
  - q4: 폰트 패밀리 ∈ {Pretendard, Noto Sans KR}?
  - q5: 자막 세그먼트가 음성 t_start/t_end와 1초 단위 blocking (SUBT 연동)?

**ins-thumbnail-hook/AGENT.md** (~150 lines, maxTurns=3):
```yaml
  ---
name: ins-thumbnail-hook
description: thumbnail 텍스트 가독성 + 색상 대비 + CTR hook 패턴 검증. 트리거 키워드 ins-thumbnail-hook, thumbnail, 썸네일, CTR, hook. Input: thumbnail-designer 산출 JSON (text overlay + colors + layout). Output: rubric. maxTurns=3. 창작 금지. ≤1024자.
version: 1.0
role: inspector
category: style
maxTurns: 3
  ---
```
- Purpose: thumbnail CTR hook (질문형 / 숫자 / 고유명사) + 텍스트 대비 (WCAG AA ≥ 4.5:1) + 색상 팔레트 채널바이블 준수
- LogicQA sub_qs:
  - q1: thumbnail 텍스트 ≤ 7글자 (쇼츠 세로 썸네일)?
  - q2: 텍스트 - 배경 명도 대비 ≥ 4.5:1?
  - q3: 텍스트에 질문형 or 숫자 or 고유명사 hook?
  - q4: 색상 팔레트가 Continuity Bible(Phase 6)과 일치?
  - q5: 인물/로고 blur 또는 모자이크 적용 (AF-5 예방)?

All 3: MUST REMEMBER at END containing 창작 금지 (RUB-02) + producer_prompt 금지 (RUB-06) + 5 sub-q LogicQA 의무 + maxTurns 준수 + rubric schema 준수 + Supervisor 재호출 금지.
  </action>
  <verify>
    <automated>
py -3.11 -m scripts.validate.validate_all_agents --path .claude/agents/inspectors/style ; py -3.11 -c "import pathlib; f='.claude/agents/inspectors/style/ins-tone-brand/AGENT.md'; body=pathlib.Path(f).read_text('utf-8'); assert 'theme_bible_raw' in body, 'missing theme_bible_raw ref'; assert 'maxTurns: 5' in body, 'maxTurns=5 missing'"
    </automated>
  </verify>
  <done>
3 AGENT.md files; ins-tone-brand maxTurns=5 + references theme_bible_raw; ins-readability body contains 24/32/1/4 + Pretendard; ins-thumbnail-hook maxTurns=3; all pass validate_all_agents.py.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write Style Inspector compliance tests</name>
  <files>
    tests/phase04/test_inspector_style.py
  </files>
  <behavior>
    - exactly 3 AGENT.md files under style/
    - role=inspector, category=style for all 3
    - ins-tone-brand maxTurns=5; ins-readability + ins-thumbnail-hook maxTurns=3
    - all 3 have 5 sub_qs LogicQA
    - ins-tone-brand references "theme_bible_raw"
    - ins-readability body contains "24" AND "32" AND "Pretendard"
    - all MUST REMEMBER at END (ratio_from_end ≤ 0.4)
    - all MUST REMEMBER contain 창작 금지 + producer_prompt
  </behavior>
  <action>
Pattern identical to test_inspector_structural.py + test_inspector_content.py, parametrized for style category.

Include specific assertions:
```python
def test_ins_tone_brand_maxturns_5(style_agents):
    for path, meta, body in style_agents:
        if meta["name"] == "ins-tone-brand":
            assert int(meta["maxTurns"]) == 5
            assert "theme_bible_raw" in body
            return
    pytest.fail("ins-tone-brand not found")

def test_ins_readability_content_06(style_agents):
    for path, meta, body in style_agents:
        if meta["name"] == "ins-readability":
            assert "24" in body and "32" in body
            assert "Pretendard" in body or "Noto Sans KR" in body
            return
    pytest.fail("ins-readability not found")
```
  </action>
  <verify>
    <automated>
py -3.11 -m pytest tests/phase04/test_inspector_style.py -v --tb=short 2>&1 | tail -15
    </automated>
  </verify>
  <done>
All tests PASS. 3 style inspectors compliance verified.
  </done>
</task>

</tasks>

<verification>
Wave 1c Style category complete when:
1. 3 AGENT.md files under .claude/agents/inspectors/style/*/
2. ins-tone-brand maxTurns=5 + references theme_bible_raw
3. ins-readability hardcodes 24/32/Pretendard (CONTENT-06)
4. validate_all_agents.py passes
5. test_inspector_style.py all tests PASS
</verification>

<success_criteria>
- [ ] 3 Style Inspector AGENT.md files with correct frontmatter
- [ ] ins-tone-brand maxTurns=5 (RUB-05 exception) + theme_bible_raw reference
- [ ] ins-readability contains 24/32pt + 1/4 단어/라인 + Pretendard (CONTENT-06)
- [ ] ins-thumbnail-hook maxTurns=3 + 5 sub_qs LogicQA
- [ ] All MUST REMEMBER at END + contain 창작 금지 (RUB-02) + producer_prompt (RUB-06)
- [ ] test_inspector_style.py all PASS
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-team-design/04-04-SUMMARY.md`
</output>
