---
phase: 04-agent-team-design
plan: 08
type: execute
wave: 3
depends_on: [01, 02, 03, 04, 05, 06, 07]
files_modified:
  - .claude/agents/producers/trend-collector/AGENT.md
  - .claude/agents/producers/niche-classifier/AGENT.md
  - .claude/agents/producers/researcher/AGENT.md
  - .claude/agents/producers/director/AGENT.md
  - .claude/agents/producers/scene-planner/AGENT.md
  - .claude/agents/producers/shot-planner/AGENT.md
  - .claude/agents/producers/scripter/AGENT.md
  - .claude/agents/producers/script-polisher/AGENT.md
  - .claude/agents/producers/metadata-seo/AGENT.md
  - tests/phase04/test_producer_core.py
  - tests/phase04/test_producer_3split.py
  - tests/phase04/test_scripter_rules.py
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02
  - AGENT-07
  - AGENT-08
  - AGENT-09
  - RUB-03
  - CONTENT-01
  - CONTENT-02
  - CONTENT-03
  - CONTENT-04
  - CONTENT-05
  - CONTENT-07

must_haves:
  truths:
    - "Producer Core 6 (AGENT-01): trend-collector, niche-classifier, researcher, scripter, script-polisher, metadata-seo all exist"
    - "Producer 3단 분리 (AGENT-02): director, scene-planner, shot-planner all exist"
    - "All 9 producers have role=producer, maxTurns=3, category ∈ {core, split3}"
    - "All 9 producers have <prior_vqqa> or <prior_feedback> block in prompt body (RUB-03 VQQA 반영)"
    - "scripter prompt body contains '3초 hook' + '질문형' + '숫자' + '고유명사' + '탐정' + '하오체' + '조수' + '해요체' (CONTENT-01 + CONTENT-02)"
    - "niche-classifier prompt references '.preserved/harvested/theme_bible_raw/' (CONTENT-03)"
    - "researcher prompt body contains 'citation' + ('source-grounded' OR 'NotebookLM') (CONTENT-04)"
    - "metadata-seo prompt body contains 'roman' or '로마자' + '한국어' (CONTENT-07)"
    - "shot-planner prompt body contains 'I2V only' + 'anchor frame' + 'T2V 금지' (NotebookLM T1)"
    - "scene-planner prompt body contains '1 Move Rule' (NotebookLM T2)"
  artifacts:
    - path: ".claude/agents/producers/trend-collector/AGENT.md"
      provides: "한국 실시간 트렌드 수집 → 10-20 keyword candidates + niche tag"
      max_lines: 500
    - path: ".claude/agents/producers/niche-classifier/AGENT.md"
      provides: "keyword → channel_bible 매핑, theme_bible_raw 참조 (CONTENT-03)"
      max_lines: 500
    - path: ".claude/agents/producers/researcher/AGENT.md"
      provides: "NotebookLM grounded research manifest (CONTENT-04), Fallback chain stub (Phase 6 실장)"
      max_lines: 500
    - path: ".claude/agents/producers/director/AGENT.md"
      provides: "Blueprint JSON (tone, high-level structure, target_emotion, channel_bible_ref, scene_count 5-10)"
      max_lines: 500
    - path: ".claude/agents/producers/scene-planner/AGENT.md"
      provides: "Scenes JSON (4-8 scene, 1 Move Rule per scene, visual_motif)"
      max_lines: 500
    - path: ".claude/agents/producers/shot-planner/AGENT.md"
      provides: "Shots JSON (1-3 shot per scene, anchor_frame_ref, camera_move, I2V prompt; T2V 금지)"
      max_lines: 500
    - path: ".claude/agents/producers/scripter/AGENT.md"
      provides: "대본 JSON (3초 hook + duo dialogue + ≤59s + citation; CONTENT-01/02/04/05)"
      max_lines: 500
    - path: ".claude/agents/producers/script-polisher/AGENT.md"
      provides: "문체/리듬/종결어미 교정 (의미 변경 금지)"
      max_lines: 500
    - path: ".claude/agents/producers/metadata-seo/AGENT.md"
      provides: "한국어 + 로마자 병기 메타데이터 (CONTENT-07)"
      max_lines: 500
  key_links:
    - from: "scripter/AGENT.md"
      to: "ins-narrative-quality + ins-korean-naturalness (Inspector 예상 피드백)"
      via: "Prompt body 'Inspector FAIL 시 VQQA 피드백 재입력' 구절"
      pattern: "prior_vqqa|prior_feedback|VQQA"
    - from: "niche-classifier/AGENT.md"
      to: ".preserved/harvested/theme_bible_raw/"
      via: "References + MUST REMEMBER 채널바이블 주입 의무"
      pattern: "theme_bible_raw"
    - from: "shot-planner/AGENT.md"
      to: "NotebookLM T1 (I2V only, T2V 금지)"
      via: "Prompt body + MUST REMEMBER hardcoded"
      pattern: "I2V|anchor frame|T2V 금지"
---

<objective>
Wave 3. Create 9 Producer AGENT.md files = Core 6 (AGENT-01) + 3단 분리 3 (AGENT-02). This is the **generation side of the GAN pipeline** — these agents produce artifacts that Inspectors (Plans 02-07) evaluate. The 3단 분리 (director → scene-planner → shot-planner) is NotebookLM T6 (FilmAgent Level 1~3 hierarchy).

Purpose: All 9 producers must:
- Accept `prior_vqqa` input (RUB-03) for retry loop after Inspector FAIL
- Reference channel bible from `.preserved/harvested/theme_bible_raw/` (CONTENT-03)
- Emit next-GATE JSON payload conforming to ins-schema-integrity expectations
- Have MUST REMEMBER at END including "Inspector 프롬프트 읽기 금지 (GAN 분리)"

Output: 9 AGENT.md files + 3 pytest files.

**CRITICAL dependency note:** This plan depends on ALL Wave 1-2 inspector plans (02-07) because Producer prompts reference Inspector FAIL conditions (e.g., scripter MUST REMEMBER: "ins-korean-naturalness FAIL 시 VQQA 교정 필요"). Inspector names must stabilize first.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-agent-team-design/04-CONTEXT.md
@.planning/phases/04-agent-team-design/04-RESEARCH.md
@.claude/agents/_shared/agent-template.md
@.claude/agents/_shared/rubric-schema.json

<interfaces>
RESEARCH.md §3.1 Producer AGENT.md template (lines 1002-1015)
RESEARCH.md §3.2 Producer 3단 분리 (lines 1017-1023):
- director: niche + research → Blueprint JSON (tone, high-level structure, target_emotion, channel_bible_ref). 5-10 scenes 개요.
- scene-planner: Blueprint → Scenes JSON (4-8 scene 분절, t_start/t_end, mood, visual_motif). **1 Move Rule** (T2) enforcement.
- shot-planner: Scenes → Shots JSON (1-3 shot per scene, anchor_frame_ref, camera_move, I2V 프롬프트). **T2V 금지** (T1).

RESEARCH.md §5.1 Producer Core 6 (lines 1105-1112):
- trend-collector: 한국 실시간 트렌드 수집 (NotebookLM RAG). 10-20 keyword candidates + niche tag.
- niche-classifier: keyword → channel_bible 매핑. .preserved/harvested/theme_bible_raw/{niche}.md 참조 필수.
- researcher (=nlm-fetcher): NotebookLM 2 노트북 질의 → citation list + fact sheet. Fallback chain (WIKI-04) 언급 (실 구현 Phase 6).
- scripter: niche + blueprint + scenes + research → 대본 JSON. 3초 hook + duo dialogue + 60초 상한 + citation 의무.
- script-polisher: scripter output의 문체/리듬/종결어미 교정. 기능 보강 금지 (표현만 다듬기).
- metadata-seo: 한국어 제목/설명/태그 + 로마자 병기 (CONTENT-07).

RESEARCH.md §5.2 scripter 3초 hook + duo dialogue 상세 (lines 1114-1123):
- 3초 hook: 문장 말 `?` + 숫자 or 고유명사
- 탐정 하오체 + 조수 해요체

RESEARCH.md lines 524-596: scripter AGENT.md EXAMPLE (축약본) — 이것을 starting point로.

Pattern 4 VQQA feedback loop (lines 357-373): Producer retry 의사코드:
- Input `--prior-vqqa` flag (빈 문자열 시 첫 호출)
- Producer reads feedback, re-generates failing scenes only (not full rewrite)
- Self-check no repetition of same mistake
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Producer Core 6 AGENT.md files</name>
  <files>
    .claude/agents/producers/trend-collector/AGENT.md
    .claude/agents/producers/niche-classifier/AGENT.md
    .claude/agents/producers/researcher/AGENT.md
    .claude/agents/producers/scripter/AGENT.md
    .claude/agents/producers/script-polisher/AGENT.md
    .claude/agents/producers/metadata-seo/AGENT.md
  </files>
  <action>
Use agent-template.md Producer variant. maxTurns=3, category=core for all 6.

**Common Producer pattern (all 6):**
- Frontmatter: role=producer, category=core, maxTurns=3
- Inputs section includes `--prior-vqqa` flag (RUB-03 feedback loop)
- Prompt body includes `### prior_vqqa 반영 서브섹션` per §3.1 line 1012
- Prompt body includes `### 채널바이블 인라인 주입 (CONTENT-03)` if applicable
- MUST REMEMBER 4-6 bullets at END with:
  - creation role + schema compliance
  - prior_vqqa 반영 의무
  - inspector_prompt 읽기 금지 (GAN 분리 mirror)
  - maxTurns 준수
  - channel_bible 참조 (where applicable)

**trend-collector/AGENT.md** (~200 lines):
- Purpose: 한국 실시간 트렌드 수집 (NotebookLM RAG Phase 6, Phase 4는 스펙). Output: {"keywords": [...10-20], "niche_tag": "..."}
- Inputs: `--source` (news feeds + NaverDataLab + Google Trends KR), `--prior-vqqa`
- Prompt body includes NotebookLM 2 노트북 참조 (일반 + 채널바이블, WIKI-03)

**niche-classifier/AGENT.md** (~200 lines):
- Purpose: keyword → channel_bible 매핑 (CONTENT-03)
- Inputs: `--keywords` (from trend-collector), `--channel-bibles .preserved/harvested/theme_bible_raw/`, `--prior-vqqa`
- Prompt body MUST reference: ".preserved/harvested/theme_bible_raw/" (exact path)
- Output: `{"niche_tag": "...", "channel_bible_ref": ".preserved/harvested/theme_bible_raw/{niche}.md", "matched_fields": {10 fields}}`
- 10 필드 체크: 타겟/길이/목표/톤/금지어/문장규칙/구조/근거규칙/화면규칙/CTA규칙

**researcher/AGENT.md** (~250 lines):
- Purpose: NotebookLM grounded research manifest (CONTENT-04)
- Inputs: `--niche-tag`, `--keywords`, `--prior-vqqa`
- Prompt body MUST contain: "citation", "source-grounded" or "NotebookLM", + Fallback chain stub 언급 (WIKI-04 Phase 6 실 구현)
- Output: `{"manifest": [{claim, sources: [url, tier, quote], ...}, ...]}`
- MUST REMEMBER: "Phase 4는 스펙만, 실 NotebookLM API 호출은 Phase 6 이후"

**scripter/AGENT.md** (~350 lines — longest producer, most rules):
- Base: RESEARCH.md lines 522-596 (축약본), expand to full ~350 lines
- Purpose: CONTENT-01/02/04/05/07
- Prompt body MUST contain: "3초 hook", "질문형", "숫자", "고유명사", "탐정", "하오체", "조수", "해요체"
- Inputs: `--blueprint`, `--scenes`, `--research-manifest`, `--prior-vqqa`, `--channel-bible`
- Output JSON: `{duration_sec, hook_text, scenes: [{t_start, t_end, speaker: "detective"|"assistant", register: "하오체"|"해요체", text, citations}]}`
- VQQA feedback block (RUB-03): re-generate failing scenes only, not full rewrite, self-check no repetition
- 채널바이블 인라인 주입 block (CONTENT-03): 10 필드 준수
- MUST REMEMBER 7 bullets: 3초 hook 필수 / 존댓말반말 혼용 금지 / 60초 상한 / citation 의무 / maxTurns=3 / prior_vqqa 반영 / inspector_prompt 읽기 금지

**script-polisher/AGENT.md** (~200 lines):
- Purpose: scripter 산출의 문체/리듬/종결어미 교정. **기능 보강 금지** (의미 변경 금지, 표현만).
- Inputs: `--script-json`, `--prior-vqqa`
- Output: 동일 schema의 script JSON, 단 scene text 필드만 수정
- MUST REMEMBER: "의미 변경 금지 — 표현만 다듬기. scripter가 쓴 hook/citation은 유지."

**metadata-seo/AGENT.md** (~200 lines):
- Purpose: CONTENT-07 한국어 + 로마자 메타데이터
- MUST CONTAIN: "roman" 또는 "로마자", "한국어"
- Inputs: `--script-json`, `--niche-tag`, `--prior-vqqa`
- Output: `{"title_ko": "...", "title_roman": "...", "description_ko": "...", "description_roman": "...", "tags_ko": [...], "tags_roman": [...]}`
- YouTube 글자수 제한 준수 언급
  </action>
  <verify>
    <automated>
py -3.11 -m scripts.validate.validate_all_agents --path .claude/agents/producers ; py -3.11 -c "import pathlib; scripter=pathlib.Path('.claude/agents/producers/scripter/AGENT.md').read_text('utf-8'); kws=['3초 hook','질문형','숫자','고유명사','탐정','하오체','조수','해요체']; miss=[k for k in kws if k not in scripter]; assert not miss, f'scripter missing: {miss}'; niche=pathlib.Path('.claude/agents/producers/niche-classifier/AGENT.md').read_text('utf-8'); assert 'theme_bible_raw' in niche; researcher=pathlib.Path('.claude/agents/producers/researcher/AGENT.md').read_text('utf-8'); assert 'citation' in researcher and ('NotebookLM' in researcher or 'source-grounded' in researcher); meta=pathlib.Path('.claude/agents/producers/metadata-seo/AGENT.md').read_text('utf-8'); assert '한국어' in meta and ('roman' in meta.lower() or '로마자' in meta)"
    </automated>
  </verify>
  <done>
6 Producer Core AGENT.md files exist; all pass validator; scripter contains 8 required keywords; niche-classifier references theme_bible_raw; researcher mentions citation + NotebookLM; metadata-seo contains 한국어 + roman/로마자.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write Producer 3단 분리 3 AGENT.md files (AGENT-02, T6)</name>
  <files>
    .claude/agents/producers/director/AGENT.md
    .claude/agents/producers/scene-planner/AGENT.md
    .claude/agents/producers/shot-planner/AGENT.md
  </files>
  <action>
Use agent-template.md Producer variant. category=split3 for all 3. maxTurns=3.

**director/AGENT.md** (~250 lines):
- Purpose: niche + research → Blueprint JSON
- Inputs: `--niche-tag`, `--channel-bible-ref`, `--research-manifest`, `--prior-vqqa`
- Output JSON: `{"tone": "...", "high_level_structure": ["hook","build","reveal","cta"], "target_emotion": "tense|mysterious|...", "channel_bible_ref": "...", "scene_count": 5-10}`
- MUST REMEMBER: "scene-planner/shot-planner prompt 읽기 금지 (3단 격리). Output은 순수 JSON hand-off."

**scene-planner/AGENT.md** (~250 lines):
- Purpose: Blueprint → Scenes JSON (4-8 scene, 1 Move Rule per scene)
- Prompt body MUST CONTAIN: "1 Move Rule" (T2, NotebookLM)
- Inputs: `--blueprint`, `--prior-vqqa`
- Output JSON: `{"scenes": [{"scene_idx": 1, "t_start": 0.0, "t_end": 3.0, "mood": "...", "visual_motif": "...", "speaker_hint": "detective|assistant", "move_hint": "1 camera action + 1 subject action"}]}`
- 1 Move Rule 정의: 카메라 워킹 1 + 피사체 액션 1 (총 2 moves ≠ 3+ moves)
- MUST REMEMBER: "1 Move Rule 위반 시 자기 검열 FAIL. 4초 이상 scene 지양 (T2)."

**shot-planner/AGENT.md** (~250 lines):
- Purpose: Scenes → Shots JSON (1-3 shot per scene, I2V + anchor frame)
- Prompt body MUST CONTAIN: "I2V only", "anchor frame", "T2V 금지"
- Inputs: `--scenes`, `--prior-vqqa`, `--channel-bible-ref` (색상 팔레트 + 카메라 렌즈 - Continuity Bible)
- Output JSON: `{"shots": [{"scene_idx": 1, "shot_idx": 1, "anchor_frame_ref": "asset://...", "camera_move": "pan_left 0.5sec", "i2v_prompt": "... (Nano Banana style)", "duration_sec": 3.0}]}`
- MUST REMEMBER: "T2V 금지 — 모든 shot은 anchor frame 기반 I2V. T2V prompt 작성 시 자기 검열 FAIL (NotebookLM T1)."
  </action>
  <verify>
    <automated>
py -3.11 -c "import pathlib; d=pathlib.Path('.claude/agents/producers/director/AGENT.md').read_text('utf-8'); assert 'Blueprint' in d; sp=pathlib.Path('.claude/agents/producers/scene-planner/AGENT.md').read_text('utf-8'); assert '1 Move Rule' in sp; shp=pathlib.Path('.claude/agents/producers/shot-planner/AGENT.md').read_text('utf-8'); assert 'I2V' in shp and 'anchor frame' in shp and 'T2V 금지' in shp"
    </automated>
  </verify>
  <done>
3 Producer 3단 AGENT.md files; scene-planner contains '1 Move Rule'; shot-planner contains I2V + anchor frame + T2V 금지.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write Producer Core + 3split + scripter rules tests</name>
  <files>
    tests/phase04/test_producer_core.py
    tests/phase04/test_producer_3split.py
    tests/phase04/test_scripter_rules.py
  </files>
  <behavior>
    test_producer_core.py:
    - exactly 6 Producer Core AGENT.md files (trend-collector, niche-classifier, researcher, scripter, script-polisher, metadata-seo)
    - All role=producer, category=core, maxTurns=3
    - Each contains `prior_vqqa` reference in Inputs section (RUB-03)
    - niche-classifier body contains "theme_bible_raw" (CONTENT-03)
    - researcher body contains "citation" AND ("NotebookLM" OR "source-grounded") (CONTENT-04)
    - metadata-seo body contains "한국어" AND ("roman" OR "로마자") (CONTENT-07)

    test_producer_3split.py:
    - exactly 3 Producer 3split files (director, scene-planner, shot-planner)
    - All role=producer, category=split3, maxTurns=3
    - scene-planner body contains "1 Move Rule"
    - shot-planner body contains "I2V" AND "anchor frame" AND "T2V 금지"
    - All have MUST REMEMBER at END with "inspector_prompt 읽기 금지" (GAN mirror)

    test_scripter_rules.py:
    - scripter body contains all 8 keywords: 3초 hook / 질문형 / 숫자 / 고유명사 / 탐정 / 하오체 / 조수 / 해요체
    - scripter MUST REMEMBER has 7+ bullets
    - scripter Inputs section includes both --prior-vqqa and --channel-bible
  </behavior>
  <action>
Write 3 pytest files following structural/content/style test patterns but adapted for Producer role.

Key adaptations:
- `role == "producer"`
- Test for `prior_vqqa` or `--prior-vqqa` in Inputs
- Test MUST REMEMBER contains "inspector_prompt" (GAN separation mirror of Inspector's "producer_prompt 읽기 금지")
- Specific keyword presence tests per producer
  </action>
  <verify>
    <automated>
py -3.11 -m pytest tests/phase04/test_producer_core.py tests/phase04/test_producer_3split.py tests/phase04/test_scripter_rules.py -v --tb=short 2>&1 | tail -30
    </automated>
  </verify>
  <done>
All 3 pytest files run, all tests PASS.
  </done>
</task>

</tasks>

<verification>
Wave 3 Producer Core + 3단 분리 complete when:
1. 9 AGENT.md files exist under .claude/agents/producers/*/ (6 core + 3 split3)
2. All 9 pass validate_all_agents.py (≤500 lines, description ≤1024 chars, MUST REMEMBER at END)
3. scripter contains all 8 CONTENT-01/02 keywords
4. niche-classifier references theme_bible_raw (CONTENT-03)
5. researcher contains citation + NotebookLM/source-grounded (CONTENT-04)
6. metadata-seo contains 한국어 + roman/로마자 (CONTENT-07)
7. scene-planner: 1 Move Rule (T2)
8. shot-planner: I2V + anchor frame + T2V 금지 (T1)
9. All 9 have `prior_vqqa` in Inputs + inspector_prompt 읽기 금지 in MUST REMEMBER (GAN mirror)
10. All 3 pytest files exit 0
</verification>

<success_criteria>
- [ ] Producer Core 6 (AGENT-01) AGENT.md files exist with correct frontmatter
- [ ] Producer 3단 3 (AGENT-02) AGENT.md files exist with category=split3
- [ ] scripter has 3초 hook + 질문형 + 숫자/고유명사 + duo dialogue (CONTENT-01/02)
- [ ] niche-classifier references theme_bible_raw (CONTENT-03)
- [ ] researcher has citation + NotebookLM (CONTENT-04)
- [ ] metadata-seo has 한국어 + roman (CONTENT-07)
- [ ] scene-planner has 1 Move Rule (T2)
- [ ] shot-planner has I2V + T2V 금지 (T1)
- [ ] All have --prior-vqqa input + MUST REMEMBER "inspector_prompt 읽기 금지"
- [ ] All 3 pytest files PASS
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-team-design/04-08-SUMMARY.md`
</output>
