---
phase: 04-agent-team-design
plan: 09
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05, 06, 07, 08]
files_modified:
  - .claude/agents/producers/voice-producer/AGENT.md
  - .claude/agents/producers/asset-sourcer/AGENT.md
  - .claude/agents/producers/assembler/AGENT.md
  - .claude/agents/producers/thumbnail-designer/AGENT.md
  - .claude/agents/producers/publisher/AGENT.md
  - .claude/agents/supervisor/shorts-supervisor/AGENT.md
  - tests/phase04/test_producer_support.py
  - tests/phase04/test_supervisor_depth_guard.py
  - tests/phase04/test_maxturns_matrix.py
autonomous: true
requirements:
  - AGENT-03
  - AGENT-05
  - AGENT-07
  - AGENT-08
  - AGENT-09
  - RUB-05
  - AUDIO-01
  - AUDIO-02
  - AUDIO-03
  - AUDIO-04

must_haves:
  truths:
    - "Producer 지원 5 (AGENT-03): voice-producer, asset-sourcer, assembler, thumbnail-designer, publisher exist under .claude/agents/producers/*/"
    - "Supervisor 1 (AGENT-05): shorts-supervisor exists under .claude/agents/supervisor/shorts-supervisor/"
    - "voice-producer body contains 'Typecast' (primary) + 'ElevenLabs' (fallback) + emotion enum ≥ 4 (neutral/tense/sad/happy) (AUDIO-01, AUDIO-03)"
    - "asset-sourcer body contains 'royalty-free' + '3' + '5' + 'crossfade' (AUDIO-02) + Epidemic Sound + Artlist whitelist"
    - "shorts-supervisor body contains '_delegation_depth' guard clause + '>= 1' raise logic (AGENT-05)"
    - "shorts-supervisor body contains '17' + 'inspector' + 'fan-out' + Supervisor aggregation formula"
    - "shorts-supervisor maxTurns=3, delegation_depth default=0, output follows supervisor-rubric-schema.json"
    - "All 17 inspector + all 14 producer + 1 supervisor = 32 agents total (entire .claude/agents/ .rglob('AGENT.md') excluding harvest-importer)"
    - "maxTurns matrix enforced: factcheck=10, tone-brand=5, structural 3 ins=1, others=3"
  artifacts:
    - path: ".claude/agents/producers/voice-producer/AGENT.md"
      provides: "Typecast primary + ElevenLabs fallback + 감정 동적 enum (AUDIO-01, 03) + AF-4 2차 방어"
      max_lines: 500
    - path: ".claude/agents/producers/asset-sourcer/AGENT.md"
      provides: "royalty-free 3-5s crossfade hybrid audio (AUDIO-02) + whitelist 도메인 검증"
      max_lines: 500
    - path: ".claude/agents/producers/assembler/AGENT.md"
      provides: "Remotion composition 호출 orchestration 스펙 (Phase 5 실 호출)"
      max_lines: 500
    - path: ".claude/agents/producers/thumbnail-designer/AGENT.md"
      provides: "thumbnail 생성 스펙 (텍스트 overlay + 색상 + layout)"
      max_lines: 500
    - path: ".claude/agents/producers/publisher/AGENT.md"
      provides: "YouTube Data API v3 업로드 스펙 (Phase 8 실 호출, Phase 4는 AI disclosure toggle 강제 명시)"
      max_lines: 500
    - path: ".claude/agents/supervisor/shorts-supervisor/AGENT.md"
      provides: "17 inspector fan-out + rubric 합산 + 1-hop 재귀 방지 + routing decision"
      max_lines: 500
  key_links:
    - from: "shorts-supervisor/AGENT.md"
      to: ".claude/agents/_shared/supervisor-rubric-schema.json"
      via: "Outputs section + References"
      pattern: "supervisor-rubric-schema\\.json"
    - from: "shorts-supervisor/AGENT.md"
      to: "_delegation_depth guard (AGENT-05)"
      via: "Prompt body embeds pseudo-code"
      pattern: "_delegation_depth|delegation_depth"
    - from: "voice-producer/AGENT.md"
      to: "AF-4 bank (ins-license upstream + voice-producer 2차 방어)"
      via: "MUST REMEMBER bullet + References"
      pattern: "af_bank\\.json|AF-4"
---

<objective>
Wave 4. Create 5 Producer Support + 1 Supervisor AGENT.md files. This completes the 32-agent roster (17 inspector + 14 producer + 1 supervisor). The Supervisor is the **single 1-hop orchestrator** — all inspector fan-out happens here, and AGENT-05 재귀 방지 guard is the critical invariant.

Purpose:
- voice-producer: Typecast primary + ElevenLabs fallback + 감정 동적 enum (AUDIO-01, 03)
- asset-sourcer: royalty-free 3-5s crossfade + whitelist (AUDIO-02, 04)
- assembler: Remotion composition spec (Phase 5 실행)
- thumbnail-designer: thumbnail 생성 spec
- publisher: YouTube Data API v3 spec + AI disclosure 강제 (Phase 8 실행)
- shorts-supervisor: 17 inspector fan-out + _delegation_depth guard

Output: 6 AGENT.md files + 3 pytest files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-agent-team-design/04-CONTEXT.md
@.planning/phases/04-agent-team-design/04-RESEARCH.md
@.claude/agents/_shared/agent-template.md
@.claude/agents/_shared/rubric-schema.json
@.claude/agents/_shared/supervisor-rubric-schema.json

<interfaces>
RESEARCH.md §5.4 lines 1144-1157 (AUDIO 규칙):
- voice-producer: Typecast primary, ElevenLabs fallback, 감정 enum `neutral | tense | sad | happy | urgent | mysterious | empathetic`, 탐정/조수 speaker별 voice_id preset, AF-4 차단 2차
- asset-sourcer: royalty-free whitelist (Epidemic Sound, Artlist, YouTube Audio Library, Free Music Archive), 3-5s crossfade with 본 음악

RESEARCH.md §3.4 (line 1038-1040): Supervisor _delegation_depth guard
RESEARCH.md Pattern 3 (lines 338-349): Supervisor fan-out pseudocode
RESEARCH.md lines 686-748: shorts-supervisor AGENT.md EXAMPLE (축약본)

RESEARCH.md §3.9 maxTurns matrix (lines 1080-1089):
- Producer (표준) = 3
- Inspector (표준) = 3
- ins-factcheck = 10
- ins-tone-brand = 5
- ins-schema-integrity 등 순수 regex = 1 (structural 3개)
- Supervisor = 3

.preserved/harvested/api_wrappers_raw/ contains: elevenlabs_alignment.py + tts_generate.py + _kling_i2v_batch.py + runway_client.py + heygen_client.py (읽기 참조용 Phase 5)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 5 Producer Support AGENT.md files</name>
  <files>
    .claude/agents/producers/voice-producer/AGENT.md
    .claude/agents/producers/asset-sourcer/AGENT.md
    .claude/agents/producers/assembler/AGENT.md
    .claude/agents/producers/thumbnail-designer/AGENT.md
    .claude/agents/producers/publisher/AGENT.md
  </files>
  <action>
Use agent-template.md Producer variant. role=producer, category=support, maxTurns=3 for all 5.

**voice-producer/AGENT.md** (~250 lines):
```yaml
  ---
name: voice-producer
description: Typecast primary (한국어 TTS 1위) + ElevenLabs fallback + 감정 동적 파라미터 (neutral/tense/sad/happy/urgent/mysterious/empathetic). AF-4 실존 인물 voice clone 차단 2차 방어. 트리거 키워드 voice-producer, TTS, Typecast, ElevenLabs, 음성. Input: script JSON + speaker_name + emotion_hint. Output: audio segment JSON. maxTurns=3. ≤1024자.
version: 1.0
role: producer
category: support
maxTurns: 3
  ---
```
- MUST CONTAIN body: "Typecast" + "ElevenLabs" + 7 emotion enum (neutral, tense, sad, happy, urgent, mysterious, empathetic) + "AF-4"
- 탐정 voice_id preset + 조수 voice_id preset 언급 (Phase 5에서 실 id 확정)
- AF-4 2차 방어 pseudocode: speaker_name이 `AF-4 bank`에 매치 시 즉시 raise

**asset-sourcer/AGENT.md** (~250 lines):
```yaml
  ---
name: asset-sourcer
description: 하이브리드 오디오 (트렌딩 3-5초 + royalty-free crossfade). 영상 asset (이미지/영상) whitelist 도메인만. Epidemic Sound, Artlist, YouTube Audio Library, Free Music Archive. 트리거 키워드 asset-sourcer, asset, royalty-free, 음원, 이미지. Input: script JSON + scene_count. Output: assets JSON (audio + image/video URLs). maxTurns=3. ≤1024자.
version: 1.0
role: producer
category: support
maxTurns: 3
  ---
```
- MUST CONTAIN body: "royalty-free", "3", "5", "crossfade", "Epidemic Sound", "Artlist", "YouTube Audio Library", "Free Music Archive"
- AUDIO-02 하이브리드 오디오 규칙 명시

**assembler/AGENT.md** (~200 lines):
- Purpose: Remotion composition 호출 orchestration (Phase 5 실행)
- References: `.preserved/harvested/remotion_src_raw/` (읽기 전용)
- Input: script + voice_segments + assets + shots → Output: video file path (or Phase 4 스펙 JSON)
- MUST REMEMBER: "Phase 4는 스펙만. 실 Remotion CLI 호출은 Phase 5 assembler.py 모듈이 수행."

**thumbnail-designer/AGENT.md** (~200 lines):
- Purpose: thumbnail JSON 생성 (텍스트 + 색상 + layout)
- Output JSON: `{"text_overlay": "...", "font_family": "Pretendard", "font_size": 80, "text_color": "#...", "bg_color": "#...", "layout": "center-top", "anchor_image_ref": "..."}`
- AF-5 연계: 실존 피해자 얼굴 사용 금지 (ins-mosaic upstream 방어)
- MUST REMEMBER: "AI 얼굴 생성 시 실존 인물명 caption 금지 (AF-5 차단 2차 방어)"

**publisher/AGENT.md** (~250 lines):
- Purpose: YouTube Data API v3 업로드 spec (Phase 8 실행)
- MUST CONTAIN body: "YouTube Data API v3", "AI disclosure", "48시간", "평일 20-23" 또는 "peak", "production_metadata"
- Selenium 금지 (AF-8) 명시
- publish lock: 48시간+ 랜덤 간격, 평일 20-23 / 주말 12-15 KST 윈도우
- MUST REMEMBER 5+ bullets: Selenium 금지 / AI disclosure 강제 ON / 48시간+ 랜덤 간격 / production_metadata 첨부 / 핀 댓글+end-screen funnel

All 5: frontmatter category=support. MUST REMEMBER at END with 창작 role + inspector_prompt 읽기 금지 + prior_vqqa 반영 + maxTurns=3 + rubric schema.
  </action>
  <verify>
    <automated>
py -3.11 -m scripts.validate.validate_all_agents --path .claude/agents/producers ; py -3.11 -c "import pathlib; v=pathlib.Path('.claude/agents/producers/voice-producer/AGENT.md').read_text('utf-8'); assert 'Typecast' in v and 'ElevenLabs' in v and 'AF-4' in v; emotions=['neutral','tense','sad','happy','urgent','mysterious','empathetic']; missing=[e for e in emotions if e not in v.lower()]; assert not missing, f'emotions missing: {missing}'; a=pathlib.Path('.claude/agents/producers/asset-sourcer/AGENT.md').read_text('utf-8'); whitelist=['Epidemic Sound','Artlist','YouTube Audio Library','Free Music Archive']; mw=[w for w in whitelist if w not in a]; assert not mw, f'whitelist missing: {mw}'; assert 'crossfade' in a.lower(); p=pathlib.Path('.claude/agents/producers/publisher/AGENT.md').read_text('utf-8'); assert 'YouTube Data API v3' in p and 'AI disclosure' in p and 'Selenium' in p"
    </automated>
  </verify>
  <done>
5 Producer Support AGENT.md files exist; voice-producer contains Typecast+ElevenLabs+7 emotions+AF-4; asset-sourcer contains 4-domain whitelist + crossfade; publisher contains YouTube Data API v3 + AI disclosure + Selenium(금지).
  </done>
</task>

<task type="auto">
  <name>Task 2: Write Supervisor AGENT.md (AGENT-05)</name>
  <files>
    .claude/agents/supervisor/shorts-supervisor/AGENT.md
  </files>
  <action>
Base on RESEARCH.md lines 686-748 (축약본). Expand to ~300 lines.

```yaml
  ---
name: shorts-supervisor
description: Producer/Inspector 오케스트레이션 에이전트 (17 inspector fan-out + rubric 합산 + 1-hop 재귀 방지). 트리거 키워드 shorts-supervisor, supervisor, 감독, 오케스트레이션, fan-out. Input: producer output + target REQ list + delegation_depth. Output: aggregated rubric (17 individual_verdicts + overall_verdict + routing). 1-hop fan-out only (재귀 금지, AGENT-05). maxTurns=3. ≤1024자.
version: 1.0
role: supervisor
category: supervisor
maxTurns: 3
  ---
```

Body sections:
- Purpose: AGENT-05 단일 1-hop 오케스트레이터 + 17 rubric verdict 합산 규칙
- Inputs: `--producer-output`, `--target-reqs`, `--delegation-depth` (default 0, 1+ 시 raise)
- Outputs: supervisor-rubric-schema.json compliant 합산 객체
- Prompt body:
  - **합산 규칙**: overall_verdict = FAIL if ANY individual.verdict == "FAIL" else "PASS"; routing: 모든 PASS → next_gate / 일부 FAIL + retry < 3 → retry (aggregated_vqqa 주입) / 일부 FAIL + retry >= 3 → circuit_breaker
  - **fan-out 구현**: 17 inspector를 카테고리별 병렬 호출 (structural 3 || content 3 || style 3 || compliance 3 || technical 3 || media 2)
  - **_delegation_depth guard pseudocode (AGENT-05):**
    ```python
    def fan_out_to_inspectors(producer_output, _delegation_depth=0):
        if _delegation_depth >= 1:
            raise DelegationDepthExceeded("Supervisor 1-hop only. Inspector가 다시 위임하지 않는다 (AGENT-05).")
        verdicts = []
        for ins_name in ALL_17_INSPECTORS:  # structural 3 + content 3 + style 3 + compliance 3 + technical 3 + media 2
            v = invoke_sub_agent(ins_name, producer_output, _delegation_depth=_delegation_depth + 1)
            verdicts.append(v)
        return aggregate_rubric(verdicts)
    ```
  - **17 inspector 리스트 열거** (모든 inspector name 명시적 나열 — harness-audit 감사 hook):
    ```
    structural: ins-blueprint-compliance, ins-timing-consistency, ins-schema-integrity
    content: ins-factcheck, ins-narrative-quality, ins-korean-naturalness
    style: ins-tone-brand, ins-readability, ins-thumbnail-hook
    compliance: ins-license, ins-platform-policy, ins-safety
    technical: ins-audio-quality, ins-render-integrity, ins-subtitle-alignment
    media: ins-mosaic, ins-gore
    ```

- References: 
  - `.claude/agents/_shared/supervisor-rubric-schema.json`
  - `.claude/agents/_shared/rubric-schema.json`
  - 17 inspector AGENT.md paths (relative)

- MUST REMEMBER at END (6 bullets):
  1. **재귀 금지 (AGENT-05)** — `_delegation_depth >= 1`이면 raise DelegationDepthExceeded.
  2. **Inspector간 대화 금지** — 각 inspector 독립 context (RUB-06 GAN). Supervisor가 결과만 합산.
  3. **창작 금지** — Supervisor는 "새 스크립트 제안" 불가. VQQA 통합만.
  4. **maxTurns = 3** — 17 inspector 병렬 호출 1턴 + 합산 1턴 + 응답 1턴.
  5. **17 inspector 전수 호출 의무** — 일부 skip 시 ins-schema-integrity가 보충 검증 (Phase 5 오케스트레이터 책임).
  6. **aggregated_vqqa는 원문 concat** — 요약 금지 (정보 손실).
  </action>
  <verify>
    <automated>
py -3.11 -m scripts.validate.validate_all_agents --path .claude/agents/supervisor ; py -3.11 -c "import pathlib; s=pathlib.Path('.claude/agents/supervisor/shorts-supervisor/AGENT.md').read_text('utf-8'); assert '_delegation_depth' in s and '>= 1' in s; assert 'fan-out' in s or 'fan_out' in s; inspectors=['ins-blueprint-compliance','ins-timing-consistency','ins-schema-integrity','ins-factcheck','ins-narrative-quality','ins-korean-naturalness','ins-tone-brand','ins-readability','ins-thumbnail-hook','ins-license','ins-platform-policy','ins-safety','ins-audio-quality','ins-render-integrity','ins-subtitle-alignment','ins-mosaic','ins-gore']; missing=[i for i in inspectors if i not in s]; assert not missing, f'Missing inspector refs: {missing}'"
    </automated>
  </verify>
  <done>
shorts-supervisor AGENT.md exists; contains _delegation_depth guard + 17 inspector names + fan-out pseudocode + supervisor-rubric-schema.json reference.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write Producer Support + Supervisor depth guard + maxTurns matrix tests</name>
  <files>
    tests/phase04/test_producer_support.py
    tests/phase04/test_supervisor_depth_guard.py
    tests/phase04/test_maxturns_matrix.py
  </files>
  <behavior>
    test_producer_support.py:
    - exactly 5 support producers (voice-producer, asset-sourcer, assembler, thumbnail-designer, publisher)
    - role=producer, category=support, maxTurns=3
    - voice-producer: Typecast + ElevenLabs + 7 emotions + AF-4
    - asset-sourcer: 4 whitelist domains + crossfade + 3-5s
    - publisher: YouTube Data API v3 + AI disclosure + Selenium(금지)

    test_supervisor_depth_guard.py (VALIDATION row 4-09-01):
    - Exactly 1 supervisor AGENT.md at .claude/agents/supervisor/shorts-supervisor/AGENT.md
    - role=supervisor, category=supervisor, maxTurns=3
    - Body contains "_delegation_depth" + ">= 1" + "raise" (or "raises")
    - Body contains all 17 inspector names
    - Body contains "fan-out" or "fan_out"
    - MUST REMEMBER at END with 재귀 금지

    test_maxturns_matrix.py (RUB-05 RESEARCH.md §8.5 lines 1434-1451):
    - Load all AGENT.md under .claude/agents/ (exclude harvest-importer)
    - For each, parse frontmatter.maxTurns
    - Expected matrix:
      - ins-factcheck = 10
      - ins-tone-brand = 5
      - ins-blueprint-compliance / ins-timing-consistency / ins-schema-integrity = 1
      - all others = 3
    - Assert 100% match
  </behavior>
  <action>
**test_producer_support.py:** follow earlier producer test patterns.

**test_supervisor_depth_guard.py:**
```python
import pathlib, re
import pytest
from scripts.validate.parse_frontmatter import parse_frontmatter

SUPERVISOR_PATH = pathlib.Path(".claude/agents/supervisor/shorts-supervisor/AGENT.md")

@pytest.fixture(scope="module")
def supervisor():
    meta, body = parse_frontmatter(SUPERVISOR_PATH)
    return meta, body

def test_supervisor_exists_and_unique(supervisor):
    all_sup = list(pathlib.Path(".claude/agents/supervisor").rglob("AGENT.md"))
    assert len(all_sup) == 1
    assert all_sup[0] == SUPERVISOR_PATH

def test_supervisor_frontmatter(supervisor):
    meta, _ = supervisor
    assert meta["role"] == "supervisor"
    assert meta["category"] == "supervisor"
    assert int(meta["maxTurns"]) == 3

def test_delegation_depth_guard(supervisor):
    _, body = supervisor
    assert "_delegation_depth" in body
    assert ">= 1" in body
    assert "raise" in body.lower()

def test_17_inspector_names_present(supervisor):
    _, body = supervisor
    inspectors = [
        "ins-blueprint-compliance", "ins-timing-consistency", "ins-schema-integrity",
        "ins-factcheck", "ins-narrative-quality", "ins-korean-naturalness",
        "ins-tone-brand", "ins-readability", "ins-thumbnail-hook",
        "ins-license", "ins-platform-policy", "ins-safety",
        "ins-audio-quality", "ins-render-integrity", "ins-subtitle-alignment",
        "ins-mosaic", "ins-gore",
    ]
    missing = [i for i in inspectors if i not in body]
    assert not missing, f"Missing inspector names in Supervisor: {missing}"

def test_fanout_mention(supervisor):
    _, body = supervisor
    assert "fan-out" in body or "fan_out" in body
```

**test_maxturns_matrix.py:**
```python
import pathlib, pytest
from scripts.validate.parse_frontmatter import parse_frontmatter

EXPECTED_MAXTURNS = {
    "ins-factcheck": 10,
    "ins-tone-brand": 5,
    "ins-blueprint-compliance": 1,
    "ins-timing-consistency": 1,
    "ins-schema-integrity": 1,
}
# Default for all others: 3

AGENTS_DIR = pathlib.Path(".claude/agents")

def agent_md_paths():
    paths = []
    for p in AGENTS_DIR.rglob("AGENT.md"):
        # Exclude harvest-importer (Phase 3 deprecated)
        if "harvest-importer" in str(p):
            continue
        paths.append(p)
    return paths

@pytest.mark.parametrize("md_path", agent_md_paths())
def test_maxturns_value(md_path):
    meta, _ = parse_frontmatter(md_path)
    name = meta["name"]
    expected = EXPECTED_MAXTURNS.get(name, 3)
    actual = int(meta.get("maxTurns", 3))
    assert actual == expected, f"{name}: maxTurns expected {expected}, got {actual}"
```
  </action>
  <verify>
    <automated>
py -3.11 -m pytest tests/phase04/test_producer_support.py tests/phase04/test_supervisor_depth_guard.py tests/phase04/test_maxturns_matrix.py -v --tb=short 2>&1 | tail -40
    </automated>
  </verify>
  <done>
All 3 pytest files run and all tests PASS. maxTurns matrix confirms: ins-factcheck=10, ins-tone-brand=5, 3 structural ins=1, all others=3 (no exceptions).
  </done>
</task>

</tasks>

<verification>
Wave 4 Producer Support + Supervisor complete when:
1. 5 Producer Support AGENT.md files + 1 Supervisor AGENT.md file
2. Total agents in .claude/agents/: 17 inspector + 14 producer + 1 supervisor = 32 (excluding harvest-importer)
3. shorts-supervisor has _delegation_depth guard + 17 inspector names + fan-out pseudocode
4. maxTurns matrix 100% compliant (RUB-05 enforced)
5. voice-producer AUDIO-01/03 fully specified
6. asset-sourcer AUDIO-02 fully specified
7. publisher PUB-01/02/03/04/05 stubs present
8. test_producer_support.py + test_supervisor_depth_guard.py + test_maxturns_matrix.py all PASS
</verification>

<success_criteria>
- [ ] 5 Producer Support AGENT.md (AGENT-03)
- [ ] 1 Supervisor AGENT.md (AGENT-05) with _delegation_depth guard
- [ ] 17 inspector names all referenced in Supervisor body
- [ ] maxTurns matrix: ins-factcheck=10, ins-tone-brand=5, ins-blueprint-compliance/timing-consistency/schema-integrity=1, all others=3
- [ ] voice-producer has Typecast + ElevenLabs + 7 emotions + AF-4
- [ ] asset-sourcer has 4-domain whitelist + 3-5s crossfade
- [ ] publisher has YouTube Data API v3 + AI disclosure + Selenium blocked
- [ ] 3 pytest files all PASS
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-team-design/04-09-SUMMARY.md`
</output>
