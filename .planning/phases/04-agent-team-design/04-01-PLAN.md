---
phase: 04-agent-team-design
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .claude/agents/_shared/rubric-schema.json
  - .claude/agents/_shared/supervisor-rubric-schema.json
  - .claude/agents/_shared/agent-template.md
  - .claude/agents/_shared/af_bank.json
  - .claude/agents/_shared/korean_speech_samples.json
  - .claude/agents/_shared/vqqa_corpus.md
  - scripts/validate/__init__.py
  - scripts/validate/parse_frontmatter.py
  - scripts/validate/rubric_stdlib_validator.py
  - scripts/validate/validate_all_agents.py
  - scripts/validate/harness_audit.py
  - tests/phase04/__init__.py
  - tests/phase04/conftest.py
  - tests/phase04/test_rubric_schema.py
  - tests/phase04/test_af_bank_shape.py
  - tests/phase04/test_korean_samples_shape.py
  - tests/phase04/fixtures/.gitkeep
autonomous: true
requirements:
  - RUB-04
  - AGENT-07
  - AGENT-08
  - AGENT-09
  - COMPLY-01
  - COMPLY-02
  - COMPLY-03
  - COMPLY-04
  - COMPLY-05
  - COMPLY-06
  - AUDIO-04
  - SUBT-02

must_haves:
  truths:
    - "rubric-schema.json exists and validates a sample inspector output via stdlib-only validator (jsonschema lib NOT required)"
    - "agent-template.md exists with MUST REMEMBER section at end and includes Producer + Inspector variants"
    - "af_bank.json contains ≥10 samples per class (AF-4 voice clone, AF-5 real face, AF-13 K-pop)"
    - "korean_speech_samples.json contains 10 positive (화법 정합) + 10 negative (혼용/호칭 누출/외래어 남용)"
    - "validate_all_agents.py + harness_audit.py run on stdlib only and enforce AGENT-07/08/09 rules"
    - "pytest tests/phase04/ -q exits 0 on all Wave 0 tests"
  artifacts:
    - path: ".claude/agents/_shared/rubric-schema.json"
      provides: "RUB-04 single source of truth for 17 inspector outputs"
      contains: "verdict enum PASS|FAIL, score 0-100, evidence[] with type enum regex|citation|heuristic, semantic_feedback"
    - path: ".claude/agents/_shared/supervisor-rubric-schema.json"
      provides: "Supervisor aggregated rubric schema (17-item individual_verdicts + routing enum)"
      contains: "overall_verdict, individual_verdicts array minItems=17 maxItems=17, routing enum next_gate|retry|circuit_breaker"
    - path: ".claude/agents/_shared/agent-template.md"
      provides: "Standard AGENT.md template with Producer + Inspector variants, MUST REMEMBER at end"
      min_lines: 120
    - path: ".claude/agents/_shared/af_bank.json"
      provides: "AF-4/5/13 positive + negative sample bank, ≥10 per class"
      contains: "af4_voice_clone array, af5_real_face array, af13_kpop array"
    - path: ".claude/agents/_shared/korean_speech_samples.json"
      provides: "존댓말/반말 화법 샘플 bank for ins-korean-naturalness regression"
      contains: "positive array 10+ (하오체/해요체 정합), negative array 10+ (혼용/호칭 누출/외래어 남용)"
    - path: ".claude/agents/_shared/vqqa_corpus.md"
      provides: "RUB-03 VQQA semantic_feedback 예시 코퍼스 (3-5 samples)"
      min_lines: 30
    - path: "scripts/validate/validate_all_agents.py"
      provides: "AGENT-07 (≤500 lines) + AGENT-08 (description ≤1024 chars + 트리거 키워드 ≥3) + AGENT-09 (MUST REMEMBER 끝 배치) enforcement"
      contains: "check_line_count, check_description_chars, check_must_remember_position"
    - path: "scripts/validate/rubric_stdlib_validator.py"
      provides: "stdlib-only JSON Schema validator for rubric-schema.json (no jsonschema lib dependency)"
      contains: "validate_rubric function"
    - path: "tests/phase04/conftest.py"
      provides: "pytest fixtures (AGENT.md loader, frontmatter parser, rubric validator)"
      min_lines: 40
  key_links:
    - from: "scripts/validate/validate_all_agents.py"
      to: ".claude/agents/_shared/rubric-schema.json"
      via: "json.load(Path) + validate_rubric call"
      pattern: "rubric-schema\\.json"
    - from: "tests/phase04/conftest.py"
      to: "scripts/validate/parse_frontmatter.py"
      via: "fixture import"
      pattern: "from scripts\\.validate"
    - from: ".claude/agents/_shared/agent-template.md"
      to: "AGENT-07/08/09 rules"
      via: "template body structure — frontmatter + 8 sections + MUST REMEMBER last"
      pattern: "## MUST REMEMBER"
---

<objective>
Wave 0 shared foundation. Every downstream Wave (1-5) depends on these 6 shared artifacts. Without this foundation:
- No rubric schema → 17 inspectors have no common output contract (RUB-04 violation)
- No agent template → 32 AGENT.md files diverge in structure (AGENT-07/08/09 unenforceable)
- No AF bank / korean samples → COMPLY / SUBT regression tests cannot run
- No validator scripts → AGENT-07/08/09 regressions silently slip through

Purpose: Install the single source of truth for rubric structure + agent design standards + regression fixtures + stdlib validators. This is the contract layer — every Wave 1-5 plan consumes these files.

Output:
- 2 JSON schemas (rubric + supervisor-rubric, draft-07)
- 1 agent template (Markdown with Producer/Inspector variants)
- 2 JSON sample banks (af_bank + korean_speech_samples)
- 1 VQQA corpus (Markdown)
- 4 Python validator scripts (stdlib only)
- 1 pytest conftest + 3 Wave 0 tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-agent-team-design/04-CONTEXT.md
@.planning/phases/04-agent-team-design/04-RESEARCH.md
@.planning/phases/04-agent-team-design/04-VALIDATION.md
@.claude/agents/harvest-importer/AGENT.md

<interfaces>
<!-- Key references downstream plans will reuse. Extracted from 04-RESEARCH.md. -->

RESEARCH.md §7 (lines 1200-1313): rubric-schema.json full copy-paste ready, draft-07 compliant
RESEARCH.md §8.1 (lines 1321-1340): parse_frontmatter stdlib helper
RESEARCH.md §8.2 (lines 768-806): validate_rubric stdlib-only function (Python 3.11.9 live-tested)
RESEARCH.md §8.3 (lines 1349-1393): check_line_count + check_description_chars + check_must_remember_position
RESEARCH.md §5.3 (lines 1126-1142): Korean speech regex bank (하오체/해요체/반말/호칭 누출)
RESEARCH.md §5.5 (lines 1161-1164): AF-13 K-pop regex bank (19 아티스트 + 곡명 bank)
RESEARCH.md §3.1-§3.10: AGENT.md template section structure
harvest-importer AGENT.md: 107 lines reference format with frontmatter + 8 sections + MUST REMEMBER at end

Phase 3 path_manifest.json pattern (harvested/{raw_dir_name}/): reference for _shared/ layout
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 2 rubric JSON schemas + agent template + VQQA corpus</name>
  <files>
    .claude/agents/_shared/rubric-schema.json
    .claude/agents/_shared/supervisor-rubric-schema.json
    .claude/agents/_shared/agent-template.md
    .claude/agents/_shared/vqqa_corpus.md
  </files>
  <action>
Create 4 shared AGENT foundation files under `.claude/agents/_shared/`.

**File 1: rubric-schema.json** — copy verbatim from RESEARCH.md §7 lines 1202-1289. Ensure `additionalProperties: false`, `required: ["verdict", "score", "evidence", "semantic_feedback"]`, verdict enum `["PASS", "FAIL"]`, score integer 0-100, evidence array of objects with type enum `["regex", "citation", "heuristic"]` + detail minLength 1 maxLength 2000, optional fields: location, severity enum `["info","warning","critical"]`, inspector_name pattern `^ins-[a-z0-9-]+$`, logicqa_sub_verdicts array minItems 5 maxItems 5 with q_id pattern `^q[1-5]$` + result enum Y|N + reason maxLength 500, maxTurns_used integer minimum 0. Addresses RUB-04.

**File 2: supervisor-rubric-schema.json** — copy verbatim from RESEARCH.md §7 lines 1293-1313. Fields: overall_verdict enum PASS|FAIL, individual_verdicts array minItems 17 maxItems 17 with `$ref: "./rubric-schema.json"`, aggregated_vqqa string maxLength 20000, routing enum `["next_gate", "retry", "circuit_breaker"]`, delegation_depth integer 0-1, retry_count integer 0-3. Addresses AGENT-05.

**File 3: agent-template.md** — Based on RESEARCH.md §3.1-§3.3 and harvest-importer AGENT.md. Structure:
```
  ---
name: <agent-slug>
description: <≤1024자 트리거 키워드 3+ + 한 줄 역할 + Input/Output + 제약>
version: 1.0
role: producer | inspector | supervisor
category: core | split3 | support | structural | content | style | compliance | technical | media | supervisor
maxTurns: 3
  ---

# <agent-slug>
<role summary paragraph>
## Purpose
## Inputs
## Outputs
## Prompt
  ### (Inspector only) LogicQA block — <main_q>...</main_q><sub_qs>q1-q5</sub_qs>
  ### (Inspector only) VQQA feedback block
  ### (Producer only) prior_vqqa reflection block
  ### (Producer only) 채널바이블 인라인 주입 (CONTENT-03)
## References
## MUST REMEMBER (DO NOT VIOLATE) — LAST SECTION (AGENT-09)
```
Include 2 variants: `### Producer variant` + `### Inspector variant` side-by-side with explicit RUB-06 note ("Inspector Inputs MUST NOT include producer_prompt / producer_system_context"). MUST REMEMBER bullet examples: 창작 금지 (RUB-02), producer_prompt 읽기 금지 (RUB-06), LogicQA 다수결 의무 (RUB-01), maxTurns 준수 (RUB-05), rubric schema 준수 (RUB-04), Supervisor 재호출 금지 (AGENT-05).

**File 4: vqqa_corpus.md** — Copy RESEARCH.md §3.8 lines 1070-1078 (5 VQQA examples) + format note. Addresses RUB-03.

Rationale: RESEARCH.md §3.1-§3.10 defines the exact template; §7 is live-verified schema. All paths use forward slashes for cross-platform compat.
  </action>
  <verify>
    <automated>
python -c "import json, pathlib; [json.loads(pathlib.Path(p).read_text('utf-8')) for p in ['.claude/agents/_shared/rubric-schema.json', '.claude/agents/_shared/supervisor-rubric-schema.json']]; assert pathlib.Path('.claude/agents/_shared/agent-template.md').read_text('utf-8').count('## MUST REMEMBER') >= 1; assert pathlib.Path('.claude/agents/_shared/vqqa_corpus.md').exists(); print('OK')"
    </automated>
  </verify>
  <done>
rubric-schema.json parses as valid JSON with correct draft-07 structure; supervisor-rubric-schema.json parses similarly; agent-template.md has MUST REMEMBER header (at end), Producer + Inspector variants, and explicit RUB-06 GAN note; vqqa_corpus.md has ≥3 examples.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 2 sample JSON banks (af_bank.json + korean_speech_samples.json)</name>
  <files>
    .claude/agents/_shared/af_bank.json
    .claude/agents/_shared/korean_speech_samples.json
  </files>
  <action>
**File 1: af_bank.json** — AF-4/5/13 sample bank. Structure:
```json
{
  "af4_voice_clone": [
    {"id": "af4-001", "name": "BTS 지민", "expected_verdict": "FAIL", "reason": "실존 가수 voice cloning"},
    {"id": "af4-002", "name": "손흥민", "expected_verdict": "FAIL", "reason": "실존 인물"},
    ... 10+ 총
  ],
  "af5_real_face": [
    {"id": "af5-001", "url_pattern": "press-photo.com/victim_2020", "expected_verdict": "FAIL", "reason": "실존 피해자 press photo"},
    ... 10+ 총
  ],
  "af13_kpop": [
    {"id": "af13-001", "title": "Dynamite", "artist": "BTS", "expected_verdict": "FAIL", "reason": "K-pop 직접 사용 KOMCA strike"},
    {"id": "af13-002", "title": "Butter", "artist": "BTS", "expected_verdict": "FAIL"},
    {"id": "af13-003", "title": "How Sweet", "artist": "NewJeans", "expected_verdict": "FAIL"},
    ... 10+ 총
  ]
}
```

MUST CONTAIN ≥10 entries per class. AF-13 artists MUST include ≥5 from RESEARCH.md §5.5 line 1162-1163 regex bank (BTS / 방탄소년단 / 블랙핑크 / BLACKPINK / 뉴진스 / NewJeans / 아이브 / IVE / 에스파 / aespa / 르세라핌 / LE SSERAFIM / 스트레이 키즈 / Stray Kids / 세븐틴 / SEVENTEEN / NCT / 트와이스 / TWICE). Addresses COMPLY-04/05, AUDIO-04.

**File 2: korean_speech_samples.json** — 존댓말/반말 regression bank. Structure:
```json
{
  "positive": [
    {"id": "kor-pos-01", "speaker": "detective", "register": "하오체", "text": "그 사건을 알고 있소.", "expected_verdict": "PASS"},
    {"id": "kor-pos-02", "speaker": "assistant", "register": "해요체", "text": "저도 들었어요.", "expected_verdict": "PASS"},
    ... 10 total
  ],
  "negative": [
    {"id": "kor-neg-01", "speaker": "detective", "register": "하오체", "text": "그 사건을 알아요.", "expected_verdict": "FAIL", "reason": "해요체 '알아요' 탐정 발화에 혼입"},
    {"id": "kor-neg-02", "speaker": "detective", "text": "탐정님이 먼저 확인했소.", "expected_verdict": "FAIL", "reason": "호칭 누출 (자신을 탐정님으로 지칭)"},
    {"id": "kor-neg-03", "speaker": "assistant", "register": "해요체", "text": "아 그거 알지.", "expected_verdict": "FAIL", "reason": "반말 '알지' 조수 발화에 혼입"},
    ... 10 total (혼용 4 + 호칭 누출 3 + 외래어 남용 3)
  ]
}
```

MUST CONTAIN exactly 10 positive + 10 negative. Addresses SUBT-02, CONTENT-02.
  </action>
  <verify>
    <automated>
python -c "import json, pathlib; af=json.loads(pathlib.Path('.claude/agents/_shared/af_bank.json').read_text('utf-8')); ks=json.loads(pathlib.Path('.claude/agents/_shared/korean_speech_samples.json').read_text('utf-8')); assert len(af['af4_voice_clone'])>=10 and len(af['af5_real_face'])>=10 and len(af['af13_kpop'])>=10; assert len(ks['positive'])>=10 and len(ks['negative'])>=10; print('OK')"
    </automated>
  </verify>
  <done>
Both JSON files parse; af_bank has ≥10 entries in each of 3 classes; korean_speech_samples has ≥10 positive + ≥10 negative with speaker/register/text/expected_verdict fields.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write 4 stdlib validator scripts + pytest conftest + 3 Wave 0 tests</name>
  <files>
    scripts/validate/__init__.py
    scripts/validate/parse_frontmatter.py
    scripts/validate/rubric_stdlib_validator.py
    scripts/validate/validate_all_agents.py
    scripts/validate/harness_audit.py
    tests/phase04/__init__.py
    tests/phase04/conftest.py
    tests/phase04/test_rubric_schema.py
    tests/phase04/test_af_bank_shape.py
    tests/phase04/test_korean_samples_shape.py
    tests/phase04/fixtures/.gitkeep
  </files>
  <behavior>
    - Test 1 (test_rubric_schema.py): a canned valid rubric dict passes validate_rubric; 5 canned invalid rubrics each return exactly 1 specific error (missing required field / wrong type / enum violation / score>100 / evidence not array)
    - Test 2 (test_af_bank_shape.py): af_bank.json keys are exactly {af4_voice_clone, af5_real_face, af13_kpop}; each value is list with len≥10; every entry has "id" + "expected_verdict" keys
    - Test 3 (test_korean_samples_shape.py): positive array all entries have expected_verdict=="PASS"; negative array all entries have expected_verdict=="FAIL" + "reason" field; exactly 10 positive + ≥10 negative
    - validate_all_agents.py on `.claude/agents/harvest-importer/AGENT.md` (only existing agent): passes line_count (107≤500), description (~378 chars ≤1024), but MUST REMEMBER position check MUST be relaxed for harvest-importer specifically (Invariants section serves as MUST REMEMBER, at line 45 of 107 = 58% from end — see harvest-importer AGENT.md) OR skip harvest-importer via `--exclude harvest-importer` flag
  </behavior>
  <action>
**File 1: scripts/validate/__init__.py** — empty file (package marker).

**File 2: scripts/validate/parse_frontmatter.py** — Copy RESEARCH.md §8.1 lines 1321-1340 verbatim. Stdlib only (pathlib). Returns `tuple[dict, str]` = (meta, body). Raises ValueError on missing or unclosed frontmatter.

**File 3: scripts/validate/rubric_stdlib_validator.py** — Copy RESEARCH.md §8.2 lines 770-806 verbatim `validate_rubric(doc, schema)` function returning `list[str]` errors. Handles: type object (required + properties), type string (with enum), type integer (with minimum/maximum), type array, additionalProperties false. NO external jsonschema dependency.

**File 4: scripts/validate/validate_all_agents.py** — Based on RESEARCH.md §8.3 lines 1349-1393. Functions: `check_line_count(md_path, limit=500)`, `check_description_chars(meta, limit=1024)`, `check_must_remember_position(body)` — returns (line_no, ratio_from_end). CLI: `python -m scripts.validate.validate_all_agents [--path DIR] [--exclude AGENT_NAME] [--strict]`. MUST REMEMBER position threshold: header must appear in final 40% of body (ratio_from_end ≥ 0.0 and ≤ 0.4). Exit non-zero if any violation. Produce summary `OK: {N} agents validated` on success.

**File 5: scripts/validate/harness_audit.py** — Wrapper that runs validate_all_agents + loads rubric-schema.json + runs validate_rubric against a sample inspector output + greps `.claude/agents/inspectors/` for forbidden `producer_prompt|producer_system_context` strings (RUB-06). Scoring: 100 points base, -10 per file violation, -5 per warning. Print `HARNESS_AUDIT_SCORE: {N}` and exit 0 if ≥80 else 1. This script is called by Plan 10 Wave 5.

**File 6: tests/phase04/__init__.py** — empty.

**File 7: tests/phase04/conftest.py** — pytest fixtures:
```python
import json, pathlib, pytest
from scripts.validate.parse_frontmatter import parse_frontmatter
from scripts.validate.rubric_stdlib_validator import validate_rubric

@pytest.fixture(scope="session")
def rubric_schema():
    return json.loads(pathlib.Path(".claude/agents/_shared/rubric-schema.json").read_text("utf-8"))

@pytest.fixture(scope="session")
def af_bank():
    return json.loads(pathlib.Path(".claude/agents/_shared/af_bank.json").read_text("utf-8"))

@pytest.fixture(scope="session")
def korean_samples():
    return json.loads(pathlib.Path(".claude/agents/_shared/korean_speech_samples.json").read_text("utf-8"))

@pytest.fixture
def agent_md_loader():
    def _load(path: str):
        return parse_frontmatter(pathlib.Path(path))
    return _load
```

**File 8: tests/phase04/test_rubric_schema.py** — TDD: write 6 tests FIRST.
- `test_valid_rubric_passes(rubric_schema)` — valid dict `{verdict: "PASS", score: 85, evidence: [], semantic_feedback: ""}` → `validate_rubric` returns `[]`
- `test_missing_required_field(rubric_schema)` — drop "verdict" → error contains "missing required field: verdict"
- `test_invalid_verdict_enum(rubric_schema)` — `verdict: "MAYBE"` → error contains "verdict: value 'MAYBE' not in enum"
- `test_score_above_max(rubric_schema)` — `score: 150` → error contains "above maximum"
- `test_score_below_min(rubric_schema)` — `score: -1` → error contains "below minimum"
- `test_evidence_wrong_type(rubric_schema)` — `evidence: "string"` → error contains "evidence: expected array"

**File 9: tests/phase04/test_af_bank_shape.py** — TDD:
- `test_af_bank_has_3_classes(af_bank)` — keys exactly == {"af4_voice_clone", "af5_real_face", "af13_kpop"}
- `test_each_class_has_10_plus(af_bank)` — each value list len ≥ 10
- `test_entries_have_required_fields(af_bank)` — every entry dict has keys {"id", "expected_verdict"}
- `test_af13_includes_core_kpop(af_bank)` — af13 artists include ≥ 5 of BTS/BLACKPINK/NewJeans/IVE/aespa/LE SSERAFIM/Stray Kids/SEVENTEEN/NCT/TWICE

**File 10: tests/phase04/test_korean_samples_shape.py** — TDD:
- `test_positive_10_all_pass(korean_samples)` — positive len >= 10, all expected_verdict=="PASS"
- `test_negative_10_all_fail(korean_samples)` — negative len >= 10, all expected_verdict=="FAIL"
- `test_negative_has_reason(korean_samples)` — every negative entry has non-empty "reason"

**File 11: tests/phase04/fixtures/.gitkeep** — empty.

RED: All tests fail on import (modules don't exist yet).
GREEN: Implement validators + sample banks → tests pass.
REFACTOR: Only if clear simplification emerges.
  </action>
  <verify>
    <automated>
py -3.11 -m pytest tests/phase04/ -q --tb=short 2>&1 | tail -10 ; py -3.11 -m scripts.validate.validate_all_agents --path .claude/agents --exclude harvest-importer 2>&1 ; py -3.11 -c "from scripts.validate.rubric_stdlib_validator import validate_rubric; import json, pathlib; schema=json.loads(pathlib.Path('.claude/agents/_shared/rubric-schema.json').read_text('utf-8')); errs=validate_rubric({'verdict':'PASS','score':85,'evidence':[],'semantic_feedback':''}, schema); assert errs==[], f'UNEXPECTED_ERRS: {errs}'; print('VALIDATOR_OK')"
    </automated>
  </verify>
  <done>
pytest tests/phase04/ exits 0 (at least 13 tests PASS: 6 rubric + 4 af_bank + 3 korean_samples); validate_all_agents.py runs without violations on .claude/agents (excluding harvest-importer); rubric_stdlib_validator.py validates sample rubric with no errors.
  </done>
</task>

</tasks>

<verification>
Wave 0 foundation is complete when:
1. `.claude/agents/_shared/` contains 6 files (2 JSON schemas + agent-template.md + vqqa_corpus.md + 2 sample banks)
2. `scripts/validate/` contains 5 Python modules (all stdlib only)
3. `tests/phase04/` contains pytest config + 3 test files + 13+ tests passing
4. `py -3.11 -m pytest tests/phase04/ -q` exits 0
5. `py -3.11 -m scripts.validate.validate_all_agents --path .claude/agents --exclude harvest-importer` exits 0 with "OK" summary
6. No jsonschema external dependency needed (stdlib validator works)
</verification>

<success_criteria>
- [ ] 6 shared foundation files exist in .claude/agents/_shared/
- [ ] 5 Python stdlib validator scripts in scripts/validate/
- [ ] pytest tests/phase04/ exits 0 with 13+ tests passing
- [ ] rubric-schema.json parses valid JSON draft-07
- [ ] af_bank.json has ≥10 entries per class (AF-4/5/13)
- [ ] korean_speech_samples.json has 10+ positive + 10+ negative with required fields
- [ ] agent-template.md has MUST REMEMBER at end + Producer/Inspector variants + RUB-06 note
- [ ] validate_all_agents.py enforces AGENT-07/08/09 rules (≤500 lines / ≤1024 chars desc / MUST REMEMBER position)
- [ ] harness_audit.py emits HARNESS_AUDIT_SCORE and respects 80 threshold
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-team-design/04-01-SUMMARY.md`
</output>
