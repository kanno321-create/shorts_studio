---
phase: 03-harvest
plan: 03
type: execute
wave: 1
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - studios/shorts/.preserved/harvested/theme_bible_raw/
autonomous: true
requirements:
  - HARVEST-01
gap_closure: false

must_haves:
  truths:
    - ".preserved/harvested/theme_bible_raw/ 디렉토리가 존재한다"
    - "원본 .claude/channel_bibles/*.md 파일 수(7개)와 복사본 파일 수가 일치한다"
    - "filecmp.dircmp deep diff 결과 mismatches == []"
    - "secret file (client_secret*.json, token_*.json)이 복사본에 존재하지 않는다"
  artifacts:
    - path: "studios/shorts/.preserved/harvested/theme_bible_raw/"
      provides: "shorts_naberal channel_bibles 전체 복사본 (HARVEST-01)"
      contains: "README.md + 6 niche bibles (documentary, humor, incidents, politics, trend, wildlife)"
  key_links:
    - from: "theme_bible_raw/"
      to: "shorts_naberal/.claude/channel_bibles/"
      via: "shutil.copytree via harvest_importer.py stage copy"
      pattern: "filecmp.dircmp"
---

<objective>
4 개의 병렬 복사 작업 중 첫 번째 — `shorts_naberal/.claude/channel_bibles/` 를 `.preserved/harvested/theme_bible_raw/` 로 무필터 복사 (HARVEST-01). Phase 4 에이전트가 채널 바이블을 레퍼런스로 사용할 수 있도록 작동 검증된 자산을 읽기 전용 복사본으로 보존한다.

Purpose:
- HARVEST-01 요구사항 충족 (theme-bible 전체 복사)
- 7 채널 바이블 파일 (README + 6 niche) 물리 이관
- Wave 1 병렬 실행의 한 축 (다른 3개와 파일 충돌 없음)

Output:
- `.preserved/harvested/theme_bible_raw/` 디렉토리 + 7 .md 파일
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@studios/shorts/.planning/phases/03-harvest/path_manifest.json
@studios/shorts/.planning/phases/03-harvest/03-RESEARCH.md
@studios/shorts/.planning/phases/03-harvest/03-VALIDATION.md
@studios/shorts/.planning/phases/02-domain-definition/02-HARVEST_SCOPE.md
@studios/shorts/scripts/harvest/harvest_importer.py
@studios/shorts/scripts/harvest/diff_verifier.py

<interfaces>
<!-- From Plan 02 path_manifest.json -->

manifest["theme_bible_raw"] = {
  "source": ".claude/channel_bibles",   // relative to source_root
  "cherry_pick": null,
  "dest": ".preserved/harvested/theme_bible_raw",
  "ignore": [],
  "req_id": "HARVEST-01"
}
manifest["source_root"] = "C:/Users/PC/Desktop/shorts_naberal"
manifest["global_ignore"] = ["client_secret*.json", "token_*.json", ".env*", "*.key", "*.pem", "__pycache__", "*.pyc"]

<!-- Source content (verified 2026-04-19) -->
shorts_naberal/.claude/channel_bibles/
  ├── README.md
  ├── documentary.md
  ├── humor.md
  ├── incidents.md
  ├── politics.md
  ├── trend.md
  └── wildlife.md
(7 files, all .md, no subdirectories)

<!-- harvest_importer.py CLI (from Plan 01) -->
python scripts/harvest/harvest_importer.py \
  --source <source_root> \
  --manifest .planning/phases/03-harvest/path_manifest.json \
  --stage copy --name theme_bible_raw \
  --audit-log scripts/harvest/audit_log.md
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Execute theme_bible_raw copy via harvest_importer</name>
  <read_first>
    - studios/shorts/.planning/phases/03-harvest/path_manifest.json (theme_bible_raw entry + global_ignore)
    - studios/shorts/.planning/phases/03-harvest/03-RESEARCH.md §3 Copy Strategy (shutil.copytree with ignore_patterns)
    - studios/shorts/.planning/phases/03-harvest/03-RESEARCH.md § Edge Cases E2 (pre-existing destination handling)
    - studios/shorts/scripts/harvest/harvest_importer.py (to understand the --stage copy contract)
  </read_first>
  <files>
    - studios/shorts/.preserved/harvested/theme_bible_raw/ (CREATE dir + 7 files via harvest_importer)
  </files>
  <behavior>
    Invoke harvest_importer.py in copy-only mode (no lockdown, no decisions) targeting theme_bible_raw specifically.

    Pre-condition: if `.preserved/harvested/theme_bible_raw/` already exists, rmtree it first (E2 edge case). Lockdown has not been applied yet (Wave 4), so rmtree is safe.

    Post-condition: directory exists with exactly the same file set as source, byte-identical content.

    Ignore patterns applied (merged from manifest):
    - Per-entry ignore: [] (channel_bibles has no subdirs to skip)
    - global_ignore: client_secret*.json, token_*.json, .env*, *.key, *.pem, __pycache__, *.pyc

    (Ignore list is defensive — channel_bibles/ contains only .md files, but the pattern set is applied uniformly across all 4 raw dirs.)
  </behavior>
  <action>
    From `studios/shorts/` working directory, run:

    ```bash

    # Safety: remove any pre-existing partial copy (lockdown not yet applied)
    python -c "import shutil; from pathlib import Path; p=Path('C:/Users/PC/Desktop/naberal_group/studios/shorts/.preserved/harvested/theme_bible_raw'); shutil.rmtree(p) if p.exists() else None; print('pre-clean done')"

    # Invoke harvest_importer.py stage=copy, name=theme_bible_raw
    python C:/Users/PC/Desktop/naberal_group/studios/shorts/scripts/harvest/harvest_importer.py \
      --source "C:/Users/PC/Desktop/shorts_naberal" \
      --manifest C:/Users/PC/Desktop/naberal_group/studios/shorts/.planning/phases/03-harvest/path_manifest.json \
      --stage copy \
      --name theme_bible_raw \
      --audit-log C:/Users/PC/Desktop/naberal_group/studios/shorts/scripts/harvest/audit_log.md

    # Expected audit_log entry: [STAGE_COPY_theme_bible_raw_OK] N files copied
    ```

    If `--stage copy` CLI is not yet wired in harvest_importer.py (Plan 01 deliverable), use this fallback Python one-liner implementing the exact same logic:

    ```bash
    python <<'PY'
    import json, shutil
    from pathlib import Path
    m = json.load(open('C:/Users/PC/Desktop/naberal_group/studios/shorts/.planning/phases/03-harvest/path_manifest.json', encoding='utf-8'))
    entry = m['theme_bible_raw']
    src = Path(m['source_root']) / entry['source']
    dst = Path('C:/Users/PC/Desktop/naberal_group/studios/shorts') / entry['dest']
    if dst.exists():
        shutil.rmtree(dst)
    ignore = shutil.ignore_patterns(*(entry['ignore'] + m['global_ignore']))
    shutil.copytree(src, dst, ignore=ignore, symlinks=False, dirs_exist_ok=False)
    count = sum(1 for _ in dst.rglob('*') if _.is_file())
    print(f"[OK] theme_bible_raw: {count} files copied")
    PY
    ```

    After copy, immediately run a spot check:
    ```bash
    ls C:/Users/PC/Desktop/naberal_group/studios/shorts/.preserved/harvested/theme_bible_raw/*.md | wc -l
    # Expected: 7
    ```
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Desktop/naberal_group/studios/shorts" &amp;&amp; test -d .preserved/harvested/theme_bible_raw &amp;&amp; [ $(ls .preserved/harvested/theme_bible_raw/*.md 2>/dev/null | wc -l) -ge 7 ] &amp;&amp; test -f .preserved/harvested/theme_bible_raw/README.md &amp;&amp; test -f .preserved/harvested/theme_bible_raw/documentary.md &amp;&amp; test -f .preserved/harvested/theme_bible_raw/incidents.md &amp;&amp; python scripts/harvest/diff_verifier.py theme_bible_raw &amp;&amp; ! ls .preserved/harvested/theme_bible_raw/client_secret*.json 2&gt;/dev/null &amp;&amp; ! ls .preserved/harvested/theme_bible_raw/token_*.json 2&gt;/dev/null &amp;&amp; echo "theme_bible_raw verified"</automated>
  </verify>
  <acceptance_criteria>
    - `test -d .preserved/harvested/theme_bible_raw` exits 0
    - `ls .preserved/harvested/theme_bible_raw/*.md | wc -l` ≥ 7 (matches source)
    - All 7 expected files present: README.md, documentary.md, humor.md, incidents.md, politics.md, trend.md, wildlife.md
    - `python scripts/harvest/diff_verifier.py theme_bible_raw` exits 0 (mismatches list empty)
    - No `client_secret*.json` or `token_*.json` files present in destination
    - File content is byte-identical to source (filecmp.cmp succeeds on every file)
    - `scripts/harvest/audit_log.md` contains `[STAGE_COPY_theme_bible_raw_OK]` line (if Plan 01 CLI wired)
  </acceptance_criteria>
  <done>
    - theme_bible_raw/ directory populated with 7 .md files matching source
    - Diff verify passes (empty mismatch list)
    - No secret file leakage
    - Wave 2 can proceed to diff verification aggregation
  </done>
</task>

</tasks>

<verification>
```bash
cd "C:/Users/PC/Desktop/naberal_group/studios/shorts"
ls .preserved/harvested/theme_bible_raw/
python scripts/harvest/diff_verifier.py theme_bible_raw
```
Expected: 7 files listed, diff_verifier exits 0.
</verification>

<success_criteria>
- HARVEST-01 satisfied: theme_bible_raw/ populated with 7 .md files byte-identical to source
- diff_verifier returns mismatches=[]
- No secret file leakage
</success_criteria>

<output>
After completion, create `.planning/phases/03-harvest/03-03-SUMMARY.md` with:
- File count copied (7 expected)
- diff_verifier exit code (0 expected)
- Total bytes copied (optional)
- Any ignored files (should be empty for this entry)
</output>
