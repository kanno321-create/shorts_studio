---
phase: 03-harvest
plan: 08
type: execute
wave: 3
depends_on:
  - "03-07"
files_modified:
  - studios/shorts/.planning/phases/03-harvest/03-HARVEST_DECISIONS.md
  - studios/shorts/scripts/harvest/audit_log.md
autonomous: true
requirements:
  - HARVEST-07
  - HARVEST-08
gap_closure: false

must_haves:
  truths:
    - "03-HARVEST_DECISIONS.md 파일이 존재하고 정확히 39 data rows (A:13 + B:16 + C:10)를 포함한다"
    - "A-1~A-13 rows은 02-HARVEST_SCOPE.md § A급 13건 판정 테이블에서 verbatim으로 복사되었다"
    - "B-1~B-16 + C-1~C-10 rows은 5-rule algorithm (02-HARVEST_SCOPE.md § B/C 위임 알고리즘)을 적용한 결과다"
    - "각 B/C row의 '근거' 컬럼에는 적용된 rule 번호가 명시되어 있다 (예: 'rule 2: scope boundary longform/')"
    - "grep -r 'skip_gates=True' .preserved/harvested/ 결과 0건"
    - "grep -r 'TODO(next-session)' .preserved/harvested/ 결과 0건"
    - ".preserved/harvested/ 내부에 orchestrate.py, create-video/, longform/ 경로 파일이 존재하지 않는다"
  artifacts:
    - path: "studios/shorts/.planning/phases/03-harvest/03-HARVEST_DECISIONS.md"
      provides: "CONFLICT_MAP 39건 전수 판정 결과 (HARVEST-08)"
      expected_rows: 39
      format: "5-column table matching 02-HARVEST_SCOPE.md § A"
  key_links:
    - from: "03-HARVEST_DECISIONS.md"
      to: "02-HARVEST_SCOPE.md § A급 13건"
      via: "verbatim copy of 13 rows + algorithm-generated 26 rows"
      pattern: "^\\| A-(1[0-3]|[1-9]) \\|"
    - from: "Blacklist grep audit"
      to: ".preserved/harvested/**"
      via: "grep -rE pattern"
      pattern: "skip_gates=True|TODO\\(next-session\\)|selenium"
---

<objective>
Wave 3 — 2개 task:
1. HARVEST-08 — CONFLICT_MAP 39건 전수 판정 문서 생성 (A급 13 verbatim + B/C급 26 5-rule 알고리즘)
2. HARVEST-07 — Blacklist grep audit (skip_gates, TODO(next-session), selenium, orchestrate.py, create-video/, longform/)

Purpose:
- HARVEST-07 + HARVEST-08 요구사항 충족
- Phase 4 Agent Team Design이 "무엇을 승계/폐기" 질문 없이 진입 가능
- 10 blacklist entries 전수 준수 증명

Output:
- `03-HARVEST_DECISIONS.md` (39 data rows, 5-column table)
- audit_log.md 업데이트 (blacklist 감사 결과)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@studios/shorts/.planning/phases/02-domain-definition/02-HARVEST_SCOPE.md
@studios/shorts/.planning/phases/03-harvest/03-RESEARCH.md
@studios/shorts/.planning/phases/03-harvest/03-VALIDATION.md
@studios/shorts/scripts/harvest/conflict_parser.py
@studios/shorts/scripts/harvest/decision_builder.py
@studios/shorts/scripts/harvest/blacklist_parser.py

<interfaces>
<!-- CONFLICT_MAP source (verified) -->
C:/Users/PC/Desktop/shorts_naberal/.planning/codebase/CONFLICT_MAP.md
  - Heading regex: ^### ([ABC])-(\d+)\.\s+(.*)$
  - Counts: A=13, B=16, C=10, total=39

<!-- A급 13 verbatim source (from 02-HARVEST_SCOPE.md § A급 13건 사전 판정) -->
5-column table header: "| ID | 드리프트 요약 | 판정 | 근거 | 실행 지시 |"
13 rows to copy verbatim: A-1 through A-13
  - A-2, A-9 → 승계
  - A-5, A-6, A-11 → 폐기
  - A-1, A-3, A-4, A-7, A-8, A-10, A-12, A-13 → 통합-재작성

<!-- 5-rule algorithm (from 02-HARVEST_SCOPE.md § B/C 위임 알고리즘) -->
1. Harvest blacklist match → 폐기 (근거: "rule 1: blacklist match <pattern>")
2. Scope boundary (longform/ / incidents_jp / worktree / duo_japan) → 폐기 (근거: "rule 2: scope boundary <token>")
3. Session 77+ canonical OR "17일 uncommitted" match → 승계 신형 (근거: "rule 3: session 77+ canonical")
4. C-class + (gitignore_missing / worktree_copy / tmp_committed) → cleanup (근거: "rule 4: cosmetic cleanup")
5. Default → 통합-재작성 (근거: "rule 5: default rewrite")

<!-- Blacklist grep audit (from 03-VALIDATION.md rows 3-08-01, 3-08-02) -->
Command 1: grep -r "skip_gates=True" .preserved/harvested/ | wc -l     → expected 0
Command 2: grep -r "TODO(next-session)" .preserved/harvested/ | wc -l  → expected 0
Command 3: find .preserved/harvested -name "orchestrate.py" -o -name "SKILL.md" -path "*create-shorts*" -o -path "*longform*" | wc -l → expected 0
Command 4: grep -rlE "^\s*(import|from)\s+selenium" .preserved/harvested/ | wc -l → expected 0

<!-- decision_builder.py interface (from Plan 01) -->
decision_builder.build_decisions_md(
    conflict_entries,     # output of conflict_parser.parse_conflict_map()
    blacklist,            # output of blacklist_parser.parse_blacklist()
    scope_md_path,        # Path to 02-HARVEST_SCOPE.md (for verbatim A-row extraction)
    out_path,             # Path to 03-HARVEST_DECISIONS.md
)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build 03-HARVEST_DECISIONS.md (39 rows, A verbatim + B/C algorithmic)</name>
  <read_first>
    - studios/shorts/.planning/phases/02-domain-definition/02-HARVEST_SCOPE.md § A급 13건 사전 판정 (lines 10-38 — the canonical 13-row table to copy verbatim)
    - studios/shorts/.planning/phases/02-domain-definition/02-HARVEST_SCOPE.md § Harvest Blacklist (Python dict literal)
    - studios/shorts/.planning/phases/02-domain-definition/02-HARVEST_SCOPE.md § B/C 위임 알고리즘 (pseudocode + hints)
    - C:/Users/PC/Desktop/shorts_naberal/.planning/codebase/CONFLICT_MAP.md (source of 39 entries — read the full file to extract B/C body sections)
    - studios/shorts/scripts/harvest/decision_builder.py (Plan 01 implementation)
    - studios/shorts/.planning/phases/03-harvest/03-RESEARCH.md § Common Pitfalls P6 (format fidelity)
  </read_first>
  <files>
    - studios/shorts/.planning/phases/03-harvest/03-HARVEST_DECISIONS.md (CREATE)
    - studios/shorts/scripts/harvest/audit_log.md (APPEND)
  </files>
  <behavior>
    Generate a single markdown file with EXACTLY 39 data rows in a 5-column table.

    Column header (verbatim from 02-HARVEST_SCOPE.md § A):
    `| ID | 드리프트 요약 | 판정 | 근거 | 실행 지시 |`
    `|----|---------------|------|------|-----------|`

    Rows 1-13: copy A-1 through A-13 rows byte-for-byte from 02-HARVEST_SCOPE.md § A급 13건 사전 판정 table.

    Rows 14-39: generate B-1..B-16 and C-1..C-10 by running the 5-rule algorithm on CONFLICT_MAP.md entries.

    For each B/C row:
    - ID: e.g., "B-5" or "C-2"
    - 드리프트 요약: match `### B-5. <summary>` line from CONFLICT_MAP.md
    - 판정: algorithm output (폐기 | 승계 신형 | 통합-재작성 | cleanup)
    - 근거: which rule fired, e.g., "rule 2: scope boundary longform/"
    - 실행 지시: one-line instruction tied to verdict:
      - 폐기 → "Phase 3 harvest skip (blacklist/scope)"
      - 승계 신형 → "Phase 4+ 에이전트 프롬프트에 신형 반영 (session 77+ 기준)"
      - 통합-재작성 → "Phase 4+ 에이전트 설계 시 개념만 참조, 신규 구현"
      - cleanup → "Wave X cosmetic cleanup 커밋 대상"

    Count invariant: `grep -cE "^\| [ABC]-[0-9]+" 03-HARVEST_DECISIONS.md` MUST return exactly 39.

    File also contains:
    - YAML frontmatter with phase/plan/type metadata
    - Brief header explaining 5-rule algorithm
    - Counts summary at the end: "A: 승계 2, 폐기 3, 통합-재작성 8 = 13", same for B, C, total
  </behavior>
  <action>
    From `studios/shorts/` working directory:

    ```bash

    # Invoke harvest_importer decisions stage
    python C:/Users/PC/Desktop/naberal_group/studios/shorts/scripts/harvest/harvest_importer.py \
      --source "C:/Users/PC/Desktop/shorts_naberal" \
      --manifest C:/Users/PC/Desktop/naberal_group/studios/shorts/.planning/phases/03-harvest/path_manifest.json \
      --scope C:/Users/PC/Desktop/naberal_group/studios/shorts/.planning/phases/02-domain-definition/02-HARVEST_SCOPE.md \
      --conflict-map "C:/Users/PC/Desktop/shorts_naberal/.planning/codebase/CONFLICT_MAP.md" \
      --stage build_decisions \
      --decisions-out C:/Users/PC/Desktop/naberal_group/studios/shorts/.planning/phases/03-harvest/03-HARVEST_DECISIONS.md \
      --audit-log C:/Users/PC/Desktop/naberal_group/studios/shorts/scripts/harvest/audit_log.md

    # Fallback (direct call to decision_builder):
    python <<'PY'
    from pathlib import Path
    from scripts.harvest.conflict_parser import parse_conflict_map
    from scripts.harvest.blacklist_parser import parse_blacklist
    from scripts.harvest.decision_builder import build_decisions_md

    scope_md = Path('.planning/phases/02-domain-definition/02-HARVEST_SCOPE.md')
    conflict_map = Path('C:/Users/PC/Desktop/shorts_naberal/.planning/codebase/CONFLICT_MAP.md')
    out = Path('.planning/phases/03-harvest/03-HARVEST_DECISIONS.md')

    entries = parse_conflict_map(conflict_map)
    assert len([e for e in entries if e['class']=='A']) == 13
    assert len([e for e in entries if e['class']=='B']) == 16
    assert len([e for e in entries if e['class']=='C']) == 10

    # M-2 FIX: blacklist count invariant is owned by blacklist_parser.parse_blacklist
    # (Plan 01 Task 2 contract: parser raises ValueError if entry count != 10).
    # Plan 08 delegates — no redundant assertion.
    blacklist = parse_blacklist(scope_md)
    # len(blacklist) validation happens inside parse_blacklist() itself.

    build_decisions_md(entries, blacklist, scope_md, out)

    # Row-count invariant
    import re
    text = out.read_text(encoding='utf-8')
    row_count = len(re.findall(r'^\| [ABC]-\d+', text, re.M))
    assert row_count == 39, f'expected 39 rows, got {row_count}'
    print(f'[OK] {out}: {row_count} rows')
    PY

    # Audit log entry
    echo "" >> C:/Users/PC/Desktop/naberal_group/studios/shorts/scripts/harvest/audit_log.md
    echo "## Wave 3 Task 1 — build_decisions ($(date -u +%Y-%m-%dT%H:%M:%SZ))" >> C:/Users/PC/Desktop/naberal_group/studios/shorts/scripts/harvest/audit_log.md
    echo "Output: C:/Users/PC/Desktop/naberal_group/studios/shorts/.planning/phases/03-harvest/03-HARVEST_DECISIONS.md (39 rows)" >> C:/Users/PC/Desktop/naberal_group/studios/shorts/scripts/harvest/audit_log.md
    ```
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Desktop/naberal_group/studios/shorts" &amp;&amp; test -f .planning/phases/03-harvest/03-HARVEST_DECISIONS.md &amp;&amp; [ $(grep -cE "^\| [ABC]-[0-9]+" .planning/phases/03-harvest/03-HARVEST_DECISIONS.md) -eq 39 ] &amp;&amp; [ $(grep -cE "^\| A-[0-9]+" .planning/phases/03-harvest/03-HARVEST_DECISIONS.md) -eq 13 ] &amp;&amp; [ $(grep -cE "^\| B-[0-9]+" .planning/phases/03-harvest/03-HARVEST_DECISIONS.md) -eq 16 ] &amp;&amp; [ $(grep -cE "^\| C-[0-9]+" .planning/phases/03-harvest/03-HARVEST_DECISIONS.md) -eq 10 ] &amp;&amp; grep -q "ID | 드리프트 요약 | 판정 | 근거 | 실행 지시" .planning/phases/03-harvest/03-HARVEST_DECISIONS.md &amp;&amp; echo "DECISIONS 39 rows verified"</automated>
  </verify>
  <acceptance_criteria>
    - File `03-HARVEST_DECISIONS.md` exists at expected path
    - Exactly 39 data rows (13 A + 16 B + 10 C) — `grep -cE "^\| [ABC]-[0-9]+"` returns 39
    - A-row count exactly 13, B-row count exactly 16, C-row count exactly 10
    - Column header matches verbatim: `| ID | 드리프트 요약 | 판정 | 근거 | 실행 지시 |`
    - A-rows are verbatim copies from 02-HARVEST_SCOPE.md § A (spot check A-6 row contains "skip_gates" and verdict "전수 폐기")
    - B/C rows each have 판정 ∈ {폐기, 승계 신형, 통합-재작성, cleanup}
    - B/C rows 근거 column contains "rule N:" prefix citing applied rule
    - Counts summary block at end shows totals per class
  </acceptance_criteria>
  <done>
    - HARVEST-08 satisfied
    - 39-row decision table serves as canonical Phase 4+ input
    - Phase 4 can start without revisiting A/B/C conflict judgments
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Blacklist grep audit (HARVEST-07) across all 4 raw dirs</name>
  <read_first>
    - studios/shorts/.planning/phases/03-harvest/03-VALIDATION.md rows 3-08-01 and 3-08-02 (exact grep commands)
    - studios/shorts/.planning/phases/02-domain-definition/02-HARVEST_SCOPE.md § Harvest Blacklist (all 10 entries)
    - studios/shorts/scripts/harvest/audit_log.md (to append findings)
  </read_first>
  <files>
    - studios/shorts/scripts/harvest/audit_log.md (APPEND — audit results)
  </files>
  <behavior>
    Execute 4 grep/find commands across `.preserved/harvested/**`. All four MUST return 0 matches.

    Audit log entry format:
    ```
    ## Wave 3 Task 2 — Blacklist grep audit (YYYY-MM-DDTHH:MM:SSZ)
    - skip_gates=True matches: N (expected 0)
    - TODO(next-session) matches: N (expected 0)
    - orchestrate.py/create-shorts SKILL.md/longform path matches: N (expected 0)
    - selenium import matches: N (expected 0)
    Result: PASS / FAIL
    ```
  </behavior>
  <action>
    From `studios/shorts/` working directory:

    ```bash

    # Audit 1: skip_gates=True
    SKIP_GATES_MATCHES=$(grep -r "skip_gates=True" C:/Users/PC/Desktop/naberal_group/studios/shorts/.preserved/harvested/ 2>/dev/null | wc -l)
    echo "Audit 1 skip_gates=True: $SKIP_GATES_MATCHES (expected 0)"

    # Audit 2: TODO(next-session)
    TODO_MATCHES=$(grep -r "TODO(next-session)" C:/Users/PC/Desktop/naberal_group/studios/shorts/.preserved/harvested/ 2>/dev/null | wc -l)
    echo "Audit 2 TODO(next-session): $TODO_MATCHES (expected 0)"

    # Audit 3: blacklisted file paths (orchestrate.py, create-shorts SKILL.md, longform/)
    BLACKLIST_FILES=$(find .preserved/harvested \( -name "orchestrate.py" -o \( -name "SKILL.md" -path "*create-shorts*" \) -o -path "*/longform/*" -o -path "*/create-video/*" \) 2>/dev/null | wc -l)
    echo "Audit 3 blacklist file paths: $BLACKLIST_FILES (expected 0)"

    # Audit 4: selenium imports
    SELENIUM_MATCHES=$(grep -rlE "^\s*(import|from)\s+selenium" C:/Users/PC/Desktop/naberal_group/studios/shorts/.preserved/harvested/ 2>/dev/null | wc -l)
    echo "Audit 4 selenium imports: $SELENIUM_MATCHES (expected 0)"

    # Aggregate pass/fail
    TOTAL=$((SKIP_GATES_MATCHES + TODO_MATCHES + BLACKLIST_FILES + SELENIUM_MATCHES))
    if [ "$TOTAL" -eq 0 ]; then
        RESULT="PASS"
    else
        RESULT="FAIL (total violations: $TOTAL)"
    fi

    # Append to audit log
    cat >> C:/Users/PC/Desktop/naberal_group/studios/shorts/scripts/harvest/audit_log.md <<EOF

## Wave 3 Task 2 — Blacklist grep audit ($(date -u +%Y-%m-%dT%H:%M:%SZ))
- skip_gates=True matches: $SKIP_GATES_MATCHES (expected 0)
- TODO(next-session) matches: $TODO_MATCHES (expected 0)
- blacklist file paths (orchestrate.py / create-shorts SKILL.md / longform / create-video): $BLACKLIST_FILES (expected 0)
- selenium import matches: $SELENIUM_MATCHES (expected 0)
Result: $RESULT
EOF

    # Must exit non-zero if any violation
    test "$TOTAL" -eq 0
    ```
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Desktop/naberal_group/studios/shorts" &amp;&amp; [ $(grep -r "skip_gates=True" .preserved/harvested/ 2&gt;/dev/null | wc -l) -eq 0 ] &amp;&amp; [ $(grep -r "TODO(next-session)" .preserved/harvested/ 2&gt;/dev/null | wc -l) -eq 0 ] &amp;&amp; [ $(find .preserved/harvested \( -name "orchestrate.py" -o \( -name "SKILL.md" -path "*create-shorts*" \) -o -path "*/longform/*" -o -path "*/create-video/*" \) 2&gt;/dev/null | wc -l) -eq 0 ] &amp;&amp; [ $(grep -rlE "^\s*(import|from)\s+selenium" .preserved/harvested/ 2&gt;/dev/null | wc -l) -eq 0 ] &amp;&amp; grep -q "Wave 3 Task 2 — Blacklist grep audit" scripts/harvest/audit_log.md &amp;&amp; echo "BLACKLIST audit PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "skip_gates=True" .preserved/harvested/` returns 0 lines
    - `grep -r "TODO(next-session)" .preserved/harvested/` returns 0 lines
    - `find .preserved/harvested -name "orchestrate.py" -o -path "*longform*" -o -path "*create-video*"` returns 0 paths
    - `grep -rlE "^\s*(import|from)\s+selenium" .preserved/harvested/` returns 0 files
    - audit_log.md has a Wave 3 Task 2 block with all 4 counts + "Result: PASS"
    - IF any count > 0, task fails and blocks Plan 09 (lockdown must not proceed with violations present)
  </acceptance_criteria>
  <done>
    - HARVEST-07 satisfied
    - 10-entry blacklist enforcement proven across all harvested content
    - audit_log.md contains pass/fail record
  </done>
</task>

</tasks>

<verification>
```bash
cd "C:/Users/PC/Desktop/naberal_group/studios/shorts"

# Decisions
wc -l .planning/phases/03-harvest/03-HARVEST_DECISIONS.md
grep -cE "^\| [ABC]-[0-9]+" .planning/phases/03-harvest/03-HARVEST_DECISIONS.md

# Blacklist audit
grep -r "skip_gates=True" .preserved/harvested/ | wc -l       # 0
grep -r "TODO(next-session)" .preserved/harvested/ | wc -l    # 0
find .preserved/harvested -name "orchestrate.py" | wc -l      # 0
```
Expected: DECISIONS has 39 rows; all blacklist counts = 0.
</verification>

<success_criteria>
- HARVEST-08: 03-HARVEST_DECISIONS.md exists with 39 rows in 5-col format
- HARVEST-07: 0 blacklist violations across .preserved/harvested/
- audit_log.md has Wave 3 entries for both tasks
</success_criteria>

<output>
After completion, create `.planning/phases/03-harvest/03-08-SUMMARY.md` with:
- DECISIONS row count breakdown (A:13/B:16/C:10)
- Verdict distribution (B/C 26: 폐기 X / 승계 신형 Y / 통합-재작성 Z / cleanup W)
- Blacklist audit results (all 4 counts)
- Any unexpected findings (should be none)
</output>
