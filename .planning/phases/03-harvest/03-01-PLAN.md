---
phase: 03-harvest
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - studios/shorts/.claude/agents/harvest-importer/AGENT.md
  - studios/shorts/scripts/harvest/__init__.py
  - studios/shorts/scripts/harvest/harvest_importer.py
  - studios/shorts/scripts/harvest/blacklist_parser.py
  - studios/shorts/scripts/harvest/conflict_parser.py
  - studios/shorts/scripts/harvest/decision_builder.py
  - studios/shorts/scripts/harvest/diff_verifier.py
  - studios/shorts/scripts/harvest/lockdown.py
  - studios/shorts/scripts/harvest/verify_harvest.py
autonomous: true
requirements:
  - AGENT-06
gap_closure: false

must_haves:
  truths:
    - "harvest-importer 에이전트 스펙이 AGENT.md로 존재한다 (≤500 lines, description ≤1024 chars)"
    - "scripts/harvest/ 하위에 6개 실행 모듈(harvest_importer, blacklist_parser, conflict_parser, decision_builder, diff_verifier, lockdown)과 verify_harvest.py가 존재한다"
    - "blacklist_parser가 02-HARVEST_SCOPE.md의 HARVEST_BLACKLIST dict를 ast.literal_eval로 안전하게 로드한다"
    - "diff_verifier가 filecmp.dircmp를 재귀적으로 순회하여 mismatch 리스트를 반환한다"
    - "lockdown이 cmd.exe //c attrib +R /S /D 형식으로 호출한다 (Git Bash glob 직접 호출 금지)"
  artifacts:
    - path: "studios/shorts/.claude/agents/harvest-importer/AGENT.md"
      provides: "AGENT-06 에이전트 스펙 (inputs/outputs/invariants, one-shot, Phase 3 only)"
      max_lines: 500
    - path: "studios/shorts/scripts/harvest/harvest_importer.py"
      provides: "Main entry orchestrating 6 stages (argparse: --source --scope --conflict-map --dest --failures-out --decisions-out --lockdown --audit-log)"
    - path: "studios/shorts/scripts/harvest/blacklist_parser.py"
      provides: "ast.literal_eval 기반 HARVEST_BLACKLIST 로더 (eval 금지)"
    - path: "studios/shorts/scripts/harvest/conflict_parser.py"
      provides: "CONFLICT_MAP.md regex 파서 (### [ABC]-N.), 39 assertion"
    - path: "studios/shorts/scripts/harvest/decision_builder.py"
      provides: "B/C 26건 5-rule 알고리즘 판정 + HARVEST_DECISIONS.md emitter"
    - path: "studios/shorts/scripts/harvest/diff_verifier.py"
      provides: "filecmp.dircmp 재귀 wrapper, CLI: python diff_verifier.py <raw_dir_name>"
    - path: "studios/shorts/scripts/harvest/lockdown.py"
      provides: "subprocess.run(['cmd.exe', '/c', 'attrib +R /S /D', win_path+'\\\\*']) + PermissionError 검증"
    - path: "studios/shorts/scripts/harvest/verify_harvest.py"
      provides: "argparse --quick / --full, 13 task-level checks, exit 0 iff all pass"
  key_links:
    - from: "harvest_importer.py"
      to: "02-HARVEST_SCOPE.md"
      via: "blacklist_parser.parse_blacklist()"
      pattern: "HARVEST_BLACKLIST\\s*=\\s*\\["
    - from: "harvest_importer.py"
      to: "CONFLICT_MAP.md (shorts_naberal)"
      via: "conflict_parser.parse_conflict_map()"
      pattern: "### [ABC]-\\d+"
    - from: "verify_harvest.py"
      to: "diff_verifier.py"
      via: "subprocess / direct import"
      pattern: "from diff_verifier import|import diff_verifier"
---

<objective>
harvest-importer 에이전트 스펙 + 실행 스크립트 스캐폴드를 생성한다. Phase 3 모든 후속 Wave가 이 스크립트들을 호출하므로, **복사 작업 시작 전에 반드시 존재해야 한다**.

Purpose:
- AGENT-06 요구사항(harvest-importer 에이전트) 충족
- Wave 1~4 모든 검증/실행 명령이 의존하는 단일 진입점 확보
- Windows-safe Python stdlib 전용 구현 (외부 패키지 0)

Output:
- 1 AGENT.md (스펙, ≤500 lines)
- 7 Python 파일 (__init__ + harvest_importer + 5 modules + verify_harvest)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@studios/shorts/.planning/PROJECT.md
@studios/shorts/.planning/ROADMAP.md
@studios/shorts/.planning/STATE.md
@studios/shorts/.planning/phases/02-domain-definition/02-HARVEST_SCOPE.md
@studios/shorts/.planning/phases/03-harvest/03-RESEARCH.md
@studios/shorts/.planning/phases/03-harvest/03-VALIDATION.md
@studios/shorts/CLAUDE.md

<interfaces>
<!-- Locked contracts from 02-HARVEST_SCOPE.md and 03-RESEARCH.md -->

HARVEST_BLACKLIST entry shape (from 02-HARVEST_SCOPE.md § Harvest Blacklist):
```python
{"file": "scripts/orchestrator/orchestrate.py", "lines": "1239-1291", "reason": "..."}
{"path": "longform/", "reason": "..."}
{"file": ".claude/skills/create-video/", "reason": "..."}
{"pattern": "selenium", "reason": "..."}
```

CONFLICT_MAP.md heading regex (verified via grep, 39 entries):
  Pattern: ^### ([ABC])-(\d+)\.\s+(.*)$
  Expected counts: A=13, B=16, C=10

5-rule algorithm (from 02-HARVEST_SCOPE.md § B/C 위임 알고리즘):
  1. blacklist match → 폐기
  2. scope boundary (longform/incidents_jp/worktree/duo_japan) → 폐기
  3. session 77+ canonical → 승계 신형
  4. C-class + (gitignore/worktree_copy/tmp_committed) → cleanup
  5. default → 통합-재작성

verify_harvest.py CLI contract (from 03-VALIDATION.md Per-Task map):
  --quick : run 13 task-level checks (file-exists/grep/python-probe), ~5s
  --full  : add recursive dircmp + sha256 sample (10%), ~30s
  exit 0 iff all checks pass, non-zero with structured error list otherwise

Windows attrib invariant (from 03-RESEARCH.md §5 live-tested):
  WRONG: attrib +R /S /D "path\\*"  # Git Bash glob mangles
  RIGHT: subprocess.run(["cmd.exe", "/c", f"attrib +R /S /D {win_path}\\*"])
  Verify via PermissionError on probe write

Secret file hazard (from 03-RESEARCH.md § Runtime State Inventory):
  shorts_naberal/client_secret.json and token_*.json MUST NOT be copied
  Add to shutil.ignore_patterns: "client_secret*.json", "token_*.json", ".env*", "*.key", "*.pem"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create harvest-importer AGENT.md spec</name>
  <read_first>
    - studios/shorts/.planning/phases/02-domain-definition/02-HARVEST_SCOPE.md (entire file — canonical decision source)
    - studios/shorts/.planning/phases/03-harvest/03-RESEARCH.md §1 (Python Script design) and §2 (Path Remapping)
    - studios/shorts/CLAUDE.md (lines 51-61 — 도메인 절대 규칙, rule #6 shorts_naberal 원본 수정 금지)
  </read_first>
  <files>
    - studios/shorts/.claude/agents/harvest-importer/AGENT.md (CREATE)
  </files>
  <behavior>
    - AGENT.md is minimal spec (inputs/outputs/invariants), NOT implementation pseudocode
    - description frontmatter ≤ 1024 chars, includes trigger keywords: "harvest-importer", "Phase 3", "AGENT-06"
    - Body ≤ 500 lines total
    - One-shot agent (Phase 3 only, deprecated after Phase 4 entry)
    - Lists exact invariants: (1) NEVER writes to shorts_naberal/, (2) lockdown must be last, (3) blacklist is read-only, (4) ast.literal_eval only (no eval)
  </behavior>
  <action>
    CREATE `studios/shorts/.claude/agents/harvest-importer/AGENT.md` with this exact structure:

    ```yaml
    ---
    name: harvest-importer
    description: |
      Phase 3 전용 일회성 에이전트. shorts_naberal의 작동 검증 자산(theme_bible / remotion_src / hc_checks / api_wrappers)을 studios/shorts/.preserved/harvested/로 읽기 전용 복사하고, CONFLICT_MAP 39건을 5-rule 알고리즘으로 판정하여 HARVEST_DECISIONS.md를 생성한다. FAILURES.md를 _imported_from_shorts_naberal.md로 병합하고 attrib +R /S /D로 Tier 3 lockdown을 적용한다. 트리거: "harvest-importer", "Phase 3 실행", "AGENT-06". Phase 4 진입 후 deprecated.
    version: 1.0
    phase: 3
    deprecated_after: "Phase 4 entry"
    ---
    ```

    Sections (in this order):

    ## Purpose
    - AGENT-06 REQ 충족, Phase 3 one-shot executor.
    - Pre-locked decision runner (02-HARVEST_SCOPE.md 입력, 재판정 금지).

    ## Inputs
    - `--source` : `C:/Users/PC/Desktop/shorts_naberal` (absolute path)
    - `--scope` : `.planning/phases/02-domain-definition/02-HARVEST_SCOPE.md`
    - `--conflict-map` : `<source>/.planning/codebase/CONFLICT_MAP.md`
    - `--manifest` : `.planning/phases/03-harvest/path_manifest.json` (produced by Plan 02)
    - `--dest` : `.preserved/harvested`
    - `--failures-out` : `.claude/failures/_imported_from_shorts_naberal.md`
    - `--decisions-out` : `.planning/phases/03-harvest/03-HARVEST_DECISIONS.md`
    - `--audit-log` : `scripts/harvest/audit_log.md`
    - `--lockdown` : boolean flag, applies attrib +R when true

    ## Outputs
    - 4 raw dirs under `.preserved/harvested/` (theme_bible_raw, remotion_src_raw, hc_checks_raw, api_wrappers_raw)
    - `_imported_from_shorts_naberal.md` (FAILURES merge, source comments preserved)
    - `03-HARVEST_DECISIONS.md` (39 rows = A-1..A-13 verbatim + B-1..B-16 + C-1..C-10 algorithm output)
    - `audit_log.md` (append-only structured log per stage)
    - OS-level read-only on all of `.preserved/harvested/**` (post-lockdown)

    ## Invariants (MUST REMEMBER — DO NOT VIOLATE)
    1. **shorts_naberal 원본 수정 금지** — CLAUDE.md rule #6. harvest_importer.py never opens any file under `<source>/` with `open(..., 'w')` or `Path.write_*`.
    2. **Lockdown is the LAST stage** — verification fails if attrib +R runs before diff_verify completes.
    3. **Blacklist is read-only** — `ast.literal_eval` only. `eval()` / `exec()` PROHIBITED.
    4. **try-except 침묵 폴백 금지** — every exception either re-raises with context OR emits structured error to audit_log AND exits non-zero. `except: pass` forbidden (CLAUDE.md rule #3).
    5. **Windows attrib invocation** — always via `subprocess.run(["cmd.exe", "/c", ...])`. Git Bash glob direct call produces silent security hole.
    6. **Secret file filter** — `shutil.ignore_patterns("client_secret*.json", "token_*.json", ".env*", "*.key", "*.pem")` applied to every copytree call.
    7. **Idempotency** — if `<!-- source: ... -->` comment already present in `_imported_from_shorts_naberal.md`, skip re-append (prevents P5 doubling).
    8. **CONFLICT_MAP invariant** — assert `len(A)==13 and len(B)==16 and len(C)==10`. On mismatch: raise CONFLICT_MAP_COUNT_MISMATCH and exit non-zero.

    ## Execution Order (6 stages)
    1. Load blacklist (blacklist_parser.parse_blacklist)
    2. Load manifest (json.load on path_manifest.json)
    3. Copy 4 raw dirs in sequence (Plan 03-06 tasks; agent supports sequential OR parallel-safe since different dests)
    4. Diff verify all dirs (diff_verifier.deep_diff — mismatches==[])
    5. Merge FAILURES (append-only with source comments)
    6. Parse CONFLICT_MAP + run 5-rule → HARVEST_DECISIONS.md
    7. Blacklist grep audit (`skip_gates=True`, `TODO(next-session)` 0 matches)
    8. IF --lockdown: apply cmd.exe attrib +R + verify via PermissionError probe

    ## Deprecation
    After Phase 4 entry (`/gsd:execute-phase 4` starts), this agent is NEVER called again. Do not import harvest_importer from any Phase 4+ code.

    ## References
    - Python 3.11 stdlib: shutil, filecmp, ast, subprocess, pathlib, hashlib, re
    - 02-HARVEST_SCOPE.md § Harvest Blacklist — 10 entries
    - 02-HARVEST_SCOPE.md § B/C 위임 알고리즘 — 5 rules
    - 03-RESEARCH.md §2 — path remapping requirements
    - 03-RESEARCH.md §5 — Windows attrib invocation discipline
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Desktop/naberal_group/studios/shorts" &amp;&amp; test -f .claude/agents/harvest-importer/AGENT.md &amp;&amp; test $(wc -l &lt; .claude/agents/harvest-importer/AGENT.md) -le 500 &amp;&amp; python -c "import re; d=open('.claude/agents/harvest-importer/AGENT.md',encoding='utf-8').read(); m=re.search(r'^description:\s*\|?\s*\n?((?:.+\n)+?)(?=^\w)', d, re.M); desc=m.group(1) if m else ''; assert len(desc) &lt;= 1024, f'description {len(desc)}&gt;1024'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `studios/shorts/.claude/agents/harvest-importer/AGENT.md`
    - Line count ≤ 500 (harness-audit compatible)
    - Description field ≤ 1024 chars
    - Contains all 8 invariants listed verbatim
    - Contains trigger keyword "harvest-importer" in description
    - `grep -c "eval()" .claude/agents/harvest-importer/AGENT.md` returns 0 or only within "MUST NOT use eval()" context
  </acceptance_criteria>
  <done>
    - AGENT.md exists with frontmatter + 6 sections (Purpose / Inputs / Outputs / Invariants / Execution Order / Deprecation / References)
    - harness-audit stub check passes (≤500 lines, description ≤1024 chars)
    - Ready to be called by Plans 03-09 via `python scripts/harvest/harvest_importer.py --stage <N>`
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create Python stdlib harvest modules (7 files)</name>
  <read_first>
    - studios/shorts/.planning/phases/03-harvest/03-RESEARCH.md § Implementation Approaches (§1-§8, entire "Code Examples" section)
    - studios/shorts/.planning/phases/03-harvest/03-VALIDATION.md (Per-Task Verification Map — 13 rows)
    - studios/shorts/.planning/phases/02-domain-definition/02-HARVEST_SCOPE.md § Harvest Blacklist (Python dict literal to parse)
    - studios/shorts/CLAUDE.md rule #3 (try-except 침묵 폴백 금지)
  </read_first>
  <files>
    - studios/shorts/scripts/harvest/__init__.py (CREATE, makes package importable)
    - studios/shorts/scripts/harvest/harvest_importer.py (CREATE, main entry ≤ 400 lines)
    - studios/shorts/scripts/harvest/blacklist_parser.py (CREATE, ≤ 80 lines)
    - studios/shorts/scripts/harvest/conflict_parser.py (CREATE, ≤ 80 lines)
    - studios/shorts/scripts/harvest/decision_builder.py (CREATE, ≤ 200 lines — 5-rule algo)
    - studios/shorts/scripts/harvest/diff_verifier.py (CREATE, ≤ 120 lines — filecmp.dircmp recursion + CLI)
    - studios/shorts/scripts/harvest/lockdown.py (CREATE, ≤ 100 lines)
    - studios/shorts/scripts/harvest/verify_harvest.py (CREATE, ≤ 300 lines — 13 checks CLI)
  </files>
  <behavior>
    Each module has stdlib-only imports (shutil, filecmp, ast, subprocess, pathlib, hashlib, re, argparse, json, sys). NO external packages.

    Specific behavior contracts:

    blacklist_parser.parse_blacklist(scope_md: Path) -> list[dict]:
      - re.search(r"HARVEST_BLACKLIST\s*=\s*(\[.*?\n\])", text, re.DOTALL)
      - ast.literal_eval ONLY. raise ValueError if not found.
      - Returns list of 10 dicts with keys: file|path|pattern + reason (+ optional lines).
      - **Count invariant (M-2 contract — SINGLE SOURCE OF TRUTH):** MUST raise `ValueError(f"HARVEST_BLACKLIST entry count mismatch: expected 10, got {len(result)}")` if parsed list length != 10. Callers (Plan 08) rely on this parser-owned validation and do NOT re-assert count.

    conflict_parser.parse_conflict_map(path: Path) -> list[dict]:
      - ENTRY_RE = re.compile(r"^### ([ABC])-(\d+)\.\s+(.*)$", re.MULTILINE)
      - Returns list with {"class", "num", "summary", "id"}
      - assert len(A)==13, len(B)==16, len(C)==10 → raise CONFLICT_MAP_COUNT_MISMATCH with actual counts

    decision_builder.judge(entry, body, blacklist) -> str:
      - Implements exact 5 rules from 02-HARVEST_SCOPE.md § B/C 위임 알고리즘
      - decision_builder.build_decisions_md(entries, blacklist, scope_md_path, out_path):
        - Copies A-1..A-13 5-column rows verbatim from 02-HARVEST_SCOPE.md § A
        - Appends B-1..B-16 + C-1..C-10 with algorithm verdict + rationale (cite rule number)
        - Output format: 5-col table `| ID | 드리프트 요약 | 판정 | 근거 | 실행 지시 |`
        - Total 39 data rows

    diff_verifier.deep_diff(src: Path, dst: Path) -> list[str]:
      - Recursively walks filecmp.dircmp via common_dirs
      - Returns mismatch strings prefixed with: MISSING_IN_DEST, EXTRA_IN_DEST, CONTENT_DIFF, COMPARE_ERROR
      - Empty list == perfect copy
      - CLI: python diff_verifier.py <raw_dir_name> exits 0 iff mismatches==[]

    lockdown.apply_lockdown(target: Path):
      - win_path = str(target.resolve()).replace("/", "\\")
      - cmd = ["cmd.exe", "/c", f"attrib +R /S /D {win_path}\\*"]
      - subprocess.run(cmd, capture_output=True, text=True, check=False)
      - if rc != 0: raise RuntimeError with stderr context
    lockdown.verify_lockdown(target: Path):
      - probe = next(target.rglob("*.md"), None); raise if None
      - try: probe.write_text("LOCKDOWN_VERIFY_TAMPER") → if succeeds, raise AssertionError
      - except PermissionError: return

    verify_harvest.py (CLI):
      - argparse --quick (13 basic checks) | --full (+ deep_diff + sha256 10% sample)
      - Each check emits: `[OK] <check_name>` or `[FAIL] <check_name>: <detail>`
      - Final: exit 0 if all OK; exit 1 with count otherwise
      - Checks mapped to 03-VALIDATION.md Per-Task map (rows 3-01-01 through 3-09-02)

    harvest_importer.py (main orchestrator):
      - argparse for all flags in AGENT.md
      - main() calls stages 1-8 in order
      - Every exception caught with context + audit_log append + sys.exit(non-zero)
      - No silent except
  </behavior>
  <action>
    CREATE 7 Python files under `studios/shorts/scripts/harvest/`:

    1. `__init__.py` — empty file with single line comment `# harvest-importer package (Phase 3 only)`

    2. `blacklist_parser.py` — implement `parse_blacklist(scope_md: Path) -> list[dict]` using `ast.literal_eval`. Use the regex `r"HARVEST_BLACKLIST\s*=\s*(\[.*?\n\])"` with `re.DOTALL`. Raise `ValueError("HARVEST_BLACKLIST not found in scope md")` if match fails. **M-2 contract:** after `ast.literal_eval`, if `len(result) != 10` raise `ValueError(f"HARVEST_BLACKLIST entry count mismatch: expected 10, got {len(result)}")`. This is the SINGLE SOURCE OF TRUTH for the blacklist count invariant — downstream modules (Plan 08) must NOT re-assert. Do NOT use `eval()`.

    3. `conflict_parser.py` — implement `parse_conflict_map(path: Path) -> list[dict]`. Regex `r"^### ([ABC])-(\d+)\.\s+(.*)$"` with `re.MULTILINE`. Build entries list with keys class/num/summary/id. After parsing assert counts 13/16/10 — on mismatch raise `CONFLICT_MAP_COUNT_MISMATCH` Exception class with message `f"expected 13/16/10, got {a}/{b}/{c}"`.

    4. `decision_builder.py` — implement:
       - `judge(entry, body, blacklist) -> tuple[str, str]` returning `(verdict, rationale)` per 5-rule algorithm. Rationale cites the matching rule (e.g., "rule 2: scope boundary longform/").
       - `build_decisions_md(conflict_entries, blacklist, scope_md_path, out_path)` that (a) reads 02-HARVEST_SCOPE.md and extracts A-class rows verbatim via regex, (b) generates B-class + C-class rows by calling `judge()` on each entry's body section, (c) writes 39-row markdown with header `| ID | 드리프트 요약 | 판정 | 근거 | 실행 지시 |`.

    5. `diff_verifier.py` — implement `deep_diff(src, dst)` using `filecmp.dircmp` with manual recursion over `common_dirs`. CLI section under `if __name__ == "__main__":` accepts positional raw_dir_name arg, reads path_manifest.json for source path, compares with `.preserved/harvested/<raw_dir_name>`, exits 0 iff empty mismatches list. On mismatches print them and exit 1.

    6. `lockdown.py` — implement `apply_lockdown(target)` using `subprocess.run(["cmd.exe", "/c", f"attrib +R /S /D {win_path}\\*"], capture_output=True, text=True, check=False)`. Normalize path via `str(target.resolve()).replace("/", "\\")`. If `result.returncode != 0` raise `RuntimeError(f"attrib failed: rc={result.returncode}, stderr={result.stderr!r}")`. Also implement `verify_lockdown(target)` that picks first `*.md` via `rglob`, tries `write_text("LOCKDOWN_VERIFY_TAMPER")`, expects `PermissionError`, else raises `AssertionError`.

    7. `harvest_importer.py` — main entry. argparse flags per AGENT.md Inputs section. main() orchestrates 8 stages with explicit `try/except Exception as e: audit_append(f"STAGE_{n}_ERROR: {e}"); sys.exit(1)`. Each stage emits one `[STAGE_N_OK]` audit line on success. NO `except: pass`.

    8. `verify_harvest.py` — argparse `--quick` / `--full` / `--check <name>`. 13 checks hard-coded per 03-VALIDATION.md Per-Task map. Emit `[OK]`/`[FAIL]` lines. Final exit 0 iff all OK.

    **Stdlib-only discipline:** grep confirms only these imports are used across all 7 files:
    ```
    shutil, filecmp, ast, subprocess, pathlib, hashlib, re, argparse, json, sys, os
    ```
    No pip install, no external packages.
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Desktop/naberal_group/studios/shorts" &amp;&amp; test -f scripts/harvest/__init__.py &amp;&amp; test -f scripts/harvest/harvest_importer.py &amp;&amp; test -f scripts/harvest/blacklist_parser.py &amp;&amp; test -f scripts/harvest/conflict_parser.py &amp;&amp; test -f scripts/harvest/decision_builder.py &amp;&amp; test -f scripts/harvest/diff_verifier.py &amp;&amp; test -f scripts/harvest/lockdown.py &amp;&amp; test -f scripts/harvest/verify_harvest.py &amp;&amp; python -c "from scripts.harvest import blacklist_parser, conflict_parser, decision_builder, diff_verifier, lockdown; print('imports OK')" &amp;&amp; python -c "import ast, re; [ast.parse(open(f,encoding='utf-8').read()) for f in ['scripts/harvest/harvest_importer.py','scripts/harvest/blacklist_parser.py','scripts/harvest/conflict_parser.py','scripts/harvest/decision_builder.py','scripts/harvest/diff_verifier.py','scripts/harvest/lockdown.py','scripts/harvest/verify_harvest.py']]; print('syntax OK')" &amp;&amp; ! grep -rE "^\s*(eval|exec)\(" scripts/harvest/ &amp;&amp; ! grep -rE "except\s*:\s*pass" scripts/harvest/</automated>
  </verify>
  <acceptance_criteria>
    - All 7 .py files exist with valid Python 3.11 syntax (ast.parse succeeds)
    - `python -c "from scripts.harvest import ..."` imports succeed with no external dependencies
    - `grep -rE "^\s*(eval|exec)\(" scripts/harvest/` returns 0 matches (forbidden functions)
    - `grep -rE "except\s*:\s*pass" scripts/harvest/` returns 0 matches (silent fallback forbidden)
    - `grep -r "import " scripts/harvest/ | grep -vE "shutil|filecmp|ast|subprocess|pathlib|hashlib|re|argparse|json|sys|os|^scripts\.harvest"` returns 0 extra import lines
    - `python scripts/harvest/verify_harvest.py --help` exits 0 (argparse works)
    - `python scripts/harvest/diff_verifier.py --help` exits 0 OR usage is printed to stderr (CLI parseable)
    - `lockdown.py` contains literal string `"cmd.exe"` and `"attrib +R /S /D"` (Windows invocation)
    - `conflict_parser.py` contains assertion `len(.*) == 13` and `len(.*) == 16` and `len(.*) == 10` (invariant enforcement)
  </acceptance_criteria>
  <done>
    - 7 scripts compile and import cleanly
    - No eval/exec/silent-except violations
    - CLI entry points (verify_harvest.py, diff_verifier.py, harvest_importer.py) runnable
    - Wave 1 copy tasks can invoke `python scripts/harvest/harvest_importer.py --stage copy --name <raw_dir>` without ModuleNotFoundError
  </done>
</task>

</tasks>

<verification>
Plan-level quick check after both tasks commit:

```bash
cd "C:/Users/PC/Desktop/naberal_group/studios/shorts"
# Task 1 artifact
test -f .claude/agents/harvest-importer/AGENT.md
wc -l .claude/agents/harvest-importer/AGENT.md  # ≤ 500

# Task 2 artifacts
ls scripts/harvest/*.py | wc -l  # = 7 (init + 6 modules + verify_harvest = 8; __init__+6 modules+verify = 8; confirm ≥7)
python -c "from scripts.harvest import blacklist_parser, conflict_parser, decision_builder, diff_verifier, lockdown"

# Discipline enforcement
! grep -rE "^\s*(eval|exec)\(" scripts/harvest/
! grep -rE "except\s*:\s*pass" scripts/harvest/
```

No physical file copies or lockdown triggered by this plan. This is pure scaffold.
</verification>

<success_criteria>
- harvest-importer AGENT.md exists ≤500 lines with description ≤1024 chars
- 7 Python scripts in `scripts/harvest/` with stdlib-only imports
- All modules importable via `from scripts.harvest import ...`
- No `eval()`, `exec()`, or `except: pass` in any harvest script
- Wave 1 can now invoke `harvest_importer.py` to perform copy stages (dependency unlocked)
</success_criteria>

<output>
After completion, create `.planning/phases/03-harvest/03-01-SUMMARY.md` with:
- Which files were created (exact paths)
- Line counts for AGENT.md and each .py module
- Any deviations from the action contract (if zero, state "none")
- Confirmation that Wave 1 (Plans 03-06) is unblocked
</output>
