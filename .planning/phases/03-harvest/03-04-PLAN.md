---
phase: 03-harvest
plan: 04
type: execute
wave: 1
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - studios/shorts/.preserved/harvested/remotion_src_raw/
autonomous: true
requirements:
  - HARVEST-02
gap_closure: false

must_haves:
  truths:
    - ".preserved/harvested/remotion_src_raw/ 디렉토리가 존재한다"
    - "remotion_src_raw 내부에 node_modules 디렉토리가 존재하지 않는다 (758 MB 제외 확인)"
    - "remotion/src/components, compositions, lib 구조가 그대로 보존된다"
    - "filecmp.dircmp deep diff (node_modules 제외 시) mismatches == []"
    - "secret file 및 __pycache__이 복사본에 존재하지 않는다"
  artifacts:
    - path: "studios/shorts/.preserved/harvested/remotion_src_raw/"
      provides: "Remotion composition source tree (HARVEST-02, node_modules 제외)"
      contains: "Root.tsx, index.ts, components/, compositions/, lib/"
  key_links:
    - from: "remotion_src_raw/"
      to: "shorts_naberal/remotion/src/"
      via: "shutil.copytree via harvest_importer.py stage copy with ignore"
      pattern: "shutil.ignore_patterns.*node_modules"
---

<objective>
4 개의 병렬 복사 작업 중 두 번째 — `shorts_naberal/remotion/src/` 를 `.preserved/harvested/remotion_src_raw/` 로 복사하며 `node_modules/` (758 MB, 부모 디렉토리에 위치)를 배제한다 (HARVEST-02). Phase 5 Orchestrator v2 rewrite와 Phase 4 에이전트 설계가 Remotion composition 로직을 레퍼런스로 사용할 수 있도록 한다.

Purpose:
- HARVEST-02 요구사항 충족
- Remotion composition code (Root.tsx / components / compositions / lib) 이관
- node_modules 758 MB 폭주 방지

Output:
- `.preserved/harvested/remotion_src_raw/` (Remotion src tree, node_modules 제외)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@studios/shorts/.planning/phases/03-harvest/path_manifest.json
@studios/shorts/.planning/phases/03-harvest/03-RESEARCH.md
@studios/shorts/.planning/phases/03-harvest/03-VALIDATION.md
@studios/shorts/.planning/phases/02-domain-definition/02-HARVEST_SCOPE.md
@studios/shorts/scripts/harvest/harvest_importer.py
@studios/shorts/scripts/harvest/diff_verifier.py

<interfaces>
<!-- From Plan 02 path_manifest.json -->

manifest["remotion_src_raw"] = {
  "source": "remotion/src",
  "cherry_pick": null,
  "dest": ".preserved/harvested/remotion_src_raw",
  "ignore": ["node_modules", "__pycache__", "*.pyc", ".venv", ".git"],
  "req_id": "HARVEST-02"
}

<!-- Source content (verified 2026-04-19) -->
shorts_naberal/remotion/src/
  ├── Root.tsx
  ├── index.ts
  ├── components/
  ├── compositions/
  └── lib/

shorts_naberal/remotion/  (parent — also contains node_modules/ 758 MB)
  ├── node_modules/   ← NOT included in src/; but defensive ignore prevents accidental traversal
  ├── src/
  ├── package.json
  ├── package-lock.json
  ├── public/
  ├── tsconfig.json
  └── vitest.config.ts

Note: The source path is `remotion/src`, so node_modules (located at `remotion/node_modules`, outside of `src/`) is not actually in the copy tree. The `node_modules` ignore pattern is a defense against future restructuring.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Execute remotion_src_raw copy with node_modules/pycache ignore</name>
  <read_first>
    - studios/shorts/.planning/phases/03-harvest/path_manifest.json (remotion_src_raw entry with ignore list)
    - studios/shorts/.planning/phases/03-harvest/03-RESEARCH.md §3 Copy Strategy (ignore_patterns example)
    - studios/shorts/.planning/phases/03-harvest/03-RESEARCH.md § Edge Cases E8 (Windows path length — remotion src tree is deep)
  </read_first>
  <files>
    - studios/shorts/.preserved/harvested/remotion_src_raw/ (CREATE dir + tree via harvest_importer)
  </files>
  <behavior>
    Invoke harvest_importer.py in copy-only mode for remotion_src_raw. The ignore_patterns must include: `node_modules`, `__pycache__`, `*.pyc`, `.venv`, `.git` PLUS global_ignore (secret files).

    Pre-condition: rmtree existing dest if present (not yet locked down).
    Post-condition: remotion_src_raw/ contains Root.tsx + subdirs, total size ≪ 758 MB (i.e., no node_modules leaked in).
  </behavior>
  <action>
    From `studios/shorts/` working directory, run:

    ```bash

    # Safety: remove any pre-existing partial copy
    python -c "import shutil; from pathlib import Path; p=Path('C:/Users/PC/Desktop/naberal_group/studios/shorts/.preserved/harvested/remotion_src_raw'); shutil.rmtree(p) if p.exists() else None; print('pre-clean done')"

    # Invoke harvest_importer.py
    python C:/Users/PC/Desktop/naberal_group/studios/shorts/scripts/harvest/harvest_importer.py \
      --source "C:/Users/PC/Desktop/shorts_naberal" \
      --manifest C:/Users/PC/Desktop/naberal_group/studios/shorts/.planning/phases/03-harvest/path_manifest.json \
      --stage copy \
      --name remotion_src_raw \
      --audit-log C:/Users/PC/Desktop/naberal_group/studios/shorts/scripts/harvest/audit_log.md

    # If --stage copy not yet wired, fallback:
    python <<'PY'
    import json, shutil
    from pathlib import Path
    m = json.load(open('C:/Users/PC/Desktop/naberal_group/studios/shorts/.planning/phases/03-harvest/path_manifest.json', encoding='utf-8'))
    entry = m['remotion_src_raw']
    src = Path(m['source_root']) / entry['source']
    dst = Path('C:/Users/PC/Desktop/naberal_group/studios/shorts') / entry['dest']
    if dst.exists():
        shutil.rmtree(dst)
    ignore = shutil.ignore_patterns(*(entry['ignore'] + m['global_ignore']))
    shutil.copytree(src, dst, ignore=ignore, symlinks=False, dirs_exist_ok=False)
    count = sum(1 for _ in dst.rglob('*') if _.is_file())
    size_mb = sum(f.stat().st_size for f in dst.rglob('*') if f.is_file()) / (1024*1024)
    print(f"[OK] remotion_src_raw: {count} files, {size_mb:.2f} MB")
    assert size_mb < 100, f"size {size_mb:.2f} MB too large — node_modules likely leaked"
    PY
    ```

    After copy, run verification spot checks:
    ```bash
    # Root files present
    test -f C:/Users/PC/Desktop/naberal_group/studios/shorts/.preserved/harvested/remotion_src_raw/Root.tsx || test -f C:/Users/PC/Desktop/naberal_group/studios/shorts/.preserved/harvested/remotion_src_raw/index.ts

    # Subdirectories present
    test -d C:/Users/PC/Desktop/naberal_group/studios/shorts/.preserved/harvested/remotion_src_raw/components && test -d C:/Users/PC/Desktop/naberal_group/studios/shorts/.preserved/harvested/remotion_src_raw/compositions && test -d C:/Users/PC/Desktop/naberal_group/studios/shorts/.preserved/harvested/remotion_src_raw/lib

    # node_modules NOT present
    ! test -d C:/Users/PC/Desktop/naberal_group/studios/shorts/.preserved/harvested/remotion_src_raw/node_modules

    # Size check (< 100 MB; if node_modules leaked it would be > 500 MB)
    du -sm C:/Users/PC/Desktop/naberal_group/studios/shorts/.preserved/harvested/remotion_src_raw/ | awk '{print $1}'
    ```
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Desktop/naberal_group/studios/shorts" &amp;&amp; test -d .preserved/harvested/remotion_src_raw &amp;&amp; test -d .preserved/harvested/remotion_src_raw/components &amp;&amp; test -d .preserved/harvested/remotion_src_raw/compositions &amp;&amp; test -d .preserved/harvested/remotion_src_raw/lib &amp;&amp; ! test -d .preserved/harvested/remotion_src_raw/node_modules &amp;&amp; python -c "from pathlib import Path; p=Path('.preserved/harvested/remotion_src_raw'); size_mb=sum(f.stat().st_size for f in p.rglob('*') if f.is_file())/(1024*1024); assert size_mb &lt; 100, f'size={size_mb:.2f} MB — node_modules likely leaked'; print(f'size={size_mb:.2f} MB OK')" &amp;&amp; python scripts/harvest/diff_verifier.py remotion_src_raw &amp;&amp; echo "remotion_src_raw verified"</automated>
  </verify>
  <acceptance_criteria>
    - `test -d .preserved/harvested/remotion_src_raw` exits 0
    - Subdirectories `components/`, `compositions/`, `lib/` all present
    - `node_modules/` subdirectory DOES NOT exist anywhere under remotion_src_raw/
    - `__pycache__` not present
    - Total size < 100 MB (sanity check — source src/ alone is ≪ this; > 100 MB indicates node_modules leak)
    - `python scripts/harvest/diff_verifier.py remotion_src_raw` exits 0 (mismatches=[] after ignore patterns applied)
    - No secret files (client_secret*.json, token_*.json) in copy tree
  </acceptance_criteria>
  <done>
    - remotion_src_raw/ populated with Remotion src tree minus ignored patterns
    - Size sanity check passes (no 758 MB node_modules leakage)
    - Diff verify clean (after ignore application)
  </done>
</task>

</tasks>

<verification>
```bash
cd "C:/Users/PC/Desktop/naberal_group/studios/shorts"
ls .preserved/harvested/remotion_src_raw/
du -sm .preserved/harvested/remotion_src_raw/
python scripts/harvest/diff_verifier.py remotion_src_raw
```
Expected: Root.tsx + 3 subdirs, size << 100 MB, diff exits 0.
</verification>

<success_criteria>
- HARVEST-02 satisfied: remotion_src_raw/ contains Remotion src tree
- node_modules (758 MB) verifiably excluded
- diff_verifier passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-harvest/03-04-SUMMARY.md` with:
- Total files copied + total size (MB)
- Confirmation node_modules absent
- diff_verifier exit code
</output>
