---
phase: 03-harvest
plan: 06
type: execute
wave: 1
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - studios/shorts/.preserved/harvested/api_wrappers_raw/
autonomous: true
requirements:
  - HARVEST-05
gap_closure: false

must_haves:
  truths:
    - ".preserved/harvested/api_wrappers_raw/ 디렉토리가 존재한다"
    - "manifest의 cherry_pick 리스트에 있는 모든 API wrapper 파일(최소 4개)이 복사되었다"
    - "elevenlabs_alignment.py, tts_generate.py, _kling_i2v_batch.py, heygen_client.py 각각 byte-identical"
    - "orchestrate.py가 api_wrappers_raw에 존재하지 않는다 (D-7 blacklist 준수)"
    - "selenium 패턴을 포함한 파일이 존재하지 않는다 (AF-8 blacklist 준수)"
  artifacts:
    - path: "studios/shorts/.preserved/harvested/api_wrappers_raw/"
      provides: "Runway / Kling / ElevenLabs / Typecast / HeyGen API wrapper 복사본 (HARVEST-05)"
      min_files: 4
  key_links:
    - from: "api_wrappers_raw/"
      to: "shorts_naberal/scripts/{audio-pipeline,video-pipeline,avatar}/"
      via: "shutil.copy2 per cherry_pick path (flattened)"
      pattern: "filecmp.cmp.*shallow=False"
---

<objective>
4 개의 병렬 복사 작업 중 네 번째 — Kling / ElevenLabs / Typecast / HeyGen 등 API wrapper 파일들을 `.preserved/harvested/api_wrappers_raw/` 로 cherry-pick 복사한다 (HARVEST-05). HARVEST_SCOPE.md의 "scripts/api/" 디렉토리가 실제로는 존재하지 않으며, wrapper들이 `audio-pipeline/`, `video-pipeline/`, `avatar/`에 흩어져 있음이 RESEARCH.md §2에서 확인되었다. Phase 4+ 에이전트 설계가 이 wrapper들을 레퍼런스로 사용한다.

Purpose:
- HARVEST-05 요구사항 충족
- 흩어진 API wrapper 수집 (scripts/api/ 디렉토리 부재 우회)
- orchestrate.py 및 selenium 패턴 차단 (blacklist)

Output:
- `.preserved/harvested/api_wrappers_raw/` with 4+ API wrapper .py files
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@studios/shorts/.planning/phases/03-harvest/path_manifest.json
@studios/shorts/.planning/phases/03-harvest/03-RESEARCH.md
@studios/shorts/.planning/phases/03-harvest/03-VALIDATION.md
@studios/shorts/.planning/phases/02-domain-definition/02-HARVEST_SCOPE.md
@studios/shorts/scripts/harvest/harvest_importer.py
@studios/shorts/scripts/harvest/diff_verifier.py

<interfaces>
<!-- From Plan 02 path_manifest.json -->

manifest["api_wrappers_raw"] = {
  "source": null,
  "cherry_pick": [
    "scripts/audio-pipeline/elevenlabs_alignment.py",
    "scripts/audio-pipeline/tts_generate.py",
    "scripts/video-pipeline/_kling_i2v_batch.py",
    "scripts/avatar/heygen_client.py"
  ],
  "dest": ".preserved/harvested/api_wrappers_raw",
  "ignore": [],
  "req_id": "HARVEST-05"
}

<!-- Source verification (from RESEARCH.md §2 and Plan 02 Task 1) -->
Verified existing at 2026-04-19:
  - scripts/audio-pipeline/elevenlabs_alignment.py ✓
  - scripts/audio-pipeline/tts_generate.py ✓
  - scripts/video-pipeline/_kling_i2v_batch.py ✓
  - scripts/avatar/heygen_client.py ✓

scripts/video-pipeline/runway_client.py — NOT in manifest (not verified present); D-3 says Kling is primary.

<!-- BLACKLIST enforcement: NOT copied -->
scripts/orchestrator/orchestrate.py — D-7 full-file block (5166 lines)
Any file matching pattern "selenium" — AF-8 block
.claude/skills/create-video/ — A-12 block (not in cherry_pick anyway)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Execute api_wrappers_raw cherry_pick copy + blacklist grep audit</name>
  <read_first>
    - studios/shorts/.planning/phases/03-harvest/path_manifest.json (api_wrappers_raw.cherry_pick list)
    - studios/shorts/.planning/phases/03-harvest/03-RESEARCH.md §2 (confirms scripts/api/ does NOT exist — scatter pattern)
    - studios/shorts/.planning/phases/02-domain-definition/02-HARVEST_SCOPE.md § Harvest Blacklist (selenium pattern + orchestrate.py)
    - studios/shorts/scripts/harvest/harvest_importer.py (copy_raw cherry_pick branch)
  </read_first>
  <files>
    - studios/shorts/.preserved/harvested/api_wrappers_raw/elevenlabs_alignment.py (CREATE)
    - studios/shorts/.preserved/harvested/api_wrappers_raw/tts_generate.py (CREATE)
    - studios/shorts/.preserved/harvested/api_wrappers_raw/_kling_i2v_batch.py (CREATE)
    - studios/shorts/.preserved/harvested/api_wrappers_raw/heygen_client.py (CREATE)
  </files>
  <behavior>
    Cherry-pick copy each file from manifest["api_wrappers_raw"]["cherry_pick"] using `shutil.copy2` (preserves mtime). Flatten target to `dest_dir / basename(rel_path)`.

    Post-copy blacklist grep audit (local to this plan):
    - No file in api_wrappers_raw/ may contain the string `selenium` (import or function call) — AF-8 enforcement
    - No orchestrate.py file may be present

    NOTE: "selenium" appearing in a comment string (e.g., `"# selenium forbidden (AF-8)"`) is NOT a violation — only actual import or invocation. The grep here is defensive; the core blacklist audit happens in Plan 08.
  </behavior>
  <action>
    From `studios/shorts/` working directory:

    ```bash

    # Pre-clean
    python -c "import shutil; from pathlib import Path; p=Path('C:/Users/PC/Desktop/naberal_group/studios/shorts/.preserved/harvested/api_wrappers_raw'); shutil.rmtree(p) if p.exists() else None; print('pre-clean done')"

    # Invoke harvest_importer.py
    python C:/Users/PC/Desktop/naberal_group/studios/shorts/scripts/harvest/harvest_importer.py \
      --source "C:/Users/PC/Desktop/shorts_naberal" \
      --manifest C:/Users/PC/Desktop/naberal_group/studios/shorts/.planning/phases/03-harvest/path_manifest.json \
      --stage copy \
      --name api_wrappers_raw \
      --audit-log C:/Users/PC/Desktop/naberal_group/studios/shorts/scripts/harvest/audit_log.md

    # Fallback (cherry_pick branch of harvest_importer.copy_raw):
    python <<'PY'
    import json, shutil
    from pathlib import Path
    m = json.load(open('C:/Users/PC/Desktop/naberal_group/studios/shorts/.planning/phases/03-harvest/path_manifest.json', encoding='utf-8'))
    entry = m['api_wrappers_raw']
    root = Path(m['source_root'])
    dst_dir = Path('C:/Users/PC/Desktop/naberal_group/studios/shorts') / entry['dest']
    if dst_dir.exists():
        shutil.rmtree(dst_dir)
    dst_dir.mkdir(parents=True, exist_ok=False)
    copied = []
    missing = []
    for rel in entry['cherry_pick']:
        src = root / rel
        if not src.exists():
            missing.append(rel)
            continue
        tgt = dst_dir / Path(rel).name
        shutil.copy2(src, tgt)
        copied.append(rel)
    print(f"[OK] api_wrappers_raw: copied={len(copied)}, missing={missing}")
    assert len(copied) >= 4, f"expected ≥4 wrappers, got {len(copied)}"
    PY

    # Post-copy blacklist audit (selenium, orchestrate)
    ! grep -l "^import selenium\|^from selenium" C:/Users/PC/Desktop/naberal_group/studios/shorts/.preserved/harvested/api_wrappers_raw/*.py 2>/dev/null
    ! test -f C:/Users/PC/Desktop/naberal_group/studios/shorts/.preserved/harvested/api_wrappers_raw/orchestrate.py

    # Byte-identity check on each copied file
    python <<'PY'
    import json, filecmp
    from pathlib import Path
    m = json.load(open('C:/Users/PC/Desktop/naberal_group/studios/shorts/.planning/phases/03-harvest/path_manifest.json', encoding='utf-8'))
    root = Path(m['source_root'])
    entry = m['api_wrappers_raw']
    dst_dir = Path('C:/Users/PC/Desktop/naberal_group/studios/shorts') / entry['dest']
    for rel in entry['cherry_pick']:
        src = root / rel
        tgt = dst_dir / Path(rel).name
        if not src.exists() or not tgt.exists():
            continue
        assert filecmp.cmp(str(src), str(tgt), shallow=False), f"content diff: {rel}"
    print("all byte-identical")
    PY
    ```
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Desktop/naberal_group/studios/shorts" &amp;&amp; test -d .preserved/harvested/api_wrappers_raw &amp;&amp; [ $(ls .preserved/harvested/api_wrappers_raw/*.py 2&gt;/dev/null | wc -l) -ge 4 ] &amp;&amp; test -f .preserved/harvested/api_wrappers_raw/elevenlabs_alignment.py &amp;&amp; test -f .preserved/harvested/api_wrappers_raw/tts_generate.py &amp;&amp; test -f .preserved/harvested/api_wrappers_raw/_kling_i2v_batch.py &amp;&amp; test -f .preserved/harvested/api_wrappers_raw/heygen_client.py &amp;&amp; ! test -f .preserved/harvested/api_wrappers_raw/orchestrate.py &amp;&amp; [ $(grep -lE "^\s*(import|from)\s+selenium" .preserved/harvested/api_wrappers_raw/*.py 2&gt;/dev/null | wc -l) -eq 0 ] &amp;&amp; python scripts/harvest/diff_verifier.py api_wrappers_raw &amp;&amp; echo "api_wrappers_raw verified"</automated>
  </verify>
  <acceptance_criteria>
    - Directory `.preserved/harvested/api_wrappers_raw/` exists
    - At least 4 .py files copied (all 4 expected files present: elevenlabs_alignment.py, tts_generate.py, _kling_i2v_batch.py, heygen_client.py)
    - Each copied file is byte-identical to source (filecmp.cmp shallow=False)
    - `orchestrate.py` DOES NOT exist in api_wrappers_raw/
    - `grep -lE "^\s*(import|from)\s+selenium" api_wrappers_raw/*.py` returns 0 matches (no real selenium imports)
    - `python scripts/harvest/diff_verifier.py api_wrappers_raw` exits 0
    - audit_log.md records copied_count ≥ 4 and missing list (missing may be non-empty if some wrappers were genuinely absent at source)
  </acceptance_criteria>
  <done>
    - 4+ API wrapper files cherry-picked (flattened into single dir)
    - Blacklist enforced locally (selenium imports absent, orchestrate.py absent)
    - diff_verifier clean
  </done>
</task>

</tasks>

<verification>
```bash
cd "C:/Users/PC/Desktop/naberal_group/studios/shorts"
ls .preserved/harvested/api_wrappers_raw/
python scripts/harvest/diff_verifier.py api_wrappers_raw
grep -lE "^\s*(import|from)\s+selenium" .preserved/harvested/api_wrappers_raw/*.py | wc -l
```
Expected: ≥ 4 .py files, diff exits 0, selenium import count = 0.
</verification>

<success_criteria>
- HARVEST-05 satisfied: 4+ API wrappers cherry-picked with byte-identity
- Blacklist enforced (no orchestrate.py, no selenium imports)
</success_criteria>

<output>
After completion, create `.planning/phases/03-harvest/03-06-SUMMARY.md` with:
- Copied wrappers list (4 expected)
- Any missing wrappers from cherry_pick (should be 0 based on Plan 02 verification)
- Blacklist audit results (orchestrate.py absent: true, selenium imports: 0)
</output>
