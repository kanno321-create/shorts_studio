---
plan: 07-01
phase: 7
wave: 0
depends_on: []
files_modified:
  - tests/phase07/__init__.py
  - tests/phase07/conftest.py
  - tests/phase07/fixtures/__init__.py
  - tests/phase07/fixtures/mock_kling.mp4
  - tests/phase07/fixtures/mock_runway.mp4
  - tests/phase07/fixtures/mock_typecast.wav
  - tests/phase07/fixtures/mock_elevenlabs.wav
  - tests/phase07/fixtures/mock_shotstack.mp4
  - tests/phase07/fixtures/still_image.jpg
  - tests/phase07/test_infra_smoke.py
  - tests/phase07/test_mock_fixtures_bytes.py
  - scripts/validate/harness_audit.py
  - tests/phase07/test_harness_audit_json_flag.py
files_read_first:
  - .planning/phases/07-integration-test/07-CONTEXT.md
  - .planning/phases/07-integration-test/07-RESEARCH.md
  - .planning/phases/07-integration-test/07-VALIDATION.md
  - scripts/validate/harness_audit.py
  - tests/phase05/conftest.py
  - tests/phase06/conftest.py
  - tests/phase05/test_pipeline_e2e_mock.py
autonomous: true
requirements_addressed: [TEST-01, AUDIT-02]
success_criteria: [SC1, SC5]
estimated_commits: 3
parallel_boundary: sequential

must_haves:
  truths:
    - "tests/phase07/__init__.py exists as empty package marker"
    - "tests/phase07/conftest.py defines fixtures _fake_env, tmp_session_id, mock_pass_verdict, mock_fail_verdict"
    - "tests/phase07/fixtures/ has 6 placeholder files (5 media + still_image.jpg) — zero-byte acceptable per RESEARCH Don't Hand-Roll row"
    - "scripts/validate/harness_audit.py preserves existing 'HARNESS_AUDIT_SCORE: N' stdout line (Pitfall 8 — backward compatibility)"
    - "scripts/validate/harness_audit.py accepts --json-out PATH flag writing D-11 6-key schema"
    - "pytest tests/phase07/test_infra_smoke.py -q exits 0"
    - "pytest tests/phase07/test_harness_audit_json_flag.py -q exits 0"
  artifacts:
    - path: "tests/phase07/conftest.py"
      provides: "Phase 7 shared fixtures (NOT a copy of phase05/06 conftest per D-13)"
      min_lines: 50
    - path: "scripts/validate/harness_audit.py"
      provides: "Extended CLI with --json-out D-11 6-key JSON emission (additive, no breaking change)"
      contains: "--json-out"
    - path: "tests/phase07/test_harness_audit_json_flag.py"
      provides: "Locks the backward-compatibility invariant (text output preserved) + JSON emission"
      min_lines: 40
  key_links:
    - from: "scripts/validate/harness_audit.py"
      to: ".claude/deprecated_patterns.json"
      via: "_scan_deprecated_patterns helper reads 8 regex"
      pattern: "deprecated_patterns\\.json"
    - from: "tests/phase07/conftest.py"
      to: "tests/phase07/fixtures/"
      via: "Path resolution via _REPO_ROOT resolve-at-import (session #16 decision #40 precedent)"
      pattern: "_REPO_ROOT|fixtures"
---

<objective>
Wave 0 FOUNDATION — Create `tests/phase07/` package skeleton with shared fixtures independent from phase05/06 conftests (D-13), seed 6 zero-byte placeholder media fixtures under `tests/phase07/fixtures/`, and extend the existing 122-line `scripts/validate/harness_audit.py` with a `--json-out PATH` flag emitting the D-11 6-key JSON schema while preserving the legacy `HARNESS_AUDIT_SCORE: N` stdout text (Pitfall 8 — additive, zero-breaking-change).

Purpose: Wave 0 infrastructure unblocks every downstream Wave 1-5 plan. TEST-01..04 cannot run without `_fake_env` fixture + placeholder files; AUDIT-02 tests cannot parse JSON unless `--json-out` flag exists.

Output: 2 package markers + 1 conftest + 6 fixtures + 1 harness_audit extension + 2 Wave-0 gate tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-integration-test/07-CONTEXT.md
@.planning/phases/07-integration-test/07-RESEARCH.md
@.planning/phases/07-integration-test/07-VALIDATION.md
@scripts/validate/harness_audit.py
@tests/phase05/conftest.py
@tests/phase06/conftest.py
@tests/phase05/test_pipeline_e2e_mock.py

<interfaces>
## harness_audit.py existing CLI contract (DO NOT BREAK — Pitfall 8)

Current behavior (lines 90-121, verified 2026-04-19):
```
py -3.11 -m scripts.validate.harness_audit [--agents-root DIR] [--exclude NAME] [--threshold 80]
```
Stdout line (line 113): `HARNESS_AUDIT_SCORE: N` — Phase 4 Wave 5 acceptance grep-parses this verbatim.
Exit: 0 if `score >= threshold`, 1 otherwise.

## harness_audit.py NEW flag contract (additive)

```
py -3.11 -m scripts.validate.harness_audit [...existing flags...] [--json-out PATH]
```
When `--json-out PATH` is provided:
1. Existing text output (`HARNESS_AUDIT_SCORE: N`) STILL printed to stdout (unchanged).
2. D-11 6-key JSON schema written to `PATH`:
   - `score: int`
   - `a_rank_drift_count: int`
   - `skill_over_500_lines: list[str]`
   - `agent_count: int`
   - `description_over_1024: list[str]`
   - `deprecated_pattern_matches: dict[str, int]`
   - (plus `phase: int`, `timestamp: str` metadata)
3. Exit code unchanged (0 or 1 based on threshold).

## Phase 7 conftest fixtures (D-13 — SEPARATE from phase05/06)

```python
@pytest.fixture
def _fake_env(monkeypatch):
    for var in ("KLING_API_KEY", "RUNWAY_API_KEY", "TYPECAST_API_KEY",
                "ELEVENLABS_API_KEY", "SHOTSTACK_API_KEY"):
        monkeypatch.setenv(var, "fake")

@pytest.fixture
def tmp_session_id() -> str:
    return "tst_phase07_e2e"

@pytest.fixture
def mock_pass_verdict():
    from scripts.orchestrator import Verdict
    return Verdict(result="PASS", score=90, evidence=[],
                   semantic_feedback="", inspector_name="shorts-supervisor")

@pytest.fixture
def mock_fail_verdict():
    from scripts.orchestrator import Verdict
    return Verdict(result="FAIL", score=20,
                   evidence=[{"rule": "thumbnail_hook_weak", "detail": "no face, no caption"}],
                   semantic_feedback="face + caption missing", inspector_name="ins-thumbnail-hook")
```

## Media fixtures (D-2 + Don't Hand-Roll)

Zero-byte `Path.touch()` sufficient because Phase 7 MagicMock adapters never `.read_bytes()` on returned paths. RESEARCH §Don't Hand-Roll "MP4 / WAV validity" row confirms Phase 5 precedent.

## D-11 6-key JSON schema canonical example (RESEARCH Pattern 4 lines 518-538)

```json
{
  "score": 95,
  "a_rank_drift_count": 0,
  "skill_over_500_lines": [],
  "agent_count": 32,
  "description_over_1024": [],
  "deprecated_pattern_matches": {
    "selenium": 0, "T2V": 0, "skip_gates": 0, "segments": 0,
    "TODO_gate": 0, "background_publish": 0,
    "FAILURES_removed": 0, "SKILL_direct_edit": 0
  },
  "phase": 7,
  "timestamp": "2026-04-19T00:00:00Z"
}
```
</interfaces>
</context>

<tasks>

<task id="7-01-01">
  <action>
Create `tests/phase07/__init__.py` (empty file, one blank line — package marker per D-13).

Create `tests/phase07/fixtures/__init__.py` (empty file, one blank line — sub-package marker).

Create the 6 placeholder fixture files. All are zero-byte via `Path.touch()` per RESEARCH Don't Hand-Roll row (Phase 5 MagicMock adapters never read bytes):
- `tests/phase07/fixtures/mock_kling.mp4`
- `tests/phase07/fixtures/mock_runway.mp4`
- `tests/phase07/fixtures/mock_typecast.wav`
- `tests/phase07/fixtures/mock_elevenlabs.wav`
- `tests/phase07/fixtures/mock_shotstack.mp4`
- `tests/phase07/fixtures/still_image.jpg`

Create `tests/phase07/conftest.py` (~70 lines) with:

```python
"""Phase 7 integration-test shared fixtures (D-13: independent from phase05/06 conftests).

Per 07-CONTEXT.md D-13: 'Phase 5/6 conftest 복사 금지 — Phase별 fixture 독립 원칙'.
Each fixture is purpose-built for Phase 7 E2E + fault injection + harness-audit scenarios.
"""
from __future__ import annotations

from pathlib import Path

import pytest

# Resolve-at-import pattern from session #16 decision #40 (phase05/phase06 precedent).
_REPO_ROOT = Path(__file__).resolve().parents[2]
_FIXTURES = Path(__file__).resolve().parent / "fixtures"


@pytest.fixture
def repo_root() -> Path:
    """Absolute path to studios/shorts repo root."""
    return _REPO_ROOT


@pytest.fixture
def phase07_fixtures() -> Path:
    """Absolute path to tests/phase07/fixtures/ directory."""
    return _FIXTURES


@pytest.fixture
def _fake_env(monkeypatch: pytest.MonkeyPatch) -> None:
    """Set 5 API keys to 'fake' so adapter constructors do not raise ValueError.

    Phase 5 precedent (tests/phase05/test_pipeline_e2e_mock.py:40-48). MagicMock
    adapters bypass real network; 'fake' value is never transmitted.
    """
    for var in ("KLING_API_KEY", "RUNWAY_API_KEY", "TYPECAST_API_KEY",
                "ELEVENLABS_API_KEY", "SHOTSTACK_API_KEY"):
        monkeypatch.setenv(var, "fake")


@pytest.fixture
def tmp_session_id() -> str:
    """Deterministic session id for Phase 7 E2E runs (D-21)."""
    return "tst_phase07_e2e"


@pytest.fixture
def mock_pass_verdict():
    from scripts.orchestrator import Verdict
    return Verdict(
        result="PASS",
        score=90,
        evidence=[],
        semantic_feedback="",
        inspector_name="shorts-supervisor",
    )


@pytest.fixture
def mock_fail_verdict():
    from scripts.orchestrator import Verdict
    return Verdict(
        result="FAIL",
        score=20,
        evidence=[{"rule": "thumbnail_hook_weak", "detail": "no face, no caption"}],
        semantic_feedback="face + caption missing",
        inspector_name="ins-thumbnail-hook",
    )
```

Create `tests/phase07/test_infra_smoke.py` (~40 lines) asserting Wave 0 fixtures exist:

```python
"""Wave 0 smoke — verifies tests/phase07/ scaffold and fixtures are in place."""
from __future__ import annotations

from pathlib import Path

import pytest


_FIXTURES = Path(__file__).resolve().parent / "fixtures"

_REQUIRED_FIXTURES = [
    "mock_kling.mp4",
    "mock_runway.mp4",
    "mock_typecast.wav",
    "mock_elevenlabs.wav",
    "mock_shotstack.mp4",
    "still_image.jpg",
]


def test_phase07_package_marker_exists():
    assert (Path(__file__).resolve().parent / "__init__.py").exists()


def test_fixtures_package_marker_exists():
    assert (_FIXTURES / "__init__.py").exists()


@pytest.mark.parametrize("name", _REQUIRED_FIXTURES)
def test_fixture_file_exists(name: str):
    p = _FIXTURES / name
    assert p.exists(), f"Wave 0 missing fixture: {p}"


def test_fake_env_fixture_sets_5_keys(_fake_env, monkeypatch):
    import os
    for var in ("KLING_API_KEY", "RUNWAY_API_KEY", "TYPECAST_API_KEY",
                "ELEVENLABS_API_KEY", "SHOTSTACK_API_KEY"):
        assert os.environ.get(var) == "fake", f"{var} not set to 'fake' by _fake_env"


def test_tmp_session_id_is_deterministic(tmp_session_id):
    assert tmp_session_id == "tst_phase07_e2e"


def test_pass_verdict_shape(mock_pass_verdict):
    assert mock_pass_verdict.result == "PASS"
    assert mock_pass_verdict.score == 90


def test_fail_verdict_shape(mock_fail_verdict):
    assert mock_fail_verdict.result == "FAIL"
    assert mock_fail_verdict.evidence[0]["rule"] == "thumbnail_hook_weak"
```

Create `tests/phase07/test_mock_fixtures_bytes.py` (~30 lines) asserting zero-byte placeholder invariant (Don't Hand-Roll row):

```python
"""D-2 + Don't Hand-Roll: fixtures are zero-byte placeholders, not real MP4/WAV content."""
from __future__ import annotations

from pathlib import Path

import pytest


_FIXTURES = Path(__file__).resolve().parent / "fixtures"


@pytest.mark.parametrize("name", [
    "mock_kling.mp4", "mock_runway.mp4", "mock_typecast.wav",
    "mock_elevenlabs.wav", "mock_shotstack.mp4", "still_image.jpg",
])
def test_fixture_is_zero_byte_placeholder(name: str):
    """Per RESEARCH Don't Hand-Roll: MagicMock adapters never read_bytes().

    Zero-byte is deterministic + minimal disk footprint + no MP4/WAV header complexity.
    """
    p = _FIXTURES / name
    assert p.exists()
    assert p.stat().st_size == 0, (
        f"{name} is {p.stat().st_size} bytes; Phase 7 expects 0-byte placeholders "
        f"(Don't Hand-Roll row; Phase 5 precedent)"
    )
```
  </action>
  <read_first>
    - tests/phase05/conftest.py (Phase 5 fixture patterns)
    - tests/phase06/conftest.py (Phase 6 fixture patterns)
    - tests/phase05/test_pipeline_e2e_mock.py (_fake_env precedent at lines 40-48)
    - .planning/phases/07-integration-test/07-CONTEXT.md §D-13 (independent conftest rule)
    - .planning/phases/07-integration-test/07-RESEARCH.md §Don't Hand-Roll (zero-byte placeholder decision)
  </read_first>
  <acceptance_criteria>
    - `test -f tests/phase07/__init__.py` exits 0
    - `test -f tests/phase07/fixtures/__init__.py` exits 0
    - For each of 6 fixtures: `test -f tests/phase07/fixtures/$NAME` exits 0 AND `wc -c < tests/phase07/fixtures/$NAME` outputs `0`
    - `grep -c "def _fake_env" tests/phase07/conftest.py` outputs `1`
    - `grep -c "def mock_pass_verdict\|def mock_fail_verdict\|def tmp_session_id\|def repo_root\|def phase07_fixtures" tests/phase07/conftest.py` outputs `5`
    - `grep -c "_REPO_ROOT = Path" tests/phase07/conftest.py` outputs `1`
    - `python -m pytest tests/phase07/test_infra_smoke.py tests/phase07/test_mock_fixtures_bytes.py -q --no-cov` exits 0
  </acceptance_criteria>
  <automated>python -m pytest tests/phase07/test_infra_smoke.py tests/phase07/test_mock_fixtures_bytes.py -q --no-cov</automated>
  <task_type>impl</task_type>
</task>

<task id="7-01-02">
  <action>
Extend `scripts/validate/harness_audit.py` from the current 122-line CLI with a `--json-out PATH` flag that emits the D-11 6-key JSON schema. DO NOT delete or modify the existing `print(f"HARNESS_AUDIT_SCORE: {score}")` line (Pitfall 8).

Add these imports at the TOP of the file (after existing imports, before `SAMPLE_VALID_RUBRIC`):

```python
import datetime as _dt
```

Add these helper functions BEFORE `def main(argv: list[str] | None = None) -> int:`. Use minimal dependencies (stdlib only):

```python
_SCAN_ROOTS = [pathlib.Path("scripts"), pathlib.Path(".claude/agents"),
               pathlib.Path("tests"), pathlib.Path("wiki")]
_SKILL_ROOT = pathlib.Path(".claude/skills")
_DEPRECATED_PATTERNS_JSON = pathlib.Path(".claude/deprecated_patterns.json")


def _scan_deprecated_patterns(patterns_json: pathlib.Path = _DEPRECATED_PATTERNS_JSON) -> dict[str, int]:
    """Scan 4 roots for each of the 8 deprecated_patterns.json regex; return {name: count}."""
    if not patterns_json.exists():
        return {}
    raw = json.loads(patterns_json.read_text(encoding="utf-8"))
    # deprecated_patterns.json format: {"patterns": [{"name"|"reason": str, "regex": str}, ...]}
    patterns = raw.get("patterns", raw) if isinstance(raw, dict) else raw
    counts: dict[str, int] = {}
    for entry in patterns:
        regex_str = entry.get("regex") or entry.get("pattern") or ""
        if not regex_str:
            continue
        key = entry.get("name") or entry.get("reason", "").split(":")[0].strip() or regex_str[:24]
        try:
            rx = re.compile(regex_str)
        except re.error:
            counts[key] = 0
            continue
        n = 0
        for root in _SCAN_ROOTS:
            if not root.exists():
                continue
            for p in root.rglob("*"):
                if not p.is_file() or p.suffix not in {".py", ".md", ".json"}:
                    continue
                try:
                    text = p.read_text(encoding="utf-8", errors="ignore")
                except OSError:
                    continue
                n += len(rx.findall(text))
        counts[key] = n
    return counts


def _count_agents(agents_root: pathlib.Path) -> int:
    if not agents_root.exists():
        return 0
    return sum(1 for _ in agents_root.rglob("AGENT.md"))


def _skills_over_500_lines(skills_root: pathlib.Path = _SKILL_ROOT) -> list[str]:
    out: list[str] = []
    if not skills_root.exists():
        return out
    for p in skills_root.rglob("SKILL.md"):
        try:
            with p.open(encoding="utf-8") as f:
                line_count = sum(1 for _ in f)
        except OSError:
            continue
        if line_count > 500:
            out.append(str(p).replace("\\", "/"))
    return out


def _descriptions_over_1024(agents_root: pathlib.Path) -> list[str]:
    out: list[str] = []
    if not agents_root.exists():
        return out
    desc_rx = re.compile(r"^description:\s*(.*)$", re.MULTILINE)
    for p in agents_root.rglob("AGENT.md"):
        try:
            text = p.read_text(encoding="utf-8", errors="ignore")
        except OSError:
            continue
        m = desc_rx.search(text)
        if m and len(m.group(1).strip()) > 1024:
            out.append(str(p).replace("\\", "/"))
    return out


def _a_rank_drift_count(violations: list[str]) -> int:
    """'A급 drift' ≡ deprecated_pattern_matches count — per D-12 every match is an A-rank violation."""
    return sum(1 for v in violations if "A급" in v or "A-rank" in v or "drift" in v.lower())


def _build_d11_report(score: int, violations: list[str], agents_root: pathlib.Path) -> dict:
    """D-11 6-key schema (+ phase + timestamp metadata)."""
    deprecated = _scan_deprecated_patterns()
    a_rank_from_deprecated = sum(deprecated.values())
    return {
        "score": int(score),
        "a_rank_drift_count": _a_rank_drift_count(violations) + a_rank_from_deprecated,
        "skill_over_500_lines": _skills_over_500_lines(),
        "agent_count": _count_agents(agents_root),
        "description_over_1024": _descriptions_over_1024(agents_root),
        "deprecated_pattern_matches": deprecated,
        "phase": 7,
        "timestamp": _dt.datetime.now(_dt.timezone.utc)
            .isoformat().replace("+00:00", "Z"),
    }
```

Then extend `def main(argv)`. INSIDE the existing `main()`, add the `--json-out` argparse arg BEFORE `args = parser.parse_args(argv)`:

```python
    parser.add_argument(
        "--json-out", type=pathlib.Path, default=None,
        help="If set, write D-11 6-key JSON report to this path (text output preserved).",
    )
```

AFTER the existing `print(f"HARNESS_AUDIT_SCORE: {score}")` block (after line 115 — DO NOT modify those prints), insert:

```python
    if args.json_out is not None:
        report = _build_d11_report(score, violations, pathlib.Path(args.agents_root))
        args.json_out.parent.mkdir(parents=True, exist_ok=True)
        args.json_out.write_text(
            json.dumps(report, ensure_ascii=False, indent=2) + "\n",
            encoding="utf-8",
        )
```

The final `return 0 if score >= args.threshold else 1` line is preserved.

Create `tests/phase07/test_harness_audit_json_flag.py` (~60 lines) to lock the backward-compatibility + new-flag contract:

```python
"""Locks the backward-compatibility invariant (Pitfall 8) + new --json-out D-11 emission."""
from __future__ import annotations

import json
import subprocess
import sys
from pathlib import Path


_REPO = Path(__file__).resolve().parents[2]
_AUDIT = _REPO / "scripts" / "validate" / "harness_audit.py"


def test_audit_script_exists():
    assert _AUDIT.exists()


def test_text_output_backward_compatible():
    """Pitfall 8: existing 'HARNESS_AUDIT_SCORE: N' line MUST still be printed."""
    result = subprocess.run(
        [sys.executable, str(_AUDIT)],
        cwd=str(_REPO), capture_output=True, text=True,
        encoding="utf-8", errors="replace", timeout=60,
    )
    # Exit code may be 0 or 1 depending on threshold; we only care about stdout line.
    assert "HARNESS_AUDIT_SCORE:" in result.stdout, (
        f"Backward-compat broken — legacy text line missing.\nSTDOUT:\n{result.stdout}"
    )


def test_json_out_flag_emits_d11_schema(tmp_path: Path):
    out = tmp_path / "report.json"
    result = subprocess.run(
        [sys.executable, str(_AUDIT), "--json-out", str(out)],
        cwd=str(_REPO), capture_output=True, text=True,
        encoding="utf-8", errors="replace", timeout=60,
    )
    # Text output still present.
    assert "HARNESS_AUDIT_SCORE:" in result.stdout
    # JSON file created.
    assert out.exists(), f"--json-out did not create {out}\nSTDERR:{result.stderr}"
    report = json.loads(out.read_text(encoding="utf-8"))
    # D-11 6 mandatory keys.
    required = {
        "score", "a_rank_drift_count", "skill_over_500_lines",
        "agent_count", "description_over_1024", "deprecated_pattern_matches",
    }
    missing = required - set(report.keys())
    assert not missing, f"D-11 missing keys: {missing}"


def test_json_types_match_d11_contract(tmp_path: Path):
    out = tmp_path / "report.json"
    subprocess.run(
        [sys.executable, str(_AUDIT), "--json-out", str(out)],
        cwd=str(_REPO), capture_output=True, text=True,
        encoding="utf-8", errors="replace", timeout=60,
    )
    report = json.loads(out.read_text(encoding="utf-8"))
    assert isinstance(report["score"], int)
    assert 0 <= report["score"] <= 100
    assert isinstance(report["a_rank_drift_count"], int)
    assert isinstance(report["skill_over_500_lines"], list)
    assert isinstance(report["agent_count"], int)
    assert isinstance(report["description_over_1024"], list)
    assert isinstance(report["deprecated_pattern_matches"], dict)
```
  </action>
  <read_first>
    - scripts/validate/harness_audit.py (all 122 lines — know what to preserve vs extend)
    - .planning/phases/07-integration-test/07-CONTEXT.md §D-10, §D-11 (JSON schema 6 keys)
    - .planning/phases/07-integration-test/07-RESEARCH.md §Pattern 4 lines 510-540 + §Example 6 lines 926-998
    - .claude/deprecated_patterns.json (8 regex format confirmation)
  </read_first>
  <acceptance_criteria>
    - `grep -c "HARNESS_AUDIT_SCORE" scripts/validate/harness_audit.py` outputs `>= 1` (legacy line preserved)
    - `grep -c "\-\-json-out" scripts/validate/harness_audit.py` outputs `>= 1`
    - `grep -c "_build_d11_report\|_scan_deprecated_patterns\|_skills_over_500_lines\|_count_agents\|_descriptions_over_1024" scripts/validate/harness_audit.py` outputs `>= 5` (5 new helpers)
    - `python scripts/validate/harness_audit.py 2>/dev/null | grep -c HARNESS_AUDIT_SCORE` outputs `1` (backward-compat sanity)
    - `python -m pytest tests/phase07/test_harness_audit_json_flag.py -q --no-cov` exits 0
    - `python -m py_compile scripts/validate/harness_audit.py` exits 0
  </acceptance_criteria>
  <automated>python -m pytest tests/phase07/test_harness_audit_json_flag.py -q --no-cov</automated>
  <task_type>impl</task_type>
</task>

<task id="7-01-03">
  <action>
Run regression sweep to confirm Phase 4/5/6 baseline still green after harness_audit.py extension. This is a verification-only step — no file changes.

Execute:
```
python -m pytest tests/phase04 tests/phase05 tests/phase06 -q --no-cov
```

Must exit 0 with 809 tests passing. If any regression observed, the harness_audit.py extension introduced a break — STOP and diagnose before flipping Wave 0 gate.

Additionally run the two Wave 0 gate commands:
```
python -m pytest tests/phase07/ -q --no-cov
python scripts/validate/harness_audit.py
```
Both must exit 0 (pytest: all 4 test files; audit: score >= 80 on current codebase).
  </action>
  <read_first>
    - .planning/phases/07-integration-test/07-CONTEXT.md §D-23 (809 regression invariant)
  </read_first>
  <acceptance_criteria>
    - `python -m pytest tests/phase04 tests/phase05 tests/phase06 -q --no-cov` exits 0
    - `python -m pytest tests/phase07/ -q --no-cov` exits 0
    - `python scripts/validate/harness_audit.py` exits 0 (score >= 80 on current codebase baseline)
  </acceptance_criteria>
  <automated>python -m pytest tests/phase04 tests/phase05 tests/phase06 tests/phase07 -q --no-cov</automated>
  <task_type>regression</task_type>
</task>

</tasks>

<verification>
1. `tests/phase07/__init__.py` + `tests/phase07/fixtures/__init__.py` exist as package markers.
2. 6 zero-byte fixtures present under `tests/phase07/fixtures/` (5 media + 1 still_image.jpg).
3. `tests/phase07/conftest.py` defines fixtures: `_fake_env`, `tmp_session_id`, `mock_pass_verdict`, `mock_fail_verdict`, `repo_root`, `phase07_fixtures`.
4. `scripts/validate/harness_audit.py` preserves `HARNESS_AUDIT_SCORE: N` text output.
5. `scripts/validate/harness_audit.py --json-out PATH` writes D-11 6-key JSON.
6. All Phase 4/5/6 tests green (809 baseline).
7. Phase 7 infra smoke tests green.
</verification>

<success_criteria>
Plan 07-01 is COMPLETE when:
- Wave 0 scaffold is on disk (conftest + 6 fixtures + 2 smoke tests).
- harness_audit.py is backward-compatible AND emits D-11 JSON.
- 809 regression baseline preserved.
- Wave 0 gate flipped → downstream Waves 1-5 unblocked.
</success_criteria>

<output>
Create `.planning/phases/07-integration-test/07-01-SUMMARY.md` documenting:
- Files created (2 init + 6 fixtures + 1 conftest + 2 smoke tests = 11 files)
- harness_audit.py line-count before/after (122 → ~200)
- D-11 6-key JSON sample output
- 809 regression sweep result
- Commit hash
</output>
