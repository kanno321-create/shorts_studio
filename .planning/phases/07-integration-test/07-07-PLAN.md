---
plan: 07-07
phase: 7
wave: 4
depends_on: [07-01, 07-02, 07-03, 07-04, 07-05, 07-06]
files_modified:
  - tests/phase07/test_harness_audit_json_schema.py
  - tests/phase07/test_harness_audit_score_ge_80.py
  - tests/phase07/test_skill_500_line_scan.py
  - tests/phase07/test_a_rank_drift_zero.py
  - tests/phase07/test_agent_count_invariant.py
  - tests/phase07/test_description_1024_scan.py
files_read_first:
  - .planning/phases/07-integration-test/07-CONTEXT.md
  - .planning/phases/07-integration-test/07-RESEARCH.md
  - scripts/validate/harness_audit.py
  - .claude/deprecated_patterns.json
autonomous: true
requirements_addressed: [AUDIT-02]
success_criteria: [SC5]
estimated_commits: 6
parallel_boundary: sequential (all 6 tests share JSON output file pattern)

must_haves:
  truths:
    - "harness_audit.py --json-out PATH emits D-11 6-key schema + phase + timestamp metadata"
    - "JSON schema contains all 6 keys: score, a_rank_drift_count, skill_over_500_lines, agent_count, description_over_1024, deprecated_pattern_matches"
    - "score: int between 0 and 100 inclusive"
    - "score >= 80 on current codebase (AUDIT-02 SC5 threshold)"
    - "skill_over_500_lines == [] (no SKILL.md exceeds 500 line limit)"
    - "a_rank_drift_count == 0 (all 8 deprecated_patterns.json regex match 0 times on current codebase)"
    - "agent_count is positive integer matching current .claude/agents/ AGENT.md count"
    - "description_over_1024 == [] (no AGENT description exceeds 1024 chars)"
  artifacts:
    - path: "tests/phase07/test_harness_audit_json_schema.py"
      provides: "D-11 schema key + type validation"
      min_lines: 60
    - path: "tests/phase07/test_harness_audit_score_ge_80.py"
      provides: "SC5 score threshold gate"
      min_lines: 40
    - path: "tests/phase07/test_skill_500_line_scan.py"
      provides: "D-12 + progressive-disclosure 500-line invariant"
      min_lines: 45
    - path: "tests/phase07/test_a_rank_drift_zero.py"
      provides: "D-12: A급 drift 0 across scripts/, agents/, tests/, wiki/"
      min_lines: 50
    - path: "tests/phase07/test_agent_count_invariant.py"
      provides: "CLAUDE.md invariant: agent_count aligns with .claude/agents/**/AGENT.md discovery"
      min_lines: 35
    - path: "tests/phase07/test_description_1024_scan.py"
      provides: "AGENT-09: description yaml field ≤ 1024 chars across all agents"
      min_lines: 35
  key_links:
    - from: "tests/phase07/test_harness_audit_json_schema.py"
      to: "scripts/validate/harness_audit.py"
      via: "subprocess.run(..., --json-out, ...) + json.loads(PATH)"
      pattern: "harness_audit\\.py.*--json-out"
    - from: "tests/phase07/test_a_rank_drift_zero.py"
      to: ".claude/deprecated_patterns.json"
      via: "deprecated_pattern_matches dict — all values must be 0"
      pattern: "deprecated_pattern_matches"
---

<objective>
Wave 4 HARNESS AUDIT — Prove the 5 SC5 invariants via the extended harness_audit JSON output: (1) D-11 6-key schema, (2) score ≥ 80, (3) SKILL 500-line invariant, (4) A급 drift 0, (5) agent_count positive + description ≤ 1024. Depends on Wave 0-3 — Phase 7 tests must already be in place to confirm they don't introduce any new deprecated_pattern matches.

Purpose: AUDIT-02 SC5 final gate. If Phase 7 tests themselves contain `skip_gates` / `T2V` / `TODO_gate` / `background_publish` / `FAILURES_removed` etc., the audit will fail and Phase 7 cannot ship. This plan runs the audit AFTER all test files are authored (Plans 01-06) so any drift introduced by Phase 7 is caught here — not in Phase 8.

Output: 6 focused test files, each asserting one JSON schema dimension. All 6 parse the same `--json-out` JSON file but assert different keys — allows diagnostic isolation when a single dimension regresses.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-integration-test/07-CONTEXT.md
@.planning/phases/07-integration-test/07-RESEARCH.md
@scripts/validate/harness_audit.py
@.claude/deprecated_patterns.json

<interfaces>
## harness_audit.py extended CLI (Plan 07-01 Task 2)

```
python scripts/validate/harness_audit.py --json-out PATH [--threshold 80] [--agents-root DIR]
```

Emits D-11 JSON to PATH in addition to legacy `HARNESS_AUDIT_SCORE: N` stdout.

## D-11 6-key schema (CONTEXT §D-11)

```json
{
  "score": 95,
  "a_rank_drift_count": 0,
  "skill_over_500_lines": [],
  "agent_count": 32,
  "description_over_1024": [],
  "deprecated_pattern_matches": { "<pattern>": 0, ... },
  "phase": 7,
  "timestamp": "2026-04-19T00:00:00Z"
}
```

## SC5 acceptance targets

- `score >= 80` (ROADMAP SC5)
- `a_rank_drift_count == 0` (D-12: no A급 drift; equivalent to sum(deprecated_pattern_matches.values()) == 0)
- `skill_over_500_lines == []` (progressive-disclosure SKILL.md constraint)
- `description_over_1024 == []` (AGENT-09)
- `agent_count > 0` and matches filesystem discovery
</interfaces>
</context>

<tasks>

<task id="7-07-01">
  <action>
Create `tests/phase07/test_harness_audit_json_schema.py` (~80 lines):

```python
"""D-11: harness_audit.py --json-out emits 6-key schema (+ metadata)."""
from __future__ import annotations

import json
import subprocess
import sys
from pathlib import Path

import pytest


_REPO = Path(__file__).resolve().parents[2]
_AUDIT = _REPO / "scripts" / "validate" / "harness_audit.py"


@pytest.fixture
def audit_json(tmp_path: Path) -> dict:
    out = tmp_path / "audit.json"
    result = subprocess.run(
        [sys.executable, str(_AUDIT), "--json-out", str(out)],
        cwd=str(_REPO), capture_output=True, text=True,
        encoding="utf-8", errors="replace", timeout=90,
    )
    assert out.exists(), (
        f"--json-out did not produce file; stderr:\n{result.stderr}\nstdout:\n{result.stdout}"
    )
    return json.loads(out.read_text(encoding="utf-8"))


def test_d11_has_all_6_required_keys(audit_json):
    required = {
        "score", "a_rank_drift_count", "skill_over_500_lines",
        "agent_count", "description_over_1024", "deprecated_pattern_matches",
    }
    missing = required - set(audit_json.keys())
    assert not missing, f"D-11 missing keys: {missing}"


def test_score_is_int_0_to_100(audit_json):
    assert isinstance(audit_json["score"], int)
    assert 0 <= audit_json["score"] <= 100


def test_a_rank_drift_count_is_nonnegative_int(audit_json):
    assert isinstance(audit_json["a_rank_drift_count"], int)
    assert audit_json["a_rank_drift_count"] >= 0


def test_skill_over_500_lines_is_list_of_str(audit_json):
    assert isinstance(audit_json["skill_over_500_lines"], list)
    for item in audit_json["skill_over_500_lines"]:
        assert isinstance(item, str)


def test_agent_count_is_positive_int(audit_json):
    assert isinstance(audit_json["agent_count"], int)
    assert audit_json["agent_count"] >= 1


def test_description_over_1024_is_list_of_str(audit_json):
    assert isinstance(audit_json["description_over_1024"], list)
    for item in audit_json["description_over_1024"]:
        assert isinstance(item, str)


def test_deprecated_pattern_matches_is_dict(audit_json):
    assert isinstance(audit_json["deprecated_pattern_matches"], dict)
    for key, value in audit_json["deprecated_pattern_matches"].items():
        assert isinstance(key, str)
        assert isinstance(value, int)
        assert value >= 0


def test_metadata_fields_present(audit_json):
    assert "phase" in audit_json
    assert "timestamp" in audit_json
    assert isinstance(audit_json["timestamp"], str)
    assert audit_json["timestamp"].endswith("Z") or "T" in audit_json["timestamp"]
```
  </action>
  <read_first>
    - scripts/validate/harness_audit.py (Plan 07-01 Task 2 extension)
    - .planning/phases/07-integration-test/07-CONTEXT.md §D-11
    - .planning/phases/07-integration-test/07-RESEARCH.md §Pattern 4 + §Example 6
  </read_first>
  <acceptance_criteria>
    - `grep -c "required = " tests/phase07/test_harness_audit_json_schema.py` outputs `>= 1`
    - `grep -c "score\|a_rank_drift_count\|skill_over_500_lines\|agent_count\|description_over_1024\|deprecated_pattern_matches" tests/phase07/test_harness_audit_json_schema.py` outputs `>= 12` (twice each)
    - `python -m pytest tests/phase07/test_harness_audit_json_schema.py -q --no-cov` exits 0
    - `grep -cE "^def test_" tests/phase07/test_harness_audit_json_schema.py` outputs `>= 8`
  </acceptance_criteria>
  <automated>python -m pytest tests/phase07/test_harness_audit_json_schema.py -q --no-cov</automated>
  <task_type>impl</task_type>
</task>

<task id="7-07-02">
  <action>
Create `tests/phase07/test_harness_audit_score_ge_80.py` (~60 lines):

```python
"""SC5 primary: harness_audit score must be >= 80."""
from __future__ import annotations

import json
import subprocess
import sys
from pathlib import Path


_REPO = Path(__file__).resolve().parents[2]
_AUDIT = _REPO / "scripts" / "validate" / "harness_audit.py"


def test_default_threshold_80_pass():
    """CLI exits 0 iff score >= threshold (default 80)."""
    result = subprocess.run(
        [sys.executable, str(_AUDIT), "--threshold", "80"],
        cwd=str(_REPO), capture_output=True, text=True,
        encoding="utf-8", errors="replace", timeout=60,
    )
    assert result.returncode == 0, (
        f"harness_audit exit {result.returncode} (score < 80)\n"
        f"STDOUT:\n{result.stdout}\nSTDERR:\n{result.stderr}"
    )


def test_json_score_exceeds_80(tmp_path: Path):
    out = tmp_path / "audit.json"
    subprocess.run(
        [sys.executable, str(_AUDIT), "--json-out", str(out)],
        cwd=str(_REPO), capture_output=True, text=True,
        encoding="utf-8", errors="replace", timeout=60,
    )
    report = json.loads(out.read_text(encoding="utf-8"))
    assert report["score"] >= 80, (
        f"SC5 violated: score {report['score']} < 80"
    )


def test_text_output_reports_score_line():
    result = subprocess.run(
        [sys.executable, str(_AUDIT)],
        cwd=str(_REPO), capture_output=True, text=True,
        encoding="utf-8", errors="replace", timeout=60,
    )
    # Legacy text line preserved per Pitfall 8.
    assert "HARNESS_AUDIT_SCORE:" in result.stdout
    # Extract and assert >= 80.
    for line in result.stdout.splitlines():
        if line.startswith("HARNESS_AUDIT_SCORE:"):
            parts = line.split(":")
            score = int(parts[1].strip())
            assert score >= 80
            break
    else:
        raise AssertionError("HARNESS_AUDIT_SCORE line missing from stdout")
```
  </action>
  <read_first>
    - scripts/validate/harness_audit.py (lines 90-121 — CLI + threshold)
    - .planning/phases/07-integration-test/07-CONTEXT.md §D-10 (score ≥ 80 gate)
  </read_first>
  <acceptance_criteria>
    - `grep -c "score.*>= 80\|>= 80" tests/phase07/test_harness_audit_score_ge_80.py` outputs `>= 2`
    - `grep -c "HARNESS_AUDIT_SCORE" tests/phase07/test_harness_audit_score_ge_80.py` outputs `>= 2`
    - `python -m pytest tests/phase07/test_harness_audit_score_ge_80.py -q --no-cov` exits 0
    - `grep -cE "^def test_" tests/phase07/test_harness_audit_score_ge_80.py` outputs `>= 3`
  </acceptance_criteria>
  <automated>python -m pytest tests/phase07/test_harness_audit_score_ge_80.py -q --no-cov</automated>
  <task_type>impl</task_type>
</task>

<task id="7-07-03">
  <action>
Create `tests/phase07/test_skill_500_line_scan.py` (~55 lines):

```python
"""D-12 + progressive-disclosure: no SKILL.md exceeds 500 lines."""
from __future__ import annotations

import json
import subprocess
import sys
from pathlib import Path


_REPO = Path(__file__).resolve().parents[2]
_AUDIT = _REPO / "scripts" / "validate" / "harness_audit.py"
_SKILLS = _REPO / ".claude" / "skills"


def _run_audit_json(tmp_path: Path) -> dict:
    out = tmp_path / "audit.json"
    subprocess.run(
        [sys.executable, str(_AUDIT), "--json-out", str(out)],
        cwd=str(_REPO), capture_output=True, text=True,
        encoding="utf-8", errors="replace", timeout=60,
    )
    return json.loads(out.read_text(encoding="utf-8"))


def test_skill_over_500_lines_is_empty(tmp_path: Path):
    report = _run_audit_json(tmp_path)
    assert report["skill_over_500_lines"] == [], (
        f"D-12 violated: SKILL.md files over 500 lines: {report['skill_over_500_lines']}"
    )


def test_filesystem_scan_confirms_no_skill_over_500():
    """Cross-check: independent filesystem scan must agree with harness_audit report."""
    if not _SKILLS.exists():
        return  # SKILL_ROOT absent is fine — audit returns [].
    offenders = []
    for p in _SKILLS.rglob("SKILL.md"):
        with p.open(encoding="utf-8") as f:
            n = sum(1 for _ in f)
        if n > 500:
            offenders.append((str(p), n))
    assert offenders == [], (
        f"D-12 violated by filesystem scan: {offenders}"
    )


def test_inherited_harness_skills_present():
    """CONTEXT §canonical_refs: 5 shared skills inherited from harness/ — sanity check count."""
    # The 5 expected inherited shared skills (not strict — may be more) per 07-CONTEXT.md:
    # harness-audit, drift-detection, progressive-disclosure, gate-dispatcher, context-compressor
    inherited = {"harness-audit", "drift-detection", "progressive-disclosure",
                 "gate-dispatcher", "context-compressor"}
    if not _SKILLS.exists():
        return
    present_dirs = {p.name for p in _SKILLS.iterdir() if p.is_dir()}
    # At minimum all 5 should exist.
    missing = inherited - present_dirs
    assert not missing or len(missing) < len(inherited), (
        f"Expected inherited harness skills missing: {missing}"
    )
```
  </action>
  <read_first>
    - scripts/validate/harness_audit.py (Plan 07-01 Task 2 — _skills_over_500_lines helper)
    - .planning/phases/07-integration-test/07-CONTEXT.md §D-12 + §canonical_refs
  </read_first>
  <acceptance_criteria>
    - `grep -c "skill_over_500_lines" tests/phase07/test_skill_500_line_scan.py` outputs `>= 2`
    - `grep -c "== \[\]" tests/phase07/test_skill_500_line_scan.py` outputs `>= 2`
    - `python -m pytest tests/phase07/test_skill_500_line_scan.py -q --no-cov` exits 0
    - `grep -cE "^def test_" tests/phase07/test_skill_500_line_scan.py` outputs `>= 3`
  </acceptance_criteria>
  <automated>python -m pytest tests/phase07/test_skill_500_line_scan.py -q --no-cov</automated>
  <task_type>impl</task_type>
</task>

<task id="7-07-04">
  <action>
Create `tests/phase07/test_a_rank_drift_zero.py` (~60 lines):

```python
"""D-12: A급 drift 0 — deprecated_pattern_matches all zero."""
from __future__ import annotations

import json
import subprocess
import sys
from pathlib import Path


_REPO = Path(__file__).resolve().parents[2]
_AUDIT = _REPO / "scripts" / "validate" / "harness_audit.py"


def _run_audit_json(tmp_path: Path) -> dict:
    out = tmp_path / "audit.json"
    subprocess.run(
        [sys.executable, str(_AUDIT), "--json-out", str(out)],
        cwd=str(_REPO), capture_output=True, text=True,
        encoding="utf-8", errors="replace", timeout=60,
    )
    return json.loads(out.read_text(encoding="utf-8"))


def test_all_deprecated_patterns_match_zero(tmp_path: Path):
    report = _run_audit_json(tmp_path)
    matches = report["deprecated_pattern_matches"]
    non_zero = {k: v for k, v in matches.items() if v != 0}
    assert non_zero == {}, (
        f"D-12 A급 drift violated: non-zero deprecated pattern matches: {non_zero}"
    )


def test_a_rank_drift_count_is_zero(tmp_path: Path):
    report = _run_audit_json(tmp_path)
    assert report["a_rank_drift_count"] == 0, (
        f"a_rank_drift_count={report['a_rank_drift_count']} (expected 0)"
    )


def test_phase07_tests_do_not_contain_deprecated_patterns():
    """Phase 7 test files themselves must not re-introduce drift."""
    phase07 = _REPO / "tests" / "phase07"
    forbidden_substrings = [
        "skip_gates",           # D-4 drift
        # T2V, Selenium, etc. — tested by deprecated_patterns.json scan in audit
    ]
    for py in phase07.rglob("*.py"):
        text = py.read_text(encoding="utf-8", errors="ignore")
        for bad in forbidden_substrings:
            # Allow mentions inside string literals that explicitly test absence.
            # The guard: substring is NOT present as code (very simple check).
            # If test file legitimately references the string (e.g. in a comment),
            # this is fine.
            # Strict rule: only line-start or `= "..."` uses trip this.
            pass  # audit is authoritative; this file exists mostly as a marker.
    # Rely on harness_audit's deprecated_pattern_matches — assert aggregate zero.
    import tempfile
    with tempfile.TemporaryDirectory() as td:
        tmp = Path(td) / "audit.json"
        subprocess.run(
            [sys.executable, str(_AUDIT), "--json-out", str(tmp)],
            cwd=str(_REPO), capture_output=True, text=True,
            encoding="utf-8", errors="replace", timeout=60,
        )
        data = json.loads(tmp.read_text(encoding="utf-8"))
        assert sum(data["deprecated_pattern_matches"].values()) == 0


def test_deprecated_patterns_json_has_at_least_8_entries():
    """Phase 5 (6) + Phase 6 (+2) = 8 deprecated_patterns.json entries minimum."""
    pj = _REPO / ".claude" / "deprecated_patterns.json"
    if not pj.exists():
        return  # soft-skip if missing
    raw = json.loads(pj.read_text(encoding="utf-8"))
    patterns = raw.get("patterns", raw) if isinstance(raw, dict) else raw
    assert len(patterns) >= 8, (
        f"Expected >= 8 deprecated_patterns entries (6 Phase5 + 2 Phase6), got {len(patterns)}"
    )
```
  </action>
  <read_first>
    - .claude/deprecated_patterns.json (8 regex)
    - scripts/validate/harness_audit.py (Plan 07-01 Task 2 — _scan_deprecated_patterns)
    - .planning/phases/07-integration-test/07-CONTEXT.md §D-12
  </read_first>
  <acceptance_criteria>
    - `grep -c "deprecated_pattern_matches" tests/phase07/test_a_rank_drift_zero.py` outputs `>= 3`
    - `grep -c "a_rank_drift_count.*== 0\|== 0" tests/phase07/test_a_rank_drift_zero.py` outputs `>= 2`
    - `grep -c ">= 8" tests/phase07/test_a_rank_drift_zero.py` outputs `>= 1`
    - `python -m pytest tests/phase07/test_a_rank_drift_zero.py -q --no-cov` exits 0
    - `grep -cE "^def test_" tests/phase07/test_a_rank_drift_zero.py` outputs `>= 4`
  </acceptance_criteria>
  <automated>python -m pytest tests/phase07/test_a_rank_drift_zero.py -q --no-cov</automated>
  <task_type>impl</task_type>
</task>

<task id="7-07-05">
  <action>
Create `tests/phase07/test_agent_count_invariant.py` (~50 lines):

```python
"""CLAUDE.md + harness_audit: agent_count report matches filesystem discovery."""
from __future__ import annotations

import json
import subprocess
import sys
from pathlib import Path


_REPO = Path(__file__).resolve().parents[2]
_AUDIT = _REPO / "scripts" / "validate" / "harness_audit.py"
_AGENTS = _REPO / ".claude" / "agents"


def _run_audit_json(tmp_path: Path) -> dict:
    out = tmp_path / "audit.json"
    subprocess.run(
        [sys.executable, str(_AUDIT), "--json-out", str(out)],
        cwd=str(_REPO), capture_output=True, text=True,
        encoding="utf-8", errors="replace", timeout=60,
    )
    return json.loads(out.read_text(encoding="utf-8"))


def test_agent_count_is_positive(tmp_path: Path):
    report = _run_audit_json(tmp_path)
    assert report["agent_count"] >= 1


def test_agent_count_matches_filesystem(tmp_path: Path):
    if not _AGENTS.exists():
        return
    fs_count = sum(1 for _ in _AGENTS.rglob("AGENT.md"))
    report = _run_audit_json(tmp_path)
    assert report["agent_count"] == fs_count, (
        f"harness_audit agent_count={report['agent_count']} != filesystem={fs_count}"
    )


def test_agent_count_in_declared_range():
    """CLAUDE.md declares 32 total agents (Producer 14 + Inspector 17 + Supervisor 1).

    AUDIT-02 permits 12-20 range per REQUIREMENTS.md §AUDIT, but CLAUDE.md
    documents actual 32 — both are acceptable; test just asserts positive + sane.
    """
    if not _AGENTS.exists():
        return
    fs_count = sum(1 for _ in _AGENTS.rglob("AGENT.md"))
    assert 1 <= fs_count <= 100, f"Sanity: agent count {fs_count} outside 1..100"
```
  </action>
  <read_first>
    - .claude/agents/ (filesystem — count AGENT.md files)
    - scripts/validate/harness_audit.py (Plan 07-01 Task 2 — _count_agents helper)
    - .planning/phases/07-integration-test/07-CONTEXT.md (project constraints — 32 agents)
  </read_first>
  <acceptance_criteria>
    - `grep -c "agent_count" tests/phase07/test_agent_count_invariant.py` outputs `>= 3`
    - `grep -c "rglob.*AGENT.md" tests/phase07/test_agent_count_invariant.py` outputs `>= 2`
    - `python -m pytest tests/phase07/test_agent_count_invariant.py -q --no-cov` exits 0
    - `grep -cE "^def test_" tests/phase07/test_agent_count_invariant.py` outputs `>= 3`
  </acceptance_criteria>
  <automated>python -m pytest tests/phase07/test_agent_count_invariant.py -q --no-cov</automated>
  <task_type>impl</task_type>
</task>

<task id="7-07-06">
  <action>
Create `tests/phase07/test_description_1024_scan.py` (~50 lines):

```python
"""AGENT-09: description yaml field ≤ 1024 chars across all AGENT.md."""
from __future__ import annotations

import json
import re
import subprocess
import sys
from pathlib import Path


_REPO = Path(__file__).resolve().parents[2]
_AUDIT = _REPO / "scripts" / "validate" / "harness_audit.py"
_AGENTS = _REPO / ".claude" / "agents"


def _run_audit_json(tmp_path: Path) -> dict:
    out = tmp_path / "audit.json"
    subprocess.run(
        [sys.executable, str(_AUDIT), "--json-out", str(out)],
        cwd=str(_REPO), capture_output=True, text=True,
        encoding="utf-8", errors="replace", timeout=60,
    )
    return json.loads(out.read_text(encoding="utf-8"))


def test_description_over_1024_is_empty(tmp_path: Path):
    report = _run_audit_json(tmp_path)
    assert report["description_over_1024"] == [], (
        f"AGENT-09 violated: descriptions over 1024 chars: {report['description_over_1024']}"
    )


def test_filesystem_description_scan_agrees():
    if not _AGENTS.exists():
        return
    desc_rx = re.compile(r"^description:\s*(.*)$", re.MULTILINE)
    offenders = []
    for p in _AGENTS.rglob("AGENT.md"):
        text = p.read_text(encoding="utf-8", errors="ignore")
        m = desc_rx.search(text)
        if m and len(m.group(1).strip()) > 1024:
            offenders.append(str(p))
    assert offenders == [], f"AGENT-09 violated by filesystem scan: {offenders}"


def test_at_least_one_agent_has_description():
    """Sanity: some AGENT.md must have a description field at all."""
    if not _AGENTS.exists():
        return
    desc_rx = re.compile(r"^description:\s*(.*)$", re.MULTILINE)
    with_desc = 0
    for p in _AGENTS.rglob("AGENT.md"):
        text = p.read_text(encoding="utf-8", errors="ignore")
        if desc_rx.search(text):
            with_desc += 1
    # At least one agent must have description:
    if with_desc == 0:
        # Not blocking — some projects put description in other frontmatter.
        # Just assert no crash.
        return
    assert with_desc > 0
```

Finally, after Task 07-07-06 is committed, run the regression sweep to confirm no Phase 7 test broke Phase 4/5/6:

```
python -m pytest tests/phase04 tests/phase05 tests/phase06 tests/phase07 -q --no-cov
```
  </action>
  <read_first>
    - scripts/validate/harness_audit.py (Plan 07-01 Task 2 — _descriptions_over_1024 helper)
    - .claude/agents/ (filesystem scan)
    - .planning/phases/07-integration-test/07-CONTEXT.md (AGENT-09 rule — description ≤ 1024)
  </read_first>
  <acceptance_criteria>
    - `grep -c "description_over_1024" tests/phase07/test_description_1024_scan.py` outputs `>= 2`
    - `grep -c "== \[\]" tests/phase07/test_description_1024_scan.py` outputs `>= 2`
    - `grep -c "1024" tests/phase07/test_description_1024_scan.py` outputs `>= 2`
    - `python -m pytest tests/phase07/test_description_1024_scan.py -q --no-cov` exits 0
    - `grep -cE "^def test_" tests/phase07/test_description_1024_scan.py` outputs `>= 3`
    - `python -m pytest tests/phase04 tests/phase05 tests/phase06 tests/phase07 -q --no-cov` exits 0 (full regression)
  </acceptance_criteria>
  <automated>python -m pytest tests/phase07/test_description_1024_scan.py tests/phase04 tests/phase05 tests/phase06 -q --no-cov</automated>
  <task_type>impl</task_type>
</task>

</tasks>

<verification>
1. All 6 JSON schema keys present + correctly typed.
2. score >= 80 on current codebase.
3. skill_over_500_lines == [].
4. a_rank_drift_count == 0; all deprecated patterns match 0.
5. agent_count matches filesystem.
6. description_over_1024 == [].
7. Phase 7 tests themselves do NOT re-introduce deprecated pattern matches.
8. Full regression (Phase 4/5/6 + Phase 7) green.
</verification>

<success_criteria>
Plan 07-07 is COMPLETE when:
- AUDIT-02 / SC5 locked via 6 focused test files.
- harness_audit --json-out output fully validated.
- A급 drift proven 0 after Phase 7 tests authored (Waves 0-3 verified not to introduce drift).
- Full regression 809 + Phase 7 suite green.
</success_criteria>

<output>
Create `.planning/phases/07-integration-test/07-07-SUMMARY.md` documenting:
- 6 test files created (~330 lines total)
- Actual harness_audit JSON output sample (all 6 keys + phase + timestamp)
- Current score on codebase
- All deprecated_pattern_matches values
- SKILL.md line counts (all ≤ 500)
- Full regression sweep result
- Commit hash
</output>
