---
plan: 07-08
phase: 7
wave: 5
depends_on: [07-01, 07-02, 07-03, 07-04, 07-05, 07-06, 07-07]
files_modified:
  - scripts/validate/phase07_acceptance.py
  - tests/phase07/test_phase07_acceptance.py
  - tests/phase07/test_traceability_matrix.py
  - tests/phase07/test_regression_809_green.py
  - .planning/phases/07-integration-test/07-TRACEABILITY.md
  - .planning/phases/07-integration-test/07-VALIDATION.md
files_read_first:
  - .planning/phases/07-integration-test/07-CONTEXT.md
  - .planning/phases/07-integration-test/07-RESEARCH.md
  - .planning/phases/07-integration-test/07-VALIDATION.md
  - scripts/validate/phase05_acceptance.py
  - scripts/validate/phase06_acceptance.py
  - .planning/phases/06-wiki-notebooklm-integration-failures-reservoir/06-TRACEABILITY.md
  - .planning/phases/06-wiki-notebooklm-integration-failures-reservoir/06-VALIDATION.md
autonomous: true
requirements_addressed: [TEST-01, TEST-02, TEST-03, TEST-04, AUDIT-02]
success_criteria: [SC1, SC2, SC3, SC4, SC5]
estimated_commits: 4
parallel_boundary: sequential (final gate)

must_haves:
  truths:
    - "scripts/validate/phase07_acceptance.py exists as subprocess CLI wrapper mirroring phase06_acceptance.py pattern"
    - "phase07_acceptance.py exits 0 when all 5 SC groups (SC1..SC5) report PASS"
    - "phase07_acceptance.py uses encoding='utf-8' + errors='replace' (Phase 6 precedent + Pitfall 4)"
    - "tests/phase07/test_phase07_acceptance.py invokes phase07_acceptance.py via subprocess and asserts exit 0 + all 5 SC PASS markers"
    - "tests/phase07/test_traceability_matrix.py asserts 07-TRACEABILITY.md contains all 4 REQ IDs (TEST-01..04) + AUDIT-02 + all 5 SC references"
    - "tests/phase07/test_regression_809_green.py runs pytest tests/phase04 tests/phase05 tests/phase06 via subprocess and asserts exit 0"
    - "07-TRACEABILITY.md maps each of 5 REQs to source file(s) + test file(s) + SC reference, matching 06-TRACEABILITY.md layout"
    - "07-VALIDATION.md frontmatter flipped: status=complete, nyquist_compliant=true, wave_0_complete=true, completed=<EXECUTION_DATE>"
    - "All 24 rows in 07-VALIDATION.md Per-Task Verification Map table transitioned from '⬜ pending' to '✅ green'"
  artifacts:
    - path: "scripts/validate/phase07_acceptance.py"
      provides: "SC1-5 CLI aggregator mirroring phase06_acceptance.py"
      min_lines: 80
    - path: "tests/phase07/test_phase07_acceptance.py"
      provides: "E2E wrapper asserts acceptance script exit 0 + all 5 SC pass"
      min_lines: 80
    - path: "tests/phase07/test_traceability_matrix.py"
      provides: "Automated REQ/SC coverage guard for 07-TRACEABILITY.md"
      min_lines: 70
    - path: "tests/phase07/test_regression_809_green.py"
      provides: "D-23: Phase 4+5+6 regression baseline preserved end-to-end"
      min_lines: 40
    - path: ".planning/phases/07-integration-test/07-TRACEABILITY.md"
      provides: "5-REQ × source × test × SC matrix (Phase 6 format)"
      min_lines: 60
    - path: ".planning/phases/07-integration-test/07-VALIDATION.md"
      provides: "Frontmatter flipped to complete + 24 rows green + Completion Summary appended"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "tests/phase07/test_phase07_acceptance.py"
      to: "scripts/validate/phase07_acceptance.py"
      via: "subprocess.run(..., encoding='utf-8', errors='replace') asserting exit 0 + SC1..SC5 PASS"
      pattern: "phase07_acceptance\\.py"
    - from: ".planning/phases/07-integration-test/07-TRACEABILITY.md"
      to: "tests/phase07/test_traceability_matrix.py"
      via: "traceability matrix file referenced by test_traceability_matrix for REQ ID coverage check"
      pattern: "07-TRACEABILITY\\.md"
---

<objective>
Wave 5 PHASE GATE — Close Phase 7. Build `scripts/validate/phase07_acceptance.py` (SC1-5 subprocess aggregator mirroring the Phase 6 pattern), add 3 phase-level tests (acceptance wrapper + traceability matrix + regression 809), author `07-TRACEABILITY.md` (5 REQs × source × test × SC matrix), and flip `07-VALIDATION.md` frontmatter to `status=complete / nyquist_compliant=true / wave_0_complete=true` with Completion Summary. This plan ships verification artifacts only — no new production code.

Purpose: Phase 7 is officially shippable when this plan's tests are green. `/gsd:verify-work 07` can then run with confidence. Direct analogue of Phase 5 Plan 05-10 and Phase 6 Plan 06-11.

Output: 1 acceptance CLI + 3 test files + 1 traceability matrix + 1 frontmatter flip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-integration-test/07-CONTEXT.md
@.planning/phases/07-integration-test/07-RESEARCH.md
@.planning/phases/07-integration-test/07-VALIDATION.md
@scripts/validate/phase05_acceptance.py
@scripts/validate/phase06_acceptance.py
@.planning/phases/06-wiki-notebooklm-integration-failures-reservoir/06-TRACEABILITY.md
@.planning/phases/06-wiki-notebooklm-integration-failures-reservoir/06-VALIDATION.md

<interfaces>
## Phase 7 SC → Test mapping (canonical)

| SC | Description | Primary Test File(s) |
|----|-------------|----------------------|
| SC1 | E2E mock pipeline 1회 완주 + $0 비용 | test_e2e_happy_path.py + test_notebooklm_tier2_only.py |
| SC2 | verify_all_dispatched 13 operational gate | test_operational_gate_count_equals_13.py + test_verify_all_dispatched_13.py + test_gate_order_violation.py + test_checkpointer_atomic_writes_13.py |
| SC3 | CircuitBreaker 3회 발동 + 300s cooldown | test_circuit_breaker_3x_open.py + test_cooldown_300s_enforced.py |
| SC4 | Fallback ken-burns no CIRCUIT_OPEN | test_fallback_ken_burns_thumbnail.py + test_failures_append_on_retry_exceeded.py |
| SC5 | harness-audit ≥ 80 + drift 0 + SKILL 500줄 0 | test_harness_audit_json_schema.py + test_harness_audit_score_ge_80.py + test_skill_500_line_scan.py + test_a_rank_drift_zero.py + test_agent_count_invariant.py + test_description_1024_scan.py |

## Phase 7 REQ → SC mapping (ROADMAP + REQUIREMENTS cross-ref)

- TEST-01 → SC1 (E2E mock)
- TEST-02 → SC2 (verify_all_dispatched 13 gates)
- TEST-03 → SC3 (CircuitBreaker)
- TEST-04 → SC4 (Fallback ken-burns)
- AUDIT-02 → SC5 (harness-audit cross-cutting)

## phase06_acceptance.py pattern (structural precedent)

```python
REPO = Path(__file__).resolve().parents[2]

def _run(cmd: list[str], cwd: Path = REPO) -> tuple[int, str, str]:
    p = subprocess.run(cmd, cwd=str(cwd), capture_output=True, text=True,
                       encoding="utf-8", errors="replace", timeout=120)
    return p.returncode, p.stdout, p.stderr

def main() -> int:
    rc1, _, _ = _run(["python", "-m", "pytest", "tests/phase07/test_e2e_happy_path.py", "-q", "--no-cov"])
    sc1 = "PASS" if rc1 == 0 else "FAIL"
    # ... etc. 5 SCs.
    print("| SC | Status |")
    print("|----|--------|")
    for name, status in [("SC1", sc1), ...]:
        print(f"| {name} | {status} |")
    return 0 if all(s == "PASS" for s in [sc1, sc2, sc3, sc4, sc5]) else 1
```

## 07-VALIDATION.md frontmatter transition

Before (current):
```yaml
---
phase: 7
slug: integration-test
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-19
---
```

After:
```yaml
---
phase: 7
slug: integration-test
status: complete
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-19
completed: <EXECUTION_DATE>
---
```

Per-task table (24 rows) — flip every `⬜ pending` to `✅ green` + flip File Exists `❌ W0` to `✅`.
</interfaces>
</context>

<tasks>

<task id="7-08-01">
  <action>
Create `scripts/validate/phase07_acceptance.py` (~110 lines) — SC1-5 CLI aggregator mirroring `scripts/validate/phase06_acceptance.py`:

```python
#!/usr/bin/env python3
"""Phase 7 Success Criteria 1-5 acceptance verifier.

Mirrors scripts/validate/phase05_acceptance.py and phase06_acceptance.py:
subprocess invokes each SC's primary pytest target, collects PASS/FAIL, prints
a markdown table, returns 0 iff all 5 SCs PASS.

SC mapping (canonical):
  SC1: E2E mock + NotebookLM tier-2-only   → test_e2e_happy_path.py + test_notebooklm_tier2_only.py
  SC2: 13 operational gates + dispatch     → test_verify_all_dispatched_13.py + sibling
  SC3: CircuitBreaker 3× + 300s cooldown   → test_circuit_breaker_3x_open.py + test_cooldown_300s_enforced.py
  SC4: Fallback ken-burns no CIRCUIT_OPEN  → test_fallback_ken_burns_thumbnail.py + sibling
  SC5: harness-audit ≥ 80 + drift 0        → harness_audit.py + test_harness_audit_*.py
"""
from __future__ import annotations

import subprocess
import sys
from pathlib import Path

REPO = Path(__file__).resolve().parents[2]


def _run(cmd: list[str], cwd: Path = REPO, timeout: int = 180) -> tuple[int, str, str]:
    try:
        p = subprocess.run(
            cmd,
            cwd=str(cwd),
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=timeout,
        )
    except FileNotFoundError as e:
        return 2, "", str(e)
    except subprocess.TimeoutExpired as e:
        return 3, "", f"TIMEOUT: {e}"
    return p.returncode, p.stdout, p.stderr


def main() -> int:
    # SC1: E2E mock pipeline + NotebookLM tier-2-only
    rc1a, _, _ = _run([sys.executable, "-m", "pytest",
                       "tests/phase07/test_e2e_happy_path.py", "-q", "--no-cov"])
    rc1b, _, _ = _run([sys.executable, "-m", "pytest",
                       "tests/phase07/test_notebooklm_tier2_only.py", "-q", "--no-cov"])
    sc1 = "PASS" if rc1a == 0 and rc1b == 0 else "FAIL"

    # SC2: 13 operational gates
    rc2, _, _ = _run([sys.executable, "-m", "pytest",
                      "tests/phase07/test_operational_gate_count_equals_13.py",
                      "tests/phase07/test_verify_all_dispatched_13.py",
                      "tests/phase07/test_gate_order_violation.py",
                      "tests/phase07/test_checkpointer_atomic_writes_13.py",
                      "-q", "--no-cov"])
    sc2 = "PASS" if rc2 == 0 else "FAIL"

    # SC3: CircuitBreaker 3× + cooldown
    rc3, _, _ = _run([sys.executable, "-m", "pytest",
                      "tests/phase07/test_circuit_breaker_3x_open.py",
                      "tests/phase07/test_cooldown_300s_enforced.py",
                      "-q", "--no-cov"])
    sc3 = "PASS" if rc3 == 0 else "FAIL"

    # SC4: Fallback ken-burns + FAILURES append
    rc4, _, _ = _run([sys.executable, "-m", "pytest",
                      "tests/phase07/test_fallback_ken_burns_thumbnail.py",
                      "tests/phase07/test_failures_append_on_retry_exceeded.py",
                      "-q", "--no-cov"])
    sc4 = "PASS" if rc4 == 0 else "FAIL"

    # SC5: harness-audit ≥ 80 + drift 0 + SKILL ≤ 500 + agent count + description ≤ 1024
    rc5a, _, _ = _run([sys.executable, "scripts/validate/harness_audit.py",
                       "--threshold", "80"])
    rc5b, _, _ = _run([sys.executable, "-m", "pytest",
                       "tests/phase07/test_harness_audit_json_schema.py",
                       "tests/phase07/test_harness_audit_score_ge_80.py",
                       "tests/phase07/test_skill_500_line_scan.py",
                       "tests/phase07/test_a_rank_drift_zero.py",
                       "tests/phase07/test_agent_count_invariant.py",
                       "tests/phase07/test_description_1024_scan.py",
                       "-q", "--no-cov"])
    sc5 = "PASS" if rc5a == 0 and rc5b == 0 else "FAIL"

    try:
        sys.stdout.reconfigure(encoding="utf-8")  # Windows cp949 safety (Pitfall 4)
    except Exception:
        pass

    print("| SC | Status | Coverage |")
    print("|----|--------|----------|")
    print(f"| SC1: E2E mock + NotebookLM tier-2-only | {sc1} | TEST-01 |")
    print(f"| SC2: verify_all_dispatched 13 operational gates | {sc2} | TEST-02 |")
    print(f"| SC3: CircuitBreaker 3x + 300s cooldown | {sc3} | TEST-03 |")
    print(f"| SC4: Fallback ken-burns no CIRCUIT_OPEN | {sc4} | TEST-04 |")
    print(f"| SC5: harness-audit >= 80 + drift 0 + SKILL <= 500 | {sc5} | AUDIT-02 |")

    all_pass = all(s == "PASS" for s in (sc1, sc2, sc3, sc4, sc5))
    return 0 if all_pass else 1


if __name__ == "__main__":
    sys.exit(main())
```

Create `tests/phase07/test_phase07_acceptance.py` (~100 lines):

```python
"""E2E wrapper asserting phase07_acceptance.py exit 0 + all 5 SC PASS."""
from __future__ import annotations

import subprocess
import sys
from pathlib import Path


_REPO = Path(__file__).resolve().parents[2]
_ACCEPT = _REPO / "scripts" / "validate" / "phase07_acceptance.py"


def test_acceptance_script_exists():
    assert _ACCEPT.exists(), f"phase07_acceptance.py missing: {_ACCEPT}"


def test_acceptance_script_valid_python():
    result = subprocess.run(
        [sys.executable, "-m", "py_compile", str(_ACCEPT)],
        capture_output=True, text=True, timeout=20,
        encoding="utf-8", errors="replace",
    )
    assert result.returncode == 0, f"Syntax error: {result.stderr}"


def test_acceptance_exits_zero():
    """All 5 SCs must PASS."""
    result = subprocess.run(
        [sys.executable, str(_ACCEPT)],
        cwd=str(_REPO), capture_output=True, text=True,
        encoding="utf-8", errors="replace", timeout=600,
    )
    if result.returncode != 0:
        # Print tail for diagnostic.
        print("=== STDOUT ===")
        print(result.stdout)
        print("=== STDERR ===")
        print(result.stderr[-2000:] if result.stderr else "")
    assert result.returncode == 0, (
        f"phase07_acceptance.py exit {result.returncode}"
    )


def test_acceptance_output_contains_all_5_sc_labels():
    result = subprocess.run(
        [sys.executable, str(_ACCEPT)],
        cwd=str(_REPO), capture_output=True, text=True,
        encoding="utf-8", errors="replace", timeout=600,
    )
    for sc in ("SC1:", "SC2:", "SC3:", "SC4:", "SC5:"):
        assert sc in result.stdout, f"{sc} missing from acceptance output:\n{result.stdout}"


def test_acceptance_all_sc_marked_pass():
    result = subprocess.run(
        [sys.executable, str(_ACCEPT)],
        cwd=str(_REPO), capture_output=True, text=True,
        encoding="utf-8", errors="replace", timeout=600,
    )
    pass_rows = sum(1 for line in result.stdout.splitlines()
                    if line.startswith("|") and "PASS" in line)
    assert pass_rows >= 5, (
        f"Only {pass_rows} SC rows PASS, expected >=5\n{result.stdout}"
    )


def test_full_phase07_suite_green_excluding_wrapper():
    """Regression: tests/phase07/ all green (excluding this wrapper to avoid recursion)."""
    result = subprocess.run(
        [sys.executable, "-m", "pytest", "tests/phase07/", "-q", "--no-cov",
         "--ignore=tests/phase07/test_phase07_acceptance.py"],
        cwd=str(_REPO), capture_output=True, text=True,
        encoding="utf-8", errors="replace", timeout=600,
    )
    if result.returncode != 0:
        print(result.stdout[-3000:])
        print(result.stderr[-1500:])
    assert result.returncode == 0, "tests/phase07/ has failing tests"


def test_phase_4_5_6_still_green():
    """D-23 regression: Phase 4/5/6 baseline preserved."""
    result = subprocess.run(
        [sys.executable, "-m", "pytest",
         "tests/phase04", "tests/phase05", "tests/phase06",
         "-q", "--no-cov"],
        cwd=str(_REPO), capture_output=True, text=True,
        encoding="utf-8", errors="replace", timeout=600,
    )
    assert result.returncode == 0, (
        f"Phase 4/5/6 regression — Phase 7 work broke prior phases\n"
        f"{result.stdout[-2000:]}\n{result.stderr[-1000:]}"
    )
```
  </action>
  <read_first>
    - scripts/validate/phase05_acceptance.py (structural precedent)
    - scripts/validate/phase06_acceptance.py (Phase 6 mirror — UTF-8 reconfigure + exit codes)
    - .planning/phases/07-integration-test/07-RESEARCH.md §Example 5 lines 863-922
    - .planning/phases/07-integration-test/07-CONTEXT.md §D-23
  </read_first>
  <acceptance_criteria>
    - `test -f scripts/validate/phase07_acceptance.py` exits 0
    - `grep -c "SC1:\|SC2:\|SC3:\|SC4:\|SC5:" scripts/validate/phase07_acceptance.py` outputs `>= 5`
    - `grep -c "encoding=\"utf-8\"" scripts/validate/phase07_acceptance.py` outputs `>= 1`
    - `grep -c "errors=\"replace\"" scripts/validate/phase07_acceptance.py` outputs `>= 1`
    - `python -m py_compile scripts/validate/phase07_acceptance.py` exits 0
    - `python -m pytest tests/phase07/test_phase07_acceptance.py -q --no-cov` exits 0
    - `grep -cE "^def test_" tests/phase07/test_phase07_acceptance.py` outputs `>= 6`
    - `python scripts/validate/phase07_acceptance.py` exits 0 (all 5 SC PASS)
  </acceptance_criteria>
  <automated>python -m pytest tests/phase07/test_phase07_acceptance.py -q --no-cov</automated>
  <task_type>impl</task_type>
</task>

<task id="7-08-02">
  <action>
Create `.planning/phases/07-integration-test/07-TRACEABILITY.md` (~100 lines) — 5-REQ matrix mirroring `.planning/phases/06-wiki-notebooklm-integration-failures-reservoir/06-TRACEABILITY.md`:

```markdown
# Phase 7 REQ Traceability Matrix

Every REQ ID from REQUIREMENTS.md §TEST (TEST-01..04) + §AUDIT (AUDIT-02 cross-cutting) MUST appear below with at least one passing test file.

## Matrix

| REQ ID | Spec (short) | Primary Source File(s) | Primary Test File(s) | SC Acceptance |
|--------|--------------|-------------------------|----------------------|---------------|
| TEST-01 | E2E mock asset 파이프라인 1회 성공 ($0 비용) | scripts/orchestrator/shorts_pipeline.py + 5 api/*.py adapters | tests/phase07/test_e2e_happy_path.py + tests/phase07/test_notebooklm_tier2_only.py | SC1 |
| TEST-02 | verify_all_dispatched() 13 operational gate lock (Correction 1 — NOT 17) | scripts/orchestrator/gate_guard.py:169-176 + scripts/orchestrator/gates.py + scripts/orchestrator/shorts_pipeline.py | tests/phase07/test_operational_gate_count_equals_13.py + test_verify_all_dispatched_13.py + test_gate_order_violation.py + test_checkpointer_atomic_writes_13.py | SC2 |
| TEST-03 | CircuitBreaker 3회 발동 + 300s cooldown (Correction 2 — CircuitBreakerOpenError, not *TriggerError) | scripts/orchestrator/circuit_breaker.py:57-72, 108-150 | tests/phase07/test_circuit_breaker_3x_open.py + test_cooldown_300s_enforced.py | SC3 |
| TEST-04 | Fallback 정지 이미지 + 줌인 (Correction 3 — THUMBNAIL target, ken-burns standalone POST) | scripts/orchestrator/shorts_pipeline.py:576-655 + scripts/orchestrator/fallback.py:30-122 + scripts/orchestrator/api/shotstack.py:155-216 | tests/phase07/test_fallback_ken_burns_thumbnail.py + test_failures_append_on_retry_exceeded.py | SC4 |
| AUDIT-02 | harness-audit 점수 ≥ 80 + A급 drift 0 + SKILL 500줄 0 | scripts/validate/harness_audit.py (extended Plan 07-01 with --json-out) | tests/phase07/test_harness_audit_json_schema.py + test_harness_audit_score_ge_80.py + test_skill_500_line_scan.py + test_a_rank_drift_zero.py + test_agent_count_invariant.py + test_description_1024_scan.py | SC5 |

**Coverage check:** 5/5 REQs mapped, all 5 SC covered, no orphans.

## Success Criteria → Primary Tests

- **SC1** (E2E mock $0): test_e2e_happy_path.py + test_notebooklm_tier2_only.py
- **SC2** (verify_all_dispatched 13 ops): test_operational_gate_count_equals_13.py + test_verify_all_dispatched_13.py + test_gate_order_violation.py + test_checkpointer_atomic_writes_13.py
- **SC3** (CircuitBreaker 5-min cooldown): test_circuit_breaker_3x_open.py + test_cooldown_300s_enforced.py
- **SC4** (Fallback ken-burns no CIRCUIT_OPEN): test_fallback_ken_burns_thumbnail.py + test_failures_append_on_retry_exceeded.py
- **SC5** (harness-audit ≥ 80 + drift 0 + SKILL ≤ 500 lines): 6 test files under Plan 07-07 + `scripts/validate/harness_audit.py --threshold 80` subprocess

## Research Corrections Honored

| # | CONTEXT assertion | Actual code | Phase 7 test anchor |
|---|---|---|---|
| 1 | "17 GATE = 12 + 5 sub-gate" | `_OPERATIONAL_GATES` = 13 (gate_guard.py:94-96) | test_operational_gate_count_equals_13.py |
| 2 | `CircuitBreakerTriggerError` | `CircuitBreakerOpenError` (circuit_breaker.py:57) | test_circuit_breaker_3x_open.py::test_circuit_breaker_trigger_error_does_NOT_exist |
| 3 | ASSETS ken-burns via embedded filter | THUMBNAIL via standalone Shotstack POST (shotstack.py:155-216) | test_fallback_ken_burns_thumbnail.py |

## Plan Summary

| Plan | Tasks | Artifacts | Gate |
|------|-------|-----------|------|
| 07-01 | 3 | tests/phase07/ scaffold + 6 fixtures + harness_audit --json-out ext | Wave 0 |
| 07-02 | 5 | 5 mock adapters + 5 per-mock unit tests | Wave 1 |
| 07-03 | 2 | test_e2e_happy_path.py + test_notebooklm_tier2_only.py | Wave 2 |
| 07-04 | 4 | 4 TEST-02 dimension tests | Wave 2 |
| 07-05 | 2 | TEST-03 CircuitBreaker 3x + cooldown tests | Wave 3 |
| 07-06 | 2 | TEST-04 ken-burns THUMBNAIL + FAILURES append tests | Wave 3 |
| 07-07 | 6 | 6 harness-audit dimension tests (JSON, score, SKILL, drift, agents, desc) | Wave 4 |
| 07-08 | 3 | phase07_acceptance.py + 3 gate tests + 07-TRACEABILITY.md + 07-VALIDATION.md flip | Wave 5 gate |

Generated: <EXECUTION_DATE>
Phase 7 status: shippable when all listed tests are green.
```

Create `tests/phase07/test_traceability_matrix.py` (~90 lines):

```python
"""Automated REQ/SC coverage guard for 07-TRACEABILITY.md."""
from __future__ import annotations

import re
from pathlib import Path


_REPO = Path(__file__).resolve().parents[2]
TESTS_DIR = _REPO / "tests" / "phase07"
MATRIX = _REPO / ".planning" / "phases" / "07-integration-test" / "07-TRACEABILITY.md"


PHASE7_REQS = ["TEST-01", "TEST-02", "TEST-03", "TEST-04", "AUDIT-02"]
PHASE7_SCS = ["SC1", "SC2", "SC3", "SC4", "SC5"]


REQ_TO_TEST_MARKERS: dict[str, list[str]] = {
    "TEST-01": ["test_e2e_happy_path", "test_notebooklm_tier2_only"],
    "TEST-02": ["test_operational_gate_count_equals_13",
                "test_verify_all_dispatched_13",
                "test_gate_order_violation",
                "test_checkpointer_atomic_writes_13"],
    "TEST-03": ["test_circuit_breaker_3x_open", "test_cooldown_300s_enforced"],
    "TEST-04": ["test_fallback_ken_burns_thumbnail",
                "test_failures_append_on_retry_exceeded"],
    "AUDIT-02": ["test_harness_audit_json_schema",
                 "test_harness_audit_score_ge_80",
                 "test_skill_500_line_scan",
                 "test_a_rank_drift_zero",
                 "test_agent_count_invariant",
                 "test_description_1024_scan"],
}


def _test_stems() -> set[str]:
    return {p.stem for p in TESTS_DIR.glob("test_*.py")}


def test_traceability_md_exists():
    assert MATRIX.exists(), f"07-TRACEABILITY.md missing: {MATRIX}"


def test_every_req_present_in_matrix():
    content = MATRIX.read_text(encoding="utf-8")
    for req in PHASE7_REQS:
        assert req in content, f"{req} missing from 07-TRACEABILITY.md"


def test_every_sc_present_in_matrix():
    content = MATRIX.read_text(encoding="utf-8")
    for sc in PHASE7_SCS:
        # SC markers in matrix body.
        assert sc in content, f"{sc} missing from 07-TRACEABILITY.md"


def test_every_req_has_at_least_one_test_file():
    stems = _test_stems()
    uncovered = []
    for req, markers in REQ_TO_TEST_MARKERS.items():
        found = [m for m in markers if any(m in s for s in stems)]
        if not found:
            uncovered.append(req)
    assert uncovered == [], f"UNCOVERED REQs (no matching test file): {uncovered}"


def test_correction_markers_present():
    """RESEARCH Corrections 1/2/3 MUST be acknowledged in traceability."""
    content = MATRIX.read_text(encoding="utf-8")
    assert "Correction 1" in content or "13 operational" in content
    assert "Correction 2" in content or "CircuitBreakerOpenError" in content
    assert "Correction 3" in content or "THUMBNAIL" in content


def test_plan_summary_lists_all_8_plans():
    content = MATRIX.read_text(encoding="utf-8")
    for plan_id in ("07-01", "07-02", "07-03", "07-04", "07-05", "07-06", "07-07", "07-08"):
        assert plan_id in content, f"Plan {plan_id} missing from Plan Summary"
```

Create `tests/phase07/test_regression_809_green.py` (~50 lines):

```python
"""D-23 regression invariant: Phase 4 + 5 + 6 baseline preserved end-to-end."""
from __future__ import annotations

import subprocess
import sys
from pathlib import Path


_REPO = Path(__file__).resolve().parents[2]


def test_phase04_green():
    result = subprocess.run(
        [sys.executable, "-m", "pytest", "tests/phase04", "-q", "--no-cov"],
        cwd=str(_REPO), capture_output=True, text=True,
        encoding="utf-8", errors="replace", timeout=600,
    )
    assert result.returncode == 0, (
        f"Phase 4 regression broken.\n{result.stdout[-2000:]}\n{result.stderr[-1000:]}"
    )


def test_phase05_green():
    result = subprocess.run(
        [sys.executable, "-m", "pytest", "tests/phase05", "-q", "--no-cov"],
        cwd=str(_REPO), capture_output=True, text=True,
        encoding="utf-8", errors="replace", timeout=600,
    )
    assert result.returncode == 0, (
        f"Phase 5 regression broken.\n{result.stdout[-2000:]}\n{result.stderr[-1000:]}"
    )


def test_phase06_green():
    result = subprocess.run(
        [sys.executable, "-m", "pytest", "tests/phase06", "-q", "--no-cov"],
        cwd=str(_REPO), capture_output=True, text=True,
        encoding="utf-8", errors="replace", timeout=600,
    )
    assert result.returncode == 0, (
        f"Phase 6 regression broken.\n{result.stdout[-2000:]}\n{result.stderr[-1000:]}"
    )


def test_combined_baseline_passes():
    """D-23: Phase 4 + 5 + 6 combined must stay at 809 green."""
    result = subprocess.run(
        [sys.executable, "-m", "pytest",
         "tests/phase04", "tests/phase05", "tests/phase06", "-q", "--no-cov"],
        cwd=str(_REPO), capture_output=True, text=True,
        encoding="utf-8", errors="replace", timeout=900,
    )
    assert result.returncode == 0, (
        f"Combined regression broken.\n{result.stdout[-2000:]}\n{result.stderr[-1000:]}"
    )
```
  </action>
  <read_first>
    - .planning/phases/06-wiki-notebooklm-integration-failures-reservoir/06-TRACEABILITY.md (layout precedent)
    - .planning/phases/06-wiki-notebooklm-integration-failures-reservoir/06-11-PLAN.md (phase gate plan pattern)
    - .planning/REQUIREMENTS.md §TEST + §AUDIT
    - .planning/ROADMAP.md §Phase 7 (line 171-183)
  </read_first>
  <acceptance_criteria>
    - `test -f .planning/phases/07-integration-test/07-TRACEABILITY.md` exits 0
    - `grep -c "TEST-01\|TEST-02\|TEST-03\|TEST-04\|AUDIT-02" .planning/phases/07-integration-test/07-TRACEABILITY.md` outputs `>= 5`
    - `grep -c "SC1\|SC2\|SC3\|SC4\|SC5" .planning/phases/07-integration-test/07-TRACEABILITY.md` outputs `>= 5`
    - `grep -c "Correction 1\|Correction 2\|Correction 3" .planning/phases/07-integration-test/07-TRACEABILITY.md` outputs `>= 3`
    - `grep -c "07-01\|07-02\|07-03\|07-04\|07-05\|07-06\|07-07\|07-08" .planning/phases/07-integration-test/07-TRACEABILITY.md` outputs `>= 8`
    - `python -m pytest tests/phase07/test_traceability_matrix.py -q --no-cov` exits 0
    - `grep -cE "^def test_" tests/phase07/test_traceability_matrix.py` outputs `>= 5`
    - `python -m pytest tests/phase07/test_regression_809_green.py -q --no-cov` exits 0
    - `grep -cE "^def test_" tests/phase07/test_regression_809_green.py` outputs `>= 4`
  </acceptance_criteria>
  <automated>python -m pytest tests/phase07/test_traceability_matrix.py tests/phase07/test_regression_809_green.py -q --no-cov</automated>
  <task_type>impl</task_type>
</task>

<task id="7-08-03">
  <action>
Edit `.planning/phases/07-integration-test/07-VALIDATION.md` in place. Three transformations:

**1. Frontmatter flip (top of file):**
Change:
```yaml
---
phase: 7
slug: integration-test
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-19
---
```
To:
```yaml
---
phase: 7
slug: integration-test
status: complete
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-19
completed: <EXECUTION_DATE>
---
```
(Fill `<EXECUTION_DATE>` with the actual execution date like `2026-04-19`.)

**2. Per-Task Verification Map table status flip** — for every row in the table (24 rows), change:
- Last column `⬜ pending` → `✅ green`
- `File Exists` column `❌ W0` → `✅`
- Row 18 (Task 7-07-01) `File Exists` was `⚠️ exists (text-only)` → change to `✅ extended with --json-out`

**3. Append Validation Sign-Off + Completion Summary** AT THE END of the file:

```markdown
## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (all 24 rows auto-verified)
- [x] Wave 0 covers all MISSING references (tests/phase07/ infra + harness_audit --json-out extension)
- [x] No watch-mode flags (all pytest one-shot)
- [x] Feedback latency < 15s (Phase 7 suite ~10s, regression sweep ~68s per wave)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** complete

---

## Completion Summary

**Phase 7:** Integration Test — mock asset E2E + CircuitBreaker + Fallback ken-burns + harness-audit ≥ 80
**Status:** ✅ COMPLETE <EXECUTION_DATE>
**Plans:** 8/8
**REQs:** 5/5 (TEST-01..04 + AUDIT-02 cross-cutting)

### Success Criteria Results

| SC | Description | Status |
|----|-------------|--------|
| 1 | E2E mock pipeline + $0 API cost | ✅ PASS |
| 2 | verify_all_dispatched() 13 operational gates (Correction 1 locked) | ✅ PASS |
| 3 | CircuitBreaker 3× fail + 300s cooldown (Correction 2 locked) | ✅ PASS |
| 4 | Fallback ken-burns at THUMBNAIL, no CIRCUIT_OPEN (Correction 3 locked) | ✅ PASS |
| 5 | harness-audit ≥ 80 + A급 drift 0 + SKILL 500줄 0 | ✅ PASS |

### Test Counts

- tests/phase07/: ~30+ test files across Plans 01-08
- tests/phase04/ regression: 244/244 PASS (no impact)
- tests/phase05/ regression: 329/329 PASS (no impact)
- tests/phase06/ regression: 236/236 PASS (no impact)
- Combined baseline preserved: 809/809 green
- phase07_acceptance.py exit 0

### Research Corrections Locked

1. **13 operational gates** (NOT 17) — `test_operational_gate_count_equals_13.py` anchors this invariant.
2. **CircuitBreakerOpenError** (NOT CircuitBreakerTriggerError) — `test_circuit_breaker_3x_open.py::test_circuit_breaker_trigger_error_does_NOT_exist` prevents re-introduction.
3. **THUMBNAIL ken-burns target** (NOT ASSETS) — `test_fallback_ken_burns_thumbnail.py::test_target_is_thumbnail_not_assets` asserts structurally.

### harness-audit Extension

- `scripts/validate/harness_audit.py --json-out PATH` added (backward-compat: legacy `HARNESS_AUDIT_SCORE: N` text output preserved per Pitfall 8).
- D-11 6-key JSON schema emitted: `score`, `a_rank_drift_count`, `skill_over_500_lines`, `agent_count`, `description_over_1024`, `deprecated_pattern_matches` + `phase` + `timestamp` metadata.

### Commit Hashes

(Fill from SUMMARY files per plan: 07-01 ... 07-08 commit hashes)

### Ready for

`/gsd:verify-work 7` then `/gsd:plan-phase 8` (Remote + Publishing + Production Metadata — real YouTube upload).
```

After editing, also run a final regression sweep to confirm the flip itself did not introduce a test failure:

```
python -m pytest tests/phase04 tests/phase05 tests/phase06 tests/phase07 -q --no-cov
python scripts/validate/phase07_acceptance.py
```

Both must exit 0.
  </action>
  <read_first>
    - .planning/phases/07-integration-test/07-VALIDATION.md (current draft state)
    - .planning/phases/06-wiki-notebooklm-integration-failures-reservoir/06-VALIDATION.md (flipped precedent — lines 500+ Completion Summary)
    - .planning/phases/05-orchestrator-v2-write/05-VALIDATION.md (Phase 5 precedent)
  </read_first>
  <acceptance_criteria>
    - `grep -c "status: complete" .planning/phases/07-integration-test/07-VALIDATION.md` outputs `>= 1`
    - `grep -c "nyquist_compliant: true" .planning/phases/07-integration-test/07-VALIDATION.md` outputs `>= 1`
    - `grep -c "wave_0_complete: true" .planning/phases/07-integration-test/07-VALIDATION.md` outputs `>= 1`
    - `grep -c "completed:" .planning/phases/07-integration-test/07-VALIDATION.md` outputs `>= 1`
    - `! grep -q "status: draft" .planning/phases/07-integration-test/07-VALIDATION.md`
    - `! grep -q "nyquist_compliant: false" .planning/phases/07-integration-test/07-VALIDATION.md`
    - `grep -c "⬜ pending" .planning/phases/07-integration-test/07-VALIDATION.md` outputs `0` (all flipped)
    - `grep -c "✅ green" .planning/phases/07-integration-test/07-VALIDATION.md` outputs `>= 20`
    - `grep -c "Completion Summary" .planning/phases/07-integration-test/07-VALIDATION.md` outputs `>= 1`
    - `grep -c "Correction 1\|Correction 2\|Correction 3" .planning/phases/07-integration-test/07-VALIDATION.md` outputs `>= 3`
    - `python -m pytest tests/phase04 tests/phase05 tests/phase06 tests/phase07 -q --no-cov` exits 0
    - `python scripts/validate/phase07_acceptance.py` exits 0 (final gate)
  </acceptance_criteria>
  <automated>python -m pytest tests/phase04 tests/phase05 tests/phase06 tests/phase07 -q --no-cov</automated>
  <task_type>docs</task_type>
</task>

</tasks>

<verification>
1. `scripts/validate/phase07_acceptance.py` exists, UTF-8 safe, exits 0 when all 5 SC PASS.
2. `test_phase07_acceptance.py` invokes the acceptance CLI and asserts PASS on every row.
3. `07-TRACEABILITY.md` covers 5 REQs × sources × tests × SCs + cites 3 Research corrections.
4. `test_traceability_matrix.py` guards REQ orphaning + correction-marker presence.
5. `test_regression_809_green.py` runs Phase 4+5+6 sweep via subprocess and asserts green.
6. `07-VALIDATION.md` frontmatter flipped to `status: complete` / `nyquist_compliant: true` / `wave_0_complete: true`.
7. All 24 Per-Task table rows flipped to ✅ green.
8. Completion Summary appended with 5 SC PASS rows + 3 Research Corrections honored.
9. Final `phase07_acceptance.py` exit 0 + full regression 809 + Phase 7 suite green.
</verification>

<success_criteria>
Plan 07-08 is COMPLETE when:
- Phase 7 is officially shippable: all 5 SC PASS, 5/5 REQs covered, 809/809 regression preserved.
- `/gsd:verify-work 07` can run immediately after this plan merges.
- 07-VALIDATION.md frontmatter flipped to complete.
- Next action: `/gsd:plan-phase 8` (Remote + Publishing).
</success_criteria>

<output>
Create these summary files:

1. `.planning/phases/07-integration-test/07-08-SUMMARY.md` — task-level summary:
   - Files created (1 acceptance CLI + 3 tests + 1 matrix + 1 flip = 6 file updates)
   - phase07_acceptance.py final output (all 5 SC PASS rows)
   - 5-REQ coverage status
   - 07-VALIDATION.md pre-flip vs post-flip diff (status / nyquist_compliant / wave_0_complete)
   - Final regression sweep result (Phase 4+5+6 + Phase 7 green)
   - Commit hash

2. `.planning/phases/07-integration-test/07-SUMMARY.md` — phase-level aggregation of all 8 plan summaries:
   - Total plans: 8/8
   - Total source files created (harness_audit.py extension + phase07_acceptance.py)
   - Total tests green (Phase 7 total + Phase 4+5+6 regression preserved)
   - All 5 REQ IDs with final status
   - 3 Research Corrections locked
   - Outstanding deferrals (Phase 8 smoke tests, Phase 10 monthly cron, etc. — from 07-CONTEXT.md <deferred>)
   - Recommended next action: `/gsd:verify-work 7` then `/gsd:plan-phase 8`
</output>
