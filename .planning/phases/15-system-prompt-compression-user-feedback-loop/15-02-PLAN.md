---
phase: 15-system-prompt-compression-user-feedback-loop
plan: 02
plan_id: "15-02"
type: tdd
wave: 1
depends_on: ["15-01"]
files_modified:
  - scripts/orchestrator/invokers.py
  - tests/adapters/test_invokers_encoding_contract.py
  - tests/phase15/test_encoding_repro.py
autonomous: true
requirements: [SPC-01, SPC-05]
must_haves:
  truths:
    - "`_invoke_claude_cli_once` 가 system_prompt body 를 argv 에 직접 전달하지 않고 tempfile 경유 path 로 전달한다"
    - "10KB+ Korean body 호출 시 rc=1 '프롬프트가 너무 깁니다' 가 재현되지 않는다 (mock 기준)"
    - "tempfile cleanup 은 명시적 finally 블록 + OSError 시 warn + raise 안 함 (CLAUDE.md 금기 #3)"
    - "Phase 11 stdin invariant (user_prompt stdin 경유) 보존"
    - "Phase 12 `_compress_producer_output` invariant 보존 (orthogonal 압축, 간섭 없음)"
  artifacts:
    - path: "scripts/orchestrator/invokers.py"
      provides: "SPC-01 fix — tempfile + `--append-system-prompt-file` argv"
      contains: "tempfile.NamedTemporaryFile"
    - path: "tests/adapters/test_invokers_encoding_contract.py"
      provides: "SPC-05 contract tests — 10 tests, adapter_contract marker"
      min_lines: 200
    - path: "tests/phase15/test_encoding_repro.py"
      provides: "Wave 0 baseline flip — before 2 tests remove, after 2 tests add (file path 검증)"
  key_links:
    - from: "scripts/orchestrator/invokers.py:_invoke_claude_cli_once"
      to: "tempfile.NamedTemporaryFile"
      via: "with block + path arg to subprocess.Popen"
      pattern: "NamedTemporaryFile\\(.*encoding=.utf-8"
    - from: "scripts/orchestrator/invokers.py:_invoke_claude_cli_once finally"
      to: "os.unlink"
      via: "explicit cleanup + OSError warning"
      pattern: "os\\.unlink\\(.*path"
    - from: "tests/adapters/test_invokers_encoding_contract.py"
      to: "pytest.mark.adapter_contract"
      via: "Phase 14 marker"
      pattern: "@pytest\\.mark\\.adapter_contract"
---

<objective>
SPC-01 root-cause fix: `_invoke_claude_cli_once` 의 argv 직접 전달 경로 (10591 chars Korean body → rc=1 "프롬프트가 너무 깁니다") 를 tempfile + `--append-system-prompt-file` 경유로 교체. SPC-05 contract test 10개로 fix 를 고정하고 Phase 11/12 invariant 를 보존합니다.

**Purpose**: Phase 13 live smoke 2026-04-22 attempt 에서 0.09s/rc=1 로 실패한 원인을 완결. 동일 body 가 bash 에서는 성공하는 경로 비대칭을 file-based path 로 우회 — H1/H2/H3/H4 root cause 가 무엇이든 fix 유효.

**Output**: invokers.py 패치 + 10 contract tests (tests/adapters/) + Wave 0 baseline 2 test flip.

**의존**: Plan 15-01 (tests/phase15/conftest.py fixture + SPC-04 flag 확증 로그).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15-system-prompt-compression-user-feedback-loop/15-RESEARCH.md
@.planning/phases/15-system-prompt-compression-user-feedback-loop/15-VALIDATION.md
@.planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/15-01-cli-probe.log
@scripts/orchestrator/invokers.py
@tests/phase11/test_invoker_stdin.py
@tests/adapters/test_kling_i2v_contract.py
@tests/phase15/conftest.py
@tests/phase15/test_encoding_repro.py

<interfaces>
<!-- Before (invokers.py L121~187, Phase 11 baseline) -->

```python
def _invoke_claude_cli_once(system_prompt, user_prompt, json_schema, cli_path, timeout_s=DEFAULT_TIMEOUT_S) -> str:
    cmd = [cli_path, "--print", "--append-system-prompt", system_prompt, "--json-schema", json_schema]
    proc = subprocess.Popen(cmd, stdin=PIPE, stdout=PIPE, stderr=PIPE,
                            text=True, encoding="utf-8", errors="replace")
    stdout, stderr = proc.communicate(input=user_prompt, timeout=timeout_s)
    # ... rc check + RuntimeError ...
    return stdout.strip()
```

<!-- After (Phase 15 SPC-01 fix) — signature unchanged, body uses tempfile -->

```python
import tempfile, os
def _invoke_claude_cli_once(system_prompt, user_prompt, json_schema, cli_path, timeout_s=DEFAULT_TIMEOUT_S) -> str:
    with tempfile.NamedTemporaryFile(
        mode="w", suffix=".md", delete=False,
        encoding="utf-8", newline="\n",
    ) as sys_fp:
        sys_fp.write(system_prompt)
        sys_prompt_path = sys_fp.name
    try:
        cmd = [cli_path, "--print",
               "--append-system-prompt-file", sys_prompt_path,
               "--json-schema", json_schema]
        proc = subprocess.Popen(cmd, stdin=PIPE, stdout=PIPE, stderr=PIPE,
                                text=True, encoding="utf-8", errors="replace")
        stdout, stderr = proc.communicate(input=user_prompt, timeout=timeout_s)
        # ... 기존 rc check + RuntimeError unchanged ...
        return stdout.strip()
    finally:
        try:
            os.unlink(sys_prompt_path)
        except OSError as err:
            logger.warning(
                "[invoker] temp system prompt 파일 삭제 실패 %s (대표님): %s",
                sys_prompt_path, err,
            )
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 15-02-01: RED — test_invokers_encoding_contract.py 10 tests (failing)</name>
  <read_first>
    - tests/phase11/test_invoker_stdin.py (_make_popen_mock + subprocess.Popen patch 패턴)
    - tests/adapters/test_kling_i2v_contract.py (adapter_contract marker 사용 예시)
    - pytest.ini (adapter_contract marker 등록 확인)
    - .planning/phases/15-system-prompt-compression-user-feedback-loop/15-RESEARCH.md §SPC-05 Test cases (10 tests list)
    - scripts/orchestrator/invokers.py (현 상태 — tempfile 없음)
  </read_first>
  <files>
    - tests/adapters/test_invokers_encoding_contract.py
  </files>
  <behavior>
    - 10 test cases (15-RESEARCH §SPC-05):
      1. `test_system_prompt_file_argv_shape` — argv 에 `--append-system-prompt-file` + path (body 아님)
      2. `test_system_prompt_file_content_matches` — argv path 파일 읽으면 system_prompt 내용과 일치
      3. `test_10kb_body_korean_chars_success` — 10KB Korean body + mock rc=0 → 예외 없음
      4. `test_10kb_body_ascii_success` — 10KB ASCII body + mock rc=0 → 예외 없음
      5. `test_temp_file_cleanup_on_success` — 정상 흐름에서 tempfile 삭제됨
      6. `test_temp_file_cleanup_on_timeout` — TimeoutExpired 시에도 cleanup 호출
      7. `test_temp_file_cleanup_on_rc1` — rc=1 RuntimeError 시에도 cleanup 호출
      8. `test_temp_file_utf8_encoding` — 파일이 UTF-8 BOM 없이 작성됨
      9. `test_unlink_oserror_logged_not_raised` — os.unlink OSError → logger.warning + no raise
      10. `test_stdin_unchanged_user_prompt` — user_prompt 는 여전히 stdin (argv 아님, Phase 11 invariant)
    - 모든 test `@pytest.mark.adapter_contract` marker
    - 모든 test mock-only ($0, 실 subprocess 호출 없음)
    - RED 단계: 이 test 들은 **현재 invokers.py 상태에서 실패** 해야 함 (fix 전이니까)
  </behavior>
  <action>
    1. `tests/adapters/test_invokers_encoding_contract.py` 작성 (~230줄):
       ```python
       """SPC-05 — invokers.py tempfile + --append-system-prompt-file contract.
       
       Phase 14 adapter_contract marker 승계. 10 tests, mock-only, $0.
       
       Fixture 재사용: tests/phase15/conftest.py 의 tmp_agent_md + mock_popen
       (via conftest.py resolution — pytest 가 rootdir conftest 를 adapters/ 에도 적용).
       """
       from __future__ import annotations
       import json
       import logging
       import subprocess
       from pathlib import Path
       from unittest.mock import MagicMock, patch
       import pytest
       from scripts.orchestrator.invokers import _invoke_claude_cli_once
       
       pytestmark = pytest.mark.adapter_contract
       
       KOREAN_10KB = "대표님이 원하시는 최고 품질의 영상 제작 지침서입니다. " * 200
       ASCII_10KB = "You are a careful script writer. " * 320
       
       def _make_popen_mock(stdout='{"ok":true}', stderr="", returncode=0):
           proc = MagicMock()
           proc.communicate.return_value = (stdout, stderr)
           proc.returncode = returncode
           return proc
       
       class TestArgvShape:
           def test_system_prompt_file_argv_shape(self, tmp_path):
               with patch("scripts.orchestrator.invokers.subprocess.Popen") as popen_cls:
                   popen_cls.return_value = _make_popen_mock()
                   _invoke_claude_cli_once(
                       system_prompt=KOREAN_10KB, user_prompt="u",
                       json_schema='{}', cli_path="/fake/claude",
                   )
                   argv = popen_cls.call_args.args[0]
                   assert "--append-system-prompt-file" in argv, (
                       "SPC-01 fix 미적용 — body 가 여전히 argv 직접 전달 (대표님)"
                   )
                   idx = argv.index("--append-system-prompt-file")
                   path_arg = argv[idx + 1]
                   # Path 여야 하고, body 내용 자체가 argv 에 없어야 함
                   assert Path(path_arg).suffix == ".md" or len(path_arg) < 300
                   assert KOREAN_10KB[:1000] not in " ".join(argv)
           
           def test_system_prompt_file_content_matches(self):
               captured = {}
               def _capture_popen(cmd, **kw):
                   captured["cmd"] = cmd
                   idx = cmd.index("--append-system-prompt-file")
                   captured["content"] = Path(cmd[idx + 1]).read_text(encoding="utf-8")
                   return _make_popen_mock()
               with patch("scripts.orchestrator.invokers.subprocess.Popen",
                          side_effect=_capture_popen):
                   _invoke_claude_cli_once(
                       system_prompt=KOREAN_10KB, user_prompt="u",
                       json_schema='{}', cli_path="/fake/claude",
                   )
               assert captured["content"] == KOREAN_10KB
       
       class TestLargeBodyHandling:
           def test_10kb_body_korean_chars_success(self):
               with patch("scripts.orchestrator.invokers.subprocess.Popen") as popen_cls:
                   popen_cls.return_value = _make_popen_mock()
                   result = _invoke_claude_cli_once(
                       system_prompt=KOREAN_10KB, user_prompt="u",
                       json_schema='{}', cli_path="/fake/claude",
                   )
                   assert result  # 예외 없이 return
           
           def test_10kb_body_ascii_success(self):
               with patch("scripts.orchestrator.invokers.subprocess.Popen") as popen_cls:
                   popen_cls.return_value = _make_popen_mock()
                   _invoke_claude_cli_once(
                       system_prompt=ASCII_10KB, user_prompt="u",
                       json_schema='{}', cli_path="/fake/claude",
                   )
       
       class TestCleanup:
           def test_temp_file_cleanup_on_success(self):
               paths_seen = []
               def _capture_popen(cmd, **kw):
                   idx = cmd.index("--append-system-prompt-file")
                   paths_seen.append(cmd[idx + 1])
                   return _make_popen_mock()
               with patch("scripts.orchestrator.invokers.subprocess.Popen",
                          side_effect=_capture_popen):
                   _invoke_claude_cli_once(
                       system_prompt="x", user_prompt="u",
                       json_schema='{}', cli_path="/fake/claude",
                   )
               assert paths_seen, "argv 에서 path 미발견"
               assert not Path(paths_seen[0]).exists(), "cleanup 안 됨 (대표님 SPC-01 fix 필요)"
           
           def test_temp_file_cleanup_on_timeout(self):
               paths_seen = []
               def _capture_popen(cmd, **kw):
                   idx = cmd.index("--append-system-prompt-file")
                   paths_seen.append(cmd[idx + 1])
                   proc = MagicMock()
                   proc.communicate.side_effect = subprocess.TimeoutExpired(
                       cmd=cmd[0], timeout=30,
                   )
                   return proc
               with patch("scripts.orchestrator.invokers.subprocess.Popen",
                          side_effect=_capture_popen):
                   with pytest.raises(RuntimeError, match="타임아웃"):
                       _invoke_claude_cli_once(
                           system_prompt="x", user_prompt="u",
                           json_schema='{}', cli_path="/fake/claude",
                       )
               assert paths_seen
               assert not Path(paths_seen[0]).exists(), "timeout 경로에서 cleanup 누락 (대표님)"
           
           def test_temp_file_cleanup_on_rc1(self):
               paths_seen = []
               def _capture_popen(cmd, **kw):
                   idx = cmd.index("--append-system-prompt-file")
                   paths_seen.append(cmd[idx + 1])
                   return _make_popen_mock(returncode=1, stderr="err")
               with patch("scripts.orchestrator.invokers.subprocess.Popen",
                          side_effect=_capture_popen):
                   with pytest.raises(RuntimeError, match="rc=1"):
                       _invoke_claude_cli_once(
                           system_prompt="x", user_prompt="u",
                           json_schema='{}', cli_path="/fake/claude",
                       )
               assert not Path(paths_seen[0]).exists()
       
       class TestEncoding:
           def test_temp_file_utf8_encoding(self):
               captured = {}
               def _capture_popen(cmd, **kw):
                   idx = cmd.index("--append-system-prompt-file")
                   p = Path(cmd[idx + 1])
                   captured["bytes"] = p.read_bytes()
                   return _make_popen_mock()
               with patch("scripts.orchestrator.invokers.subprocess.Popen",
                          side_effect=_capture_popen):
                   _invoke_claude_cli_once(
                       system_prompt=KOREAN_10KB, user_prompt="u",
                       json_schema='{}', cli_path="/fake/claude",
                   )
               assert not captured["bytes"].startswith(b"\xef\xbb\xbf"), "UTF-8 BOM 금지 (대표님)"
               # 한국어 문자가 cp949 아닌 UTF-8 로 직렬화 확인
               assert "대표님".encode("utf-8") in captured["bytes"]
       
       class TestCleanupErrorHandling:
           def test_unlink_oserror_logged_not_raised(self, caplog):
               with patch("scripts.orchestrator.invokers.subprocess.Popen") as popen_cls, \
                    patch("scripts.orchestrator.invokers.os.unlink",
                          side_effect=OSError("Access denied")):
                   popen_cls.return_value = _make_popen_mock()
                   caplog.set_level(logging.WARNING)
                   # OSError 는 삼켜지고 정상 return (CLAUDE.md 금기 #3: warn + continue)
                   result = _invoke_claude_cli_once(
                       system_prompt="x", user_prompt="u",
                       json_schema='{}', cli_path="/fake/claude",
                   )
                   assert result
                   assert any("삭제 실패" in r.message for r in caplog.records), (
                       "unlink OSError 가 warning 으로 로깅 안 됨 (대표님)"
                   )
       
       class TestPhase11Invariant:
           def test_stdin_unchanged_user_prompt(self):
               mock_proc = _make_popen_mock()
               with patch("scripts.orchestrator.invokers.subprocess.Popen",
                          return_value=mock_proc):
                   _invoke_claude_cli_once(
                       system_prompt="s", user_prompt="내_payload_JSON",
                       json_schema='{}', cli_path="/fake/claude",
                   )
                   mock_proc.communicate.assert_called_once()
                   kw = mock_proc.communicate.call_args.kwargs
                   assert kw.get("input") == "내_payload_JSON"
       ```
    2. 실행 (RED 확인): `py -3.11 -m pytest tests/adapters/test_invokers_encoding_contract.py -v --no-cov`
    3. **기대**: 10 tests **fail** (argv 에 `--append-system-prompt-file` 없음 — body 가 직접 있음)
    4. RED phase 커밋: `git add tests/adapters/test_invokers_encoding_contract.py && git commit -m "test(15-02): RED — invokers.py tempfile contract 10 tests"`
  </action>
  <acceptance_criteria>
    - `py -3.11 -m pytest tests/adapters/test_invokers_encoding_contract.py --no-cov -q 2>&1 | grep -cE "[0-9]+ failed"` ≥ 1 (RED = 실패 present)
    - `grep -c "pytestmark = pytest.mark.adapter_contract" tests/adapters/test_invokers_encoding_contract.py` = 1
    - `grep -c "def test_" tests/adapters/test_invokers_encoding_contract.py` = 10
    - `grep -c "대표님" tests/adapters/test_invokers_encoding_contract.py` ≥ 3 (CLAUDE.md 필수 #7)
    - `grep -cE "except:\s*pass|except\s+Exception:\s*pass" tests/adapters/test_invokers_encoding_contract.py` = 0 (금기 #3)
  </acceptance_criteria>
  <verify>
    <automated>py -3.11 -m pytest tests/adapters/test_invokers_encoding_contract.py --no-cov -q --tb=no</automated>
  </verify>
  <done>
    10 contract tests RED 상태 commit — Wave 1 fix (다음 task) 의 측정 기준선 확립.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 15-02-02: GREEN — invokers.py tempfile patch + 10 tests 통과</name>
  <read_first>
    - scripts/orchestrator/invokers.py L121~187 (현재 `_invoke_claude_cli_once`)
    - scripts/orchestrator/invokers.py L1~50 (imports — tempfile + os 추가 필요 여부 확인)
    - .planning/phases/15-system-prompt-compression-user-feedback-loop/15-RESEARCH.md §Fix patch (line 210~270)
    - CLAUDE.md 금기 #3 (try-except 침묵 폴백 금지)
  </read_first>
  <files>
    - scripts/orchestrator/invokers.py
  </files>
  <behavior>
    - `_invoke_claude_cli_once` 를 tempfile 경유로 교체:
      - `tempfile.NamedTemporaryFile(mode="w", suffix=".md", delete=False, encoding="utf-8", newline="\n")` 로 body 를 tempfile 에 write
      - cmd: `[cli_path, "--print", "--append-system-prompt-file", sys_prompt_path, "--json-schema", json_schema]`
      - Popen stdin/stdout/stderr + `text=True, encoding="utf-8", errors="replace"` (Phase 11 invariant 유지)
      - `communicate(input=user_prompt, timeout=timeout_s)` — stdin user_prompt 유지
      - rc / timeout / empty stdout RuntimeError 모두 기존 메시지 포맷 그대로 (대표님 + 존댓말)
      - `finally` 블록: `os.unlink(sys_prompt_path)` + `except OSError as err: logger.warning("[invoker] temp system prompt 파일 삭제 실패 %s (대표님): %s", ...)` (raise 안 함 — 금기 #3: 삼키는 것 아니라 명시적 log + 이유 있는 continue).
    - imports 추가: `import tempfile, os` (이미 있으면 skip).
    - 기존 docstring 업데이트: Phase 15 SPC-01 tempfile 경유 설명 추가, Phase 11 stdin invariant 보존 명시.
    - `_invoke_claude_cli` wrapper (L213~305), Producer/Supervisor `__call__` (L334/L396) 변경 없음 — signature 동일.
    - GREEN 확인: `_invoke_claude_cli_once` 는 path 만 argv 로 넘기므로 10KB 한국어 body 가 argv 를 거치지 않음.
  </behavior>
  <action>
    1. `scripts/orchestrator/invokers.py` 편집:
       - imports (파일 상단 L20 근처): `import tempfile, os` 추가 (os 는 이미 있을 가능성 — 확인 후 없으면 추가)
       - `_invoke_claude_cli_once` 함수 body 를 다음으로 교체:
       ```python
       def _invoke_claude_cli_once(
           system_prompt: str,
           user_prompt: str,
           json_schema: str,
           cli_path: str,
           timeout_s: int = DEFAULT_TIMEOUT_S,
       ) -> str:
           """Run ``claude --print`` via tempfile --append-system-prompt-file + stdin user_prompt.
       
           Phase 15 SPC-01 fix: system_prompt body 를 argv 직접 전달하지 않고
           UTF-8 tempfile 경유 path 로 전달하여 10KB+ Korean body rc=1
           "프롬프트가 너무 깁니다" 를 우회합니다. user_prompt 는 Phase 11 baseline 대로
           stdin 경유 유지 (invariant preserved).
       
           Args:
               system_prompt: AGENT.md body (Phase 4 agent definition).
               user_prompt: JSON-encoded input payload (producer/supervisor).
               json_schema: JSON schema string enforcing structured output.
               cli_path: Absolute path to ``claude`` binary.
               timeout_s: subprocess timeout in seconds.
       
           Returns:
               stdout text (JSON string per ``--json-schema`` contract).
       
           Raises:
               RuntimeError: non-zero exit code, timeout, or stdout empty
                   (Korean-first diagnostic, "대표님" 존댓말).
           """
           with tempfile.NamedTemporaryFile(
               mode="w",
               suffix=".md",
               delete=False,
               encoding="utf-8",
               newline="\n",
           ) as sys_fp:
               sys_fp.write(system_prompt)
               sys_prompt_path = sys_fp.name
           try:
               cmd = [
                   cli_path,
                   "--print",
                   "--append-system-prompt-file", sys_prompt_path,
                   "--json-schema", json_schema,
               ]
               proc = subprocess.Popen(
                   cmd,
                   stdin=subprocess.PIPE,
                   stdout=subprocess.PIPE,
                   stderr=subprocess.PIPE,
                   text=True,
                   encoding="utf-8",
                   errors="replace",
               )
               try:
                   stdout, stderr = proc.communicate(input=user_prompt, timeout=timeout_s)
               except subprocess.TimeoutExpired as err:
                   proc.kill()
                   proc.communicate()
                   raise RuntimeError(
                       f"claude CLI 타임아웃 ({timeout_s}s 초과, 대표님): {err}"
                   ) from err
               if proc.returncode != 0:
                   stderr_tail = (stderr or "")[-500:]
                   raise RuntimeError(
                       f"claude CLI 실패 (rc={proc.returncode}, 대표님): {stderr_tail}"
                   )
               if not stdout or not stdout.strip():
                   raise RuntimeError(
                       "claude CLI stdout 비어있음 — --json-schema 응답 미수신 (대표님)"
                   )
               return stdout.strip()
           finally:
               # 명시적 cleanup (CLAUDE.md 금기 #3: except: pass 금지, warn + continue).
               try:
                   os.unlink(sys_prompt_path)
               except OSError as err:
                   logger.warning(
                       "[invoker] temp system prompt 파일 삭제 실패 %s (대표님): %s",
                       sys_prompt_path, err,
                   )
       ```
    2. Wave 0 baseline flip — `tests/phase15/test_encoding_repro.py`:
       - `test_current_argv_passes_body_direct` → `test_fix_applied_argv_path_not_body`: assertion 반전 (argv 에 body 직접 없음, path 만)
       - `test_current_argv_no_file_path_yet` → `test_append_system_prompt_file_present`: assertion 반전 (flag 존재)
    3. 실행 (GREEN 확인):
       ```
       py -3.11 -m pytest tests/adapters/test_invokers_encoding_contract.py tests/phase15 -v --no-cov
       py -3.11 -m pytest tests/phase11/test_invoker_stdin.py --no-cov -q  # Phase 11 regression
       py -3.11 -m pytest tests/phase091/test_producer_invoker.py --no-cov -q  # Phase 9.1 regression
       ```
    4. GREEN phase 커밋:
       - `git commit scripts/orchestrator/invokers.py -m "feat(15-02): SPC-01 — tempfile --append-system-prompt-file path"`
       - `git commit tests/phase15/test_encoding_repro.py -m "test(15-02): flip baseline to post-fix assertions"`
  </action>
  <acceptance_criteria>
    - `grep -c "tempfile.NamedTemporaryFile" scripts/orchestrator/invokers.py` ≥ 1
    - `grep -c "append-system-prompt-file" scripts/orchestrator/invokers.py` ≥ 1
    - `grep -cE "^\s*cmd\s*=\s*\[.*--append-system-prompt\",\s*system_prompt" scripts/orchestrator/invokers.py` = 0 (직접 전달 경로 제거됨)
    - `grep -c "os.unlink" scripts/orchestrator/invokers.py` ≥ 1
    - `grep -c "temp system prompt 파일 삭제 실패" scripts/orchestrator/invokers.py` ≥ 1 (존댓말 warn message)
    - `py -3.11 -m pytest tests/adapters/test_invokers_encoding_contract.py --no-cov -q 2>&1 | grep -cE "10 passed"` ≥ 1
    - `py -3.11 -m pytest tests/phase15 --no-cov -q 2>&1 | grep -cE "failed"` = 0 또는 "0 failed"
    - `py -3.11 -m pytest tests/phase11/test_invoker_stdin.py --no-cov -q 2>&1 | grep -cE "failed"` = 0 또는 "0 failed"
    - `grep -cE "except\s*:\s*pass|except\s+Exception\s*:\s*pass" scripts/orchestrator/invokers.py` = 0 (금기 #3)
  </acceptance_criteria>
  <verify>
    <automated>py -3.11 -m pytest tests/adapters/test_invokers_encoding_contract.py tests/phase15 tests/phase11/test_invoker_stdin.py tests/phase091/test_producer_invoker.py -v --no-cov --tb=short</automated>
  </verify>
  <done>
    10 contract tests GREEN, Wave 0 baseline 2 tests flipped + pass, Phase 11 / Phase 9.1 regression 0 failures. invokers.py tempfile 경유 경로 live — Wave 3/5 depends_on 해소.
  </done>
</task>

</tasks>

<verification>
Plan 15-02 전체 verify:
```bash
# SPC-01/05 GREEN
py -3.11 -m pytest tests/adapters/test_invokers_encoding_contract.py -v --no-cov
# 10 passed

# Wave 0 baseline flip
py -3.11 -m pytest tests/phase15 -v --no-cov
# 7 passed (4 repro flipped + 3 probe)

# Phase 11 invariant
py -3.11 -m pytest tests/phase11/test_invoker_stdin.py --no-cov -q
# 0 failed

# Phase 9.1 regression (Producer invoker, Supervisor invoker)
py -3.11 -m pytest tests/phase091/ --no-cov -q

# adapter_contract marker gate
py -3.11 -m pytest -m adapter_contract --no-cov -q
# Phase 14 30 + SPC-05 10 = 40 passed
```
</verification>

<success_criteria>
- [ ] invokers.py L121~187 tempfile + --append-system-prompt-file 패치 적용
- [ ] 10 SPC-05 contract tests green (adapter_contract marker)
- [ ] Wave 0 reproducer 2 tests flipped + green (post-fix assertion)
- [ ] Phase 11 test_invoker_stdin.py 0 failures
- [ ] Phase 9.1 tests/phase091/ 0 failures (Producer/Supervisor __call__ signature 보존)
- [ ] CLAUDE.md 금기 #3 준수 — `os.unlink` OSError 명시적 log + no raise (no silent pass)
- [ ] CLAUDE.md 필수 #7 준수 — error/warn 메시지 "대표님" 존댓말
- [ ] pytest -m adapter_contract 40 tests green (Phase 14 30 + Phase 15 10)
</success_criteria>

<output>
완료 후 `.planning/phases/15-system-prompt-compression-user-feedback-loop/15-02-SUMMARY.md`.
- 15-VALIDATION.md rows 15-02-01, 15-02-02 ⬜ → ✅
- ROADMAP.md Phase 15 Plan 02 [ ] → [x]
- Wave 3 (Plan 04 UFL-01~03) + Wave 5 (Plan 06 live retry) unblocked.
</output>
