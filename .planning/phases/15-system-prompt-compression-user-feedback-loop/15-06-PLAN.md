---
phase: 15-system-prompt-compression-user-feedback-loop
plan: 06
plan_id: "15-06"
type: execute
wave: 5
depends_on: ["15-02", "15-03"]
files_modified:
  - .planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/producer_output_<sid>.json
  - .planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/supervisor_output_<sid>.json
  - .planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/smoke_upload_<sid>.json
  - .planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/budget_usage_<sid>.json
  - .planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/smoke_e2e_<sid>.json
  - .planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/15-06-live-run.log
autonomous: false
requirements: [SPC-06]
user_setup:
  - service: claude-cli
    why: "Max 구독 인증 유효 + ANTHROPIC_API_KEY 없음 상태 확인"
    dashboard_config:
      - task: "claude /login 최신 상태 확인"
        location: "Max 구독 계정"
  - service: youtube-data-api
    why: "과금 환경 실 업로드 + 자동 cleanup"
    env_vars:
      - name: config/client_secret.json
        source: "Google Cloud Console OAuth credentials"
      - name: config/youtube_token.json
        source: "첫 로그인 후 자동 생성 (Phase 8 Plan 06 ready)"
  - service: "Kling / Typecast / NanoBanana"
    why: "실 API 호출 for 1 shot asset"
    env_vars:
      - name: KLING_API_KEY
        source: "Kling 2.6 Pro 계정"
      - name: TYPECAST_API_KEY
        source: "Typecast 계정"
      - name: NANOBANANA_API_KEY
        source: "NanoBanana 계정 (Phase 9.1 wired)"
must_haves:
  truths:
    - "phase13_live_smoke.py --live --topic '해외범죄,FBI 수사,인터폴,국제 범죄 사건' --niche incidents 가 13 operational gate 전수 dispatch 완료"
    - "supervisor_rc1_count == 0 — SPC-01 encoding fix 의 empirical 검증"
    - "budget_cap_usd=5.00 enforcement + 실제 usage ≤ $5"
    - "5 evidence 파일 생성 완료 + sid anchored"
    - "YouTube 과금 환경 video_id 수신 + auto cleanup (unlisted 만 발행)"
  artifacts:
    - path: ".planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/smoke_e2e_<sid>.json"
      provides: "13 gate timestamp + final_video_id + total_cost_usd + supervisor_rc1_count"
    - path: ".planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/15-06-live-run.log"
      provides: "실시간 run stdout/stderr tail"
  key_links:
    - from: "scripts/smoke/phase13_live_smoke.py"
      to: ".planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/"
      via: "phase-aware evidence path override"
      pattern: "evidence_dir.*15-system-prompt"
---

<objective>
SPC-06: Phase 13 live smoke 를 Phase 15 SPC-01~05 fix 완결 상태에서 재실행하여 encoding root cause 가 empirically 해소되었음을 실 증거로 anchor.

**Purpose**: 2026-04-22 Phase 13 attempt 에서 0.09s/rc=1 로 실패한 "프롬프트가 너무 깁니다" 가 더 이상 재현되지 않음을 실 과금 환경 1회 완주로 입증. supervisor_rc1_count = 0 이 Phase 15 의 핵심 SC.

**Budget**: $1.50~$3.00 (Kling 8 cuts × $0.25 + Typecast 1K × $0.15 + NanoBanana 1 × $0.04 + YouTube free). Budget cap $5.00 enforcement.

**Checkpoint**: autonomous=false — 대표님 명시 승인 없이 live 호출 금지.

**의존**: Plan 15-02 (invokers.py fix), Plan 15-03 (AGENT.md 압축). 두 plan 완결 후에만 retry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/15-system-prompt-compression-user-feedback-loop/15-RESEARCH.md
@.planning/phases/13-live-smoke/evidence/14-05-live-run.log
@scripts/smoke/phase13_live_smoke.py
@scripts/orchestrator/shorts_pipeline.py
@scripts/orchestrator/invokers.py

<interfaces>
phase13_live_smoke.py 실행 명령 (RESEARCH line 339~346):
```
py -3.11 scripts/smoke/phase13_live_smoke.py \
    --live \
    --topic "해외범죄,FBI 수사,인터폴,국제 범죄 사건" \
    --niche incidents \
    --budget-cap-usd 5.00 \
    --max-attempts 2
```

Phase 13 Evidence schema (5 files per session):
1. `producer_output_<sid>.json` — producer agents 호출 결과 dump
2. `supervisor_output_<sid>.json` — supervisor dispatches + rubric
3. `smoke_upload_<sid>.json` — YouTube upload response + video_id
4. `budget_usage_<sid>.json` — $ breakdown per API
5. `smoke_e2e_<sid>.json` — 13 gate timestamps + supervisor_rc1_count + total_cost_usd + final_video_id

Phase 13 failure signature (2026-04-22 attempt):
- rc=1 at 0.09s, 0 gate dispatched
- stderr: "프롬프트가 너무 깁니다" (cp949-corrupted form in log)
- Phase 15 target: rc=0, 13 gate dispatched, supervisor_rc1_count=0
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 15-06-01: 대표님 live run 승인 + 환경 정합성 확인</name>
  <read_first>
    - .planning/phases/15-system-prompt-compression-user-feedback-loop/15-02-SUMMARY.md (Plan 15-02 완결 확인)
    - .planning/phases/15-system-prompt-compression-user-feedback-loop/15-03-SUMMARY.md (Plan 15-03 완결 확인)
    - .env (API key 존재 확인, 재질문 금지 per CLAUDE.md F-CTX-01)
    - config/client_secret.json + config/youtube_token.json 존재 확인
  </read_first>
  <what-built>
    Plan 15-02 (invokers.py tempfile + --append-system-prompt-file) + Plan 15-03 (shorts-supervisor AGENT.md 압축 < 7000 + Producer 18000 ceiling) 완결 상태.
    
    Task 15-06-02 live run 실행 직전 최종 확인:
    1. tests/phase15/ + tests/adapters/test_invokers_encoding_contract.py 전수 green
    2. Phase 13 baseline (tests/phase13/ -m "not live_smoke") 0 failures
    3. claude CLI Max 구독 정상 (`claude --version` 2.1.112+ 반환)
    4. YouTube OAuth token 유효 (config/youtube_token.json 최근 발급)
    5. 과금 API keys 준비 (Kling + Typecast + NanoBanana)
  </what-built>
  <how-to-verify>
    1. `py -3.11 -m pytest tests/phase15 tests/adapters/test_invokers_encoding_contract.py -m "not live_smoke" --no-cov -q` — 전수 green 확인
    2. `py -3.11 -m pytest tests/phase13/ -m "not live_smoke" --no-cov -q` — Phase 13 baseline 0 failures
    3. `claude --version` → 2.1.112+ 반환
    4. `ls config/client_secret.json config/youtube_token.json` — 둘 다 존재
    5. `.env` 확인 (대표님 직접, 재질문 금지):
       - `KLING_API_KEY` + `TYPECAST_API_KEY` + `NANOBANANA_API_KEY` 셋 다 값 있음
    6. AGENT.md 최종 크기: `py -3.11 -c "from pathlib import Path; body = Path('.claude/agents/supervisor/shorts-supervisor/AGENT.md').read_text(encoding='utf-8').split('---', 2)[-1].strip(); print(len(body))"` < 7000
    7. 대표님 명시 승인 확인: **"진행" 또는 "live retry 승인"** 타이핑.
  </how-to-verify>
  <resume-signal>대표님이 "진행" 또는 "승인" 타이핑 후 Task 15-06-02 진행. 문제 발견 시 "중단" + 이슈 명시.</resume-signal>
</task>

<task type="auto">
  <name>Task 15-06-02: Live smoke retry 실행 + 5 evidence anchor</name>
  <read_first>
    - scripts/smoke/phase13_live_smoke.py (실행 함수 구조)
    - .planning/phases/13-live-smoke/evidence/ (과거 live run evidence 샘플 — 구조 참조)
    - .planning/phases/15-system-prompt-compression-user-feedback-loop/15-RESEARCH.md §SPC-06 Command + Expected evidence
    - Plan 15-04 UFL-01/02/03 (flag 이미 구현됨 — 이번엔 bare live run, flag 미사용)
  </read_first>
  <files>
    - .planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/producer_output_<sid>.json
    - .planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/supervisor_output_<sid>.json
    - .planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/smoke_upload_<sid>.json
    - .planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/budget_usage_<sid>.json
    - .planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/smoke_e2e_<sid>.json
    - .planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/15-06-live-run.log
  </files>
  <behavior>
    - phase13_live_smoke.py 를 --evidence-dir 로 Phase 15 evidence 경로로 redirect 하여 실행.
    - 실행 인자: `--live --topic "해외범죄,FBI 수사,인터폴,국제 범죄 사건" --niche incidents --budget-cap-usd 5.00 --max-attempts 2`
    - stdout/stderr 를 `15-06-live-run.log` 로 tee (실시간 관찰용 + 영구 증거)
    - 완료 조건:
      - Process exit 0
      - 5 evidence 파일 모두 생성 (producer/supervisor/smoke_upload/budget/smoke_e2e)
      - smoke_e2e.json 내: `dispatched_count == 13`, `supervisor_rc1_count == 0`, `final_video_id != null`, `total_cost_usd <= 5.00`
    - 실패 조건 (→ Plan 중단 + FAILURES.md entry):
      - rc != 0
      - supervisor_rc1_count > 0 (SPC-01 fix 미완결 재증명)
      - 13 gate 미만 dispatch
      - budget $5 초과
  </behavior>
  <action>
    1. Session ID 고정: `SID=$(py -3.11 -c "import time; print('phase15_spc06_'+time.strftime('%Y%m%d_%H%M%S'))")`
    2. 실행:
       ```
       py -3.11 scripts/smoke/phase13_live_smoke.py \
           --live \
           --topic "해외범죄,FBI 수사,인터폴,국제 범죄 사건" \
           --niche incidents \
           --budget-cap-usd 5.00 \
           --max-attempts 2 \
           --session-id $SID \
           --evidence-dir .planning/phases/15-system-prompt-compression-user-feedback-loop/evidence \
           2>&1 | tee .planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/15-06-live-run.log
       ```
       (phase13_live_smoke.py 에 --evidence-dir flag 가 이미 있지 않으면 Plan 15-04 후속 1-line 추가 필요 — 없으면 evidence 파일이 default phase13 경로에 생성되므로 move)
    3. Evidence verify:
       ```
       ls .planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/
       # 기대: producer_output_${SID}.json + supervisor_output + smoke_upload + budget_usage + smoke_e2e + 15-06-live-run.log
       
       py -3.11 -c "
       import json
       from pathlib import Path
       ev = Path('.planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/')
       e2e = list(ev.glob('smoke_e2e_*.json'))[0]
       data = json.loads(e2e.read_text(encoding='utf-8'))
       print('dispatched_count:', data.get('dispatched_count'))
       print('supervisor_rc1_count:', data.get('supervisor_rc1_count'))
       print('final_video_id:', data.get('final_video_id'))
       print('total_cost_usd:', data.get('total_cost_usd'))
       assert data['dispatched_count'] == 13, '13 gate 미달'
       assert data['supervisor_rc1_count'] == 0, 'SPC-01 fix 미입증'
       assert data['final_video_id'], 'video_id 누락'
       assert data['total_cost_usd'] <= 5.00, 'budget 초과'
       print('OK — SPC-06 all green (대표님)')
       "
       ```
    4. Cleanup 검증: YouTube API 로 업로드된 video_id 가 cleanup 된 상태 or unlisted 상태 확인 (Phase 8 PUB-04 invariant).
    5. 실행 실패 시:
       - rc != 0 또는 SPC-01 fix 미입증 (rc1_count > 0) → `.claude/failures/FAILURES.md` append (F-SPC-06-FAILURE-01 entry)
       - Plan 15-06 중단, Plan 15-02 재검토 necessary
  </action>
  <acceptance_criteria>
    - `ls .planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/ 2>&1 | grep -cE "producer_output_.*\.json"` ≥ 1
    - `ls .planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/ 2>&1 | grep -cE "supervisor_output_.*\.json"` ≥ 1
    - `ls .planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/ 2>&1 | grep -cE "smoke_upload_.*\.json"` ≥ 1
    - `ls .planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/ 2>&1 | grep -cE "budget_usage_.*\.json"` ≥ 1
    - `ls .planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/ 2>&1 | grep -cE "smoke_e2e_.*\.json"` ≥ 1
    - `ls .planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/15-06-live-run.log` 존재
    - `py -3.11 -c "import json,glob; p=glob.glob('.planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/smoke_e2e_*.json')[0]; d=json.load(open(p, encoding='utf-8')); assert d['dispatched_count']==13, d; assert d['supervisor_rc1_count']==0, d; assert d['final_video_id'], d; assert d['total_cost_usd']<=5.00, d; print('OK')"` "OK" 출력
    - `grep -c "프롬프트가 너무 깁니다" .planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/15-06-live-run.log` = 0 (rc=1 증상 재현 없음)
  </acceptance_criteria>
  <verify>
    <automated>py -3.11 -c "import json, glob; p = glob.glob('.planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/smoke_e2e_*.json')[0]; d = json.load(open(p, encoding='utf-8')); assert d['dispatched_count']==13 and d['supervisor_rc1_count']==0 and d['final_video_id'] and d['total_cost_usd']<=5.00, d; print('SPC-06 OK')"</automated>
  </verify>
  <done>
    5 evidence + log anchored, 13 gate dispatched, supervisor_rc1_count=0, video_id received, total_cost ≤ $5. SPC-01 fix empirically 확증. SPC-06 충족.
  </done>
</task>

</tasks>

<verification>
Plan 15-06 전체 verify:
```bash
# 5 evidence 파일 + log 존재
ls .planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/

# smoke_e2e.json 4 조건 전수 통과
py -3.11 -c "import json, glob; p = glob.glob('.planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/smoke_e2e_*.json')[0]; d = json.load(open(p, encoding='utf-8')); assert all([d['dispatched_count']==13, d['supervisor_rc1_count']==0, d['final_video_id'], d['total_cost_usd']<=5.00]); print('SPC-06 all green')"

# rc=1 증상 재현 없음 확증
grep -c "프롬프트가 너무 깁니다" .planning/phases/15-system-prompt-compression-user-feedback-loop/evidence/15-06-live-run.log
# 0

# Phase 13 live_smoke marker 호환 (재실행 가능)
py -3.11 -m pytest tests/phase13 -m live_smoke --run-live --collect-only --no-cov -q
```
</verification>

<success_criteria>
- [ ] 대표님 명시 승인 획득 (Task 15-06-01 checkpoint 통과)
- [ ] phase13_live_smoke.py --live 실행 + exit 0
- [ ] 5 evidence 파일 anchored under .planning/phases/15-.../evidence/
- [ ] 15-06-live-run.log 생성
- [ ] smoke_e2e.json: dispatched_count == 13, supervisor_rc1_count == 0, video_id != null, total_cost_usd ≤ 5.00
- [ ] YouTube video cleanup 또는 unlisted 상태 유지 (Phase 8 PUB-04)
- [ ] "프롬프트가 너무 깁니다" log 미재현 (SPC-01 empirical 입증)
- [ ] CLAUDE.md 금기 #8 준수 (주 3~4편 + 48h lock) — Phase 13 기존 publish_lock 경로 유지
</success_criteria>

<output>
완료 후 `.planning/phases/15-system-prompt-compression-user-feedback-loop/15-06-SUMMARY.md`.
- 15-VALIDATION.md row 15-06-01 ⬜ → ✅
- ROADMAP.md Phase 15 Plan 06 [ ] → [x]
- Wave 6 (Plan 07 phase gate) unblocked.
- Phase 13 Milestone v1.0.2 SMOKE-01~06 이미 [x] 상태였으나, 본 retry 는 **Phase 15 encoding fix 의 empirical 입증** 로 별도 가치.
</output>
