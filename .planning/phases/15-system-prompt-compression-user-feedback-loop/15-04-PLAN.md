---
phase: 15-system-prompt-compression-user-feedback-loop
plan: 04
plan_id: "15-04"
type: execute
wave: 3
depends_on: ["15-01", "15-02"]
files_modified:
  - scripts/smoke/phase13_live_smoke.py
  - scripts/orchestrator/shorts_pipeline.py
  - scripts/orchestrator/gate_guard.py
  - scripts/orchestrator/invokers.py
  - tests/phase15/test_revision_flag.py
  - tests/phase15/test_producer_feedback_injection.py
  - tests/phase15/test_prescripted_invoker.py
  - tests/phase15/test_pause_after_gate.py
  - tests/phase15/test_runner_pause_signal.py
autonomous: true
requirements: [UFL-01, UFL-02, UFL-03]
must_haves:
  truths:
    - "phase13_live_smoke.py --revision-from GATE --feedback TEXT 가 target gate 이상의 gate_NN.json 삭제 + feedback 주입"
    - "Producer invoker 가 inputs['prior_user_feedback'] 을 user_payload JSON 에 포함시켜 하류 producer 에 전달"
    - "phase13_live_smoke.py --revise-script PATH 가 SCRIPT gate 에서 scripter skip, 파일 내용이 script_md 로 주입"
    - "phase13_live_smoke.py --pause-after GATE 가 해당 gate 완료 후 PipelinePauseSignal raise, runner 가 잡아 graceful exit 0"
    - "phase13_live_smoke.py --evidence-dir PATH 가 EVIDENCE_DIR 상수를 런타임 override (대표님 격리 실행)"
    - "기존 live smoke path (flag 무주입) 는 Phase 13 baseline 동일 동작 (regression 없음)"
  artifacts:
    - path: "scripts/smoke/phase13_live_smoke.py"
      provides: "4 flag (--revision-from, --feedback, --revise-script, --pause-after, --evidence-dir) 추가 + _PreScriptedProducerInvoker 클래스"
    - path: "scripts/orchestrator/shorts_pipeline.py"
      provides: "GateContext.config['prior_user_feedback'] + 'pause_after_gate' 주입 + 13 `_run_<gate>` 에 feedback pass-through + GateGuard 생성 순서 재정렬 (ctx 뒤로)"
    - path: "scripts/orchestrator/gate_guard.py"
      provides: "__init__(checkpointer, session_id, ctx_config=None) + dispatch() 끝에 pause_after_gate check → PipelinePauseSignal raise"
    - path: "scripts/orchestrator/invokers.py"
      provides: "Producer `__call__` 에 prior_user_feedback user_payload 주입 (기존 signature 불변)"
    - path: "tests/phase15/test_revision_flag.py + test_producer_feedback_injection.py + test_prescripted_invoker.py + test_pause_after_gate.py + test_runner_pause_signal.py"
      provides: "UFL-01/02/03 5 test file × 3~5 tests = 20+ tests"
  key_links:
    - from: "scripts/smoke/phase13_live_smoke.py::_apply_revision"
      to: "state/{sid}/gate_NN.json unlink"
      via: "sorted glob + idx >= from_gate.value"
      pattern: "def _apply_revision"
    - from: "scripts/orchestrator/invokers.py::ClaudeAgentProducerInvoker.__call__"
      to: "user_payload_dict['prior_user_feedback']"
      via: "inputs.get('prior_user_feedback')"
      pattern: "prior_user_feedback"
    - from: "scripts/orchestrator/gate_guard.py::GateGuard.dispatch"
      to: "PipelinePauseSignal"
      via: "self._ctx_config.get('pause_after_gate')"
      pattern: "PipelinePauseSignal"
    - from: "scripts/orchestrator/shorts_pipeline.py::ShortsPipeline.__init__"
      to: "GateGuard(checkpointer, session_id, ctx_config=self.ctx.config)"
      via: "GateContext 생성 후 GateGuard 생성 (by-reference dict propagation)"
      pattern: "ctx_config=self.ctx.config"
---

<objective>
UFL-01 / UFL-02 / UFL-03: 대표님 피드백 loop 의 3 CLI flag 추가. 기존 영상을 특정 gate 부터 재실행 (--revision-from), 대본 수동 주입 (--revise-script), gate 후 일시중지 (--pause-after).

**Purpose**: 대표님 직접 승인 2026-04-22: "어떻게해서든 내가 명령할수있는기능을 만들어야지 그렇지않으면 절대안되... 한동안은 내 피드백을 받으면서 영상을 제작 수정 재재작해야". ShortsPipeline resume 메커니즘 (L269~319) 재활용으로 pipeline 코어 변경 최소 (~30 lines 추가).

**Output**: 4 flag (+ --evidence-dir) + PipelinePauseSignal + _PreScriptedProducerInvoker + 5 test files.

**의존**: Plan 15-01 (fake_state_dir fixture) + Plan 15-02 (invokers.py tempfile fix — producer __call__ 수정 시 충돌 회피).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/15-system-prompt-compression-user-feedback-loop/15-RESEARCH.md
@scripts/smoke/phase13_live_smoke.py
@scripts/orchestrator/shorts_pipeline.py
@scripts/orchestrator/gate_guard.py
@scripts/orchestrator/invokers.py
@scripts/orchestrator/gates.py
@scripts/orchestrator/checkpointer.py
@tests/phase15/conftest.py

<interfaces>
GateName enum (gates.py):
```
IDLE=0, TREND=1, NICHE=2, RESEARCH_NLM=3, BLUEPRINT=4, SCRIPT=5, POLISH=6,
VOICE=7, ASSETS=8, ASSEMBLY=9, THUMBNAIL=10, METADATA=11, UPLOAD=12, MONITOR=13, COMPLETE=14
```

ClaudeAgentProducerInvoker **actual signature** (invokers.py L321~327 — 재확인 결과):
```python
def __init__(
    self,
    agent_dir_root: Path,           # REQUIRED positional — 누락 시 TypeError
    circuit_breaker=None,
    cli_path: str | None = None,
    cli_runner: Callable | None = None,
) -> None:
```
`__call__` 은 L335 에서 `agent_dir = self.agent_dir_root / agent_name` 생성 후 `if not agent_dir.exists(): raise FileNotFoundError(...)` — **테스트는 stub AGENT.md 디렉토리를 tmp_path 하위에 사전 생성해야 함**.

`_invoke_claude_cli` signature (cli_runner seam 계약):
```python
def _invoke_claude_cli(
    system_prompt: str, user_prompt: str, json_schema: dict, cli_path: str,
) -> str
```

GateGuard **actual signature** (gate_guard.py L120):
```python
def __init__(self, checkpointer: Any | None, session_id: str) -> None:
```
**본 플랜에서 확장**: `__init__(self, checkpointer, session_id, ctx_config: dict | None = None)` — 3번째 positional+keyword 추가.
`dispatch(gate: GateName, verdict: Verdict | None, artifact_path: Path | None = None)` — verdict 은 **Verdict dataclass** (result/score/evidence/semantic_feedback/inspector_name), dict 아님. 테스트는 `Verdict(result="PASS", score=100, evidence=[], semantic_feedback="")` 형태 또는 MagicMock(.result="PASS") 사용.

ShortsPipeline.__init__ 현 생성 순서 (shorts_pipeline.py L193~263):
```
L194  self.checkpointer = Checkpointer(self.state_root)
L195  self.gate_guard   = GateGuard(self.checkpointer, session_id)   # ← BEFORE ctx
...
L263  self.ctx = GateContext(session_id=session_id)                   # ← AFTER guard
```
**본 플랜에서 재정렬**: GateContext 생성을 GateGuard 앞으로 이동 또는 GateGuard 생성을 ctx 뒤로 이동. 후자 권장 (breakers/adapters 초기화 후 ctx 생성 패턴 유지, 최소 변경).

Phase 13 `_PreSeededProducerInvoker` (phase13_live_smoke.py L155~228) — chain wrapping pattern:
```python
class _PreSeededProducerInvoker:
    def __init__(self, real_invoker, pre_seeds: dict):
        self._real = real_invoker
        self._pre_seeds = pre_seeds  # {"TREND": {...}, "NICHE": {...}}
    def __call__(self, agent_name, gate, inputs):
        if gate in self._pre_seeds:
            return {...seeded output...}
        return self._real(agent_name, gate, inputs)
```

ShortsPipeline.run() resume (shorts_pipeline.py L269~319):
```python
last_idx = self.checkpointer.resume(self.session_id)  # -1 if none
for name in self.checkpointer.dispatched_gates(self.session_id): skip already-done
for gate in operational[last_idx+1:]: method = getattr(self, f"_run_{gate.name.lower()}"); method(ctx)
```

phase13_live_smoke.py EVIDENCE_DIR (L222 부근 argparse block):
```python
EVIDENCE_DIR = Path(".planning/phases/13-live-smoke/evidence")   # 상수
# --evidence-dir 플래그 추가 후: effective_evidence_dir = Path(args.evidence_dir) if args.evidence_dir else EVIDENCE_DIR
```

UFL-01 pseudo-code (RESEARCH line 165~177):
```python
target = GateName[args.revision_from.upper()]
for gate_file in state_dir.glob("gate_*.json"):
    idx = int(gate_file.stem.split("_")[1])
    if idx >= target.value:
        gate_file.unlink()
pipeline.ctx.config["prior_user_feedback"] = args.feedback
pipeline.ctx.config["revision_from_gate"] = target.name
```

_compress_producer_output (invokers.py L495) 은 Supervisor CLI 경로 전용 — Producer 출력에는 호출되지 않음. 단, SCRIPT gate 의 seeded verdict="PASS" 는 `gate`/`verdict`/`error_codes[]` 는 전수 보존 계약이므로 compression 후에도 유지.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 15-04-00: --evidence-dir flag 추가 (phase13_live_smoke.py)</name>
  <read_first>
    - scripts/smoke/phase13_live_smoke.py L210~240 (EVIDENCE_DIR 상수 정의 + 사용처)
    - scripts/smoke/phase13_live_smoke.py L305~385 (_parse_args argparse)
  </read_first>
  <files>
    - scripts/smoke/phase13_live_smoke.py
  </files>
  <behavior>
    - `--evidence-dir PATH` flag 추가 (default None — 미지정 시 기존 `EVIDENCE_DIR` 상수 사용).
    - 런타임 override: `effective_evidence_dir = Path(args.evidence_dir) if args.evidence_dir else EVIDENCE_DIR`.
    - help 메시지에 한국어 존댓말 "대표님 격리 실행" 설명.
    - 기존 동작 (flag 무주입) 100% 호환 — EVIDENCE_DIR 경유.
  </behavior>
  <action>
    1. `scripts/smoke/phase13_live_smoke.py` argparse block (L220~240 부근):
       ```python
       parser.add_argument(
           "--evidence-dir", default=None,
           help="evidence 저장 루트 override (대표님 격리 실행용). 미지정 시 기본 EVIDENCE_DIR 사용.",
       )
       ```
    2. EVIDENCE_DIR 사용 지점 (main 또는 _run_live 진입점):
       ```python
       effective_evidence_dir = (
           Path(args.evidence_dir) if args.evidence_dir else EVIDENCE_DIR
       )
       effective_evidence_dir.mkdir(parents=True, exist_ok=True)
       ```
       이후 모든 evidence 저장은 `effective_evidence_dir` 기준으로 변경.
  </action>
  <acceptance_criteria>
    - `grep -c "evidence-dir" scripts/smoke/phase13_live_smoke.py` ≥ 2 (argparse + 사용처)
    - `py -3.11 scripts/smoke/phase13_live_smoke.py --help 2>&1 | grep -c "evidence-dir"` ≥ 1
    - `grep -c "effective_evidence_dir" scripts/smoke/phase13_live_smoke.py` ≥ 2 (정의 + 사용)
  </acceptance_criteria>
  <verify>
    <automated>py -3.11 scripts/smoke/phase13_live_smoke.py --help</automated>
  </verify>
  <done>
    --evidence-dir flag 노출 + help + runtime override. Plan 15-06 live smoke retry 단계에서 대표님 격리 실행 기반 구축.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 15-04-01: UFL-01 — --revision-from + --feedback flag + producer injection</name>
  <read_first>
    - scripts/smoke/phase13_live_smoke.py L305~385 (_parse_args argparse)
    - scripts/orchestrator/shorts_pipeline.py L120~143 (GateContext) + L269~319 (run() resume)
    - scripts/orchestrator/invokers.py L321~368 (ClaudeAgentProducerInvoker __init__ + __call__, Plan 15-02 patch 반영 상태)
    - scripts/orchestrator/checkpointer.py (gate_NN.json layout)
    - scripts/orchestrator/gates.py (GateName enum values)
    - tests/phase15/conftest.py (fake_state_dir fixture)
  </read_first>
  <files>
    - scripts/smoke/phase13_live_smoke.py
    - scripts/orchestrator/shorts_pipeline.py
    - scripts/orchestrator/invokers.py
    - tests/phase15/test_revision_flag.py
    - tests/phase15/test_producer_feedback_injection.py
  </files>
  <behavior>
    - `--revision-from GATE` flag 추가 (choices = GateName enum 중 IDLE/COMPLETE 제외).
    - `--feedback TEXT` flag 추가 (independent, `--revision-from` 과 함께 권장).
    - `_apply_revision(state_root, sid, from_gate)` helper: `state_root/sid/gate_*.json` 중 idx >= from_gate.value 인 파일 unlink.
    - `_build_pipeline_with_seed` 에 feedback 주입: `pipeline.ctx.config["prior_user_feedback"] = feedback` + `pipeline.ctx.config["revision_from_gate"] = from_gate.name`.
    - `ShortsPipeline._run_<gate>` 각 메서드에서 inputs 에 `"prior_user_feedback": self.ctx.config.get("prior_user_feedback")` 추가 (13 methods 1줄씩).
    - `ClaudeAgentProducerInvoker.__call__`: inputs.get('prior_user_feedback') → user_payload_dict 에 주입 (signature 불변).
    - TDD RED → GREEN:
      - test_revision_flag.py (4 tests): fake_state_dir fixture 로 gate_*.json 14개 만들고, `_apply_revision(sid, GateName.SCRIPT)` 호출 후 gate_05~13 사라지고 gate_00~04 남음 검증.
      - test_producer_feedback_injection.py (3 tests): **실제 ClaudeAgentProducerInvoker signature 를 준수** — tmp_path 하위 stub AGENT.md 디렉토리 생성 후 `agent_dir_root=tmp_path` 전달, `cli_runner=fake` (키워드 인자, `_invoke_claude_cli` signature 일치 — `system_prompt`, `user_prompt`, `json_schema`, `cli_path`).
  </behavior>
  <action>
    1. RED — `tests/phase15/test_revision_flag.py` 작성 (4 tests):
       ```python
       """UFL-01 --revision-from flag integration test."""
       from __future__ import annotations
       import json
       from pathlib import Path
       import pytest
       from scripts.orchestrator.gates import GateName
       from scripts.smoke.phase13_live_smoke import _apply_revision

       class TestApplyRevision:
           def test_deletes_gates_at_and_after_target(self, fake_state_dir: Path):
               deleted = _apply_revision(
                   fake_state_dir.parent.parent,  # state root
                   fake_state_dir.name,  # session_id
                   GateName.SCRIPT,
               )
               # gate_05 (SCRIPT) 부터 gate_13 (MONITOR) 삭제 — 9 files
               assert len(deleted) == 9
               assert not (fake_state_dir / "gate_05.json").exists()
               assert not (fake_state_dir / "gate_13.json").exists()
               # gate_00 ~ gate_04 보존
               assert (fake_state_dir / "gate_04.json").exists()
               assert (fake_state_dir / "gate_00.json").exists()

           def test_deletes_only_target_and_above(self, fake_state_dir: Path):
               _apply_revision(
                   fake_state_dir.parent.parent, fake_state_dir.name, GateName.THUMBNAIL,
               )
               # gate_10 (THUMBNAIL) 부터 gate_13 삭제 — 4 files
               assert not (fake_state_dir / "gate_10.json").exists()
               assert (fake_state_dir / "gate_09.json").exists()
               assert (fake_state_dir / "gate_00.json").exists()

           def test_handles_missing_state_dir_gracefully(self, tmp_path: Path):
               # 존재하지 않는 session → 빈 리스트 반환, 예외 없음
               result = _apply_revision(tmp_path, "nonexistent_sid", GateName.SCRIPT)
               assert result == []

           def test_skips_malformed_filenames(self, tmp_path: Path):
               state = tmp_path / "state" / "sid1"
               state.mkdir(parents=True)
               (state / "gate_05.json").write_text("{}", encoding="utf-8")
               (state / "not_a_gate.json").write_text("{}", encoding="utf-8")
               (state / "gate_XX.json").write_text("{}", encoding="utf-8")
               _apply_revision(tmp_path / "state", "sid1", GateName.SCRIPT)
               # gate_05 는 삭제
               assert not (state / "gate_05.json").exists()
               # malformed 는 보존 (무시)
               assert (state / "not_a_gate.json").exists()
               assert (state / "gate_XX.json").exists()
       ```
    2. RED — `tests/phase15/test_producer_feedback_injection.py` (3 tests) — **실제 signature 준수**:
       ```python
       """UFL-01 Producer invoker prior_user_feedback injection.

       ClaudeAgentProducerInvoker.__init__ 은 agent_dir_root 필수 positional.
       __call__ 은 agent_dir.exists() 체크 → tmp_path 하위 stub AGENT.md 사전 생성 필요.
       cli_runner signature (system_prompt, user_prompt, json_schema, cli_path).
       """
       from __future__ import annotations
       import json
       from pathlib import Path
       import pytest
       from scripts.orchestrator.invokers import ClaudeAgentProducerInvoker


       def _make_scripter_stub(tmp_path: Path) -> Path:
           """agent_dir.exists() 통과용 stub scripter/AGENT.md 생성."""
           scripter_dir = tmp_path / "scripter"
           scripter_dir.mkdir(parents=True, exist_ok=True)
           (scripter_dir / "AGENT.md").write_text(
               "stub body (대표님 Phase 15 test)", encoding="utf-8",
           )
           return tmp_path


       class TestFeedbackInjection:
           def test_feedback_injected_into_user_payload(self, tmp_path):
               agent_root = _make_scripter_stub(tmp_path)
               captured = {}

               def fake_cli_runner(system_prompt, user_prompt, json_schema, cli_path):
                   captured["user_prompt"] = user_prompt
                   return '{"gate": "SCRIPT", "verdict": "PASS", "script": "..."}'

               invoker = ClaudeAgentProducerInvoker(
                   agent_dir_root=agent_root,
                   cli_runner=fake_cli_runner,
               )
               result = invoker("scripter", "SCRIPT", {
                   "topic": "test",
                   "prior_user_feedback": "hook 이 약합니다 (대표님)",
               })
               payload = json.loads(captured["user_prompt"])
               assert "prior_user_feedback" in captured["user_prompt"]
               assert "hook 이 약합니다 (대표님)" in captured["user_prompt"]
               assert payload.get("prior_user_feedback") == "hook 이 약합니다 (대표님)"
               assert result["verdict"] == "PASS"

           def test_feedback_absent_when_not_provided(self, tmp_path):
               agent_root = _make_scripter_stub(tmp_path)
               captured = {}

               def fake_cli_runner(system_prompt, user_prompt, json_schema, cli_path):
                   captured["user_prompt"] = user_prompt
                   return '{"gate": "SCRIPT", "verdict": "PASS"}'

               invoker = ClaudeAgentProducerInvoker(
                   agent_dir_root=agent_root,
                   cli_runner=fake_cli_runner,
               )
               invoker("scripter", "SCRIPT", {"topic": "test"})
               payload = json.loads(captured["user_prompt"])
               assert "prior_user_feedback" not in payload

           def test_feedback_none_treated_as_absent(self, tmp_path):
               agent_root = _make_scripter_stub(tmp_path)
               captured = {}

               def fake_cli_runner(system_prompt, user_prompt, json_schema, cli_path):
                   captured["user_prompt"] = user_prompt
                   return '{"ok": true}'

               invoker = ClaudeAgentProducerInvoker(
                   agent_dir_root=agent_root,
                   cli_runner=fake_cli_runner,
               )
               invoker("scripter", "SCRIPT", {"topic": "t", "prior_user_feedback": None})
               payload = json.loads(captured["user_prompt"])
               assert "prior_user_feedback" not in payload  # None 이면 skip
       ```
    3. GREEN — `scripts/smoke/phase13_live_smoke.py` 편집:
       - imports 확장: `from scripts.orchestrator.gates import GateName`
       - `_apply_revision` helper 함수 추가 (RESEARCH pseudo-code):
         ```python
         def _apply_revision(state_root: Path, session_id: str, from_gate: GateName) -> list[Path]:
             """UFL-01 — Delete gate_N..14.json for N >= from_gate.value."""
             state_dir = state_root / session_id
             if not state_dir.is_dir():
                 logger.info("[revision] state_dir 없음 (대표님): %s", state_dir)
                 return []
             deleted: list[Path] = []
             for gate_file in sorted(state_dir.glob("gate_*.json")):
                 try:
                     idx = int(gate_file.stem.split("_")[1])
                 except (ValueError, IndexError):
                     continue  # malformed skip
                 if idx >= from_gate.value:
                     gate_file.unlink()
                     logger.info("[revision] %s 삭제 (대표님 재작업 요청)", gate_file.name)
                     deleted.append(gate_file)
             return deleted
         ```
       - `_parse_args` 에 flag 추가 (RESEARCH §UFL-01):
         ```python
         parser.add_argument(
             "--revision-from", default=None,
             choices=[g.name for g in GateName if g not in (GateName.IDLE, GateName.COMPLETE)],
             help="기존 세션의 특정 GATE 부터 재실행. 해당 GATE 이후 checkpoint 삭제.",
         )
         parser.add_argument(
             "--feedback", default=None,
             help="대표님 피드백 텍스트. Producer 의 prior_user_feedback 에 주입.",
         )
         ```
       - `main()` / `_build_pipeline_with_seed` 에서 revision 적용:
         ```python
         if args.revision_from:
             target = GateName[args.revision_from.upper()]
             _apply_revision(state_root, args.session_id, target)
         ...
         if args.feedback:
             pipeline.ctx.config["prior_user_feedback"] = args.feedback
         if args.revision_from:
             pipeline.ctx.config["revision_from_gate"] = args.revision_from.upper()
         ```
    4. GREEN — `scripts/orchestrator/shorts_pipeline.py` 편집:
       - 13 `_run_<gate>` 메서드 각각의 inputs 빌드 블록에 1줄 추가:
         ```python
         inputs["prior_user_feedback"] = self.ctx.config.get("prior_user_feedback")
         ```
       - `GateContext` 에 이미 config 딕트 있으니 구조 변경 불요
    5. GREEN — `scripts/orchestrator/invokers.py` `ClaudeAgentProducerInvoker.__call__` 편집 (L334 근처):
       ```python
       # Phase 15 UFL-01 — prior_user_feedback 이 inputs 에 있으면 user_payload 에 주입
       inputs_copy = dict(inputs)
       feedback = inputs_copy.pop("prior_user_feedback", None)
       user_payload_dict = {"gate": gate, "inputs": inputs_copy}
       if feedback:
           user_payload_dict["prior_user_feedback"] = feedback
       user_payload = json.dumps(user_payload_dict, ensure_ascii=False)
       ```
       (기존 user_payload 구성 로직을 feedback-aware 로 교체, signature 불변)
    6. 실행:
       ```
       py -3.11 -m pytest tests/phase15/test_revision_flag.py tests/phase15/test_producer_feedback_injection.py -v --no-cov
       # Phase 11 regression
       py -3.11 -m pytest tests/phase11/ tests/phase091/ --no-cov -q
       ```
  </action>
  <acceptance_criteria>
    - `grep -c "def _apply_revision" scripts/smoke/phase13_live_smoke.py` = 1
    - `grep -c "revision-from" scripts/smoke/phase13_live_smoke.py` ≥ 2 (argparse + usage)
    - `grep -c "prior_user_feedback" scripts/orchestrator/invokers.py` ≥ 1
    - `grep -c "prior_user_feedback" scripts/orchestrator/shorts_pipeline.py` ≥ 2 (multiple gate methods)
    - `py -3.11 -m pytest tests/phase15/test_revision_flag.py --no-cov -q 2>&1 | grep -cE "4 passed|PASSED"` ≥ 4
    - `py -3.11 -m pytest tests/phase15/test_producer_feedback_injection.py --no-cov -q 2>&1 | grep -cE "3 passed|PASSED"` ≥ 3
    - `py -3.11 -m pytest tests/phase11/ --no-cov -q` 0 failures
    - `grep -c "대표님" scripts/smoke/phase13_live_smoke.py` ≥ 3 (최소 3 log/help/error 메시지)
    - **Additional W5 acceptance** — SCRIPT gate seeded verdict compression 불변 검증:
      ```
      py -3.11 -c "from scripts.orchestrator.invokers import _compress_producer_output; c = _compress_producer_output({'decisions': [{'severity':'low','score':1,'msg':'x'}], 'gate':'SCRIPT', 'verdict':'PASS', 'error_codes':[]}); assert c['verdict']=='PASS', 'SCRIPT gate 의 seeded verdict 는 compression 후에도 preserve 되어야 합니다 (대표님)'; print('compression verdict preserve OK')"
      ```
      expected stdout: `compression verdict preserve OK`
  </acceptance_criteria>
  <verify>
    <automated>py -3.11 -m pytest tests/phase15/test_revision_flag.py tests/phase15/test_producer_feedback_injection.py tests/phase11/ -v --no-cov --tb=short</automated>
  </verify>
  <done>
    UFL-01 flag 2종 + helper + producer injection + 7 tests green. Phase 11 regression 0 failures. Phase 12 compression preserves SCRIPT verdict.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 15-04-02: UFL-02 — --revise-script + _PreScriptedProducerInvoker</name>
  <read_first>
    - scripts/smoke/phase13_live_smoke.py L155~228 (_PreSeededProducerInvoker — chain wrapping 선례)
    - scripts/smoke/phase13_live_smoke.py L231~269 (_build_pipeline_with_seed factory)
    - scripts/orchestrator/shorts_pipeline.py L384~397 (_run_script — scripter 호출 경로)
    - .claude/agents/producers/script-polisher/AGENT.md — script_md input 수용 여부 확인
  </read_first>
  <files>
    - scripts/smoke/phase13_live_smoke.py
    - tests/phase15/test_prescripted_invoker.py
  </files>
  <behavior>
    - `--revise-script PATH` flag 추가 (type=Path). `.md` 또는 `.txt` 파일 경로.
    - `_PreScriptedProducerInvoker` 클래스 추가 — Phase 13 `_PreSeededProducerInvoker` L155~228 패턴 복제:
      - `__init__(self, real_invoker, script_path)`: file 읽어 self._script_content 에 저장
      - `__call__(self, agent_name, gate, inputs)`: gate == "SCRIPT" 이면 seeded output 반환 (script_md = content, verdict=PASS, user_provided=True). 그 외 gate 는 real_invoker 로 pass-through.
    - `_build_pipeline_with_seed` 에 chain: 기존 pre_seed (TREND/NICHE) 래핑 → 추가로 prescripted 래핑.
    - UFL-02 Q4 Option A (권장): script-polisher 는 **실행** (POLISH gate 유지) — 대표님 수동 대본도 RUB 검증. POLISH skip 하지 않음.
    - TDD: test_prescripted_invoker.py (4~5 tests).
  </behavior>
  <action>
    1. RED — `tests/phase15/test_prescripted_invoker.py` (5 tests):
       ```python
       """UFL-02 _PreScriptedProducerInvoker behavior."""
       from __future__ import annotations
       from pathlib import Path
       from unittest.mock import MagicMock
       import pytest
       from scripts.smoke.phase13_live_smoke import _PreScriptedProducerInvoker

       @pytest.fixture
       def sample_script(tmp_path: Path) -> Path:
           p = tmp_path / "sample_script.md"
           p.write_text(
               "# 대표님 수동 대본\n\n탐정: 이거 수상합니다.\n조수: 네, 확인해보죠.",
               encoding="utf-8",
           )
           return p

       class TestPreScripted:
           def test_script_gate_returns_seeded_output(self, sample_script):
               real = MagicMock()
               invoker = _PreScriptedProducerInvoker(real, sample_script)
               result = invoker("scripter", "SCRIPT", {"topic": "test"})
               assert result["gate"] == "SCRIPT"
               assert result["verdict"] == "PASS"
               assert result["script_md"] == sample_script.read_text(encoding="utf-8")
               assert result.get("user_provided") is True
               real.assert_not_called()  # scripter 를 실 호출 안 함

           def test_non_script_gate_passes_through(self, sample_script):
               real = MagicMock(return_value={"gate": "NICHE", "verdict": "PASS"})
               invoker = _PreScriptedProducerInvoker(real, sample_script)
               result = invoker("niche-classifier", "NICHE", {"topic": "x"})
               real.assert_called_once()
               assert result == {"gate": "NICHE", "verdict": "PASS"}

           def test_script_content_preserves_utf8_korean(self, sample_script):
               invoker = _PreScriptedProducerInvoker(MagicMock(), sample_script)
               result = invoker("scripter", "SCRIPT", {})
               assert "탐정" in result["script_md"]
               assert "조수" in result["script_md"]

           def test_decisions_log_script_path(self, sample_script):
               invoker = _PreScriptedProducerInvoker(MagicMock(), sample_script)
               result = invoker("scripter", "SCRIPT", {})
               # decisions 필드가 existence + 파일 경로 log
               assert "decisions" in result
               assert any(str(sample_script) in d for d in result["decisions"])

           def test_missing_file_raises_clear_error(self, tmp_path):
               nonexist = tmp_path / "missing.md"
               with pytest.raises((FileNotFoundError, OSError)):
                   _PreScriptedProducerInvoker(MagicMock(), nonexist)
       ```
    2. GREEN — `scripts/smoke/phase13_live_smoke.py` 편집:
       - `_PreSeededProducerInvoker` 아래 `_PreScriptedProducerInvoker` 추가:
         ```python
         class _PreScriptedProducerInvoker:
             """UFL-02 — 대표님 수동 대본 주입. SCRIPT gate 에서 scripter skip."""
             def __init__(self, real_invoker, script_path: Path):
                 self._real = real_invoker
                 # __init__ 에서 바로 읽어 missing 을 조기 감지 (FileNotFoundError raise)
                 self._script_content = script_path.read_text(encoding="utf-8")
                 self._script_path = str(script_path)
                 logger.info(
                     "[pre-script] 대표님 수동 대본 로드: %s (%d chars)",
                     self._script_path, len(self._script_content),
                 )
             def __call__(self, agent_name: str, gate: str, inputs: dict) -> dict:
                 if gate == "SCRIPT":
                     logger.info(
                         "[pre-script] SCRIPT gate — scripter skip, 대표님 수동 대본 주입",
                     )
                     return {
                         "gate": "SCRIPT",
                         "verdict": "PASS",
                         "script_md": self._script_content,
                         "user_provided": True,
                         "seeded": True,
                         "decisions": [
                             f"대표님 --revise-script flag: {self._script_path}",
                         ],
                         "error_codes": [],
                     }
                 return self._real(agent_name, gate, inputs)
         ```
       - `_parse_args` 에 flag:
         ```python
         parser.add_argument(
             "--revise-script", default=None, type=Path,
             help="대표님 수동 대본 .md/.txt 경로. SCRIPT gate 에서 scripter skip.",
         )
         ```
       - `_build_pipeline_with_seed` 에서 chain:
         ```python
         if args.revise_script:
             pipeline.producer_invoker = _PreScriptedProducerInvoker(
                 pipeline.producer_invoker, args.revise_script,
             )
         ```
    3. 실행:
       ```
       py -3.11 -m pytest tests/phase15/test_prescripted_invoker.py -v --no-cov
       ```
  </action>
  <acceptance_criteria>
    - `grep -c "class _PreScriptedProducerInvoker" scripts/smoke/phase13_live_smoke.py` = 1
    - `grep -c "revise-script" scripts/smoke/phase13_live_smoke.py` ≥ 2
    - `py -3.11 -m pytest tests/phase15/test_prescripted_invoker.py --no-cov -q 2>&1 | grep -cE "5 passed|PASSED"` ≥ 5
    - `grep -c "대표님" scripts/smoke/phase13_live_smoke.py` increased vs. Plan 15-04 task 1 baseline (>= 5 total)
  </acceptance_criteria>
  <verify>
    <automated>py -3.11 -m pytest tests/phase15/test_prescripted_invoker.py -v --no-cov --tb=short</automated>
  </verify>
  <done>
    UFL-02 flag + _PreScriptedProducerInvoker 클래스 + 5 tests green. SCRIPT gate 에서 대표님 수동 대본 주입 경로 구동.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 15-04-03: UFL-03 — --pause-after + PipelinePauseSignal</name>
  <read_first>
    - scripts/orchestrator/gate_guard.py L104~198 (GateGuard **실제 __init__(checkpointer, session_id)** signature + dispatch(gate, verdict: Verdict))
    - scripts/orchestrator/gate_guard.py L45~60 (Verdict dataclass — result/score/evidence/semantic_feedback/inspector_name)
    - scripts/orchestrator/shorts_pipeline.py L171~263 (ShortsPipeline.__init__ 현 생성 순서 — GateGuard L195 vs ctx L263)
    - scripts/smoke/phase13_live_smoke.py L687~891 (_run_live exception loop)
    - .planning/phases/15-system-prompt-compression-user-feedback-loop/15-RESEARCH.md §UFL-03 (single choke-point recommendation)
  </read_first>
  <files>
    - scripts/orchestrator/shorts_pipeline.py
    - scripts/orchestrator/gate_guard.py
    - scripts/smoke/phase13_live_smoke.py
    - tests/phase15/test_pause_after_gate.py
    - tests/phase15/test_runner_pause_signal.py
  </files>
  <behavior>
    - `PipelinePauseSignal(Exception)` 클래스 선언 (shorts_pipeline.py 상단) — paused_at: GateName attribute.
    - **GateGuard signature 확장**: `__init__(self, checkpointer, session_id, ctx_config: dict | None = None)` — 3번째 positional+keyword 추가. 내부 `self._ctx_config = ctx_config or {}`.
    - **ShortsPipeline.__init__ 재정렬**: 기존 L195 `self.gate_guard = GateGuard(self.checkpointer, session_id)` 를 **L263 `self.ctx = GateContext(session_id=session_id)` 뒤로 이동**하여 `self.gate_guard = GateGuard(self.checkpointer, session_id, ctx_config=self.ctx.config)` 로 **dict by reference 전달**. 런타임에 `pipeline.ctx.config["pause_after_gate"] = ...` 설정 시 GateGuard 에 자동 전파.
    - `GateGuard.dispatch(gate, verdict)` 끝에 check: `self._ctx_config.get("pause_after_gate")` == gate.name 이면 raise PipelinePauseSignal.
    - `--pause-after GATE` flag (phase13_live_smoke.py argparse, choices = operational gates).
    - `_build_pipeline_with_seed`: `pipeline.ctx.config["pause_after_gate"] = args.pause_after.upper()` (설정 시) — GateGuard 가 by-reference dict 를 이미 참조하므로 자동 반영.
    - `_run_live` runner 의 exception loop: `except PipelinePauseSignal as sig:` branch 추가 — evidence 저장 + graceful exit 0.
    - TDD 2 test files:
      - test_pause_after_gate.py (3 tests): GateGuard.dispatch 호출 시 ctx_config 에 pause_after 설정하면 Signal raise — **실제 signature (checkpointer + session_id positional) + Verdict dataclass** 사용.
      - test_runner_pause_signal.py (3 tests): runner 가 Signal 받아 status="PAUSED" evidence 쓰고 exit 0
  </behavior>
  <action>
    1. RED — `tests/phase15/test_pause_after_gate.py` (3 tests) — **실제 signature 준수**:
       ```python
       """UFL-03 GateGuard dispatch pause_after_gate trigger.

       GateGuard.__init__ 실제 signature: (checkpointer, session_id, ctx_config=None).
       dispatch(gate: GateName, verdict: Verdict, artifact_path=None) — Verdict dataclass 필요.
       """
       from __future__ import annotations
       from unittest.mock import MagicMock
       import pytest
       from scripts.orchestrator.gates import GateName
       from scripts.orchestrator.gate_guard import GateGuard, Verdict
       from scripts.orchestrator.shorts_pipeline import PipelinePauseSignal


       def _mk_verdict() -> Verdict:
           """SCRIPT gate PASS Verdict (실제 dataclass)."""
           return Verdict(
               result="PASS",
               score=100,
               evidence=[],
               semantic_feedback="",
               inspector_name="test",
           )


       class TestPauseAfterDispatch:
           def test_signal_raised_when_gate_matches(self):
               checkpointer = MagicMock()
               ctx_config = {"pause_after_gate": "SCRIPT"}
               guard = GateGuard(checkpointer, "test_pause_sid", ctx_config=ctx_config)
               verdict = _mk_verdict()
               with pytest.raises(PipelinePauseSignal) as exc:
                   guard.dispatch(GateName.SCRIPT, verdict)
               assert exc.value.paused_at == GateName.SCRIPT

           def test_signal_not_raised_for_other_gate(self):
               checkpointer = MagicMock()
               ctx_config = {"pause_after_gate": "SCRIPT"}
               guard = GateGuard(checkpointer, "test_sid2", ctx_config=ctx_config)
               verdict = _mk_verdict()
               # NICHE != SCRIPT → signal 없음, 정상 return
               guard.dispatch(GateName.NICHE, verdict)
               assert GateName.NICHE in guard.dispatched

           def test_no_config_no_signal(self):
               checkpointer = MagicMock()
               # ctx_config 미지정 (None) — 기본 dict = {} → signal 없음
               guard = GateGuard(checkpointer, "test_sid3")
               verdict = _mk_verdict()
               guard.dispatch(GateName.SCRIPT, verdict)  # no Signal
               assert GateName.SCRIPT in guard.dispatched
       ```
    2. RED — `tests/phase15/test_runner_pause_signal.py` (3 tests):
       ```python
       """UFL-03 runner PipelinePauseSignal graceful exit."""
       from __future__ import annotations
       from pathlib import Path
       from unittest.mock import patch, MagicMock
       import pytest
       from scripts.orchestrator.gates import GateName
       from scripts.orchestrator.shorts_pipeline import PipelinePauseSignal

       class TestRunnerCatch:
           def test_runner_returns_0_on_pause_signal(self, tmp_path, monkeypatch):
               """_run_live 유사 단순화 stub — pause signal 시 exit 0"""
               from scripts.smoke.phase13_live_smoke import _handle_pause_signal
               sig = PipelinePauseSignal(GateName.SCRIPT)
               exit_code = _handle_pause_signal(sig, tmp_path / "ev")
               assert exit_code == 0

           def test_evidence_written_with_paused_status(self, tmp_path):
               from scripts.smoke.phase13_live_smoke import _handle_pause_signal
               ev_dir = tmp_path / "ev"
               ev_dir.mkdir()
               sig = PipelinePauseSignal(GateName.VOICE)
               _handle_pause_signal(sig, ev_dir)
               # evidence pause.json 기록
               pause_log = list(ev_dir.glob("pause_*.json"))
               assert pause_log, "evidence pause log 미생성"
               import json
               data = json.loads(pause_log[0].read_text(encoding="utf-8"))
               assert data["status"] == "PAUSED"
               assert data["paused_at"] == "VOICE"

           def test_signal_paused_at_stored(self):
               sig = PipelinePauseSignal(GateName.THUMBNAIL)
               assert sig.paused_at == GateName.THUMBNAIL
               assert "THUMBNAIL" in str(sig)
       ```
    3. GREEN — `scripts/orchestrator/shorts_pipeline.py` 편집 (상단 imports 아래):
       ```python
       class PipelinePauseSignal(Exception):
           """UFL-03 — 대표님 --pause-after <gate> 이후 pipeline 일시중지 신호.

           Runner 가 잡아 graceful exit 0 + evidence pause_*.json 저장.
           """
           def __init__(self, paused_at):  # GateName
               super().__init__(
                   f"Pipeline paused after {paused_at.name} (대표님 UFL-03)"
               )
               self.paused_at = paused_at
       ```
    4. GREEN — `scripts/orchestrator/gate_guard.py`:
       - `GateGuard.__init__(self, checkpointer, session_id, ctx_config: dict | None = None)`:
         ```python
         def __init__(
             self,
             checkpointer: Any | None,
             session_id: str,
             ctx_config: dict | None = None,
         ) -> None:
             self.cp = checkpointer
             self.session_id = session_id
             self._dispatched: set[GateName] = set()
             # UFL-03 — ctx_config 은 ShortsPipeline 이 by-reference 로 전달.
             # 런타임에 pipeline.ctx.config["pause_after_gate"] = ... 설정 시 자동 전파.
             self._ctx_config = ctx_config if ctx_config is not None else {}
         ```
       - `dispatch` 끝에 추가 (`self._dispatched.add(gate)` 뒤):
         ```python
         # UFL-03 — pause_after_gate 체크 (대표님)
         pause_after = self._ctx_config.get("pause_after_gate")
         if pause_after and gate.name == pause_after:
             # lazy import to avoid circular (shorts_pipeline imports gate_guard)
             from scripts.orchestrator.shorts_pipeline import PipelinePauseSignal
             raise PipelinePauseSignal(gate)
         ```
    5. GREEN — `scripts/orchestrator/shorts_pipeline.py` **__init__ 재정렬**:
       - 기존 L193~195 (Checkpointer + GateGuard) 블록에서 **GateGuard 생성 라인만 제거** (Checkpointer 는 유지):
         ```python
         # 현 L194 유지
         self.checkpointer = Checkpointer(self.state_root)
         # L195 GateGuard 생성 제거 — L263 뒤로 이동
         ```
       - 기존 L263 `self.ctx = GateContext(session_id=session_id)` 직후에 추가:
         ```python
         self.ctx = GateContext(session_id=session_id)
         # UFL-03 — GateGuard 는 ctx.config 를 by-reference 로 참조하여
         # 런타임 pause_after_gate 설정을 반영 가능. ctx 생성 이후로 이동.
         self.gate_guard = GateGuard(
             self.checkpointer, session_id, ctx_config=self.ctx.config,
         )
         ```
    6. GREEN — `scripts/smoke/phase13_live_smoke.py`:
       - argparse:
         ```python
         parser.add_argument(
             "--pause-after", default=None,
             choices=[g.name for g in GateName if g not in (GateName.IDLE, GateName.COMPLETE)],
             help="지정 GATE 완료 후 pipeline 일시중지 (대표님 검토용).",
         )
         ```
       - `_handle_pause_signal(sig, ev_dir)` helper:
         ```python
         def _handle_pause_signal(sig: "PipelinePauseSignal", ev_dir: Path) -> int:
             """UFL-03 — Signal 수신 시 evidence pause 저장 + exit 0 반환."""
             import json, time
             ev_dir.mkdir(parents=True, exist_ok=True)
             ts = time.strftime("%Y%m%d_%H%M%S")
             payload = {
                 "status": "PAUSED",
                 "paused_at": sig.paused_at.name,
                 "timestamp": ts,
                 "message": f"대표님 --pause-after {sig.paused_at.name} 수신, 일시중지합니다.",
             }
             (ev_dir / f"pause_{ts}.json").write_text(
                 json.dumps(payload, ensure_ascii=False, indent=2),
                 encoding="utf-8",
             )
             logger.info("[pause] evidence 저장 완료 (대표님): %s", sig.paused_at.name)
             return 0
         ```
       - `_build_pipeline_with_seed`:
         ```python
         if args.pause_after:
             pipeline.ctx.config["pause_after_gate"] = args.pause_after.upper()
         ```
         (GateGuard 는 ctx.config 를 이미 by-reference 로 참조하므로 dict mutation 이 자동 전파)
       - `_run_live` exception loop:
         ```python
         except PipelinePauseSignal as sig:
             logger.info("[phase13] pause signal (대표님): %s", sig.paused_at.name)
             return _handle_pause_signal(sig, effective_evidence_dir)
         ```
    7. 실행:
       ```
       py -3.11 -m pytest tests/phase15/test_pause_after_gate.py tests/phase15/test_runner_pause_signal.py -v --no-cov
       # Phase 13 regression
       py -3.11 -m pytest tests/phase13/ -m "not live_smoke" --no-cov -q
       # Phase 5/9.1 regression (GateGuard signature 확장 영향 범위)
       py -3.11 -m pytest tests/phase05/ tests/phase091/ --no-cov -q
       ```
  </action>
  <acceptance_criteria>
    - `grep -c "class PipelinePauseSignal" scripts/orchestrator/shorts_pipeline.py` = 1
    - `grep -c "pause_after_gate" scripts/orchestrator/gate_guard.py` ≥ 1
    - `grep -c "ctx_config" scripts/orchestrator/gate_guard.py` ≥ 2 (__init__ param + self._ctx_config)
    - `grep -c "ctx_config=self.ctx.config" scripts/orchestrator/shorts_pipeline.py` = 1 (by-reference 전달)
    - `grep -c "pause-after" scripts/smoke/phase13_live_smoke.py` ≥ 2
    - `grep -c "_handle_pause_signal" scripts/smoke/phase13_live_smoke.py` ≥ 2 (def + call)
    - `py -3.11 -m pytest tests/phase15/test_pause_after_gate.py --no-cov -q 2>&1 | grep -cE "3 passed|PASSED"` ≥ 3
    - `py -3.11 -m pytest tests/phase15/test_runner_pause_signal.py --no-cov -q 2>&1 | grep -cE "3 passed|PASSED"` ≥ 3
    - `py -3.11 -m pytest tests/phase13/ -m "not live_smoke" --no-cov -q` 0 failures (Phase 13 regression)
    - `py -3.11 -m pytest tests/phase05/ tests/phase091/ --no-cov -q` 0 failures (GateGuard signature 확장 regression 안전)
    - `grep -c "대표님" scripts/orchestrator/shorts_pipeline.py` ≥ 1 (PipelinePauseSignal 메시지)
  </acceptance_criteria>
  <verify>
    <automated>py -3.11 -m pytest tests/phase15/test_pause_after_gate.py tests/phase15/test_runner_pause_signal.py tests/phase13/ -m "not live_smoke" tests/phase05/ tests/phase091/ -v --no-cov --tb=short</automated>
  </verify>
  <done>
    UFL-03 flag + PipelinePauseSignal + GateGuard ctx_config + __init__ 재정렬 + runner catch + 6 tests green + Phase 5/9.1/13 regression 0 failures.
  </done>
</task>

</tasks>

<verification>
Plan 15-04 전체 verify:
```bash
# UFL-01 + UFL-02 + UFL-03 tests
py -3.11 -m pytest tests/phase15/test_revision_flag.py tests/phase15/test_producer_feedback_injection.py tests/phase15/test_prescripted_invoker.py tests/phase15/test_pause_after_gate.py tests/phase15/test_runner_pause_signal.py -v --no-cov
# Expected: 18+ passed

# Regression
py -3.11 -m pytest tests/phase05/ tests/phase11/ tests/phase13/ -m "not live_smoke" tests/phase091/ tests/adapters/ --no-cov -q
# Phase 5 + Phase 11 + Phase 13 quick + Phase 9.1 + adapter contracts all green (GateGuard signature 확장 영향)

# CLI smoke (실 호출 없이 --help)
py -3.11 scripts/smoke/phase13_live_smoke.py --help 2>&1 | grep -cE "revision-from|feedback|revise-script|pause-after|evidence-dir"
# >= 5

# Phase 12 compression invariant
py -3.11 -c "from scripts.orchestrator.invokers import _compress_producer_output; c = _compress_producer_output({'gate':'SCRIPT','verdict':'PASS','error_codes':[]}); assert c['verdict']=='PASS'"
```
</verification>

<success_criteria>
- [ ] --evidence-dir flag 추가 (Task 15-04-00)
- [ ] UFL-01 --revision-from + --feedback flag 추가 + _apply_revision helper
- [ ] UFL-02 --revise-script + _PreScriptedProducerInvoker 클래스
- [ ] UFL-03 PipelinePauseSignal + GateGuard ctx_config + dispatch check + runner _handle_pause_signal
- [ ] GateGuard.__init__ signature 확장 (ctx_config kwarg) + ShortsPipeline.__init__ 재정렬 (ctx 뒤로)
- [ ] Producer __call__ prior_user_feedback user_payload 주입 (실제 signature 준수)
- [ ] Phase 12 _compress_producer_output SCRIPT verdict preservation 검증
- [ ] 18+ test green (4 revision + 3 feedback + 5 prescripted + 3 pause + 3 runner)
- [ ] Phase 5 / Phase 11 / Phase 13 non-live / Phase 9.1 regression 0 failures
- [ ] CLAUDE.md 금기 #3/#5 준수
- [ ] CLAUDE.md 필수 #7 — 모든 log/help/error 한국어 존댓말
</success_criteria>

<output>
완료 후 `.planning/phases/15-system-prompt-compression-user-feedback-loop/15-04-SUMMARY.md`.
- 15-VALIDATION.md rows 15-04-00, 15-04-01, 15-04-02, 15-04-03 ⬜ → ✅
- ROADMAP.md Phase 15 Plan 04 [ ] → [x]
- Wave 5 (Plan 06) unblocked for user feedback-aware live retry + isolated evidence-dir.
</output>
