---
phase: 12-agent-standardization-skill-routing-failures-protocol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/templates/producer.md.template
  - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/templates/inspector.md.template
  - tests/phase12/__init__.py
  - tests/phase12/conftest.py
  - tests/phase12/mocks/__init__.py
  - tests/phase12/mocks/mock_claude_cli.py
  - tests/phase12/fixtures/.gitkeep
  - tests/phase12/test_agent_md_schema.py
  - tests/phase12/test_mandatory_reads_prose.py
  - tests/phase12/test_skill_matrix_format.py
  - tests/phase12/test_failures_rotation.py
  - tests/phase12/test_f_d2_exception_batch.py
  - tests/phase12/test_supervisor_compress.py
  - scripts/validate/verify_agent_md_schema.py
  - .claude/agents/producers/trend-collector/AGENT.md
  - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-VALIDATION.md
autonomous: true
requirements:
  - AGENT-STD-01
  - AGENT-STD-02
status: pending
must_haves:
  truths:
    - "Producer template + Inspector template 2종 파일 존재 — 5 XML 블록 고정 순서 + 한국어 본문 + '샘플링 금지' 한국어 literal 포함"
    - "tests/phase12/ 6 test file + fixtures/ + mocks/ 스캐폴드 완성 — red stub 으로 import error 없이 collection 성공"
    - "scripts/validate/verify_agent_md_schema.py CLI 존재 — `--all` 플래그 + 단일 파일 인자 모두 동작 + exit 0/1 구분"
    - "trend-collector AGENT.md v1.1 → v1.2 승격 — 5 XML 블록 전수 완비 (3 블록 신규 삽입: `<output_format>` 보강 + `<skills>` 신규 + `<constraints>` 신규)"
    - "12-VALIDATION.md frontmatter `wave_0_complete: true` + `nyquist_compliant: true` 로 플립 + Per-Task Verification Map 15 row 완비"
  artifacts:
    - path: ".planning/phases/12-agent-standardization-skill-routing-failures-protocol/templates/producer.md.template"
      provides: "5-block AGENT.md schema baseline for 13 producers (Plan 02)"
      contains: "<mandatory_reads>, <output_format>, <skills>, <constraints>, 샘플링 금지"
    - path: ".planning/phases/12-agent-standardization-skill-routing-failures-protocol/templates/inspector.md.template"
      provides: "5-block AGENT.md schema baseline for 17 inspectors (Plan 03)"
      contains: "<mandatory_reads>, <output_format>, <skills>, <constraints>, producer_prompt 읽기 금지 (RUB-06 GAN 분리 mirror)"
    - path: "tests/phase12/conftest.py"
      provides: "공통 fixture — tmp FAILURES.md / MockClaudeCLI / synthetic producer_output 최소 seed"
    - path: "scripts/validate/verify_agent_md_schema.py"
      provides: "AGENT-STD-01 CI 엔트리포인트 — 5 XML 블록 regex scan + exit code"
    - path: ".claude/agents/producers/trend-collector/AGENT.md"
      provides: "v1.2 proof-of-concept — 첫 번째 migration 이 template round-trip 검증"
      contains: "version: 1.2"
  key_links:
    - from: "tests/phase12/test_agent_md_schema.py"
      to: ".claude/agents/producers/trend-collector/AGENT.md"
      via: "regex scan 5 XML 블록 presence + 고정 순서"
      pattern: "<role>.*<mandatory_reads>.*<output_format>.*<skills>.*<constraints>"
    - from: ".planning/phases/12-.../templates/producer.md.template"
      to: "Plan 02 Producer 12 migration targets"
      via: "copy-then-customize base file (D-A2-02 금지 direct clone)"
---

<objective>
Phase 12 의 기초 인프라를 단일 Plan 에 집중. 5 XML 블록 schema template 2종 + phase12 test 스캐폴드 + schema verifier CLI + trend-collector v1.2 승격(첫 migration 실측 round-trip) + VALIDATION.md 완성.

**Purpose**: Wave 0 는 후속 모든 Plan 의 regression 기반. 본 Plan 실패 시 Plan 02~07 착수 불가. trend-collector v1.2 승격은 template 이 실제로 동작함을 증명하는 smoke test.

**Output**: 2 templates + 6 test red stubs + 1 schema verifier + 1 mock CLI + trend-collector AGENT.md v1.2 + VALIDATION.md 플립.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-CONTEXT.md
@.planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-RESEARCH.md
@.planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-VALIDATION.md
@.claude/agents/producers/trend-collector/AGENT.md
@CLAUDE.md
</context>

<interfaces>
<!-- Phase 12 Plan 01 핵심 계약 — 후속 Plan 이 의존하는 surface. -->

trend-collector v1.1 실측 블록 순서 (부분 준수 2/5):
```
# (frontmatter boundary)
name, description, version: 1.1, role, category, maxTurns (frontmatter)
# (frontmatter boundary)
# trend-collector
<mandatory_reads> ... </mandatory_reads>
<output_format> ... </output_format>
## Purpose / Inputs / Outputs / Prompt / References / MUST REMEMBER (prose body)
```

v1.2 목표 5 블록 고정 순서 (D-A1-01):
```
# (frontmatter boundary)
frontmatter (version: 1.2, 기존 필드 유지)
# (frontmatter boundary)
# {agent_name}
<role> [한국어 1-3 sentence 핵심 역할] </role>
<mandatory_reads> [FAILURES.md + channel_bible + skills + "샘플링 금지" literal] </mandatory_reads>
<output_format> [JSON / markdown schema + 금지 패턴 예시] </output_format>
<skills> [required/optional 스킬 목록 — matrix SSOT 참조] </skills>
<constraints> [금지사항 + RUB-NN 미러 + AF-NN 관련] </constraints>
## Purpose / Inputs / Outputs / Prompt / References / MUST REMEMBER (prose body 보존)
```

verify_agent_md_schema.py 계약:
- CLI: `py -3.11 scripts/validate/verify_agent_md_schema.py [--all | <path>]`
- Exit 0 = 모든 scan 대상 schema 준수. Exit 1 = 하나 이상 위반 + stderr 에 violation report.
- `--all` = `.claude/agents/producers/*/AGENT.md` + `.claude/agents/inspectors/*/*/AGENT.md` 스캔 (harvest-importer + shorts-supervisor exclude).
- 5 블록 presence 검사 + 순서 검사 (regex: `<role>.*?</role>.*?<mandatory_reads>.*?</mandatory_reads>.*?<output_format>.*?</output_format>.*?<skills>.*?</skills>.*?<constraints>.*?</constraints>` DOTALL).

MockClaudeCLI 계약 (Plan 07 consumer) — **실측 검증 완료 2026-04-21**:
- 실제 `_invoke_claude_cli` signature (scripts/orchestrator/invokers.py L213-220): `(system_prompt: str, user_prompt: str, json_schema: str, cli_path: str, timeout_s: int = DEFAULT_TIMEOUT_S, agent_label: str = "claude-cli") -> str`
- `ClaudeAgentSupervisorInvoker._call` (L406-412) 호출 시 **4개 kwargs** 만 전달 (`system_prompt=`, `user_prompt=`, `json_schema=`, `cli_path=`). `timeout_s` / `agent_label` 은 default 사용.
- `class MockClaudeCLI` callable seam — `__call__(self, *args, **kwargs) -> str` (defensive 전면 수용 패턴. real signature 변동 시 `TypeError` 없이 동작).
- kwargs 접근 편의 추출: `self.last_system_prompt = kwargs.get("system_prompt") or (args[0] if args else None)`, `self.last_user_prompt = kwargs.get("user_prompt") or (args[1] if len(args) >= 2 else None)`.
- Returns pre-seeded JSON string (configurable via `seed(response_json)`).
- Tracks `last_user_prompt` / `last_system_prompt` / `call_count` for assertion.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Producer + Inspector template 2종 작성 (AGENT-STD-01/02 baseline)</name>
  <files>.planning/phases/12-agent-standardization-skill-routing-failures-protocol/templates/producer.md.template, .planning/phases/12-agent-standardization-skill-routing-failures-protocol/templates/inspector.md.template</files>
  <read_first>
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-CONTEXT.md (§D-A1-01~04 + §canonical_refs + §specifics — 16 locked decisions)
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-RESEARCH.md (§Architecture Patterns Pattern 1 — trend-collector v1.1 → v1.2 schema, §Pitfalls #1 RUB-06 손실 방지)
    - .claude/agents/producers/trend-collector/AGENT.md (prototype — 2/5 블록 기존 상태, body prose 보존 대상)
    - .claude/agents/inspectors/structural/ins-schema-integrity/AGENT.md (inspector baseline — 본문 구조 참조용, 읽기 전용)
    - CLAUDE.md §🔴 금기 + §🟢 필수 (한국어 존댓말 + T2V/Selenium 금지 + FAILURES append-only)
  </read_first>
  <action>
두 template 파일을 생성합니다. 두 파일 모두 frontmatter 는 Jinja 스타일 placeholder (`{{ name }}`, `{{ description }}`, `{{ version }}`, `{{ role }}`, `{{ category }}`, `{{ maxTurns }}`) 로 작성합니다.

### producer.md.template 내용 (고정 스켈레톤):

```markdown
# (frontmatter boundary)
name: {{ name }}
description: {{ description }}
version: 1.2
role: producer
category: {{ category }}  # core | support
maxTurns: {{ maxTurns }}
# (frontmatter boundary)

# {{ name }}

<role>
{{ role_body_ko }}
(한국어 1~3 문장 — 본 에이전트의 핵심 책임 + 파이프라인 내 위치 + 상하류 agent 명시)
</role>

<mandatory_reads>
## 필수 읽기 (매 호출마다 전수 읽기, 샘플링 금지 — 대표님 session #29 지시)

1. `.claude/failures/FAILURES.md` — 전체 (500줄 cap 하 전수 읽기 가능 — FAIL-PROTO-01). 과거 실패 전수 인지 후 작업.
2. `wiki/continuity_bible/channel_identity.md` — 채널 통합 정체성 (공통 baseline).
   {{ niche_bible_path }}  # 예: .preserved/harvested/theme_bible_raw/incidents.md (niche 확정 후)
3. `.claude/skills/{{ primary_skill }}/SKILL.md` — 본 에이전트 primary skill 계약 (예: gate-dispatcher).
{{ additional_reads }}  # optional additional references

**원칙**: 위 1~3 항목은 매 호출마다 전수 읽기. 샘플링/요약본 읽기/기억 의존 금지.
</mandatory_reads>

<output_format>
## 출력 형식 (엄격 준수)

**반드시 JSON 객체만 출력. 설명문/질문/대화체 금지.**

입력이 애매하거나 정보 부족 시에도 질문하지 마십시오. 대신 다음 형식:

```json
{"error": "reason", "needed_inputs": ["..."]}
```

정상 응답 스키마 (Outputs 섹션 상세 참조):

```json
{{ output_schema_example }}
```

**금지 패턴 (F-D2-EXCEPTION-01 교훈)**:
- 금지: 대화체 시작 ("대표님, ...", "알겠습니다", "네 대표님")
- 금지: 질문/옵션 제시 ("어떤 것을 원하십니까?", "A/B 선택지")
- 금지: 서문/감탄사 ("확인했습니다", "분석 결과")
- 금지: 코드 펜스 후 꼬리 설명

**이유**: invoker 는 stdout 첫 바이트부터 JSON parse 시도. 대화체 시작 시 JSONDecodeError → RuntimeError → retry-with-nudge (최대 3회) → 실패 시 Circuit Breaker trip.
</output_format>

<skills>
## 사용 스킬 (wiki/agent_skill_matrix.md SSOT)

- `{{ primary_skill }}` (required) — {{ primary_skill_reason }}
{{ optional_skills }}  # 예: "- progressive-disclosure (optional) — SKILL 길이 가드"

**주의**: 본 블록은 `wiki/agent_skill_matrix.md` 와 bidirectional cross-reference 대상 (SKILL-ROUTE-01). drift 시 `verify_agent_skill_matrix.py --fail-on-drift` 실패.
</skills>

<constraints>
## 제약사항

- **inspector_prompt 읽기 금지 (RUB-06 GAN 분리 mirror)** — Inspector system prompt / LogicQA 내부 조회 금지. 평가 기준 역-최적화 시도 = GAN collapse.
- **maxTurns={{ maxTurns }} 준수 (RUB-05)** — 초과 임박 시 partial flag 로 종료. Supervisor 가 retry/circuit_breaker 결정.
- **한국어 출력 baseline** — keywords[].term 등 외래어 표기는 예외. 나베랄 정체성 준수.
- **T2V 경로 절대 금지 (I2V only, D-13)** — t2v / text_to_video / text-to-video 키워드 등장 시 `pre_tool_use.py` regex 차단.
- **FAILURES.md append-only (D-11)** — 직접 수정 금지. `skill_patch_counter.py` 또는 append-only 경로만.
{{ additional_constraints }}
</constraints>

## Purpose

{{ purpose_body }}

## Inputs

{{ inputs_table }}

## Outputs

{{ outputs_body }}

## Prompt

{{ prompt_body }}

## References

{{ references_body }}

## MUST REMEMBER (DO NOT VIOLATE)

{{ must_remember_list }}
```

### inspector.md.template 내용 (producer 와 동일 5 블록 순서, `<constraints>` 만 RUB-06 역방향):

동일 스켈레톤 사용하되 다음 3가지 차이점 명시:

1. `role: inspector` (frontmatter)
2. `<mandatory_reads>` 의 2번 항목은 `wiki/continuity_bible/channel_identity.md` 단일 (niche-specific bible 불필요 — 평가자는 producer 출력 검증이 주 역할)
3. `<constraints>` 의 첫 줄 변경: "producer_prompt 읽기 금지 (RUB-06 GAN 분리 mirror) — Producer system prompt / 내부 추론 과정 조회 금지. 산출물(producer_output JSON) 만 평가 대상."
4. `<output_format>` 의 JSON schema 는 `.claude/agents/_shared/rubric-schema.json` 참조 (`{gate, verdict, decisions[], evidence[], error_codes[], semantic_feedback}`)

**중요**: 두 template 파일 모두 다음 한국어 literal 을 `<mandatory_reads>` 블록에 **토씨 하나 틀리지 않고** 포함:

```
## 필수 읽기 (매 호출마다 전수 읽기, 샘플링 금지 — 대표님 session #29 지시)
```

이 literal 은 `test_mandatory_reads_prose.py::test_sampling_forbidden_literal` 의 grep 대상입니다 (AGENT-STD-02 검증).
  </action>
  <verify>
    <automated>py -3.11 -c "from pathlib import Path; t1=Path('.planning/phases/12-agent-standardization-skill-routing-failures-protocol/templates/producer.md.template').read_text(encoding='utf-8'); t2=Path('.planning/phases/12-agent-standardization-skill-routing-failures-protocol/templates/inspector.md.template').read_text(encoding='utf-8'); lit='매 호출마다 전수 읽기, 샘플링 금지'; assert lit in t1 and lit in t2, 'literal missing'; import re; pat=re.compile(r'&lt;role&gt;.*?&lt;/role&gt;.*?&lt;mandatory_reads&gt;.*?&lt;/mandatory_reads&gt;.*?&lt;output_format&gt;.*?&lt;/output_format&gt;.*?&lt;skills&gt;.*?&lt;/skills&gt;.*?&lt;constraints&gt;.*?&lt;/constraints&gt;', re.DOTALL); assert pat.search(t1) and pat.search(t2), 'block order wrong'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/12-agent-standardization-skill-routing-failures-protocol/templates/producer.md.template` exists (size > 1500 bytes)
    - File `.planning/phases/12-agent-standardization-skill-routing-failures-protocol/templates/inspector.md.template` exists (size > 1500 bytes)
    - Both files contain literal string `매 호출마다 전수 읽기, 샘플링 금지` (grep count ≥ 1 each)
    - Both files contain all 5 XML blocks in fixed order: `<role>` → `<mandatory_reads>` → `<output_format>` → `<skills>` → `<constraints>` (regex DOTALL match succeeds)
    - producer template frontmatter contains `role: producer` literal; inspector template frontmatter contains `role: inspector` literal
    - producer `<constraints>` contains `inspector_prompt 읽기 금지 (RUB-06 GAN 분리 mirror)`
    - inspector `<constraints>` contains `producer_prompt 읽기 금지 (RUB-06 GAN 분리 mirror)`
    - Both templates contain literal `T2V 경로 절대 금지 (I2V only, D-13)`
  </acceptance_criteria>
  <done>
2 template 파일 모두 5 블록 schema + "샘플링 금지" literal + RUB-06 역방향 미러 + I2V-only 제약 명시. Plan 02/03 clone base 로 즉시 사용 가능.
  </done>
</task>

<task type="auto">
  <name>Task 2: tests/phase12/ 스캐폴드 + 6 test red stubs + MockClaudeCLI</name>
  <files>tests/phase12/__init__.py, tests/phase12/conftest.py, tests/phase12/mocks/__init__.py, tests/phase12/mocks/mock_claude_cli.py, tests/phase12/fixtures/.gitkeep, tests/phase12/test_agent_md_schema.py, tests/phase12/test_mandatory_reads_prose.py, tests/phase12/test_skill_matrix_format.py, tests/phase12/test_failures_rotation.py, tests/phase12/test_f_d2_exception_batch.py, tests/phase12/test_supervisor_compress.py</files>
  <read_first>
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-RESEARCH.md (§Validation Architecture §Wave 0 Gaps — 15 test case 정의)
    - tests/phase07/mocks/mock_kling.py (Phase 7 MockXxx 패턴 참조용 — import convention)
    - tests/phase11/conftest.py (conftest fixture 패턴 참조용, sys.path prelude convention)
    - scripts/orchestrator/invokers.py lines 121-167 (`_invoke_claude_cli_once` 5-arg 기본 signature — MockClaudeCLI 가 `_call` 호출 시 kwargs 4개 수용)
    - scripts/orchestrator/invokers.py lines 213-260 (`_invoke_claude_cli` retry wrapper 6-arg signature — `timeout_s`, `agent_label` 포함. MockClaudeCLI 는 real function 대체이므로 signature 호환 필수)
    - scripts/orchestrator/invokers.py lines 371-426 (ClaudeAgentSupervisorInvoker `_cli_runner` seam — `__call__` → `_call` → `self._cli_runner(system_prompt=..., user_prompt=..., json_schema=..., cli_path=...)` 4개 kwargs 만 전달. `timeout_s`/`agent_label` 은 default.)
  </read_first>
  <action>
다음 11 파일 생성.

### 1. tests/phase12/__init__.py
빈 파일 (Phase 4~11 컨벤션).

### 2. tests/phase12/conftest.py
공통 fixture:
```python
"""Phase 12 공통 fixture — tmp FAILURES.md, MockClaudeCLI, synthetic producer_output."""

from __future__ import annotations
import json
import sys
from pathlib import Path
import pytest

# Phase 11 precedent: sys.path prelude for mocks module resolution
sys.path.insert(0, str(Path(__file__).parent))

REPO_ROOT = Path(__file__).resolve().parents[2]


@pytest.fixture
def tmp_failures_file(tmp_path: Path) -> Path:
    """tmp FAILURES.md — 31-line head (schema + notice) + configurable body."""
    head = (
        "# FAILURES.md — Append-Only Reservoir\n\n"
        "> **D-11:** Append-only.\n\n"
        "## Entry Schema\n\n```\n### FAIL-NNN\n```\n\n"
        "## Entries\n\n"
    )
    # Head is exactly 10 lines; pad to 31 with blank lines so tests control tail
    lines = head.splitlines()
    while len(lines) < 31:
        lines.append("")
    p = tmp_path / "FAILURES.md"
    p.write_text("\n".join(lines) + "\n", encoding="utf-8")
    return p


@pytest.fixture
def synthetic_producer_output_small() -> dict:
    """5 decisions × 2 error_codes — under compression budget (2000 chars)."""
    return {
        "gate": "SCRIPT",
        "verdict": "RETRY",
        "decisions": [
            {"rule": f"rule_{i}", "severity": "medium", "score": 60 + i,
             "evidence": f"decision {i} evidence"}
            for i in range(5)
        ],
        "error_codes": ["ERR_A", "ERR_B"],
        "semantic_feedback": "5 decisions seed",
    }
```

### 3. tests/phase12/mocks/__init__.py
빈 파일.

### 4. tests/phase12/mocks/mock_claude_cli.py

**중요 (실측 검증 2026-04-21)**: 실제 `_invoke_claude_cli` (invokers.py L213-220) signature 은 6 arg (`system_prompt, user_prompt, json_schema, cli_path, timeout_s=DEFAULT_TIMEOUT_S, agent_label="claude-cli"`) 이고 `ClaudeAgentSupervisorInvoker._call` (L406-412) 은 그중 **4개 kwargs** 만 전달 (`system_prompt=`, `user_prompt=`, `json_schema=`, `cli_path=`). MockClaudeCLI 는 미래 signature drift 에도 안전하도록 `*args, **kwargs` defensive 패턴 채택.

```python
"""MockClaudeCLI — Phase 7 MockKling 패턴 복제. Plan 07 supervisor compression 테스트 seam.

실제 `_invoke_claude_cli` signature (invokers.py L213-220):
    (system_prompt, user_prompt, json_schema, cli_path, timeout_s=DEFAULT_TIMEOUT_S, agent_label="claude-cli")

`ClaudeAgentSupervisorInvoker._call` (L406-412) 호출 시 4개 kwargs 만 전달 — timeout_s/agent_label 은 default 사용.
MockClaudeCLI 는 *args/**kwargs 로 전면 수용하여 real signature 변동 시 TypeError 회피.
"""

from __future__ import annotations
import json
from typing import Any


class MockClaudeCLI:
    """Callable replacement for `_invoke_claude_cli`.

    Usage in tests:
        mock = MockClaudeCLI()
        mock.seed({"verdict": "PASS"})
        invoker = ClaudeAgentSupervisorInvoker(..., cli_runner=mock)
        verdict = invoker(GateName.SCRIPT, producer_output)
        assert mock.last_user_prompt  # assertable
    """

    def __init__(self) -> None:
        self._seeded: str = json.dumps({"verdict": "PASS"})
        self.last_user_prompt: str | None = None
        self.last_system_prompt: str | None = None
        self.last_kwargs: dict | None = None
        self.call_count: int = 0

    def seed(self, response: Any) -> None:
        self._seeded = response if isinstance(response, str) else json.dumps(response)

    def __call__(self, *args: Any, **kwargs: Any) -> str:
        """Defensive signature — accepts both positional and keyword form.

        Caller (`ClaudeAgentSupervisorInvoker._call`) uses kwargs:
            self._cli_runner(system_prompt=..., user_prompt=..., json_schema=..., cli_path=...)
        But future callers may use positional. Extract key fields from either.
        """
        self.last_kwargs = dict(kwargs)
        self.last_system_prompt = (
            kwargs.get("system_prompt") if "system_prompt" in kwargs
            else (args[0] if len(args) >= 1 else None)
        )
        self.last_user_prompt = (
            kwargs.get("user_prompt") if "user_prompt" in kwargs
            else (args[1] if len(args) >= 2 else None)
        )
        self.call_count += 1
        return self._seeded
```

### 5. tests/phase12/fixtures/.gitkeep
빈 파일. Plan 07 Task 1 이 `producer_output_gate2_oversized.json` 여기에 생성.

### 6~11. 6 test red stubs
각 파일은 최소 1개 `pytest.mark.skip("Wave 0 red stub — populated by Plan NN")` decorated test 로 구성 — import error 없이 collection 성공하고 skip 으로 처리되도록 합니다.

**tests/phase12/test_agent_md_schema.py** (Plan 02/03 에서 populate):
```python
"""AGENT-STD-01 — 30 AGENT.md × 5 XML block schema compliance."""
import pytest


@pytest.mark.skip(reason="Wave 0 red stub — populated by Plan 02 (producer) + Plan 03 (inspector)")
def test_all_30_agents_have_5_blocks():
    raise NotImplementedError


@pytest.mark.skip(reason="Wave 0 red stub — populated by Plan 02/03")
def test_excluded_agents_not_scanned():
    raise NotImplementedError
```

**tests/phase12/test_mandatory_reads_prose.py** (Plan 06 에서 populate):
```python
"""AGENT-STD-02 — <mandatory_reads> 블록 한국어 literal + 3 항목 keyword."""
import pytest


@pytest.mark.skip(reason="Wave 0 red stub — populated by Plan 06")
def test_sampling_forbidden_literal():
    raise NotImplementedError


@pytest.mark.skip(reason="Wave 0 red stub — populated by Plan 06")
def test_three_mandatory_items_present():
    raise NotImplementedError
```

**tests/phase12/test_skill_matrix_format.py** (Plan 04 에서 populate):
```python
"""SKILL-ROUTE-01 — markdown matrix parse + cell value validation + reciprocity."""
import pytest


@pytest.mark.skip(reason="Wave 0 red stub — populated by Plan 04")
def test_matrix_has_30_rows():
    raise NotImplementedError


@pytest.mark.skip(reason="Wave 0 red stub — populated by Plan 04")
def test_matrix_cell_values_legal():
    raise NotImplementedError


@pytest.mark.skip(reason="Wave 0 red stub — populated by Plan 04")
def test_matrix_reciprocity_with_agent_md():
    raise NotImplementedError
```

**tests/phase12/test_failures_rotation.py** (Plan 05 에서 populate):
```python
"""FAIL-PROTO-01 — 500줄 cap Hook + rotation idempotency + immutable _imported file."""
import pytest


@pytest.mark.skip(reason="Wave 0 red stub — populated by Plan 05")
def test_hook_denies_over_500_lines():
    raise NotImplementedError


@pytest.mark.skip(reason="Wave 0 red stub — populated by Plan 05")
def test_rotate_idempotent():
    raise NotImplementedError


@pytest.mark.skip(reason="Wave 0 red stub — populated by Plan 05")
def test_imported_file_sha256_unchanged():
    raise NotImplementedError


@pytest.mark.skip(reason="Wave 0 red stub — populated by Plan 05")
def test_archive_month_tag():
    raise NotImplementedError
```

**tests/phase12/test_f_d2_exception_batch.py** (Plan 02 Wave commit 시 populate):
```python
"""FAIL-PROTO-02 — 30+ 파일 batch commit 단일 F-D2-EXCEPTION-02 entry."""
import pytest


@pytest.mark.skip(reason="Wave 0 red stub — populated by Plan 02 (first batch use)")
def test_batch_commit_single_entry():
    raise NotImplementedError


@pytest.mark.skip(reason="Wave 0 red stub — populated by Plan 02")
def test_batch_idempotent_replay():
    raise NotImplementedError
```

**tests/phase12/test_supervisor_compress.py** (Plan 07 에서 populate):
```python
"""AGENT-STD-03 — _compress_producer_output(raw) summary-only + severity ordering."""
import pytest


@pytest.mark.skip(reason="Wave 0 red stub — populated by Plan 07")
def test_compression_ratio_over_40pct():
    raise NotImplementedError


@pytest.mark.skip(reason="Wave 0 red stub — populated by Plan 07")
def test_critical_decisions_preserved():
    raise NotImplementedError


@pytest.mark.skip(reason="Wave 0 red stub — populated by Plan 07")
def test_phase11_smoke_replay_under_cli_limit():
    raise NotImplementedError
```
  </action>
  <verify>
    <automated>py -3.11 -m pytest tests/phase12/ --collect-only -q 2>&1 | grep -E "collected|test_" | head -40; py -3.11 -m pytest tests/phase12/ -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `py -3.11 -m pytest tests/phase12/ --collect-only -q` exits 0 (collection succeeds, no import errors)
    - Collection report shows ≥ 15 tests collected (sum of all stubs)
    - `py -3.11 -m pytest tests/phase12/ -q` exits 0 with "skipped" count ≥ 15 and 0 failed
    - File `tests/phase12/mocks/mock_claude_cli.py` defines class `MockClaudeCLI` with `__call__(self, *args, **kwargs)` defensive signature (grep `def __call__(self, \*args` ≥ 1)
    - MockClaudeCLI handles both kwargs-form call (`mock(system_prompt=..., user_prompt=..., json_schema=..., cli_path=...)`) and positional (`mock("sys", "user", "schema", "path")`) — smoke: `py -3.11 -c "from tests.phase12.mocks.mock_claude_cli import MockClaudeCLI; m=MockClaudeCLI(); m.seed({'verdict':'PASS'}); r=m(system_prompt='s', user_prompt='u', json_schema='{}', cli_path='p'); assert m.last_user_prompt=='u'; r2=m('a','b','c','d'); assert m.last_user_prompt=='b'; print('OK')"` exits 0
    - File `tests/phase12/conftest.py` defines fixture `tmp_failures_file` (returns Path with 31-line head) and `synthetic_producer_output_small` (returns dict with 5 decisions)
    - File `tests/phase12/fixtures/.gitkeep` exists (non-None empty file for git tracking)
  </acceptance_criteria>
  <done>
phase12 test suite collection + skip 실행 0 failure. Plan 02~07 populate 시 skip decorator 제거 + 실 assertion 채우면 즉시 active. MockClaudeCLI 가 Plan 07 의 `cli_runner=mock` 주입 경로 개통 + defensive *args/**kwargs 로 real signature drift 차단.
  </done>
</task>

<task type="auto">
  <name>Task 3: verify_agent_md_schema.py CLI — 5 블록 regex + exit code 계약</name>
  <files>scripts/validate/verify_agent_md_schema.py</files>
  <read_first>
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-RESEARCH.md (§Pattern 1 — trend-collector v1.1 → v1.2 + §Pitfalls #5 Wave regression)
    - scripts/validate/validate_all_agents.py (기존 AGENT-07/08/09 validator — 패턴 참조)
    - .claude/agents/producers/trend-collector/AGENT.md (v1.1 현재 상태 — 2/5 블록 부분 준수, 본 CLI 가 Plan 01 Task 4 이후 v1.2 로 승격되면 exit 0 기대)
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/templates/producer.md.template (Task 1 산출)
  </read_first>
  <action>
`scripts/validate/verify_agent_md_schema.py` 신규 파일 생성. Python 3.11 stdlib-only (argparse, re, pathlib, sys). Windows cp949 guard (`sys.stdout.reconfigure(encoding='utf-8', errors='replace')`).

핵심 로직:

```python
#!/usr/bin/env python3
"""verify_agent_md_schema.py — AGENT-STD-01 compliance checker.

Scans AGENT.md files for the 5-XML-block schema in fixed order:
  <role> → <mandatory_reads> → <output_format> → <skills> → <constraints>

Exits 0 if all scanned files comply. Exits 1 if any violation,
with stderr report per file.

Scope:
  --all : scan .claude/agents/producers/*/AGENT.md + .claude/agents/inspectors/*/*/AGENT.md
          EXCLUDE: harvest-importer (Phase 3 deprecated), shorts-supervisor (out of Phase 12 scope).
  <path>: scan single file.

D-A1-01 / AGENT-STD-01.
"""

from __future__ import annotations
import argparse
import re
import sys
from pathlib import Path

# Windows cp949 guard (Phase 10 D-22)
try:
    sys.stdout.reconfigure(encoding="utf-8", errors="replace")
    sys.stderr.reconfigure(encoding="utf-8", errors="replace")
except Exception:
    pass

# D-A1-01 fixed order regex — DOTALL 로 다중 라인 허용
SCHEMA_RE = re.compile(
    r"<role>.*?</role>"
    r".*?<mandatory_reads>.*?</mandatory_reads>"
    r".*?<output_format>.*?</output_format>"
    r".*?<skills>.*?</skills>"
    r".*?<constraints>.*?</constraints>",
    re.DOTALL,
)

BLOCK_NAMES = ("role", "mandatory_reads", "output_format", "skills", "constraints")

# Phase 12 scope exclusions
EXCLUDED_AGENTS = {"harvest-importer", "shorts-supervisor"}

REPO_ROOT = Path(__file__).resolve().parents[2]


def _collect_all_agent_mds() -> list[Path]:
    """Scan producer + inspector AGENT.md files, excluding Phase 12 scope outs."""
    targets = []
    producers_dir = REPO_ROOT / ".claude" / "agents" / "producers"
    for agent_dir in sorted(producers_dir.iterdir()):
        if not agent_dir.is_dir() or agent_dir.name in EXCLUDED_AGENTS:
            continue
        agent_md = agent_dir / "AGENT.md"
        if agent_md.exists():
            targets.append(agent_md)
    inspectors_dir = REPO_ROOT / ".claude" / "agents" / "inspectors"
    for category_dir in sorted(inspectors_dir.iterdir()):
        if not category_dir.is_dir():
            continue
        for agent_dir in sorted(category_dir.iterdir()):
            if not agent_dir.is_dir() or agent_dir.name in EXCLUDED_AGENTS:
                continue
            agent_md = agent_dir / "AGENT.md"
            if agent_md.exists():
                targets.append(agent_md)
    return targets


def verify_file(path: Path) -> tuple[bool, list[str]]:
    """Return (ok, missing_blocks). ok=True if all 5 blocks present in order."""
    text = path.read_text(encoding="utf-8")
    # Per-block presence
    missing = [name for name in BLOCK_NAMES
               if f"<{name}>" not in text or f"</{name}>" not in text]
    if missing:
        return False, missing
    # Fixed order
    if not SCHEMA_RE.search(text):
        return False, ["(order violation — blocks exist but not in fixed sequence)"]
    return True, []


def main(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description="AGENT-STD-01 compliance checker")
    group = parser.add_mutually_exclusive_group(required=True)
    group.add_argument("--all", action="store_true",
                       help="Scan all 30 producer + inspector AGENT.md files")
    group.add_argument("path", type=Path, nargs="?",
                       help="Single AGENT.md file to scan")
    args = parser.parse_args(argv)

    if args.all:
        targets = _collect_all_agent_mds()
    else:
        if not args.path or not args.path.exists():
            print(f"FAIL: {args.path} does not exist", file=sys.stderr)
            return 1
        targets = [args.path]

    violations = []
    for path in targets:
        ok, missing = verify_file(path)
        if not ok:
            rel = path.relative_to(REPO_ROOT) if REPO_ROOT in path.parents else path
            violations.append((str(rel), missing))

    if violations:
        print(f"FAIL: {len(violations)}/{len(targets)} AGENT.md violate 5-block schema",
              file=sys.stderr)
        for rel, missing in violations:
            print(f"  - {rel}: missing/misordered = {missing}", file=sys.stderr)
        return 1

    print(f"OK: {len(targets)}/{len(targets)} AGENT.md comply with 5-block schema")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

Plan 01 단계에서 `--all` 실행 시 29/30 FAIL 예상 (trend-collector 만 v1.2 승격 후 통과). Plan 02/03 이 나머지 29 개 를 순차 migration 하면서 `--all` exit 0 로 수렴.
  </action>
  <verify>
    <automated>py -3.11 scripts/validate/verify_agent_md_schema.py .planning/phases/12-agent-standardization-skill-routing-failures-protocol/templates/producer.md.template 2>&1; py -3.11 scripts/validate/verify_agent_md_schema.py --all 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `py -3.11 scripts/validate/verify_agent_md_schema.py --all` executes without exception (exit code 0 or 1, not crash)
    - Running with `--all` before Task 4 produces exit 1 + stderr lists ≥ 29 violating AGENT.md files
    - Running with `--all` after Task 4 (trend-collector v1.2) produces exit 1 + stderr lists exactly 29 violations (not 30 — trend-collector now compliant)
    - `py -3.11 scripts/validate/verify_agent_md_schema.py --help` shows `--all` flag documented
    - File has `sys.stdout.reconfigure(encoding="utf-8", errors="replace")` block for Windows cp949 guard
    - Function `_collect_all_agent_mds()` excludes `harvest-importer` and `shorts-supervisor` (grep `EXCLUDED_AGENTS` finds 2 entries)
  </acceptance_criteria>
  <done>
verify_agent_md_schema.py CLI 개통. AGENT-STD-01 자동 검증 surface 확보. Plan 02/03 각 Wave commit 후 `--all` 실행으로 점진적 GREEN 수렴 확인.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: trend-collector AGENT.md v1.1 → v1.2 승격 (첫 migration — template round-trip 증명)</name>
  <files>.claude/agents/producers/trend-collector/AGENT.md</files>
  <behavior>
    - Test: `py -3.11 scripts/validate/verify_agent_md_schema.py .claude/agents/producers/trend-collector/AGENT.md` exits 0
    - Test: frontmatter `version` 필드가 `1.2` (unit test: YAML parse + field check)
    - Test: 5 XML 블록 모두 존재 + 고정 순서 (regex DOTALL)
    - Test: `<skills>` 블록에 `gate-dispatcher (required)` literal 존재
    - Test: `<constraints>` 블록에 `inspector_prompt 읽기 금지 (RUB-06 GAN 분리 mirror)` literal 존재
    - Test: 기존 body prose (Purpose / Inputs / Outputs / Prompt / References / MUST REMEMBER) 보존 — `grep -c "## MUST REMEMBER"` ≥ 1
  </behavior>
  <read_first>
    - .claude/agents/producers/trend-collector/AGENT.md (현 v1.1 상태 — 본 파일을 v1.2 로 승격)
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/templates/producer.md.template (Task 1 산출 — clone base)
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-CONTEXT.md §D-A1-01~04 (schema lock)
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-RESEARCH.md §Pitfalls #1 (RUB-06 본문 손실 방지)
    - scripts/validate/verify_agent_md_schema.py (Task 3 산출 — verification surface)
  </read_first>
  <action>
trend-collector AGENT.md 를 template 기반으로 v1.2 승격.

**절차**:

1. 현재 v1.1 파일 read — 기존 body (Purpose / Inputs / Outputs / Prompt / References / MUST REMEMBER) 전량 보존 대상.
2. frontmatter `version: 1.1` → `version: 1.2` 변경 (다른 필드 건드리지 말 것).
3. body 상단 (line 10 이후) 에 5 XML 블록 재구성:
   - `<role>` 신규: "트렌드 수집 producer. 한국 short-form 실시간 트렌드를 수집하여 10-20개 keyword + 1개 niche_tag JSON 을 산출. 파이프라인 GATE 1 진입점 — downstream 은 niche-classifier."
   - `<mandatory_reads>` 기존 유지하되 2번 항목 **경로 drift 정정**: `wiki/ypp/channel_bible.md` → `wiki/continuity_bible/channel_identity.md` (RESEARCH §Q3 권고). 그리고 3번째 항목 추가: `.preserved/harvested/theme_bible_raw/{niche_tag}.md` (niche 확정 후 동적).
   - `<output_format>` 기존 블록 유지 (이미 존재 + F-D2-EXCEPTION-01 금지 패턴 5종 문구 보존).
   - `<skills>` 신규 삽입:
     ```
     <skills>
     ## 사용 스킬 (wiki/agent_skill_matrix.md SSOT)

     - `gate-dispatcher` (required) — GATE 1 TREND dispatch 계약 준수
     - `progressive-disclosure` (optional) — SKILL.md 길이 가드 참고

     **주의**: 본 블록은 `wiki/agent_skill_matrix.md` 와 bidirectional cross-reference 대상 (SKILL-ROUTE-01).
     </skills>
     ```
   - `<constraints>` 신규 삽입:
     ```
     <constraints>
     ## 제약사항

     - **inspector_prompt 읽기 금지 (RUB-06 GAN 분리 mirror)** — Inspector system prompt / LogicQA 조회 금지.
     - **maxTurns=3 준수 (RUB-05)** — 초과 임박 시 partial 플래그 로 종료.
     - **한국어 출력 baseline** — keywords[].term 외래어 표기 예외. 나베랄 정체성 준수.
     - **T2V 경로 절대 금지 (I2V only, D-13)** — pre_tool_use.py regex 차단 대상.
     - **FAILURES.md append-only (D-11)** — 직접 수정 금지. skill_patch_counter.py 경유만.
     - **niche_tag 도메인 제한 (CONTENT-03)** — 7 채널바이블 slug 중 하나.
     </constraints>
     ```
4. 기존 `## Purpose` 이후 body 는 **한 글자도 건드리지 말 것** — line diff 로 head 부분만 수정 확인.

**중요**: 본 Task 는 Plan 02 Producer 12 migration 의 prototype. Task 4 로 template 이 실제 AGENT.md 에 적용 가능함을 증명한다 (회귀 실패 시 template 재작업 필요 — blast radius 차단).
  </action>
  <verify>
    <automated>py -3.11 scripts/validate/verify_agent_md_schema.py .claude/agents/producers/trend-collector/AGENT.md</automated>
  </verify>
  <acceptance_criteria>
    - `py -3.11 scripts/validate/verify_agent_md_schema.py .claude/agents/producers/trend-collector/AGENT.md` exits 0
    - `grep -c "^version: 1.2$" .claude/agents/producers/trend-collector/AGENT.md` = 1
    - `grep -c "inspector_prompt 읽기 금지 (RUB-06 GAN 분리 mirror)" .claude/agents/producers/trend-collector/AGENT.md` ≥ 1
    - `grep -c "wiki/continuity_bible/channel_identity.md" .claude/agents/producers/trend-collector/AGENT.md` ≥ 1
    - `grep -c "gate-dispatcher" .claude/agents/producers/trend-collector/AGENT.md` ≥ 1
    - `grep -c "^## MUST REMEMBER" .claude/agents/producers/trend-collector/AGENT.md` ≥ 1 (body prose 보존)
    - `grep -c "^## Purpose$" .claude/agents/producers/trend-collector/AGENT.md` ≥ 1
    - `py -3.11 -m pytest tests/phase04/ -q` exits 0 (244 passed — Phase 4 regression 무회귀)
  </acceptance_criteria>
  <done>
trend-collector v1.2 승격 성공. template round-trip 증명 완료. Plan 02 Producer 12 migration 이 safe — 동일 절차 반복.
  </done>
</task>

<task type="auto">
  <name>Task 5: 12-VALIDATION.md frontmatter flip + Per-Task Verification Map 완비</name>
  <files>.planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-VALIDATION.md</files>
  <read_first>
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-VALIDATION.md (skeleton — Plan 01 이 populate)
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-RESEARCH.md §Validation Architecture (15 test case — Map populate source)
  </read_first>
  <action>
`.planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-VALIDATION.md` 수정:

1. Frontmatter:
   - `status: draft` → `status: wave_0_complete`
   - `nyquist_compliant: false` → `nyquist_compliant: true`
   - `wave_0_complete: false` → `wave_0_complete: true`

2. "Per-Task Verification Map" 섹션 — placeholder 행을 15개 구체 row 로 교체. 각 row 는 RESEARCH.md §Validation Architecture Phase Requirements → Test Map 의 15 test case 을 1:1 매핑:

```markdown
| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 12-01-W0-01 | 01 | 1 | AGENT-STD-01 | unit | `pytest tests/phase12/test_agent_md_schema.py::test_all_30_agents_have_5_blocks -x` | ✅ | ⬜ pending |
| 12-01-W0-02 | 01 | 1 | AGENT-STD-01 (neg) | unit | `pytest tests/phase12/test_agent_md_schema.py::test_excluded_agents_not_scanned -x` | ✅ | ⬜ pending |
| 12-01-W0-03 | 01 | 1 | AGENT-STD-01 | unit | `py -3.11 scripts/validate/verify_agent_md_schema.py .claude/agents/producers/trend-collector/AGENT.md` | ✅ | ✅ green (Plan 01 Task 4) |
| 12-02-01 | 02 | 2 | AGENT-STD-01 | batch | `pytest tests/phase12/test_agent_md_schema.py::test_all_30_agents_have_5_blocks -x` (after 12 producer migration) | ✅ | ⬜ pending |
| 12-03-01 | 03 | 3 | AGENT-STD-01 | batch | `pytest tests/phase12/test_agent_md_schema.py::test_all_30_agents_have_5_blocks -x` (after 17 inspector migration) | ✅ | ⬜ pending |
| 12-04-01 | 04 | 2 | SKILL-ROUTE-01 | unit | `pytest tests/phase12/test_skill_matrix_format.py::test_matrix_has_30_rows -x` | ✅ | ⬜ pending |
| 12-04-02 | 04 | 2 | SKILL-ROUTE-01 | unit | `pytest tests/phase12/test_skill_matrix_format.py::test_matrix_cell_values_legal -x` | ✅ | ⬜ pending |
| 12-04-03 | 04 | 2 | SKILL-ROUTE-01 (recip) | integration | `py -3.11 scripts/validate/verify_agent_skill_matrix.py --fail-on-drift` | ✅ | ⬜ pending |
| 12-05-01 | 05 | 2 | FAIL-PROTO-01 | unit | `pytest tests/phase12/test_failures_rotation.py::test_hook_denies_over_500_lines -x` | ✅ | ⬜ pending |
| 12-05-02 | 05 | 2 | FAIL-PROTO-01 | unit | `pytest tests/phase12/test_failures_rotation.py::test_rotate_idempotent -x` | ✅ | ⬜ pending |
| 12-05-03 | 05 | 2 | FAIL-PROTO-01 | unit | `pytest tests/phase12/test_failures_rotation.py::test_imported_file_sha256_unchanged -x` | ✅ | ⬜ pending |
| 12-05-04 | 05 | 2 | FAIL-PROTO-01 | unit | `pytest tests/phase12/test_failures_rotation.py::test_archive_month_tag -x` | ✅ | ⬜ pending |
| 12-06-01 | 06 | 4 | AGENT-STD-02 | unit | `pytest tests/phase12/test_mandatory_reads_prose.py::test_sampling_forbidden_literal -x` | ✅ | ⬜ pending |
| 12-06-02 | 06 | 4 | AGENT-STD-02 | unit | `pytest tests/phase12/test_mandatory_reads_prose.py::test_three_mandatory_items_present -x` | ✅ | ⬜ pending |
| 12-07-01 | 07 | 2 | AGENT-STD-03 | unit | `pytest tests/phase12/test_supervisor_compress.py::test_compression_ratio_over_40pct -x` | ✅ | ⬜ pending |
| 12-07-02 | 07 | 2 | AGENT-STD-03 | unit | `pytest tests/phase12/test_supervisor_compress.py::test_critical_decisions_preserved -x` | ✅ | ⬜ pending |
| 12-07-03 | 07 | 2 | AGENT-STD-03 | integration | `pytest tests/phase12/test_supervisor_compress.py::test_phase11_smoke_replay_under_cli_limit -x` | ✅ | ⬜ pending |
| 12-02-f-d2 | 02 | 2 | FAIL-PROTO-02 | unit | `pytest tests/phase12/test_f_d2_exception_batch.py::test_batch_commit_single_entry -x` | ✅ | ⬜ pending |
| 12-02-idemp | 02 | 2 | FAIL-PROTO-02 | unit | `pytest tests/phase12/test_f_d2_exception_batch.py::test_batch_idempotent_replay -x` | ✅ | ⬜ pending |
```

3. "Wave 0 Requirements" 체크박스 전체 [x] 로 플립 (Task 2 가 모두 생성).

4. Approval 라인: `**Approval:** pending` → `**Approval:** GREEN (Plan 01 Wave 0 완결 2026-04-21)` — 다만 실제 날짜는 실행 시점으로 갱신.
  </action>
  <verify>
    <automated>grep -c "nyquist_compliant: true" .planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-VALIDATION.md; grep -c "wave_0_complete: true" .planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-VALIDATION.md; grep -cE "^\| 12-[0-9]{2}-[^|]+ \|" .planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^nyquist_compliant: true$" ...12-VALIDATION.md` = 1
    - `grep -c "^wave_0_complete: true$" ...12-VALIDATION.md` = 1
    - Per-Task Verification Map table has ≥ 19 data rows (15 test cases + 4 wave-gate summary rows — populated from RESEARCH.md §Validation Architecture)
    - No `placeholder` / `TODO` / `(planner fills)` text remains in Per-Task Verification Map section
    - Wave 0 Requirements checklist has ≥ 10 items with `[x]` marks
  </acceptance_criteria>
  <done>
12-VALIDATION.md 가 Nyquist compliant 상태로 플립. 후속 Plan 은 이 map 의 row 를 consumer 로 읽고 status 컬럼만 ✅ green 으로 플립하면 됨.
  </done>
</task>

</tasks>

<verification>
전체 Plan 01 완료 후:

```bash
py -3.11 -m pytest tests/phase12/ -q                                    # exit 0 (all skipped/green, 0 failed)
py -3.11 -m pytest tests/phase04/ -q                                    # exit 0 (244 passed regression)
py -3.11 scripts/validate/verify_agent_md_schema.py --all               # exit 1 (29 violations, trend-collector only passing)
py -3.11 scripts/validate/verify_agent_md_schema.py .claude/agents/producers/trend-collector/AGENT.md  # exit 0
```

Phase 11 regression 도 확인:
```bash
py -3.11 -m pytest tests/phase11/ -q                                    # exit 0 (36 passed)
```
</verification>

<success_criteria>
1. 2 template 파일 존재 + 5 XML 블록 schema + "샘플링 금지" 한국어 literal + RUB-06 역방향 미러
2. tests/phase12/ 스캐폴드 완성 — 6 test 파일 + conftest + mocks + fixtures, collection exit 0 + skip ≥ 15
3. verify_agent_md_schema.py CLI 개통 — `--all` + 단일 파일 인자 모두 동작
4. trend-collector AGENT.md v1.2 승격 — 5 블록 전수 준수 + body 보존 + Phase 4 regression 244 GREEN
5. 12-VALIDATION.md frontmatter `nyquist_compliant: true` + `wave_0_complete: true` 플립 + 15 test row map 완비
6. Plan 02~07 모두 본 Plan 의 산출에 의존 — Plan 01 FAIL = Phase 12 차단
</success_criteria>

<output>
After completion, create `.planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-01-SUMMARY.md`.

대표님께 보고드립니다. 본 Plan 은 Phase 12 의 기초 인프라를 단일 Wave 에 집중하여 후속 6 Plan 의 regression 기반을 확보하는 임무입니다. 완결 시 template round-trip 증명 (trend-collector v1.2) 을 통해 30 AGENT.md migration 경로의 안전성이 입증됩니다.
</output>
