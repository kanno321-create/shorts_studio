---
phase: 12-agent-standardization-skill-routing-failures-protocol
plan: 07
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - tests/phase12/fixtures/producer_output_gate2_oversized.json
  - scripts/orchestrator/invokers.py
  - tests/phase12/test_supervisor_compress.py
autonomous: true
requirements:
  - AGENT-STD-03
status: pending
must_haves:
  truths:
    - "tests/phase12/fixtures/producer_output_gate2_oversized.json 합성 fixture 존재 — 8~15KB (Phase 11 smoke 2차 attempt `프롬프트가 너무 깁니다` 재현용 — RESEARCH §Open Question Q2)"
    - "`_compress_producer_output(output: dict) -> dict` private function 이 `scripts/orchestrator/invokers.py` 에 존재 — decisions/evidence key 2종 fallback + severity_desc → score_asc 정렬 + char budget 2000 truncate + error_codes[] 전수 보존 + _truncated 메타"
    - "ClaudeAgentSupervisorInvoker.__call__ line 401-404 수정 — `user_payload = json.dumps(...)` 직전 `output = _compress_producer_output(output)` 삽입, test seam (cli_runner) 보존"
    - "test_supervisor_compress.py 5 tests GREEN — test_compression_ratio_over_40pct + test_critical_decisions_preserved + test_error_codes_always_preserved + test_raw_response_dropped + test_phase11_smoke_replay_under_cli_limit"
    - "Phase 11 regression 36 tests GREEN — retry-with-nudge + stdin piping + dotenv + adapter graceful degrade 전수 무회귀"
    - "Phase 4 regression 244 tests GREEN — 에이전트 구조 변경 없음 검증"
  artifacts:
    - path: "tests/phase12/fixtures/producer_output_gate2_oversized.json"
      provides: "Phase 11 smoke 2차 producer_output 재현 fixture — 합성, 실측 추출 불가 (output/ empty)"
      contains: "decisions[], evidence[], error_codes[], semantic_feedback"
    - path: "scripts/orchestrator/invokers.py"
      provides: "`_compress_producer_output()` private function + ClaudeAgentSupervisorInvoker.__call__ 삽입점"
      contains: "_compress_producer_output, _COMPRESS_CHAR_BUDGET = 2000"
    - path: "tests/phase12/test_supervisor_compress.py"
      provides: "AGENT-STD-03 pytest coverage 5 tests"
  key_links:
    - from: "scripts/orchestrator/invokers.py::ClaudeAgentSupervisorInvoker.__call__"
      to: "_compress_producer_output"
      via: "line 401 이전 output = _compress_producer_output(output) 삽입"
      pattern: "_compress_producer_output"
    - from: "tests/phase12/test_supervisor_compress.py"
      to: "tests/phase12/fixtures/producer_output_gate2_oversized.json"
      via: "fixture load + MockClaudeCLI 경유 invoker 실행 + compressed byte size assertion"
      pattern: "producer_output_gate2_oversized\\.json"
---

<objective>
AGENT-STD-03 Supervisor prompt compression 구현. Phase 11 live smoke 2차 attempt 에서 노출된 "프롬프트가 너무 깁니다" (rc=1) gap 해소. `ClaudeAgentSupervisorInvoker.__call__` 이 producer_output 을 summary-only (`{gate, verdict, decisions[], error_codes[]}`) 모드로 압축하여 Claude CLI context limit 회피.

**Purpose**: Phase 11 Gap #1 의 source-of-truth. 본 Plan 성공 = Phase 12 완결 시 Phase 11 SC#1 (live smoke 완주) 재도전 경로 개통. 실 smoke 추가 실행은 Phase 13 deferred (CONTEXT.md D-A4-03 + RESEARCH.md §Q2).

**Output**: 합성 fixture (8~15KB) + _compress_producer_output() private function + ClaudeAgentSupervisorInvoker.__call__ 수정 + test_supervisor_compress.py 5 tests GREEN.

**검증 완료 (2026-04-21)** — Issue #3 resolution: `ClaudeAgentSupervisorInvoker.__init__` (invokers.py L383-393) 은 `cli_runner: Callable | None = None` kwarg 를 이미 수용하며 L393 에서 `self._cli_runner = cli_runner or _invoke_claude_cli` 로 fallback. 별도 test-seam 추가 작업 불필요 — Plan 07 Task 3 의 `ClaudeAgentSupervisorInvoker(..., cli_runner=mock)` 호출은 현 signature 로 바로 동작.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-CONTEXT.md
@.planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-RESEARCH.md
@.planning/phases/11-pipeline-real-run-activation-script-quality-mode/11-VERIFICATION.md
@scripts/orchestrator/invokers.py
@CLAUDE.md
</context>

<interfaces>
**실측 검증 완료 2026-04-21 (Issue #3 + Issue #4 resolution)**:

`_invoke_claude_cli` signature (invokers.py L213-220, retry-with-nudge wrapper):
```python
def _invoke_claude_cli(
    system_prompt: str,
    user_prompt: str,
    json_schema: str,
    cli_path: str,
    timeout_s: int = DEFAULT_TIMEOUT_S,
    agent_label: str = "claude-cli",
) -> str:
```

`_invoke_claude_cli_once` signature (invokers.py L121-127, low-level once-shot):
```python
def _invoke_claude_cli_once(
    system_prompt: str,
    user_prompt: str,
    json_schema: str,
    cli_path: str,
    timeout_s: int = DEFAULT_TIMEOUT_S,
) -> str:
```

`ClaudeAgentSupervisorInvoker.__init__` signature (invokers.py L383-393) — `cli_runner` kwarg EXISTS:
```python
def __init__(
    self,
    agent_dir: Path,
    circuit_breaker=None,
    cli_path: str | None = None,
    cli_runner: Callable | None = None,  # <-- test-seam EXISTS
) -> None:
    self.agent_dir = Path(agent_dir)
    self.circuit_breaker = circuit_breaker
    self._cli_path = _resolve_cli_path(cli_path)
    self._cli_runner = cli_runner or _invoke_claude_cli  # <-- fallback to real
    self._system, _ = load_agent_system_prompt(self.agent_dir)
```

`_call` (L406-412) 호출 시 4개 kwargs 만 전달:
```python
def _call():
    return self._cli_runner(
        system_prompt=self._system,
        user_prompt=user_payload,
        json_schema=_SUPERVISOR_JSON_SCHEMA,
        cli_path=self._cli_path,
    )
```

기존 ClaudeAgentSupervisorInvoker.__call__ 계약 (invokers.py L396-426):
```python
def __call__(self, gate, output: dict):
    from .state import Verdict
    gate_name = getattr(gate, "name", str(gate))
    user_payload = json.dumps(                              # <-- 삽입점 (L401-404)
        {"gate": gate_name, "producer_output": output},
        ensure_ascii=False,
    )
    def _call():
        return self._cli_runner(...)
    stdout = self.circuit_breaker.call(_call) if ... else _call()
    parsed = json.loads(stdout)
    verdict_str = (parsed.get("verdict") or "").upper()
    return Verdict[verdict_str]
```

Plan 07 수정 (D-A4-04):
- line 400 이후 (user_payload 직전) `output = _compress_producer_output(output)` 삽입
- 나머지 로직 건드리지 말 것 — 테스트 seam (`cli_runner`, `circuit_breaker`) 보존

_compress_producer_output() 계약 (D-A4-01 + RESEARCH §Pattern 3 + Pitfall 4):
- 보존: `gate` / `verdict` / `error_codes[]` (전수) / `_truncated` 메타
- decisions[] OR evidence[] key 2종 fallback (RESEARCH Pitfall 4: Inspector 일부가 `evidence[]` 사용)
- severity 필드 존재 시 severity_desc → score_asc 정렬, 부재 시 원순서
- char budget = 2000 (D-A4-01 default, `_COMPRESS_CHAR_BUDGET` module constant)
- 초과 시 앞에서부터 char 단위 truncate + `_truncated` 메타 추가
- verbose prose (`raw_response`, `semantic_feedback` full) drop

합성 fixture 스펙 (RESEARCH §Q2):
- 파일: `tests/phase12/fixtures/producer_output_gate2_oversized.json`
- 크기 목표: 8~15KB (Phase 11 smoke 2차 "프롬프트가 너무 깁니다" 유발 수준 근사)
- 구조:
  - `decisions[]` 15~30 entry — rule / severity (critical/high/medium/low 섞음) / score / evidence (long prose)
  - `evidence[]` 중첩 구조 — Inspector rubric-schema.json 기반
  - `semantic_feedback` 3000+ chars verbose prose
  - `error_codes[]` 3~5 entry (전수 보존 대상)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 합성 producer_output fixture 생성 (8~15KB, decisions + evidence + error_codes)</name>
  <files>tests/phase12/fixtures/producer_output_gate2_oversized.json</files>
  <read_first>
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-RESEARCH.md §Open Question Q2 (fixture 부재 + 합성 규칙)
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-RESEARCH.md §Pitfalls #4 (evidence vs decisions key)
    - .claude/agents/_shared/rubric-schema.json (Inspector 공통 출력 schema — decisions/evidence/error_codes 구조)
    - reports/ 디렉토리 (Phase 11 smoke attempts logs — 실측 없으므로 합성 필수 확인)
  </read_first>
  <action>
`tests/phase12/fixtures/producer_output_gate2_oversized.json` 신규 생성. Python stdlib 으로 합성:

```python
# 실행용 inline script (Plan 실행자가 복사해서 run):
import json
from pathlib import Path

VERBOSE_SEMANTIC_FEEDBACK = (
    "본 에이전트 출력은 여러 차례 재검토가 필요한 품질 이슈를 동반합니다. "
    * 50  # ~3000 chars
) + (
    "특히 hook 의 3초 후킹 강도가 낮고 CTA 가 명확하지 않은 것으로 보입니다. "
    "또한 act1 의 긴장감 조성이 부족하며 climax 이전 단계에서 내러티브 플로우가 끊어지는 "
    "느낌이 있습니다. 이는 장면 전환 속도가 Shorts 60초 포맷에 비해 느리기 때문으로 추정됩니다. "
    * 10
)

SEVERITIES = ["critical", "high", "medium", "low"]

def _mk_decision(i: int) -> dict:
    return {
        "rule": f"rule_{i:03d}_quality_check",
        "severity": SEVERITIES[i % 4],  # critical/high/medium/low 섞음
        "score": 40 + (i * 3) % 60,     # 40~99
        "evidence": (
            f"Decision {i}: 본 항목은 {SEVERITIES[i % 4]} severity 로 분류되며, "
            "구체적 근거는 씬 {i}의 타이밍 drift 및 자막 alignment 이슈로 확인됩니다. "
            f"권장 조치: scripter 가 prior_vqqa 를 반영하여 hook 부분을 재작성 후 재호출. "
            * 3  # ~500 chars per decision
        ),
        "scene_id": f"scene_{i:02d}",
    }

def _mk_evidence_entry(i: int) -> dict:
    return {
        "type": "timing_drift",
        "scene_id": f"scene_{i:02d}",
        "observed_ms": 150 + i * 10,
        "expected_ms": 100,
        "note": (
            f"Evidence {i}: 실측 타이밍이 기대값을 {50 + i * 10}ms 초과함. "
            "Shotstack VoiceFirstTimeline 재계산 권장. "
            * 2
        ),
    }

output = {
    "gate": "SCRIPT",
    "verdict": "RETRY",
    "decisions": [_mk_decision(i) for i in range(22)],  # 22 decisions
    "evidence": [_mk_evidence_entry(i) for i in range(15)],  # 15 evidence
    "error_codes": ["ERR_HOOK_WEAK", "ERR_TIMING_DRIFT", "ERR_CTA_MISSING", "ERR_KOREAN_UNNATURAL"],
    "semantic_feedback": VERBOSE_SEMANTIC_FEEDBACK,
    "raw_response": "((((verbose Claude CLI raw output wrapping 2000+ chars -- dropped in compression))))" * 30,
}

out_path = Path("tests/phase12/fixtures/producer_output_gate2_oversized.json")
out_path.write_text(json.dumps(output, ensure_ascii=False, indent=2), encoding="utf-8")

# Verify size target 8~15KB
size_kb = out_path.stat().st_size / 1024
print(f"fixture size: {size_kb:.1f} KB")
assert 8 <= size_kb <= 15, f"fixture size {size_kb} KB out of target 8-15 KB"
```

**실행 방법**:
```bash
py -3.11 -c "<위 스크립트 한 줄로 붙여넣기 또는 heredoc>"
```

또는 Write 로 스크립트 내용을 JSON 자체로 직접 작성하되, 위 Python 생성 절차를 먼저 돌려 JSON 파일 만든 후 commit.

**크기 검증**: 생성 후 `py -3.11 -c "from pathlib import Path; print(Path('tests/phase12/fixtures/producer_output_gate2_oversized.json').stat().st_size / 1024)"` 결과가 8~15 KB 사이 확인.
  </action>
  <verify>
    <automated>py -3.11 -c "import json; from pathlib import Path; p=Path('tests/phase12/fixtures/producer_output_gate2_oversized.json'); size_kb=p.stat().st_size/1024; print(f'size: {size_kb:.1f} KB'); d=json.loads(p.read_text(encoding='utf-8')); assert 8<=size_kb<=15, size_kb; assert 'critical' in str(d['decisions']); assert len(d['decisions'])>=15; assert len(d['error_codes'])>=3; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/phase12/fixtures/producer_output_gate2_oversized.json` exists
    - File size: 8 KB ≤ size ≤ 15 KB
    - JSON load succeeds (well-formed)
    - `decisions[]` length ≥ 15 entries
    - `error_codes[]` length ≥ 3 entries
    - At least 1 decision has `severity: "critical"` (for test_critical_decisions_preserved)
    - `semantic_feedback` field length ≥ 2500 chars
    - `raw_response` field length ≥ 2000 chars (drop 대상)
    - Valid UTF-8 (Korean characters render correctly)
  </acceptance_criteria>
  <done>
합성 fixture 완성. Phase 11 smoke 2차 attempt 재현 시나리오 확보. Plan 07 Task 3 pytest consumer 준비 완료.
  </done>
</task>

<task type="auto">
  <name>Task 2: _compress_producer_output() private function + ClaudeAgentSupervisorInvoker 수정</name>
  <files>scripts/orchestrator/invokers.py</files>
  <read_first>
    - scripts/orchestrator/invokers.py lines 121-167 (`_invoke_claude_cli_once` 5-arg signature — MockClaudeCLI signature 참조)
    - scripts/orchestrator/invokers.py lines 213-260 (`_invoke_claude_cli` retry-with-nudge wrapper 6-arg signature — `timeout_s`, `agent_label` 포함)
    - scripts/orchestrator/invokers.py lines 371-426 (ClaudeAgentSupervisorInvoker class 전체 — __init__ L383-393 에 `cli_runner: Callable | None = None` 기존 확인, __call__ L396-426 수정 대상)
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-RESEARCH.md §Pattern 3 (코드 template)
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-RESEARCH.md §Pitfalls #4 (evidence vs decisions fallback)
    - .claude/agents/_shared/rubric-schema.json (Inspector 공통 출력 스키마)
  </read_first>
  <action>
`scripts/orchestrator/invokers.py` 2 곳 수정. **주의 — Issue #3 검증 완료: `__init__` 의 `cli_runner` kwarg (L388) 은 이미 존재하므로 signature 변경 불필요. Task 2 는 `_compress_producer_output()` 추가 + `__call__` 1줄 삽입만 수행**.

### 1. 파일 끝에 `_compress_producer_output()` + `_COMPRESS_CHAR_BUDGET` 추가

ClaudeAgentSupervisorInvoker class 정의 **이후** (파일 맨 끝, 또는 `# ===` 구분선 이후):

```python
# ============================================================================
# Phase 12 AGENT-STD-03: Supervisor producer_output compression (D-A4-01~04)
# ============================================================================

_COMPRESS_CHAR_BUDGET = 2000  # D-A4-01 default — decisions/evidence entries char budget


def _compress_producer_output(output: dict) -> dict:
    """Compress producer_output to summary-only form for Supervisor CLI prompt.

    D-A4-01 contract:
      - Preserve: gate, verdict, error_codes[] (전수)
      - Truncate: decisions[] OR evidence[] — severity_desc → score_asc 정렬 후
        char budget 내 앞에서부터 수집, 초과 시 `_truncated` 메타 추가
      - Drop: verbose prose (raw_response, full semantic_feedback)
      - Keep: short semantic_feedback prefix (first 200 chars if present)

    Pitfall 4 mitigation: decisions[] AND evidence[] key 2종 fallback —
    Inspector 일부는 `evidence[]` primary (ins-factcheck rubric-schema), 둘 다 조회.

    Args:
        output: raw producer_output dict from Producer/Inspector invoker.

    Returns:
        compressed dict with summary-only shape, suitable for Claude CLI body.
    """
    compressed: dict = {
        "gate": output.get("gate"),
        "verdict": output.get("verdict"),
        "error_codes": list(output.get("error_codes", [])),
    }

    # Keep short semantic_feedback prefix (drop full verbose)
    sf = output.get("semantic_feedback")
    if sf and isinstance(sf, str):
        compressed["semantic_feedback_prefix"] = sf[:200]

    # Pitfall 4: decisions[] OR evidence[] 2종 fallback
    source_key = None
    source_list: list = []
    for key in ("decisions", "evidence"):
        val = output.get(key, [])
        if isinstance(val, list) and val:
            source_key = key
            source_list = val
            break

    if not source_list:
        return compressed

    # severity 필드 존재 시 severity_desc → score_asc 정렬
    sev_rank = {"critical": 0, "high": 1, "medium": 2, "low": 3}
    has_severity = any(isinstance(d, dict) and "severity" in d for d in source_list)

    def _sort_key(d: dict) -> tuple[int, float]:
        s_rank = sev_rank.get(d.get("severity", "low"), 3)
        score = d.get("score", 9999)
        return (s_rank, score if isinstance(score, (int, float)) else 9999)

    if has_severity:
        sorted_source = sorted(source_list, key=_sort_key)
    else:
        sorted_source = list(source_list)

    # char budget 내 앞에서부터 수집 (JSON 직렬화 size 기준)
    import json as _json
    kept: list = []
    char_used = 0
    for d in sorted_source:
        entry_size = len(_json.dumps(d, ensure_ascii=False))
        if char_used + entry_size > _COMPRESS_CHAR_BUDGET:
            break
        kept.append(d)
        char_used += entry_size

    compressed[source_key] = kept
    if len(kept) < len(source_list):
        compressed["_truncated"] = (
            f"{len(source_list) - len(kept)} more {source_key} entries truncated "
            f"(char budget {_COMPRESS_CHAR_BUDGET} exceeded, severity-sorted)"
        )

    return compressed
```

### 2. ClaudeAgentSupervisorInvoker.__call__ 수정 (L396-404)

**현재 코드** (L396-404):
```python
def __call__(self, gate, output: dict):
    # Lazy import to avoid a cycle.
    from .state import Verdict

    gate_name = getattr(gate, "name", str(gate))
    user_payload = json.dumps(
        {"gate": gate_name, "producer_output": output},
        ensure_ascii=False,
    )
```

**수정 후** (L400 부근에 1 줄 삽입):
```python
def __call__(self, gate, output: dict):
    # Lazy import to avoid a cycle.
    from .state import Verdict

    gate_name = getattr(gate, "name", str(gate))
    # --- Phase 12 AGENT-STD-03 (D-A4-04): compress producer_output ---
    output = _compress_producer_output(output)
    # -----------------------------------------------------------------
    user_payload = json.dumps(
        {"gate": gate_name, "producer_output": output},
        ensure_ascii=False,
    )
```

**중요**:
- 나머지 로직 (`_call`, `circuit_breaker`, `json.loads(stdout)`, `Verdict[...]`) 건드리지 말 것.
- `__init__` (L383-393) 건드리지 말 것 — `cli_runner` kwarg 이미 존재 (Issue #3 실측 검증 완료).
- `cli_runner` test seam 보존 — MockClaudeCLI (Plan 01 Task 2) 가 unchanged signature 로 호출 가능.
- Phase 11 regression 대상: `tests/phase11/` 36 tests — stdin piping + retry-with-nudge + dotenv + adapter 모두 Producer invoker 경로 → Supervisor invoker 수정 영향 없음 기대.
  </action>
  <verify>
    <automated>grep -c "_compress_producer_output" scripts/orchestrator/invokers.py; grep -c "_COMPRESS_CHAR_BUDGET = 2000" scripts/orchestrator/invokers.py; py -3.11 -c "from scripts.orchestrator.invokers import _compress_producer_output; r=_compress_producer_output({'gate':'SCRIPT','verdict':'RETRY','decisions':[{'rule':'r1','severity':'critical','score':50}]*30,'error_codes':['E1','E2'],'raw_response':'x'*5000}); print('compressed keys:', list(r.keys())); print('has raw_response:', 'raw_response' in r); print('has _truncated:', '_truncated' in r)"; py -3.11 -m pytest tests/phase11/ -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_compress_producer_output" scripts/orchestrator/invokers.py` ≥ 3 (function def + __call__ usage + docstring)
    - `grep -c "_COMPRESS_CHAR_BUDGET = 2000" scripts/orchestrator/invokers.py` = 1
    - Python import test: `from scripts.orchestrator.invokers import _compress_producer_output` succeeds
    - Direct call test: `_compress_producer_output({'gate':...,'decisions':[...]*30,...})` returns dict
    - Returned dict keys: `gate`, `verdict`, `error_codes`, `decisions` OR `evidence`, `_truncated` (if truncation occurred)
    - Returned dict does NOT contain `raw_response` (dropped)
    - `__init__` signature 에 `cli_runner: Callable | None = None` kwarg 존재 (grep 확인): `grep -c "cli_runner: Callable" scripts/orchestrator/invokers.py` ≥ 1 (Issue #3 invariant guard — Task 2 가 이 kwarg 를 우발적으로 제거하지 않았음을 확인)
    - `py -3.11 -m pytest tests/phase11/ -q` exits 0 (36 passed — regression 무회귀)
    - `py -3.11 -m pytest tests/phase04/ -q` exits 0 (244 passed — regression)
  </acceptance_criteria>
  <done>
`_compress_producer_output()` 함수 + ClaudeAgentSupervisorInvoker 수정 완료. `__init__` cli_runner kwarg 유지 (Issue #3 invariant). Phase 11 + Phase 4 regression 무회귀 확인.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: test_supervisor_compress.py 실 assertion 5 tests GREEN</name>
  <files>tests/phase12/test_supervisor_compress.py</files>
  <behavior>
    - Test 1: `test_compression_ratio_over_40pct` — fixture load (8~15KB) → _compress_producer_output → compressed JSON byte size 가 원본 대비 60% 미만 (압축률 > 40%)
    - Test 2: `test_critical_decisions_preserved` — fixture 에 severity=critical 있을 때 compressed.decisions[] 앞부분 에 critical 보존 (severity_desc 정렬)
    - Test 3: `test_phase11_smoke_replay_under_cli_limit` — fixture 를 MockClaudeCLI 경유 ClaudeAgentSupervisorInvoker 로 실행 → `mock.last_user_prompt` 길이 가 Claude CLI context limit 근사치 (기본 10000 chars) 이하
    - Test 4: `test_error_codes_always_preserved` — compressed.error_codes 가 원본 error_codes 와 동일 (전수 보존)
    - Test 5: `test_raw_response_dropped` — compressed dict 에 `raw_response` key 부재
  </behavior>
  <read_first>
    - tests/phase12/test_supervisor_compress.py (Plan 01 Task 2 red stub)
    - tests/phase12/fixtures/producer_output_gate2_oversized.json (Task 1 산출)
    - tests/phase12/mocks/mock_claude_cli.py (Plan 01 Task 2 — MockClaudeCLI, `*args/**kwargs` defensive signature)
    - scripts/orchestrator/invokers.py lines 383-395 (**Issue #3 verification — ClaudeAgentSupervisorInvoker.__init__ 의 `cli_runner: Callable | None = None` kwarg 존재 확인**. 본 Task 의 `ClaudeAgentSupervisorInvoker(agent_dir=..., circuit_breaker=None, cli_runner=mock)` 호출이 TypeError 없이 성공해야 함.)
    - scripts/orchestrator/invokers.py lines 406-412 (`_call` 의 `self._cli_runner(system_prompt=..., user_prompt=..., json_schema=..., cli_path=...)` 호출 — MockClaudeCLI `*args/**kwargs` 수용 대상)
    - scripts/orchestrator/invokers.py (Task 2 산출)
  </read_first>
  <action>
red stub 제거 + 5 tests 실 assertion:

```python
"""AGENT-STD-03 — _compress_producer_output() + SupervisorInvoker replay."""

from __future__ import annotations
import json
import sys
from pathlib import Path

import pytest

REPO_ROOT = Path(__file__).resolve().parents[2]
sys.path.insert(0, str(REPO_ROOT))
sys.path.insert(0, str(Path(__file__).parent))  # mocks/ module resolution

from scripts.orchestrator.invokers import _compress_producer_output  # noqa: E402
from mocks.mock_claude_cli import MockClaudeCLI  # noqa: E402


@pytest.fixture
def oversized_fixture() -> dict:
    """Load Phase 11 smoke 2차 replay fixture (8~15KB)."""
    p = REPO_ROOT / "tests" / "phase12" / "fixtures" / "producer_output_gate2_oversized.json"
    return json.loads(p.read_text(encoding="utf-8"))


def test_compression_ratio_over_40pct(oversized_fixture):
    """compressed byte size < 60% of original (압축률 > 40%)."""
    raw_json = json.dumps(oversized_fixture, ensure_ascii=False)
    compressed = _compress_producer_output(oversized_fixture)
    compressed_json = json.dumps(compressed, ensure_ascii=False)
    ratio = len(compressed_json) / len(raw_json)
    assert ratio < 0.60, (
        f"compression ratio = {ratio:.2%} (compressed {len(compressed_json)} / "
        f"raw {len(raw_json)} bytes) — expected <60%"
    )


def test_critical_decisions_preserved(oversized_fixture):
    """severity=critical decisions 이 compressed.decisions 앞부분 에 보존."""
    # Pre-condition: fixture 에 critical 존재
    has_critical_in_raw = any(
        d.get("severity") == "critical"
        for d in oversized_fixture.get("decisions", [])
    )
    assert has_critical_in_raw, "fixture missing critical severity — 합성 스펙 위반"

    compressed = _compress_producer_output(oversized_fixture)
    kept = compressed.get("decisions", []) or compressed.get("evidence", [])
    # severity_desc 정렬 → 첫 엔트리가 critical 이어야 함
    assert len(kept) > 0, "no decisions kept after compression"
    first_severity = kept[0].get("severity")
    assert first_severity == "critical", (
        f"critical not first after severity_desc sort: got {first_severity!r}, "
        f"all kept severities: {[d.get('severity') for d in kept]}"
    )


def test_error_codes_always_preserved(oversized_fixture):
    """error_codes[] 는 전수 보존 (truncate 대상 아님)."""
    compressed = _compress_producer_output(oversized_fixture)
    assert compressed["error_codes"] == oversized_fixture["error_codes"]


def test_raw_response_dropped(oversized_fixture):
    """verbose raw_response 는 drop."""
    assert "raw_response" in oversized_fixture  # fixture 에 있음
    compressed = _compress_producer_output(oversized_fixture)
    assert "raw_response" not in compressed, \
        f"raw_response leaked into compressed: {list(compressed.keys())}"


def test_phase11_smoke_replay_under_cli_limit(oversized_fixture, tmp_path, monkeypatch):
    """MockClaudeCLI 경유 SupervisorInvoker 실행 → user_prompt < CLI context limit.

    Issue #3 verified (2026-04-21): ClaudeAgentSupervisorInvoker.__init__ (L383-393)
    의 `cli_runner: Callable | None = None` kwarg 는 기존 존재. 본 호출은
    `TypeError: unexpected keyword argument 'cli_runner'` 없이 성공해야 함.
    """
    from scripts.orchestrator.invokers import ClaudeAgentSupervisorInvoker

    # Minimal agent_dir for test (supervisor AGENT.md 실제로 존재)
    agent_dir = REPO_ROOT / ".claude" / "agents" / "supervisor"
    if not agent_dir.exists():
        # Fallback: use any inspector dir (schema only matters for system prompt load)
        agent_dir = REPO_ROOT / ".claude" / "agents" / "inspectors" / "structural" / "ins-schema-integrity"
    assert agent_dir.exists(), f"no agent dir available for test: {agent_dir}"

    mock = MockClaudeCLI()
    mock.seed({"verdict": "RETRY", "decisions": [], "error_codes": []})

    # __init__ signature (L383-393 verified) accepts cli_runner=
    invoker = ClaudeAgentSupervisorInvoker(
        agent_dir=agent_dir,
        circuit_breaker=None,
        cli_runner=mock,
    )

    # Fake gate
    class FakeGate:
        name = "SCRIPT"
    verdict = invoker(FakeGate(), oversized_fixture)

    # CLI context limit 근사치 = 10000 chars (Claude CLI --append-system-prompt body)
    assert mock.last_user_prompt is not None
    payload_size = len(mock.last_user_prompt)
    CLI_LIMIT = 10000
    assert payload_size < CLI_LIMIT, (
        f"compressed user_prompt = {payload_size} chars > CLI limit {CLI_LIMIT}. "
        f"Phase 11 smoke 2차 '프롬프트가 너무 깁니다' 재발 위험"
    )
    assert verdict is not None  # verdict parse succeeded
```

완료 후 5 tests 전수 GREEN 확인. Phase 11 + Phase 4 regression 전수 GREEN 확인.
  </action>
  <verify>
    <automated>py -3.11 -m pytest tests/phase12/test_supervisor_compress.py -v 2>&1 | tail -15; py -3.11 -m pytest tests/phase11/ -q 2>&1 | tail -3; py -3.11 -m pytest tests/phase04/ -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `py -3.11 -m pytest tests/phase12/test_supervisor_compress.py -v` exits 0, 5 passed
    - Test `test_compression_ratio_over_40pct` PASSES (compressed bytes < 60% of raw)
    - Test `test_critical_decisions_preserved` PASSES (first kept decision has severity=critical)
    - Test `test_error_codes_always_preserved` PASSES
    - Test `test_raw_response_dropped` PASSES
    - Test `test_phase11_smoke_replay_under_cli_limit` PASSES (payload < 10000 chars, `cli_runner=mock` 주입 성공 — Issue #3 invariant)
    - `py -3.11 -m pytest tests/phase11/ -q` exits 0 (36 passed — regression 무회귀)
    - `py -3.11 -m pytest tests/phase04/ -q` exits 0 (244 passed — regression)
    - Skip decorator count in file = 0 (red stub 제거)
  </acceptance_criteria>
  <done>
AGENT-STD-03 5 tests GREEN. Phase 11 smoke 2차 "프롬프트가 너무 깁니다" 재발 방지 구조적 보장. Phase 11 + Phase 4 regression 전수 무회귀. Issue #3 `cli_runner` kwarg invariant pytest 로 lock.
  </done>
</task>

</tasks>

<verification>
```bash
py -3.11 -m pytest tests/phase12/test_supervisor_compress.py -v         # 5 passed
py -3.11 -m pytest tests/phase11/ -q                                    # 36 passed (regression)
py -3.11 -m pytest tests/phase04/ -q                                    # 244 passed (regression)
py -3.11 -m pytest tests/phase12/ -q                                    # full phase12 GREEN
py -3.11 -c "from scripts.orchestrator.invokers import _compress_producer_output; print('OK')"
grep -c "_compress_producer_output" scripts/orchestrator/invokers.py    # ≥ 3
grep -c "cli_runner: Callable" scripts/orchestrator/invokers.py         # ≥ 1 (Issue #3 invariant)
```
</verification>

<success_criteria>
1. 합성 fixture `producer_output_gate2_oversized.json` 8~15KB 존재 — Phase 11 smoke 2차 replay surface
2. `_compress_producer_output()` private function — decisions/evidence 2종 fallback + severity_desc 정렬 + char budget 2000 + error_codes 전수 보존 + _truncated 메타
3. ClaudeAgentSupervisorInvoker.__call__ 수정 — 1 줄 삽입, test seam 보존
4. test_supervisor_compress.py 5 tests GREEN — 압축률 / critical 보존 / error_codes 보존 / raw_response drop / CLI limit
5. Phase 11 regression 36 tests + Phase 4 regression 244 tests 전수 GREEN — 타 phase 영향 없음
6. Phase 11 Gap #1 (AGENT-STD-03) 구조적 해소 — Phase 13 live smoke 재도전 경로 개통
7. **Issue #3 실측 검증 완료 (2026-04-21)**: `ClaudeAgentSupervisorInvoker.__init__` (invokers.py L383-393) 의 `cli_runner: Callable | None = None` kwarg 기존 존재. Task 2 는 signature 변경 불필요 — `_compress_producer_output()` 추가 + `__call__` 1줄 삽입만 수행. Task 3 의 `cli_runner=mock` 주입은 현 signature 로 정상 동작 (TypeError 없음).
</success_criteria>

<output>
After completion, create `.planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-07-SUMMARY.md`.

대표님께 보고드립니다. AGENT-STD-03 완결. Supervisor producer_output 압축 인프라 확보 — severity_desc → score_asc 정렬 + char budget 2000 + error_codes 전수 보존. Phase 11 smoke 2차 "프롬프트가 너무 깁니다" (rc=1) gap 구조적 해소. Phase 13 에서 live smoke 재도전 시 본 압축 경로가 GATE 2 supervisor block 해제. Issue #3 invariant (`cli_runner` kwarg 존재) pytest regression 으로 lock.
</output>
