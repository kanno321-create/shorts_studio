---
phase: 12-agent-standardization-skill-routing-failures-protocol
plan: 05
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - .claude/hooks/pre_tool_use.py
  - scripts/audit/failures_rotate.py
  - tests/phase12/test_failures_rotation.py
autonomous: true
requirements:
  - FAIL-PROTO-01
status: pending
must_haves:
  truths:
    - "pre_tool_use.py `check_failures_append_only` 에 500줄 cap check 추가 — Write/Edit 로 FAILURES.md 에 501줄 이상 content 시도 시 deny + 한국어 안내 메시지"
    - "`FAILURES_ROTATE_CTX=1` env var whitelist — rotation script 실행 시에만 cap check bypass, 다른 경로는 보호"
    - "scripts/audit/failures_rotate.py 신규 — 500줄 cap 초과 시 oldest entries 를 `.claude/failures/_archive/{YYYY-MM}.md` 로 이관, head 31줄 (schema + notice) 보존, idempotent"
    - "`_imported_from_shorts_naberal.md` HARD-EXCLUDE — basename 절대 일치 검사, rotation 대상에서 명시적 제외 (Phase 3 D-14 sha256-lock 영구 보호)"
    - "test_failures_rotation.py 4 tests GREEN — test_hook_denies_over_500_lines + test_rotate_idempotent + test_imported_file_sha256_unchanged + test_archive_month_tag"
    - "Phase 6 D-11 append-only regression 무회귀 — 기존 check_failures_append_only 로직 보존"
  artifacts:
    - path: "scripts/audit/failures_rotate.py"
      provides: "FAIL-PROTO-01 rotation CLI — idempotent + env whitelist bypass"
      contains: "FAILURES_ROTATE_CTX, HEAD_PRESERVE_LINES = 31, CAP_LINES = 500"
    - path: ".claude/hooks/pre_tool_use.py"
      provides: "500줄 cap enforcement via check_failures_append_only 확장"
      contains: "500줄 cap 초과"
    - path: "tests/phase12/test_failures_rotation.py"
      provides: "FAIL-PROTO-01 pytest coverage 4 tests"
  key_links:
    - from: ".claude/hooks/pre_tool_use.py::check_failures_append_only"
      to: ".claude/failures/FAILURES.md"
      via: "line count check before Write/Edit + FAILURES_ROTATE_CTX env whitelist"
      pattern: "FAILURES_ROTATE_CTX"
    - from: "scripts/audit/failures_rotate.py"
      to: ".claude/failures/_archive/{YYYY-MM}.md"
      via: "oldest entries 이동 + head 31 preserve + idempotent sha256 check"
      pattern: "_archive.*YYYY-MM"
---

<objective>
FAILURES.md 500줄 rotation protocol 구현. (1) pre_tool_use.py check_failures_append_only 에 500줄 cap 추가 + env var whitelist, (2) scripts/audit/failures_rotate.py 신규 rotation CLI, (3) `_imported_from_shorts_naberal.md` HARD-EXCLUDE (RESEARCH §Pitfall 3 500줄 정각 오진단 방지), (4) pytest 4 cases.

**Purpose**: AGENT-STD-02 의 "FAILURES.md 500줄 전수 읽기" 전제 조건. 500줄 넘는 순간 에이전트가 `<mandatory_reads>` 시점에 일부만 읽게 되어 샘플링 발생 — FAIL-PROTO-01 이 이를 구조적으로 차단. `_imported_from_shorts_naberal.md` Phase 3 sha256-lock 영구 보호.

**Output**: pre_tool_use.py 확장 + failures_rotate.py 신규 + test_failures_rotation.py 4 tests GREEN + Phase 6 D-11 regression 무회귀.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-CONTEXT.md
@.planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-RESEARCH.md
@.claude/hooks/pre_tool_use.py
@.claude/failures/FAILURES.md
@.claude/failures/_imported_from_shorts_naberal.md
@CLAUDE.md
</context>

<interfaces>
기존 check_failures_append_only 계약 (Phase 6 D-11, pre_tool_use.py:160-210):
- Returns `str | None` (deny reason string vs allow)
- Scope: Write/Edit/MultiEdit on files whose basename is exactly `FAILURES.md`
- Explicit exclusion: `_imported_from_shorts_naberal.md` (D-14 immutable)

Plan 05 확장 계약 (D-A3-01 + D-A3-04):
- **NEW env whitelist**: `os.environ.get("FAILURES_ROTATE_CTX") == "1"` → return None (allow all, rotation CTX)
- **NEW 500줄 cap**: Write/Edit 로 FAILURES.md content 가 501줄 이상이 되면 deny + 한국어 안내 메시지
- 기존 append-only 로직 **보존** — 본 확장은 추가 guard rail

failures_rotate.py 계약 (RESEARCH §Pattern 2):
- Constants: `CAP_LINES = 500`, `HEAD_PRESERVE_LINES = 31` (schema + notice — FAILURES.md 실측 1-31줄)
- Target path: HARD-CODED `.claude/failures/FAILURES.md` (glob 금지 — _imported 보호)
- Archive path: `.claude/failures/_archive/{YYYY-MM}.md` (rotation 시점 KST 월)
- Idempotent: 동일 상태 재실행 시 sha256 unchanged + no-op
- Hook bypass: `os.environ["FAILURES_ROTATE_CTX"] = "1"` 설정 후 Write/open('a')

_imported_from_shorts_naberal.md 현재 크기 = 500줄 (RESEARCH §Pitfall 3 정각 충돌) — sha256 Phase 3 D-14 checkpoint 로 lock.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: test_failures_rotation.py 실 assertion 4 cases (TDD RED — Hook 확장 전 작성)</name>
  <files>tests/phase12/test_failures_rotation.py</files>
  <behavior>
    - Test 1: `test_hook_denies_over_500_lines` — tmp FAILURES.md 501줄 content Write 시도 시 check_failures_append_only 가 deny reason 반환 (contains "500줄 cap")
    - Test 2: `test_rotate_idempotent` — 501줄 FAILURES.md rotate() 실행 후 동일 상태 재실행 시 2회차 no-op (sha256 unchanged after 1st rotate)
    - Test 3: `test_imported_file_sha256_unchanged` — rotation 전후 `_imported_from_shorts_naberal.md` sha256 unchanged (Phase 3 D-14 invariant)
    - Test 4: `test_archive_month_tag` — rotation 후 `_archive/{YYYY-MM}.md` 생성 + 이관된 line count = 500 - 31 head
    - Test 5: `test_env_whitelist_bypasses_cap` — `FAILURES_ROTATE_CTX=1` 환경 하 check_failures_append_only 는 None 반환 (bypass)
  </behavior>
  <read_first>
    - tests/phase12/test_failures_rotation.py (Plan 01 Task 2 red stub)
    - .claude/hooks/pre_tool_use.py lines 160-210 (check_failures_append_only 기존)
    - .claude/failures/_imported_from_shorts_naberal.md (500줄 정각 실측, sha256 baseline 참조)
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-RESEARCH.md §Pitfalls #3 (sha256 lock 관련)
  </read_first>
  <action>
red stub 제거 + 실 assertion 5 tests:

```python
"""FAIL-PROTO-01 — 500줄 cap Hook + rotation idempotency + immutable _imported file."""

from __future__ import annotations
import hashlib
import os
import sys
from pathlib import Path

import pytest

REPO_ROOT = Path(__file__).resolve().parents[2]
sys.path.insert(0, str(REPO_ROOT / ".claude" / "hooks"))
sys.path.insert(0, str(REPO_ROOT))

# Lazy imports after sys.path setup
from pre_tool_use import check_failures_append_only  # noqa: E402


def _sha256(p: Path) -> str:
    return hashlib.sha256(p.read_bytes()).hexdigest()


def test_env_whitelist_bypasses_cap(tmp_path, monkeypatch):
    """FAILURES_ROTATE_CTX=1 → cap check 우회."""
    f = tmp_path / "FAILURES.md"
    f.write_text("\n".join(f"line {i}" for i in range(100)), encoding="utf-8")
    monkeypatch.setenv("FAILURES_ROTATE_CTX", "1")
    result = check_failures_append_only(
        "Write",
        {"file_path": str(f), "content": "\n".join(f"line {i}" for i in range(600))},
    )
    assert result is None, f"env whitelist failed: {result}"


def test_hook_denies_over_500_lines(tmp_path, monkeypatch):
    """501 줄 content Write 시도 시 deny + 한국어 안내."""
    monkeypatch.delenv("FAILURES_ROTATE_CTX", raising=False)
    f = tmp_path / "FAILURES.md"
    f.write_text("existing\n", encoding="utf-8")
    new_content = "\n".join(f"line {i}" for i in range(501))
    result = check_failures_append_only(
        "Write",
        {"file_path": str(f), "content": new_content},
    )
    assert result is not None, "expected deny, got None"
    assert "500줄" in result or "500줄 cap" in result, f"no cap message: {result}"
    assert "failures_rotate.py" in result, f"no rotation guidance: {result}"


def test_rotate_idempotent(tmp_path, monkeypatch):
    """rotate() 2회 연속 실행 시 2회차는 no-op."""
    # Import rotate() from Plan 05 Task 3 산출
    sys.path.insert(0, str(REPO_ROOT))
    from scripts.audit import failures_rotate

    # Seed 501-line FAILURES.md (31 head + 470 body)
    f_dir = tmp_path / ".claude" / "failures"
    f_dir.mkdir(parents=True)
    head = ["# FAILURES.md"] + [f"> line {i}" for i in range(30)]  # 31 lines
    body = [f"### FAIL-{i:03d}" for i in range(470)]
    (f_dir / "FAILURES.md").write_text("\n".join(head + body) + "\n", encoding="utf-8")

    monkeypatch.setattr(failures_rotate, "REPO_ROOT", tmp_path)
    monkeypatch.setattr(failures_rotate, "FAILURES_FILE", f_dir / "FAILURES.md")
    monkeypatch.setattr(failures_rotate, "ARCHIVE_DIR", f_dir / "_archive")

    r1 = failures_rotate.rotate()
    sha_after_first = _sha256(f_dir / "FAILURES.md")
    r2 = failures_rotate.rotate()
    sha_after_second = _sha256(f_dir / "FAILURES.md")
    assert r1 == 1, f"first rotate expected 1, got {r1}"
    assert r2 == 0, f"second rotate expected 0 (idempotent), got {r2}"
    assert sha_after_first == sha_after_second, "FAILURES.md changed between identical rotations"


def test_imported_file_sha256_unchanged(tmp_path, monkeypatch):
    """_imported_from_shorts_naberal.md sha256 은 rotation 전후 불변 (D-14 lock)."""
    from scripts.audit import failures_rotate

    # Seed real _imported_from_shorts_naberal.md 경로에 copy 대신 tmp 경로 재현
    f_dir = tmp_path / ".claude" / "failures"
    f_dir.mkdir(parents=True)
    imported = f_dir / "_imported_from_shorts_naberal.md"
    imported.write_text("\n".join(f"imported line {i}" for i in range(500)) + "\n",
                       encoding="utf-8")
    sha_before = _sha256(imported)

    # Seed oversized FAILURES.md (to trigger rotation)
    head = ["# FAILURES.md"] + [f"> line {i}" for i in range(30)]
    body = [f"### FAIL-{i:03d}" for i in range(470)]
    (f_dir / "FAILURES.md").write_text("\n".join(head + body) + "\n", encoding="utf-8")

    monkeypatch.setattr(failures_rotate, "REPO_ROOT", tmp_path)
    monkeypatch.setattr(failures_rotate, "FAILURES_FILE", f_dir / "FAILURES.md")
    monkeypatch.setattr(failures_rotate, "ARCHIVE_DIR", f_dir / "_archive")

    failures_rotate.rotate()
    sha_after = _sha256(imported)
    assert sha_before == sha_after, "_imported file sha256 changed — D-14 lock broken"


def test_archive_month_tag(tmp_path, monkeypatch):
    """rotation 후 _archive/{YYYY-MM}.md 생성 + 이관된 line count 검증."""
    from datetime import datetime
    from zoneinfo import ZoneInfo
    from scripts.audit import failures_rotate

    f_dir = tmp_path / ".claude" / "failures"
    f_dir.mkdir(parents=True)
    head = ["# FAILURES.md"] + [f"> line {i}" for i in range(30)]
    body = [f"### FAIL-{i:03d}" for i in range(470)]
    (f_dir / "FAILURES.md").write_text("\n".join(head + body) + "\n", encoding="utf-8")

    monkeypatch.setattr(failures_rotate, "REPO_ROOT", tmp_path)
    monkeypatch.setattr(failures_rotate, "FAILURES_FILE", f_dir / "FAILURES.md")
    monkeypatch.setattr(failures_rotate, "ARCHIVE_DIR", f_dir / "_archive")

    failures_rotate.rotate()
    now = datetime.now(ZoneInfo("Asia/Seoul"))
    tag = now.strftime("%Y-%m")
    archive = f_dir / "_archive" / f"{tag}.md"
    assert archive.exists(), f"archive not created: {archive}"
    # 이관 lines = 500 cap - 31 head = 469 (range 31..500)
    archive_lines = archive.read_text(encoding="utf-8").strip().splitlines()
    assert 460 <= len(archive_lines) <= 475, f"archive lines {len(archive_lines)} out of expected 460-475"
```

**중요**: 본 Task 는 TDD RED. Task 2 (Hook 확장) + Task 3 (rotate 스크립트) 완료 전에는 5 tests 모두 FAIL 예상 (check_failures_append_only 에 cap check 없음 + failures_rotate 모듈 부재 → ImportError). Task 2+3 완료 후 GREEN 전환.
  </action>
  <verify>
    <automated>py -3.11 -m pytest tests/phase12/test_failures_rotation.py -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - 5 tests 정의 존재 (grep `^def test_` tests/phase12/test_failures_rotation.py count = 5)
    - Task 2+3 완료 **전**: pytest 실행 시 5 tests 모두 FAIL 또는 ERROR (RED 상태) — 정상
    - 각 test 의 body 에 assertion 존재 (`assert` keyword ≥ 2 per test)
    - `monkeypatch` fixture 사용 ≥ 3 times (env + REPO_ROOT + FAILURES_FILE)
    - Skip decorator 제거됨 (`@pytest.mark.skip` count = 0)
  </acceptance_criteria>
  <done>
TDD RED 단계 완결. 5 tests 정의 + Task 2+3 GREEN 전환 대상 확보.
  </done>
</task>

<task type="auto">
  <name>Task 2: pre_tool_use.py check_failures_append_only 500줄 cap + env whitelist 확장</name>
  <files>.claude/hooks/pre_tool_use.py</files>
  <read_first>
    - .claude/hooks/pre_tool_use.py lines 155-210 (기존 check_failures_append_only 본문)
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-RESEARCH.md §Code Examples Example 1 (확장 코드 템플릿)
    - .claude/failures/FAILURES.md (현 58줄 — cap 여유 확인)
    - tests/phase12/test_failures_rotation.py (Task 1 산출 — assertion 대상)
  </read_first>
  <action>
`.claude/hooks/pre_tool_use.py` 의 `check_failures_append_only` function (line 160-210) 수정.

**절차**:

1. Function 본문 **맨 앞** (line 179, `fp = tool_input.get(...)` 직전) 에 env whitelist 추가:

```python
def check_failures_append_only(tool_name: str, tool_input: dict) -> str | None:
    """D-11 append-only + D-A3-01 500줄 cap + D-A3-04 env whitelist.
    ... (기존 docstring 보존)
    """
    # --- D-A3-04 env whitelist (Phase 12 FAIL-PROTO-01) ---
    import os as _os
    if _os.environ.get("FAILURES_ROTATE_CTX") == "1":
        return None  # rotation script whitelist — bypass all checks
    # ---------------------------------------------------------

    fp = tool_input.get("file_path", "")
    if not fp:
        return None
    name = fp.replace("\\", "/").rsplit("/", 1)[-1]
    if name != "FAILURES.md":
        return None

    # --- D-A3-01 500줄 cap (Phase 12 FAIL-PROTO-01) ---
    if tool_name in ("Write", "Edit"):
        from pathlib import Path as _Path
        p = _Path(fp)
        existing = p.read_text(encoding="utf-8") if p.exists() else ""
        if tool_name == "Write":
            candidate = tool_input.get("content", "")
        else:  # Edit
            old_s = tool_input.get("old_string", "")
            new_s = tool_input.get("new_string", "")
            if old_s:
                candidate = existing.replace(old_s, new_s, 1)
            else:
                candidate = existing + new_s
        if len(candidate.splitlines()) > 500:
            return (
                "FAILURES.md 500줄 cap 초과 — "
                "`python scripts/audit/failures_rotate.py` 실행 후 재시도. "
                "(Phase 12 FAIL-PROTO-01, 500줄 cap 는 에이전트 <mandatory_reads> 전수 읽기 전제)"
            )
    # ----------------------------------------------------

    # [기존 append-only 로직 전량 보존 — line 187-210 그대로]
    if tool_name == "Edit":
        ...
    if tool_name == "Write":
        ...
    if tool_name == "MultiEdit":
        ...
    return None
```

**주의**:
- `import os` 와 `from pathlib import Path` 는 function 내부에서 `_os` / `_Path` alias 로 lazy import (기존 module level import 영향 최소화).
- 기존 append-only 검사 (Edit non-empty old_string / Write not-prefix / MultiEdit non-empty) 는 **한 줄도 건드리지 말 것**. Phase 6 regression 무회귀.
- 500줄 cap 는 tool_name == "Write" OR "Edit" 만 체크. MultiEdit 은 복잡 — MultiEdit 에도 cap 적용하려면 별도 loop 필요하나 본 Plan scope 밖 (Phase 13 candidate).
  </action>
  <verify>
    <automated>py -3.11 -m pytest tests/phase12/test_failures_rotation.py::test_hook_denies_over_500_lines tests/phase12/test_failures_rotation.py::test_env_whitelist_bypasses_cap -v 2>&1 | tail -10; py -3.11 -m pytest tests/phase06/test_failures_append_only.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `tests/phase12/test_failures_rotation.py::test_hook_denies_over_500_lines` PASSES
    - `tests/phase12/test_failures_rotation.py::test_env_whitelist_bypasses_cap` PASSES
    - `tests/phase06/test_failures_append_only.py` exit 0 (기존 D-11 regression 무회귀)
    - `grep -c "FAILURES_ROTATE_CTX" .claude/hooks/pre_tool_use.py` ≥ 1
    - `grep -c "500줄 cap" .claude/hooks/pre_tool_use.py` ≥ 1
    - `grep -c "failures_rotate.py" .claude/hooks/pre_tool_use.py` ≥ 1
  </acceptance_criteria>
  <done>
pre_tool_use.py check_failures_append_only 확장 완료. 500줄 cap + env whitelist 이중 계약. Phase 6 D-11 regression 무회귀.
  </done>
</task>

<task type="auto">
  <name>Task 3: scripts/audit/failures_rotate.py 신규 (rotation CLI + idempotent + _imported HARD-EXCLUDE)</name>
  <files>scripts/audit/failures_rotate.py</files>
  <read_first>
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-RESEARCH.md §Pattern 2 (failures_rotate.py 코드 template)
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-RESEARCH.md §Pitfalls #3 (_imported 500줄 정각 충돌 방지)
    - scripts/audit/skill_patch_counter.py (stdlib-only 패턴 + Windows cp949 guard 참조)
    - .claude/failures/FAILURES.md lines 1-35 (head boundary — line 31 = blank, line 32 = 첫 FAIL entry)
    - .claude/failures/_imported_from_shorts_naberal.md (HARD-EXCLUDE 대상)
  </read_first>
  <action>
`scripts/audit/failures_rotate.py` 신규 파일 생성:

```python
#!/usr/bin/env python3
"""failures_rotate.py — FAIL-PROTO-01 rotation CLI.

FAILURES.md 가 500줄 cap 초과 시 oldest entries 를 `.claude/failures/_archive/{YYYY-MM}.md`
로 이관. head 31줄 (schema + notice) 은 보존. idempotent (동일 상태 재실행 시 no-op).

CRITICAL: `_imported_from_shorts_naberal.md` 는 HARD-EXCLUDE (Phase 3 D-14 sha256-lock).
본 스크립트 는 `.claude/failures/FAILURES.md` 만 스캔 — glob 사용 금지.

Usage:
    py -3.11 scripts/audit/failures_rotate.py

Exit codes:
    0: no rotation needed (idempotent no-op) OR rotation succeeded
    1: error (file not found, permission, etc.)

D-A3-01 / D-A3-02 / D-A3-03 / D-A3-04.
"""

from __future__ import annotations
import hashlib
import os
import sys
from datetime import datetime
from pathlib import Path
from zoneinfo import ZoneInfo

try:
    sys.stdout.reconfigure(encoding="utf-8", errors="replace")
    sys.stderr.reconfigure(encoding="utf-8", errors="replace")
except Exception:
    pass

KST = ZoneInfo("Asia/Seoul")

REPO_ROOT = Path(__file__).resolve().parents[2]

# HARD-CODED paths — NO glob, NO wildcard (Pitfall 3 방지)
FAILURES_FILE = REPO_ROOT / ".claude" / "failures" / "FAILURES.md"
ARCHIVE_DIR = REPO_ROOT / ".claude" / "failures" / "_archive"

# HARD-EXCLUDE paths — 절대 rotation 대상 아님 (D-14 sha256-lock, D-A3-03)
IMPORTED_FILE_BASENAME = "_imported_from_shorts_naberal.md"

CAP_LINES = 500
HEAD_PRESERVE_LINES = 31  # schema (line 1-26) + notice (27-31), 실측 기반


def _assert_not_imported_file(path: Path) -> None:
    """D-A3-03 guard: never rotate _imported_from_shorts_naberal.md."""
    if path.name == IMPORTED_FILE_BASENAME:
        raise RuntimeError(
            f"HARD-EXCLUDE violation: refuse to rotate {path.name} "
            f"(Phase 3 D-14 sha256-lock, D-A3-03 영구 면제)"
        )


def rotate() -> int:
    """Rotate FAILURES.md if over cap. Returns:
        1: rotated
        0: no-op (idempotent — under cap, or already rotated)
    """
    _assert_not_imported_file(FAILURES_FILE)

    if not FAILURES_FILE.exists():
        return 0  # nothing to rotate

    text = FAILURES_FILE.read_text(encoding="utf-8")
    lines = text.splitlines()
    if len(lines) <= CAP_LINES:
        return 0  # idempotent — under cap

    # Compute archive path (월 기준 KST)
    month_tag = datetime.now(KST).strftime("%Y-%m")
    archive_path = ARCHIVE_DIR / f"{month_tag}.md"
    archive_path.parent.mkdir(parents=True, exist_ok=True)

    # Oldest entries = line 32 ~ CAP_LINES (head 31 preserved, tail from CAP_LINES+1)
    to_archive = lines[HEAD_PRESERVE_LINES:CAP_LINES]

    # Set env whitelist BEFORE write (Hook bypass via D-A3-04)
    os.environ["FAILURES_ROTATE_CTX"] = "1"
    try:
        # 1. Archive append (oldest entries move)
        with archive_path.open("a", encoding="utf-8") as f:
            f.write(f"\n<!-- Rotated at {datetime.now(KST).isoformat()} -->\n")
            f.write("\n".join(to_archive) + "\n")
        # 2. FAILURES.md rewrite: head 31 + tail (CAP_LINES 이후)
        new_head = lines[:HEAD_PRESERVE_LINES]
        new_tail = lines[CAP_LINES:]
        new_content = "\n".join(new_head + new_tail) + "\n"
        FAILURES_FILE.write_text(new_content, encoding="utf-8")
    finally:
        del os.environ["FAILURES_ROTATE_CTX"]

    return 1


def main(argv: list[str] | None = None) -> int:
    """CLI entry point. Exit 0 on success/no-op, 1 on error."""
    try:
        result = rotate()
    except Exception as e:
        print(f"ERROR: {e}", file=sys.stderr)
        return 1
    if result == 1:
        print(f"OK: rotated FAILURES.md (oldest entries → {ARCHIVE_DIR}/{datetime.now(KST).strftime('%Y-%m')}.md)")
    else:
        print(f"OK: no rotation needed (FAILURES.md under {CAP_LINES} lines)")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

**중요**:
- `_imported_from_shorts_naberal.md` 가 basename 일치하면 `_assert_not_imported_file` 가 즉시 RuntimeError raise — 방어적 프로그래밍으로 Pitfall 3 영구 차단.
- `FAILURES_ROTATE_CTX=1` 설정 → archive append + FAILURES.md 재작성 → `del os.environ[...]` (finally). Hook 는 이 env 세팅 기간 동안만 bypass.
- `archive_path.open("a", ...)` + `FAILURES_FILE.write_text(...)` 직접 I/O — Hook 는 이 경로로 호출 안 됨 (Python subprocess 가 아닌 직접 file API).
- idempotent: 두 번째 호출 시 line count 가 cap 이하 → 즉시 return 0.
  </action>
  <verify>
    <automated>py -3.11 -m pytest tests/phase12/test_failures_rotation.py -v 2>&1 | tail -15; py -3.11 scripts/audit/failures_rotate.py 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `py -3.11 -m pytest tests/phase12/test_failures_rotation.py -v` exits 0, 5 passed
    - `py -3.11 scripts/audit/failures_rotate.py` exits 0 (현재 FAILURES.md 58줄이므로 no-op)
    - File `scripts/audit/failures_rotate.py` contains `_assert_not_imported_file` function
    - File contains `IMPORTED_FILE_BASENAME = "_imported_from_shorts_naberal.md"` literal
    - File contains `CAP_LINES = 500` + `HEAD_PRESERVE_LINES = 31` constants
    - File contains `os.environ["FAILURES_ROTATE_CTX"] = "1"` setter
    - File contains `del os.environ["FAILURES_ROTATE_CTX"]` cleanup
    - `wc -l .claude/failures/_imported_from_shorts_naberal.md` = 500 (unchanged, Phase 3 D-14 보존)
    - Phase 6 regression: `py -3.11 -m pytest tests/phase06/test_failures_append_only.py -q` exit 0
  </acceptance_criteria>
  <done>
failures_rotate.py CLI 개통. 5 pytest tests GREEN. _imported 영구 보호. Hook whitelist 계약 완결.
  </done>
</task>

</tasks>

<verification>
```bash
py -3.11 -m pytest tests/phase12/test_failures_rotation.py -v           # 5 passed
py -3.11 -m pytest tests/phase06/test_failures_append_only.py -q        # exit 0 (regression)
py -3.11 scripts/audit/failures_rotate.py                                # exit 0 (no-op, 58 lines)
wc -l .claude/failures/_imported_from_shorts_naberal.md                  # 500 (unchanged)
wc -l .claude/failures/FAILURES.md                                       # ≤ 500
grep -c "FAILURES_ROTATE_CTX" .claude/hooks/pre_tool_use.py              # ≥ 1
grep -c "FAILURES_ROTATE_CTX" scripts/audit/failures_rotate.py           # ≥ 2 (setter + cleanup)
```
</verification>

<success_criteria>
1. pre_tool_use.py 500줄 cap + env whitelist 확장 — Phase 6 D-11 regression 무회귀
2. failures_rotate.py rotation CLI — idempotent + _imported HARD-EXCLUDE + KST 월 archive
3. test_failures_rotation.py 5 tests GREEN — FAIL-PROTO-01 full coverage
4. `_imported_from_shorts_naberal.md` sha256 invariant 보존 (Phase 3 D-14 lock)
5. 에이전트 `<mandatory_reads>` 전수 읽기 feasibility 확보 — 500줄 이하 유지
</success_criteria>

<output>
After completion, create `.planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-05-SUMMARY.md`.

대표님께 보고드립니다. FAIL-PROTO-01 완결. FAILURES.md 500줄 cap + archive rotation 프로토콜 확보. `_imported_from_shorts_naberal.md` Phase 3 D-14 영구 lock 보존. 에이전트 전수 읽기 전제 조건 구조적으로 보장.
</output>
