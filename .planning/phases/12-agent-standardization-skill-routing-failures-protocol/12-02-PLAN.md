---
phase: 12-agent-standardization-skill-routing-failures-protocol
plan: 02
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - .claude/agents/producers/niche-classifier/AGENT.md
  - .claude/agents/producers/researcher/AGENT.md
  - .claude/agents/producers/director/AGENT.md
  - .claude/agents/producers/scripter/AGENT.md
  - .claude/agents/producers/metadata-seo/AGENT.md
  - .claude/agents/producers/scene-planner/AGENT.md
  - .claude/agents/producers/shot-planner/AGENT.md
  - .claude/agents/producers/script-polisher/AGENT.md
  - .claude/agents/producers/voice-producer/AGENT.md
  - .claude/agents/producers/asset-sourcer/AGENT.md
  - .claude/agents/producers/thumbnail-designer/AGENT.md
  - .claude/agents/producers/assembler/AGENT.md
  - .claude/agents/producers/publisher/AGENT.md
  - tests/phase12/test_agent_md_schema.py
  - tests/phase12/test_f_d2_exception_batch.py
  - scripts/audit/skill_patch_counter.py
  - .claude/failures/FAILURES.md
autonomous: true
requirements:
  - AGENT-STD-01
  - AGENT-STD-02
  - FAIL-PROTO-02
status: pending
commit_convention:
  # Issue #5 fix — Task 8 commit granularity explicit
  - "Tasks 1-6 each commit as ONE atomic commit per task (not per file)."
  - "Commit subject MUST start with `feat(12-02):` and include `[plan-02]` marker token anywhere in subject."
  - "Example Task 1: `feat(12-02): niche-classifier AGENT.md v1.2 migration [plan-02]`"
  - "Task 8 audit script queries git log via `--grep='\\[plan-02\\]' --name-only --pretty=format:` (marker-filtered, NOT `-n 8`)."
must_haves:
  truths:
    - "13 Producer (harvest-importer 제외) 전체 AGENT.md 가 5 XML 블록 schema 준수 — trend-collector 포함 13/13 verify_agent_md_schema.py 통과"
    - "각 Producer AGENT.md 의 기존 body prose (Purpose / Inputs / Outputs / Prompt / References / MUST REMEMBER) 전량 보존 — git diff 로 head 섹션만 수정"
    - "Phase 4 regression 244 pytest GREEN 유지 — AGENT.md 본문 구조 변경이 validate_all_agents.py / grep_gan_contamination.py 를 깨지 않음"
    - "FAIL-PROTO-02 F-D2-EXCEPTION-02 단일 entry 기록 — 12 Producer batch commits 가 skill_patch_counter 에서 중복 기록되지 않음 (AUDIT-05 idempotency 재활용)"
    - "test_agent_md_schema.py + test_f_d2_exception_batch.py 실 assertion 포함 — Wave 0 red stub 제거"
    - "Tasks 1-6 의 모든 target 파일에 '매 호출마다 전수 읽기, 샘플링 금지' 한국어 literal 이 포함 — per-task acceptance_criteria 로 fast-fail 보장 (D-A1-03)"
  artifacts:
    - path: ".claude/agents/producers/niche-classifier/AGENT.md"
      provides: "Wave 2a Core Producer — 5-block schema compliant"
      contains: "<role>, <mandatory_reads>, <output_format>, <skills>, <constraints>, version: 1.2"
    - path: ".claude/agents/producers/scripter/AGENT.md"
      provides: "Wave 2a Core Producer — 5-block schema compliant (대본 생성 핵심)"
      contains: "scripter 고유 prose 보존 + 5 XML 블록"
    - path: "tests/phase12/test_agent_md_schema.py"
      provides: "30 AGENT.md × 5 block pytest — Wave 2 완료 시 13 producer GREEN"
    - path: ".claude/failures/FAILURES.md"
      provides: "F-D2-EXCEPTION-02 single batch entry — 12 commits 일괄 directive-authorized"
      contains: "F-D2-EXCEPTION-02"
  key_links:
    - from: "tests/phase12/test_agent_md_schema.py::test_all_30_agents_have_5_blocks"
      to: "13 producer AGENT.md files"
      via: "verify_file() call for each path in _collect_all_agent_mds()"
      pattern: "verify_file.*producers.*AGENT.md"
    - from: "scripts/audit/skill_patch_counter.py"
      to: ".claude/failures/FAILURES.md"
      via: "append_f_d2_exception_02 (신규 function)"
      pattern: "F-D2-EXCEPTION-02"
---

<objective>
13 Producer 중 trend-collector 제외 12개의 AGENT.md 를 Plan 01 template 기반으로 5-block schema 로 migration. 2 sub-wave 구조 (Core 5 → Support 7) 로 파일 수준 병렬 + 카테고리 수준 순차. 완료 시 13/13 producer schema compliant + Phase 4 regression 무회귀. 단일 F-D2-EXCEPTION-02 batch entry 로 30+ 파일 patch 를 `skill_patch_counter` 가 기록.

**Purpose**: Producer 전수 표준화는 Phase 11 F-D2-EXCEPTION-01 (trend-collector JSON 미준수) 재발 차단의 핵심. 모든 producer 가 `<output_format>` 블록 + 금지 패턴 5종 명시 + `<mandatory_reads>` 전수 읽기 문구를 받아 3대 고질 (출력 drift / 도구 오용 / 재호출 루프) 해소.

**Output**: 12 producer AGENT.md v1.2 migration + test_agent_md_schema.py 실 assertion + test_f_d2_exception_batch.py 실 assertion + skill_patch_counter 확장 + FAILURES.md F-D2-EXCEPTION-02 entry.

**Commit Convention (Issue #5 fix)**: Tasks 1~6 각각 단일 atomic commit, 제목 prefix `feat(12-02):` + marker `[plan-02]` 포함. Task 8 은 `git log --grep='\[plan-02\]' --name-only --pretty=format:` 로 마커 기반 조회 (fragile `-n 8` 제거).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-CONTEXT.md
@.planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-RESEARCH.md
@.planning/phases/12-agent-standardization-skill-routing-failures-protocol/templates/producer.md.template
@.claude/agents/producers/trend-collector/AGENT.md
@CLAUDE.md
</context>

<interfaces>
Plan 01 에서 확보된 계약:
- Template: `.planning/phases/12-.../templates/producer.md.template` — clone base (direct clone 금지, 에이전트별 customize)
- Verifier: `py -3.11 scripts/validate/verify_agent_md_schema.py --all` — exit 0 기대점
- Schema fixed order (regex): `<role>` → `<mandatory_reads>` → `<output_format>` → `<skills>` → `<constraints>`
- Mandatory reads 한국어 literal: `매 호출마다 전수 읽기, 샘플링 금지`
- v1.2 frontmatter 버전 (Plan 01 trend-collector 선례)

skill_patch_counter 확장 계약 (Task 8):
- 신규 function: `append_f_d2_exception_02(wave_label: str, commits: list[dict], scope_files: list[str], authorized_by: str, repo_root: Path, now: datetime) -> None`
- Idempotency: FAILURES.md 에 이미 `F-D2-EXCEPTION-02` 있으면 wave supplement 만 append (중복 header 금지)
- Reuses `append_failures` 의 `open('a')` Hook-bypass 패턴 (line 263)

13 Producer 분류 (D-A2-01 실측 기반):
- **Core 5 (Wave 2a)**: niche-classifier, researcher, director, scripter, metadata-seo — 파이프라인 핵심 생성 체인
- **Support 7 (Wave 2b)**: scene-planner, shot-planner, script-polisher, voice-producer, asset-sourcer, thumbnail-designer, assembler, publisher — 실측 8개 → Support 7 확정하되 publisher 는 upload 파이프라인 boundary 이므로 Support 끝에 배치

(harvest-importer Phase 3 deprecated → scope 밖. 1 + 12 = 13 Producer Phase 12 scope.)

**Commit & marker convention**:
- Tasks 1~6 (migration tasks) 각각 **1 commit per task** (파일당 commit 금지, 태스크당 commit).
- Subject format: `feat(12-02): <short description> [plan-02]` — `[plan-02]` marker token 필수.
- Task 8 `append_f_d2_exception_02` 호출 시 commits 조회는 marker filter:
  ```
  git log --grep='\[plan-02\]' --name-only --pretty=format:%n---commit---%n%h %s%n%b
  ```
- 마커는 rev-list 수 기반 `-n N` fragile 쿼리 대체 — pre-existing commits 혼입 / per-task granularity 변동 모두 무관.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: niche-classifier AGENT.md v1.2 migration (Core 2/5)</name>
  <files>.claude/agents/producers/niche-classifier/AGENT.md</files>
  <read_first>
    - .claude/agents/producers/niche-classifier/AGENT.md (현 상태 — body prose 보존 대상)
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/templates/producer.md.template (Plan 01 산출)
    - .claude/agents/producers/trend-collector/AGENT.md (Plan 01 Task 4 v1.2 선례 — 비교 기준)
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-RESEARCH.md §Pitfalls #1 (RUB-06 본문 손실 방지)
  </read_first>
  <action>
niche-classifier AGENT.md 를 template 기반 v1.2 로 승격.

**절차**:
1. 현재 파일 read — frontmatter + body 구조 파악.
2. frontmatter: `version: X.Y` → `version: 1.2` (다른 필드 보존).
3. body head (line 10 이후) 에 5 XML 블록 재구성 — template 을 copy + 에이전트별 customize:
   - `<role>`: "니치 분류 producer. trend-collector 로부터 keywords[10-20] + niche_tag 후보 를 받아 7 채널바이블 중 1개 최종 매핑 + channel_bible 인라인 요약 을 downstream researcher 에 전달. CONTENT-03 바이블 매핑 권한 보유."
   - `<mandatory_reads>` 3 항목 (**'매 호출마다 전수 읽기, 샘플링 금지' literal 필수 — D-A1-03**):
     1. `.claude/failures/FAILURES.md` — 전체 (500줄 cap)
     2. `wiki/continuity_bible/channel_identity.md` — 공통 정체성
     3. `.claude/skills/gate-dispatcher/SKILL.md` — GATE 2 NICHE dispatch 계약
   - `<output_format>`: JSON 스키마 `{gate, niche_tag, channel_bible_summary, confidence, rationale}` + 금지 패턴 5종 (대화체/질문/옵션/서문/꼬리) + F-D2-EXCEPTION-01 이유
   - `<skills>`:
     - `gate-dispatcher` (required) — GATE 2 NICHE dispatch
     - `progressive-disclosure` (optional) — SKILL 길이 가드
   - `<constraints>`:
     - `inspector_prompt 읽기 금지 (RUB-06 GAN 분리 mirror)`
     - `maxTurns=2 준수 (RUB-05)` (niche 분류는 간단, 2 turn 충분)
     - `한국어 출력 baseline`
     - `T2V 경로 절대 금지 (I2V only, D-13)`
     - `FAILURES.md append-only (D-11)`
     - `niche_tag 도메인 제한 (CONTENT-03)` — 7 채널바이블 slug 중 하나
4. 기존 `## Purpose` 이후 body 한 글자도 건드리지 말 것.
5. 완료 후 `py -3.11 scripts/validate/verify_agent_md_schema.py .claude/agents/producers/niche-classifier/AGENT.md` exit 0 확인.

**Commit (Issue #5)**: 본 Task 완료 시 단일 commit. Subject: `feat(12-02): niche-classifier AGENT.md v1.2 migration [plan-02]`.
  </action>
  <verify>
    <automated>py -3.11 scripts/validate/verify_agent_md_schema.py .claude/agents/producers/niche-classifier/AGENT.md</automated>
  </verify>
  <acceptance_criteria>
    - verify_agent_md_schema.py 단일 파일 인자 호출 exit 0
    - `grep -c "^version: 1.2$" .claude/agents/producers/niche-classifier/AGENT.md` = 1
    - `grep -c "inspector_prompt 읽기 금지 (RUB-06 GAN 분리 mirror)" .claude/agents/producers/niche-classifier/AGENT.md` ≥ 1
    - `grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/producers/niche-classifier/AGENT.md` ≥ 1
    - `grep -c "^## MUST REMEMBER" .claude/agents/producers/niche-classifier/AGENT.md` ≥ 1 (body prose 보존 증거)
  </acceptance_criteria>
  <done>
niche-classifier v1.2 — Core 2/5 완료.
  </done>
</task>

<task type="auto">
  <name>Task 2: researcher AGENT.md v1.2 migration (Core 3/5)</name>
  <files>.claude/agents/producers/researcher/AGENT.md</files>
  <read_first>
    - .claude/agents/producers/researcher/AGENT.md (현 상태)
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/templates/producer.md.template
    - .claude/memory/feedback_notebooklm_query.md (researcher 고유 NotebookLM RAG 의존성 참조)
  </read_first>
  <action>
researcher AGENT.md 를 v1.2 로 승격. Task 1 절차 따르되 에이전트 고유값 치환:

- `<role>`: "리서치 producer. niche-classifier 확정 niche 기반으로 NotebookLM RAG + wiki 참조 로 팩트 7-15건 수집 + citation URL 포함. downstream director 의 연출 근거 제공."
- `<mandatory_reads>` 3 항목 (**'매 호출마다 전수 읽기, 샘플링 금지' literal 필수 — D-A1-03**):
  1. `.claude/failures/FAILURES.md`
  2. `wiki/continuity_bible/channel_identity.md` + `.preserved/harvested/theme_bible_raw/{niche}.md` (niche 확정 후)
  3. `.claude/skills/gate-dispatcher/SKILL.md`
- `<output_format>`: JSON `{gate: "RESEARCH_NLM", facts[], citations[], confidence_per_fact[]}` + 금지 패턴 5종
- `<skills>`: `gate-dispatcher` (required), `progressive-disclosure` (optional)
- `<constraints>`:
  - `inspector_prompt 읽기 금지 (RUB-06 GAN 분리 mirror)`
  - `maxTurns=5 준수` (NotebookLM fallback chain — 3-tier 고려)
  - `NotebookLM Fallback Chain 의무 (WIKI-04)` — RAG → grep wiki/ → hardcoded defaults
  - `한국어 출력 baseline` (citation URL 은 원어 보존)
  - `T2V 경로 절대 금지`
  - `FAILURES.md append-only`

body 보존 + schema verifier exit 0 확인.

**Commit (Issue #5)**: `feat(12-02): researcher AGENT.md v1.2 migration [plan-02]`.
  </action>
  <verify>
    <automated>py -3.11 scripts/validate/verify_agent_md_schema.py .claude/agents/producers/researcher/AGENT.md</automated>
  </verify>
  <acceptance_criteria>
    - verify_agent_md_schema.py exit 0
    - `grep -c "^version: 1.2$" .claude/agents/producers/researcher/AGENT.md` = 1
    - `grep -c "NotebookLM Fallback Chain 의무 (WIKI-04)" .claude/agents/producers/researcher/AGENT.md` ≥ 1
    - `grep -c "inspector_prompt 읽기 금지" .claude/agents/producers/researcher/AGENT.md` ≥ 1
    - **`grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/producers/researcher/AGENT.md` ≥ 1 (D-A1-03 literal — Issue #2 fix)**
    - Body prose 보존: `grep -c "^## Purpose\|^## Inputs\|^## Outputs\|^## References" .claude/agents/producers/researcher/AGENT.md` ≥ 3
  </acceptance_criteria>
  <done>
researcher v1.2 — Core 3/5 완료.
  </done>
</task>

<task type="auto">
  <name>Task 3: director + scripter + metadata-seo AGENT.md v1.2 migration (Core 4-5/5 + 파이프라인 중축)</name>
  <files>.claude/agents/producers/director/AGENT.md, .claude/agents/producers/scripter/AGENT.md, .claude/agents/producers/metadata-seo/AGENT.md</files>
  <read_first>
    - .claude/agents/producers/director/AGENT.md (현 상태)
    - .claude/agents/producers/scripter/AGENT.md (현 상태 — 대본 품질 핵심, 가장 긴 prose body 예상)
    - .claude/agents/producers/metadata-seo/AGENT.md (현 상태)
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/templates/producer.md.template
    - wiki/script/NLM_2STEP_TEMPLATE.md (scripter 의존성)
  </read_first>
  <action>
3 에이전트를 동일 절차 v1.2 로 승격. **모든 3 파일의 `<mandatory_reads>` 블록 head 에 '매 호출마다 전수 읽기, 샘플링 금지' 한국어 literal 반드시 포함 (D-A1-03)**.

**director**:
- `<role>`: "연출 producer. researcher 팩트 기반으로 Blueprint JSON (hook / act1 / act2 / climax / resolution + CTA) 생성. CTA 는 channel_bible 의 brand voice 준수."
- `<mandatory_reads>`: FAILURES.md / channel_identity.md + niche bible / gate-dispatcher SKILL
- `<output_format>`: JSON Blueprint schema `{gate: "BLUEPRINT", blueprint: {hook, act1, act2, climax, resolution, cta}, estimated_duration_s}`
- `<skills>`: gate-dispatcher (required), progressive-disclosure (optional)
- `<constraints>`: RUB-06 mirror + maxTurns=3 + 한국어 + T2V 금지 + FAILURES append-only + estimated_duration_s ≤ 60s (Shorts)

**scripter**:
- `<role>`: "대본 producer. director Blueprint 를 받아 씬별 내레이션 대본 + 자막 타이밍 + 톤 지시 생성. 한국어 존댓말 baseline."
- `<mandatory_reads>`: FAILURES.md / channel_identity.md + niche bible / gate-dispatcher + drift-detection SKILL (NLM_2STEP_TEMPLATE drift 감지)
- `<output_format>`: JSON `{gate: "SCRIPT", scenes[], subtitles[], tone_instructions}` + 금지 패턴
- `<skills>`: gate-dispatcher (required), progressive-disclosure (optional), drift-detection (optional)
- `<constraints>`: RUB-06 mirror + maxTurns=5 (품질 중요) + 한국어 존댓말 + T2V 금지 + FAILURES append-only + scenes[].duration_s 합계 ≤ 60s

**metadata-seo**:
- `<role>`: "메타데이터 SEO producer. assembler 완성 영상 기반으로 title + description + tags + thumbnail_text SEO 최적화. YouTube 알고리즘 ranking_factors 참조."
- `<mandatory_reads>`: FAILURES.md / channel_identity.md / gate-dispatcher + `@wiki/shorts/algorithm/ranking_factors.md`
- `<output_format>`: JSON `{gate: "METADATA", title, description, tags[], thumbnail_text}`
- `<skills>`: gate-dispatcher (required), progressive-disclosure (optional)
- `<constraints>`: RUB-06 mirror + maxTurns=3 + 한국어 (title/description) + T2V 금지 + FAILURES append-only + title ≤ 60 chars + tags.length ≤ 15

각 파일 body prose 보존 — head 5 블록만 재구성. 완료 후 3 파일 verify_agent_md_schema.py 개별 exit 0 확인.

**Commit (Issue #5)**: `feat(12-02): director + scripter + metadata-seo AGENT.md v1.2 migration [plan-02]` (단일 commit, 3 파일 동시).
  </action>
  <verify>
    <automated>for f in director scripter metadata-seo; do py -3.11 scripts/validate/verify_agent_md_schema.py .claude/agents/producers/$f/AGENT.md || exit 1; done; echo ALL_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - 3 파일 모두 verify_agent_md_schema.py exit 0
    - 3 파일 모두 `version: 1.2` frontmatter
    - 3 파일 모두 `inspector_prompt 읽기 금지 (RUB-06 GAN 분리 mirror)` 포함
    - **3 파일 모두 `grep -c "매 호출마다 전수 읽기, 샘플링 금지"` ≥ 1 (D-A1-03 literal — Issue #2 fix, per target 전수 검증):**
      - `grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/producers/director/AGENT.md` ≥ 1
      - `grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/producers/scripter/AGENT.md` ≥ 1
      - `grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/producers/metadata-seo/AGENT.md` ≥ 1
    - scripter 에 `drift-detection (optional)` skill 언급 (NLM drift 감지 차별화)
    - metadata-seo 에 `@wiki/shorts/algorithm/ranking_factors.md` 참조
    - 3 파일 모두 body prose 보존 (`## Purpose` literal ≥ 1 per file)
  </acceptance_criteria>
  <done>
Core 5/5 완료 — trend-collector + niche-classifier + researcher + director + scripter + metadata-seo (총 6 producer, Core 완결).
  </done>
</task>

<task type="auto">
  <name>Task 4: scene-planner + shot-planner + script-polisher AGENT.md v1.2 migration (Support 1-3/7)</name>
  <files>.claude/agents/producers/scene-planner/AGENT.md, .claude/agents/producers/shot-planner/AGENT.md, .claude/agents/producers/script-polisher/AGENT.md</files>
  <read_first>
    - .claude/agents/producers/scene-planner/AGENT.md (현 상태)
    - .claude/agents/producers/shot-planner/AGENT.md (현 상태)
    - .claude/agents/producers/script-polisher/AGENT.md (현 상태)
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/templates/producer.md.template
    - wiki/script/QUALITY_PATTERNS.md (script-polisher 의존성)
  </read_first>
  <action>
3 에이전트 v1.2 승격. 동일 절차 — 에이전트별 customize. **모든 3 파일의 `<mandatory_reads>` 블록 head 에 '매 호출마다 전수 읽기, 샘플링 금지' 한국어 literal 반드시 포함 (D-A1-03)**.

**scene-planner**:
- `<role>`: "씬 기획 producer. director Blueprint 를 씬 단위로 분해 — 각 씬에 duration / 배경 / 핵심 action / 감정 톤 할당. shot-planner 의 입력."
- `<output_format>`: JSON `{gate: "SCENE_PLAN", scenes[{id, duration_s, setting, action, emotion_tag}]}`
- `<skills>`: gate-dispatcher (required)
- `<constraints>`: RUB-06 + maxTurns=3 + 한국어 + T2V 금지 + scenes 합계 ≤ 60s

**shot-planner**:
- `<role>`: "샷 기획 producer. scene-planner 씬 단위를 샷 단위로 세분 — 각 샷에 camera_angle / lens / movement / i2v_hint (I2V only, D-13) 지시."
- `<output_format>`: JSON `{gate: "SHOT_PLAN", shots[{scene_id, shot_id, camera_angle, lens_mm, movement, i2v_hint}]}`
- `<skills>`: gate-dispatcher (required), drift-detection (optional)
- `<constraints>`: RUB-06 + maxTurns=3 + 한국어 + **T2V 경로 절대 금지 (I2V only, D-13) — i2v_hint 필드만 허용, t2v_hint/text_to_video 금지** + FAILURES append-only

**script-polisher**:
- `<role>`: "대본 교정 producer. scripter 1차 대본 을 톤/가독성/존댓말 검수 + QUALITY_PATTERNS.md 패턴 적용 + polish pass."
- `<mandatory_reads>` 에 `wiki/script/QUALITY_PATTERNS.md` 추가
- `<output_format>`: JSON `{gate: "POLISH", polished_scenes[], diff_summary}`
- `<skills>`: gate-dispatcher (required), drift-detection (optional)
- `<constraints>`: RUB-06 + maxTurns=3 + 한국어 존댓말 + T2V 금지 + FAILURES append-only

각 파일 body 보존. 3 파일 verify 개별 exit 0.

**Commit (Issue #5)**: `feat(12-02): scene-planner + shot-planner + script-polisher AGENT.md v1.2 migration [plan-02]`.
  </action>
  <verify>
    <automated>for f in scene-planner shot-planner script-polisher; do py -3.11 scripts/validate/verify_agent_md_schema.py .claude/agents/producers/$f/AGENT.md || exit 1; done; echo ALL_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - 3 파일 모두 verify_agent_md_schema.py exit 0
    - shot-planner 에 `i2v_hint` literal ≥ 1 AND `t2v_hint` literal 0 (grep count = 0)
    - script-polisher 에 `wiki/script/QUALITY_PATTERNS.md` 참조 ≥ 1
    - 3 파일 모두 `version: 1.2` + body prose 보존
    - **3 파일 모두 `grep -c "매 호출마다 전수 읽기, 샘플링 금지"` ≥ 1 (D-A1-03 literal — Issue #2 fix, per target 전수 검증):**
      - `grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/producers/scene-planner/AGENT.md` ≥ 1
      - `grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/producers/shot-planner/AGENT.md` ≥ 1
      - `grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/producers/script-polisher/AGENT.md` ≥ 1
  </acceptance_criteria>
  <done>
Support 3/7 완료.
  </done>
</task>

<task type="auto">
  <name>Task 5: voice-producer + asset-sourcer + thumbnail-designer AGENT.md v1.2 migration (Support 4-6/7)</name>
  <files>.claude/agents/producers/voice-producer/AGENT.md, .claude/agents/producers/asset-sourcer/AGENT.md, .claude/agents/producers/thumbnail-designer/AGENT.md</files>
  <read_first>
    - .claude/agents/producers/voice-producer/AGENT.md (현 상태)
    - .claude/agents/producers/asset-sourcer/AGENT.md (현 상태 — I2V 경로 핵심)
    - .claude/agents/producers/thumbnail-designer/AGENT.md (현 상태)
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/templates/producer.md.template
    - .claude/memory/project_tts_stack_typecast.md (voice-producer 의존성)
    - .claude/memory/project_video_stack_kling26.md (asset-sourcer 의존성 — Kling 2.6 I2V)
  </read_first>
  <action>
3 에이전트 v1.2 승격. **모든 3 파일의 `<mandatory_reads>` 블록 head 에 '매 호출마다 전수 읽기, 샘플링 금지' 한국어 literal 반드시 포함 (D-A1-03)**.

**voice-producer**:
- `<role>`: "보이스 생성 producer. script-polisher 대본 을 Typecast (primary) + ElevenLabs (fallback) TTS 로 오디오 생성. 감정/톤 지시 반영. AUDIO-01/02/03 담당."
- `<mandatory_reads>` 에 `.claude/memory/project_tts_stack_typecast.md` 추가
- `<output_format>`: JSON `{gate: "VOICE", audio_segments[{scene_id, path, duration_s, provider}]}`
- `<skills>`: gate-dispatcher (required)
- `<constraints>`: RUB-06 + maxTurns=3 + 한국어 존댓말 (TTS input) + T2V 금지 + FAILURES append-only + Typecast rate limit 준수

**asset-sourcer**:
- `<role>`: "I2V 영상 생성 producer. shot-planner i2v_hint 기반으로 Kling 2.6 (primary) + Runway (fallback) I2V 영상 생성. Anchor Frame 강제 (T1 정체성 유지)."
- `<mandatory_reads>` 에 `.claude/memory/project_video_stack_kling26.md` + `.claude/memory/feedback_i2v_prompt_principles.md` + `wiki/render/i2v_prompt_engineering.md` 추가
- `<output_format>`: JSON `{gate: "ASSETS", i2v_clips[{shot_id, path, duration_s, anchor_frame_path, provider}]}`
- `<skills>`: gate-dispatcher (required), drift-detection (optional)
- `<constraints>`:
  - RUB-06 mirror
  - **T2V 경로 절대 금지 (I2V only, D-13) — Anchor Frame 강제, text_to_video/t2v 키워드 등장 시 pre_tool_use.py 차단**
  - maxTurns=5 (I2V 품질 중요)
  - 한국어 (i2v_hint 는 원어 혼용 가능)
  - FAILURES append-only
  - Kling 2.6 API rate limit + cost budget 준수

**thumbnail-designer**:
- `<role>`: "썸네일 기획 producer. 영상 주제 + hook 를 기반으로 썸네일 텍스트 + 구도 + 색감 + 얼굴 배치 지시. ins-thumbnail-hook 검증 대상."
- `<output_format>`: JSON `{gate: "THUMBNAIL", thumbnail_spec: {text, composition, color_palette, face_position}}`
- `<skills>`: gate-dispatcher (required)
- `<constraints>`: RUB-06 + maxTurns=3 + 한국어 (text) + T2V 금지 + FAILURES append-only + text ≤ 20 chars (Shorts 썸네일 가독성)

3 파일 body 보존. verify 개별 exit 0.

**Commit (Issue #5)**: `feat(12-02): voice-producer + asset-sourcer + thumbnail-designer AGENT.md v1.2 migration [plan-02]`.
  </action>
  <verify>
    <automated>for f in voice-producer asset-sourcer thumbnail-designer; do py -3.11 scripts/validate/verify_agent_md_schema.py .claude/agents/producers/$f/AGENT.md || exit 1; done; echo ALL_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - 3 파일 모두 verify_agent_md_schema.py exit 0
    - asset-sourcer 에 `Anchor Frame 강제` literal ≥ 1 AND `text_to_video`/`t2v_hint` literal 0 (금지 키워드 grep count = 0)
    - voice-producer 에 `project_tts_stack_typecast.md` 참조 ≥ 1
    - asset-sourcer 에 `project_video_stack_kling26.md` 참조 ≥ 1
    - 3 파일 모두 `version: 1.2` + body prose 보존
    - **3 파일 모두 `grep -c "매 호출마다 전수 읽기, 샘플링 금지"` ≥ 1 (D-A1-03 literal — Issue #2 fix, per target 전수 검증):**
      - `grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/producers/voice-producer/AGENT.md` ≥ 1
      - `grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/producers/asset-sourcer/AGENT.md` ≥ 1
      - `grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/producers/thumbnail-designer/AGENT.md` ≥ 1
  </acceptance_criteria>
  <done>
Support 6/7 완료.
  </done>
</task>

<task type="auto">
  <name>Task 6: assembler + publisher AGENT.md v1.2 migration (Support 7/7 + 파이프라인 boundary)</name>
  <files>.claude/agents/producers/assembler/AGENT.md, .claude/agents/producers/publisher/AGENT.md</files>
  <read_first>
    - .claude/agents/producers/assembler/AGENT.md (현 상태)
    - .claude/agents/producers/publisher/AGENT.md (현 상태 — YouTube API upload boundary)
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/templates/producer.md.template
    - scripts/publisher/youtube_uploader.py (publisher 실 구현 참조 — YouTube Data API v3)
  </read_first>
  <action>
2 에이전트 v1.2 승격. **모든 2 파일의 `<mandatory_reads>` 블록 head 에 '매 호출마다 전수 읽기, 샘플링 금지' 한국어 literal 반드시 포함 (D-A1-03)**.

**assembler**:
- `<role>`: "영상 조립 producer. voice-producer 오디오 + asset-sourcer I2V clip + thumbnail 을 Shotstack API 로 assemble. VoiceFirstTimeline (ORCH-10) 계약 준수 — 오디오 길이 기준 씬 타이밍."
- `<output_format>`: JSON `{gate: "ASSEMBLY", rendered_video_path, duration_s, shotstack_job_id}`
- `<skills>`: gate-dispatcher (required)
- `<constraints>`: RUB-06 + maxTurns=3 + T2V 금지 + FAILURES append-only + duration_s ≤ 60s (Shorts 제약)

**publisher**:
- `<role>`: "YouTube 업로드 producer. assembler 완성 영상 + metadata-seo 메타 를 YouTube Data API v3 로 업로드. AF-8 Selenium 금지, 공식 API 만. 48h 이상 간격 준수 (AF-1 일일 금지)."
- `<mandatory_reads>` 에 추가: `wiki/ypp/MOC.md` (YPP 정책 + YouTube Data API v3 업로드 계약)
- `<output_format>`: JSON `{gate: "UPLOAD", youtube_video_id, published_at, privacy_status}`
- `<skills>`: gate-dispatcher (required)
- `<constraints>`:
  - RUB-06 mirror
  - maxTurns=2 (업로드는 단순, 재시도는 invoker 책임)
  - **Selenium / playwright / webdriver 절대 금지 (AF-8) — 공식 YouTube Data API v3 만. ANCHOR C 차단 대상**
  - 48h+ 랜덤 간격 준수 (AF-1 일일 업로드 차단 = Inauthentic Content strike 회피)
  - containsSyntheticMedia=True 하드코딩 (AF-canonical, PUB-01)
  - 한국 피크 시간 (KST 20-23시 평일 / 12-15시 주말) 업로드
  - T2V 금지 + FAILURES append-only

2 파일 body 보존. verify 개별 exit 0.

**Commit (Issue #5)**: `feat(12-02): assembler + publisher AGENT.md v1.2 migration [plan-02]`.
  </action>
  <verify>
    <automated>for f in assembler publisher; do py -3.11 scripts/validate/verify_agent_md_schema.py .claude/agents/producers/$f/AGENT.md || exit 1; done; echo ALL_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - 2 파일 모두 verify_agent_md_schema.py exit 0
    - publisher 에 `Selenium / playwright / webdriver 절대 금지 (AF-8)` literal ≥ 1
    - publisher 에 `containsSyntheticMedia=True` literal ≥ 1
    - publisher 에 `48h+` 관련 AF-1 문구 ≥ 1
    - 2 파일 모두 `version: 1.2` + body prose 보존
    - **2 파일 모두 `grep -c "매 호출마다 전수 읽기, 샘플링 금지"` ≥ 1 (D-A1-03 literal — Issue #2 fix, per target 전수 검증):**
      - `grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/producers/assembler/AGENT.md` ≥ 1
      - `grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/producers/publisher/AGENT.md` ≥ 1
  </acceptance_criteria>
  <done>
Support 7/7 완료. 12 producer v1.2 migration 전부 완결 (Plan 01 trend-collector 포함 13/13).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: test_agent_md_schema.py 실 assertion 채우기 + Phase 4 regression</name>
  <files>tests/phase12/test_agent_md_schema.py</files>
  <behavior>
    - Test: `test_all_30_agents_have_5_blocks` — `_collect_all_agent_mds()` 로 수집한 모든 파일 에서 `verify_file()` ok=True (현 시점 13 producer + 0 inspector = 13 GREEN, Plan 03 완료 시 30 GREEN)
    - Test: `test_excluded_agents_not_scanned` — `.claude/agents/producers/harvest-importer/AGENT.md` 와 `.claude/agents/supervisor/` 가 collect 결과에 부재
    - Test: 13 producer 개별 파일 별 자동 generate parametrized test — 각 producer AGENT.md 가 5 블록 + `version: 1.2` + `inspector_prompt 읽기 금지` 포함
  </behavior>
  <read_first>
    - tests/phase12/test_agent_md_schema.py (Plan 01 Task 2 red stub)
    - scripts/validate/verify_agent_md_schema.py (Plan 01 Task 3 — _collect_all_agent_mds + verify_file 재사용)
    - .claude/agents/producers/trend-collector/AGENT.md (v1.2 샘플)
  </read_first>
  <action>
`tests/phase12/test_agent_md_schema.py` 의 skip decorator 제거 + 실 assertion 삽입:

```python
"""AGENT-STD-01 — 30 AGENT.md × 5 XML block schema compliance."""

from __future__ import annotations
import sys
from pathlib import Path
import pytest

REPO_ROOT = Path(__file__).resolve().parents[2]
sys.path.insert(0, str(REPO_ROOT / "scripts" / "validate"))

from verify_agent_md_schema import (  # noqa: E402
    _collect_all_agent_mds,
    verify_file,
    EXCLUDED_AGENTS,
)


def test_all_scoped_agents_have_5_blocks():
    """Plan 02 완료 시 13 producer + 0 inspector GREEN.
    Plan 03 완료 시 30 GREEN."""
    targets = _collect_all_agent_mds()
    assert len(targets) >= 13, f"expected >= 13 producer after Plan 02, got {len(targets)}"
    violations = []
    for path in targets:
        ok, missing = verify_file(path)
        if not ok:
            rel = path.relative_to(REPO_ROOT) if REPO_ROOT in path.parents else path
            violations.append((str(rel), missing))
    if violations:
        pytest.fail(
            f"{len(violations)}/{len(targets)} AGENT.md violate 5-block schema: "
            + ", ".join(f"{r}: {m}" for r, m in violations[:5])
        )


def test_excluded_agents_not_scanned():
    """harvest-importer + shorts-supervisor 는 Phase 12 scope 밖."""
    targets = _collect_all_agent_mds()
    assert "harvest-importer" in EXCLUDED_AGENTS
    assert "shorts-supervisor" in EXCLUDED_AGENTS
    for p in targets:
        parts = {part for part in p.parts}
        assert "harvest-importer" not in parts, f"harvest-importer leaked into scan: {p}"
        assert "shorts-supervisor" not in parts, f"shorts-supervisor leaked into scan: {p}"


PRODUCERS_V1_2 = [
    "trend-collector", "niche-classifier", "researcher", "director", "scripter",
    "metadata-seo", "scene-planner", "shot-planner", "script-polisher",
    "voice-producer", "asset-sourcer", "thumbnail-designer", "assembler", "publisher",
]


@pytest.mark.parametrize("producer", PRODUCERS_V1_2)
def test_producer_has_version_1_2_and_rub_06(producer):
    p = REPO_ROOT / ".claude" / "agents" / "producers" / producer / "AGENT.md"
    assert p.exists(), f"{producer}/AGENT.md missing"
    text = p.read_text(encoding="utf-8")
    assert "version: 1.2" in text, f"{producer} frontmatter not bumped to 1.2"
    assert "inspector_prompt 읽기 금지 (RUB-06 GAN 분리 mirror)" in text, \
        f"{producer} missing RUB-06 mirror in <constraints>"
    assert "매 호출마다 전수 읽기, 샘플링 금지" in text, \
        f"{producer} missing 샘플링 금지 literal in <mandatory_reads>"
```

완료 후 pytest 실행 — 13 producer 개별 test 가 모두 GREEN + 2 collective test 도 GREEN 이어야 함.

**Commit (Issue #5)**: `test(12-02): populate test_agent_md_schema.py 실 assertion [plan-02]`.
  </action>
  <verify>
    <automated>py -3.11 -m pytest tests/phase12/test_agent_md_schema.py -v 2>&1 | tail -20; py -3.11 -m pytest tests/phase04/ -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `py -3.11 -m pytest tests/phase12/test_agent_md_schema.py -v` exits 0 with ≥ 16 passed (2 collective + 14 parametrized with 14 producers)
    - No tests marked `SKIPPED` in this file (all red stubs removed)
    - `py -3.11 -m pytest tests/phase04/ -q` exits 0 (244 passed — Phase 4 regression preserved)
    - `py -3.11 scripts/validate/verify_agent_md_schema.py --all` exit 1 (17 inspector still FAIL, 13 producer PASS — expected pre-Plan-03)
  </acceptance_criteria>
  <done>
Wave 2 Producer 13/13 migration + pytest 실 assertion + Phase 4 regression GREEN. Plan 03 이 17 inspector migration 시 동일 test collective 가 자동으로 17 추가 producer 포함 범위 확장.
  </done>
</task>

<task type="auto">
  <name>Task 8: skill_patch_counter 확장 + F-D2-EXCEPTION-02 batch entry (marker-filtered commits) + test_f_d2_exception_batch 실 assertion</name>
  <files>scripts/audit/skill_patch_counter.py, tests/phase12/test_f_d2_exception_batch.py, .claude/failures/FAILURES.md</files>
  <read_first>
    - scripts/audit/skill_patch_counter.py lines 190-265 (`_existing_violation_hashes` + `append_failures` + AUDIT-05 idempotency — 재사용 대상)
    - tests/phase10/test_skill_patch_counter.py (기존 12 tests — idempotency 패턴 참조)
    - tests/phase12/test_f_d2_exception_batch.py (Plan 01 Task 2 red stub)
    - .claude/failures/FAILURES.md (현 58줄 — F-D2-EXCEPTION-02 append 대상, 500줄 cap 하 안전)
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-RESEARCH.md §Example 2 (append_f_d2_exception_02 code sample)
  </read_first>
  <action>
3 단계 작업:

### 1. scripts/audit/skill_patch_counter.py 확장

파일 끝에 신규 function 추가 (기존 function 건드리지 말 것):

```python
def append_f_d2_exception_02(
    wave_label: str,
    commits: list[dict],
    scope_files: list[str],
    authorized_by: str,
    repo_root: Path,
    now: datetime,
) -> bool:
    """Emit single F-D2-EXCEPTION-02 batch entry — idempotent.

    Returns True if new entry written, False if already exists (idempotent no-op).
    AUDIT-05 idempotency re-used — same wave_label + same commits → no duplicate.
    """
    failures = repo_root / ".claude" / "failures" / "FAILURES.md"
    if not failures.exists():
        return False
    existing = failures.read_text(encoding="utf-8")
    marker = f"### F-D2-EXCEPTION-02 — Wave {wave_label}"
    if marker in existing:
        return False  # idempotent
    body: list[str] = [
        "",
        "---",
        "",
        f"{marker} (Phase 12 agent standardization directive-authorized batch, {now.date().isoformat()})",
        "",
        "- **Tier**: B",
        f"- **Authorized by**: {authorized_by}",
        f"- **Scope**: {len(scope_files)} files",
        f"- **Commits**: {', '.join(c.get('hash','')[:7] for c in commits)}",
        "- **Trigger**: AGENT-STD-01 + AGENT-STD-02 + FAIL-PROTO-02",
        "- **검증**: phase12 regression GREEN + verify_agent_md_schema.py exit 0",
        "- **상태**: resolved",
        "- **관련**: F-D2-EXCEPTION-01 (prototype, Phase 11 trend-collector JSON)",
        "",
    ]
    with failures.open("a", encoding="utf-8") as f:  # Hook-bypass via direct open
        f.write("\n".join(body) + "\n")
    return True
```

### 2. tests/phase12/test_f_d2_exception_batch.py 실 assertion

red stub 제거 + 실제 test:

```python
"""FAIL-PROTO-02 — 30+ 파일 batch commit 단일 F-D2-EXCEPTION-02 entry."""

from __future__ import annotations
import sys
from datetime import datetime
from pathlib import Path
from zoneinfo import ZoneInfo

import pytest

REPO_ROOT = Path(__file__).resolve().parents[2]
sys.path.insert(0, str(REPO_ROOT))

from scripts.audit.skill_patch_counter import append_f_d2_exception_02  # noqa: E402

KST = ZoneInfo("Asia/Seoul")


def _seed_failures(tmp_root: Path) -> Path:
    """Seed tmp FAILURES.md head identical to real schema."""
    failures_dir = tmp_root / ".claude" / "failures"
    failures_dir.mkdir(parents=True, exist_ok=True)
    f = failures_dir / "FAILURES.md"
    f.write_text(
        "# FAILURES.md — Append-Only Reservoir\n\n"
        "> **D-11:** Append-only.\n\n"
        "## Entries\n\n(none yet)\n",
        encoding="utf-8",
    )
    return f


def test_batch_commit_single_entry(tmp_path):
    f = _seed_failures(tmp_path)
    now = datetime.now(KST)
    commits = [{"hash": f"abc{i:04d}", "date": "2026-04-21"} for i in range(13)]
    scope_files = [f".claude/agents/producers/p{i}/AGENT.md" for i in range(13)]
    wrote = append_f_d2_exception_02(
        wave_label="2",
        commits=commits,
        scope_files=scope_files,
        authorized_by="대표님 session #29",
        repo_root=tmp_path,
        now=now,
    )
    assert wrote is True
    text = f.read_text(encoding="utf-8")
    assert "F-D2-EXCEPTION-02 — Wave 2" in text
    # Exactly 1 Wave 2 entry
    assert text.count("F-D2-EXCEPTION-02 — Wave 2") == 1


def test_batch_idempotent_replay(tmp_path):
    f = _seed_failures(tmp_path)
    now = datetime.now(KST)
    commits = [{"hash": "abc1234"}, {"hash": "def5678"}]
    # First call writes
    w1 = append_f_d2_exception_02("2", commits, ["f1"], "대표님", tmp_path, now)
    # Second call must be idempotent no-op
    w2 = append_f_d2_exception_02("2", commits, ["f1"], "대표님", tmp_path, now)
    assert w1 is True
    assert w2 is False
    text = f.read_text(encoding="utf-8")
    assert text.count("F-D2-EXCEPTION-02 — Wave 2") == 1
```

### 3. 실 FAILURES.md 에 F-D2-EXCEPTION-02 Wave 2 entry 기록 (marker-filtered git query — Issue #5 fix)

Plan 02 실행 시 Task 1~7 commits 완료 후 Task 8 에서 `append_f_d2_exception_02` 를 호출. Commit 조회는 `-n 8` 같은 rev-count 기반 fragile 쿼리 대신 `--grep='\[plan-02\]'` marker filter 사용 (pre-existing commit 혼입 차단 + 미래 per-task commit 분할 변동 무관):

```bash
py -3.11 -c "
from datetime import datetime
from zoneinfo import ZoneInfo
from pathlib import Path
import subprocess
import sys
sys.path.insert(0, '.')
from scripts.audit.skill_patch_counter import append_f_d2_exception_02

# Issue #5 fix — marker-filtered git query (robust to commit granularity drift).
# Every Task 1~7 commit subject includes literal '[plan-02]' marker.
RAW_COMMITS = subprocess.check_output(
    ['git', 'log', '--grep=\\\\[plan-02\\\\]', '--format=%h %s']
).decode().strip().split('\n')
commits = []
for line in RAW_COMMITS:
    if not line.strip():
        continue
    parts = line.split(' ', 1)
    commits.append({'hash': parts[0], 'subject': parts[1] if len(parts) > 1 else ''})

# Files touched across those commits (name-only, de-duplicated, AGENT.md only).
RAW_FILES = subprocess.check_output(
    ['git', 'log', '--grep=\\\\[plan-02\\\\]', '--name-only', '--pretty=format:']
).decode().strip().split('\n')
scope_files = sorted({f for f in RAW_FILES if f.strip() and 'AGENT.md' in f})

assert len(commits) >= 6, f'expected >= 6 marker commits (Tasks 1-7), got {len(commits)}'
assert len(scope_files) >= 12, f'expected >= 12 AGENT.md in scope, got {len(scope_files)}'

append_f_d2_exception_02(
    wave_label='2',
    commits=commits,
    scope_files=scope_files,
    authorized_by='대표님 session #29 (Phase 12 발의 승인)',
    repo_root=Path('.'),
    now=datetime.now(ZoneInfo('Asia/Seoul')),
)
print(f'F-D2-EXCEPTION-02 Wave 2 entry written — {len(commits)} commits, {len(scope_files)} files')
"
```

완료 후 `grep -c "F-D2-EXCEPTION-02" .claude/failures/FAILURES.md` ≥ 1 확인.

**Commit (Issue #5)**: `chore(12-02): skill_patch_counter F-D2-EXCEPTION-02 Wave 2 batch entry [plan-02]`.
  </action>
  <verify>
    <automated>py -3.11 -m pytest tests/phase12/test_f_d2_exception_batch.py -v 2>&1 | tail -10; grep -c "F-D2-EXCEPTION-02" .claude/failures/FAILURES.md</automated>
  </verify>
  <acceptance_criteria>
    - `py -3.11 -m pytest tests/phase12/test_f_d2_exception_batch.py -v` exits 0, 2 passed (test_batch_commit_single_entry + test_batch_idempotent_replay)
    - `scripts/audit/skill_patch_counter.py` contains function `append_f_d2_exception_02` (grep literal ≥ 1)
    - `.claude/failures/FAILURES.md` contains literal `F-D2-EXCEPTION-02 — Wave 2` ≥ 1
    - `wc -l .claude/failures/FAILURES.md` result ≤ 500 (500줄 cap 하 안전 — FAIL-PROTO-01 호환)
    - `py -3.11 -m pytest tests/phase10/test_skill_patch_counter.py -q` exits 0 (12 passed — Phase 10 regression 무회귀)
    - **Issue #5 marker query 검증**: `git log --grep='\[plan-02\]' --oneline | wc -l` ≥ 6 (Tasks 1-7 commits all carry marker)
    - **Issue #5 marker query 검증**: `git log --grep='\[plan-02\]' --name-only --pretty=format: | grep -c "AGENT.md"` ≥ 12 (12 Producer AGENT.md 전수 capture)
  </acceptance_criteria>
  <done>
skill_patch_counter 확장 + FAIL-PROTO-02 batch entry 기록 + idempotency test GREEN + marker-filtered git query (Issue #5 fix). 12+ AGENT.md patch 가 단일 F-D2-EXCEPTION-02 entry 로 기록됨 (AUDIT-05 idempotency 재활용).
  </done>
</task>

</tasks>

<verification>
전체 Plan 02 완료 후:

```bash
py -3.11 scripts/validate/verify_agent_md_schema.py --all           # exit 1 (17 inspector FAIL, 13 producer PASS)
py -3.11 -m pytest tests/phase12/ -v                                 # ≥ 18 passed (Plan 02 contribution)
py -3.11 -m pytest tests/phase04/ -q                                 # exit 0 (244 passed)
py -3.11 -m pytest tests/phase10/test_skill_patch_counter.py -q     # exit 0 (12 passed)
py -3.11 -m pytest tests/phase11/ -q                                 # exit 0 (36 passed)
grep -c "F-D2-EXCEPTION-02" .claude/failures/FAILURES.md            # ≥ 1

# Issue #5 marker convention — robust commit granularity check:
git log --grep='\[plan-02\]' --oneline | wc -l                       # ≥ 6 (Tasks 1-7)
git log --grep='\[plan-02\]' --name-only --pretty=format: | grep -c "AGENT.md"  # ≥ 12

# Issue #2 D-A1-03 literal — all 12 target files per-task enforced:
for f in niche-classifier researcher director scripter metadata-seo \
         scene-planner shot-planner script-polisher voice-producer \
         asset-sourcer thumbnail-designer assembler publisher; do
  grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/producers/$f/AGENT.md
done | sort -u                                                       # output only "1" or ">1", no "0"
```
</verification>

<success_criteria>
1. 12 Producer (trend-collector 외) AGENT.md v1.2 승격 — 5 XML 블록 schema + body prose 보존
2. test_agent_md_schema.py 실 assertion — 13 producer GREEN, 17 inspector 미migration 확인
3. test_f_d2_exception_batch.py 2 tests GREEN — AGENT-STD-01 + FAIL-PROTO-02 cross-coverage
4. FAILURES.md F-D2-EXCEPTION-02 Wave 2 entry 기록 — idempotent + marker-filtered commit query (Issue #5 fix)
5. Phase 4 + Phase 10 + Phase 11 regression 전수 GREEN (244 + 12 + 36 = 292 tests)
6. skill_patch_counter.py append_f_d2_exception_02 function 추가 — AUDIT-05 idempotency 재활용
7. **D-A1-03 literal per-task enforced** — Tasks 2-6 각 target file 에 '매 호출마다 전수 읽기, 샘플링 금지' 한국어 literal grep ≥ 1 (Issue #2 fix)
8. **Commit marker convention** — Tasks 1-7 각 commit subject 에 `[plan-02]` marker 포함 → Task 8 의 git query 가 rev-count fragile 쿼리 대체 (Issue #5 fix)
</success_criteria>

<output>
After completion, create `.planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-02-SUMMARY.md`.

대표님께 보고드립니다. 13 Producer AGENT.md v1.2 migration 완결. 파이프라인 생성 체인 전체가 표준 5-block schema + `<output_format>` 금지 패턴 5종 명시 + `<mandatory_reads>` 전수 읽기 soft 강제를 갖추어 Phase 11 F-D2-EXCEPTION-01 재발 차단 인프라가 확보되었습니다.
</output>
