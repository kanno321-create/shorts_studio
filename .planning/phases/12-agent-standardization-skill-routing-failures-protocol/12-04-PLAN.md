---
phase: 12-agent-standardization-skill-routing-failures-protocol
plan: 04
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - .planning/REQUIREMENTS.md
  - wiki/agent_skill_matrix.md
  - scripts/validate/verify_agent_skill_matrix.py
  - tests/phase12/test_skill_matrix_format.py
autonomous: true
requirements:
  - SKILL-ROUTE-01
status: pending
must_haves:
  truths:
    - "REQUIREMENTS.md §383 8-skill 약속이 5-skill + additional 컬럼 으로 정정 — 실측 `.claude/skills/` 5개 dir 기반 (Research §Q1 Option A)"
    - "wiki/agent_skill_matrix.md 가 30 row × (5 skill + additional + description) 컬럼 으로 존재 — 각 cell 은 required / optional / n/a 중 하나"
    - "verify_agent_skill_matrix.py CLI 존재 + `--fail-on-drift` 플래그 — matrix cell 의 `required` ↔ AGENT.md `<skills>` 블록 양방향 reciprocity 검증, drift 시 exit 1"
    - "test_skill_matrix_format.py 3 tests GREEN — matrix_has_30_rows + cell_values_legal + reciprocity_with_agent_md"
    - "matrix + AGENT.md `<skills>` 양방향 무충돌 — 30 agent × 5 skill = 150 cells 모두 모순 없음"
  artifacts:
    - path: "wiki/agent_skill_matrix.md"
      provides: "Agent × Skill SSOT (Single Source of Truth) — AGENT.md `<skills>` 블록 과 양방향 cross-reference 대상"
      contains: "30 × (5+1+1) matrix, required | optional | n/a legend"
    - path: "scripts/validate/verify_agent_skill_matrix.py"
      provides: "SKILL-ROUTE-01 CI enforcement — bidirectional reciprocity check CLI"
      contains: "def verify_reciprocity"
    - path: ".planning/REQUIREMENTS.md"
      provides: "§383 정정 — 8-skill 약속 → 5-skill + additional (실측 기반)"
      contains: "5 공용 skill"
  key_links:
    - from: "scripts/validate/verify_agent_skill_matrix.py"
      to: "wiki/agent_skill_matrix.md + .claude/agents/**/AGENT.md"
      via: "matrix parse + AGENT.md <skills> 블록 파싱 + set 비교"
      pattern: "required.*<skills>"
    - from: "wiki/agent_skill_matrix.md"
      to: "30 AGENT.md <skills> 블록"
      via: "required/optional cell → <skills> block entry, bidirectional"
      pattern: "\\| [a-z-]+ \\|"
---

<objective>
Agent × Skill SSOT 매트릭스 생성 + bidirectional reciprocity verifier + REQUIREMENTS.md §383 drift 정정. RESEARCH.md Open Question Q1 에서 flag 한 "8 skills" 약속 vs "5 skills 실측" 충돌 해소 — Option A 채택 (REQUIREMENTS 정정 + D-2 Lock 보존).

**Purpose**: SKILL-ROUTE-01 의 핵심 산출. 매 AGENT.md 의 `<skills>` 블록 은 이 matrix 와 bidirectional cross-reference 대상 — drift 시 `verify_agent_skill_matrix.py --fail-on-drift` 가 Phase 13+ CI 에서 차단. D-A1-02 (frontmatter SSOT 중복 금지) 준수.

**Output**: REQUIREMENTS §383 정정 + wiki/agent_skill_matrix.md 신규 + verify_agent_skill_matrix.py CLI + test_skill_matrix_format.py 실 assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-CONTEXT.md
@.planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-01-PLAN.md
@CLAUDE.md
</context>

<interfaces>
5 공용 skill 실측 (RESEARCH §Pitfall 2):
1. `progressive-disclosure` — SKILL ≤ 500줄 guard
2. `gate-dispatcher` — 13 GATE dispatch 계약
3. `drift-detection` — 구형-신형 drift 감지
4. `context-compressor` — 긴 context 압축
5. `harness-audit` — 하네스 전수 점검

30 agent row 분류:
- 13 producer (Plan 02 migration 대상)
- 17 inspector (Plan 03 migration 대상)

matrix cell values (D-A3-legend): `required` | `optional` | `n/a`
(forbidden 4단계는 CONTEXT.md deferred — `<constraints>` 블록 개별 명시로 대체)

REQUIREMENTS §383 정정 권고 (RESEARCH §Open Questions Q1 Option A):
- 기존: "30 × 8 매트릭스 (에이전트 × 스킬). Column: progressive-disclosure, drift-detection, gate-dispatcher, context-compressor, harness-audit, notebooklm, korean-naturalness, korean-nat-rules"
- 정정: "30 × 5 공용 skill + 1 additional 컬럼 (optional, agent-specific). Column: 5 공용 (실측) + additional (agent 특수 스킬 freeform)"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: REQUIREMENTS.md §383 drift 정정 (SKILL-ROUTE-01 scope 정정)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 371-405 (§383 원문 확인)
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-RESEARCH.md §Open Questions Q1 + §Pitfall 2 (drift 증거 + Option A 권고)
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-CONTEXT.md §Claude's Discretion (planner 재량 범위)
  </read_first>
  <action>
`.planning/REQUIREMENTS.md` 의 SKILL-ROUTE-01 정의 (§383) 를 정정.

현재 문구 (read 후 확인):
```
- [ ] **SKILL-ROUTE-01**: `wiki/agent_skill_matrix.md` 생성 — 30 × 8 매트릭스 (에이전트 × 스킬). Row: 13 producer + 17 inspector. Column: progressive-disclosure, drift-detection, gate-dispatcher, context-compressor, harness-audit, notebooklm, korean-naturalness, korean-nat-rules. 각 cell: required / optional / n/a. Pytest validation: `scripts/validate/verify_agent_skill_matrix.py` — 매트릭스 entry 와 실제 AGENT.md `<skills>` 블록 cross-reference.
```

정정 대체 (Edit tool 로 정확한 string 교체):
```
- [ ] **SKILL-ROUTE-01**: `wiki/agent_skill_matrix.md` 생성 — 30 × 6 매트릭스 (에이전트 × 5 공용 skill + 1 additional 컬럼). Row: 13 producer + 17 inspector. Column: progressive-disclosure, gate-dispatcher, drift-detection, context-compressor, harness-audit, additional. 각 cell (공용 5): required / optional / n/a. additional 컬럼: agent-specific skill freeform (예: ins-factcheck "notebooklm-query*"). Pytest validation: `scripts/validate/verify_agent_skill_matrix.py` — 매트릭스 entry 와 실제 AGENT.md `<skills>` 블록 bidirectional reciprocity (`--fail-on-drift`). **Note**: 기존 "8 column" 초안 은 2026-04-21 실측 시 `.claude/skills/` 5 dir 만 존재 확인 → RESEARCH §Open Question Q1 Option A 채택 (D-2 Lock 2026-04-20~06-20 기간 신규 SKILL.md 생성 금지).
```

Phase 12 Traceability 표 + Phase 12 Coverage 문구 는 변경 불필요 (SKILL-ROUTE-01 ID 자체는 유지).

**검증**: 정정 후 `grep -c "5 공용 skill" .planning/REQUIREMENTS.md` ≥ 1 AND `grep -c "8 매트릭스" .planning/REQUIREMENTS.md` = 0.
  </action>
  <verify>
    <automated>grep -c "5 공용 skill" .planning/REQUIREMENTS.md; grep -c "30 × 6 매트릭스" .planning/REQUIREMENTS.md; grep -c "30 × 8 매트릭스" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "5 공용 skill" .planning/REQUIREMENTS.md` ≥ 1
    - `grep -c "30 × 6 매트릭스" .planning/REQUIREMENTS.md` = 1
    - `grep -c "30 × 8 매트릭스" .planning/REQUIREMENTS.md` = 0 (구형 문구 제거)
    - `grep -c "korean-nat-rules" .planning/REQUIREMENTS.md` = 0 (실측 부재 skill 제거)
    - `grep -c "additional 컬럼" .planning/REQUIREMENTS.md` ≥ 1
  </acceptance_criteria>
  <done>
REQUIREMENTS §383 정정 완료. 5-skill 실측 + additional 컬럼 spec 확정. D-2 Lock 위반 없이 scope 조정 — 대표님 session #29 Claude's Discretion 범위 행사.
  </done>
</task>

<task type="auto">
  <name>Task 2: wiki/agent_skill_matrix.md 신규 생성 (30 × 6 SSOT)</name>
  <files>wiki/agent_skill_matrix.md</files>
  <read_first>
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-RESEARCH.md §Example 3 (matrix format template)
    - .planning/REQUIREMENTS.md (Task 1 정정본 §383)
    - .claude/skills/progressive-disclosure/SKILL.md (skill 1 — purpose 확인)
    - .claude/skills/gate-dispatcher/SKILL.md (skill 2)
    - .claude/skills/drift-detection/SKILL.md (skill 3)
    - .claude/skills/context-compressor/SKILL.md (skill 4)
    - .claude/skills/harness-audit/SKILL.md (skill 5)
  </read_first>
  <action>
`wiki/agent_skill_matrix.md` 신규 파일 생성:

```markdown
# Agent × Skill Matrix — Phase 12 SKILL-ROUTE-01

**Single Source of Truth (SSOT)** for Agent × Skill routing.
AGENT.md `<skills>` 블록 과 bidirectional cross-reference.
`scripts/validate/verify_agent_skill_matrix.py --fail-on-drift` 가 CI 검증 surface.

## Legend
- `required`: 에이전트가 본 skill 을 반드시 읽고 준수
- `optional`: 에이전트가 본 skill 을 보조적으로 활용 가능
- `n/a`: 본 skill 은 해당 에이전트 업무와 무관

## Matrix (30 agents × 5 공용 skill + 1 additional)

### Producers (13)

| Agent | progressive-disclosure | gate-dispatcher | drift-detection | context-compressor | harness-audit | additional |
|-------|:---:|:---:|:---:|:---:|:---:|------------|
| trend-collector    | optional | required | n/a      | n/a | n/a | — |
| niche-classifier   | optional | required | n/a      | n/a | n/a | — |
| researcher         | optional | required | n/a      | n/a | n/a | — |
| director           | optional | required | n/a      | n/a | n/a | — |
| scripter           | optional | required | optional | n/a | n/a | — |
| script-polisher    | optional | required | optional | n/a | n/a | — |
| metadata-seo       | optional | required | n/a      | n/a | n/a | — |
| scene-planner      | optional | required | n/a      | n/a | n/a | — |
| shot-planner       | optional | required | optional | n/a | n/a | — |
| voice-producer     | optional | required | n/a      | n/a | n/a | — |
| asset-sourcer      | optional | required | optional | n/a | n/a | — |
| thumbnail-designer | optional | required | n/a      | n/a | n/a | — |
| assembler          | optional | required | n/a      | n/a | n/a | — |
| publisher          | optional | required | n/a      | n/a | n/a | — |

**Note**: trend-collector + scripter + asset-sourcer + shot-planner + script-polisher 만 `drift-detection` optional — 다른 producer 는 drift 검출이 책임 범위 외.

### Inspectors (17)

| Agent | progressive-disclosure | gate-dispatcher | drift-detection | context-compressor | harness-audit | additional |
|-------|:---:|:---:|:---:|:---:|:---:|------------|
| ins-schema-integrity    | n/a | required | n/a      | n/a      | n/a | — |
| ins-timing-consistency  | n/a | required | n/a      | n/a      | n/a | — |
| ins-blueprint-compliance| n/a | required | n/a      | n/a      | n/a | — |
| ins-factcheck           | n/a | required | optional | n/a      | n/a | notebooklm-query* |
| ins-narrative-quality   | n/a | required | n/a      | n/a      | n/a | — |
| ins-korean-naturalness  | n/a | required | n/a      | optional | n/a | — |
| ins-thumbnail-hook      | n/a | required | n/a      | n/a      | n/a | — |
| ins-tone-brand          | n/a | required | optional | n/a      | n/a | — |
| ins-readability         | n/a | required | n/a      | n/a      | n/a | — |
| ins-license             | n/a | required | n/a      | n/a      | n/a | — |
| ins-platform-policy     | n/a | required | n/a      | n/a      | n/a | — |
| ins-safety              | n/a | required | n/a      | n/a      | n/a | — |
| ins-audio-quality       | n/a | required | n/a      | n/a      | n/a | — |
| ins-render-integrity    | n/a | required | n/a      | n/a      | n/a | — |
| ins-subtitle-alignment  | n/a | required | n/a      | n/a      | n/a | — |
| ins-mosaic              | n/a | required | n/a      | n/a      | n/a | — |
| ins-gore                | n/a | required | n/a      | n/a      | n/a | — |

**Note**: `additional` 컬럼 의 `*` 접미사 는 D-2 Lock 기간 (2026-04-20 ~ 2026-06-20) 중 **신규 SKILL.md 생성 금지** 원칙 하 agent-specific 참조 (Phase 13+ 에서 실제 SKILL 생성 고려 시 drop).

## Reciprocity Contract (SKILL-ROUTE-01)

- **matrix cell = `required` → AGENT.md `<skills>` 에 "{skill} (required)" literal 존재** (bidirectional)
- **matrix cell = `optional` → AGENT.md `<skills>` 에 "{skill} (optional)" literal 존재** (bidirectional)
- **matrix cell = `n/a` → AGENT.md `<skills>` 에 해당 skill name 부재** (one-way)
- Inspector 전원 `progressive-disclosure` = n/a (Inspector 는 SKILL 작성자 아님 — 준수자)
- harness-audit 전원 n/a (agent 직접 호출 대상 아님 — 오케스트레이터 전용)

## Phase 12 Traceability

| REQ-ID | Implementation |
|--------|---------------|
| SKILL-ROUTE-01 | 본 파일 (SSOT) + `scripts/validate/verify_agent_skill_matrix.py` (CI) |

## Change History

- 2026-04-21: Phase 12 Plan 04 — 초기 생성 (5 공용 skill 실측 + additional 컬럼).
  REQUIREMENTS.md §383 "8 skill" 초안 은 Option A 로 정정 (RESEARCH.md §Open Question Q1).
```

파일 byte size 확인: 3000~5000 bytes 예상. Windows cp949 guard 문제 없음 (UTF-8 markdown).
  </action>
  <verify>
    <automated>py -3.11 -c "from pathlib import Path; t=Path('wiki/agent_skill_matrix.md').read_text(encoding='utf-8'); assert t.count('| trend-collector') >= 1; assert t.count('| ins-gore') >= 1; assert t.count('| required |') >= 30; import re; producer_rows=len(re.findall(r'^\| [a-z][a-z-]+ +\|', t, re.MULTILINE)); assert producer_rows >= 30, f'rows={producer_rows}'; print(f'matrix rows: {producer_rows}')"</automated>
  </verify>
  <acceptance_criteria>
    - File `wiki/agent_skill_matrix.md` exists (size 3000-6000 bytes)
    - `grep -c "^| trend-collector" wiki/agent_skill_matrix.md` = 1
    - `grep -c "^| ins-gore" wiki/agent_skill_matrix.md` = 1
    - 30 agent rows total (13 producer + 17 inspector) — regex count ≥ 30
    - Each row has exactly 7 columns (agent + 5 skill + additional)
    - Legend section contains `required` / `optional` / `n/a` (all 3 literals)
    - `grep -c "notebooklm-query\*" wiki/agent_skill_matrix.md` = 1 (ins-factcheck additional)
  </acceptance_criteria>
  <done>
wiki/agent_skill_matrix.md SSOT 확보. 30 × 6 matrix 완비. `required` cell ≥ 30 (각 에이전트 최소 1 required skill = gate-dispatcher).
  </done>
</task>

<task type="auto">
  <name>Task 3: verify_agent_skill_matrix.py CLI — bidirectional reciprocity check</name>
  <files>scripts/validate/verify_agent_skill_matrix.py</files>
  <read_first>
    - wiki/agent_skill_matrix.md (Task 2 산출)
    - .claude/agents/producers/trend-collector/AGENT.md (`<skills>` 블록 파싱 대상 예시 — Plan 01 Task 4 v1.2)
    - scripts/validate/verify_agent_md_schema.py (Plan 01 Task 3 — stdlib-only 패턴 참조)
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-RESEARCH.md §Example 3
  </read_first>
  <action>
`scripts/validate/verify_agent_skill_matrix.py` 신규 파일 생성. Python 3.11 stdlib-only (argparse, re, pathlib, sys).

```python
#!/usr/bin/env python3
"""verify_agent_skill_matrix.py — SKILL-ROUTE-01 bidirectional reciprocity check.

Parses wiki/agent_skill_matrix.md + all AGENT.md `<skills>` blocks.
For each (agent, skill) pair:
  - matrix cell = "required" ↔ AGENT.md <skills> contains "{skill} (required)"
  - matrix cell = "optional" ↔ AGENT.md <skills> contains "{skill} (optional)"
  - matrix cell = "n/a" ↔ AGENT.md <skills> does NOT contain skill name

Exits 0 if all 30 × 5 = 150 cells reciprocate. Exits 1 with stderr report on drift.

D-A1-02 / SKILL-ROUTE-01.
"""

from __future__ import annotations
import argparse
import re
import sys
from pathlib import Path

try:
    sys.stdout.reconfigure(encoding="utf-8", errors="replace")
    sys.stderr.reconfigure(encoding="utf-8", errors="replace")
except Exception:
    pass

REPO_ROOT = Path(__file__).resolve().parents[2]
MATRIX_PATH = REPO_ROOT / "wiki" / "agent_skill_matrix.md"

COMMON_SKILLS = [
    "progressive-disclosure",
    "gate-dispatcher",
    "drift-detection",
    "context-compressor",
    "harness-audit",
]

PRODUCERS = [
    "trend-collector", "niche-classifier", "researcher", "director", "scripter",
    "script-polisher", "metadata-seo", "scene-planner", "shot-planner",
    "voice-producer", "asset-sourcer", "thumbnail-designer", "assembler", "publisher",
]

INSPECTORS = [
    ("structural", "ins-schema-integrity"),
    ("structural", "ins-timing-consistency"),
    ("structural", "ins-blueprint-compliance"),
    ("content", "ins-factcheck"),
    ("content", "ins-narrative-quality"),
    ("content", "ins-korean-naturalness"),
    ("style", "ins-thumbnail-hook"),
    ("style", "ins-tone-brand"),
    ("style", "ins-readability"),
    ("compliance", "ins-license"),
    ("compliance", "ins-platform-policy"),
    ("compliance", "ins-safety"),
    ("technical", "ins-audio-quality"),
    ("technical", "ins-render-integrity"),
    ("technical", "ins-subtitle-alignment"),
    ("media", "ins-mosaic"),
    ("media", "ins-gore"),
]


def parse_matrix() -> dict[str, dict[str, str]]:
    """Parse wiki/agent_skill_matrix.md → {agent: {skill: required|optional|n/a}}."""
    text = MATRIX_PATH.read_text(encoding="utf-8")
    result: dict[str, dict[str, str]] = {}
    # Matrix row regex: | agent-name | v1 | v2 | v3 | v4 | v5 | additional |
    row_re = re.compile(
        r"^\|\s*([a-z][a-z0-9-]+)\s*\|"
        r"\s*(required|optional|n/a)\s*\|"
        r"\s*(required|optional|n/a)\s*\|"
        r"\s*(required|optional|n/a)\s*\|"
        r"\s*(required|optional|n/a)\s*\|"
        r"\s*(required|optional|n/a)\s*\|"
        r"[^\|]*\|",
        re.MULTILINE,
    )
    for m in row_re.finditer(text):
        agent = m.group(1)
        cells = [m.group(i) for i in range(2, 7)]
        result[agent] = dict(zip(COMMON_SKILLS, cells))
    return result


def parse_agent_skills_block(agent_path: Path) -> dict[str, str]:
    """Parse <skills> block of AGENT.md → {skill_name: required|optional} (n/a omitted)."""
    text = agent_path.read_text(encoding="utf-8")
    block_re = re.compile(r"<skills>(.*?)</skills>", re.DOTALL)
    m = block_re.search(text)
    if not m:
        return {}
    block = m.group(1)
    result: dict[str, str] = {}
    entry_re = re.compile(r"`?([a-z][a-z0-9-]+)`?\s*\((required|optional)\)")
    for em in entry_re.finditer(block):
        result[em.group(1)] = em.group(2)
    return result


def _agent_md_path(agent: str) -> Path | None:
    """Resolve agent name → AGENT.md path."""
    if agent in PRODUCERS:
        return REPO_ROOT / ".claude" / "agents" / "producers" / agent / "AGENT.md"
    for cat, name in INSPECTORS:
        if name == agent:
            return REPO_ROOT / ".claude" / "agents" / "inspectors" / cat / name / "AGENT.md"
    return None


def verify_reciprocity(fail_on_drift: bool = False) -> tuple[int, list[str]]:
    """Compare matrix vs AGENT.md <skills> blocks. Return (drift_count, messages)."""
    matrix = parse_matrix()
    drifts: list[str] = []
    for agent, cells in matrix.items():
        p = _agent_md_path(agent)
        if p is None or not p.exists():
            drifts.append(f"DRIFT: agent '{agent}' in matrix but AGENT.md not found")
            continue
        skills_block = parse_agent_skills_block(p)
        for skill, cell_value in cells.items():
            in_block = skills_block.get(skill)
            # Reciprocity rules
            if cell_value == "required" and in_block != "required":
                drifts.append(
                    f"DRIFT: {agent}/{skill}: matrix=required, AGENT.md=<{in_block or 'missing'}>"
                )
            elif cell_value == "optional" and in_block != "optional":
                # optional in matrix → <skills> must have optional entry OR absent (soft)
                if in_block and in_block != "optional":
                    drifts.append(
                        f"DRIFT: {agent}/{skill}: matrix=optional, AGENT.md=<{in_block}>"
                    )
            elif cell_value == "n/a" and in_block is not None:
                drifts.append(
                    f"DRIFT: {agent}/{skill}: matrix=n/a, AGENT.md=<{in_block}> (should be absent)"
                )
    return len(drifts), drifts


def main(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description="SKILL-ROUTE-01 reciprocity check")
    parser.add_argument("--fail-on-drift", action="store_true",
                        help="Exit 1 on any drift (CI default)")
    args = parser.parse_args(argv)

    count, drifts = verify_reciprocity(fail_on_drift=args.fail_on_drift)
    if count == 0:
        print("OK: 30 × 5 = 150 cells reciprocate between matrix and AGENT.md <skills>")
        return 0
    print(f"FAIL: {count} drift(s) detected", file=sys.stderr)
    for d in drifts[:20]:
        print(f"  {d}", file=sys.stderr)
    if count > 20:
        print(f"  ... and {count - 20} more", file=sys.stderr)
    return 1 if args.fail_on_drift else 0


if __name__ == "__main__":
    sys.exit(main())
```

**중요**: 본 Plan 04 는 Plan 02/03 에 depend 하지 않음 (Wave 2 병렬). 따라서 실행 시점에 inspector AGENT.md 가 v1.1 migration 되지 않은 상태일 수 있음. 이 경우 `--fail-on-drift` 없이 호출하면 drift 경고만 stdout 출력 후 exit 0 — Plan 03 완료 후 `--fail-on-drift` 로 재확인.
  </action>
  <verify>
    <automated>py -3.11 scripts/validate/verify_agent_skill_matrix.py 2>&1 | head -20; py -3.11 scripts/validate/verify_agent_skill_matrix.py --help 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `py -3.11 scripts/validate/verify_agent_skill_matrix.py` executes without Python exception (exit 0 OR 1 but no crash)
    - `py -3.11 scripts/validate/verify_agent_skill_matrix.py --help` shows `--fail-on-drift` flag
    - File contains `parse_matrix()` function
    - File contains `parse_agent_skills_block()` function
    - File contains `verify_reciprocity()` function
    - File contains `sys.stdout.reconfigure` Windows cp949 guard
    - `COMMON_SKILLS` list contains exactly 5 items matching matrix columns (progressive-disclosure, gate-dispatcher, drift-detection, context-compressor, harness-audit)
    - After Plan 02 + Plan 03 완료: `py -3.11 scripts/validate/verify_agent_skill_matrix.py --fail-on-drift` exit 0
  </acceptance_criteria>
  <done>
verify_agent_skill_matrix.py CLI 개통. SKILL-ROUTE-01 CI enforcement surface 확보. Phase 13+ 에서 pre-commit hook 연계 가능.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: test_skill_matrix_format.py 실 assertion 채우기</name>
  <files>tests/phase12/test_skill_matrix_format.py</files>
  <behavior>
    - Test: `test_matrix_has_30_rows` — parse_matrix() 가 30 agent 를 반환
    - Test: `test_matrix_cell_values_legal` — 모든 cell 이 required / optional / n/a 중 하나
    - Test: `test_matrix_reciprocity_with_agent_md` — verify_reciprocity() 호출 결과가 Plan 02 + Plan 03 완료 후 0 drift
  </behavior>
  <read_first>
    - tests/phase12/test_skill_matrix_format.py (Plan 01 Task 2 red stub)
    - wiki/agent_skill_matrix.md (Task 2 산출)
    - scripts/validate/verify_agent_skill_matrix.py (Task 3 산출)
  </read_first>
  <action>
red stub 제거 + 실 assertion:

```python
"""SKILL-ROUTE-01 — markdown matrix parse + cell value validation + reciprocity."""

from __future__ import annotations
import sys
from pathlib import Path

import pytest

REPO_ROOT = Path(__file__).resolve().parents[2]
sys.path.insert(0, str(REPO_ROOT / "scripts" / "validate"))

from verify_agent_skill_matrix import (  # noqa: E402
    parse_matrix,
    parse_agent_skills_block,
    verify_reciprocity,
    COMMON_SKILLS,
    PRODUCERS,
    INSPECTORS,
    _agent_md_path,
)


def test_matrix_has_30_rows():
    matrix = parse_matrix()
    assert len(matrix) == 30, f"expected 30 rows, got {len(matrix)}: {sorted(matrix.keys())[:5]}..."
    # 13 producer
    for p in PRODUCERS:
        assert p in matrix, f"producer {p} missing from matrix"
    # 17 inspector
    for _, i in INSPECTORS:
        assert i in matrix, f"inspector {i} missing from matrix"


def test_matrix_cell_values_legal():
    matrix = parse_matrix()
    legal = {"required", "optional", "n/a"}
    for agent, cells in matrix.items():
        for skill, value in cells.items():
            assert value in legal, f"illegal cell [{agent}][{skill}] = {value!r}"


def test_matrix_every_agent_has_required_gate_dispatcher():
    """모든 30 agent 는 gate-dispatcher=required — 파이프라인 dispatch 계약 baseline."""
    matrix = parse_matrix()
    for agent, cells in matrix.items():
        assert cells.get("gate-dispatcher") == "required", \
            f"{agent} missing gate-dispatcher=required"


def test_common_skills_count_is_5():
    assert len(COMMON_SKILLS) == 5, \
        f"REQUIREMENTS §383 정정 후 COMMON_SKILLS 는 5 — got {len(COMMON_SKILLS)}"


def test_matrix_reciprocity_with_agent_md():
    """Plan 02 + Plan 03 완료 후 실행 — 0 drift 기대.
    Plan 04 단독 실행 시 drift 존재할 수 있음 (AGENT.md migration 전)."""
    drift_count, drifts = verify_reciprocity(fail_on_drift=False)
    # Plan 02 + Plan 03 완료 시 0 기대. 미완료 시 skip 조건부.
    if drift_count > 0:
        # Check if AGENT.md migration is complete (v1.2/v1.1 bumped)
        t = _agent_md_path("trend-collector")
        if t and "version: 1.2" not in t.read_text(encoding="utf-8"):
            pytest.skip(
                "AGENT.md migration incomplete — re-run after Plan 02 + Plan 03. "
                f"Current drift: {drift_count}"
            )
        pytest.fail(
            f"{drift_count} drifts despite AGENT.md migration complete: {drifts[:3]}"
        )
    assert drift_count == 0
```

**Plan 02/03 완료 전 실행 시**: `test_matrix_reciprocity_with_agent_md` 는 skip 처리 (trend-collector version check). 완료 후 실행 시 PASS.
  </action>
  <verify>
    <automated>py -3.11 -m pytest tests/phase12/test_skill_matrix_format.py -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `py -3.11 -m pytest tests/phase12/test_skill_matrix_format.py -v` exits 0
    - ≥ 4 tests passed: test_matrix_has_30_rows + test_matrix_cell_values_legal + test_matrix_every_agent_has_required_gate_dispatcher + test_common_skills_count_is_5
    - test_matrix_reciprocity_with_agent_md PASSES (if Plan 02+03 complete) OR SKIPS (if incomplete, with reason `AGENT.md migration incomplete`)
    - No tests FAIL
  </acceptance_criteria>
  <done>
test_skill_matrix_format.py 실 assertion 5 test GREEN. SKILL-ROUTE-01 pytest coverage 확보.
  </done>
</task>

</tasks>

<verification>
```bash
py -3.11 -m pytest tests/phase12/test_skill_matrix_format.py -v       # ≥ 4 passed + 1 pass/skip
py -3.11 scripts/validate/verify_agent_skill_matrix.py                # runs without crash
py -3.11 scripts/validate/verify_agent_skill_matrix.py --fail-on-drift # exit 0 (after Plan 02+03)
grep -c "5 공용 skill" .planning/REQUIREMENTS.md                      # ≥ 1
grep -c "30 × 8 매트릭스" .planning/REQUIREMENTS.md                   # = 0 (구형 제거)
```
</verification>

<success_criteria>
1. REQUIREMENTS.md §383 정정 완료 — "8 skill" → "5 공용 skill + additional" (Option A 채택)
2. wiki/agent_skill_matrix.md 신규 — 30 row × 6 col SSOT 매트릭스
3. verify_agent_skill_matrix.py CLI — `--fail-on-drift` 플래그 + bidirectional reciprocity
4. test_skill_matrix_format.py 5 tests GREEN (reciprocity test 는 Plan 02+03 후 GREEN 전환)
5. 150 cells (30×5) 양방향 무충돌 — Plan 02 + Plan 03 완료 시 CI level SKILL-ROUTE-01 fully verifiable
</success_criteria>

<output>
After completion, create `.planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-04-SUMMARY.md`.

대표님께 보고드립니다. Agent × Skill SSOT 매트릭스 확보. REQUIREMENTS §383 drift (8 vs 5) 정정 — D-2 Lock 기간 신규 SKILL 생성 금지 원칙 보존. verify_agent_skill_matrix.py CI enforcement surface 개통. bidirectional reciprocity 계약으로 drift 재발 차단.
</output>
