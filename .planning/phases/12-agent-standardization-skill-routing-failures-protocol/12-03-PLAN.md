---
phase: 12-agent-standardization-skill-routing-failures-protocol
plan: 03
type: execute
wave: 3
depends_on:
  - "12-01"
  - "12-02"
files_modified:
  - .claude/agents/inspectors/structural/ins-schema-integrity/AGENT.md
  - .claude/agents/inspectors/structural/ins-timing-consistency/AGENT.md
  - .claude/agents/inspectors/structural/ins-blueprint-compliance/AGENT.md
  - .claude/agents/inspectors/content/ins-factcheck/AGENT.md
  - .claude/agents/inspectors/content/ins-narrative-quality/AGENT.md
  - .claude/agents/inspectors/content/ins-korean-naturalness/AGENT.md
  - .claude/agents/inspectors/style/ins-thumbnail-hook/AGENT.md
  - .claude/agents/inspectors/style/ins-tone-brand/AGENT.md
  - .claude/agents/inspectors/style/ins-readability/AGENT.md
  - .claude/agents/inspectors/compliance/ins-license/AGENT.md
  - .claude/agents/inspectors/compliance/ins-platform-policy/AGENT.md
  - .claude/agents/inspectors/compliance/ins-safety/AGENT.md
  - .claude/agents/inspectors/technical/ins-audio-quality/AGENT.md
  - .claude/agents/inspectors/technical/ins-render-integrity/AGENT.md
  - .claude/agents/inspectors/technical/ins-subtitle-alignment/AGENT.md
  - .claude/agents/inspectors/media/ins-mosaic/AGENT.md
  - .claude/agents/inspectors/media/ins-gore/AGENT.md
  - .claude/failures/FAILURES.md
autonomous: true
requirements:
  - AGENT-STD-01
  - AGENT-STD-02
  - FAIL-PROTO-02
status: pending
must_haves:
  truths:
    - "17 Inspector 전체 AGENT.md 가 5 XML 블록 schema 준수 — 13 producer + 17 inspector = 30/30 verify_agent_md_schema.py --all exit 0"
    - "각 Inspector 의 RUB-06 역방향 미러 명시 — producer_prompt 읽기 금지 (Producer 의 inspector_prompt 읽기 금지 반대 방향)"
    - "Inspector 고유 maxTurns 준수 — ins-factcheck=10, ins-tone-brand=5, 나머지 15개=1"
    - "Phase 4 GAN_CLEAN 17/17 regression 무회귀 — grep_gan_contamination.py exit 0"
    - "F-D2-EXCEPTION-02 Wave 3 supplement 기록 — 새 header 금지 idempotent"
    - "Structural Inspector 3 (ins-schema-integrity / ins-timing-consistency / ins-blueprint-compliance) 의 `<skills>` 블록이 Plan 04 matrix 와 reciprocity 보장 — progressive-disclosure literal 부재 (D-A1-02 SSOT 일치, Issue #1 fix)"
    - "각 Inspector AGENT.md 의 `<mandatory_reads>` 블록에 '매 호출마다 전수 읽기, 샘플링 금지' 한국어 literal 포함 — per-task acceptance enforced (D-A1-03, Issue #2 fix)"
  artifacts:
    - path: ".claude/agents/inspectors/structural/ins-schema-integrity/AGENT.md"
      provides: "Wave 3a Structural Inspector — 5-block schema compliant"
      contains: "producer_prompt 읽기 금지 (RUB-06 GAN 분리 mirror)"
    - path: ".claude/agents/inspectors/content/ins-factcheck/AGENT.md"
      provides: "Wave 3b Content Inspector — maxTurns=10 RUB-05 exception"
      contains: "maxTurns: 10"
    - path: ".claude/failures/FAILURES.md"
      provides: "F-D2-EXCEPTION-02 Wave 3 supplement"
  key_links:
    - from: "tests/phase12/test_agent_md_schema.py::test_all_scoped_agents_have_5_blocks"
      to: "30 AGENT.md"
      via: "_collect_all_agent_mds() full scan"
      pattern: "producers.*AGENT.md|inspectors.*AGENT.md"
    - from: "tests/phase04/test_grep_gan_clean_17_of_17.py"
      to: "17 Inspector AGENT.md"
      via: "grep_gan_contamination scan post-migration"
      pattern: "grep_gan_contamination.*17"
    - from: "Plan 04 verify_agent_skill_matrix.py --fail-on-drift"
      to: "Structural 3 AGENT.md <skills> blocks"
      via: "reciprocity rule — matrix cell n/a ↔ AGENT.md skill literal 부재"
      pattern: "progressive-disclosure (부재 검증, Issue #1 fix)"
---

<objective>
17 Inspector AGENT.md 를 template 기반 v1.1 로 migration. 6 sub-wave (Structural 3 → Content 3 → Style 3 → Compliance 3 → Technical 3 → Media 2). 완료 시 30/30 AGENT.md schema compliant + Phase 4 GAN_CLEAN 17/17 regression 유지 + FAIL-PROTO-02 Wave 3 supplement 기록.

**Purpose**: Inspector 전수 표준화는 LogicQA 품질 + GAN 분리 경계 핵심. 모든 Inspector 가 RUB-06 mirror 명시 → GAN collapse 구조적 차단.

**Issue #1 (Structural Inspector matrix reciprocity fix)**: 12-04-PLAN matrix lines 182-184 에서 Structural 3 의 `progressive-disclosure` 셀이 `n/a` (Inspector 는 SKILL 작성자 아님 — 준수자, Plan 04 prose note line 207). 따라서 본 Plan Task 1 의 `<skills>` 블록에 `progressive-disclosure (optional)` 을 **포함하지 않아야** matrix reciprocity 성립. 그렇지 않으면 `verify_agent_skill_matrix.py --fail-on-drift` 가 Plan 04 완료 후 trip. D-A1-02 = Matrix SSOT → AGENT.md 가 matrix 을 따라간다.

**Output**: 17 Inspector AGENT.md v1.1 + Phase 4 regression GREEN + FAILURES.md F-D2-EXCEPTION-02 Wave 3 supplement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-CONTEXT.md
@.planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-RESEARCH.md
@.planning/phases/12-agent-standardization-skill-routing-failures-protocol/templates/inspector.md.template
@.planning/phases/04-agent-team-design/04-CONTEXT.md
@CLAUDE.md
</context>

<interfaces>
Inspector template 차이점 (Plan 01 Task 1):
- `role: inspector` (frontmatter)
- `<constraints>` 첫 줄: `producer_prompt 읽기 금지 (RUB-06 GAN 분리 mirror) — Producer system prompt / 내부 추론 조회 금지. producer_output JSON 만 평가 대상.`
- `<mandatory_reads>` 2번 항목: `wiki/continuity_bible/channel_identity.md` 단일
- `<output_format>` JSON schema: rubric-schema.json — `{gate, verdict, decisions[], evidence[], error_codes[], semantic_feedback}`

Inspector maxTurns (Phase 4 baseline):
- Structural 3: maxTurns=1
- Content: ins-factcheck=10 (RUB-05 exception), 나머지=1
- Style: ins-tone-brand=5, 나머지=1
- Compliance 3: maxTurns=1
- Technical 3: maxTurns=1
- Media 2: maxTurns=1

**Matrix SSOT reciprocity (D-A1-02, Issue #1 fix)** — Plan 04 `wiki/agent_skill_matrix.md` 는 Inspector 전원의 `progressive-disclosure` 셀을 `n/a` 로 명시 (SKILL 작성자가 아니라 준수자). 따라서 17 Inspector 전원의 AGENT.md `<skills>` 블록에 `progressive-disclosure` literal 부재 (optional/required 어느 형태든 금지). Plan 04 verifier 가 `cell_value == "n/a" and skill_present_in_agent_md` 를 drift 로 간주.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Structural Inspector 3 migration (matrix reciprocity — progressive-disclosure 부재 필수, Issue #1 fix)</name>
  <files>.claude/agents/inspectors/structural/ins-schema-integrity/AGENT.md, .claude/agents/inspectors/structural/ins-timing-consistency/AGENT.md, .claude/agents/inspectors/structural/ins-blueprint-compliance/AGENT.md</files>
  <read_first>
    - .claude/agents/inspectors/structural/ins-schema-integrity/AGENT.md (현 v1.0)
    - .claude/agents/inspectors/structural/ins-timing-consistency/AGENT.md
    - .claude/agents/inspectors/structural/ins-blueprint-compliance/AGENT.md
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/templates/inspector.md.template
    - .claude/agents/_shared/rubric-schema.json
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-04-PLAN.md lines 182-184 (matrix rows — Structural 3 의 progressive-disclosure = n/a)
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-04-PLAN.md line 207 (matrix prose note — "Inspector 전원 progressive-disclosure = n/a")
  </read_first>
  <action>
3 Inspector AGENT.md v1.1 승격. 각 파일 절차:

1. frontmatter `version: X.Y` → `version: 1.1`.
2. body head (line 10 이후) 에 5 XML 블록 재구성:
   - `<role>` 에이전트 고유:
     - **ins-schema-integrity**: "스키마 무결성 inspector. producer_output JSON 이 gate 별 계약 스키마 (gates.py GATE_DEPS) 준수 여부 평가 — 누락 필드 / 타입 오류 / enum 위반 detect."
     - **ins-timing-consistency**: "타이밍 일관성 inspector. scenes[].duration_s 합계 vs 전체 duration + subtitles[].start_time vs audio_segments[].end_time alignment 검증 (drift ±100ms 허용)."
     - **ins-blueprint-compliance**: "Blueprint 준수 inspector. director Blueprint 와 downstream (scenes/shots/subtitles) 구조적 일치 평가 — hook/act1/act2/climax/resolution 5 섹션 mapping."
   - `<mandatory_reads>` 표준 3 항목 (**'매 호출마다 전수 읽기, 샘플링 금지' literal 필수 — D-A1-03, Issue #2 fix**):
     1. `.claude/failures/FAILURES.md`
     2. `wiki/continuity_bible/channel_identity.md`
     3. `.claude/skills/gate-dispatcher/SKILL.md`
   - `<output_format>`: JSON `{gate, verdict: PASS|RETRY|FAIL, decisions[{rule, severity, score, evidence}], evidence[], error_codes[], semantic_feedback}` + 금지 패턴 5종
   - `<skills>` (**Issue #1 fix — matrix SSOT 일치, progressive-disclosure 포함 금지**):
     - `gate-dispatcher` (required) — Gate dispatch contract
     - **NO other skills** — Structural 3 Inspector 는 matrix 에서 progressive-disclosure / drift-detection / context-compressor 전부 `n/a`. `<skills>` 블록에 literal `progressive-disclosure` / `drift-detection` / `context-compressor` 절대 등장 금지 (Plan 04 reciprocity verifier 실패 방지).
   - `<constraints>`:
     - **producer_prompt 읽기 금지 (RUB-06 GAN 분리 mirror)**
     - `maxTurns=1 준수 (RUB-05)`
     - `한국어 출력 baseline` (decisions[].rule 영문 snake_case 허용)
     - `T2V 경로 절대 금지 (I2V only, D-13)`
     - `FAILURES.md append-only (D-11)`
3. body prose 전량 보존.

**Issue #1 fix 검증**: 완료 후 각 Structural 3 파일에 대해 `grep -c "progressive-disclosure" <file>` = **0** 확인 (AGENT.md 전체 — `<skills>` / `<constraints>` / 본문 prose 어디서도 등장 금지. matrix note 줄의 정보성 언급은 `<role>` 이후 body 에 넣지 말 것).
  </action>
  <verify>
    <automated>for f in ins-schema-integrity ins-timing-consistency ins-blueprint-compliance; do py -3.11 scripts/validate/verify_agent_md_schema.py .claude/agents/inspectors/structural/$f/AGENT.md || exit 1; done; for f in ins-schema-integrity ins-timing-consistency ins-blueprint-compliance; do N=$(grep -c "progressive-disclosure" .claude/agents/inspectors/structural/$f/AGENT.md); test "$N" = "0" || { echo "FAIL: $f has progressive-disclosure literal (Issue #1 reciprocity violation)"; exit 1; }; done; py -3.11 -m pytest tests/phase04/test_grep_gan_clean_17_of_17.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - 3 파일 모두 verify_agent_md_schema.py exit 0
    - 3 파일 모두 `version: 1.1` frontmatter
    - 3 파일 모두 `producer_prompt 읽기 금지 (RUB-06 GAN 분리 mirror)` literal 포함
    - `tests/phase04/test_grep_gan_clean_17_of_17.py` exit 0 (GAN_CLEAN 유지)
    - 3 파일 모두 body prose 보존 (`## MUST REMEMBER` literal ≥ 1 per file)
    - **Issue #1 fix — Structural 3 전원 `grep -c "progressive-disclosure" <file>` = 0 (matrix reciprocity SSOT 일치):**
      - `grep -c "progressive-disclosure" .claude/agents/inspectors/structural/ins-schema-integrity/AGENT.md` = 0
      - `grep -c "progressive-disclosure" .claude/agents/inspectors/structural/ins-timing-consistency/AGENT.md` = 0
      - `grep -c "progressive-disclosure" .claude/agents/inspectors/structural/ins-blueprint-compliance/AGENT.md` = 0
    - **Issue #2 fix — 3 파일 모두 `grep -c "매 호출마다 전수 읽기, 샘플링 금지"` ≥ 1 (D-A1-03 literal, per target 전수 검증):**
      - `grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/inspectors/structural/ins-schema-integrity/AGENT.md` ≥ 1
      - `grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/inspectors/structural/ins-timing-consistency/AGENT.md` ≥ 1
      - `grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/inspectors/structural/ins-blueprint-compliance/AGENT.md` ≥ 1
  </acceptance_criteria>
  <done>Structural 3/17 완료. Issue #1 matrix reciprocity 보장 + Issue #2 D-A1-03 literal 전수 기입.</done>
</task>

<task type="auto">
  <name>Task 2: Content Inspector 3 migration (factcheck maxTurns=10 RUB-05 exception 보존)</name>
  <files>.claude/agents/inspectors/content/ins-factcheck/AGENT.md, .claude/agents/inspectors/content/ins-narrative-quality/AGENT.md, .claude/agents/inspectors/content/ins-korean-naturalness/AGENT.md</files>
  <read_first>
    - .claude/agents/inspectors/content/ins-factcheck/AGENT.md (현 v1.0, maxTurns=10)
    - .claude/agents/inspectors/content/ins-narrative-quality/AGENT.md
    - .claude/agents/inspectors/content/ins-korean-naturalness/AGENT.md
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/templates/inspector.md.template
  </read_first>
  <action>
3 Inspector v1.1 승격. 에이전트 고유 `<role>`. **모든 3 파일의 `<mandatory_reads>` 에 '매 호출마다 전수 읽기, 샘플링 금지' 한국어 literal 필수 (D-A1-03, Issue #2 fix)**.

- **ins-factcheck**: "팩트체크 inspector. researcher facts[] + citations[] 을 NotebookLM RAG 로 재검증 — 인용 정확성 / 출처 신뢰도 / 추측 남용 detect. **maxTurns=10 (RUB-05 exception)** — NotebookLM 쿼리 반복 필요."
- **ins-narrative-quality**: "서사 품질 inspector. hook 3초 후킹 / act 간 긴장 변화 / 결말 CTA 명확성 평가. 품질 점수 0-100 + improvement suggestions."
- **ins-korean-naturalness**: "한국어 자연스러움 inspector. 존댓말/반말 일관성 / 부자연스러운 번역투 / 고빈도 어색 표현 detect. wiki/script/KOREAN_PATTERNS.md 참조."

각 에이전트 frontmatter `maxTurns`:
- ins-factcheck: `maxTurns: 10` 유지 (변경 금지)
- 나머지 2: `maxTurns: 1` 유지

`<constraints>` 에 `maxTurns=N 준수 (RUB-05)` — N 값 에이전트별 반영. ins-factcheck 는 "maxTurns=10 RUB-05 exception — NotebookLM 쿼리 반복" 명시.

`<skills>` 특이점 (matrix SSOT 준수):
- ins-factcheck: `gate-dispatcher` (required), `drift-detection` (optional) — citation drift 감지
- ins-korean-naturalness: `gate-dispatcher` (required), `context-compressor` (optional) — 긴 대본 부분 검증
- ins-narrative-quality: `gate-dispatcher` (required) — 단독

body prose 보존. 3 파일 verify 개별 exit 0.
  </action>
  <verify>
    <automated>for f in ins-factcheck ins-narrative-quality ins-korean-naturalness; do py -3.11 scripts/validate/verify_agent_md_schema.py .claude/agents/inspectors/content/$f/AGENT.md || exit 1; done; grep -c "^maxTurns: 10$" .claude/agents/inspectors/content/ins-factcheck/AGENT.md</automated>
  </verify>
  <acceptance_criteria>
    - 3 파일 모두 verify_agent_md_schema.py exit 0
    - `grep -c "^maxTurns: 10$" .claude/agents/inspectors/content/ins-factcheck/AGENT.md` = 1 (RUB-05 exception 보존)
    - ins-factcheck 의 `<constraints>` 에 `maxTurns=10 RUB-05 exception` literal ≥ 1
    - 3 파일 모두 `version: 1.1` + RUB-06 mirror
    - `tests/phase04/test_grep_gan_clean_17_of_17.py` exit 0
    - **Issue #2 fix — 3 파일 모두 `grep -c "매 호출마다 전수 읽기, 샘플링 금지"` ≥ 1 (D-A1-03 literal, per target 전수 검증):**
      - `grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/inspectors/content/ins-factcheck/AGENT.md` ≥ 1
      - `grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/inspectors/content/ins-narrative-quality/AGENT.md` ≥ 1
      - `grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/inspectors/content/ins-korean-naturalness/AGENT.md` ≥ 1
  </acceptance_criteria>
  <done>Content 3/17 완료 (6/17 누적).</done>
</task>

<task type="auto">
  <name>Task 3: Style Inspector 3 migration (tone-brand maxTurns=5 보존)</name>
  <files>.claude/agents/inspectors/style/ins-thumbnail-hook/AGENT.md, .claude/agents/inspectors/style/ins-tone-brand/AGENT.md, .claude/agents/inspectors/style/ins-readability/AGENT.md</files>
  <read_first>
    - .claude/agents/inspectors/style/ins-thumbnail-hook/AGENT.md
    - .claude/agents/inspectors/style/ins-tone-brand/AGENT.md (maxTurns=5)
    - .claude/agents/inspectors/style/ins-readability/AGENT.md
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/templates/inspector.md.template
  </read_first>
  <action>
3 Inspector v1.1 승격. **모든 3 파일의 `<mandatory_reads>` 에 '매 호출마다 전수 읽기, 샘플링 금지' 한국어 literal 필수 (D-A1-03, Issue #2 fix)**.

- **ins-thumbnail-hook**: "썸네일 후킹 inspector. thumbnail_spec text 가독성 / 얼굴 배치 / 색감 대비 / 3초 후킹 강도 평가. face_detected=True + caption_visible=True + hook_strength ≥ 70."
- **ins-tone-brand**: "브랜드 톤 inspector. channel_identity.md 브랜드 보이스 (formal/serious/respectful) 준수 평가. **maxTurns=5** — 주관적 판단 복잡."
- **ins-readability**: "가독성 inspector. 자막 / title / description / thumbnail text 길이·난이도 평가 (Shorts 60초 기준). Flesh-Kincaid 한국어 근사치."

maxTurns:
- ins-thumbnail-hook: 1
- ins-tone-brand: 5 (보존)
- ins-readability: 1

ins-tone-brand 의 `<constraints>` 에 `maxTurns=5 (주관적 판단 복잡)` 명시.

`<skills>` 특이점 (matrix SSOT 준수):
- ins-thumbnail-hook: `gate-dispatcher` (required)
- ins-tone-brand: `gate-dispatcher` (required), `drift-detection` (optional) — 브랜드 drift 감지
- ins-readability: `gate-dispatcher` (required)

body 보존. 3 파일 verify exit 0.
  </action>
  <verify>
    <automated>for f in ins-thumbnail-hook ins-tone-brand ins-readability; do py -3.11 scripts/validate/verify_agent_md_schema.py .claude/agents/inspectors/style/$f/AGENT.md || exit 1; done; grep -c "^maxTurns: 5$" .claude/agents/inspectors/style/ins-tone-brand/AGENT.md</automated>
  </verify>
  <acceptance_criteria>
    - 3 파일 verify exit 0
    - `grep -c "^maxTurns: 5$" .claude/agents/inspectors/style/ins-tone-brand/AGENT.md` = 1
    - 3 파일 모두 `version: 1.1` + RUB-06 mirror
    - `tests/phase04/test_grep_gan_clean_17_of_17.py` exit 0
    - **Issue #2 fix — 3 파일 모두 `grep -c "매 호출마다 전수 읽기, 샘플링 금지"` ≥ 1 (D-A1-03 literal, per target 전수 검증):**
      - `grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/inspectors/style/ins-thumbnail-hook/AGENT.md` ≥ 1
      - `grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/inspectors/style/ins-tone-brand/AGENT.md` ≥ 1
      - `grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/inspectors/style/ins-readability/AGENT.md` ≥ 1
  </acceptance_criteria>
  <done>Style 3/17 완료 (9/17 누적).</done>
</task>

<task type="auto">
  <name>Task 4: Compliance Inspector 3 migration (license + platform-policy + safety — AF-4/13 차단)</name>
  <files>.claude/agents/inspectors/compliance/ins-license/AGENT.md, .claude/agents/inspectors/compliance/ins-platform-policy/AGENT.md, .claude/agents/inspectors/compliance/ins-safety/AGENT.md</files>
  <read_first>
    - .claude/agents/inspectors/compliance/ins-license/AGENT.md
    - .claude/agents/inspectors/compliance/ins-platform-policy/AGENT.md
    - .claude/agents/inspectors/compliance/ins-safety/AGENT.md
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/templates/inspector.md.template
  </read_first>
  <action>
3 Inspector v1.1 승격. **모든 3 파일의 `<mandatory_reads>` 에 '매 호출마다 전수 읽기, 샘플링 금지' 한국어 literal 필수 (D-A1-03, Issue #2 fix)**.

- **ins-license**: "라이선스 inspector. I2V clip / TTS audio / 배경음 / 이미지 출처 라이선스 확인 — KOMCA K-pop 금지 (AF-13), CC-BY / royalty-free 만. 하이브리드 crossfade 탐지."
- **ins-platform-policy**: "플랫폼 정책 inspector. YouTube 커뮤니티 가이드라인 / 광고 친화성 / Inauthentic Content 규정 (AF-1, AF-11) 준수. containsSyntheticMedia=True 확인 (PUB-01)."
- **ins-safety**: "안전성 inspector. 자살 / 자해 / 극단주의 / 폭력 수위 평가. 위험 수위 > 중간 시 verdict=FAIL. 한국 방송법 준수."

`<constraints>` 에이전트 특이:
- ins-license: `KOMCA K-pop 트렌드 음원 직접 사용 금지 (AF-13)` 명시
- ins-platform-policy: `일일 업로드 금지 (AF-1 + AF-11)` + `Selenium 업로드 금지 (AF-8)` 명시
- ins-safety: `한국 방송법 + YouTube 커뮤니티 가이드라인 우선` 명시

`<skills>` 전원 `gate-dispatcher` (required). body 보존. 3 파일 verify exit 0.
  </action>
  <verify>
    <automated>for f in ins-license ins-platform-policy ins-safety; do py -3.11 scripts/validate/verify_agent_md_schema.py .claude/agents/inspectors/compliance/$f/AGENT.md || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - 3 파일 verify exit 0
    - ins-license 에 `KOMCA K-pop 트렌드 음원 직접 사용 금지 (AF-13)` ≥ 1
    - ins-platform-policy 에 `일일 업로드 금지 (AF-1` ≥ 1
    - 3 파일 모두 `version: 1.1` + RUB-06 mirror
    - `tests/phase04/test_grep_gan_clean_17_of_17.py` exit 0
    - **Issue #2 fix — 3 파일 모두 `grep -c "매 호출마다 전수 읽기, 샘플링 금지"` ≥ 1 (D-A1-03 literal, per target 전수 검증):**
      - `grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/inspectors/compliance/ins-license/AGENT.md` ≥ 1
      - `grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/inspectors/compliance/ins-platform-policy/AGENT.md` ≥ 1
      - `grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/inspectors/compliance/ins-safety/AGENT.md` ≥ 1
  </acceptance_criteria>
  <done>Compliance 3/17 완료 (12/17 누적).</done>
</task>

<task type="auto">
  <name>Task 5: Technical Inspector 3 migration (audio + render + subtitle)</name>
  <files>.claude/agents/inspectors/technical/ins-audio-quality/AGENT.md, .claude/agents/inspectors/technical/ins-render-integrity/AGENT.md, .claude/agents/inspectors/technical/ins-subtitle-alignment/AGENT.md</files>
  <read_first>
    - .claude/agents/inspectors/technical/ins-audio-quality/AGENT.md
    - .claude/agents/inspectors/technical/ins-render-integrity/AGENT.md
    - .claude/agents/inspectors/technical/ins-subtitle-alignment/AGENT.md
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/templates/inspector.md.template
  </read_first>
  <action>
3 Inspector v1.1 승격. **모든 3 파일의 `<mandatory_reads>` 에 '매 호출마다 전수 읽기, 샘플링 금지' 한국어 literal 필수 (D-A1-03, Issue #2 fix)**.

- **ins-audio-quality**: "오디오 품질 inspector. voice-producer 오디오 의 볼륨 일관성 / 클리핑 / 배경 소음 / TTS artifact detect. LUFS -16 ± 1 기준."
- **ins-render-integrity**: "렌더 무결성 inspector. assembler 완성 영상 의 해상도 / FPS / 코덱 / duration / 세로형 9:16 준수 검증."
- **ins-subtitle-alignment**: "자막 alignment inspector. subtitles[].start_time / end_time 과 audio_segments 정렬 검증 — drift ±150ms + 자막 길이 ≤ 25 chars per line."

frontmatter maxTurns=1 전원. `<skills>` `gate-dispatcher` (required). body 보존. 3 파일 verify exit 0.
  </action>
  <verify>
    <automated>for f in ins-audio-quality ins-render-integrity ins-subtitle-alignment; do py -3.11 scripts/validate/verify_agent_md_schema.py .claude/agents/inspectors/technical/$f/AGENT.md || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - 3 파일 verify exit 0
    - 3 파일 모두 `version: 1.1` + RUB-06 mirror
    - ins-render-integrity 에 `9:16` literal ≥ 1 (Shorts 세로형 lock)
    - `tests/phase04/test_grep_gan_clean_17_of_17.py` exit 0
    - **Issue #2 fix — 3 파일 모두 `grep -c "매 호출마다 전수 읽기, 샘플링 금지"` ≥ 1 (D-A1-03 literal, per target 전수 검증):**
      - `grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/inspectors/technical/ins-audio-quality/AGENT.md` ≥ 1
      - `grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/inspectors/technical/ins-render-integrity/AGENT.md` ≥ 1
      - `grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/inspectors/technical/ins-subtitle-alignment/AGENT.md` ≥ 1
  </acceptance_criteria>
  <done>Technical 3/17 완료 (15/17 누적).</done>
</task>

<task type="auto">
  <name>Task 6: Media Inspector 2 migration (mosaic + gore — AF-5 차단)</name>
  <files>.claude/agents/inspectors/media/ins-mosaic/AGENT.md, .claude/agents/inspectors/media/ins-gore/AGENT.md</files>
  <read_first>
    - .claude/agents/inspectors/media/ins-mosaic/AGENT.md
    - .claude/agents/inspectors/media/ins-gore/AGENT.md
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/templates/inspector.md.template
  </read_first>
  <action>
2 Inspector v1.1 승격. **모든 2 파일의 `<mandatory_reads>` 에 '매 호출마다 전수 읽기, 샘플링 금지' 한국어 literal 필수 (D-A1-03, Issue #2 fix)**.

- **ins-mosaic**: "모자이크 inspector. I2V clip / thumbnail 에서 얼굴 / 차량 번호판 / 로고 등 모자이크 필요 영역 detect. face_detection + manual_review_flag."
- **ins-gore**: "고어 inspector. 유혈 / 시신 / 잔혹 묘사 수위 평가. 임계치 초과 시 verdict=FAIL. AF-5 (극도 gore 100% 차단)."

`<constraints>` 에이전트 특이:
- ins-gore: `AF-5 극도 gore 100% 차단` 명시

frontmatter maxTurns=1. `<skills>` `gate-dispatcher` (required). body 보존. 2 파일 verify exit 0.
  </action>
  <verify>
    <automated>for f in ins-mosaic ins-gore; do py -3.11 scripts/validate/verify_agent_md_schema.py .claude/agents/inspectors/media/$f/AGENT.md || exit 1; done; py -3.11 scripts/validate/verify_agent_md_schema.py --all 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - 2 파일 verify exit 0
    - ins-gore 에 `AF-5 극도 gore 100% 차단` literal ≥ 1
    - 2 파일 모두 `version: 1.1` + RUB-06 mirror
    - `py -3.11 scripts/validate/verify_agent_md_schema.py --all` exit 0 (30/30 AGENT.md all compliant)
    - `tests/phase04/test_grep_gan_clean_17_of_17.py` exit 0
    - **Issue #2 fix — 2 파일 모두 `grep -c "매 호출마다 전수 읽기, 샘플링 금지"` ≥ 1 (D-A1-03 literal, per target 전수 검증):**
      - `grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/inspectors/media/ins-mosaic/AGENT.md` ≥ 1
      - `grep -c "매 호출마다 전수 읽기, 샘플링 금지" .claude/agents/inspectors/media/ins-gore/AGENT.md` ≥ 1
  </acceptance_criteria>
  <done>Media 2/17 완료 — 17/17 Inspector 전체 migration 완결. 30/30 AGENT.md schema compliant.</done>
</task>

<task type="auto">
  <name>Task 7: test_agent_md_schema.py 확장 (17 inspector parametrized) + F-D2-EXCEPTION-02 Wave 3 supplement</name>
  <files>tests/phase12/test_agent_md_schema.py, .claude/failures/FAILURES.md</files>
  <read_first>
    - tests/phase12/test_agent_md_schema.py (Plan 02 Task 7 확장본)
    - scripts/audit/skill_patch_counter.py (Plan 02 Task 8 `append_f_d2_exception_02`)
    - .claude/failures/FAILURES.md (현재 F-D2-EXCEPTION-02 Wave 2 entry 존재, Wave 3 supplement 추가 대상)
  </read_first>
  <action>
### 1. test_agent_md_schema.py 확장

파일 끝에 inspector parametrized test 추가 (기존 producer parametrized 유지):

```python
INSPECTORS_V1_1 = [
    ("structural", "ins-schema-integrity"),
    ("structural", "ins-timing-consistency"),
    ("structural", "ins-blueprint-compliance"),
    ("content", "ins-factcheck"),
    ("content", "ins-narrative-quality"),
    ("content", "ins-korean-naturalness"),
    ("style", "ins-thumbnail-hook"),
    ("style", "ins-tone-brand"),
    ("style", "ins-readability"),
    ("compliance", "ins-license"),
    ("compliance", "ins-platform-policy"),
    ("compliance", "ins-safety"),
    ("technical", "ins-audio-quality"),
    ("technical", "ins-render-integrity"),
    ("technical", "ins-subtitle-alignment"),
    ("media", "ins-mosaic"),
    ("media", "ins-gore"),
]


@pytest.mark.parametrize("category,inspector", INSPECTORS_V1_1)
def test_inspector_has_version_1_1_and_rub_06_mirror(category, inspector):
    p = REPO_ROOT / ".claude" / "agents" / "inspectors" / category / inspector / "AGENT.md"
    assert p.exists(), f"{category}/{inspector}/AGENT.md missing"
    text = p.read_text(encoding="utf-8")
    assert "version: 1.1" in text, f"{inspector} frontmatter not bumped to 1.1"
    assert "producer_prompt 읽기 금지 (RUB-06 GAN 분리 mirror)" in text, \
        f"{inspector} missing RUB-06 mirror in <constraints>"
    assert "매 호출마다 전수 읽기, 샘플링 금지" in text, \
        f"{inspector} missing 샘플링 금지 literal"


# Issue #1 fix — Structural 3 matrix reciprocity regression test
STRUCTURAL_3_NO_PROGRESSIVE_DISCLOSURE = [
    "ins-schema-integrity",
    "ins-timing-consistency",
    "ins-blueprint-compliance",
]


@pytest.mark.parametrize("inspector", STRUCTURAL_3_NO_PROGRESSIVE_DISCLOSURE)
def test_structural_inspector_no_progressive_disclosure(inspector):
    """Plan 04 matrix 에서 Structural 3 의 progressive-disclosure = n/a.
    AGENT.md 에 literal 등장 시 Plan 04 --fail-on-drift trip."""
    p = REPO_ROOT / ".claude" / "agents" / "inspectors" / "structural" / inspector / "AGENT.md"
    text = p.read_text(encoding="utf-8")
    assert "progressive-disclosure" not in text, (
        f"{inspector} AGENT.md has 'progressive-disclosure' literal — "
        f"matrix SSOT (Plan 04) marks this cell n/a for Structural Inspectors. "
        f"Reciprocity verifier will FAIL."
    )


def test_total_30_agents_scanned():
    """13 producer + 17 inspector = 30 AGENT.md in scope."""
    targets = _collect_all_agent_mds()
    assert len(targets) == 30, f"expected 30, got {len(targets)}"
```

### 2. F-D2-EXCEPTION-02 Wave 3 supplement

Plan 02 Task 8 의 `append_f_d2_exception_02` 를 Wave 3 label 로 재호출 — idempotent check 는 "Wave 3" 별도 marker 이므로 새 supplement 추가됨. Commit 조회는 marker-filter (Plan 02 Issue #5 fix 재활용 — `[plan-03]` marker):

```bash
py -3.11 -c "
from datetime import datetime
from zoneinfo import ZoneInfo
from pathlib import Path
import subprocess
import sys
sys.path.insert(0, '.')
from scripts.audit.skill_patch_counter import append_f_d2_exception_02

RAW_COMMITS = subprocess.check_output(
    ['git', 'log', '--grep=\\\\[plan-03\\\\]', '--format=%h %s']
).decode().strip().split('\n')
commits = []
for line in RAW_COMMITS:
    if not line.strip():
        continue
    parts = line.split(' ', 1)
    commits.append({'hash': parts[0], 'subject': parts[1] if len(parts) > 1 else ''})

RAW_FILES = subprocess.check_output(
    ['git', 'log', '--grep=\\\\[plan-03\\\\]', '--name-only', '--pretty=format:']
).decode().strip().split('\n')
scope_files = sorted({f for f in RAW_FILES if f.strip() and 'AGENT.md' in f})

append_f_d2_exception_02(
    wave_label='3',
    commits=commits,
    scope_files=scope_files,
    authorized_by='대표님 session #29 (Phase 12 Wave 3 Inspector)',
    repo_root=Path('.'),
    now=datetime.now(ZoneInfo('Asia/Seoul')),
)
print(f'F-D2-EXCEPTION-02 Wave 3 supplement written — {len(commits)} commits, {len(scope_files)} files')
"
```

(Plan 03 Tasks 1~6 commits 도 Plan 02 와 동일 marker convention — subject 에 `[plan-03]` 포함, 예: `feat(12-03): Structural Inspector 3 v1.1 migration [plan-03]`.)

완료 후 `grep -c "F-D2-EXCEPTION-02 — Wave" .claude/failures/FAILURES.md` = 2 (Wave 2 + Wave 3 각 1 entry).
  </action>
  <verify>
    <automated>py -3.11 -m pytest tests/phase12/test_agent_md_schema.py -v 2>&1 | tail -30; grep -c "F-D2-EXCEPTION-02 — Wave" .claude/failures/FAILURES.md; py -3.11 -m pytest tests/phase04/ -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `py -3.11 -m pytest tests/phase12/test_agent_md_schema.py -v` exits 0 with ≥ 36 passed (3 collective + 14 producer + 17 inspector + 3 structural-no-progressive-disclosure = 37, ≥ 36 tolerance)
    - `grep -c "F-D2-EXCEPTION-02 — Wave" .claude/failures/FAILURES.md` = 2 (Wave 2 + Wave 3 entries)
    - `py -3.11 scripts/validate/verify_agent_md_schema.py --all` exit 0
    - `py -3.11 -m pytest tests/phase04/ -q` exits 0 (244 passed)
    - `wc -l .claude/failures/FAILURES.md` ≤ 500 (FAIL-PROTO-01 pre-implementation 호환)
    - **Issue #1 regression coverage**: `test_structural_inspector_no_progressive_disclosure` 3 parametrized tests PASS (Plan 04 reciprocity invariant guard)
  </acceptance_criteria>
  <done>30/30 AGENT.md migration 완결. pytest 36+ GREEN. FAIL-PROTO-02 Wave 2 + Wave 3 entries 기록. Issue #1 reciprocity invariant 이 pytest regression 으로 lock.</done>
</task>

</tasks>

<verification>
```bash
py -3.11 scripts/validate/verify_agent_md_schema.py --all                # exit 0 (30/30)
py -3.11 -m pytest tests/phase12/ -v                                     # ≥ 36 passed
py -3.11 -m pytest tests/phase04/ -q                                     # 244 passed
py -3.11 -m pytest tests/phase10/test_skill_patch_counter.py -q          # 12 passed
py -3.11 -m pytest tests/phase11/ -q                                     # 36 passed
grep -c "F-D2-EXCEPTION-02 — Wave" .claude/failures/FAILURES.md          # 2
wc -l .claude/failures/FAILURES.md                                       # ≤ 500

# Issue #1 reciprocity guard (per Structural 3):
for f in ins-schema-integrity ins-timing-consistency ins-blueprint-compliance; do
  grep -c "progressive-disclosure" .claude/agents/inspectors/structural/$f/AGENT.md
done | sort -u                                                            # only "0"

# Issue #2 D-A1-03 literal (per 17 Inspector):
for cat in structural content style compliance technical media; do
  for d in .claude/agents/inspectors/$cat/*/; do
    grep -c "매 호출마다 전수 읽기, 샘플링 금지" "$d/AGENT.md" 2>/dev/null
  done
done | sort -u                                                            # output only "1" or ">1", no "0"
```
</verification>

<success_criteria>
1. 17 Inspector AGENT.md v1.1 승격 — 5 XML 블록 + RUB-06 역방향 mirror + body prose 보존
2. 30/30 verify_agent_md_schema.py --all exit 0 — Phase 12 AGENT-STD-01 SC#1 완결
3. test_agent_md_schema.py parametrized 14+17+3 = 34 tests + 3 collective tests GREEN
4. Phase 4 GAN_CLEAN 17/17 regression 무회귀 — producer/inspector 내부 용어 유출 없음
5. FAILURES.md F-D2-EXCEPTION-02 Wave 2 + Wave 3 entry 존재 (idempotent)
6. maxTurns RUB-05 exception 보존 — ins-factcheck=10, ins-tone-brand=5
7. **Issue #1 fix — Structural 3 AGENT.md 에 `progressive-disclosure` literal 부재 (Plan 04 matrix reciprocity SSOT 일치) + pytest regression guard**
8. **Issue #2 fix — 17 Inspector 전원 AGENT.md 에 '매 호출마다 전수 읽기, 샘플링 금지' 한국어 literal (D-A1-03, per-task acceptance_criteria 로 fast-fail 강제)**
</success_criteria>

<output>
After completion, create `.planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-03-SUMMARY.md`.

대표님께 보고드립니다. 17 Inspector AGENT.md v1.1 migration 완결. 30/30 AGENT.md 가 표준 5-block schema 준수 + RUB-06 양방향 GAN 분리 미러 구조적 보장. Plan 04 matrix SSOT 와 Structural 3 의 progressive-disclosure reciprocity 정합 (Issue #1 fix). Phase 4 GAN_CLEAN 17/17 무회귀 검증 완료. AGENT-STD-01 SC#1 완결.
</output>
