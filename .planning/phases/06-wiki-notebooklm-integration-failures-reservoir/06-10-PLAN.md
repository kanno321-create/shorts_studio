---
phase: 06-wiki-notebooklm-integration-failures-reservoir
plan: 10
type: execute
wave: 4
depends_on: [01, 02]
files_modified:
  - .claude/agents/inspectors/content/ins-factcheck/AGENT.md
  - .claude/agents/inspectors/content/ins-korean-naturalness/AGENT.md
  - .claude/agents/inspectors/content/ins-narrative-quality/AGENT.md
  - .claude/agents/inspectors/style/ins-thumbnail-hook/AGENT.md
  - .claude/agents/inspectors/technical/ins-audio-quality/AGENT.md
  - .claude/agents/inspectors/technical/ins-render-integrity/AGENT.md
  - .claude/agents/inspectors/technical/ins-subtitle-alignment/AGENT.md
  - .claude/agents/producers/director/AGENT.md
  - .claude/agents/producers/metadata-seo/AGENT.md
  - .claude/agents/producers/niche-classifier/AGENT.md
  - .claude/agents/producers/researcher/AGENT.md
  - .claude/agents/producers/scene-planner/AGENT.md
  - .claude/agents/producers/scripter/AGENT.md
  - .claude/agents/producers/shot-planner/AGENT.md
  - .claude/agents/producers/trend-collector/AGENT.md
  - tests/phase06/test_agent_prompt_wiki_refs.py
  - tests/phase06/test_agent_prompt_byte_diff.py
  - .planning/phases/06-wiki-notebooklm-integration-failures-reservoir/phase06_agent_prompt_delta.md
  - .planning/phases/06-wiki-notebooklm-integration-failures-reservoir/phase06_agents_before.txt
  - .planning/phases/06-wiki-notebooklm-integration-failures-reservoir/phase06_agents_after.txt
autonomous: true
requirements:
  - WIKI-05
user_setup: []

must_haves:
  truths:
    - "15 AGENT.md files surface-audited in RESEARCH Area 9 are the complete target set; other 17 of 32 agents are NOT touched by this plan"
    - "Every Phase 6 placeholder string (Phase 6 채움, Phase 6 wiring, Phase 6 Continuity Bible에서 정의, Phase 6에서 정의) is removed from the 15 files"
    - "Each placeholder is replaced with exactly one concrete @wiki/shorts/<category>/<node>.md reference resolving to a status=ready node from Plan 02"
    - "All @wiki/shorts/... references introduced pass validate_all_agent_refs (file exists + status=ready)"
    - "sha256 manifest files phase06_agents_before.txt and phase06_agents_after.txt record byte-level diff coverage"
    - "phase06_agent_prompt_delta.md table records per-file line count delta (wc -l before/after per D-18)"
    - "The other 17 of 32 agents are byte-identical before and after this plan (regression guard)"
    - "No agent prompt references @wiki/shorts/<any path>.md pointing at a status=stub or nonexistent node"
  artifacts:
    - path: "tests/phase06/test_agent_prompt_wiki_refs.py"
      provides: "WIKI-05 unit: validate_all_agent_refs across .claude/agents returns zero problems"
      min_lines: 80
    - path: "tests/phase06/test_agent_prompt_byte_diff.py"
      provides: "Integration: 17 non-target agents unchanged; 15 target agents have no Phase 6 placeholder strings"
      min_lines: 80
    - path: ".planning/phases/06-wiki-notebooklm-integration-failures-reservoir/phase06_agent_prompt_delta.md"
      provides: "Per-file line delta table documenting edit scope (D-18 requirement)"
      min_lines: 30
    - path: ".planning/phases/06-wiki-notebooklm-integration-failures-reservoir/phase06_agents_before.txt"
      provides: "sha256sum manifest of all 32 AGENT.md files BEFORE this plan (snapshot before edit)"
    - path: ".planning/phases/06-wiki-notebooklm-integration-failures-reservoir/phase06_agents_after.txt"
      provides: "sha256sum manifest of all 32 AGENT.md files AFTER this plan"
  key_links:
    - from: ".claude/agents/producers/scripter/AGENT.md"
      to: "wiki/shorts/continuity_bible/channel_identity.md"
      via: "@wiki/shorts/continuity_bible/channel_identity.md reference in prompt body"
      pattern: "@wiki/shorts/continuity_bible/channel_identity\\.md"
    - from: ".claude/agents/inspectors/style/ins-thumbnail-hook/AGENT.md"
      to: "wiki/shorts/continuity_bible/channel_identity.md"
      via: "@wiki/shorts/continuity_bible/channel_identity.md for color palette check"
      pattern: "@wiki/shorts/continuity_bible"
---

<objective>
Wave 4 AGENT PROMPT MASS UPDATE — Replace the Phase 6 placeholder strings in 15 of 32 Phase 4 agent files with real `@wiki/shorts/<category>/<node>.md` references per D-3 + D-18. Produce `phase06_agents_before.txt` / `phase06_agents_after.txt` sha256 manifests and a `phase06_agent_prompt_delta.md` table documenting per-file line count changes.

Purpose: ROADMAP Phase 6 SC #1 — "studios/shorts/wiki/에 ... 도메인 노드 ... 가 존재하며 `@wiki/shorts/xxx.md` 참조가 에이전트 프롬프트에서 작동한다". Per RESEARCH Area 9 grep audit, exactly 15 agents currently contain "Phase 6" placeholder strings that need substitution. The other 17 agents are complete as-shipped from Phase 4 and MUST remain byte-identical — this plan's scope is strictly surgical.

Output: 15 edited AGENT.md + 3 audit artifacts + 2 test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-wiki-notebooklm-integration-failures-reservoir/06-CONTEXT.md
@.planning/phases/06-wiki-notebooklm-integration-failures-reservoir/06-RESEARCH.md
@scripts/wiki/link_validator.py
@wiki/continuity_bible/channel_identity.md
@wiki/algorithm/ranking_factors.md
@wiki/kpi/retention_3second_hook.md
@wiki/render/remotion_kling_stack.md
@wiki/ypp/entry_conditions.md

<interfaces>
## 15 target agent files (RESEARCH Area 9 lines 1041-1057 — surface audit 2026-04-19)
```
.claude/agents/inspectors/content/ins-factcheck/AGENT.md
.claude/agents/inspectors/content/ins-korean-naturalness/AGENT.md
.claude/agents/inspectors/content/ins-narrative-quality/AGENT.md
.claude/agents/inspectors/style/ins-thumbnail-hook/AGENT.md
.claude/agents/inspectors/technical/ins-audio-quality/AGENT.md
.claude/agents/inspectors/technical/ins-render-integrity/AGENT.md
.claude/agents/inspectors/technical/ins-subtitle-alignment/AGENT.md
.claude/agents/producers/director/AGENT.md
.claude/agents/producers/metadata-seo/AGENT.md
.claude/agents/producers/niche-classifier/AGENT.md
.claude/agents/producers/researcher/AGENT.md
.claude/agents/producers/scene-planner/AGENT.md
.claude/agents/producers/scripter/AGENT.md
.claude/agents/producers/shot-planner/AGENT.md
.claude/agents/producers/trend-collector/AGENT.md
```

## Placeholder patterns to eliminate (RESEARCH Area 9 lines 1059-1065)
- `Phase 6 채움` / `(Phase 6 채움)` / `Phase 6에서 채워짐`
- `Phase 6 wiring` / `(Phase 6 wiring)`
- `Phase 6 Continuity Bible에서 정의` / `Phase 6에서 정의`
- `Phase 6 Continuity Bible 임시 proxy`
- `wiki/continuity_bible/MOC.md` (bare, without @ prefix)

## Replacement references (D-3 format)
- Continuity/channel identity → `@wiki/shorts/continuity_bible/channel_identity.md`
- Ranking/algorithm → `@wiki/shorts/algorithm/ranking_factors.md`
- 3초 retention / KPI → `@wiki/shorts/kpi/retention_3second_hook.md`
- Render stack / Remotion+Kling → `@wiki/shorts/render/remotion_kling_stack.md`
- YPP / monetization → `@wiki/shorts/ypp/entry_conditions.md`

Executor MUST read each agent's placeholder context to choose the right ref. Most inspectors referencing continuity/channel identity/color palette → `channel_identity.md`. Producers referencing retention/hook → `retention_3second_hook.md`. Etc.

## Per-file MultiEdit strategy (RESEARCH Area 9 lines 1088-1093)
- ONE MultiEdit per agent file; each edit = exact old_string → new_string pair
- NO global sed/replace — cp949 + encoding risk on Windows
- NO multi-file batch in one tool call
- After each file, run `python scripts/wiki/link_validator.py` (or test at end)

## Byte-diff strategy (RESEARCH Area 9 lines 1080-1086)
Before: `find .claude/agents -name "AGENT.md" | xargs sha256sum > phase06_agents_before.txt`
After: `find .claude/agents -name "AGENT.md" | xargs sha256sum > phase06_agents_after.txt`
Diff: 15 files changed hash, 17 identical.

## Length delta table (phase06_agent_prompt_delta.md)
| Agent | Lines before | Lines after | Delta | Refs added |
|-------|-------------:|------------:|------:|------------|
| ins-factcheck | N | M | +X | @wiki/shorts/continuity_bible/channel_identity.md |
| ...           | ... | ... | ... | ... |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Snapshot before + edit 15 AGENT.md files + snapshot after + delta table</name>
  <files>
    .planning/phases/06-wiki-notebooklm-integration-failures-reservoir/phase06_agents_before.txt,
    .claude/agents/inspectors/content/ins-factcheck/AGENT.md,
    .claude/agents/inspectors/content/ins-korean-naturalness/AGENT.md,
    .claude/agents/inspectors/content/ins-narrative-quality/AGENT.md,
    .claude/agents/inspectors/style/ins-thumbnail-hook/AGENT.md,
    .claude/agents/inspectors/technical/ins-audio-quality/AGENT.md,
    .claude/agents/inspectors/technical/ins-render-integrity/AGENT.md,
    .claude/agents/inspectors/technical/ins-subtitle-alignment/AGENT.md,
    .claude/agents/producers/director/AGENT.md,
    .claude/agents/producers/metadata-seo/AGENT.md,
    .claude/agents/producers/niche-classifier/AGENT.md,
    .claude/agents/producers/researcher/AGENT.md,
    .claude/agents/producers/scene-planner/AGENT.md,
    .claude/agents/producers/scripter/AGENT.md,
    .claude/agents/producers/shot-planner/AGENT.md,
    .claude/agents/producers/trend-collector/AGENT.md,
    .planning/phases/06-wiki-notebooklm-integration-failures-reservoir/phase06_agents_after.txt,
    .planning/phases/06-wiki-notebooklm-integration-failures-reservoir/phase06_agent_prompt_delta.md
  </files>
  <read_first>
    - .planning/phases/06-wiki-notebooklm-integration-failures-reservoir/06-CONTEXT.md §D-3 (reference format) + §D-18 (mass update scope + length delta requirement)
    - .planning/phases/06-wiki-notebooklm-integration-failures-reservoir/06-RESEARCH.md §Area 9 lines 1036-1121 (full strategy + examples + safety)
    - Each of the 15 target AGENT.md files BEFORE editing (grep for Phase 6 placeholders to locate edit points)
    - wiki/continuity_bible/channel_identity.md + other ready nodes from Plan 02 (replacement targets)
    - scripts/wiki/link_validator.py (validate refs post-edit)
  </read_first>
  <action>
**Step 1: Snapshot before**

Create `.planning/phases/06-wiki-notebooklm-integration-failures-reservoir/phase06_agents_before.txt` by running:
```bash
cd studios/shorts && find .claude/agents -name "AGENT.md" -type f -exec sha256sum {} \; | sort > .planning/phases/06-wiki-notebooklm-integration-failures-reservoir/phase06_agents_before.txt
```
(Or equivalent Python: `hashlib.sha256(p.read_bytes()).hexdigest()` per file, sorted.)

**Step 2: Edit each of 15 files — one MultiEdit per file**

For EACH of the 15 listed agent files:

1. Read the file fully.
2. Identify every line containing a Phase 6 placeholder (grep: `Phase 6 채움|Phase 6 wiring|Phase 6 Continuity Bible|Phase 6에서 정의|Phase 6에서 채워짐`).
3. For each placeholder line, choose the appropriate replacement reference per context:
   - Mentions of continuity / channel identity / color palette / visual style → `@wiki/shorts/continuity_bible/channel_identity.md`
   - Mentions of ranking / algorithm / retention signal → `@wiki/shorts/algorithm/ranking_factors.md`
   - Mentions of 3초 hook / retention / KPI → `@wiki/shorts/kpi/retention_3second_hook.md`
   - Mentions of render / Remotion / Kling / Runway stack → `@wiki/shorts/render/remotion_kling_stack.md`
   - Mentions of YPP / monetization → `@wiki/shorts/ypp/entry_conditions.md`
4. Apply MultiEdit with exact old_string → new_string pairs. Each old_string must be unique in the file (include enough surrounding text for uniqueness).
5. Verify the edited file passes D-17 reference validation: `python -c "from scripts.wiki.link_validator import find_refs_in_file; from pathlib import Path; print(find_refs_in_file(Path('.claude/agents/...')))"` should show real `@wiki/shorts/...md` entries.

Example per-file edit patterns (RESEARCH Area 9 line 1068-1075):
```
OLD: `wiki/continuity_bible/MOC.md` — Continuity Bible (Phase 6 채움).
NEW: `@wiki/shorts/continuity_bible/channel_identity.md` — Continuity Bible 5 구성요소 (D-10).
```
```
OLD: **채널 일관성** — Phase 6 Continuity Bible에서 정의될 색상 팔레트와 thumbnail 팔레트가 일치하는지 확인.
NEW: **채널 일관성** — `@wiki/shorts/continuity_bible/channel_identity.md` 색상 팔레트와 thumbnail 팔레트 일치 검증 (D-10).
```
```
OLD: | ... | no (Phase 6) | Phase 6 wiring |
NEW: | ... | yes | `@wiki/shorts/continuity_bible/channel_identity.md` |
```

**Step 3: Snapshot after**

Create `.planning/phases/06-wiki-notebooklm-integration-failures-reservoir/phase06_agents_after.txt` via the same sha256sum procedure as Step 1.

**Step 4: Create delta table**

Write `.planning/phases/06-wiki-notebooklm-integration-failures-reservoir/phase06_agent_prompt_delta.md`:
```markdown
# Phase 6 Plan 10: Agent Prompt Delta Manifest

> D-18: mass update of 15 agent prompts; other 17 of 32 agents MUST be byte-identical.

## Per-File Line Delta

| Agent | Lines before | Lines after | Delta | References Added |
|-------|-------------:|------------:|------:|------------------|
| inspectors/content/ins-factcheck | N1 | M1 | +X1 | refs... |
| inspectors/content/ins-korean-naturalness | N2 | M2 | +X2 | refs... |
| inspectors/content/ins-narrative-quality | N3 | M3 | +X3 | refs... |
| inspectors/style/ins-thumbnail-hook | N4 | M4 | +X4 | `@wiki/shorts/continuity_bible/channel_identity.md` |
| inspectors/technical/ins-audio-quality | N5 | M5 | +X5 | refs... |
| inspectors/technical/ins-render-integrity | N6 | M6 | +X6 | `@wiki/shorts/render/remotion_kling_stack.md` |
| inspectors/technical/ins-subtitle-alignment | N7 | M7 | +X7 | refs... |
| producers/director | N8 | M8 | +X8 | refs... |
| producers/metadata-seo | N9 | M9 | +X9 | refs... |
| producers/niche-classifier | N10 | M10 | +X10 | refs... |
| producers/researcher | N11 | M11 | +X11 | refs... |
| producers/scene-planner | N12 | M12 | +X12 | `@wiki/shorts/continuity_bible/channel_identity.md` |
| producers/scripter | N13 | M13 | +X13 | refs... |
| producers/shot-planner | N14 | M14 | +X14 | `@wiki/shorts/continuity_bible/channel_identity.md` |
| producers/trend-collector | N15 | M15 | +X15 | refs... |
| **Total** | - | - | **Σ** | 15 files / 5 ready nodes |

## Byte Identity — Other 17 Agents (MUST be unchanged)

Comparison of `phase06_agents_before.txt` vs `phase06_agents_after.txt`:
- 15 hashes CHANGED (listed above)
- 17 hashes IDENTICAL:
  - (enumerate all 17 untouched agents here — inspectors/structural 3 + inspectors/style 2 + inspectors/compliance 3 + inspectors/media 2 + producers-support 5 + supervisor 1 + + harvest-importer 1 = 17)

## Sanity checks
- `grep -r "Phase 6 채움" .claude/agents` returns 0 hits
- `grep -r "Phase 6 wiring" .claude/agents` returns 0 hits
- `grep -rE "@wiki/shorts/[\w_/]+\.md" .claude/agents` returns >= 15 hits
- `python -c "from scripts.wiki.link_validator import validate_all_agent_refs; from pathlib import Path; problems = validate_all_agent_refs(Path('.claude/agents'), Path('wiki')); assert not problems, problems"` exits 0
```

Fill in actual values for N*, M*, X* by reading each file's line count before/after.

CRITICAL:
- Do NOT use a single MultiEdit to touch multiple files. One MultiEdit per file (RESEARCH line 1092).
- Do NOT run `sed -i` on Windows (cp949 risk per RESEARCH line 1090).
- Do NOT modify agents NOT in the 15-file list — they would show as hash-changed in after.txt (regression red flag).
- Do NOT touch MOC.md files (Plan 02 scope, already done).
  </action>
  <verify>
    <automated>python -c "from scripts.wiki.link_validator import validate_all_agent_refs; from pathlib import Path; problems = validate_all_agent_refs(Path('.claude/agents'), Path('wiki')); assert not problems, f'Broken refs: {problems[:5]}'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/06-wiki-notebooklm-integration-failures-reservoir/phase06_agents_before.txt` exits 0
    - `test -f .planning/phases/06-wiki-notebooklm-integration-failures-reservoir/phase06_agents_after.txt` exits 0
    - `test -f .planning/phases/06-wiki-notebooklm-integration-failures-reservoir/phase06_agent_prompt_delta.md` exits 0
    - `grep -rE "Phase 6 채움|Phase 6 wiring|Phase 6 Continuity Bible에서 정의" .claude/agents | wc -l` outputs `0`
    - `grep -rE "@wiki/shorts/[\w_/]+\.md" .claude/agents | wc -l` outputs `>= 15`
    - 15 changed hashes + 17 identical hashes in diff of before.txt vs after.txt
    - `python -c "from scripts.wiki.link_validator import validate_all_agent_refs; from pathlib import Path; assert validate_all_agent_refs(Path('.claude/agents'), Path('wiki')) == []"` exits 0
  </acceptance_criteria>
  <done>All 15 agents updated; 17 unchanged; every new @wiki/shorts/*.md ref resolves to status=ready target; delta manifest + sha256 snapshots committed.</done>
</task>

<task type="auto">
  <name>Task 2: tests/phase06/test_agent_prompt_wiki_refs.py + test_agent_prompt_byte_diff.py</name>
  <files>
    tests/phase06/test_agent_prompt_wiki_refs.py,
    tests/phase06/test_agent_prompt_byte_diff.py
  </files>
  <read_first>
    - scripts/wiki/link_validator.py (Plan 01 API)
    - .planning/phases/06-wiki-notebooklm-integration-failures-reservoir/phase06_agents_before.txt + phase06_agents_after.txt (Task 1 outputs)
    - .planning/phases/06-wiki-notebooklm-integration-failures-reservoir/06-RESEARCH.md §Area 9 lines 1097-1121 (test patterns)
  </read_first>
  <action>
**File 1: tests/phase06/test_agent_prompt_wiki_refs.py (~100 lines)**

```python
from __future__ import annotations
import re
from pathlib import Path

import pytest

from scripts.wiki.frontmatter import parse_frontmatter
from scripts.wiki.link_validator import validate_all_agent_refs


REF_RE = re.compile(r"@wiki/shorts/([\w\-\_/]+\.md)")


# The 15 agents Plan 10 targeted (RESEARCH Area 9 line 1041-1057)
PLAN_10_TARGET_AGENTS = [
    "inspectors/content/ins-factcheck",
    "inspectors/content/ins-korean-naturalness",
    "inspectors/content/ins-narrative-quality",
    "inspectors/style/ins-thumbnail-hook",
    "inspectors/technical/ins-audio-quality",
    "inspectors/technical/ins-render-integrity",
    "inspectors/technical/ins-subtitle-alignment",
    "producers/director",
    "producers/metadata-seo",
    "producers/niche-classifier",
    "producers/researcher",
    "producers/scene-planner",
    "producers/scripter",
    "producers/shot-planner",
    "producers/trend-collector",
]


def test_no_phase6_placeholder_anywhere_in_agents(repo_root: Path):
    forbidden_phrases = [
        "Phase 6 채움",
        "Phase 6 wiring",
        "Phase 6 Continuity Bible에서 정의",
        "Phase 6에서 정의",
        "Phase 6에서 채워짐",
    ]
    hits: list[tuple[Path, str]] = []
    for agent_md in (repo_root / ".claude" / "agents").rglob("AGENT.md"):
        text = agent_md.read_text(encoding="utf-8", errors="replace")
        for phrase in forbidden_phrases:
            if phrase in text:
                hits.append((agent_md, phrase))
    assert hits == [], f"Remaining Phase 6 placeholders: {hits}"


def test_every_target_agent_has_wiki_ref(repo_root: Path):
    """Every Plan 10 target agent has at least 1 @wiki/shorts/<path>.md reference."""
    agents_root = repo_root / ".claude" / "agents"
    no_refs = []
    for rel in PLAN_10_TARGET_AGENTS:
        agent_md = agents_root / rel / "AGENT.md"
        assert agent_md.exists(), f"Target agent missing: {agent_md}"
        text = agent_md.read_text(encoding="utf-8", errors="replace")
        refs = REF_RE.findall(text)
        if not refs:
            no_refs.append(rel)
    assert no_refs == [], f"Target agents without @wiki/shorts refs: {no_refs}"


def test_validate_all_agent_refs_clean(repo_root: Path):
    """Plan 01's validator: every @wiki/shorts/...md resolves + target is status=ready."""
    problems = validate_all_agent_refs(
        repo_root / ".claude" / "agents",
        repo_root / "wiki",
    )
    assert problems == [], f"Broken refs: {problems[:5]}"


def test_at_least_15_refs_across_all_agents(repo_root: Path):
    agents_root = repo_root / ".claude" / "agents"
    total_refs = 0
    for agent_md in agents_root.rglob("AGENT.md"):
        text = agent_md.read_text(encoding="utf-8", errors="replace")
        total_refs += len(REF_RE.findall(text))
    assert total_refs >= 15, f"Expected >=15 @wiki/shorts refs, found {total_refs}"


def test_all_referenced_nodes_have_ready_status(repo_root: Path):
    agents_root = repo_root / ".claude" / "agents"
    wiki_root = repo_root / "wiki"
    for agent_md in agents_root.rglob("AGENT.md"):
        text = agent_md.read_text(encoding="utf-8", errors="replace")
        for ref_path in REF_RE.findall(text):
            target = wiki_root / ref_path
            assert target.exists(), f"{agent_md}: ref @wiki/shorts/{ref_path} missing"
            fm = parse_frontmatter(target)
            assert fm.get("status") == "ready", (
                f"{agent_md} refs @wiki/shorts/{ref_path} with status={fm.get('status')}"
            )


def test_no_bare_wiki_path_without_at_prefix(repo_root: Path):
    """D-3: paths must use @wiki/shorts/ prefix, not bare wiki/... or wiki/shorts/..."""
    agents_root = repo_root / ".claude" / "agents"
    bare_refs = []
    bare_re = re.compile(r"(?<!@)wiki/shorts/[\w\-\_/]+\.md")
    for agent_md in agents_root.rglob("AGENT.md"):
        text = agent_md.read_text(encoding="utf-8", errors="replace")
        for m in bare_re.finditer(text):
            bare_refs.append((agent_md, m.group(0)))
    assert bare_refs == [], f"Bare wiki/shorts refs (missing @ prefix) found: {bare_refs[:5]}"
```

**File 2: tests/phase06/test_agent_prompt_byte_diff.py (~90 lines)**

```python
from __future__ import annotations
import hashlib
from pathlib import Path

import pytest


_REPO = Path(__file__).resolve().parents[2]
AGENTS = _REPO / ".claude" / "agents"
BEFORE = _REPO / ".planning" / "phases" / "06-wiki-notebooklm-integration-failures-reservoir" / "phase06_agents_before.txt"
AFTER = _REPO / ".planning" / "phases" / "06-wiki-notebooklm-integration-failures-reservoir" / "phase06_agents_after.txt"


PLAN_10_TARGET_AGENTS = {
    "inspectors/content/ins-factcheck",
    "inspectors/content/ins-korean-naturalness",
    "inspectors/content/ins-narrative-quality",
    "inspectors/style/ins-thumbnail-hook",
    "inspectors/technical/ins-audio-quality",
    "inspectors/technical/ins-render-integrity",
    "inspectors/technical/ins-subtitle-alignment",
    "producers/director",
    "producers/metadata-seo",
    "producers/niche-classifier",
    "producers/researcher",
    "producers/scene-planner",
    "producers/scripter",
    "producers/shot-planner",
    "producers/trend-collector",
}


def _parse_manifest(path: Path) -> dict[str, str]:
    """Parse sha256sum output format: '<hash>  <path>'. Return {rel_path: hash}."""
    result: dict[str, str] = {}
    if not path.exists():
        return result
    for line in path.read_text(encoding="utf-8").splitlines():
        line = line.strip()
        if not line:
            continue
        parts = line.split(None, 1)
        if len(parts) != 2:
            continue
        result[parts[1].lstrip("*")] = parts[0]
    return result


def _rel_agent_key(path: str) -> str:
    """Normalize '.claude/agents/inspectors/content/ins-factcheck/AGENT.md' -> 'inspectors/content/ins-factcheck'."""
    p = path.replace("\\", "/")
    if ".claude/agents/" in p:
        p = p.split(".claude/agents/", 1)[1]
    if p.endswith("/AGENT.md"):
        p = p[:-len("/AGENT.md")]
    return p


def test_manifests_exist():
    assert BEFORE.exists(), "phase06_agents_before.txt missing — Task 1 snapshot"
    assert AFTER.exists(), "phase06_agents_after.txt missing — Task 1 snapshot"


def test_15_target_agents_hash_changed():
    before = _parse_manifest(BEFORE)
    after = _parse_manifest(AFTER)
    changed_targets = []
    for raw_path, h_before in before.items():
        rel = _rel_agent_key(raw_path)
        if rel in PLAN_10_TARGET_AGENTS:
            h_after = after.get(raw_path)
            if h_after and h_after != h_before:
                changed_targets.append(rel)
    # At least 10 of 15 should have changed (some may have no placeholder so unchanged)
    # but the common case per RESEARCH is all 15 changed
    assert len(changed_targets) >= 10, (
        f"Only {len(changed_targets)} target agents changed, expected >=10"
    )


def test_17_nontarget_agents_byte_identical():
    before = _parse_manifest(BEFORE)
    after = _parse_manifest(AFTER)
    drift = []
    for raw_path, h_before in before.items():
        rel = _rel_agent_key(raw_path)
        if rel in PLAN_10_TARGET_AGENTS:
            continue
        h_after = after.get(raw_path)
        if h_after and h_after != h_before:
            drift.append(rel)
    assert drift == [], f"Non-target agents changed (regression): {drift}"


def test_total_agents_is_32():
    before = _parse_manifest(BEFORE)
    assert len(before) == 32, f"Expected 32 AGENT.md files pre-plan, found {len(before)}"


def test_delta_manifest_exists():
    delta = _REPO / ".planning" / "phases" / "06-wiki-notebooklm-integration-failures-reservoir" / "phase06_agent_prompt_delta.md"
    assert delta.exists()
```
  </action>
  <verify>
    <automated>python -m pytest tests/phase06/test_agent_prompt_wiki_refs.py tests/phase06/test_agent_prompt_byte_diff.py -q --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/phase06/test_agent_prompt_wiki_refs.py -q --no-cov` exits 0
    - `python -m pytest tests/phase06/test_agent_prompt_byte_diff.py -q --no-cov` exits 0
    - `grep -cE "^def test_" tests/phase06/test_agent_prompt_wiki_refs.py` outputs `>= 6`
    - `grep -cE "^def test_" tests/phase06/test_agent_prompt_byte_diff.py` outputs `>= 4`
    - Before/after manifest files exist and parse correctly
    - `grep -rE "Phase 6 채움|Phase 6 wiring" .claude/agents | wc -l` outputs `0`
  </acceptance_criteria>
  <done>10+ tests green. Phase 6 placeholder elimination proven. 17 untargeted agents byte-identical proven. All @wiki/shorts/ refs resolve to status=ready.</done>
</task>

</tasks>

<verification>
1. `grep -rE "Phase 6 채움|Phase 6 wiring|Phase 6 Continuity Bible에서 정의" .claude/agents` returns 0 matches
2. `python -c "from scripts.wiki.link_validator import validate_all_agent_refs; from pathlib import Path; problems = validate_all_agent_refs(Path('.claude/agents'), Path('wiki')); assert problems == []"` exits 0
3. 15 hashes changed + 17 hashes identical between before.txt and after.txt
4. phase06_agent_prompt_delta.md exists with line delta table
5. `python -m pytest tests/phase06/test_agent_prompt_wiki_refs.py tests/phase06/test_agent_prompt_byte_diff.py -q --no-cov` exits 0
</verification>

<success_criteria>
- WIKI-05 @wiki/shorts/ reference format enforced
- D-18 15-agent mass update complete — all placeholders replaced with valid ready-node refs
- 17 non-target agents byte-identical (regression guard)
- Length delta manifest recorded per D-18 final paragraph
- Plan 11 final verification can grep + validate and find 0 issues
</success_criteria>

<output>
After completion, create `.planning/phases/06-wiki-notebooklm-integration-failures-reservoir/06-10-SUMMARY.md` documenting:
- 15 files edited (before/after line deltas summarized)
- sha256 before/after counts (15 changed + 17 identical)
- Per-category ref distribution (how many agents ref channel_identity.md vs retention_3second_hook.md etc.)
- Test counts (expected >=10)
- link_validator output after edits (expected: 0 problems)
- Commit hash
</output>
</content>
</invoke>