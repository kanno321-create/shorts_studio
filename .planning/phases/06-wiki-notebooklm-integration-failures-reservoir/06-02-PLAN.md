---
phase: 06-wiki-notebooklm-integration-failures-reservoir
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - wiki/algorithm/ranking_factors.md
  - wiki/ypp/entry_conditions.md
  - wiki/render/remotion_kling_stack.md
  - wiki/kpi/retention_3second_hook.md
  - wiki/continuity_bible/channel_identity.md
  - wiki/algorithm/MOC.md
  - wiki/ypp/MOC.md
  - wiki/render/MOC.md
  - wiki/kpi/MOC.md
  - wiki/continuity_bible/MOC.md
  - tests/phase06/test_wiki_nodes_ready.py
  - tests/phase06/test_moc_linkage.py
  - tests/phase06/test_continuity_bible_node.py
autonomous: true
requirements:
  - WIKI-01
  - WIKI-02
user_setup: []

must_haves:
  truths:
    - "wiki/algorithm/ranking_factors.md exists with status: ready frontmatter describing 3-sec hook + completion rate + CTR + rewatch ratio per D-16 Korean senior skew"
    - "wiki/ypp/entry_conditions.md exists with status: ready documenting 1000 subs + 10M views/yr Shorts pathway + Korean RPM ~$0.20"
    - "wiki/render/remotion_kling_stack.md exists with status: ready describing Remotion v4 + Kling 2.6 Pro primary + Runway Gen-3 Alpha Turbo backup + 720p Low-Res First + Shotstack composite"
    - "wiki/kpi/retention_3second_hook.md exists with status: ready declaring >60% retention at t=3s target + YouTube Analytics audienceRetention measurement"
    - "wiki/continuity_bible/channel_identity.md exists with status: ready AND covers all 5 D-10 components (color palette, camera lens, visual style, audience profile, bgm mood)"
    - "All 5 category MOC.md files have a Planned Nodes checkbox flipped from [ ] to [x] for the node promoted in this plan"
    - "python scripts/validate/verify_wiki_frontmatter.py --root wiki --allow-scaffold exits 0 (existing scaffold MOC + new ready nodes both valid)"
  artifacts:
    - path: "wiki/algorithm/ranking_factors.md"
      provides: "WIKI-01 algorithm ready node — ranking signals (완주율, 3s retention, CTR, rewatch)"
      contains: "status: ready"
      min_lines: 40
    - path: "wiki/ypp/entry_conditions.md"
      provides: "WIKI-01 ypp ready node (1000 subs + 10M views/yr Shorts pathway)"
      contains: "status: ready"
      min_lines: 35
    - path: "wiki/render/remotion_kling_stack.md"
      provides: "WIKI-01 render ready node (Remotion + Kling primary + Runway backup + Shotstack)"
      contains: "status: ready"
      min_lines: 40
    - path: "wiki/kpi/retention_3second_hook.md"
      provides: "WIKI-01 kpi ready node (3s retention >60% target)"
      contains: "status: ready"
      min_lines: 35
    - path: "wiki/continuity_bible/channel_identity.md"
      provides: "WIKI-02 continuity_bible with all 5 D-10 components documented"
      contains: "5 구성요소"
      min_lines: 60
    - path: "tests/phase06/test_wiki_nodes_ready.py"
      provides: "WIKI-01: 5 categories each have >=1 status=ready node"
      min_lines: 40
    - path: "tests/phase06/test_moc_linkage.py"
      provides: "Integration: every ready node filename appears checked in its category MOC.md"
      min_lines: 40
    - path: "tests/phase06/test_continuity_bible_node.py"
      provides: "WIKI-02: channel_identity.md contains all 5 D-10 components textually"
      min_lines: 40
  key_links:
    - from: "wiki/continuity_bible/channel_identity.md"
      to: "wiki/continuity_bible/MOC.md"
      via: "Obsidian [[MOC]] backlink in ## Related section"
      pattern: "\\[\\[MOC\\]\\]"
    - from: "wiki/algorithm/MOC.md"
      to: "wiki/algorithm/ranking_factors.md"
      via: "Planned Nodes checkbox [x] ranking_factors.md"
      pattern: "\\[x\\].*ranking_factors"
---

<objective>
Wave 1 WIKI CONTENT — Author 5 status=ready nodes (one per D-2 category) and flip the corresponding MOC Planned Nodes checkbox from `[ ]` to `[x]`. Establishes WIKI-01 (Tier 2 domain-specific nodes) + WIKI-02 content layer (channel_identity.md with D-10 5 components — `prefix.json` JSON serialization deferred to Plan 06 since it depends on the ContinuityPrefix pydantic model).

Purpose: Plans 03/04/05 (NotebookLM) and 10 (agent prompt mass update) require at least one `status: ready` node per category so that `@wiki/shorts/<cat>/<node>.md` references in agent prompts resolve. Plan 11 final verification gates on `verify_wiki_frontmatter.py` passing without `--allow-scaffold` for real nodes.

Output: 5 new wiki leaf files + 5 updated MOC.md files + 3 test files proving ready state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-wiki-notebooklm-integration-failures-reservoir/06-CONTEXT.md
@.planning/phases/06-wiki-notebooklm-integration-failures-reservoir/06-RESEARCH.md
@.planning/REQUIREMENTS.md
@wiki/README.md
@wiki/algorithm/MOC.md
@wiki/ypp/MOC.md
@wiki/render/MOC.md
@wiki/kpi/MOC.md
@wiki/continuity_bible/MOC.md
@scripts/wiki/frontmatter.py

<interfaces>
## D-17 frontmatter schema (required for every ready node in this plan)
```
---
category: one of [algorithm, ypp, render, kpi, continuity_bible]
status: ready
tags: [..., shorts]
updated: 2026-04-19
source_notebook: one of [shorts-production-pipeline-bible, naberal-shorts-channel-bible, hardcoded]
---
```

## D-10 Continuity Bible 5 components (MANDATORY in channel_identity.md)
(a) 색상 팔레트 — HEX 3-5색 + warmth scalar
(b) 카메라 렌즈 — 초점거리 (mm) + aperture (f-number)
(c) 시각적 스타일 — one of [photorealistic, cinematic, documentary] LOCKED
(d) 한국 시니어 시청자 특성 — D-16 audience descriptor
(e) BGM 분위기 — one of [ambient, tension, uplift] 3 presets

## Obsidian linkage convention
- Intra-wiki: `[[../category/node]]` or `[[node]]` basename
- MOC backlink: `[[MOC]]` in `## Related` section

## MOC update pattern
Existing:  `- [ ] \`ranking_factors.md\` — description`
After:     `- [x] \`ranking_factors.md\` — description`
MOC frontmatter `status: scaffold` stays as-is (MOC is ToC, not a ready leaf).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 4 non-continuity ready nodes (algorithm/ypp/render/kpi)</name>
  <files>
    wiki/algorithm/ranking_factors.md,
    wiki/ypp/entry_conditions.md,
    wiki/render/remotion_kling_stack.md,
    wiki/kpi/retention_3second_hook.md
  </files>
  <read_first>
    - .planning/phases/06-wiki-notebooklm-integration-failures-reservoir/06-CONTEXT.md §D-17 (line 79) + §D-16 (line 74)
    - .planning/phases/06-wiki-notebooklm-integration-failures-reservoir/06-RESEARCH.md §Area 1 lines 459-496 (5 required ready nodes table + minimal node example)
    - wiki/algorithm/MOC.md, wiki/ypp/MOC.md, wiki/render/MOC.md, wiki/kpi/MOC.md (Planned Nodes per category)
    - .planning/REQUIREMENTS.md KPI-06 (3초 retention >60%, 완주율 >40%, 평균 시청 >25초)
  </read_first>
  <action>
All 4 files share identical D-17 frontmatter shape. Each body is 30-50 lines of factual content derived from Phase 5 research + REQUIREMENTS + CONTEXT D-16. Include `## Related` section with `[[MOC]]` backlink and at least one intra-wiki `[[../category/node]]` link.

**File 1: wiki/algorithm/ranking_factors.md (~45 lines)** — status=ready, category=algorithm, source_notebook=shorts-production-pipeline-bible. Body sections: "핵심 신호 (우선순위 순)" enumerating 완주율 / 3초 retention / CTR / 재시청율 / 보조 신호; "한국 시니어 skew (D-16)" with 채도 낮은 톤 + 빠른 정보 전달 + 존댓말 narration; "측정 소스" naming YouTube Analytics audienceRetention + cardImpressionsRatio; "피해야 할 패턴" referencing AF-7 / AF-11 / AF-2. Related links: [[../kpi/retention_3second_hook]], [[../continuity_bible/channel_identity]], [[MOC]].

**File 2: wiki/ypp/entry_conditions.md (~40 lines)** — status=ready, category=ypp, source_notebook=shorts-production-pipeline-bible. Body: "2026 진입 기준 (Shorts 경로)" — 1000 구독자 + 10M Shorts views / 90-day window; long-form 경로 비적용 note. "Korean RPM" ~$0.20 / 1000 views + 10M views/90d = ~$2000 보수 baseline. "정책 risk" — Reused Content (E-P2, PUB-04), AI disclosure 누락 (COMPLY-03), AF-4 voice cloning. "KPI 트래킹" — 월별 구독자 증가 / 90-day rolling views / 진입률. Related: [[../algorithm/ranking_factors]], [[../kpi/retention_3second_hook]], [[MOC]].

**File 3: wiki/render/remotion_kling_stack.md (~45 lines)** — status=ready, category=render, source_notebook=shorts-production-pipeline-bible. Body: "Stack overview" table (Remotion v4 composition / Kling 2.6 Pro primary I2V / Runway Gen-3 Alpha Turbo backup / Shotstack color+composite). "금지 사항 (hard constraints)" — T2V 금지 (VIDEO-01, D-13), K-pop 직접 사용 금지 (AF-13), Selenium 금지 (AF-8). "Filter chain order (D-17 Phase 5 + D-19 Phase 6)" — 1. continuity_prefix (first) 2. color_grade 3. saturation 4. film_grain. "영상/음성 분리 합성 (ORCH-10)" Typecast → timestamp → Shotstack. "Fallback 샷 (ORCH-12)" — 재생성 루프 3회 초과 시 정지 이미지 + 줌인 for ASSETS/THUMBNAIL gates. Related: [[../continuity_bible/channel_identity]], [[../algorithm/ranking_factors]], [[MOC]].

**File 4: wiki/kpi/retention_3second_hook.md (~40 lines)** — status=ready, category=kpi, source_notebook=shorts-production-pipeline-bible. Body: "목표 (KPI-06)" table (3초 retention >60% / 완주율 >40% / 평균 시청 >25s with 임계 thresholds). "측정 방식" — YouTube Analytics audienceRetention per-second, KPI-01 cron collection (Phase 10), KPI-02 월 1회 kpi_log.md flush. "Hook 설계 규칙" referencing CONTENT-01 (질문형 / 숫자 ≥2자리 / 고유명사 ≥2자) hardcoded in Phase 4 ins-narrative-quality LogicQA q1/q2. "Taste Gate 연동 (KPI-05)" — 월 1회 대표님 상/하위 3 평가 → FAILURES.md. Related: [[../algorithm/ranking_factors]], [[../continuity_bible/channel_identity]], [[MOC]].

Each file MUST pass `validate_node()` — test by calling `python -c "from scripts.wiki.frontmatter import validate_node; from pathlib import Path; validate_node(Path('wiki/algorithm/ranking_factors.md'))"` etc.
  </action>
  <verify>
    <automated>python -c "from scripts.wiki.frontmatter import validate_node; from pathlib import Path; [validate_node(Path(p)) for p in ['wiki/algorithm/ranking_factors.md','wiki/ypp/entry_conditions.md','wiki/render/remotion_kling_stack.md','wiki/kpi/retention_3second_hook.md']]; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f wiki/algorithm/ranking_factors.md` exits 0
    - `test -f wiki/ypp/entry_conditions.md` exits 0
    - `test -f wiki/render/remotion_kling_stack.md` exits 0
    - `test -f wiki/kpi/retention_3second_hook.md` exits 0
    - `grep -c "^status: ready$" wiki/algorithm/ranking_factors.md` outputs `1`
    - `grep -c "^status: ready$" wiki/ypp/entry_conditions.md` outputs `1`
    - `grep -c "^status: ready$" wiki/render/remotion_kling_stack.md` outputs `1`
    - `grep -c "^status: ready$" wiki/kpi/retention_3second_hook.md` outputs `1`
    - `grep -c "\[\[MOC\]\]" wiki/algorithm/ranking_factors.md` outputs `>= 1`
    - `grep -c "T2V" wiki/render/remotion_kling_stack.md` outputs `>= 1` (negative constraint documented)
    - `grep -c "continuity_prefix" wiki/render/remotion_kling_stack.md` outputs `>= 1` (D-19 filter order documented)
  </acceptance_criteria>
  <done>4 ready nodes with valid D-17 frontmatter, Obsidian links, and D-16 Korean senior context documented.</done>
</task>

<task type="auto">
  <name>Task 2: Author continuity_bible/channel_identity.md with all 5 D-10 components + flip 5 MOC checkboxes</name>
  <files>
    wiki/continuity_bible/channel_identity.md,
    wiki/algorithm/MOC.md,
    wiki/ypp/MOC.md,
    wiki/render/MOC.md,
    wiki/kpi/MOC.md,
    wiki/continuity_bible/MOC.md
  </files>
  <read_first>
    - .planning/phases/06-wiki-notebooklm-integration-failures-reservoir/06-CONTEXT.md §D-10 line 57 (5 구성요소 verbatim) + §D-16 line 74 (Korean senior specificity) + §D-20 line 87 (pydantic field names to document)
    - .planning/phases/06-wiki-notebooklm-integration-failures-reservoir/06-RESEARCH.md §Code Example 1 lines 1319-1361 (channel_identity.md template)
    - wiki/continuity_bible/MOC.md (Planned Nodes — channel_identity checkbox to flip)
    - wiki/algorithm/MOC.md (line 21: `- [ ] \`ranking_factors.md\`` to flip)
    - wiki/ypp/MOC.md (Planned Nodes — entry_conditions checkbox)
    - wiki/render/MOC.md (Planned Nodes — remotion_kling_stack / equivalent checkbox)
    - wiki/kpi/MOC.md (Planned Nodes — retention_3second_hook checkbox)
  </read_first>
  <action>
**File 1: wiki/continuity_bible/channel_identity.md (~70 lines)** — MANDATORY all 5 D-10 components documented. This is the single source of truth; Plan 06 serializes it to `prefix.json` (pydantic).

Frontmatter: category=continuity_bible, status=ready, tags=[continuity, channel-identity, korean-seniors, color-palette, camera-lens], updated=2026-04-19, source_notebook=naberal-shorts-channel-bible.

Body sections:
- `## 5 구성요소 (D-10 canonical)` — header
- `### (a) 색상 팔레트` — Navy `#1A2E4A` / Gold `#C8A660` / Light `#E4E4E4` / Warmth `+0.2` (D-16 senior preference)
- `### (b) 카메라 렌즈` — 초점거리 `35mm` / Aperture `f/2.8` (pydantic field names ContinuityPrefix.focal_length_mm + ContinuityPrefix.aperture_f)
- `### (c) 시각적 스타일` — Locked `cinematic` (Literal ["photorealistic","cinematic","documentary"] one LOCK, AF-7 duplicate tangent)
- `### (d) 한국 시니어 시청자 특성 (D-16)` — 연령 50-65세 / 채도 낮은 톤 / 빠른 정보 전달 / 존댓말 narration (캐릭터 대사 예외 — 탐정 하오체 + 조수 해요체, CONTENT-02)
- `### (e) BGM 분위기` — Default=ambient / Tension=hook 3초 / Uplift=결론-CTA (Literal ["ambient","tension","uplift"])
- `## prefix.json 직렬화 규격 (Plan 06 출력)` — a table listing all 7 ContinuityPrefix pydantic fields (D-20) with type constraints and example values
- `## 사용처` — Shotstack adapter _load_continuity_preset (D-9), NotebookLM channel-bible notebook upload source (D-8), agent prompt @wiki ref (D-3, D-18)
- `## Related` — [[../algorithm/ranking_factors]], [[../kpi/retention_3second_hook]], [[../render/remotion_kling_stack]], [[MOC]]

CRITICAL: The string `5 구성요소` (exact Korean) MUST appear verbatim so `test_continuity_bible_node.py::test_contains_five_components` can grep for it. All 5 sub-headings `### (a)` through `### (e)` MUST be present.

**Files 2-6: Update 5 MOC.md files — flip one `[ ]` to `[x]`**.

For each MOC, change the Planned Nodes bullet matching the node this plan created:
- wiki/algorithm/MOC.md: `- [ ] \`ranking_factors.md\` — ...` → `- [x] \`ranking_factors.md\` — ...`
- wiki/ypp/MOC.md: locate `entry_conditions.md` line → flip `[ ]` to `[x]`. If line absent, append at end of Planned Nodes section: `- [x] \`entry_conditions.md\` — YPP Shorts 경로 진입 기준 + Korean RPM baseline (Phase 6 ready)`
- wiki/render/MOC.md: locate `remotion_kling_stack.md` or closest `stack/render` bullet → flip to `[x]`. If absent, append: `- [x] \`remotion_kling_stack.md\` — Remotion v4 + Kling primary + Runway backup + Shotstack composite (Phase 6 ready)`
- wiki/kpi/MOC.md: locate `retention_3second_hook.md` → flip to `[x]`. If absent, append: `- [x] \`retention_3second_hook.md\` — 3초 retention >60% 목표 + YouTube Analytics 측정 (Phase 6 ready)`
- wiki/continuity_bible/MOC.md: locate `channel_identity.md` → flip to `[x]`. If absent, append: `- [x] \`channel_identity.md\` — D-10 5 구성요소 (Phase 6 ready)`

Do NOT modify MOC frontmatter (status=scaffold stays — MOC is ToC, not a ready leaf). Do NOT reformat other lines. Use Edit tool per file with unique `old_string` (full bullet text) to avoid collision.
  </action>
  <verify>
    <automated>python -c "from scripts.wiki.frontmatter import validate_node; from pathlib import Path; validate_node(Path('wiki/continuity_bible/channel_identity.md')); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f wiki/continuity_bible/channel_identity.md` exits 0
    - `grep -c "^status: ready$" wiki/continuity_bible/channel_identity.md` outputs `1`
    - `grep -c "5 구성요소" wiki/continuity_bible/channel_identity.md` outputs `>= 1`
    - `grep -cE "^### \(a\)|^### \(b\)|^### \(c\)|^### \(d\)|^### \(e\)" wiki/continuity_bible/channel_identity.md` outputs `5`
    - `grep -c "#1A2E4A" wiki/continuity_bible/channel_identity.md` outputs `>= 1` (D-10 color palette sample present)
    - `grep -c "cinematic" wiki/continuity_bible/channel_identity.md` outputs `>= 1` (D-10 visual_style locked present)
    - `grep -c "ambient" wiki/continuity_bible/channel_identity.md` outputs `>= 1` (D-10 bgm_mood preset)
    - `grep -c "^- \[x\]" wiki/algorithm/MOC.md` outputs `>= 1`
    - `grep -c "^- \[x\]" wiki/ypp/MOC.md` outputs `>= 1`
    - `grep -c "^- \[x\]" wiki/render/MOC.md` outputs `>= 1`
    - `grep -c "^- \[x\]" wiki/kpi/MOC.md` outputs `>= 1`
    - `grep -c "^- \[x\]" wiki/continuity_bible/MOC.md` outputs `>= 1`
  </acceptance_criteria>
  <done>channel_identity.md contains all 5 D-10 components verbatim; 5 MOC checkboxes flipped; D-17 frontmatter valid.</done>
</task>

<task type="auto">
  <name>Task 3: 3 test files proving ready nodes + MOC linkage + D-10 5 components</name>
  <files>
    tests/phase06/test_wiki_nodes_ready.py,
    tests/phase06/test_moc_linkage.py,
    tests/phase06/test_continuity_bible_node.py
  </files>
  <read_first>
    - scripts/wiki/frontmatter.py (Plan 01 parse_frontmatter + is_ready API)
    - tests/phase06/conftest.py (Plan 01 fixtures — repo_root, fixtures_dir)
    - wiki/continuity_bible/channel_identity.md (just created Task 2 — source of truth for D-10 grep)
    - .planning/phases/06-wiki-notebooklm-integration-failures-reservoir/06-CONTEXT.md §D-10 (5 components to assert)
  </read_first>
  <action>
**File 1: tests/phase06/test_wiki_nodes_ready.py (~60 lines)**
```python
from __future__ import annotations
from pathlib import Path
import pytest

from scripts.wiki.frontmatter import parse_frontmatter, is_ready


CATEGORIES = ["algorithm", "ypp", "render", "kpi", "continuity_bible"]


def test_all_5_categories_have_at_least_one_ready_node(repo_root: Path):
    wiki = repo_root / "wiki"
    missing = []
    for cat in CATEGORIES:
        cat_dir = wiki / cat
        assert cat_dir.is_dir(), f"wiki/{cat}/ missing"
        ready_nodes = [
            p for p in cat_dir.glob("*.md")
            if p.name != "MOC.md" and is_ready(p)
        ]
        if not ready_nodes:
            missing.append(cat)
    assert missing == [], f"Categories without ready node: {missing}"


def test_algorithm_ranking_factors_ready(repo_root: Path):
    p = repo_root / "wiki" / "algorithm" / "ranking_factors.md"
    assert p.exists()
    fm = parse_frontmatter(p)
    assert fm["status"] == "ready"
    assert fm["category"] == "algorithm"


def test_ypp_entry_conditions_ready(repo_root: Path):
    p = repo_root / "wiki" / "ypp" / "entry_conditions.md"
    assert p.exists()
    fm = parse_frontmatter(p)
    assert fm["status"] == "ready"
    assert fm["category"] == "ypp"


def test_render_remotion_kling_stack_ready(repo_root: Path):
    p = repo_root / "wiki" / "render" / "remotion_kling_stack.md"
    assert p.exists()
    fm = parse_frontmatter(p)
    assert fm["status"] == "ready"
    assert fm["category"] == "render"


def test_kpi_retention_3second_hook_ready(repo_root: Path):
    p = repo_root / "wiki" / "kpi" / "retention_3second_hook.md"
    assert p.exists()
    fm = parse_frontmatter(p)
    assert fm["status"] == "ready"
    assert fm["category"] == "kpi"


def test_continuity_bible_channel_identity_ready(repo_root: Path):
    p = repo_root / "wiki" / "continuity_bible" / "channel_identity.md"
    assert p.exists()
    fm = parse_frontmatter(p)
    assert fm["status"] == "ready"
    assert fm["category"] == "continuity_bible"


def test_ready_nodes_reference_moc(repo_root: Path):
    """Every Phase 6 ready node backlinks to its MOC."""
    wiki = repo_root / "wiki"
    files = [
        wiki / "algorithm" / "ranking_factors.md",
        wiki / "ypp" / "entry_conditions.md",
        wiki / "render" / "remotion_kling_stack.md",
        wiki / "kpi" / "retention_3second_hook.md",
        wiki / "continuity_bible" / "channel_identity.md",
    ]
    for f in files:
        text = f.read_text(encoding="utf-8")
        assert "[[MOC]]" in text, f"{f}: missing [[MOC]] backlink"
```

**File 2: tests/phase06/test_moc_linkage.py (~50 lines)**
```python
from __future__ import annotations
import re
from pathlib import Path
import pytest


# Map: (MOC path relative to wiki/, expected node filename that must be checked [x])
MOC_CHECK_EXPECTATIONS = [
    ("algorithm/MOC.md", "ranking_factors.md"),
    ("ypp/MOC.md", "entry_conditions.md"),
    ("render/MOC.md", "remotion_kling_stack.md"),
    ("kpi/MOC.md", "retention_3second_hook.md"),
    ("continuity_bible/MOC.md", "channel_identity.md"),
]


@pytest.mark.parametrize("moc_rel,node_name", MOC_CHECK_EXPECTATIONS)
def test_moc_has_checked_box_for_node(repo_root: Path, moc_rel: str, node_name: str):
    """Every ready node promoted this plan has [x] in its category MOC.md."""
    moc = repo_root / "wiki" / moc_rel
    assert moc.exists(), f"{moc} missing"
    text = moc.read_text(encoding="utf-8")
    # Match `- [x] \`<node_name>\`` pattern (bullet + checked + backticked filename)
    pattern = re.compile(rf"^-\s*\[x\]\s*`{re.escape(node_name)}`", re.MULTILINE)
    assert pattern.search(text), (
        f"{moc}: no `- [x] \\`{node_name}\\`` bullet found. "
        f"Either flip existing [ ] or append new [x] line."
    )


def test_moc_frontmatter_unchanged_scaffold(repo_root: Path):
    """Sanity: MOC frontmatter status stays scaffold (not promoted to ready)."""
    for moc_rel, _ in MOC_CHECK_EXPECTATIONS:
        moc = repo_root / "wiki" / moc_rel
        text = moc.read_text(encoding="utf-8")
        assert "status: scaffold" in text, f"{moc}: MOC status MUST stay scaffold (D-17 structural)"
```

**File 3: tests/phase06/test_continuity_bible_node.py (~60 lines)**
```python
from __future__ import annotations
import re
from pathlib import Path


def test_channel_identity_contains_d10_header(repo_root: Path):
    p = repo_root / "wiki" / "continuity_bible" / "channel_identity.md"
    text = p.read_text(encoding="utf-8")
    assert "5 구성요소" in text, "D-10 '5 구성요소' literal header missing"


def test_channel_identity_contains_all_five_subsections(repo_root: Path):
    """D-10 (a) through (e) all present as ### headers."""
    p = repo_root / "wiki" / "continuity_bible" / "channel_identity.md"
    text = p.read_text(encoding="utf-8")
    for letter in ["(a)", "(b)", "(c)", "(d)", "(e)"]:
        pattern = re.compile(rf"^### {re.escape(letter)}", re.MULTILINE)
        assert pattern.search(text), f"D-10 subsection {letter} missing"


def test_channel_identity_color_palette_has_hex_values(repo_root: Path):
    """D-10 (a) requires HEX 3-5 colors."""
    p = repo_root / "wiki" / "continuity_bible" / "channel_identity.md"
    text = p.read_text(encoding="utf-8")
    hex_matches = re.findall(r"#[0-9A-Fa-f]{6}", text)
    assert len(hex_matches) >= 3, f"D-10 (a) needs >=3 HEX colors, found {len(hex_matches)}"
    assert len(hex_matches) <= 10, f"Suspiciously many HEX values ({len(hex_matches)})"


def test_channel_identity_lens_has_focal_length(repo_root: Path):
    """D-10 (b) requires focal length in mm."""
    p = repo_root / "wiki" / "continuity_bible" / "channel_identity.md"
    text = p.read_text(encoding="utf-8")
    assert re.search(r"\b\d{2,3}\s*mm\b", text), "D-10 (b) focal length mm missing"


def test_channel_identity_visual_style_locked(repo_root: Path):
    """D-10 (c) requires one of [photorealistic, cinematic, documentary] locked."""
    p = repo_root / "wiki" / "continuity_bible" / "channel_identity.md"
    text = p.read_text(encoding="utf-8")
    assert any(s in text for s in ["photorealistic", "cinematic", "documentary"]), (
        "D-10 (c) visual_style literal missing"
    )


def test_channel_identity_korean_senior_audience(repo_root: Path):
    """D-10 (d) requires Korean senior audience descriptor (D-16)."""
    p = repo_root / "wiki" / "continuity_bible" / "channel_identity.md"
    text = p.read_text(encoding="utf-8")
    assert "시니어" in text or "50-65" in text, "D-10 (d) Korean senior audience context missing"


def test_channel_identity_bgm_mood_presets(repo_root: Path):
    """D-10 (e) requires all 3 BGM presets: ambient, tension, uplift."""
    p = repo_root / "wiki" / "continuity_bible" / "channel_identity.md"
    text = p.read_text(encoding="utf-8")
    for preset in ["ambient", "tension", "uplift"]:
        assert preset in text, f"D-10 (e) preset '{preset}' missing"


def test_channel_identity_source_notebook_is_channel_bible(repo_root: Path):
    """source_notebook should point at naberal-shorts-channel-bible (D-4/D-8)."""
    from scripts.wiki.frontmatter import parse_frontmatter
    p = repo_root / "wiki" / "continuity_bible" / "channel_identity.md"
    fm = parse_frontmatter(p)
    assert fm["source_notebook"] == "naberal-shorts-channel-bible", (
        f"continuity_bible node must source from channel-bible notebook (D-4), got {fm['source_notebook']}"
    )
```
  </action>
  <verify>
    <automated>python -m pytest tests/phase06/test_wiki_nodes_ready.py tests/phase06/test_moc_linkage.py tests/phase06/test_continuity_bible_node.py -q --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/phase06/test_wiki_nodes_ready.py -q --no-cov` exits 0
    - `python -m pytest tests/phase06/test_moc_linkage.py -q --no-cov` exits 0
    - `python -m pytest tests/phase06/test_continuity_bible_node.py -q --no-cov` exits 0
    - `grep -cE "^def test_" tests/phase06/test_wiki_nodes_ready.py` outputs `>= 7`
    - `grep -cE "^def test_" tests/phase06/test_moc_linkage.py` outputs `>= 2`
    - `grep -cE "^def test_" tests/phase06/test_continuity_bible_node.py` outputs `>= 7`
    - `python scripts/validate/verify_wiki_frontmatter.py --root wiki --allow-scaffold` exits 0
  </acceptance_criteria>
  <done>3 test files green. All 5 categories proved ready. All 5 MOCs linked. D-10 components asserted.</done>
</task>

</tasks>

<verification>
After all 3 tasks:
1. `python scripts/validate/verify_wiki_frontmatter.py --root wiki --allow-scaffold` exits 0 (5 ready + 5 scaffold MOC all valid)
2. `python -m pytest tests/phase06/ -q --no-cov` exits 0 with >= 25 tests green (Plan 01 14 + Plan 02 16+)
3. `ls wiki/*/*.md | wc -l` >= 10 (5 MOC + 5 ready nodes)
4. All 5 MOC files have at least one `- [x]` checked line
5. wiki/continuity_bible/channel_identity.md contains literal `5 구성요소` + all 5 `### (a)..(e)` subsections
</verification>

<success_criteria>
- WIKI-01: 5 categories each have >=1 status=ready node
- WIKI-02 content layer: channel_identity.md has all 5 D-10 components
- 5 MOC.md files updated with checkbox flip (scaffold frontmatter preserved)
- D-16 Korean senior context documented in relevant nodes
- Obsidian linkage complete (every ready node has [[MOC]] backlink + >=1 intra-wiki link)
- 3 test files add 16+ new green tests
- Plan 10 agent prompt mass update has valid @wiki/shorts/... targets to reference
</success_criteria>

<output>
After completion, create `.planning/phases/06-wiki-notebooklm-integration-failures-reservoir/06-02-SUMMARY.md` documenting:
- Files created: 5 ready nodes + 3 test files; Files edited: 5 MOC.md
- D-10 5 components distribution in channel_identity.md (grep verification)
- MOC checkbox flip diff per file
- verify_wiki_frontmatter.py output
- pytest output (expected 16+ new tests green)
- Commit hashes
</output>
</content>
</invoke>