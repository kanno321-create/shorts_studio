---
phase: 13-live-smoke
plan: 05
plan_id: 13-05
type: execute
wave: 4
depends_on:
  - 13-01
  - 13-02
  - 13-03
  - 13-04
files_modified:
  - scripts/smoke/phase13_live_smoke.py
  - tests/phase13/test_smoke_e2e_evidence.py
  - tests/phase13/test_live_run.py
autonomous: true
requirements:
  - SMOKE-01
  - SMOKE-02
  - SMOKE-03
  - SMOKE-04
  - SMOKE-05
  - SMOKE-06
commit_discipline: per-task
must_haves:
  truths:
    - "`scripts/smoke/phase13_live_smoke.py` CLI 가 `phase11_full_run.py` 의 493-line harness 구조를 복제하면서 Wave 0~3 산출물 (budget_counter + provider_rates + evidence_extractor + upload_evidence) 을 wire — 실행 시 13 GATE TREND→COMPLETE 전수 dispatched + 5 evidence 파일 (producer_output / supervisor_output / smoke_upload / budget_usage / smoke_e2e) write + final_video_id anchor"
    - "CLI flags: `--live` (default False — dry-run mode), `--max-attempts 2` (default 2, Research Open Q #2), `--budget-cap-usd 5.00` (default 5.00), `--session-id <custom>` (default timestamp), `--verbose-compression` (optional ad-hoc raw supervisor payload dump, Research Open Q #6)"
    - "Tier 1 always-run: `pytest tests/phase13/test_smoke_e2e_evidence.py -m \"not live_smoke\"` 가 golden `sample_smoke_e2e.json` fixture 에 대해 13 gate timestamps + final_video_id + total_cost_usd + budget_breached==false + supervisor_rc1_count==0 invariant 검증"
    - "Tier 2 opt-in: `pytest tests/phase13/test_live_run.py -m live_smoke --run-live` 또는 직접 `py -3.11 scripts/smoke/phase13_live_smoke.py --live` 실행 시 실 run 이 budget_usage.json total ≤ $5.00 + gate_count==13 + evidence 5 파일 모두 write 로 rc=0 완료 (SMOKE-06)"
    - "publish_lock bypass via `SHORTS_PUBLISH_LOCK_PATH` env (Research §publish_lock Bypass L173-184) — phase13_live_smoke.py 가 pipeline.run() 전에 tempfile path 로 override → smoke 1 회가 48h+ 운영 counter 소비 금지 (금기 #8 준수)"
  artifacts:
    - path: "scripts/smoke/phase13_live_smoke.py"
      provides: "Phase 13 Full E2E smoke runner — phase11_full_run.py clone + Wave 0~3 wire"
      contains: "phase13_live_smoke"
      min_lines: 250
    - path: "tests/phase13/test_smoke_e2e_evidence.py"
      provides: "Tier 1 smoke_e2e fixture invariant + live evidence shape"
      contains: "gate_count"
      min_lines: 80
    - path: "tests/phase13/test_live_run.py"
      provides: "Tier 2 @pytest.mark.live_smoke end-to-end subprocess wrapper"
      contains: "pytest.mark.live_smoke"
      min_lines: 60
  key_links:
    - from: "scripts/smoke/phase13_live_smoke.py"
      to: "Wave 0~3 산출물 5종"
      via: "import BudgetCounter + PROVIDER_RATES_USD + extract_producer_output + extract_supervisor_output + anchor_upload_evidence"
      pattern: "from scripts.smoke.(budget_counter|provider_rates|evidence_extractor|upload_evidence)"
    - from: "scripts/smoke/phase13_live_smoke.py"
      to: "scripts/smoke/phase11_full_run.py 의 _check_env_readiness + _build_pipeline + _aggregate_gate_metrics"
      via: "from scripts.smoke.phase11_full_run import ... (직접 재사용)"
      pattern: "from scripts.smoke.phase11_full_run import"
    - from: "scripts/smoke/phase13_live_smoke.py"
      to: "SHORTS_PUBLISH_LOCK_PATH env"
      via: "os.environ.setdefault(\"SHORTS_PUBLISH_LOCK_PATH\", tempfile.gettempdir() / \"phase13_smoke_lock.json\")"
      pattern: "SHORTS_PUBLISH_LOCK_PATH"
    - from: "tests/phase13/test_live_run.py Tier 2"
      to: "subprocess.run([sys.executable, \"scripts/smoke/phase13_live_smoke.py\", \"--live\", ...])"
      via: "subprocess + rc check + evidence 5 파일 존재 assertion"
      pattern: "subprocess.run"
---

<objective>
Wave 4 Full E2E Smoke — SMOKE-06 의 **실증 harness** 구축. `phase11_full_run.py` (493 lines, Phase 11 Plan 06 ship) 을 clone + Wave 0~3 의 산출물 5종 (budget_counter / provider_rates / evidence_extractor / upload_evidence / phase13_preflight) 을 wire 하여 13 GATE 전수 dispatched + 5 evidence 파일 write + $5 cap enforcement + unlisted cleanup 을 단일 CLI 로 수행하는 `scripts/smoke/phase13_live_smoke.py` 를 신설한다.

Purpose: Phase 11 deferred SC#1 + SC#2 의 **실환경 완결** + SMOKE-01~06 의 **통합 실증 경로**. 단일 `py -3.11 scripts/smoke/phase13_live_smoke.py --live --budget-cap-usd 5.00` 호출로 TREND → COMPLETE full E2E 가 완료되면 5 evidence 파일 모두 생성되고 rc=0 반환. Wave 5 Phase Gate 가 본 evidence chain 을 검증하여 Phase 13 공식 완료 판정.

Output: 1 CLI runner (`phase13_live_smoke.py` ~300 lines) + 2 test 파일 (Tier 1 evidence shape + Tier 2 subprocess wrapper). Tier 1 always-run ≥4 tests. Tier 2 live opt-in ≥1 test (subprocess 경유).

**Sequential within wave**: Task 13-05-01 (phase13_live_smoke.py) → 13-05-02 (test_smoke_e2e_evidence.py) → 13-05-03 (test_live_run.py). CLI runner 가 선행되어야 test 가 이를 호출 가능.

**Critical dependency**: Wave 0 + Wave 1 + Wave 2 + Wave 3 모두 완료 후에만 실행 가능. 본 Plan 의 runner 는 Wave 1~3 의 모든 공용 모듈을 import.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-live-smoke/13-RESEARCH.md
@.planning/phases/13-live-smoke/13-VALIDATION.md
@CLAUDE.md

# Wave 0~3 산출물 (dependency)
@scripts/smoke/budget_counter.py
@scripts/smoke/provider_rates.py
@scripts/smoke/evidence_extractor.py
@scripts/smoke/upload_evidence.py
@scripts/smoke/phase13_preflight.py
@tests/phase13/conftest.py
@tests/phase13/fixtures/sample_smoke_e2e.json

# Reference runner — 493-line harness to clone
@scripts/smoke/phase11_full_run.py

# Pipeline core
@scripts/orchestrator/__init__.py
@scripts/orchestrator/shorts_pipeline.py
@scripts/orchestrator/invokers.py
@scripts/orchestrator/gate_guard.py

# Publish lock override mechanism (Phase 8 D-06)
@scripts/publisher/publish_lock.py
</context>

<interfaces>
<!-- All contracts below are consumed (read-only) by this Plan's runner. No new contracts defined. -->

scripts/smoke/budget_counter.py: BudgetCounter(cap, evidence_path) + charge + persist + BudgetExceededError
scripts/smoke/provider_rates.py: PROVIDER_RATES_USD + estimate_adapter_cost + all_known_providers
scripts/smoke/evidence_extractor.py: extract_producer_output(sid, state_root, evidence_dir), extract_supervisor_output(...)
scripts/smoke/upload_evidence.py: anchor_upload_evidence(youtube, video_id, sid, evidence_dir)
scripts/smoke/phase13_preflight.py: main() -> int (rc=0 OK, 1 fail) — optional pre-run sanity

scripts/smoke/phase11_full_run.py reusable helpers:
```python
def _check_env_readiness() -> dict: ...  # L155
def _build_pipeline(session_id, state_root) -> ShortsPipeline: ...  # L218
def _aggregate_gate_metrics(session_id, state_root) -> dict: ...  # L243
def _extract_upload_url(session_id, state_root) -> str | None: ...  # L306
def _write_run_report(run_summary, reports_dir) -> Path: ...  # L330
PER_GATE_COST_ESTIMATE_USD: dict  # L82
DEFAULT_BUDGET_USD = 5.00  # L99
```

scripts/orchestrator.ShortsPipeline.run(max_retries_per_gate=N) -> None  # blocking

<!-- NO NEW CONTRACTS — Wave 4 only consumes. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 13-05-01: scripts/smoke/phase13_live_smoke.py — Full E2E runner (phase11 clone + Wave 0~3 wire)</name>
  <files>scripts/smoke/phase13_live_smoke.py</files>
  <read_first>
    - scripts/smoke/phase11_full_run.py (493 lines — 전체 구조 파악 필수)
    - scripts/smoke/budget_counter.py + provider_rates.py + evidence_extractor.py + upload_evidence.py + phase13_preflight.py (Wave 0~3 5 모듈)
    - scripts/orchestrator/__init__.py (ShortsPipeline export + _load_dotenv_if_present)
    - scripts/publisher/publish_lock.py (SHORTS_PUBLISH_LOCK_PATH env override — Phase 8 D-06)
    - .planning/phases/13-live-smoke/13-RESEARCH.md §Full E2E Example (L449-494) + §Build Order Recommendation Wave 4 (L738-746)
    - .planning/phases/13-live-smoke/13-VALIDATION.md row 13-05-01
  </read_first>
  <action>
    신규 파일 `scripts/smoke/phase13_live_smoke.py` (≥250 lines, ~350 expected). `phase11_full_run.py` 구조를 복제하면서 Wave 0~3 을 wire:

    **Line 1 헤더 docstring**: "Phase 13 Live Smoke Runner — real Claude CLI + budget cap + evidence anchoring. phase11_full_run.py (493 lines) clone + Wave 0~3 wire (budget_counter + provider_rates + evidence_extractor + upload_evidence). Cost cap $5.00 hard (SMOKE-05), unlisted + cleanup (SMOKE-03, 금기 #8 준수). Evidence: 5 files in .planning/phases/13-live-smoke/evidence/."

    **Top section (Windows cp949 guard + sys.path + env bootstrap)**: phase11_full_run.py L43-72 그대로 복제. 추가로:
    ```python
    import tempfile
    # publish_lock bypass — smoke MUST NOT consume 48h+ counter (금기 #8 준수)
    os.environ.setdefault(
        "SHORTS_PUBLISH_LOCK_PATH",
        str(Path(tempfile.gettempdir()) / "phase13_smoke_lock.json"),
    )
    ```

    **Imports (Wave 0~3 wire + phase11 helpers)**:
    ```python
    from scripts.orchestrator import ShortsPipeline  # noqa: E402
    from scripts.orchestrator.invokers import (  # noqa: E402
        make_default_producer_invoker,
        make_default_supervisor_invoker,
    )
    from scripts.smoke.phase11_full_run import (  # noqa: E402
        _check_env_readiness,
        _print_env_report,
        _build_pipeline,
        _aggregate_gate_metrics,
        _extract_upload_url,
    )
    from scripts.smoke.budget_counter import BudgetCounter, BudgetExceededError  # noqa: E402
    from scripts.smoke.provider_rates import (  # noqa: E402
        PROVIDER_RATES_USD,
        estimate_adapter_cost,
    )
    from scripts.smoke.evidence_extractor import (  # noqa: E402
        extract_producer_output,
        extract_supervisor_output,
        OPERATIONAL_GATES,
    )
    from scripts.smoke.upload_evidence import anchor_upload_evidence  # noqa: E402
    ```

    **Constants**:
    ```python
    EVIDENCE_DIR = _REPO_ROOT / ".planning" / "phases" / "13-live-smoke" / "evidence"
    EVIDENCE_DIR.mkdir(parents=True, exist_ok=True)
    DEFAULT_BUDGET_CAP_USD = 5.00
    DEFAULT_MAX_ATTEMPTS = 2  # Research Open Q #2
    ```

    **_parse_args(argv)**:
    ```python
    parser = argparse.ArgumentParser(description="Phase 13 Live Smoke — full E2E real API smoke + evidence chain")
    parser.add_argument("--live", action="store_true", default=False, help="Real API calls. Default: dry-run.")
    parser.add_argument("--max-attempts", type=int, default=DEFAULT_MAX_ATTEMPTS, help="Retry attempts on run failure (cumulative cap preserved)")
    parser.add_argument("--budget-cap-usd", type=float, default=DEFAULT_BUDGET_CAP_USD)
    parser.add_argument("--session-id", default=None)
    parser.add_argument("--state-root", default="state")
    parser.add_argument("--verbose-compression", action="store_true", default=False, help="Dump raw supervisor payload pre-compression to evidence (Research Open Q #6)")
    parser.add_argument("--log-level", default="INFO")
    ```

    **Main flow (def _run_smoke + def main)**:
    1. Parse args.
    2. `_check_env_readiness()` (from phase11) — if `--live` and readiness FAIL → rc=4.
    3. `session_id = args.session_id or datetime.now().strftime("%Y%m%d_%H%M%S")`.
    4. Create `counter = BudgetCounter(args.budget_cap_usd, EVIDENCE_DIR / f"budget_usage_{session_id}.json")`.
    5. `counter.charge("claude_cli", 0.00, metadata={"note": "Max subscription inclusive"})` — Research Open Q #4.
    6. `counter.charge("notebooklm", 0.00, metadata={"note": "Max subscription inclusive"})`.
    7. If `--live`:
       a. `pipeline = _build_pipeline(session_id, args.state_root)` (phase11 helper).
       b. Attempts loop (`args.max_attempts`):
          - `try: pipeline.run()` — blocking.
          - `except BudgetExceededError: counter.persist(); sys.exit(5)`.
          - `except Exception as exc: logger.error(...); if attempt < max_attempts - 1: continue; else raise`.
       c. After pipeline success:
          - `extract_producer_output(session_id, Path(args.state_root), EVIDENCE_DIR)` — producer_output_<sid>.json.
          - `extract_supervisor_output(session_id, Path(args.state_root), EVIDENCE_DIR)` — supervisor_output_<sid>.json.
          - Metrics = `_aggregate_gate_metrics(session_id, args.state_root)` (phase11).
          - Upload URL = `_extract_upload_url(session_id, args.state_root)` → video_id parse.
          - If video_id present: build youtube client (oauth) + `anchor_upload_evidence(youtube, video_id, session_id, EVIDENCE_DIR)` → smoke_upload_<sid>.json.
          - Then immediate `videos.delete(id=video_id)` — cleanup (금기 #8 준수).
          - Adapter charges: `counter.charge("nanobanana", estimate_adapter_cost("nanobanana", 1))` + `counter.charge("kling", estimate_adapter_cost("kling", 8))` + `counter.charge("typecast", estimate_adapter_cost("typecast", 1))` + `counter.charge("youtube_api", 0.00)`.
          - `counter.persist()` → budget_usage_<sid>.json.
          - Build smoke_e2e JSON with 10 keys (session_id / status:"OK" / wall_time_seconds / gate_timestamps / gate_count / final_video_id / total_cost_usd / budget_cap_usd / budget_breached / supervisor_rc1_count).
          - Write `EVIDENCE_DIR / f"smoke_e2e_{session_id}.json"` with `ensure_ascii=False, indent=2`.
    8. If dry-run (default): skip live run — print env report + budget simulation + counter.persist() (no real spend).
    9. Return rc — 0 OK, 2 pipeline exception, 3 state missing, 4 env fail, 5 cost cap, 6 evidence missing.

    **CLI entrypoint**: `if __name__ == "__main__": sys.exit(main())`.

    **구현 주의점**:
    - `SHORTS_PUBLISH_LOCK_PATH` 는 `os.environ.setdefault` — 사용자 override 존중 (테스트 시 `monkeypatch.setenv` 가 앞서면 그 값 유지).
    - `counter.charge` 순서는 `try` 블록 내에서 **adapter 호출 후 즉시** — BudgetExceededError 가 raise 되면 이후 adapter 호출 0회 (cap 강제 진행).
    - `_aggregate_gate_metrics` 결과에 `gate_timestamps` 가 없을 경우 state/<sid>/gate_*.json 을 직접 순회하여 timestamp 수집.
    - `--verbose-compression` 활성 시 `EVIDENCE_DIR / f"supervisor_raw_{session_id}_{gate}.json"` 추가 write — Research Open Q #6 (ad-hoc).
    - 모든 evidence write 는 `ensure_ascii=False` + `encoding="utf-8"` (한국어 보존).
    - **금기 #2**: TODO 없음. **금기 #5**: googleapiclient 공식 경로만. **금기 #8**: cleanup=True + SHORTS_PUBLISH_LOCK_PATH temp path — 운영 카운터 소비 0.
    - **필수 #7**: 로그 메시지에 "대표님" literal 1+ (예: 최종 보고 "대표님, Phase 13 smoke 완료").
    - **필수 #8**: 모든 evidence 파일 경로를 logger.info 로 출력.
  </action>
  <verify>
    <automated>test -f scripts/smoke/phase13_live_smoke.py &amp;&amp; grep -c "from scripts.smoke.budget_counter import" scripts/smoke/phase13_live_smoke.py &amp;&amp; grep -c "from scripts.smoke.provider_rates import" scripts/smoke/phase13_live_smoke.py &amp;&amp; grep -c "from scripts.smoke.evidence_extractor import" scripts/smoke/phase13_live_smoke.py &amp;&amp; grep -c "from scripts.smoke.upload_evidence import" scripts/smoke/phase13_live_smoke.py &amp;&amp; grep -c "SHORTS_PUBLISH_LOCK_PATH" scripts/smoke/phase13_live_smoke.py &amp;&amp; py -3.11 scripts/smoke/phase13_live_smoke.py --help</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/smoke/phase13_live_smoke.py` returns 0.
    - `wc -l scripts/smoke/phase13_live_smoke.py` returns ≥250.
    - `grep -c "from scripts.smoke.budget_counter import" scripts/smoke/phase13_live_smoke.py` returns 1.
    - `grep -c "from scripts.smoke.provider_rates import" scripts/smoke/phase13_live_smoke.py` returns 1.
    - `grep -c "from scripts.smoke.evidence_extractor import" scripts/smoke/phase13_live_smoke.py` returns 1.
    - `grep -c "from scripts.smoke.upload_evidence import" scripts/smoke/phase13_live_smoke.py` returns 1.
    - `grep -c "from scripts.smoke.phase11_full_run import" scripts/smoke/phase13_live_smoke.py` returns 1.
    - `grep -c "SHORTS_PUBLISH_LOCK_PATH" scripts/smoke/phase13_live_smoke.py` returns ≥1.
    - `grep -c "\\-\\-live" scripts/smoke/phase13_live_smoke.py` returns ≥1.
    - `grep -c "\\-\\-max-attempts" scripts/smoke/phase13_live_smoke.py` returns ≥1.
    - `grep -c "\\-\\-budget-cap-usd" scripts/smoke/phase13_live_smoke.py` returns ≥1.
    - `grep -c "\\-\\-verbose-compression" scripts/smoke/phase13_live_smoke.py` returns ≥1.
    - `grep -c "BudgetExceededError" scripts/smoke/phase13_live_smoke.py` returns ≥1.
    - `grep -c "anchor_upload_evidence" scripts/smoke/phase13_live_smoke.py` returns ≥1.
    - `grep -c "smoke_e2e_" scripts/smoke/phase13_live_smoke.py` returns ≥1.
    - `grep -c "gate_count" scripts/smoke/phase13_live_smoke.py` returns ≥1.
    - `grep -c "ensure_ascii=False" scripts/smoke/phase13_live_smoke.py` returns ≥1.
    - `grep -ciE "selenium|webdriver" scripts/smoke/phase13_live_smoke.py` returns 0.
    - `grep -c "대표님" scripts/smoke/phase13_live_smoke.py` returns ≥1.
    - `grep -c "TODO" scripts/smoke/phase13_live_smoke.py` returns 0.
    - `py -3.11 scripts/smoke/phase13_live_smoke.py --help` exit 0 and stdout contains "Phase 13" + "--live" + "--max-attempts" + "--budget-cap-usd".
    - Dry-run smoke: `py -3.11 scripts/smoke/phase13_live_smoke.py` (no --live) exit 0 and ~$0.00 budget simulated, no real API calls.
  </acceptance_criteria>
  <done>
    단일 CLI `py -3.11 scripts/smoke/phase13_live_smoke.py --live --budget-cap-usd 5.00` 호출로 TREND → COMPLETE 13 GATE full E2E + 5 evidence 파일 생성 + cleanup + rc=0 완료 가능. dry-run mode 는 $0 로 env + simulation preview 제공.
  </done>
</task>

<task type="auto">
  <name>Task 13-05-02: tests/phase13/test_smoke_e2e_evidence.py — Tier 1 smoke_e2e fixture invariants</name>
  <files>tests/phase13/test_smoke_e2e_evidence.py</files>
  <read_first>
    - tests/phase13/fixtures/sample_smoke_e2e.json (Wave 0 산출물 — golden shape)
    - tests/phase13/test_evidence_shapes.py (Wave 1 Plan 02 Task 04 산출물 — `test_smoke_06_evidence_shape` 참조)
    - .planning/phases/13-live-smoke/13-VALIDATION.md row 13-05-02
  </read_first>
  <action>
    신규 파일 `tests/phase13/test_smoke_e2e_evidence.py` (≥80 lines, ~100 expected). Tier 1 전용 — 모든 테스트 `@pytest.mark.live_smoke` 미부착:

    1. `test_smoke_e2e_fixture_has_13_gate_timestamps(fixtures_dir)`:
       - `payload = json.loads((fixtures_dir / "sample_smoke_e2e.json").read_text(encoding="utf-8"))`.
       - `assert payload["gate_count"] == 13`.
       - `assert len(payload["gate_timestamps"]) >= 13`.
       - (IDLE + 12 operational + COMPLETE = 14 in some representations; Research fixture sample shows 13. Accept ≥13).

    2. `test_smoke_e2e_fixture_has_final_video_id(fixtures_dir)`:
       - `assert payload["final_video_id"]` — non-empty string.
       - `assert isinstance(payload["final_video_id"], str)`.
       - `assert len(payload["final_video_id"]) > 0`.

    3. `test_smoke_e2e_fixture_under_budget_cap(fixtures_dir)`:
       - `assert payload["total_cost_usd"] <= payload["budget_cap_usd"]`.
       - `assert payload["budget_cap_usd"] == 5.00` (Phase 13 SC#5 cap).
       - `assert payload["budget_breached"] is False`.

    4. `test_smoke_e2e_fixture_supervisor_rc1_zero(fixtures_dir)`:
       - `assert payload["supervisor_rc1_count"] == 0` (SMOKE-02 invariant — Phase 12 AGENT-STD-03 성과).

    5. `test_smoke_e2e_fixture_status_ok(fixtures_dir)`:
       - `assert payload["status"] == "OK"`.
       - `assert payload["wall_time_seconds"] > 0`.

    6. `test_smoke_e2e_evidence_dir_glob_pattern():`
       - Glob pattern 계약 검증 — Wave 4 runner 가 반드시 `smoke_e2e_<sid>.json` 포맷으로 파일명을 지을 것.
       - `import re; pattern = re.compile(r'^smoke_e2e_\d{8}_\d{6}\.json$'); assert pattern.match("smoke_e2e_20260421_223807.json")`.

    **구현 주의점**:
    - 본 테스트는 **fixture 만** 검증 — 실제 live evidence 는 Wave 5 Phase Gate (Plan 06) 의 `phase13_acceptance.py` 가 검증.
    - test 6 의 regex 계약은 Wave 4 runner 가 `datetime.now().strftime("%Y%m%d_%H%M%S")` 형식을 쓰도록 강제.
    - **금기 #2**: TODO 없음.
  </action>
  <verify>
    <automated>test -f tests/phase13/test_smoke_e2e_evidence.py &amp;&amp; grep -cE "^def test_smoke_e2e_" tests/phase13/test_smoke_e2e_evidence.py &amp;&amp; grep -c "gate_count" tests/phase13/test_smoke_e2e_evidence.py &amp;&amp; grep -c "final_video_id" tests/phase13/test_smoke_e2e_evidence.py &amp;&amp; grep -c "supervisor_rc1_count" tests/phase13/test_smoke_e2e_evidence.py &amp;&amp; py -3.11 -m pytest tests/phase13/test_smoke_e2e_evidence.py -m "not live_smoke" --no-cov -v</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/phase13/test_smoke_e2e_evidence.py` returns 0.
    - `wc -l tests/phase13/test_smoke_e2e_evidence.py` returns ≥80.
    - `grep -cE "^def test_smoke_e2e_" tests/phase13/test_smoke_e2e_evidence.py` returns ≥6.
    - `grep -c "gate_count" tests/phase13/test_smoke_e2e_evidence.py` returns ≥2.
    - `grep -c "final_video_id" tests/phase13/test_smoke_e2e_evidence.py` returns ≥2.
    - `grep -c "supervisor_rc1_count" tests/phase13/test_smoke_e2e_evidence.py` returns ≥1.
    - `grep -c "budget_cap_usd" tests/phase13/test_smoke_e2e_evidence.py` returns ≥1.
    - `grep -c "budget_breached" tests/phase13/test_smoke_e2e_evidence.py` returns ≥1.
    - `grep -c "smoke_e2e_" tests/phase13/test_smoke_e2e_evidence.py` returns ≥1.
    - `grep -c "pytest.mark.live_smoke" tests/phase13/test_smoke_e2e_evidence.py` returns 0 (Tier 1 only).
    - `grep -c "TODO" tests/phase13/test_smoke_e2e_evidence.py` returns 0.
    - `py -3.11 -m pytest tests/phase13/test_smoke_e2e_evidence.py -m "not live_smoke" --no-cov -v` exit 0 with ≥6 tests PASSED.
  </acceptance_criteria>
  <done>
    SMOKE-06 Tier 1 evidence invariants (6 tests) 가 golden fixture 에 대해 green. Wave 4 runner 가 생성할 live evidence 가 동일 shape 을 따라야 함을 강제.
  </done>
</task>

<task type="auto">
  <name>Task 13-05-03: tests/phase13/test_live_run.py — Tier 2 subprocess wrapper for phase13_live_smoke.py</name>
  <files>tests/phase13/test_live_run.py</files>
  <read_first>
    - scripts/smoke/phase13_live_smoke.py (Task 13-05-01 산출물 — CLI 계약)
    - tests/phase13/conftest.py (--run-live + tmp_evidence_dir)
    - .planning/phases/13-live-smoke/13-VALIDATION.md row 13-05-01 / 13-05-02
  </read_first>
  <action>
    신규 파일 `tests/phase13/test_live_run.py` (≥60 lines, ~90 expected). 2 테스트 (1 Tier 1 dry-run + 1 Tier 2 live):

    1. `test_live_run_dry_mode_exits_cleanly(tmp_path)` — Tier 1, always-run:
       - `env = {**os.environ, "PYTHONIOENCODING": "utf-8", "SHORTS_PUBLISH_LOCK_PATH": str(tmp_path / "lock.json")}`.
       - `result = subprocess.run([sys.executable, "scripts/smoke/phase13_live_smoke.py"], capture_output=True, text=True, env=env, cwd=str(REPO_ROOT), timeout=120)`.
       - `assert result.returncode == 0` — dry-run 이 기본값이므로 $0 cost + rc=0.
       - `assert "Phase 13" in result.stdout or "phase13" in result.stdout.lower()`.
       - 실 API 호출 0회 — dry-run 경로 검증만.

    2. `test_live_run_full_e2e_produces_5_evidence_files(tmp_path, tmp_evidence_dir)` — Tier 2, `@pytest.mark.live_smoke`:
       - `sid = "test_" + datetime.now().strftime("%Y%m%d_%H%M%S")`.
       - 본 test 는 **실 API 호출 경유** — 대표님 승인 후 `--run-live` 로 실행.
       - `env = {**os.environ, "PYTHONIOENCODING": "utf-8"}` (tmp_evidence_dir fixture 가 이미 SHORTS_PUBLISH_LOCK_PATH 설정).
       - `cmd = [sys.executable, "scripts/smoke/phase13_live_smoke.py", "--live", "--max-attempts", "2", "--budget-cap-usd", "5.00", "--session-id", sid, "--state-root", str(tmp_path / "state")]`.
       - `result = subprocess.run(cmd, capture_output=True, text=True, env=env, cwd=str(REPO_ROOT), timeout=1800)` — 30 min max.
       - `assert result.returncode == 0, f"phase13_live_smoke.py rc={result.returncode} stderr={result.stderr[-500:]}"`.
       - 5 evidence 파일 존재 검증 (실제 `.planning/phases/13-live-smoke/evidence/` 경로 — 본 Tier 2 는 tmp 사용하지 않음, Phase 13 완결을 위해 실 evidence anchor 필요):
         ```python
         evidence_root = REPO_ROOT / ".planning" / "phases" / "13-live-smoke" / "evidence"
         for prefix in ("producer_output", "supervisor_output", "smoke_upload", "budget_usage", "smoke_e2e"):
             matches = list(evidence_root.glob(f"{prefix}_{sid}.json"))
             assert matches, f"{prefix}_{sid}.json not found in {evidence_root}"
             payload = json.loads(matches[0].read_text(encoding="utf-8"))
             assert payload.get("session_id") == sid
         ```
       - 특별 검증: `smoke_e2e_<sid>.json` 의 `gate_count == 13` + `final_video_id` 존재 + `budget_breached is False`.

    **구현 주의점**:
    - Tier 2 는 실 YouTube upload + cleanup + 실제 evidence 파일 작성 — 한 번 실행 시 $1.50~$3.00 비용 발생 (Research Expected Total). 대표님 승인 후만 `--run-live` 로 실행.
    - subprocess timeout 1800s (30 min) — Research §Max feedback latency (live-smoke 30min upper bound).
    - Tier 2 는 `.planning/phases/13-live-smoke/evidence/` 실경로에 쓰는 이유: Phase 13 공식 완료 판정 (Plan 06 Phase Gate) 이 본 경로의 evidence chain 을 검증하기 때문. tmp 는 이 목적 달성 불가.
    - **금기 #2**: TODO 없음. **금기 #3**: subprocess 실패 시 `result.returncode != 0` 명시적 assert + stderr tail 출력.
    - **금기 #8**: cleanup=True 는 runner 내부 로직 — 본 test 는 rc=0 만 확인.
  </action>
  <verify>
    <automated>test -f tests/phase13/test_live_run.py &amp;&amp; grep -c "pytest.mark.live_smoke" tests/phase13/test_live_run.py &amp;&amp; grep -c "subprocess.run" tests/phase13/test_live_run.py &amp;&amp; grep -c "phase13_live_smoke.py" tests/phase13/test_live_run.py &amp;&amp; py -3.11 -m pytest tests/phase13/test_live_run.py -m "not live_smoke" --no-cov -v</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/phase13/test_live_run.py` returns 0.
    - `wc -l tests/phase13/test_live_run.py` returns ≥60.
    - `grep -cE "^def test_live_run_" tests/phase13/test_live_run.py` returns ≥2.
    - `grep -c "pytest.mark.live_smoke" tests/phase13/test_live_run.py` returns ≥1.
    - `grep -c "subprocess.run" tests/phase13/test_live_run.py` returns ≥2.
    - `grep -c "phase13_live_smoke.py" tests/phase13/test_live_run.py` returns ≥2.
    - `grep -c "\\-\\-live" tests/phase13/test_live_run.py` returns ≥1.
    - `grep -c "\\-\\-budget-cap-usd" tests/phase13/test_live_run.py` returns ≥1.
    - `grep -c "producer_output" tests/phase13/test_live_run.py` returns ≥1.
    - `grep -c "supervisor_output" tests/phase13/test_live_run.py` returns ≥1.
    - `grep -c "smoke_upload" tests/phase13/test_live_run.py` returns ≥1.
    - `grep -c "budget_usage" tests/phase13/test_live_run.py` returns ≥1.
    - `grep -c "smoke_e2e" tests/phase13/test_live_run.py` returns ≥1.
    - `grep -c "TODO" tests/phase13/test_live_run.py` returns 0.
    - `py -3.11 -m pytest tests/phase13/test_live_run.py -m "not live_smoke" --no-cov -v` exit 0 with ≥1 Tier 1 test PASSED (dry-run).
  </acceptance_criteria>
  <done>
    Tier 1 dry-run test (always-run 자동 검증) + Tier 2 full E2E subprocess wrapper (대표님 승인 후 `--run-live` 로 실행) 가 확립. SMOKE-01~06 모두 Wave 4 실행으로 실증 가능.
  </done>
</task>

</tasks>

<verification>
Wave 4 완료 조건 (Plan 05):
1. `scripts/smoke/phase13_live_smoke.py` 신설 (≥250 lines) + `--help` exit 0 + dry-run exit 0.
2. `tests/phase13/test_smoke_e2e_evidence.py` ≥6 Tier 1 tests GREEN.
3. `tests/phase13/test_live_run.py` ≥2 tests (1 Tier 1 dry-run + 1 Tier 2 live).
4. 누계 pytest: `pytest tests/phase13/ -m "not live_smoke" --no-cov -q` exit 0 with Wave 0~4 합산 ≥38 GREEN (Wave 0=6 + Wave 1=11 + Wave 2=7 + Wave 3=7 + Wave 4=7 min).
5. 금기 #5/#8 재확인 — `grep -rciE "selenium|webdriver" scripts/smoke/phase13_live_smoke.py` returns 0 + `grep -c "SHORTS_PUBLISH_LOCK_PATH" scripts/smoke/phase13_live_smoke.py` returns ≥1.
6. Wave 0 preflight 연계 — runner 내부에서 `scripts.smoke.phase13_preflight` import 또는 문서에 pre-run 체크 권장 명시.
</verification>

<success_criteria>
- SMOKE-06 (Full E2E 13 GATE dispatched + 5 evidence 파일 + final_video_id + cleanup) 실증 harness 구축.
- 본 Plan 완료 시 대표님이 `py -3.11 scripts/smoke/phase13_live_smoke.py --live --budget-cap-usd 5.00` 한 번 실행으로 Phase 13 SMOKE-01~06 전체 실증 가능.
- Phase 11 deferred SC#1 + SC#2 의 **실환경 완결** 경로 준비.
- CLAUDE.md 금기 #2/#3/#5/#8 전수 준수 + 필수 #7/#8 준수.
</success_criteria>

<output>
완료 후 `.planning/phases/13-live-smoke/13-05-SUMMARY.md`:
- 3 Task 산출 + verify rc.
- `phase13_live_smoke.py` 최종 line count + `--help` stdout 발췌.
- Dry-run 실행 시 counter.persist() 결과 (예상 $0.00).
- Wave 0~4 누계 Tier 1 tests 수 (예상 ≥38).
- 대표님 보고 2-문장 존댓말 요약 — "Wave 4 완료로 `--live` 실행 준비 완료".
</output>
