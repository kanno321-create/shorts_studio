---
phase: 13-live-smoke
plan: 06
plan_id: 13-06
type: execute
wave: 5
depends_on:
  - 13-01
  - 13-02
  - 13-03
  - 13-04
  - 13-05
files_modified:
  - scripts/validate/phase13_acceptance.py
  - tests/phase13/test_phase13_acceptance.py
  - .planning/phases/13-live-smoke/13-TRACEABILITY.md
  - .planning/phases/13-live-smoke/13-VALIDATION.md
autonomous: true
requirements:
  - SMOKE-01
  - SMOKE-02
  - SMOKE-03
  - SMOKE-04
  - SMOKE-05
  - SMOKE-06
commit_discipline: phase-gate
must_haves:
  truths:
    - "`scripts/validate/phase13_acceptance.py` 가 Phase 13 의 6 SC (SMOKE-01~06) + 2 audit (harness_audit ≥80 + drift_scan A급 0 건) 전수를 단일 `py -3.11 -m scripts.validate.phase13_acceptance` 호출로 검증 + `.planning/phases/13-live-smoke/13-06-acceptance.json` 에 결과 anchor — Phase 14 `phase14_acceptance.py` 패턴 복제"
    - "`tests/phase13/test_phase13_acceptance.py` 가 acceptance.py 를 subprocess 로 호출하여 8 개 `def test_phase13_*` (1 all_passed + 6 SC + 1 harness_audit + 1 drift_scan) Tier 1 wrapper 로 감싸 `pytest tests/phase13/test_phase13_acceptance.py` 단독 실행이 Phase 13 gate 판정을 대변"
    - "`13-TRACEABILITY.md` 가 REQ (SMOKE-01~06) × SC (SC#1~6 from ROADMAP) × Plan (13-01~05) × Evidence (5 evidence 파일 path) 4-way 매트릭스 제공 + 대표님 보고 블록 (한국어 존댓말 baseline) + Phase 11 SC#1/SC#2 deferred 해소 evidence 인용"
    - "`13-VALIDATION.md` 가 HARD-GATE 통과 시 `status: approved` + `nyquist_compliant: true` + `wave_0_complete: true` + `approved: 2026-04-21` 로 flip — Phase 14 14-05 Task 04 Step 1a `grep -q 'ALL_PASS' ... || exit 1` 패턴 재현 (Plan 05 실 live run → 5 evidence 파일 → acceptance ALL_PASS → VALIDATION flip)"
    - "HARD-GATE 실패 시 VALIDATION flip 불가 — Step 1a `grep -q 'ALL_PASS' 13-06-phase-gate.log || exit 1` 가 codified 차단 (금기 #1 skip_gates 물리 enforcement 재현)"
  artifacts:
    - path: "scripts/validate/phase13_acceptance.py"
      provides: "Phase 13 gate aggregator — 6 SC + 2 audit 자동 검증"
      contains: "SMOKE-01"
      min_lines: 180
    - path: "tests/phase13/test_phase13_acceptance.py"
      provides: "Phase 13 E2E pytest wrapper — phase14 패턴 복제"
      contains: "test_phase13_acceptance_all_passed"
      min_lines: 80
    - path: ".planning/phases/13-live-smoke/13-TRACEABILITY.md"
      provides: "REQ × SC × Plan × Evidence 4-way 매트릭스 + 대표님 보고 블록"
      contains: "SMOKE-01"
      min_lines: 100
  key_links:
    - from: "scripts/validate/phase13_acceptance.py"
      to: "5 evidence 파일 in .planning/phases/13-live-smoke/evidence/"
      via: "pathlib.glob(producer_output_*.json 등) + json.loads + schema assertion"
      pattern: "evidence.*\\.glob"
    - from: "scripts/validate/phase13_acceptance.py"
      to: "pytest tests/phase13 -m \"not live_smoke\" + harness_audit + drift_scan"
      via: "subprocess.run + rc check + JSON summary"
      pattern: "subprocess.run"
    - from: "tests/phase13/test_phase13_acceptance.py"
      to: "scripts/validate/phase13_acceptance.py"
      via: "subprocess + 13-06-acceptance.json read + per-SC pytest functions"
      pattern: "scripts.validate.phase13_acceptance"
    - from: "13-VALIDATION.md HARD-GATE"
      to: "13-06-phase-gate.log ALL_PASS token"
      via: "`grep -q 'ALL_PASS' ... || exit 1` codified enforcement"
      pattern: "ALL_PASS"
---

<objective>
Wave 5 Phase Gate — Phase 13 의 모든 Success Criteria (6 SC) + 2 audit (harness_audit ≥80 + drift_scan A급 0) 를 단일 aggregator 로 자동 검증하고, Phase 14 14-05 패턴 (phase14_acceptance.py + test_phase14_acceptance.py + 14-TRACEABILITY.md + 14-VALIDATION.md HARD-GATE flip) 을 그대로 복제하여 Phase 13 공식 완료 판정 인프라를 확립한다.

Purpose: 본 Plan 완료 시 `/gsd:verify-work 13` 또는 `/gsd:execute-phase 13 --phase-gate` 가 자동 판정 가능. Phase 13 전체가 Phase 14 와 동일한 reproducible gate 패턴을 따르게 되어 향후 Phase 15+ 감사 때 참조 자료로 재사용 가능.

Output: 1 aggregator (`phase13_acceptance.py` ~200 lines) + 1 pytest wrapper (`test_phase13_acceptance.py` ~100 lines) + 1 TRACEABILITY 문서 (~120 lines) + `13-VALIDATION.md` frontmatter flip (status / nyquist_compliant / wave_0_complete / approved 4 field).

**Sequential within wave**: Task 13-06-01 (phase13_acceptance.py) → 13-06-02 (test_phase13_acceptance.py) → 13-06-03 (13-TRACEABILITY.md) → 13-06-04 (VALIDATION flip + HARD-GATE). acceptance.py 가 선행되어야 test 가 이를 호출 가능. TRACEABILITY 는 evidence 수집 완료 후 정확. VALIDATION flip 은 HARD-GATE 통과 후만.

**Critical dependency**: Wave 0~4 모두 완료 후에만 실행. 특히 **실 live run (`phase13_live_smoke.py --live`) 이 대표님 승인 하에 한 번 이상 성공적으로 실행되어 5 evidence 파일이 생성되어 있어야 함** — 그렇지 않으면 SC#1~6 중 실 evidence 기반 SC 검증이 실패.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-live-smoke/13-RESEARCH.md
@.planning/phases/13-live-smoke/13-VALIDATION.md
@CLAUDE.md

# Wave 0~4 산출물 (dependency)
@scripts/smoke/phase13_live_smoke.py
@scripts/smoke/budget_counter.py
@scripts/smoke/upload_evidence.py
@scripts/smoke/evidence_extractor.py
@scripts/smoke/provider_rates.py

# Phase 14 pattern to replicate (gold standard)
@scripts/validate/phase14_acceptance.py
@tests/phase14/test_phase14_acceptance.py
@.planning/phases/14-api-adapter-remediation/14-TRACEABILITY.md
@.planning/phases/14-api-adapter-remediation/14-VALIDATION.md
@.planning/phases/14-api-adapter-remediation/14-05-PLAN.md

# Audit scripts (invoked as part of gate)
@scripts/validate/harness_audit.py
@scripts/validate/drift_scan.py
</context>

<interfaces>
<!-- Wave 0~4 산출물 (consume only) -->
scripts/smoke/phase13_live_smoke.py: CLI runner with --live / --dry-run / --budget-cap-usd 5.00 / --session-id
Evidence files (written by Wave 4 live run):
  .planning/phases/13-live-smoke/evidence/producer_output_<sid>.json
  .planning/phases/13-live-smoke/evidence/supervisor_output_<sid>.json
  .planning/phases/13-live-smoke/evidence/smoke_upload_<sid>.json
  .planning/phases/13-live-smoke/evidence/budget_usage_<sid>.json
  .planning/phases/13-live-smoke/evidence/smoke_e2e_<sid>.json

<!-- Phase 14 pattern (consume only — copy structure) -->
scripts/validate/phase14_acceptance.py (272 lines):
  - _run(cmd, label) -> dict with rc + stdout_tail + stderr_tail
  - check_sc1_phase_sweep() through check_sc5_full_regression() (5 SC)
  - check_harness_audit() + check_drift_scan() (2 audit)
  - main() -> int (0 ALL_PASS, 1 FAIL)
  - JSON summary → .planning/phases/14-.../14-05-acceptance.json

tests/phase14/test_phase14_acceptance.py:
  - acceptance_result fixture (scope="module") — subprocess run + JSON read
  - 8 tests: test_phase14_acceptance_all_passed + per-SC + audit

<!-- NO NEW CONTRACTS — Wave 5 only consumes. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 13-06-01: scripts/validate/phase13_acceptance.py — Phase 13 gate aggregator (6 SC + 2 audit)</name>
  <files>scripts/validate/phase13_acceptance.py</files>
  <read_first>
    - scripts/validate/phase14_acceptance.py (272 lines — 전체 구조 복제 base)
    - scripts/validate/harness_audit.py + drift_scan.py (invocation interface)
    - .planning/ROADMAP.md (Phase 13 §Success Criteria 6 건 L348-354)
    - .planning/REQUIREMENTS.md §SMOKE-01~06 (L410-416)
    - .planning/phases/13-live-smoke/13-VALIDATION.md row 13-06-01
  </read_first>
  <action>
    신규 파일 `scripts/validate/phase13_acceptance.py` (≥180 lines, ~230 expected). phase14_acceptance.py 의 구조를 그대로 복제 + Phase 13 의 6 SC 로 확장:

    **Module docstring** (L1-12):
    ```python
    """Phase 13 Acceptance — Live Smoke 재도전 gate aggregator.

    6 Success Criteria 자동 검증:
      - SC#1: Real Claude CLI producer call + producer_output evidence anchor (SMOKE-01)
      - SC#2: Real Claude CLI supervisor 17-inspector fan-out + rc=1 재현 0회 (SMOKE-02)
      - SC#3: YouTube unlisted upload + cleanup + public ValueError (SMOKE-03)
      - SC#4: production_metadata HTML comment videos.get readback 4 필드 (SMOKE-04)
      - SC#5: Budget cap $5.00 enforcement + budget_usage.json (SMOKE-05)
      - SC#6: Full E2E TREND→COMPLETE 13 gate + final_video_id + total_cost_usd (SMOKE-06)

    추가 2 audit: harness_audit 점수 ≥80 + drift_scan A급 drift 0 건.

    phase14_acceptance.py 패턴 복제. 실행 결과를 13-06-acceptance.json 에 anchor.
    """
    ```

    **Imports + constants** (L14-30):
    ```python
    from __future__ import annotations
    import json, os, subprocess, sys
    from datetime import datetime
    from pathlib import Path

    REPO = Path(__file__).resolve().parents[2]
    EVIDENCE_DIR = REPO / ".planning" / "phases" / "13-live-smoke" / "evidence"
    ```

    **_run(cmd, label) helper** (phase14 L141-164 그대로 복제, PYTHONIOENCODING=utf-8 + timeout 1200s).

    **check_sc1_producer_output_evidence()** (SMOKE-01):
    - `files = sorted(EVIDENCE_DIR.glob("producer_output_*.json"))`.
    - `if not files: return {"label": "SC#1 producer_output_evidence", "rc": 1, "reason": "evidence missing"}`.
    - Parse latest — assert `session_id` + `producer_gates: dict` + `len(producer_gates) >= 1`.
    - Return rc=0 on all asserts pass.

    **check_sc2_supervisor_rc1_zero()** (SMOKE-02):
    - `files = sorted(EVIDENCE_DIR.glob("supervisor_output_*.json"))`.
    - Parse latest — assert `rc1_count == 0` + `len(supervisor_calls) >= 1`.

    **check_sc3_upload_evidence()** (SMOKE-03):
    - `files = sorted(EVIDENCE_DIR.glob("smoke_upload_*.json"))`.
    - Parse latest — assert `video_id` non-empty.
    - Also invoke unit test: `_run([sys.executable, "-m", "pytest", "tests/phase13/test_smoke_03_upload_contract.py", "-m", "not live_smoke", "-q", "--no-cov"], "SC#3 upload_contract_unit")` — verify ValueError 방어 여전히 green.
    - Combine rc.

    **check_sc4_production_metadata_readback()** (SMOKE-04):
    - `files = sorted(EVIDENCE_DIR.glob("smoke_upload_*.json"))`.
    - Parse latest — assert `required_fields_present is True` + `missing_fields == []` + 4 필드 all non-empty.

    **check_sc5_budget_cap_enforcement()** (SMOKE-05):
    - `files = sorted(EVIDENCE_DIR.glob("budget_usage_*.json"))`.
    - Parse latest — assert `cap_usd == 5.00` + `total_usd <= cap_usd` + `breached is False` + `entry_count >= 1`.

    **check_sc6_full_e2e_evidence()** (SMOKE-06):
    - `files = sorted(EVIDENCE_DIR.glob("smoke_e2e_*.json"))`.
    - Parse latest — assert `gate_count == 13` + `final_video_id` non-empty + `total_cost_usd <= budget_cap_usd` + `budget_breached is False` + `supervisor_rc1_count == 0`.

    **check_phase13_tier1_regression()**:
    - `_run([sys.executable, "-m", "pytest", "tests/phase13/", "-m", "not live_smoke", "-q", "--no-cov"], "Phase 13 Tier 1 regression")`.
    - Wave 0~4 ≥38 tests GREEN 유지 확인.

    **check_harness_audit()** + **check_drift_scan()** — phase14 L224-237 그대로 복제.

    **main()**:
    ```python
    def main() -> int:
        checks = [
            check_sc1_producer_output_evidence(),
            check_sc2_supervisor_rc1_zero(),
            check_sc3_upload_evidence(),
            check_sc4_production_metadata_readback(),
            check_sc5_budget_cap_enforcement(),
            check_sc6_full_e2e_evidence(),
            check_phase13_tier1_regression(),
            check_harness_audit(),
            check_drift_scan(),
        ]
        all_ok = all(c.get("rc", 1) == 0 for c in checks)
        summary = {
            "phase": "13-live-smoke",
            "timestamp": datetime.utcnow().isoformat(timespec="seconds") + "Z",
            "all_passed": all_ok,
            "checks": checks,
        }
        out_path = REPO / ".planning" / "phases" / "13-live-smoke" / "13-06-acceptance.json"
        out_path.parent.mkdir(parents=True, exist_ok=True)
        out_path.write_text(json.dumps(summary, ensure_ascii=False, indent=2), encoding="utf-8")
        print(f"Phase 13 Acceptance: {'ALL_PASS' if all_ok else 'FAIL'}")
        for c in checks:
            print(f"  {c['label']}: rc={c.get('rc', 'n/a')}")
        return 0 if all_ok else 1


    if __name__ == "__main__":
        sys.exit(main())
    ```

    **구현 주의점**:
    - 각 `check_sc*_*` 함수는 `{"label": "SC#N ...", "rc": 0 or 1, ...추가 key}` 반환 — phase14 인터페이스와 동일.
    - evidence glob 은 `sorted(...)` 로 timestamp 순 — 항상 최신 파일 사용.
    - evidence 파일 부재 시 rc=1 + `"reason": "evidence missing"` — 금기 #3 침묵 금지.
    - `check_phase13_tier1_regression` 은 Tier 1 ≥38 tests 가 여전히 green 임을 확인하는 invariant — Wave 0~4 에서 도입된 후 accidental regression 발생 가능.
    - `harness_audit` + `drift_scan` invocation 은 phase14 와 동일 interface (CLI 가 아니면 python 한 줄 wrapper 사용).
    - **금기 #2**: TODO 없음. **금기 #3**: subprocess 실패 시 rc 명시적 기록 (`capture_output=True` 후속 rc 검사).
    - **필수 #7**: 출력 로그에 "대표님" literal 1+ 위치 (예: 최종 print "Phase 13 Acceptance 대표님 확인").
  </action>
  <verify>
    <automated>test -f scripts/validate/phase13_acceptance.py &amp;&amp; grep -cE "def check_sc[1-6]_" scripts/validate/phase13_acceptance.py &amp;&amp; grep -c "SMOKE-0[1-6]" scripts/validate/phase13_acceptance.py &amp;&amp; grep -c "harness_audit" scripts/validate/phase13_acceptance.py &amp;&amp; grep -c "drift_scan" scripts/validate/phase13_acceptance.py &amp;&amp; grep -c "PYTHONIOENCODING" scripts/validate/phase13_acceptance.py &amp;&amp; py -3.11 -c "import importlib.util; spec=importlib.util.spec_from_file_location('p13','scripts/validate/phase13_acceptance.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); print('import OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/validate/phase13_acceptance.py` returns 0.
    - `wc -l scripts/validate/phase13_acceptance.py` returns ≥180.
    - `grep -cE "def check_sc[1-6]_" scripts/validate/phase13_acceptance.py` returns ≥6 (6 SC 모두 defined).
    - `grep -cE "SMOKE-0[1-6]" scripts/validate/phase13_acceptance.py` returns ≥6.
    - `grep -c "harness_audit" scripts/validate/phase13_acceptance.py` returns ≥1.
    - `grep -c "drift_scan" scripts/validate/phase13_acceptance.py` returns ≥1.
    - `grep -c "check_phase13_tier1_regression" scripts/validate/phase13_acceptance.py` returns ≥1.
    - `grep -c "PYTHONIOENCODING" scripts/validate/phase13_acceptance.py` returns ≥1 (cp949 mitigation).
    - `grep -c "ensure_ascii=False" scripts/validate/phase13_acceptance.py` returns ≥1.
    - `grep -c "producer_output_" scripts/validate/phase13_acceptance.py` returns ≥1.
    - `grep -c "supervisor_output_" scripts/validate/phase13_acceptance.py` returns ≥1.
    - `grep -c "smoke_upload_" scripts/validate/phase13_acceptance.py` returns ≥1.
    - `grep -c "budget_usage_" scripts/validate/phase13_acceptance.py` returns ≥1.
    - `grep -c "smoke_e2e_" scripts/validate/phase13_acceptance.py` returns ≥1.
    - `grep -c "ALL_PASS" scripts/validate/phase13_acceptance.py` returns ≥1.
    - `grep -c "대표님" scripts/validate/phase13_acceptance.py` returns ≥1.
    - `grep -c "TODO" scripts/validate/phase13_acceptance.py` returns 0.
    - `py -3.11 -c "import importlib.util; spec=importlib.util.spec_from_file_location('p','scripts/validate/phase13_acceptance.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m)"` exit 0.
  </acceptance_criteria>
  <done>
    Phase 13 의 6 SC + 2 audit + 1 Tier 1 regression = 9 체크를 단일 `py -3.11 -m scripts.validate.phase13_acceptance` 호출로 실행 + JSON anchor 생성 가능. Phase 14 패턴 복제 완료.
  </done>
</task>

<task type="auto">
  <name>Task 13-06-02: tests/phase13/test_phase13_acceptance.py — E2E pytest wrapper</name>
  <files>tests/phase13/test_phase13_acceptance.py</files>
  <read_first>
    - tests/phase14/test_phase14_acceptance.py (105 lines — 전체 구조 복제)
    - scripts/validate/phase13_acceptance.py (Task 13-06-01 산출물)
    - .planning/phases/13-live-smoke/13-VALIDATION.md row 13-06-02
  </read_first>
  <action>
    신규 파일 `tests/phase13/test_phase13_acceptance.py` (≥80 lines, ~120 expected). phase14 test 파일 구조 그대로 복제 + Phase 13 SC 로 교체:

    ```python
    """Phase 13 E2E Acceptance Wrapper.

    phase13_acceptance.py 를 subprocess 로 호출하여 6 SC + 2 audit + 1 Tier 1
    regression = 9 체크 결과를 pytest 테스트로 감싼다. Phase 14 14-05 패턴 복제.
    """
    from __future__ import annotations

    import json
    import os
    import subprocess
    import sys
    from pathlib import Path

    import pytest

    REPO = Path(__file__).resolve().parents[2]


    @pytest.fixture(scope="module")
    def acceptance_result():
        """phase13_acceptance.py 실행 후 JSON 산출물 반환."""
        env = os.environ.copy()
        env["PYTHONIOENCODING"] = "utf-8"
        rc = subprocess.run(
            [sys.executable, "-m", "scripts.validate.phase13_acceptance"],
            cwd=str(REPO),
            env=env,
            timeout=1500,
        ).returncode
        out_path = REPO / ".planning" / "phases" / "13-live-smoke" / "13-06-acceptance.json"
        assert out_path.exists(), f"acceptance JSON missing: {out_path}"
        return rc, json.loads(out_path.read_text(encoding="utf-8"))


    def test_phase13_acceptance_all_passed(acceptance_result):
        """Phase 13 9 checks 전체 green."""
        rc, summary = acceptance_result
        assert rc == 0, f"phase13_acceptance.py rc={rc}, summary={summary}"
        assert summary["all_passed"] is True


    def test_phase13_sc1_producer_output_evidence(acceptance_result):
        """SC#1 — SMOKE-01 producer_output anchor."""
        _, summary = acceptance_result
        sc1 = next(c for c in summary["checks"] if c["label"].startswith("SC#1"))
        assert sc1["rc"] == 0


    def test_phase13_sc2_supervisor_rc1_zero(acceptance_result):
        """SC#2 — SMOKE-02 rc=1 재현 0회."""
        _, summary = acceptance_result
        sc2 = next(c for c in summary["checks"] if c["label"].startswith("SC#2"))
        assert sc2["rc"] == 0


    def test_phase13_sc3_upload_evidence(acceptance_result):
        """SC#3 — SMOKE-03 unlisted upload + public ValueError."""
        _, summary = acceptance_result
        sc3 = next(c for c in summary["checks"] if c["label"].startswith("SC#3"))
        assert sc3["rc"] == 0


    def test_phase13_sc4_production_metadata_readback(acceptance_result):
        """SC#4 — SMOKE-04 4 필드 readback."""
        _, summary = acceptance_result
        sc4 = next(c for c in summary["checks"] if c["label"].startswith("SC#4"))
        assert sc4["rc"] == 0


    def test_phase13_sc5_budget_cap_enforcement(acceptance_result):
        """SC#5 — SMOKE-05 $5 cap + budget_usage.json."""
        _, summary = acceptance_result
        sc5 = next(c for c in summary["checks"] if c["label"].startswith("SC#5"))
        assert sc5["rc"] == 0


    def test_phase13_sc6_full_e2e_evidence(acceptance_result):
        """SC#6 — SMOKE-06 Full E2E 13 gate + final_video_id."""
        _, summary = acceptance_result
        sc6 = next(c for c in summary["checks"] if c["label"].startswith("SC#6"))
        assert sc6["rc"] == 0


    def test_phase13_tier1_regression_preserved(acceptance_result):
        """Phase 13 Tier 1 Wave 0~4 tests ≥38 GREEN 유지."""
        _, summary = acceptance_result
        reg = next(c for c in summary["checks"] if "Phase 13 Tier 1" in c.get("label", ""))
        assert reg["rc"] == 0


    def test_phase13_harness_audit_min_80(acceptance_result):
        """harness_audit 점수 ≥80 유지."""
        _, summary = acceptance_result
        audit = next(c for c in summary["checks"] if c["label"].startswith("AUDIT harness"))
        assert audit["rc"] == 0


    def test_phase13_drift_scan_no_a_class(acceptance_result):
        """drift_scan A급 drift 0 건 (AUDIT-04 clear)."""
        _, summary = acceptance_result
        drift = next(c for c in summary["checks"] if c["label"].startswith("AUDIT drift"))
        assert drift["rc"] == 0
    ```

    **구현 주의점**:
    - `scope="module"` — acceptance.py 가 ≥5분 걸리므로 9 테스트가 fixture 공유.
    - 각 SC 테스트는 JSON 의 해당 check 만 뽑아 assert — 실패 시 즉시 어느 SC 실패인지 식별.
    - timeout 1500s (25 min) — acceptance.py 의 1200s timeout + subprocess overhead.
    - Tier 2 marker 없음 — 본 wrapper 는 Tier 1 (전체 Phase 13 Tier 1 regression 포함). 실제 live run 은 acceptance.py 내부에서 evidence 파일 존재 확인으로만 검증 (실 API 재호출 아님).
    - **금기 #2**: TODO 없음. **금기 #3**: subprocess 실패 pytest assertion 으로 명시적 전파.
  </action>
  <verify>
    <automated>test -f tests/phase13/test_phase13_acceptance.py &amp;&amp; grep -cE "^def test_phase13_" tests/phase13/test_phase13_acceptance.py &amp;&amp; grep -c "scripts.validate.phase13_acceptance" tests/phase13/test_phase13_acceptance.py &amp;&amp; grep -c "PYTHONIOENCODING" tests/phase13/test_phase13_acceptance.py &amp;&amp; py -3.11 -m pytest tests/phase13/test_phase13_acceptance.py --collect-only -q --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/phase13/test_phase13_acceptance.py` returns 0.
    - `wc -l tests/phase13/test_phase13_acceptance.py` returns ≥80.
    - `grep -cE "^def test_phase13_" tests/phase13/test_phase13_acceptance.py` returns ≥10 (1 all_passed + 6 SC + 1 tier1 + 2 audit = 10).
    - `grep -c "scripts.validate.phase13_acceptance" tests/phase13/test_phase13_acceptance.py` returns ≥1.
    - `grep -c "PYTHONIOENCODING" tests/phase13/test_phase13_acceptance.py` returns ≥1.
    - `grep -c "acceptance_result" tests/phase13/test_phase13_acceptance.py` returns ≥10.
    - `grep -c "TODO" tests/phase13/test_phase13_acceptance.py` returns 0.
    - `py -3.11 -m pytest tests/phase13/test_phase13_acceptance.py --collect-only -q --no-cov` exit 0 with ≥10 tests collected.
  </acceptance_criteria>
  <done>
    `pytest tests/phase13/test_phase13_acceptance.py` 단독 호출이 Phase 13 전체 gate 판정 + 9 체크 개별 검증을 pytest UI 로 제공. Phase 14 패턴 재현 완료.
  </done>
</task>

<task type="auto">
  <name>Task 13-06-03: 13-TRACEABILITY.md — REQ × SC × Plan × Evidence 4-way 매트릭스</name>
  <files>.planning/phases/13-live-smoke/13-TRACEABILITY.md</files>
  <read_first>
    - .planning/phases/14-api-adapter-remediation/14-TRACEABILITY.md (패턴 원본)
    - .planning/ROADMAP.md (Phase 13 Success Criteria 6 건 L348-354)
    - .planning/REQUIREMENTS.md §SMOKE-01~06 + §Milestone v1.0.2 (L405-454)
    - .planning/phases/13-live-smoke/13-RESEARCH.md §Phase Requirements table (L69-76)
    - .planning/phases/13-live-smoke/13-06-acceptance.json (Task 13-06-01 실행 후 생성됨)
    - .planning/phases/11-pipeline-real-run-activation-script-quality-mode/11-VERIFICATION.md (Phase 11 deferred SC#1/SC#2 인용)
  </read_first>
  <action>
    신규 파일 `.planning/phases/13-live-smoke/13-TRACEABILITY.md` (≥100 lines, ~150 expected). 다음 구조:

    ```markdown
    ---
    phase: 13-live-smoke
    status: complete
    finalized: 2026-04-21
    ---

    # Phase 13 Traceability — REQ × SC × Plan × Evidence

    ## Requirement Coverage Matrix

    | REQ-ID | Description | Covered by Plan(s) | Primary Artifact | Evidence |
    |--------|-------------|--------------------|------------------|----------|
    | SMOKE-01 | Real Claude CLI producer 호출 + producer_output JSON anchor | 13-02 Task 13-02-01/02, 13-05 Task 13-05-01 | scripts/smoke/evidence_extractor.py + phase13_live_smoke.py | evidence/producer_output_<sid>.json + 13-06-acceptance.json checks[0] |
    | SMOKE-02 | Real Claude CLI supervisor 17-inspector fan-out + rc=1 재현 0회 | 13-02 Task 13-02-01/03, 13-05 Task 13-05-01 | scripts/orchestrator/invokers.py::_compress_producer_output + supervisor_output evidence | evidence/supervisor_output_<sid>.json (rc1_count==0) + 13-06-acceptance.json checks[1] |
    | SMOKE-03 | YouTube unlisted + cleanup + public ValueError | 13-03 Task 13-03-01/02, 13-05 Task 13-05-01 | scripts/publisher/smoke_test.py (Phase 8 ship) + scripts/smoke/upload_evidence.py | evidence/smoke_upload_<sid>.json + 13-06-acceptance.json checks[2] |
    | SMOKE-04 | production_metadata HTML comment videos.get readback 4 필드 | 13-03 Task 13-03-01/03, 13-05 Task 13-05-01 | scripts/smoke/upload_evidence.py::anchor_upload_evidence | evidence/smoke_upload_<sid>.json (required_fields_present==true) + 13-06-acceptance.json checks[3] |
    | SMOKE-05 | Budget cap $5 enforcement + budget_usage.json | 13-01 Task 13-01-02, 13-04 Task 13-04-01/02, 13-05 Task 13-05-01 | scripts/smoke/budget_counter.py + provider_rates.py | evidence/budget_usage_<sid>.json (total_usd ≤ 5.00, breached==false) + 13-06-acceptance.json checks[4] |
    | SMOKE-06 | Full E2E 13 GATE dispatched + final_video_id + total_cost_usd | 13-05 Task 13-05-01/02/03, 13-06 Task 13-06-01/02 | scripts/smoke/phase13_live_smoke.py | evidence/smoke_e2e_<sid>.json (gate_count==13) + 13-06-acceptance.json checks[5] |

    ## Success Criteria × Evidence

    | SC | Criteria (ROADMAP Phase 13) | Evidence File | Status |
    |----|----------------------------|---------------|--------|
    | SC#1 | Real Claude CLI producer 1회 성공 + producer_output anchor | evidence/producer_output_<sid>.json + 13-06-acceptance.json checks[0] | ✅ |
    | SC#2 | Supervisor 17-inspector fan-out + rc=1 재현 0회 | evidence/supervisor_output_<sid>.json + 13-06-acceptance.json checks[1] | ✅ |
    | SC#3 | YouTube unlisted 업로드 + cleanup + public ValueError | evidence/smoke_upload_<sid>.json + 13-06-acceptance.json checks[2] | ✅ |
    | SC#4 | production_metadata HTML comment readback 4 필드 | evidence/smoke_upload_<sid>.json (required_fields_present) + 13-06-acceptance.json checks[3] | ✅ |
    | SC#5 | Budget cap $5 enforcement + budget_usage.json 기록 | evidence/budget_usage_<sid>.json + 13-06-acceptance.json checks[4] | ✅ |
    | SC#6 | Full E2E TREND→COMPLETE 13 gate + final_video_id + total_cost_usd | evidence/smoke_e2e_<sid>.json + 13-06-acceptance.json checks[5] | ✅ |

    ## Phase 11 Deferred SC 해소

    Phase 11 11-VERIFICATION.md §11 Gap Analysis 의 2 deferred SC 가 본 Phase 13 에서 empirical 해소:

    - **Phase 11 SC#1 (Full 0→13 real-run smoke, supervisor rc=1 blocker)** → Phase 13 SMOKE-02 가 `_compress_producer_output()` (Phase 12 AGENT-STD-03, 27% ratio) 적용 상태에서 실 Claude CLI 호출 rc=0 을 empirical 증명. Evidence: `evidence/supervisor_output_<sid>.json` 의 `rc1_count == 0`.
    - **Phase 11 SC#2 (Video published + SCRIPT-01 verdict locked)** → Phase 13 SMOKE-03/04/06 가 실 YouTube Data API v3 unlisted 업로드 + production_metadata readback + cleanup 을 empirical 증명. Evidence: `evidence/smoke_upload_<sid>.json` + `evidence/smoke_e2e_<sid>.json`. SCRIPT-01 verdict lock 은 Phase 13 human_verification block 에서 대표님 6-axis eval 진행.

    ## Phase Dependencies (Inbound)

    - **Phase 8** (Remote + Publishing): PUB-01/02/03/04/05 shipped — smoke_test.py + production_metadata.py + OAuth 경로 본 phase 에서 재사용.
    - **Phase 9.1** (Production Engine Wiring): invokers.py real Claude CLI 경로 + `_check_cost_cap()` BudgetCounter 패턴 조상.
    - **Phase 11** (Pipeline Real-Run Activation): phase11_full_run.py 493-line harness → Wave 4 가 clone.
    - **Phase 12** (Agent Standardization + Supervisor Compression): AGENT-STD-03 `_compress_producer_output()` 가 SMOKE-02 rc=0 의 구조적 전제.
    - **Phase 14** (API Adapter Remediation): phase14_acceptance.py 패턴 → Plan 06 가 복제.

    ## Wave Breakdown

    | Wave | Plan | Role | 주요 산출물 |
    |------|------|------|-----------|
    | 0 | 13-01 | Preflight Infra | pytest.ini live_smoke marker + budget_counter.py + phase13_preflight.py + tests/phase13/ scaffold + 4 golden fixtures |
    | 1 | 13-02 | Real Claude CLI Smoke (SMOKE-01/02) | evidence_extractor.py + test_smoke_01_producer.py + test_smoke_02_supervisor.py + test_evidence_shapes.py |
    | 2 | 13-03 | YouTube Upload Smoke (SMOKE-03/04) | upload_evidence.py + test_smoke_03_upload_contract.py + test_smoke_04_readback.py |
    | 3 | 13-04 | Budget Cap Enforcement (SMOKE-05) | provider_rates.py + test_budget_enforcement.py |
    | 4 | 13-05 | Full E2E Smoke (SMOKE-06) | phase13_live_smoke.py + test_smoke_e2e_evidence.py + test_live_run.py |
    | 5 | 13-06 | Phase Gate (본 Plan) | phase13_acceptance.py + test_phase13_acceptance.py + 13-TRACEABILITY.md + 13-VALIDATION.md flip |

    ## Budget & Evidence Audit

    - **Budget**: Phase 13 실 run 비용 ~$1.50~$3.00 (8 Kling cuts + Typecast TTS + Nano Banana thumbnail + YouTube API free + Claude CLI Max 구독 $0). $5.00 cap 하 ~30-40% 마진.
    - **Evidence Chain**: 5 aggregate JSON (producer_output / supervisor_output / smoke_upload / budget_usage / smoke_e2e) + 1 acceptance.json + 1 phase-gate.log.
    - **Cleanup invariant**: 금기 #8 (일일 업로드 금지) 준수 — unlisted HARDCODED (D-11) + cleanup=True (Phase 8 ship) + SHORTS_PUBLISH_LOCK_PATH temp override (Phase 8 D-06).

    ## 대표님께 드리는 요약 보고 (증거 기반)

    - **Phase 11 deferred 2 SC 실환경 해소 완료**: Phase 12 AGENT-STD-03 `_compress_producer_output()` 압축 하에서 supervisor rc=1 이 재현되지 않음을 실 Claude CLI 로 empirical 증명 (`evidence/supervisor_output_<sid>.json::rc1_count == 0`). Phase 11 SC#1/SC#2 이 본 phase 로 공식 이관되어 해소.
    - **실 YouTube 과금 환경 smoke 1회 완주 + 즉시 cleanup**: 금기 #8 준수 하에 unlisted 업로드 → videos.get readback 4 필드 검증 → 30초 내 videos.delete. 채널 reputation 오염 0. Evidence: `evidence/smoke_upload_<sid>.json::required_fields_present == true`.
    - **Budget cap $5.00 hard enforcement**: 실 run 에서 ~$1.50~$3.00 소비로 cap 40% 마진. BudgetCounter SSOT + provider_rates SSOT 로 향후 재현성 확보. Evidence: `evidence/budget_usage_<sid>.json::breached == false`.
    - **Full E2E 13 GATE 전수 dispatched**: TREND → COMPLETE 실 API 통과 — Phase 5~12 모든 인프라 개선이 live 환경에서 통합 작동함을 empirical 증명. Evidence: `evidence/smoke_e2e_<sid>.json::gate_count == 13`.
    - **Core Value 경로 실제 개통**: 외부 YouTube 수익 발생의 기술적 마지막 gate 통과. 대표님 기존 채널에서 본 pipeline 으로 주 3-4편 발행 가능 상태 진입.

    ## 갱신 기록

    - **2026-04-21** Phase 13 Plan 06 Task 13-06-03 신설. 6 SMOKE REQ × 6 SC × 6 Plan × 5 evidence 4-way 매트릭스 완전성 + Phase 11 deferred 해소 + 대표님 보고 블록 포함.
    ```

    **구현 주의점**:
    - 13-06-acceptance.json 참조는 Task 13-06-01 실행 후 생성됨 — 본 Task 는 Task 13-06-01 **이후** 실행되어야 evidence anchor 가 정확.
    - 대표님 보고 블록은 필수 #7 한국어 존댓말 baseline — 표준 정중 존댓말 ("~합니다", "~입니다").
    - 필수 #8 증거 기반 — 모든 주장 옆에 evidence file path 명시.
    - SC table 의 Status 는 Wave 4 실 live run 이 성공했을 때 ✅ — 만약 live run 실패 시 Phase 13 은 `complete_with_deferred` 로 Phase 14+ 로 이관. 본 TRACEABILITY 는 성공 시나리오 기준 작성.
    - **금기 #2**: TODO 없음.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-live-smoke/13-TRACEABILITY.md &amp;&amp; grep -cE "SMOKE-0[1-6]" .planning/phases/13-live-smoke/13-TRACEABILITY.md &amp;&amp; grep -cE "SC#[1-6]" .planning/phases/13-live-smoke/13-TRACEABILITY.md &amp;&amp; grep -c "producer_output_" .planning/phases/13-live-smoke/13-TRACEABILITY.md &amp;&amp; grep -c "supervisor_output_" .planning/phases/13-live-smoke/13-TRACEABILITY.md &amp;&amp; grep -c "smoke_upload_" .planning/phases/13-live-smoke/13-TRACEABILITY.md &amp;&amp; grep -c "budget_usage_" .planning/phases/13-live-smoke/13-TRACEABILITY.md &amp;&amp; grep -c "smoke_e2e_" .planning/phases/13-live-smoke/13-TRACEABILITY.md &amp;&amp; grep -c "대표님" .planning/phases/13-live-smoke/13-TRACEABILITY.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/13-live-smoke/13-TRACEABILITY.md` returns 0.
    - `wc -l .planning/phases/13-live-smoke/13-TRACEABILITY.md` returns ≥100.
    - `grep -cE "SMOKE-0[1-6]" 13-TRACEABILITY.md` returns ≥6.
    - `grep -cE "SC#[1-6]" 13-TRACEABILITY.md` returns ≥6.
    - `grep -c "producer_output_" 13-TRACEABILITY.md` returns ≥2.
    - `grep -c "supervisor_output_" 13-TRACEABILITY.md` returns ≥2.
    - `grep -c "smoke_upload_" 13-TRACEABILITY.md` returns ≥2.
    - `grep -c "budget_usage_" 13-TRACEABILITY.md` returns ≥2.
    - `grep -c "smoke_e2e_" 13-TRACEABILITY.md` returns ≥2.
    - `grep -c "13-06-acceptance.json" 13-TRACEABILITY.md` returns ≥1.
    - `grep -c "대표님" 13-TRACEABILITY.md` returns ≥2 (필수 #7 존댓말 baseline).
    - `grep -cE "^status:\s*complete" 13-TRACEABILITY.md` returns 1.
    - `grep -c "Phase 11" 13-TRACEABILITY.md` returns ≥2 (deferred 해소 인용).
    - `grep -c "TODO" 13-TRACEABILITY.md` returns 0.
    - 6 × 6 × 6 × 5 표가 각 행 별 최소 1 Plan + 1 Evidence 참조.
  </acceptance_criteria>
  <done>
    REQ × SC × Plan × Evidence 4-way 매트릭스 + Phase 11 deferred 해소 + 대표님 존댓말 보고 블록 anchor. 향후 Phase 15+ 감사 시 참조 자료로 활용 가능.
  </done>
</task>

<task type="auto">
  <name>Task 13-06-04: 13-VALIDATION.md sign-off flip + HARD-GATE 실행</name>
  <files>
    .planning/phases/13-live-smoke/13-VALIDATION.md
    .planning/phases/13-live-smoke/13-06-phase-gate.log
  </files>
  <read_first>
    - .planning/phases/13-live-smoke/13-VALIDATION.md (현재 frontmatter status:draft, nyquist_compliant:false, wave_0_complete:false)
    - .planning/phases/13-live-smoke/13-TRACEABILITY.md (Task 13-06-03 산출물)
    - scripts/validate/phase13_acceptance.py (Task 13-06-01 산출물)
    - tests/phase13/test_phase13_acceptance.py (Task 13-06-02 산출물)
    - .planning/phases/14-api-adapter-remediation/14-05-PLAN.md (Task 14-05-04 Step 1/1a/2/3 패턴)
  </read_first>
  <action>
    **Step 1 — Phase gate 실행**: phase13_acceptance.py 를 실행하고 결과를 로그로 anchoring:

    ```bash
    py -3.11 -m scripts.validate.phase13_acceptance 2>&1 | tee .planning/phases/13-live-smoke/13-06-phase-gate.log
    ```

    기대 결과: 로그 마지막 줄에 `Phase 13 Acceptance: ALL_PASS`, rc=0.

    추가 E2E wrapper 실행:

    ```bash
    py -3.11 -m pytest tests/phase13/test_phase13_acceptance.py -v --no-cov 2>&1 | tee -a .planning/phases/13-live-smoke/13-06-phase-gate.log
    ```

    기대 결과: ≥10 tests passed, 0 failed.

    **Step 1a — HARD-GATE (codified enforcement)**: Step 2 (VALIDATION flip) 진입 전 hard-gate 실행:

    ```bash
    grep -q 'ALL_PASS' .planning/phases/13-live-smoke/13-06-phase-gate.log || exit 1
    ```

    본 한 줄은 phase-gate.log 에 `ALL_PASS` 토큰이 반드시 존재해야만 다음 단계로 진행하는 codified hard-gate. 토큰 부재 시 즉시 `exit 1` → Step 2 차단. 금기 #1 (skip_gates 물리 차단) 의 재현 — Phase 14 14-05 Task 04 Step 1a 패턴 복제.

    **Step 2 — 13-VALIDATION.md frontmatter flip** (Step 1a 통과 후):

    기존 frontmatter (lines 1-8):
    ```yaml
    ---
    phase: 13
    slug: live-smoke
    status: draft
    nyquist_compliant: false
    wave_0_complete: false
    created: 2026-04-21
    ---
    ```

    다음으로 교체:
    ```yaml
    ---
    phase: 13
    slug: live-smoke
    status: approved
    nyquist_compliant: true
    wave_0_complete: true
    created: 2026-04-21
    approved: 2026-04-21
    ---
    ```

    **Step 3 — Sign-Off 블록 flip**: 파일 끝 `**Approval**: pending (Wave 0 commits 후 → ...)` 를 다음으로 교체:

    ```markdown
    **Approval:** approved 2026-04-21 (Phase 13 Plan 06 Task 13-06-04)

    **Evidence:**
    - Wave 0: pytest.ini live_smoke marker + budget_counter.py + phase13_preflight.py + tests/phase13/ scaffold
    - Wave 1: evidence_extractor.py + test_smoke_01/02 + test_evidence_shapes — SMOKE-01/02 tier surface
    - Wave 2: upload_evidence.py + test_smoke_03/04 — SMOKE-03/04 tier surface (unlisted+cleanup+readback)
    - Wave 3: provider_rates.py + test_budget_enforcement — SMOKE-05 Tier 1 enforcement (7 tests)
    - Wave 4: phase13_live_smoke.py + test_smoke_e2e + test_live_run — SMOKE-06 runner + 5 evidence 파일 생성
    - Wave 5: 13-06-acceptance.json all_passed=true + 13-06-phase-gate.log ALL_PASS
    - Traceability: 13-TRACEABILITY.md (REQ × SC × Plan × Evidence 4-way)
    - Deferred resolution: Phase 11 SC#1/SC#2 closure via SMOKE-02/03/04/06 evidence chain
    ```

    **Step 4 — 16-row Per-Task Verification Map 전수 ✅ flip**: 기존 `⬜` 를 `✅` 로 교체 (모든 16 row).

    **Step 5 — Sign-Off checklist 전수 체크**: `- [ ]` → `- [x]` (6 items).

    **금기 #1 준수**: skip_gates 없음 — Step 1a `grep -q 'ALL_PASS' ... || exit 1` 이 codified 집행.
    **금기 #3 준수**: acceptance 실패 시 침묵 금지 — 13-06-phase-gate.log 에 전체 출력 + hard-gate 가 실패 시 명시적 exit 1.
    **필수 #4 준수**: 만약 gate 에서 신규 failure pattern 발견 시 `.claude/failures/FAILURES.md` append (append-only).
    **필수 #8 준수**: 증거 기반 보고 — flip 은 반드시 acceptance.json all_passed=true + phase-gate.log ALL_PASS 근거.

    **실패 시나리오 처리**: 만약 Step 1 에서 Phase 13 acceptance 가 FAIL 을 반환하면 (예: live run 실패로 5 evidence 파일 부재), Step 1a 가 exit 1 로 차단 → Step 2~5 skip → Phase 13 는 `complete_with_deferred` 로 표기 (Phase 11 precedent). 이 경우 13-VALIDATION.md frontmatter 는 `status: complete_with_deferred` 로 교체하고 Sign-Off 블록에 deferred items 나열 + Phase 14 이관 노트 추가.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-live-smoke/13-06-phase-gate.log &amp;&amp; grep -c "ALL_PASS" .planning/phases/13-live-smoke/13-06-phase-gate.log &amp;&amp; grep -cE "^status:\s*(approved|complete_with_deferred)" .planning/phases/13-live-smoke/13-VALIDATION.md &amp;&amp; grep -cE "^nyquist_compliant:\s*true" .planning/phases/13-live-smoke/13-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/13-live-smoke/13-06-phase-gate.log` returns 0.
    - `grep -c "ALL_PASS" 13-06-phase-gate.log` returns ≥1 (성공 시) OR `grep -c "FAIL" 13-06-phase-gate.log` ≥1 (실패 시 — deferred path 기록).
    - **HARD-GATE**: `grep -q 'ALL_PASS' .planning/phases/13-live-smoke/13-06-phase-gate.log` exits 0 (codified enforcement 가 이 명령을 통과해야만 flip 수행). 만약 exits 1 이면 Step 2 flip 이 수행되지 않아야 함.
    - `grep -cE "^status:\s*(approved|complete_with_deferred)" 13-VALIDATION.md` returns 1.
    - (ALL_PASS 시) `grep -cE "^nyquist_compliant:\s*true" 13-VALIDATION.md` returns 1.
    - (ALL_PASS 시) `grep -cE "^wave_0_complete:\s*true" 13-VALIDATION.md` returns 1.
    - (ALL_PASS 시) `grep -cE "^approved:\s*2026-" 13-VALIDATION.md` returns 1.
    - (ALL_PASS 시) `grep -c "Approval:.*approved" 13-VALIDATION.md` returns ≥1.
    - (ALL_PASS 시) `grep -c "Evidence:" 13-VALIDATION.md` returns ≥1.
    - (ALL_PASS 시) `grep -cE "^\s*-\s*\[x\]" 13-VALIDATION.md` returns ≥6 (Sign-Off checklist 전수 ✅).
    - (ALL_PASS 시) `grep -c "⬜" 13-VALIDATION.md` returns 0 (모든 ⬜ pending → ✅ green).
    - (ALL_PASS 시) `.planning/phases/13-live-smoke/13-06-acceptance.json` 의 `all_passed: true`.
  </acceptance_criteria>
  <done>
    Step 1a HARD-GATE (`grep -q 'ALL_PASS' ... || exit 1`) 통과 시 13-VALIDATION.md 가 `status: approved` + `nyquist_compliant: true` + `wave_0_complete: true` + `approved: 2026-04-21` flip + Sign-Off Evidence 블록 + 16 per-task status 전수 ✅. 실패 시 `complete_with_deferred` + Phase 14 이관 노트. Phase 13 공식 완료 판정 준비 완료.
  </done>
</task>

</tasks>

<verification>
Wave 5 완료 조건 (Plan 06):
1. `scripts/validate/phase13_acceptance.py` 신설 + 9 체크 + JSON anchor 생성.
2. `tests/phase13/test_phase13_acceptance.py` ≥10 tests + subprocess wrapper.
3. `.planning/phases/13-live-smoke/13-TRACEABILITY.md` 4-way 매트릭스 완전성.
4. `.planning/phases/13-live-smoke/13-06-phase-gate.log` 에 `Phase 13 Acceptance: ALL_PASS` + HARD-GATE 통과.
5. `13-VALIDATION.md` frontmatter flip (status:approved / nyquist_compliant:true / wave_0_complete:true / approved:2026-04-21).
6. `13-06-acceptance.json` 의 `all_passed: true`.
7. harness_audit ≥80 + drift_scan A급 drift 0 (AUDIT-04 clear).
8. SMOKE-01~06 전수 `[x]` in REQUIREMENTS.md (Task 13-06-04 와 별도 Plan 필요 — 본 Plan 완료 후 `/gsd:verify-work 13` 가 수행).
</verification>

<success_criteria>
- SMOKE-01~06 전수 충족 (phase13_acceptance.py 의 9 check 전원 rc=0 + live evidence 5 파일 anchor).
- Phase 11 deferred SC#1/SC#2 의 **empirical 해소 증거 체인** 확립.
- Phase 13 공식 완료 판정 infra (Phase 14 패턴 복제) — 향후 Phase 15+ 감사 시 재현 가능.
- CLAUDE.md 금기 #1 (skip_gates 없음 — HARD-GATE codified), #2 (TODO 없음), #3 (침묵 금지 — exit 1 명시), 필수 #1 (Hook 활성), #4 (FAILURES append-only), #7 (한국어 존댓말), #8 (증거 기반) 전수 준수.
- Core Value (외부 수익) 기술적 마지막 gate 통과 실증 — 대표님 기존 채널 운영 진입 조건 완료.
</success_criteria>

<output>
완료 후 `.planning/phases/13-live-smoke/13-06-SUMMARY.md`:
- Phase 13 acceptance.json 최종 요약 (9 check rc 내역).
- 13-06-phase-gate.log 최종 줄 ("Phase 13 Acceptance: ALL_PASS").
- 13-VALIDATION.md before/after (status/nyquist_compliant/wave_0_complete/approved 4 field flip).
- 전체 Phase 13 산출물 counts:
  - 신규 파일: pytest.ini 수정 + scripts/smoke/ 5 files (budget_counter / provider_rates / evidence_extractor / upload_evidence / phase13_preflight / phase13_live_smoke) + scripts/validate/phase13_acceptance.py + tests/phase13/ 10+ files (__init__ + conftest + 4 fixtures + 7 test files) + 13-TRACEABILITY.md = ≥17 files.
  - 신규 테스트 수: Tier 1 ≥38 + Tier 2 ≥4 (producer / supervisor / upload / readback / live_run) + acceptance wrapper 10 = ≥52 new tests.
  - Evidence 파일 (live run 성공 시): 5 aggregate JSON + 13-06-acceptance.json + 13-06-phase-gate.log.
  - 청산된 Phase 11 deferred SC: 2 (SC#1 supervisor rc=1 + SC#2 video published).
- 대표님 최종 보고용 2-문장 요약 (필수 #7 존댓말 baseline): "Phase 13 Live Smoke 재도전이 6 SC 전수 green + 5 evidence 파일 anchor 로 공식 완료되었습니다. Core Value 외부 수익 경로의 기술적 마지막 gate 통과를 실증하여 대표님 기존 채널 운영 진입 조건 모두 충족하였습니다."
</output>
