---
phase: 13-live-smoke
plan: 03
plan_id: 13-03
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - tests/phase13/test_smoke_03_upload_contract.py
  - tests/phase13/test_smoke_04_readback.py
  - scripts/smoke/upload_evidence.py
autonomous: true
requirements:
  - SMOKE-03
  - SMOKE-04
commit_discipline: per-task
must_haves:
  truths:
    - "Tier 1: `pytest tests/phase13/test_smoke_03_upload_contract.py -m \"not live_smoke\"` 가 `scripts/publisher/smoke_test.py::run_smoke_test(privacy='public')` 호출 시 `ValueError` raise 를 검증 (실 API 호출 0회, PUB-04 invariant 유지 — 금기 #5 Selenium 금지 우회 불가 방어)"
    - "Tier 1: `pytest tests/phase13/test_smoke_04_readback.py -m \"not live_smoke\"` 가 golden fixture 의 `<!-- production_metadata\\n{...}\\n-->` HTML comment regex readback (re.DOTALL) 을 수행하여 4 필드 (script_seed/assets_origin/pipeline_version/checksum) 추출 성공 + 4 필드 모두 non-empty"
    - "Tier 2: `pytest tests/phase13/test_smoke_03_upload_contract.py -m live_smoke --run-live` 가 실 YouTube Data API v3 (PUB-02 공식 경로, 금기 #5 Selenium 금지) 로 `videos.insert` 호출 → video_id 수신 → 30초 processing polling → `videos.delete` cleanup 모두 수행 + evidence/smoke_upload_*.json write (SMOKE-03)"
    - "Tier 2: `pytest tests/phase13/test_smoke_04_readback.py -m live_smoke --run-live` 가 Wave 2 Tier 2 업로드 직후 `videos.list(part='snippet', id=video_id).execute()` 로 description readback + 4 필드 readback 검증 (SMOKE-04)"
    - "`scripts/smoke/upload_evidence.py::anchor_upload_evidence(youtube, video_id, sid, evidence_dir)` 유틸이 Wave 4 Full E2E 에서도 재사용되도록 모듈화 — videos.list readback + regex parse + evidence JSON 5 key (session_id / video_id / description_raw / production_metadata / required_fields_present)"
  artifacts:
    - path: "scripts/smoke/upload_evidence.py"
      provides: "YouTube videos.list readback + production_metadata regex parse + evidence JSON anchor"
      contains: "def anchor_upload_evidence"
      min_lines: 80
    - path: "tests/phase13/test_smoke_03_upload_contract.py"
      provides: "SMOKE-03 Tier 1 ValueError 방어 + Tier 2 실 업로드+cleanup"
      contains: "pytest.mark.live_smoke"
      min_lines: 90
    - path: "tests/phase13/test_smoke_04_readback.py"
      provides: "SMOKE-04 Tier 1 regex + Tier 2 실 readback"
      contains: "re.DOTALL"
      min_lines: 90
  key_links:
    - from: "scripts/smoke/upload_evidence.py::anchor_upload_evidence"
      to: "googleapiclient.discovery.build('youtube', 'v3').videos().list"
      via: "PUB-02 공식 API 경로 (금기 #5 Selenium 금지 — D-A4 보존)"
      pattern: "videos\\(\\)\\.list"
    - from: "tests/phase13/test_smoke_03_upload_contract.py Tier 1"
      to: "scripts/publisher/smoke_test.py::run_smoke_test"
      via: "privacy='public' 호출 시 ValueError raise (choices-locked D-11)"
      pattern: "privacy.*public.*ValueError|pytest.raises.ValueError"
    - from: "tests/phase13/test_smoke_04_readback.py Tier 1"
      to: "tests/phase13/fixtures/sample_smoke_upload.json"
      via: "fixtures_dir + re.search(r'<!-- production_metadata\\\\n(\\\\{.*?\\\\})\\\\n-->', description_raw, re.DOTALL)"
      pattern: "production_metadata"
    - from: "tests/phase13/test_smoke_03_upload_contract.py Tier 2"
      to: "scripts/smoke/upload_evidence.py + scripts/publisher/smoke_test.py"
      via: "run_smoke_test(privacy='unlisted', cleanup=True) + post-upload anchor_upload_evidence"
      pattern: "run_smoke_test.*unlisted.*cleanup=True"
---

<objective>
Wave 2 YouTube Upload Smoke — SMOKE-03 (unlisted 업로드 + cleanup + public ValueError) + SMOKE-04 (production_metadata HTML comment readback 4 필드) 실증. Phase 8 에서 ship 된 `scripts/publisher/smoke_test.py::run_smoke_test()` + `scripts/publisher/production_metadata.py` 를 경유하여 실 YouTube Data API v3 (금기 #5 Selenium 금지, PUB-02 공식 경로) 로 단일 업로드 후 즉시 cleanup 및 metadata readback 수행.

Purpose: Phase 11 deferred SC#2 (YouTube 과금 환경 smoke 업로드) 의 실 API 경유 증거 확보. 업로드 1회 + cleanup ≤ 30초 = 금기 #8 (일일 업로드 금지) 준수 — unlisted HARDCODED (D-11) + 즉시 삭제로 채널 reputation 오염 0.

Output: 2 test 파일 (SMOKE-03 / SMOKE-04) + 1 공용 evidence 유틸. Tier 1 always-run ≥4 tests. Tier 2 live opt-in ≥2 tests.

**Sequential within wave**: Task 13-03-01 (upload_evidence.py) → 13-03-02 (test_smoke_03) → 13-03-03 (test_smoke_04). 유틸이 선행되어야 Tier 2 test 에서 호출 가능.

**Parallel-capable with Wave 1 (Plan 02)**: 다른 evidence 파일 셋 (smoke_upload vs producer/supervisor_output) + 다른 test 파일 — disjoint files_modified. 단 depends_on 은 Wave 0 (Plan 01) 이므로 wave=2 로 분리하여 logical 순서 유지.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-live-smoke/13-RESEARCH.md
@.planning/phases/13-live-smoke/13-VALIDATION.md
@CLAUDE.md

# Wave 0 dependency
@tests/phase13/conftest.py
@tests/phase13/fixtures/sample_smoke_upload.json
@scripts/smoke/budget_counter.py

# Phase 8 ship — YouTube smoke upload SSOT
@scripts/publisher/smoke_test.py
@scripts/publisher/production_metadata.py
@scripts/publisher/youtube_uploader.py
@scripts/publisher/publish_lock.py

# Phase 8 mocks (for Tier 1 offline verification)
@tests/phase08/mocks/youtube_mock.py
</context>

<interfaces>
<!-- From Phase 8 ship (consume only) -->

scripts/publisher/smoke_test.py:
```python
def run_smoke_test(
    privacy: str = "unlisted",
    cleanup: bool = True,
    *,
    credentials_dir: Path | None = None,
    smoke_mp4_path: Path | None = None,
) -> dict:
    """Raises ValueError if privacy != 'unlisted' (D-11 HARDCODED).
       Returns {'video_id': str, 'status': 'uploaded'|'cleaned', 'privacy': 'unlisted'}.
    """
```

scripts/publisher/production_metadata.py:
```python
PIPELINE_VERSION = "1.0.0"
def compute_checksum(path: Path) -> str: ...  # "sha256:<64hex>"
def inject_html_comment(description: str, metadata: dict) -> str: ...
# HTML comment format: "<!-- production_metadata\n{...}\n-->"
# Regex readback: r'<!-- production_metadata\n(\{.*?\})\n-->' with re.DOTALL
```

scripts/publisher/publish_lock.py:
```python
# SHORTS_PUBLISH_LOCK_PATH env override (Phase 8 D-06) — set by tmp_evidence_dir fixture
```

<!-- New contracts defined here -->

scripts/smoke/upload_evidence.py:
```python
def anchor_upload_evidence(
    youtube,  # googleapiclient.discovery.build('youtube', 'v3') resource
    video_id: str,
    session_id: str,
    evidence_dir: Path = Path(".planning/phases/13-live-smoke/evidence"),
) -> dict:
    """1. videos.list(part='snippet', id=video_id).execute()
       2. description readback + regex parse HTML comment
       3. 4 필드 (script_seed, assets_origin, pipeline_version, checksum) assertion
       4. evidence/smoke_upload_<sid>.json write with 5 key
       5. Return evidence dict (for caller to assert + budget account).
       Raises RuntimeError if videos.list returns empty OR readback missing fields.
    """
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 13-03-01: scripts/smoke/upload_evidence.py — videos.list readback + regex parse + evidence anchor</name>
  <files>scripts/smoke/upload_evidence.py</files>
  <read_first>
    - scripts/publisher/smoke_test.py (전체 280 lines — run_smoke_test + _wait_for_processing + _delete_video flow 파악)
    - scripts/publisher/production_metadata.py (151 lines — HTML comment format + compute_checksum)
    - scripts/publisher/youtube_uploader.py (L207~321 publish 체인 전체)
    - .planning/phases/13-live-smoke/13-RESEARCH.md §SMOKE-04 Code Example (L549-580)
    - CLAUDE.md 금기 #5 (Selenium 금지 — YouTube Data API v3 공식만)
  </read_first>
  <action>
    신규 파일 `scripts/smoke/upload_evidence.py` (≥80 lines, ~110 expected). 다음 공개 함수 + helper:

    1. Module docstring — "Phase 13 Upload Evidence — SMOKE-04 videos.list readback + production_metadata regex + evidence anchor. Wave 2/4 공용. 금기 #5 Selenium 금지 — googleapiclient 공식 경로만."
    2. `PRODUCTION_METADATA_REGEX = re.compile(r'<!-- production_metadata\n(\{.*?\})\n-->', re.DOTALL)` — module constant.
    3. `REQUIRED_METADATA_FIELDS = ("script_seed", "assets_origin", "pipeline_version", "checksum")` — module constant (tuple 4).
    4. `def _parse_production_metadata(description_raw: str) -> dict`:
       - `match = PRODUCTION_METADATA_REGEX.search(description_raw)`.
       - `if not match: return {}` (harmless — caller 가 required_fields_present 로 판정).
       - `return json.loads(match.group(1))` — `JSONDecodeError` 시 `logger.warning` + `{}` 반환 (금기 #3 — 명시적 logger).
    5. `def anchor_upload_evidence(youtube, video_id: str, session_id: str, evidence_dir: Path = Path(".planning/phases/13-live-smoke/evidence")) -> dict`:
       - `resp = youtube.videos().list(part="snippet,status", id=video_id).execute()`.
       - `items = resp.get("items") or []`.
       - `if not items: raise RuntimeError(f"videos.list returned empty for {video_id} 대표님")` — 금기 #3 명시적 raise.
       - `desc_raw = items[0].get("snippet", {}).get("description", "")`.
       - `metadata = _parse_production_metadata(desc_raw)`.
       - `missing = [f for f in REQUIRED_METADATA_FIELDS if not metadata.get(f)]`.
       - `required_fields_present = len(missing) == 0`.
       - `evidence = {"session_id": session_id, "video_id": video_id, "description_raw": desc_raw, "production_metadata": metadata, "required_fields_present": required_fields_present, "missing_fields": missing}`.
       - `evidence_dir.mkdir(parents=True, exist_ok=True)`.
       - `evidence_path = evidence_dir / f"smoke_upload_{session_id}.json"`.
       - `evidence_path.write_text(json.dumps(evidence, ensure_ascii=False, indent=2), encoding="utf-8")`.
       - `logger.info("[upload_evidence] smoke_upload → %s (missing=%s)", evidence_path, missing)`.
       - `return evidence` (caller 가 `required_fields_present` 에 기반해 assertion 수행).

    **구현 주의점**:
    - `re.DOTALL` flag 필수 — description_raw 의 `\n` 경유 HTML comment 매칭 (production_metadata.py 주입 포맷과 정확 일치).
    - `REQUIRED_METADATA_FIELDS` tuple 순서는 production_metadata.py 의 TypedDict field 순서와 일치 (script_seed → assets_origin → pipeline_version → checksum — D-08 계약).
    - 모든 logger.info/warning 메시지는 한국어 보조 설명 가능 (필수 #7 baseline — "대표님" literal 1+ 위치).
    - 본 함수는 **caller 가 evidence["required_fields_present"] 를 assertion 해야 함** — 함수 내부에서 RuntimeError 시 caller 가 알아챌 수 있도록.
    - **금기 #2**: TODO 없음. **금기 #5**: googleapiclient.discovery.build 경로만 — Selenium import 금지.
  </action>
  <verify>
    <automated>test -f scripts/smoke/upload_evidence.py &amp;&amp; py -3.11 -c "from scripts.smoke.upload_evidence import anchor_upload_evidence, PRODUCTION_METADATA_REGEX, REQUIRED_METADATA_FIELDS; assert len(REQUIRED_METADATA_FIELDS) == 4; import re; assert PRODUCTION_METADATA_REGEX.flags &amp; re.DOTALL; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/smoke/upload_evidence.py` returns 0.
    - `wc -l scripts/smoke/upload_evidence.py` returns ≥80.
    - `grep -c "def anchor_upload_evidence" scripts/smoke/upload_evidence.py` returns 1.
    - `grep -c "PRODUCTION_METADATA_REGEX" scripts/smoke/upload_evidence.py` returns ≥2.
    - `grep -c "REQUIRED_METADATA_FIELDS" scripts/smoke/upload_evidence.py` returns ≥2.
    - `grep -c "re.DOTALL" scripts/smoke/upload_evidence.py` returns ≥1.
    - `grep -c "videos().list" scripts/smoke/upload_evidence.py` returns ≥1.
    - `grep -c "ensure_ascii=False" scripts/smoke/upload_evidence.py` returns ≥1.
    - `grep -ciE "selenium|webdriver" scripts/smoke/upload_evidence.py` returns 0 (금기 #5).
    - `grep -c "대표님" scripts/smoke/upload_evidence.py` returns ≥1 (필수 #7).
    - `grep -c "TODO" scripts/smoke/upload_evidence.py` returns 0.
    - `py -3.11 -c "from scripts.smoke.upload_evidence import anchor_upload_evidence, REQUIRED_METADATA_FIELDS; assert len(REQUIRED_METADATA_FIELDS) == 4"` exit 0.
  </acceptance_criteria>
  <done>
    Wave 2 Tier 2 + Wave 4 Full E2E 가 공통 import 할 `anchor_upload_evidence` + regex + required fields constant 가 확립. 금기 #5 Selenium 금지 + PUB-04 production_metadata 4 필드 invariant 준수.
  </done>
</task>

<task type="auto">
  <name>Task 13-03-02: tests/phase13/test_smoke_03_upload_contract.py — SMOKE-03 public ValueError + Tier 2 실 업로드</name>
  <files>tests/phase13/test_smoke_03_upload_contract.py</files>
  <read_first>
    - scripts/publisher/smoke_test.py (run_smoke_test signature + privacy HARDCODED 'unlisted')
    - tests/phase08/mocks/youtube_mock.py (MockYouTube 패턴 — Phase 8 Plan 06, 15/15 tests green)
    - scripts/smoke/upload_evidence.py (Task 13-03-01 산출물)
    - .planning/phases/13-live-smoke/13-RESEARCH.md §SMOKE-03 Technical Approach (L247-260) + §Pitfall 5 (processing timeout)
    - .planning/phases/13-live-smoke/13-VALIDATION.md row 13-03-01
  </read_first>
  <action>
    신규 파일 `tests/phase13/test_smoke_03_upload_contract.py` (≥90 lines, ~130 expected). 다음 4+ 테스트:

    **Tier 1 (3 테스트)**:
    1. `test_smoke_03_privacy_public_raises_value_error()`:
       - `from scripts.publisher.smoke_test import run_smoke_test`.
       - `with pytest.raises(ValueError) as exc_info: run_smoke_test(privacy='public', cleanup=True)`.
       - `assert "unlisted" in str(exc_info.value).lower()`.
       - **실 API 호출 0회** — smoke_test.py 가 argument 검증 단계에서 raise.
    2. `test_smoke_03_privacy_private_raises_value_error()` — `privacy='private'` 도 동일 ValueError (D-11 choices-locked 검증).
    3. `test_smoke_03_default_privacy_is_unlisted_hardcoded()`:
       - `inspect.signature(run_smoke_test)` 로 default value 가 `"unlisted"` 임을 assert — regression guard.

    **Tier 2 (1+ 테스트, @pytest.mark.live_smoke)**:
    4. `test_smoke_03_real_youtube_unlisted_upload_and_cleanup(tmp_evidence_dir)`:
       - `from scripts.publisher.smoke_test import run_smoke_test`.
       - `from scripts.smoke.upload_evidence import anchor_upload_evidence`.
       - `from googleapiclient.discovery import build` + `scripts.publisher.oauth.get_credentials()` (Phase 8 ship).
       - `result = run_smoke_test(privacy='unlisted', cleanup=True)`.
       - `assert result["video_id"]` — non-empty.
       - `assert result["privacy"] == "unlisted"`.
       - `assert result["status"] in ("cleaned", "uploaded")` (Pitfall 5 — timeout 시 "uploaded" 잔존).
       - **중요 순서**: `run_smoke_test` 가 cleanup=True 로 videos.delete 를 **호출 후** 본 test 가 `anchor_upload_evidence` 를 호출하면 videos.list 가 empty → RuntimeError 발생. 따라서 Tier 2 에서는 `run_smoke_test` 대신 직접 videos.insert → anchor → videos.delete 순서로 재구성:
         - `credentials = get_credentials()` → `youtube = build('youtube', 'v3', credentials=credentials)`.
         - `from scripts.publisher.smoke_test import _build_smoke_plan, _wait_for_processing, _delete_video` (Phase 8 내부 함수 재사용).
         - `plan = _build_smoke_plan()`.
         - `insert_resp = youtube.videos().insert(part='snippet,status', body=plan['body'], media_body=MediaFileUpload(plan['mp4_path'])).execute()`.
         - `video_id = insert_resp['id']`.
         - `_wait_for_processing(youtube, video_id, budget_seconds=30)`.
         - `sid = datetime.now().strftime("%Y%m%d_%H%M%S")`.
         - `evidence = anchor_upload_evidence(youtube, video_id, sid, evidence_dir=tmp_evidence_dir)`.
         - `assert evidence["video_id"] == video_id`.
         - `try: _delete_video(youtube, video_id); finally: pass` — cleanup 보장 (쓰는 try/finally 는 금기 #3 침묵 아님, 리소스 해제 패턴).
       - 단, 이 test 는 SMOKE-04 의 4-필드 검증까지는 **수행하지 않음** (task 13-03-03 scope). 본 테스트는 upload + cleanup round-trip 만 검증.

    **구현 주의점**:
    - Tier 1 은 **실 API 호출 0회** — smoke_test.py 의 argument validation 만 triggerring.
    - Tier 2 의 `try/finally` 로 감싼 `_delete_video` 는 silent pass 아니라 리소스 해제 — pytest framework 가 test 실패/성공 관계없이 cleanup 보장. 단 `finally: pass` 가 아닌 `finally: _delete_video(...)` 구조. `finally: pass` 문구가 grep 에 잡히지 않도록 주의.
    - Pitfall 5 — `_wait_for_processing` 30s timeout 은 Phase 8 ship 계약 그대로 (금기 #2 TODO 금지 — Phase 13 에서 timeout 연장 금지).
    - `tmp_evidence_dir` fixture 사용 — 실제 evidence 경로 오염 방지.
    - **금기 #5 준수**: `grep -ciE "selenium|webdriver"` 결과 0.
    - **금기 #8 준수**: `run_smoke_test` 기본 cleanup=True — video 채널에 미업로드 상태로 30초 내 삭제.
  </action>
  <verify>
    <automated>test -f tests/phase13/test_smoke_03_upload_contract.py &amp;&amp; grep -c "pytest.raises(ValueError)" tests/phase13/test_smoke_03_upload_contract.py &amp;&amp; grep -c "pytest.mark.live_smoke" tests/phase13/test_smoke_03_upload_contract.py &amp;&amp; grep -ciE "selenium|webdriver" tests/phase13/test_smoke_03_upload_contract.py &amp;&amp; py -3.11 -m pytest tests/phase13/test_smoke_03_upload_contract.py -m "not live_smoke" --no-cov -v</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/phase13/test_smoke_03_upload_contract.py` returns 0.
    - `wc -l tests/phase13/test_smoke_03_upload_contract.py` returns ≥90.
    - `grep -cE "^def test_smoke_03_" tests/phase13/test_smoke_03_upload_contract.py` returns ≥4.
    - `grep -c "pytest.raises(ValueError)" tests/phase13/test_smoke_03_upload_contract.py` returns ≥2.
    - `grep -c "pytest.mark.live_smoke" tests/phase13/test_smoke_03_upload_contract.py` returns ≥1.
    - `grep -c "run_smoke_test" tests/phase13/test_smoke_03_upload_contract.py` returns ≥2.
    - `grep -c "anchor_upload_evidence" tests/phase13/test_smoke_03_upload_contract.py` returns ≥1.
    - `grep -c "unlisted" tests/phase13/test_smoke_03_upload_contract.py` returns ≥2.
    - `grep -ciE "selenium|webdriver" tests/phase13/test_smoke_03_upload_contract.py` returns 0 (금기 #5).
    - `grep -c "TODO" tests/phase13/test_smoke_03_upload_contract.py` returns 0.
    - `py -3.11 -m pytest tests/phase13/test_smoke_03_upload_contract.py -m "not live_smoke" --no-cov -v` exit 0 with ≥3 Tier 1 tests PASSED.
    - Tier 2 collect: `py -3.11 -m pytest tests/phase13/test_smoke_03_upload_contract.py -m live_smoke --run-live --collect-only -q` returns ≥1 collected.
  </acceptance_criteria>
  <done>
    SMOKE-03 의 ValueError 방어 (Tier 1) + 실 업로드+cleanup (Tier 2) 검증 surface 가 확립. 금기 #5 Selenium 0 grep + 금기 #8 일일 업로드 금지 — cleanup=True 로 채널 오염 0.
  </done>
</task>

<task type="auto">
  <name>Task 13-03-03: tests/phase13/test_smoke_04_readback.py — SMOKE-04 regex + 4 필드 readback</name>
  <files>tests/phase13/test_smoke_04_readback.py</files>
  <read_first>
    - scripts/smoke/upload_evidence.py (Task 13-03-01 산출물 — PRODUCTION_METADATA_REGEX + REQUIRED_METADATA_FIELDS + anchor_upload_evidence)
    - scripts/publisher/production_metadata.py (HTML comment 주입 포맷)
    - tests/phase13/fixtures/sample_smoke_upload.json (golden — 4 필드 모두 present)
    - .planning/phases/13-live-smoke/13-RESEARCH.md §SMOKE-04 Technical Approach (L262-283)
    - .planning/phases/13-live-smoke/13-VALIDATION.md row 13-03-02
  </read_first>
  <action>
    신규 파일 `tests/phase13/test_smoke_04_readback.py` (≥90 lines, ~120 expected). 다음 5+ 테스트:

    **Tier 1 (4 테스트)**:
    1. `test_smoke_04_regex_matches_valid_html_comment()`:
       - `from scripts.smoke.upload_evidence import PRODUCTION_METADATA_REGEX`.
       - Golden description string: `"샘플\n\n<!-- production_metadata\n{\"script_seed\": \"s1\", \"assets_origin\": \"smoke:test\", \"pipeline_version\": \"1.0.0\", \"checksum\": \"sha256:abc\"}\n-->"`.
       - `match = PRODUCTION_METADATA_REGEX.search(desc)`.
       - `assert match is not None`.
       - `json.loads(match.group(1))` 4 필드 모두 presence assert.
    2. `test_smoke_04_regex_requires_dotall_flag()`:
       - 멀티라인 JSON (`\n` 경유) 가 regex 에 포착됨을 명시적으로 검증 — flags 에 `re.DOTALL` 보유 assert.
    3. `test_smoke_04_regex_fails_on_missing_comment()`:
       - plain description 에 HTML comment 없으면 `PRODUCTION_METADATA_REGEX.search(...) is None`.
    4. `test_smoke_04_fixture_readback_validates_all_four_fields(fixtures_dir)`:
       - `payload = json.loads((fixtures_dir / "sample_smoke_upload.json").read_text(encoding="utf-8"))`.
       - `payload["required_fields_present"] is True`.
       - `payload["missing_fields"] == []`.
       - `for field in ("script_seed", "assets_origin", "pipeline_version", "checksum"): assert payload["production_metadata"].get(field)`.
       - `payload["production_metadata"]["pipeline_version"] == "1.0.0"` (D-08 PIPELINE_VERSION constant).

    **Tier 2 (1 테스트, @pytest.mark.live_smoke)**:
    5. `test_smoke_04_real_videos_list_readback_after_upload(tmp_evidence_dir)`:
       - 본 테스트는 `test_smoke_03_real_youtube_unlisted_upload_and_cleanup` 와 **실 API 호출을 공유하지 않음** — 독립 Tier 2 test 로, 자체 upload + readback + cleanup 사이클을 수행.
       - Tier 2 setup:
         - `from googleapiclient.discovery import build` + credentials, 동일 패턴.
         - `from scripts.publisher.smoke_test import _build_smoke_plan, _wait_for_processing, _delete_video`.
         - videos.insert → `_wait_for_processing` → `anchor_upload_evidence(...)` → assertions → `_delete_video`.
         - try/finally 로 cleanup 보장.
       - 핵심 assertion:
         - `evidence["required_fields_present"] is True`.
         - `evidence["missing_fields"] == []`.
         - 4 필드 모두 non-empty.
         - `evidence["production_metadata"]["pipeline_version"] == "1.0.0"`.
         - `evidence_path = tmp_evidence_dir / f"smoke_upload_{sid}.json"` 존재 + `json.loads` 결과가 evidence 와 일치.

    **구현 주의점**:
    - Tier 1 4 테스트는 **실 API 호출 0회** — regex + fixture 만.
    - Tier 2 는 Tier 2 test 13-03-02 와 **독립 session_id 생성** — 과금은 2배 (2 uploads)이지만 격리성 우선. 실제 `phase13_live_smoke.py` (Wave 4) 는 1-shot 으로 두 개 모두 충족.
    - `pipeline_version == "1.0.0"` assertion 은 Phase 8 D-08 invariant — 변경 시 production_metadata.py 의 `PIPELINE_VERSION` constant bump 필요 (regression guard).
    - **금기 #2**: TODO 없음. **금기 #5**: selenium/webdriver grep 0.
  </action>
  <verify>
    <automated>test -f tests/phase13/test_smoke_04_readback.py &amp;&amp; grep -c "re.DOTALL" tests/phase13/test_smoke_04_readback.py &amp;&amp; grep -c "script_seed" tests/phase13/test_smoke_04_readback.py &amp;&amp; grep -c "assets_origin" tests/phase13/test_smoke_04_readback.py &amp;&amp; grep -c "pipeline_version" tests/phase13/test_smoke_04_readback.py &amp;&amp; grep -c "checksum" tests/phase13/test_smoke_04_readback.py &amp;&amp; py -3.11 -m pytest tests/phase13/test_smoke_04_readback.py -m "not live_smoke" --no-cov -v</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/phase13/test_smoke_04_readback.py` returns 0.
    - `wc -l tests/phase13/test_smoke_04_readback.py` returns ≥90.
    - `grep -cE "^def test_smoke_04_" tests/phase13/test_smoke_04_readback.py` returns ≥5.
    - `grep -c "pytest.mark.live_smoke" tests/phase13/test_smoke_04_readback.py` returns ≥1.
    - `grep -c "re.DOTALL" tests/phase13/test_smoke_04_readback.py` returns ≥1.
    - `grep -c "PRODUCTION_METADATA_REGEX" tests/phase13/test_smoke_04_readback.py` returns ≥2.
    - `grep -c "anchor_upload_evidence" tests/phase13/test_smoke_04_readback.py` returns ≥1.
    - `grep -c "script_seed" tests/phase13/test_smoke_04_readback.py` returns ≥2.
    - `grep -c "assets_origin" tests/phase13/test_smoke_04_readback.py` returns ≥2.
    - `grep -c "pipeline_version" tests/phase13/test_smoke_04_readback.py` returns ≥2.
    - `grep -c "checksum" tests/phase13/test_smoke_04_readback.py` returns ≥2.
    - `grep -c "required_fields_present" tests/phase13/test_smoke_04_readback.py` returns ≥2.
    - `grep -c "TODO" tests/phase13/test_smoke_04_readback.py` returns 0.
    - `grep -ciE "selenium|webdriver" tests/phase13/test_smoke_04_readback.py` returns 0.
    - `py -3.11 -m pytest tests/phase13/test_smoke_04_readback.py -m "not live_smoke" --no-cov -v` exit 0 with ≥4 Tier 1 tests PASSED.
  </acceptance_criteria>
  <done>
    SMOKE-04 regex readback 계약 (Tier 1) + 실 videos.list readback 4 필드 검증 (Tier 2) 이 확립되어 PUB-04 production_metadata invariant 가 Phase 13 에서 실증 가능.
  </done>
</task>

</tasks>

<verification>
Wave 2 완료 조건 (Plan 03):
1. `scripts/smoke/upload_evidence.py` 신설 + `anchor_upload_evidence` + `PRODUCTION_METADATA_REGEX` + `REQUIRED_METADATA_FIELDS` export.
2. `tests/phase13/test_smoke_03_upload_contract.py` ≥4 tests (3 Tier 1 + 1 Tier 2).
3. `tests/phase13/test_smoke_04_readback.py` ≥5 tests (4 Tier 1 + 1 Tier 2).
4. `pytest tests/phase13/ -m "not live_smoke" --no-cov -q` exit 0 with Wave 0+1+2 합산 ≥24 GREEN (Wave 0=6 + Wave 1=11 + Wave 2=7 min).
5. 금기 #5 regression — `grep -rciE "selenium|webdriver" scripts/smoke/ tests/phase13/` returns 0.
</verification>

<success_criteria>
- SMOKE-03 (unlisted+cleanup+public ValueError) + SMOKE-04 (4필드 readback) test surface 확립.
- Phase 11 deferred SC#2 실환경 해소 경로 준비 — Wave 4 Full E2E 가 tier 2 를 run 하여 evidence 실물 확보.
- CLAUDE.md 금기 #2/#3/#5/#8 전수 준수 + 필수 #7/#8 준수.
</success_criteria>

<output>
완료 후 `.planning/phases/13-live-smoke/13-03-SUMMARY.md`:
- 3 Task 산출 + verify rc.
- Tier 1 누계 tests (예상 Wave 2 기여 ≥7).
- 금기 #5 grep audit (selenium/webdriver = 0 hit).
- 대표님 보고 2-문장 존댓말 요약.
</output>
