---
phase: 13-live-smoke
plan: 01
plan_id: 13-01
type: execute
wave: 0
depends_on: []
files_modified:
  - pytest.ini
  - scripts/smoke/budget_counter.py
  - scripts/smoke/phase13_preflight.py
  - tests/phase13/__init__.py
  - tests/phase13/conftest.py
  - tests/phase13/test_budget_counter.py
  - tests/phase13/fixtures/sample_producer_output.json
  - tests/phase13/fixtures/sample_supervisor_output.json
  - tests/phase13/fixtures/sample_smoke_upload.json
  - tests/phase13/fixtures/sample_smoke_e2e.json
autonomous: true
requirements:
  - SMOKE-01
  - SMOKE-02
  - SMOKE-03
  - SMOKE-04
  - SMOKE-05
  - SMOKE-06
commit_discipline: per-task
must_haves:
  truths:
    - "`pytest -m live_smoke --collect-only` 가 marker 를 인식하여 단일 명령으로 Tier 2 격리 가능 (SMOKE-01~06 gating 전제)"
    - "`pytest tests/phase13 -m \"not live_smoke\" -q` 가 Wave 0 직후 Tier 1 always-run baseline 으로 ≥5 tests green 완료 (SMOKE-05 always-run 기반 확보)"
    - "`scripts/smoke/budget_counter.py` 모듈이 import 가능하고 `BudgetCounter` + `BudgetExceededError` 를 export — Wave 1~4 모두 이 single SSOT 사용 (SMOKE-05 core)"
    - "`scripts/smoke/phase13_preflight.py --preflight` 실행이 Claude CLI reachable + YouTube OAuth refresh + required .env keys + evidence dir write-access 4개 체크 후 rc=0/1 리턴 (SMOKE-01/02/03 진입 차단 게이트)"
    - "tests/phase13/conftest.py 가 `--run-live` CLI option + `repo_root` + `tmp_evidence_dir` + `_fake_claude_cli` 4 fixture 제공 — Phase 14 conftest.py 패턴 재사용"
    - "tests/phase13/fixtures/ 에 4 golden JSON (producer_output / supervisor_output / smoke_upload / smoke_e2e) 존재하여 Tier 1 evidence-shape 테스트가 live run 없이 구조 검증 가능"
  artifacts:
    - path: "pytest.ini"
      provides: "live_smoke marker registration + conftest.py --run-live hook 지원"
      contains: "live_smoke"
    - path: "scripts/smoke/budget_counter.py"
      provides: "BudgetCounter class (charge + persist + BudgetExceededError) — SMOKE-05 SSOT"
      contains: "class BudgetCounter"
      min_lines: 60
    - path: "scripts/smoke/phase13_preflight.py"
      provides: "Preflight CLI — Claude CLI probe + YouTube token refresh + env keys + evidence dir"
      contains: "def main"
      min_lines: 80
    - path: "tests/phase13/conftest.py"
      provides: "Phase 13 공용 fixtures + --run-live gate"
      contains: "addoption"
    - path: "tests/phase13/test_budget_counter.py"
      provides: "SMOKE-05 Tier 1 evidence-shape tests (mock only, always-run)"
      contains: "BudgetExceededError"
      min_lines: 80
  key_links:
    - from: "pytest.ini markers"
      to: "tests/phase13/conftest.py"
      via: "pytest collection — `--strict-markers` + addoption(\"--run-live\")"
      pattern: "live_smoke"
    - from: "scripts/smoke/budget_counter.py::BudgetCounter.charge"
      to: "RuntimeError (BudgetExceededError) 상향 전파"
      via: "projected > cap_usd 시 명시적 raise — 금기 #3 준수"
      pattern: "raise BudgetExceededError"
    - from: "scripts/smoke/phase13_preflight.py"
      to: "claude --print + googleapiclient.discovery.build YouTube + os.environ key probe"
      via: "subprocess + google.auth refresh + os.getenv"
      pattern: "subprocess.run.*claude"
    - from: "tests/phase13/conftest.py::--run-live"
      to: "tests/phase13/test_*.py 의 `@pytest.mark.live_smoke`"
      via: "pytest_collection_modifyitems — skip unless --run-live"
      pattern: "pytest.mark.live_smoke"
---

<objective>
Wave 0 Preflight Infrastructure — Phase 13 전체가 의존하는 인프라 5종 (pytest.ini live_smoke marker, budget_counter.py, phase13_preflight.py, tests/phase13/ scaffold, fixtures) 을 일괄 구축한다. 본 Plan 완료 없이는 Wave 1~5 가 실행 불가 (모든 후속 plan 이 budget_counter import + conftest fixture + live_smoke marker + evidence dir 을 의존).

Purpose: 13-RESEARCH.md §Wave 0 Preflight 가 명시한 "billable call 없이" 인프라만 구축하는 단계. 실 API 호출 0회, Tier 1 always-run pytest ≥5 tests green 이 본 Plan 의 finish line. 13-VALIDATION.md 16-row verification map 중 13-01-01~04 (row 1~4) 가 본 Plan 의 직접 책임.

Output: pytest.ini 확장 + budget_counter.py (신규 모듈) + phase13_preflight.py (신규 CLI) + tests/phase13/ 패키지 + 4 golden fixtures.

**Sequential within wave**: Task 13-01-01 → 13-01-02 → 13-01-03 → 13-01-04 순서. marker 등록이 선행되어야 conftest.py 가 이를 참조할 수 있고, budget_counter.py 가 선행되어야 test_budget_counter.py 가 import 가능.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-live-smoke/13-RESEARCH.md
@.planning/phases/13-live-smoke/13-VALIDATION.md
@CLAUDE.md

# Reference implementations — Wave 0 가 이들을 확장/재사용
@pytest.ini
@scripts/smoke/phase11_full_run.py
@scripts/orchestrator/invokers.py
@tests/adapters/conftest.py
@.planning/phases/14-api-adapter-remediation/14-01-PLAN.md

# Precedent for BudgetCounter class
@scripts/smoke/phase091_stage2_to_4.py
</context>

<interfaces>
<!-- Key contracts Wave 1~5 will consume. Extracted from codebase via grep + file read. -->
<!-- Executors MUST use these exactly — no codebase exploration needed for Wave 0. -->

From scripts/orchestrator/invokers.py (existing — DO NOT MODIFY in Wave 0):
```python
CLAUDE_CLI_BIN = "claude"  # L61
def _invoke_claude_cli(system_prompt: str, user_prompt: str, *, timeout: int = 120) -> str:  # L141
def make_default_producer_invoker() -> ClaudeAgentProducerInvoker: ...  # L439
def make_default_supervisor_invoker() -> ClaudeAgentSupervisorInvoker: ...  # L452
def _compress_producer_output(output: dict) -> dict: ...  # L495 — Phase 12 AGENT-STD-03
```

From scripts/publisher/smoke_test.py (existing — DO NOT MODIFY in Wave 0):
```python
def run_smoke_test(privacy: str = "unlisted", cleanup: bool = True) -> str: ...
# raises ValueError("privacy must be 'unlisted' for smoke test") on privacy='public'
```

From scripts/publisher/production_metadata.py (existing):
```python
HTML_COMMENT_REGEX = r'<!-- production_metadata\n(\{.*?\})\n-->'  # DOTALL required
def compute_checksum(path: Path) -> str: ...  # "sha256:<64hex>"
```

**New contracts this Wave 0 defines (consumed by Waves 1-5):**

budget_counter.py:
```python
class BudgetExceededError(RuntimeError): ...
class BudgetCounter:
    def __init__(self, cap_usd: float, evidence_path: Path) -> None: ...
    @property
    def total_usd(self) -> float: ...
    def charge(self, provider: str, amount_usd: float, metadata: dict | None = None) -> None: ...
    def persist(self) -> Path: ...
```

phase13_preflight.py:
```python
def check_claude_cli() -> dict: ...  # {"ok": bool, "version": str|None, "error": str|None}
def check_youtube_oauth() -> dict: ...
def check_env_keys() -> dict: ...
def check_evidence_dir_writable() -> dict: ...
def main() -> int: ...  # rc=0 on all OK, 1 on any fail
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 13-01-01: pytest.ini — live_smoke marker 등록 + --run-live option 준비</name>
  <files>pytest.ini</files>
  <read_first>
    - pytest.ini (현재 15 line — adapter_contract marker + addopts 6 line ignore list)
    - .planning/phases/14-api-adapter-remediation/14-01-PLAN.md (Task 14-01-02 marker registration pattern)
    - .planning/phases/13-live-smoke/13-RESEARCH.md §Validation Architecture + §Wave 0 Preflight
    - .planning/phases/13-live-smoke/13-VALIDATION.md row 13-01-03
  </read_first>
  <action>
    현재 `pytest.ini` 의 `[pytest]` 섹션 `markers =` 블록 아래에 **한 줄 추가** (adapter_contract 직후):

    ```ini
    [pytest]
    markers =
        adapter_contract: Phase 14 adapter contract tests (tests/adapters/) — run with -m adapter_contract
        live_smoke: Phase 13 live smoke tests (tests/phase13/) — real API 호출, opt-in via --run-live flag + explicit -m live_smoke
    ```

    `addopts` 블록은 **건드리지 않는다** (Phase 14 가 commit 한 ignore list 는 그대로 보존). Wave 0 는 마커 등록만 수행.

    **구현 주의점**:
    - marker 설명 한 줄은 한국어 + 영어 mix 가능 (다른 marker 와 동일한 스타일).
    - 기존 addopts 의 `--strict-markers` 가 신규 marker 미등록 시 error 를 발생시키므로 Wave 1~5 실행 전 반드시 본 Task 완료 필수.
    - **금기 #2 준수**: TODO 금지.
    - **금기 #3 준수**: pytest.ini 에 comment-out 된 코드 금지 (명시적 설정만).
  </action>
  <verify>
    <automated>grep -c "^    live_smoke:" pytest.ini &amp;&amp; py -3.11 -m pytest --markers 2>/dev/null | grep -c "live_smoke"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^    live_smoke:" pytest.ini` returns 1 (정확히 한 줄 등록).
    - `grep -c "^    adapter_contract:" pytest.ini` returns 1 (Phase 14 marker 보존).
    - `py -3.11 -m pytest --markers 2>/dev/null | grep -c "live_smoke"` returns ≥1 (pytest 가 marker 를 수집).
    - `py -3.11 -m pytest --collect-only -m live_smoke -q 2>&1 | grep -E "no tests ran|collected 0"` hits (마커 등록은 되었지만 아직 Wave 0 에는 live_smoke 테스트 0개 — 정상).
    - `grep -c "TODO" pytest.ini` returns 0.
    - `grep -c "^addopts" pytest.ini` returns 1 (Phase 14 addopts 보존 — not touched).
  </acceptance_criteria>
  <done>
    pytest.ini 가 live_smoke marker 를 등록하여 `pytest -m live_smoke` 호출이 `--strict-markers` 위반 없이 동작한다. Wave 1~5 의 live test 들이 본 marker 를 참조할 수 있다.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 13-01-02: scripts/smoke/budget_counter.py — SMOKE-05 core module</name>
  <files>
    scripts/smoke/budget_counter.py
    tests/phase13/__init__.py
    tests/phase13/test_budget_counter.py
  </files>
  <read_first>
    - scripts/smoke/phase091_stage2_to_4.py (_check_cost_cap precedent, Phase 9.1 Plan 06)
    - .planning/phases/13-live-smoke/13-RESEARCH.md §SMOKE-05 Budget cap $5 enforcement (code example L499-547)
    - .planning/phases/13-live-smoke/13-VALIDATION.md row 13-01-01/13-01-02/13-04-01
  </read_first>
  <behavior>
    - Test 1: `BudgetCounter(cap_usd=5.00, evidence_path=tmp_path/"budget.json")` 생성 성공, `total_usd == 0.0`, `entries == []`.
    - Test 2: `charge("kling", 0.35)` 호출 후 `total_usd == 0.35`, `entries[0]["provider"] == "kling"`, `entries[0]["amount_usd"] == 0.35`, `entries[0]["cumulative_usd"] == 0.35`, timestamp ISO8601 포맷 존재.
    - Test 3: 누적 `$4.99` 에 도달한 상태에서 `charge("runway", 0.02)` (합 $5.01) 호출 시 `BudgetExceededError` raise + error message 에 `"5.00"` + `"runway"` + 한국어 "대표님" 포함. entries 에 실패 호출은 append 되지 않음 (atomic).
    - Test 4: `charge("claude_cli", 0.00)` — $0 ledger 항목도 허용 (Research Open Q #4 해결책), entries 에 기록.
    - Test 5: `persist()` 호출 후 evidence_path 에 JSON 파일 생성, `json.loads(...)` 결과에 `cap_usd`, `total_usd`, `breached`, `entry_count`, `entries` 5 key 존재. `ensure_ascii=False` 로 한국어 metadata 도 readable.
    - Test 6: `charge("typecast", 0.12, metadata={"chars": 1000})` 호출 시 `entries[-1]["metadata"]["chars"] == 1000` 저장.
  </behavior>
  <action>
    **파일 1**: `tests/phase13/__init__.py` — 빈 파일 (package marker).

    **파일 2**: `scripts/smoke/budget_counter.py` (≥60 lines, ~80 expected):

    ```python
    """Phase 13 Budget Counter — SMOKE-05 $5 cap enforcement SSOT.

    Phase 9.1 ``_check_cost_cap`` 의 file-persisted 확장. 본 모듈은 Wave 1~4
    가 공통으로 import 하는 단일 SSOT — 모든 실 API 과금은 이 counter 를
    경유해야 $5 cap 이 유효하게 enforced.
    """
    from __future__ import annotations

    import json
    from datetime import datetime
    from pathlib import Path


    class BudgetExceededError(RuntimeError):
        """예산 상한 초과 — 대표님 중단.

        CLAUDE.md 금기 #3 (try/except 침묵) 위반 방지용 named class — 상위
        호출자가 `except BudgetExceededError` 로만 잡을 수 있게 좁힘.
        """


    class BudgetCounter:
        """In-memory ledger + optional persist to evidence JSON.

        SMOKE-05 요구사항:
          - charge() 가 cap 초과 projected total 을 detect → BudgetExceededError raise
          - entries[] 에 provider + amount_usd + cumulative_usd + timestamp + metadata
          - persist() 가 .planning/phases/13-*/evidence/budget_usage.json 에 write
        """

        def __init__(self, cap_usd: float, evidence_path: Path) -> None:
            self.cap_usd = float(cap_usd)
            self.evidence_path = Path(evidence_path)
            self.entries: list[dict] = []

        @property
        def total_usd(self) -> float:
            return round(sum(e["amount_usd"] for e in self.entries), 4)

        def charge(
            self,
            provider: str,
            amount_usd: float,
            metadata: dict | None = None,
        ) -> None:
            projected = self.total_usd + float(amount_usd)
            if projected > self.cap_usd:
                raise BudgetExceededError(
                    f"예산 상한 초과 ${projected:.2f} > ${self.cap_usd:.2f} "
                    f"대표님 중단 (직전: {provider} ${amount_usd:.2f})"
                )
            self.entries.append({
                "timestamp": datetime.now().isoformat(),
                "provider": provider,
                "amount_usd": round(float(amount_usd), 4),
                "cumulative_usd": round(projected, 4),
                "metadata": metadata or {},
            })

        def persist(self) -> Path:
            self.evidence_path.parent.mkdir(parents=True, exist_ok=True)
            payload = {
                "cap_usd": self.cap_usd,
                "total_usd": self.total_usd,
                "breached": self.total_usd > self.cap_usd,
                "entry_count": len(self.entries),
                "entries": self.entries,
            }
            self.evidence_path.write_text(
                json.dumps(payload, ensure_ascii=False, indent=2),
                encoding="utf-8",
            )
            return self.evidence_path
    ```

    **파일 3**: `tests/phase13/test_budget_counter.py` (≥80 lines, 6 테스트):

    ```python
    """SMOKE-05 Tier 1 evidence-shape tests — mock only, always-run.

    본 테스트는 `@pytest.mark.live_smoke` 없이 실행되어 매 commit pytest
    default 에 포함된다. 실 API 호출 0회.
    """
    from __future__ import annotations

    import json
    import pytest

    from scripts.smoke.budget_counter import BudgetCounter, BudgetExceededError


    def test_initial_state(tmp_path):
        """Test 1 — initial state."""
        counter = BudgetCounter(cap_usd=5.00, evidence_path=tmp_path / "budget.json")
        assert counter.cap_usd == 5.00
        assert counter.total_usd == 0.0
        assert counter.entries == []


    def test_charge_records_entry_with_timestamp(tmp_path):
        """Test 2 — charge records provider/amount/cumulative/timestamp."""
        counter = BudgetCounter(cap_usd=5.00, evidence_path=tmp_path / "budget.json")
        counter.charge("kling", 0.35)
        assert counter.total_usd == 0.35
        assert len(counter.entries) == 1
        entry = counter.entries[0]
        assert entry["provider"] == "kling"
        assert entry["amount_usd"] == 0.35
        assert entry["cumulative_usd"] == 0.35
        assert "timestamp" in entry
        # ISO8601 sanity
        assert "T" in entry["timestamp"]


    def test_charge_overage_raises_and_preserves_state(tmp_path):
        """Test 3 — overage raises BudgetExceededError + atomic (no partial entry)."""
        counter = BudgetCounter(cap_usd=5.00, evidence_path=tmp_path / "budget.json")
        counter.charge("kling", 4.99)
        with pytest.raises(BudgetExceededError) as exc_info:
            counter.charge("runway", 0.02)
        msg = str(exc_info.value)
        assert "5.00" in msg
        assert "runway" in msg
        assert "대표님" in msg
        # Atomic — overage did NOT append
        assert len(counter.entries) == 1
        assert counter.total_usd == 4.99


    def test_zero_cost_claude_cli_entry_allowed(tmp_path):
        """Test 4 — $0.00 ledger entry (Research Q#4 — Max subscription)."""
        counter = BudgetCounter(cap_usd=5.00, evidence_path=tmp_path / "budget.json")
        counter.charge("claude_cli", 0.00, metadata={"note": "Max subscription"})
        assert counter.total_usd == 0.0
        assert counter.entries[0]["provider"] == "claude_cli"
        assert counter.entries[0]["amount_usd"] == 0.0
        assert counter.entries[0]["metadata"] == {"note": "Max subscription"}


    def test_persist_writes_valid_json(tmp_path):
        """Test 5 — persist writes all 5 keys + Korean-readable."""
        evidence_path = tmp_path / "budget.json"
        counter = BudgetCounter(cap_usd=5.00, evidence_path=evidence_path)
        counter.charge("kling", 0.35, metadata={"설명": "I2V 5초 클립"})
        counter.charge("typecast", 0.12)
        result_path = counter.persist()
        assert result_path == evidence_path
        assert evidence_path.exists()
        payload = json.loads(evidence_path.read_text(encoding="utf-8"))
        for key in ("cap_usd", "total_usd", "breached", "entry_count", "entries"):
            assert key in payload
        assert payload["entry_count"] == 2
        assert payload["breached"] is False
        # ensure_ascii=False — Korean metadata readable
        raw_text = evidence_path.read_text(encoding="utf-8")
        assert "설명" in raw_text


    def test_metadata_dict_preserved(tmp_path):
        """Test 6 — metadata dict round-trips."""
        counter = BudgetCounter(cap_usd=5.00, evidence_path=tmp_path / "budget.json")
        counter.charge("typecast", 0.12, metadata={"chars": 1000, "voice": "ko-KR-Female"})
        assert counter.entries[-1]["metadata"]["chars"] == 1000
        assert counter.entries[-1]["metadata"]["voice"] == "ko-KR-Female"
    ```

    **구현 주의점**:
    - 모든 float 연산 후 `round(..., 4)` — epsilon drift 방지.
    - `total_usd` 는 property (mutable state 아님 — entries[] 가 source of truth).
    - `persist()` 는 parent dir mkdir + atomic write — live run 중 디렉토리 생성 실패 방지.
    - test 는 `@pytest.mark.live_smoke` 미부착 → 항상 실행 (Tier 1).
    - **금기 #2 준수**: TODO 없음.
    - **금기 #3 준수**: `BudgetExceededError` named class — 상위가 좁게 catch 가능.
  </action>
  <verify>
    <automated>test -f scripts/smoke/budget_counter.py &amp;&amp; py -3.11 -c "from scripts.smoke.budget_counter import BudgetCounter, BudgetExceededError; c = BudgetCounter(5.00, __import__('pathlib').Path('/tmp/bc.json')); c.charge('kling', 0.35); assert c.total_usd == 0.35; print('OK')" &amp;&amp; py -3.11 -m pytest tests/phase13/test_budget_counter.py -v --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/smoke/budget_counter.py` returns 0.
    - `test -f tests/phase13/__init__.py` returns 0.
    - `test -f tests/phase13/test_budget_counter.py` returns 0.
    - `wc -l scripts/smoke/budget_counter.py` returns ≥60 lines.
    - `wc -l tests/phase13/test_budget_counter.py` returns ≥80 lines.
    - `grep -c "class BudgetCounter" scripts/smoke/budget_counter.py` returns 1.
    - `grep -c "class BudgetExceededError" scripts/smoke/budget_counter.py` returns 1.
    - `grep -c "def charge" scripts/smoke/budget_counter.py` returns 1.
    - `grep -c "def persist" scripts/smoke/budget_counter.py` returns 1.
    - `grep -c "ensure_ascii=False" scripts/smoke/budget_counter.py` returns ≥1 (한국어 metadata readable).
    - `grep -c "대표님" scripts/smoke/budget_counter.py` returns ≥1.
    - `grep -c "TODO" scripts/smoke/budget_counter.py` returns 0.
    - `py -3.11 -m pytest tests/phase13/test_budget_counter.py -v --no-cov` exit 0 with ≥6 tests passing.
    - `py -3.11 -c "from scripts.smoke.budget_counter import BudgetCounter"` imports successfully.
  </acceptance_criteria>
  <done>
    Wave 1~4 가 공통 import 할 `BudgetCounter` + `BudgetExceededError` 가 scripts/smoke/budget_counter.py 에 존재하며, 6 Tier 1 always-run pytest 가 green. SMOKE-05 SSOT 확립 완료.
  </done>
</task>

<task type="auto">
  <name>Task 13-01-03: tests/phase13/conftest.py + fixtures — Phase 13 공용 fixture scaffold</name>
  <files>
    tests/phase13/conftest.py
    tests/phase13/fixtures/sample_producer_output.json
    tests/phase13/fixtures/sample_supervisor_output.json
    tests/phase13/fixtures/sample_smoke_upload.json
    tests/phase13/fixtures/sample_smoke_e2e.json
  </files>
  <read_first>
    - tests/adapters/conftest.py (Phase 14 fixture pattern — repo_root + _fake_env)
    - .planning/phases/13-live-smoke/13-RESEARCH.md §SMOKE-01/02/04/06 evidence JSON shape examples (L235-244, L347-358, L565-579)
    - .planning/phases/13-live-smoke/13-VALIDATION.md §Wave 0 Requirements
  </read_first>
  <action>
    **파일 1**: `tests/phase13/conftest.py`:

    ```python
    """Phase 13 공용 pytest fixtures — tests/phase13/ 전체가 재사용.

    Tier 2 (`@pytest.mark.live_smoke`) 테스트는 `--run-live` CLI flag 없이
    실행 시 자동 skip — 실 API 과금 방지 (CLAUDE.md 금기 #8 일일 업로드 배제).
    """
    from __future__ import annotations

    from pathlib import Path
    import sys

    import pytest

    # Phase 5/14 precedent — repo_root 확보 + sys.path 보강
    REPO_ROOT = Path(__file__).resolve().parents[2]
    if str(REPO_ROOT) not in sys.path:
        sys.path.insert(0, str(REPO_ROOT))


    def pytest_addoption(parser):
        """Add --run-live flag to enable live_smoke tier."""
        parser.addoption(
            "--run-live",
            action="store_true",
            default=False,
            help=(
                "Run live_smoke-marked tests (Phase 13 Tier 2). "
                "실 API 과금 — 대표님 승인 후 1회만 사용."
            ),
        )


    def pytest_collection_modifyitems(config, items):
        """Skip live_smoke tests unless --run-live is explicitly passed."""
        if config.getoption("--run-live"):
            return
        skip_live = pytest.mark.skip(reason="--run-live 미지정 — Tier 2 skipped")
        for item in items:
            if "live_smoke" in item.keywords:
                item.add_marker(skip_live)


    @pytest.fixture(scope="session")
    def repo_root() -> Path:
        """Phase 14 conftest 와 동일 계약."""
        return REPO_ROOT


    @pytest.fixture
    def tmp_evidence_dir(tmp_path, monkeypatch):
        """Temporary evidence dir + SHORTS_PUBLISH_LOCK_PATH override.

        Wave 2/4 live 테스트가 .planning/phases/13-*/evidence/ 대신 tmp 를
        쓰도록 유도 — 실제 evidence dir 에 테스트 fixture 오염 방지.
        """
        evidence = tmp_path / "evidence"
        evidence.mkdir(parents=True, exist_ok=True)
        lock_path = tmp_path / "phase13_smoke_lock.json"
        monkeypatch.setenv("SHORTS_PUBLISH_LOCK_PATH", str(lock_path))
        return evidence


    @pytest.fixture
    def fixtures_dir() -> Path:
        """tests/phase13/fixtures/ 경로 — golden JSON 4종."""
        return Path(__file__).resolve().parent / "fixtures"


    @pytest.fixture
    def fake_claude_cli_output():
        """Mock producer JSON output — schema 검증용 최소 페이로드.

        Wave 1/4 의 evidence-shape 테스트가 본 fixture 를 이용하여 실제
        Claude CLI 호출 없이 producer_output 구조를 검증.
        """
        return {
            "gate": "TREND",
            "verdict": "PASS",
            "decisions": [
                {"id": "D-01", "value": "k-pop 최신 앨범 이슈", "confidence": 0.9}
            ],
            "artifacts": {"producer_output": {"niche": "k-pop", "source": "reddit"}},
            "raw_response": "{'gate': 'TREND', 'verdict': 'PASS', ...}",
        }
    ```

    **파일 2**: `tests/phase13/fixtures/sample_producer_output.json` (SMOKE-01 golden):

    ```json
    {
      "session_id": "20260421_sample",
      "timestamp": "2026-04-21T22:30:00+09:00",
      "producer_gates": {
        "TREND": {"niche": "k-pop", "topic": "album-preview-v1", "confidence": 0.92},
        "SCRIPT": {"hook_ko": "이번 앨범 진짜 미쳤대요", "length_sec": 48},
        "METADATA": {"title_ko": "샘플 제목", "tags": ["shorts", "k-pop"]}
      }
    }
    ```

    **파일 3**: `tests/phase13/fixtures/sample_supervisor_output.json` (SMOKE-02 golden):

    ```json
    {
      "session_id": "20260421_sample",
      "supervisor_calls": [
        {
          "gate": "TREND",
          "inspector_count": 3,
          "pre_compress_bytes": 9824,
          "post_compress_bytes": 2373,
          "ratio": 0.24,
          "returncode": 0,
          "verdict": "PASS"
        },
        {
          "gate": "SCRIPT",
          "inspector_count": 6,
          "pre_compress_bytes": 14200,
          "post_compress_bytes": 3100,
          "ratio": 0.22,
          "returncode": 0,
          "verdict": "PASS"
        }
      ],
      "rc1_count": 0,
      "compression_ratio_avg": 0.23
    }
    ```

    **파일 4**: `tests/phase13/fixtures/sample_smoke_upload.json` (SMOKE-04 golden):

    ```json
    {
      "session_id": "20260421_sample",
      "video_id": "dQw4w9WgXcQ",
      "description_raw": "샘플 설명\n\n<!-- production_metadata\n{\"script_seed\": \"seed123\", \"assets_origin\": \"smoke:test\", \"pipeline_version\": \"1.0.0\", \"checksum\": \"sha256:abc123def456\"}\n-->",
      "production_metadata": {
        "script_seed": "seed123",
        "assets_origin": "smoke:test",
        "pipeline_version": "1.0.0",
        "checksum": "sha256:abc123def456"
      },
      "required_fields_present": true,
      "missing_fields": []
    }
    ```

    **파일 5**: `tests/phase13/fixtures/sample_smoke_e2e.json` (SMOKE-06 golden):

    ```json
    {
      "session_id": "20260421_sample",
      "status": "OK",
      "wall_time_seconds": 487.3,
      "gate_timestamps": {
        "IDLE": "2026-04-21T22:30:00+09:00",
        "TREND": "2026-04-21T22:30:15+09:00",
        "NICHE": "2026-04-21T22:30:45+09:00",
        "RESEARCH_NLM": "2026-04-21T22:31:10+09:00",
        "BLUEPRINT": "2026-04-21T22:31:40+09:00",
        "SCRIPT": "2026-04-21T22:32:30+09:00",
        "POLISH": "2026-04-21T22:33:00+09:00",
        "VOICE": "2026-04-21T22:33:45+09:00",
        "ASSETS": "2026-04-21T22:36:00+09:00",
        "ASSEMBLY": "2026-04-21T22:36:30+09:00",
        "THUMBNAIL": "2026-04-21T22:37:00+09:00",
        "METADATA": "2026-04-21T22:37:30+09:00",
        "COMPLETE": "2026-04-21T22:38:07+09:00"
      },
      "gate_count": 13,
      "final_video_id": "dQw4w9WgXcQ",
      "total_cost_usd": 2.93,
      "budget_cap_usd": 5.00,
      "budget_breached": false,
      "supervisor_rc1_count": 0
    }
    ```

    **구현 주의점**:
    - `pytest_addoption` + `pytest_collection_modifyitems` 는 pytest 표준 hook — Phase 14 에는 존재하지 않았지만 Phase 13 에서 신규 도입 (live_smoke 특유 요구).
    - fixture JSON 은 **실 production 에는 절대 쓰이지 않음** — `"session_id": "20260421_sample"` 로 식별.
    - `tmp_evidence_dir` fixture 가 `SHORTS_PUBLISH_LOCK_PATH` 를 monkeypatch — 실제 smoke lock 오염 방지 (RESEARCH §Pitfall 5).
    - **금기 #2 준수**: TODO 없음.
    - **금기 #8 준수**: `--run-live` 없이는 live_smoke tier 자동 skip — 매 pytest 호출마다 실 API 과금 trigger 불가.
  </action>
  <verify>
    <automated>test -f tests/phase13/conftest.py &amp;&amp; test -d tests/phase13/fixtures &amp;&amp; test -f tests/phase13/fixtures/sample_producer_output.json &amp;&amp; test -f tests/phase13/fixtures/sample_supervisor_output.json &amp;&amp; test -f tests/phase13/fixtures/sample_smoke_upload.json &amp;&amp; test -f tests/phase13/fixtures/sample_smoke_e2e.json &amp;&amp; py -3.11 -c "import json; [json.load(open(f'tests/phase13/fixtures/sample_{n}.json', encoding='utf-8')) for n in ('producer_output', 'supervisor_output', 'smoke_upload', 'smoke_e2e')]; print('OK')" &amp;&amp; py -3.11 -m pytest tests/phase13/ --collect-only -q --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/phase13/conftest.py` returns 0.
    - `grep -c "pytest_addoption" tests/phase13/conftest.py` returns 1.
    - `grep -c "pytest_collection_modifyitems" tests/phase13/conftest.py` returns 1.
    - `grep -c "def tmp_evidence_dir" tests/phase13/conftest.py` returns 1.
    - `grep -c "def fixtures_dir" tests/phase13/conftest.py` returns 1.
    - `grep -c "def fake_claude_cli_output" tests/phase13/conftest.py` returns 1.
    - `grep -c "SHORTS_PUBLISH_LOCK_PATH" tests/phase13/conftest.py` returns 1.
    - `grep -c "--run-live" tests/phase13/conftest.py` returns ≥2.
    - `test -f tests/phase13/fixtures/sample_producer_output.json` returns 0.
    - `test -f tests/phase13/fixtures/sample_supervisor_output.json` returns 0.
    - `test -f tests/phase13/fixtures/sample_smoke_upload.json` returns 0.
    - `test -f tests/phase13/fixtures/sample_smoke_e2e.json` returns 0.
    - `py -3.11 -c "import json; json.load(open('tests/phase13/fixtures/sample_smoke_e2e.json', encoding='utf-8'))['gate_count']"` returns 13.
    - `py -3.11 -c "import json; d=json.load(open('tests/phase13/fixtures/sample_smoke_upload.json', encoding='utf-8')); assert d['required_fields_present']"` exit 0.
    - `py -3.11 -m pytest tests/phase13/ --collect-only -q --no-cov` exit 0 (collection 에러 없음).
    - `grep -c "TODO" tests/phase13/conftest.py` returns 0.
  </acceptance_criteria>
  <done>
    Phase 13 공용 fixture 5종 (repo_root / tmp_evidence_dir / fixtures_dir / fake_claude_cli_output / pytest_addoption) + 4 golden JSON 이 anchoring 되어 Wave 1~5 tests 가 이들을 import 가능하다.
  </done>
</task>

<task type="auto">
  <name>Task 13-01-04: scripts/smoke/phase13_preflight.py — Claude CLI + YouTube OAuth + .env 사전 점검 CLI</name>
  <files>scripts/smoke/phase13_preflight.py</files>
  <read_first>
    - scripts/smoke/phase11_full_run.py (L155-194 _check_env_readiness pattern)
    - scripts/orchestrator/invokers.py (L61 CLAUDE_CLI_BIN + L141 _invoke_claude_cli)
    - .planning/phases/13-live-smoke/13-RESEARCH.md §Pitfall 1/2/4 (Credential + CLI + Quota)
    - .planning/phases/13-live-smoke/13-VALIDATION.md row 13-01-04
  </read_first>
  <action>
    `scripts/smoke/phase13_preflight.py` 신설 (≥80 lines, ~120 expected). 4 체크를 수행하고 모두 OK 시 rc=0, 하나라도 실패 시 rc=1:

    ```python
    """Phase 13 Preflight — Wave 1~5 live run 진입 전 환경 sanity check.

    4 체크:
      1. Claude CLI reachable (`claude --version` rc=0)
      2. YouTube OAuth token refresh OK (config/youtube_token.json valid)
      3. .env 필수 키 5종 (TYPECAST / GOOGLE / YOUTUBE_CLIENT_SECRETS_FILE
         / FAL 또는 KLING / 선택: ELEVENLABS / RUNWAY / SHOTSTACK)
      4. .planning/phases/13-live-smoke/evidence/ write-access

    Windows cp949 guard + ANSI-safe log + exit code contract. 본 CLI 는 Wave
    1~4 전에 대표님이 수동 실행하여 billable run 진입 전 사전 차단이 가능
    하도록 한다 (RESEARCH §Pitfall 1-4).
    """
    from __future__ import annotations

    import json
    import os
    import shutil
    import subprocess
    import sys
    from pathlib import Path

    # Windows cp949 guard (Phase 11 pattern)
    if hasattr(sys.stdout, "reconfigure"):
        try:
            sys.stdout.reconfigure(encoding="utf-8")  # type: ignore[attr-defined]
        except (AttributeError, OSError) as err:
            sys.stderr.write(f"[preflight] stdout reconfigure skipped: {err}\n")

    _REPO_ROOT = Path(__file__).resolve().parents[2]
    if str(_REPO_ROOT) not in sys.path:
        sys.path.insert(0, str(_REPO_ROOT))


    def check_claude_cli() -> dict:
        """1. Claude CLI reachable + logged-in."""
        bin_path = shutil.which("claude")
        if not bin_path:
            return {"ok": False, "error": "claude CLI not found in PATH"}
        try:
            result = subprocess.run(
                ["claude", "--version"],
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=30,
            )
        except (subprocess.TimeoutExpired, OSError) as err:
            return {"ok": False, "error": f"claude --version failed: {err}"}
        if result.returncode != 0:
            return {
                "ok": False,
                "error": f"rc={result.returncode} stderr={result.stderr[-200:]}",
            }
        return {"ok": True, "version": result.stdout.strip(), "path": bin_path}


    def check_youtube_oauth() -> dict:
        """2. YouTube OAuth refresh token valid."""
        token_path = _REPO_ROOT / "config" / "youtube_token.json"
        client_secret_path = _REPO_ROOT / "config" / "client_secret.json"
        if not token_path.exists():
            return {"ok": False, "error": f"missing {token_path}"}
        if not client_secret_path.exists():
            return {"ok": False, "error": f"missing {client_secret_path}"}
        try:
            payload = json.loads(token_path.read_text(encoding="utf-8"))
        except (OSError, json.JSONDecodeError) as err:
            return {"ok": False, "error": f"token parse failed: {err}"}
        if not payload.get("refresh_token"):
            return {"ok": False, "error": "refresh_token field missing"}
        return {"ok": True, "has_refresh_token": True, "token_path": str(token_path)}


    def check_env_keys() -> dict:
        """3. .env 필수 키 + 선택 키 보유 확인."""
        required = [
            "TYPECAST_API_KEY",
            "GOOGLE_API_KEY",
        ]
        any_of = [
            ("FAL_KEY", "KLING_API_KEY"),
        ]
        optional = [
            "ELEVENLABS_API_KEY",
            "RUNWAY_API_KEY",
            "SHOTSTACK_API_KEY",
        ]
        # Ensure .env loaded
        try:
            from scripts.orchestrator import _load_dotenv_if_present  # type: ignore
            _load_dotenv_if_present()
        except ImportError:
            pass
        missing_required = [k for k in required if not os.environ.get(k)]
        satisfied_any_of = [
            pair for pair in any_of if any(os.environ.get(k) for k in pair)
        ]
        any_of_missing = [pair for pair in any_of if pair not in satisfied_any_of]
        present_optional = [k for k in optional if os.environ.get(k)]
        ok = not missing_required and not any_of_missing
        return {
            "ok": ok,
            "missing_required": missing_required,
            "any_of_missing": [list(p) for p in any_of_missing],
            "present_optional": present_optional,
        }


    def check_evidence_dir_writable() -> dict:
        """4. .planning/phases/13-*/evidence/ write probe."""
        evidence_dir = (
            _REPO_ROOT / ".planning" / "phases" / "13-live-smoke" / "evidence"
        )
        try:
            evidence_dir.mkdir(parents=True, exist_ok=True)
            probe = evidence_dir / ".preflight_probe"
            probe.write_text("OK", encoding="utf-8")
            probe.unlink()
        except OSError as err:
            return {"ok": False, "error": f"write probe failed: {err}"}
        return {"ok": True, "path": str(evidence_dir)}


    def main() -> int:
        checks = {
            "claude_cli": check_claude_cli(),
            "youtube_oauth": check_youtube_oauth(),
            "env_keys": check_env_keys(),
            "evidence_dir": check_evidence_dir_writable(),
        }
        all_ok = all(c.get("ok") for c in checks.values())
        print("=" * 60)
        print("Phase 13 Preflight Report")
        print("=" * 60)
        for name, result in checks.items():
            status = "OK" if result.get("ok") else "FAIL"
            print(f"  [{status}] {name}: {result}")
        print("=" * 60)
        print(f"Overall: {'ALL_PASS' if all_ok else 'FAIL'}")
        return 0 if all_ok else 1


    if __name__ == "__main__":
        sys.exit(main())
    ```

    **구현 주의점**:
    - 네 체크 모두 **명시적 rc 기록** + **침묵 금지** (금기 #3) — 실패 시 stdout 에 print + rc=1.
    - `subprocess.run(["claude", "--version"])` 은 실 Anthropic API 호출 아님 — local CLI binary probe 만.
    - `scripts.orchestrator._load_dotenv_if_present` 호출 시도 — Phase 11 PIPELINE-02 ship 경로 우선.
    - **금기 #2 준수**: TODO 없음.
    - **금기 #5 준수**: Selenium 관련 코드 없음 — YouTube 체크는 config JSON 파일 존재 + refresh_token field presence 만 (실제 API 호출 없음; Wave 2 가 실 호출 수행).
    - 출력 형식은 플레인 텍스트 — Wave 1~5 실행 전 대표님이 수동으로 `py -3.11 scripts/smoke/phase13_preflight.py` 돌려 rc 확인 가능.
  </action>
  <verify>
    <automated>test -f scripts/smoke/phase13_preflight.py &amp;&amp; py -3.11 -c "import importlib.util; spec=importlib.util.spec_from_file_location('p13pre','scripts/smoke/phase13_preflight.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); print('import OK'); print('functions:', sorted([n for n in dir(m) if n.startswith('check_') or n == 'main']))"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/smoke/phase13_preflight.py` returns 0.
    - `wc -l scripts/smoke/phase13_preflight.py` returns ≥80 lines.
    - `grep -c "def check_claude_cli" scripts/smoke/phase13_preflight.py` returns 1.
    - `grep -c "def check_youtube_oauth" scripts/smoke/phase13_preflight.py` returns 1.
    - `grep -c "def check_env_keys" scripts/smoke/phase13_preflight.py` returns 1.
    - `grep -c "def check_evidence_dir_writable" scripts/smoke/phase13_preflight.py` returns 1.
    - `grep -c "def main" scripts/smoke/phase13_preflight.py` returns 1.
    - `grep -c "ALL_PASS" scripts/smoke/phase13_preflight.py` returns ≥1.
    - `grep -c "reconfigure.*utf-8" scripts/smoke/phase13_preflight.py` returns ≥1 (Windows cp949 guard).
    - `grep -c "TODO" scripts/smoke/phase13_preflight.py` returns 0.
    - `py -3.11 -c "import importlib.util; spec=importlib.util.spec_from_file_location('p','scripts/smoke/phase13_preflight.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m)"` exit 0 (import smoke).
  </acceptance_criteria>
  <done>
    대표님이 `py -3.11 scripts/smoke/phase13_preflight.py` 한 번 실행으로 Wave 1~5 live run 진입 전제 4종 (Claude CLI / YouTube OAuth / .env 키 / evidence dir) 을 단일 명령으로 사전 검증 가능. rc=0 만이 다음 wave 진입 허용.
  </done>
</task>

</tasks>

<verification>
Wave 0 완료 조건 (Plan 01 전체):
1. `pytest.ini` 에 `live_smoke` marker 등록 + Phase 14 `adapter_contract` marker 보존.
2. `scripts/smoke/budget_counter.py` 신설 + `BudgetCounter` + `BudgetExceededError` 클래스 export + 6 Tier 1 test GREEN.
3. `scripts/smoke/phase13_preflight.py` 신설 + import smoke 성공 + 4 check 함수 + main() rc contract.
4. `tests/phase13/__init__.py` + `tests/phase13/conftest.py` 신설 + `--run-live` option + 4 fixture.
5. `tests/phase13/fixtures/` 에 4 golden JSON (producer_output / supervisor_output / smoke_upload / smoke_e2e) anchoring.
6. `pytest tests/phase13/ -m "not live_smoke" --no-cov -q` exit 0 (Tier 1 baseline = 6 tests).
7. Phase 14 regression 보존 — `pytest -m adapter_contract -q` 가 여전히 ≥30 tests green (본 Plan 이 pytest.ini addopts 미수정).
</verification>

<success_criteria>
- SMOKE-01~06 전체 requirements 의 **인프라 전제** 확보 (Wave 1~5 어떤 plan 도 본 Plan 없이 진행 불가).
- Tier 1 (always-run) vs Tier 2 (live_smoke) 분리 확립 — 매 pytest 호출이 실 API 과금 trigger 하지 않음.
- CLAUDE.md 금기 #2 (TODO 금지), #3 (침묵 폴백 금지), #8 (일일 업로드 금지 — live_smoke 자동 skip) 전수 준수.
- CLAUDE.md 필수 #7 (한국어 존댓말 baseline — "대표님", "합니다"), #8 (증거 기반 — 모든 주장에 grep/test/pytest 명령 첨부) 전수 준수.
- pytest 전체 regression preserved — Phase 14 `adapter_contract` marker 30 tests green + Phase 4~12 기존 tests 영향 없음.
</success_criteria>

<output>
완료 후 `.planning/phases/13-live-smoke/13-01-SUMMARY.md` 작성:
- 4 Task 별 산출 파일 + 검증 명령 rc 기록.
- `pytest tests/phase13/ -m "not live_smoke" --no-cov -q` 결과 (예상: 6 passed).
- `pytest -m adapter_contract --no-cov -q` 결과 (예상: Phase 14 ≥30 passed — 회귀 없음).
- `py -3.11 scripts/smoke/phase13_preflight.py` 실행 시 현재 환경 rc + 4 체크 상태.
- 대표님 보고 2-문장 요약 (존댓말 baseline).
</output>
