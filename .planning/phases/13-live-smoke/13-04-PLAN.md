---
phase: 13-live-smoke
plan: 04
plan_id: 13-04
type: execute
wave: 3
depends_on:
  - 13-01
files_modified:
  - tests/phase13/test_budget_enforcement.py
  - scripts/smoke/provider_rates.py
autonomous: true
requirements:
  - SMOKE-05
commit_discipline: per-task
must_haves:
  truths:
    - "Tier 1: `pytest tests/phase13/test_budget_enforcement.py -m \"not live_smoke\"` 가 $4.99 누적 상태에서 $0.02 charge → `BudgetExceededError` raise + entries[] atomic (실패 entry append 안 됨) 검증 — mock adapter 호출 기반, 실 API 0회"
    - "Tier 1: 동일 test 가 각 provider 별 line item (nanobanana $0.04, kling $0.35 × 8 cuts, typecast $0.12, runway $0.25 fallback, elevenlabs $0.30 fallback) 을 순차 charge 후 총액이 Research §SMOKE-05 Expected Total $1.50~$3.00 범위에 진입하는 시뮬레이션 케이스 검증"
    - "Tier 1: 동일 test 가 `$0.00 claude_cli` ledger entry 를 허용 (Research Open Q #4 — Max 구독 정보 완결성)"
    - "`scripts/smoke/provider_rates.py` 모듈이 `PROVIDER_RATES_USD: dict` (Research §Provider unit prices 표 L322-331 그대로 옮김) 를 export — Wave 4 Full E2E 가 import 하여 실 adapter 호출 후 동적 charge 수행"
    - "Tier 2 (본 Plan 에서는 unit test 만 — Wave 4 Full E2E Plan 05 가 실 run + evidence 생성 수행): live run 종료 시 `evidence/budget_usage.json` 이 `cap_usd: 5.00`, `total_usd <= 5.00`, `breached: false`, `entries: list` 5 key + 각 provider line item 포함 — 본 invariant 는 `test_budget_enforcement.py` 가 fixture 기반 Tier 1 으로 검증"
  artifacts:
    - path: "scripts/smoke/provider_rates.py"
      provides: "Provider unit price SSOT — Research 표 그대로"
      contains: "PROVIDER_RATES_USD"
      min_lines: 40
    - path: "tests/phase13/test_budget_enforcement.py"
      provides: "SMOKE-05 Tier 1 + live evidence shape tests"
      contains: "BudgetExceededError"
      min_lines: 100
  key_links:
    - from: "scripts/smoke/provider_rates.py::PROVIDER_RATES_USD"
      to: "scripts/smoke/budget_counter.py::BudgetCounter.charge(provider, amount_usd)"
      via: "Wave 4 `phase13_live_smoke.py` 가 adapter 호출 후 rates[provider] 조회 → counter.charge(...)"
      pattern: "PROVIDER_RATES_USD\\["
    - from: "tests/phase13/test_budget_enforcement.py"
      to: "scripts/smoke/budget_counter.py::BudgetExceededError"
      via: "pytest.raises(BudgetExceededError) + state snapshot assertion"
      pattern: "BudgetExceededError"
---

<objective>
Wave 3 Budget Cap Enforcement — SMOKE-05 ($5 cap enforcement + budget_usage.json 기록) 의 Tier 1 always-run unit test + Wave 4 가 재사용할 provider rates SSOT 구축. 실 API 호출 없이 `BudgetCounter` 의 atomic 동작 + `BudgetExceededError` 상향 전파 + $0 claude_cli ledger entry + 각 provider 별 라인 아이템 + `budget_usage.json` shape 을 전수 검증한다.

Purpose: Phase 9.1 `_check_cost_cap` 패턴의 file-persisted 확장을 regression suite 에 anchor. Wave 4 `phase13_live_smoke.py` 실행 중 실 adapter 호출이 `counter.charge(provider, PROVIDER_RATES_USD[provider])` 형태로 단일 SSOT 를 사용하도록 강제. 본 Plan 은 Wave 4 가 실 run 전에 Tier 1 green 이어야 하는 전제 계약을 확립.

Output: 1 test 파일 (≥7 tests, all Tier 1) + 1 provider rates SSOT 모듈. 실 API 호출 0회 — 비용 $0.00.

**Sequential within wave**: Task 13-04-01 (provider_rates.py) → 13-04-02 (test_budget_enforcement.py). rates 가 선행되어야 test 에서 import 가능.

**Parallel-capable with Wave 1/2 (Plans 02/03)**: Wave 3 의 disjoint files_modified — 다른 evidence (budget_usage vs producer/supervisor/smoke_upload) + 다른 test 파일. 단 depends_on 은 Wave 0 (Plan 01) 이므로 wave=3 으로 분리. Wave 1/2 와 병렬 실행 가능 (실제 orchestrator 는 Wave 1 → Wave 2 → Wave 3 순차 실행하나 logical 의존성은 Wave 0 만).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-live-smoke/13-RESEARCH.md
@.planning/phases/13-live-smoke/13-VALIDATION.md
@CLAUDE.md

# Wave 0 dependency (consumed by Wave 3 tests)
@scripts/smoke/budget_counter.py
@tests/phase13/conftest.py

# Phase 9.1 precedent
@scripts/smoke/phase091_stage2_to_4.py

# Provider rates sources (memory files — Research §Provider Rates 근거)
@.claude/memory/project_video_stack_kling26.md
@.claude/memory/project_tts_stack_typecast.md
@.claude/memory/project_claude_code_max_no_api_key.md
</context>

<interfaces>
<!-- From Wave 0 (consume only) -->
scripts/smoke/budget_counter.py:
```python
class BudgetExceededError(RuntimeError): ...
class BudgetCounter:
    def __init__(self, cap_usd: float, evidence_path: Path) -> None: ...
    @property
    def total_usd(self) -> float: ...
    def charge(self, provider: str, amount_usd: float, metadata: dict | None = None) -> None: ...
    def persist(self) -> Path: ...
```

<!-- New contracts defined here (consumed by Wave 4) -->
scripts/smoke/provider_rates.py:
```python
PROVIDER_RATES_USD: dict[str, float]
# Keys: claude_cli (0.00), nanobanana (0.04/image), kling (0.35/5s clip),
#       runway (0.25/5s clip fallback), typecast (0.12/1K chars),
#       elevenlabs (0.30/1K chars fallback), notebooklm (0.00),
#       youtube_api (0.00)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 13-04-01: scripts/smoke/provider_rates.py — Provider unit price SSOT</name>
  <files>scripts/smoke/provider_rates.py</files>
  <read_first>
    - .planning/phases/13-live-smoke/13-RESEARCH.md §SMOKE-05 Provider unit prices table (L320-332)
    - .claude/memory/project_video_stack_kling26.md (Kling 2.6 Pro rate)
    - .claude/memory/project_tts_stack_typecast.md (Typecast rate)
    - .claude/memory/project_claude_code_max_no_api_key.md (Claude CLI Max 구독 $0 원칙)
  </read_first>
  <action>
    신규 파일 `scripts/smoke/provider_rates.py` (≥40 lines, ~60 expected):

    1. Module docstring — "Phase 13 Provider Rates — SMOKE-05 budget cap 계산을 위한 provider 별 unit price SSOT. Research §SMOKE-05 L320-332 그대로. Wave 4 `phase13_live_smoke.py` 가 import 하여 실 adapter 호출 후 counter.charge(provider, PROVIDER_RATES_USD[provider] * units) 수행."
    2. Module constant:

    ```python
    # Research §SMOKE-05 Provider unit prices table (L322-331).
    # Valid until 2026-05-21 (Research valid_until). Wave 0 Preflight 단계에서
    # 실 adapter 호출 전 가격 drift 재확인 권장 (Research §Metadata confidence: MEDIUM).
    PROVIDER_RATES_USD: dict[str, float] = {
        "claude_cli": 0.00,     # Max 구독 inclusive (project_claude_code_max_no_api_key.md)
        "notebooklm": 0.00,     # Max 구독 inclusive (Google One)
        "youtube_api": 0.00,    # Free within 10K/day quota
        "nanobanana": 0.04,     # per image (Phase 9.1 실측)
        "kling": 0.35,          # per 5s clip (FAL rate, project_video_stack_kling26.md)
        "runway": 0.25,         # per 5s clip fallback (Phase 9.1 실측 $0.25)
        "typecast": 0.12,       # per 1K chars (project_tts_stack_typecast.md creator tier)
        "elevenlabs": 0.30,     # per 1K chars fallback (published Creator tier)
    }
    ```

    3. Helper 함수:

    ```python
    def estimate_adapter_cost(provider: str, units: float = 1.0) -> float:
        """Compute adapter cost given provider key + units consumed.

        Raises KeyError explicitly if provider unknown — 금기 #3 침묵 금지.
        """
        if provider not in PROVIDER_RATES_USD:
            raise KeyError(
                f"unknown provider '{provider}' — Research §SMOKE-05 rates table 미등록"
            )
        return round(PROVIDER_RATES_USD[provider] * float(units), 4)


    def all_known_providers() -> list[str]:
        """Return sorted list of providers for audit/logging."""
        return sorted(PROVIDER_RATES_USD.keys())
    ```

    **구현 주의점**:
    - Dict 의 key 는 lowercase + snake_case — grep 검색 용이성.
    - `claude_cli` / `notebooklm` / `youtube_api` 는 $0.00 — Research Open Q #4 채택 (ledger 완결성).
    - `estimate_adapter_cost` 는 `units` 기본값 1.0 — kling 8 cuts 는 `estimate_adapter_cost("kling", 8)` = $2.80.
    - **금기 #2**: TODO 없음. **금기 #3**: 알 수 없는 provider 시 `raise KeyError` (silent $0 fallback 금지 — 오기 버그 은폐 방지).
  </action>
  <verify>
    <automated>test -f scripts/smoke/provider_rates.py &amp;&amp; py -3.11 -c "from scripts.smoke.provider_rates import PROVIDER_RATES_USD, estimate_adapter_cost, all_known_providers; assert len(PROVIDER_RATES_USD) >= 8; assert PROVIDER_RATES_USD['claude_cli'] == 0.00; assert PROVIDER_RATES_USD['kling'] == 0.35; assert estimate_adapter_cost('kling', 8) == 2.80; print('providers:', all_known_providers())"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/smoke/provider_rates.py` returns 0.
    - `wc -l scripts/smoke/provider_rates.py` returns ≥40.
    - `grep -c "PROVIDER_RATES_USD" scripts/smoke/provider_rates.py` returns ≥2.
    - `grep -c "def estimate_adapter_cost" scripts/smoke/provider_rates.py` returns 1.
    - `grep -c "def all_known_providers" scripts/smoke/provider_rates.py` returns 1.
    - `grep -c "claude_cli" scripts/smoke/provider_rates.py` returns ≥1.
    - `grep -c "notebooklm" scripts/smoke/provider_rates.py` returns ≥1.
    - `grep -c "kling" scripts/smoke/provider_rates.py` returns ≥1.
    - `grep -c "typecast" scripts/smoke/provider_rates.py` returns ≥1.
    - `grep -c "TODO" scripts/smoke/provider_rates.py` returns 0.
    - `py -3.11 -c "from scripts.smoke.provider_rates import PROVIDER_RATES_USD; assert len(PROVIDER_RATES_USD) >= 8 and PROVIDER_RATES_USD['claude_cli'] == 0.00 and PROVIDER_RATES_USD['kling'] == 0.35"` exit 0.
    - `py -3.11 -c "from scripts.smoke.provider_rates import estimate_adapter_cost; assert estimate_adapter_cost('kling', 8) == 2.80"` exit 0.
  </acceptance_criteria>
  <done>
    Wave 4 가 공통 import 할 `PROVIDER_RATES_USD` + `estimate_adapter_cost` + `all_known_providers` 확립. Research §SMOKE-05 pricing table 이 코드 SSOT 로 anchoring.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 13-04-02: tests/phase13/test_budget_enforcement.py — SMOKE-05 Tier 1 enforcement + evidence shape</name>
  <files>tests/phase13/test_budget_enforcement.py</files>
  <read_first>
    - scripts/smoke/budget_counter.py (Wave 0 산출물)
    - scripts/smoke/provider_rates.py (Task 13-04-01 산출물)
    - tests/phase13/conftest.py (tmp_evidence_dir fixture)
    - .planning/phases/13-live-smoke/13-RESEARCH.md §SMOKE-05 Expected total + ledger shape
    - .planning/phases/13-live-smoke/13-VALIDATION.md row 13-04-01 / 13-04-02
  </read_first>
  <behavior>
    - Test 1 (`test_under_cap_accumulates_correctly`): counter 생성 후 typecast $0.12 + kling $0.35 + nanobanana $0.04 순차 charge → total == $0.51, entries len == 3, each entry provider match, cumulative_usd monotonic.
    - Test 2 (`test_cap_overage_raises_and_atomic`): $4.99 charge 후 $0.02 charge 시도 → `BudgetExceededError` raise + `"5.00"` + `"runway"` + `"대표님"` in message + `counter.total_usd == 4.99` (atomic — overage entry append 안 됨) + `len(counter.entries) == 1`.
    - Test 3 (`test_zero_cost_claude_cli_ledger_entry_allowed`): `charge("claude_cli", 0.00, metadata={"reason": "Max 구독"})` → entries 에 append + `entry["amount_usd"] == 0.0` + `entry["cumulative_usd"] == 0.0` + metadata round-trip.
    - Test 4 (`test_full_run_simulation_under_cap`): Research §SMOKE-05 expected scenario simulation — nanobanana × 1 + kling × 8 + typecast × 1 + claude_cli × 0 charges. Total: `0.04 + 0.35*8 + 0.12 + 0.00 == 2.96`. Assert `counter.total_usd == 2.96`, `counter.total_usd < 5.00`, `len(counter.entries) == 10`.
    - Test 5 (`test_persist_evidence_shape_5_keys`): `counter.persist()` 호출 후 evidence_path 의 JSON 이 5 key (`cap_usd`, `total_usd`, `breached`, `entry_count`, `entries`) 보유 + `entries` list 각 항목이 5 key (`timestamp`, `provider`, `amount_usd`, `cumulative_usd`, `metadata`).
    - Test 6 (`test_provider_rates_integration`): `from scripts.smoke.provider_rates import PROVIDER_RATES_USD, estimate_adapter_cost` → counter 에 `charge("kling", estimate_adapter_cost("kling", 8))` 하면 $2.80 정확히 기록. counter 가 rates 모듈을 올바르게 consume.
    - Test 7 (`test_unknown_provider_in_rates_raises_keyerror`): `estimate_adapter_cost("fake_provider")` → `KeyError` raise (금기 #3 silent fallback 금지 증명).
  </behavior>
  <action>
    신규 파일 `tests/phase13/test_budget_enforcement.py` (≥100 lines, ~150 expected). 7 테스트 — 모두 Tier 1 (mock only, 실 API 0회):

    ```python
    """SMOKE-05 Tier 1 Budget Enforcement — mock adapter, 실 API 0회.

    Wave 4 Full E2E 가 수행할 실 live run (`budget_usage.json` 기록) 이전에
    BudgetCounter + provider_rates 조합의 enforcement 논리를 regression suite
    에 anchor. 모든 charge 는 시뮬레이션 숫자 — 실제 Kling/Typecast/Runway
    API 호출 없음.
    """
    from __future__ import annotations

    import json
    import pytest

    from scripts.smoke.budget_counter import BudgetCounter, BudgetExceededError
    from scripts.smoke.provider_rates import (
        PROVIDER_RATES_USD,
        all_known_providers,
        estimate_adapter_cost,
    )
    ```

    각 `def test_*(...)` 본문은 behavior 블록의 7 시나리오를 구현. 모든 test 는 `tmp_path` 또는 `tmp_evidence_dir` fixture 사용 — 실 evidence dir 오염 금지.

    **구현 주의점**:
    - Test 2 의 atomic 검증이 핵심 — overage 시 counter.entries 가 실패 전 상태 그대로 유지되는지 확인. BudgetCounter 의 구현에서 `raise` 가 `self.entries.append` 전에 수행되어야 함.
    - Test 4 의 10 entries 는 kling 8 cuts + 1 nanobanana + 1 typecast (claude_cli 는 별도 charge 만약 넣는다면 11) — 숫자 정확성 중요.
    - Test 5 의 5-key ledger shape 은 Wave 4 Full E2E 의 evidence JSON schema 계약 — Plan 06 `phase13_acceptance.py` 가 동일 key 를 검증.
    - Test 7 은 provider_rates.py 의 KeyError contract 직접 검증 — 금기 #3 보존.
    - **금기 #2**: TODO 없음. **금기 #3**: try/except 로 BudgetExceededError 삼키지 않음 — `pytest.raises` 경유.
    - **필수 #8**: 증거 기반 — 모든 assertion 에 명시적 메시지.
  </action>
  <verify>
    <automated>test -f tests/phase13/test_budget_enforcement.py &amp;&amp; grep -cE "^def test_" tests/phase13/test_budget_enforcement.py &amp;&amp; grep -c "BudgetExceededError" tests/phase13/test_budget_enforcement.py &amp;&amp; grep -c "PROVIDER_RATES_USD" tests/phase13/test_budget_enforcement.py &amp;&amp; grep -c "estimate_adapter_cost" tests/phase13/test_budget_enforcement.py &amp;&amp; py -3.11 -m pytest tests/phase13/test_budget_enforcement.py -m "not live_smoke" --no-cov -v</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/phase13/test_budget_enforcement.py` returns 0.
    - `wc -l tests/phase13/test_budget_enforcement.py` returns ≥100.
    - `grep -cE "^def test_" tests/phase13/test_budget_enforcement.py` returns ≥7.
    - `grep -c "BudgetExceededError" tests/phase13/test_budget_enforcement.py` returns ≥2.
    - `grep -c "PROVIDER_RATES_USD" tests/phase13/test_budget_enforcement.py` returns ≥2.
    - `grep -c "estimate_adapter_cost" tests/phase13/test_budget_enforcement.py` returns ≥2.
    - `grep -c "pytest.raises(BudgetExceededError)" tests/phase13/test_budget_enforcement.py` returns ≥1.
    - `grep -c "pytest.raises(KeyError)" tests/phase13/test_budget_enforcement.py` returns ≥1.
    - `grep -c "claude_cli" tests/phase13/test_budget_enforcement.py` returns ≥2.
    - `grep -c "atomic" tests/phase13/test_budget_enforcement.py` returns ≥1.
    - `grep -c "대표님" tests/phase13/test_budget_enforcement.py` returns ≥1.
    - `grep -c "TODO" tests/phase13/test_budget_enforcement.py` returns 0.
    - `py -3.11 -m pytest tests/phase13/test_budget_enforcement.py -m "not live_smoke" --no-cov -v` exit 0 with ≥7 Tier 1 tests PASSED.
    - Tier 2 marker 없음: `grep -c "pytest.mark.live_smoke" tests/phase13/test_budget_enforcement.py` returns 0 (본 Plan 은 Tier 1 전용 — Tier 2 live evidence 는 Wave 4 Plan 05 의 `phase13_live_smoke.py` 가 생성).
  </acceptance_criteria>
  <done>
    SMOKE-05 enforcement 로직 7 test GREEN + provider_rates SSOT 통합. Wave 4 Full E2E 가 실 run 에서 `budget_usage.json` 을 생성하기 전에 모든 edge case 가 Tier 1 에서 검증 완료.
  </done>
</task>

</tasks>

<verification>
Wave 3 완료 조건 (Plan 04):
1. `scripts/smoke/provider_rates.py` 신설 + `PROVIDER_RATES_USD` ≥8 키 + `estimate_adapter_cost` + `all_known_providers`.
2. `tests/phase13/test_budget_enforcement.py` ≥7 Tier 1 tests GREEN.
3. 누계 pytest: `pytest tests/phase13/ -m "not live_smoke" --no-cov -q` exit 0 with Wave 0~3 합산 ≥31 GREEN (Wave 0=6 + Wave 1=11 + Wave 2=7 + Wave 3=7).
4. claude_cli $0 ledger entry 허용 + unknown provider KeyError 보존 (금기 #3).
5. Phase 4/14 regression: `pytest tests/phase04 --no-cov -q` 244 passed + `pytest -m adapter_contract --no-cov -q` ≥30 passed — 회귀 없음.
</verification>

<success_criteria>
- SMOKE-05 ($5 cap enforcement + budget_usage.json 5-key shape) Tier 1 baseline 확립.
- Research §Provider Rates 표가 코드 SSOT (`PROVIDER_RATES_USD`) 로 anchoring — 드리프트 시 단일 수정 포인트.
- CLAUDE.md 금기 #2/#3 전수 준수 (TODO 없음, silent fallback 없음) + 필수 #7/#8 준수.
- 실 API 호출 0회 — 비용 $0.00.
</success_criteria>

<output>
완료 후 `.planning/phases/13-live-smoke/13-04-SUMMARY.md`:
- 2 Task 산출 + verify rc.
- Wave 3 Tier 1 tests 수 (예상: 7).
- `PROVIDER_RATES_USD` key count 실측.
- `estimate_adapter_cost("kling", 8)` 실측 결과 ($2.80 예상).
- 대표님 보고 2-문장 존댓말 요약.
</output>
