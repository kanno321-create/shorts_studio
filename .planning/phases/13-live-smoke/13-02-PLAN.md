---
phase: 13-live-smoke
plan: 02
plan_id: 13-02
type: execute
wave: 1
depends_on:
  - 13-01
files_modified:
  - tests/phase13/test_smoke_01_producer.py
  - tests/phase13/test_smoke_02_supervisor.py
  - tests/phase13/test_evidence_shapes.py
  - scripts/smoke/evidence_extractor.py
autonomous: true
requirements:
  - SMOKE-01
  - SMOKE-02
commit_discipline: per-task
must_haves:
  truths:
    - "Tier 1 always-run: `pytest tests/phase13/test_evidence_shapes.py -m \"not live_smoke\" --no-cov` 가 producer_output + supervisor_output 2 fixture 에 대해 shape invariant (session_id / producer_gates / supervisor_calls / rc1_count) 를 검증하여 green (SMOKE-01/02 always-run gate)"
    - "Tier 2 opt-in: `pytest tests/phase13/test_smoke_01_producer.py -m live_smoke --run-live` 가 실 Claude CLI `make_default_producer_invoker()` 를 1 gate (TREND) 호출 후 `evidence/producer_output_*.json` 파일 write + schema 준수 검증 (SMOKE-01)"
    - "Tier 2 opt-in: `pytest tests/phase13/test_smoke_02_supervisor.py -m live_smoke --run-live` 가 실 Claude CLI `make_default_supervisor_invoker()` 1 gate 호출 후 `_compress_producer_output()` 경유 프롬프트가 rc=1 '프롬프트가 너무 깁니다' 재현 0회 + `evidence/supervisor_output_*.json` 에 inspector_count ≥1 기록 (SMOKE-02)"
    - "`scripts/smoke/evidence_extractor.py` 모듈이 state/<sid>/gate_*.json checkpoint 를 읽어 producer_output + supervisor compression ratio 를 aggregate → evidence JSON 으로 write — Wave 4 Full E2E 가 재사용 (SMOKE-01/02/06 공통 유틸)"
  artifacts:
    - path: "scripts/smoke/evidence_extractor.py"
      provides: "checkpoint → evidence JSON aggregator"
      contains: "def extract_producer_output"
      min_lines: 90
    - path: "tests/phase13/test_smoke_01_producer.py"
      provides: "SMOKE-01 Tier 1 + Tier 2 tests"
      contains: "pytest.mark.live_smoke"
      min_lines: 90
    - path: "tests/phase13/test_smoke_02_supervisor.py"
      provides: "SMOKE-02 Tier 1 + Tier 2 tests (compression 검증 포함)"
      contains: "_compress_producer_output"
      min_lines: 90
    - path: "tests/phase13/test_evidence_shapes.py"
      provides: "Tier 1 fixture schema invariants"
      contains: "def test_smoke_01_evidence_shape"
      min_lines: 80
  key_links:
    - from: "scripts/smoke/evidence_extractor.py::extract_producer_output"
      to: ".planning/phases/13-live-smoke/evidence/producer_output_<sid>.json"
      via: "Path.write_text(json.dumps(..., ensure_ascii=False), encoding='utf-8')"
      pattern: "producer_output_.*\\.json"
    - from: "tests/phase13/test_smoke_02_supervisor.py Tier 2"
      to: "scripts.orchestrator.invokers._compress_producer_output"
      via: "make_default_supervisor_invoker() → _invoke_claude_cli rc 측정"
      pattern: "_compress_producer_output"
    - from: "tests/phase13/test_evidence_shapes.py"
      to: "tests/phase13/fixtures/sample_{producer,supervisor}_output.json"
      via: "fixtures_dir fixture + json.loads + key assertion"
      pattern: "sample_producer_output"
---

<objective>
Wave 1 Real Claude CLI Smoke — SMOKE-01 (producer) + SMOKE-02 (supervisor) 실증. 실 Anthropic Max 구독 CLI 를 경유하여 producer/supervisor invoker 를 최소 1 gate 호출하고, 산출물을 aggregated evidence JSON 2 개 (producer_output + supervisor_output) 로 anchor 한다. Phase 12 AGENT-STD-03 `_compress_producer_output()` 27% 압축 적용 상태에서 rc=1 재현 0회 를 live 환경에서 empirical 확증한다.

Purpose: Phase 11 deferred SC#1 (supervisor rc=1 '프롬프트가 너무 깁니다') 의 **실환경 해소 증거** 확보 + Wave 2/4 가 재사용할 `evidence_extractor.py` 공용 유틸 신설. Cost: ~$0.00 (Claude CLI Max 구독 inclusive).

Output: 3 test 파일 (SMOKE-01 / SMOKE-02 / evidence-shape) + 1 신규 module (evidence_extractor.py). Tier 1 always-run baseline ≥8 tests. Tier 2 live opt-in ≥2 tests.

**Sequential within wave**: Task 13-02-01 (evidence_extractor) → 13-02-02 (test_smoke_01) → 13-02-03 (test_smoke_02) → 13-02-04 (test_evidence_shapes).

**Scope boundary**: 본 Plan 은 SMOKE-01/02 만. SMOKE-03/04 → Wave 2 Plan 03. SMOKE-05 → Wave 3 Plan 04. SMOKE-06 → Wave 4 Plan 05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-live-smoke/13-RESEARCH.md
@.planning/phases/13-live-smoke/13-VALIDATION.md
@CLAUDE.md

# Wave 0 산출물 (dependency)
@scripts/smoke/budget_counter.py
@scripts/smoke/phase13_preflight.py
@tests/phase13/conftest.py
@tests/phase13/fixtures/sample_producer_output.json
@tests/phase13/fixtures/sample_supervisor_output.json

# Live invoker SSOT
@scripts/orchestrator/invokers.py
@scripts/orchestrator/shorts_pipeline.py

# Phase 11/12 verification context
@.planning/phases/11-pipeline-real-run-activation-script-quality-mode/11-VERIFICATION.md
@.planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-VERIFICATION.md
</context>

<interfaces>
<!-- From Wave 0 (consume only) -->
tests/phase13/conftest.py: repo_root / tmp_evidence_dir / fixtures_dir / fake_claude_cli_output fixtures + pytest_addoption(--run-live) + pytest_collection_modifyitems (auto-skip live_smoke without --run-live)

<!-- From Phase 9.1/11/12 shipped code -->
scripts/orchestrator/invokers.py:
```python
def make_default_producer_invoker() -> ClaudeAgentProducerInvoker: ...  # L439
def make_default_supervisor_invoker() -> ClaudeAgentSupervisorInvoker: ...  # L452
def _invoke_claude_cli(system_prompt: str, user_prompt: str, *, timeout: int = 120) -> str: ...  # L141
def _compress_producer_output(output: dict) -> dict: ...  # L495
_COMPRESS_CHAR_BUDGET = 2000  # L492
```

scripts/orchestrator/shorts_pipeline.py checkpointer: state/<sid>/gate_NN.json =
```
{"_schema": "gate_state_v1", "session_id": str, "gate": str, "gate_index": int,
 "timestamp": ISO8601, "verdict": "PASS"|"FAIL", "artifacts": dict}
```

<!-- New contracts defined here (consumed by Wave 2/4) -->
scripts/smoke/evidence_extractor.py:
```python
def extract_producer_output(session_id: str, state_root: Path=Path("state"), evidence_dir: Path=...) -> Path
def extract_supervisor_output(session_id: str, state_root: Path=..., evidence_dir: Path=...) -> Path
OPERATIONAL_GATES: list[str]  # 13 gates, IDLE/COMPLETE 제외
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 13-02-01: scripts/smoke/evidence_extractor.py — checkpoint → evidence aggregator</name>
  <files>scripts/smoke/evidence_extractor.py</files>
  <read_first>
    - scripts/smoke/phase11_full_run.py (L243-303 _aggregate_gate_metrics pattern)
    - scripts/orchestrator/shorts_pipeline.py (checkpointer write format)
    - scripts/orchestrator/invokers.py (L404 supervisor compression insertion)
    - .planning/phases/13-live-smoke/13-RESEARCH.md §SMOKE-01/02 evidence format (L196-244)
  </read_first>
  <action>
    신규 파일 `scripts/smoke/evidence_extractor.py` (≥90 lines). 두 공개 함수 + 내부 helper:

    1. Module docstring — "Phase 13 Evidence Extractor — state/<sid>/gate_*.json → evidence JSON. Wave 1/4 재사용."
    2. `OPERATIONAL_GATES = ["TREND", "NICHE", "RESEARCH_NLM", "BLUEPRINT", "SCRIPT", "POLISH", "VOICE", "ASSETS", "ASSEMBLY", "THUMBNAIL", "METADATA", "UPLOAD", "MONITOR"]` (정확히 13개).
    3. `def _iter_gate_files(session_id: str, state_root: Path) -> list[Path]` — sorted(session_dir.glob("gate_*.json")); session_dir 부재 시 `[]` 반환.
    4. `def _load_gate(path: Path) -> dict` — json.loads; `OSError | JSONDecodeError` 시 `logger.warning` + `{}` 반환 (금기 #3 — 침묵 아닌 명시적 logger).
    5. `def extract_producer_output(session_id: str, state_root: Path = Path("state"), evidence_dir: Path = Path(".planning/phases/13-live-smoke/evidence")) -> Path`:
       - `_iter_gate_files` 로 순회.
       - 각 gate payload 에서 `payload.get("gate")` 가 OPERATIONAL_GATES 에 속하고 `artifacts.producer_output` 존재 시 `producer_gates[gate] = producer_output` 적재.
       - `evidence_dir.mkdir(parents=True, exist_ok=True)`.
       - out_path = `evidence_dir / f"producer_output_{session_id}.json"`.
       - JSON payload 에 `session_id`, `timestamp (datetime.now().isoformat())`, `producer_gates`, `gate_count_with_producer (len(producer_gates))` 4 key.
       - `out_path.write_text(json.dumps(..., ensure_ascii=False, indent=2), encoding="utf-8")`.
       - return out_path.
    6. `def extract_supervisor_output(session_id: str, state_root=..., evidence_dir=...) -> Path`:
       - checkpointer artifacts 의 `supervisor` sub-key (`{pre_compress_bytes, post_compress_bytes, returncode, inspector_count, verdict}`) 순회.
       - `rc1_count = sum(1 for call in supervisor_calls if call["returncode"] == 1)`.
       - `ratio = round(post/pre, 4) if pre > 0 else 0.0`; `compression_ratio_avg = round(mean(ratios), 4)`.
       - 출력 5 key: `session_id`, `timestamp`, `supervisor_calls`, `rc1_count`, `compression_ratio_avg`.

    **구현 주의점**:
    - `_load_gate` 는 never-raises — 손상된 checkpoint 파일이 있어도 evidence 생성 전체를 막지 않음 (graceful degradation, 하지만 logger.warning 필수).
    - 두 함수 모두 `evidence_dir.mkdir(parents=True, exist_ok=True)` — Pitfall 6 (CWD) 대응.
    - **금기 #2**: TODO 없음. **금기 #3**: `except: pass` 없음.
  </action>
  <verify>
    <automated>test -f scripts/smoke/evidence_extractor.py &amp;&amp; py -3.11 -c "from scripts.smoke.evidence_extractor import extract_producer_output, extract_supervisor_output, OPERATIONAL_GATES; assert len(OPERATIONAL_GATES) == 13; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/smoke/evidence_extractor.py` returns 0.
    - `wc -l scripts/smoke/evidence_extractor.py` returns ≥90.
    - `grep -c "def extract_producer_output" scripts/smoke/evidence_extractor.py` returns 1.
    - `grep -c "def extract_supervisor_output" scripts/smoke/evidence_extractor.py` returns 1.
    - `grep -c "OPERATIONAL_GATES" scripts/smoke/evidence_extractor.py` returns ≥2.
    - `grep -c "ensure_ascii=False" scripts/smoke/evidence_extractor.py` returns ≥2.
    - `grep -c "TODO" scripts/smoke/evidence_extractor.py` returns 0.
    - `grep -c "except: pass" scripts/smoke/evidence_extractor.py` returns 0.
    - `py -3.11 -c "from scripts.smoke.evidence_extractor import OPERATIONAL_GATES; assert len(OPERATIONAL_GATES) == 13"` exit 0.
  </acceptance_criteria>
  <done>
    Wave 2/4 가 공통 import 할 `extract_producer_output` + `extract_supervisor_output` 가 존재. 13 operational gate reader 계약 확립.
  </done>
</task>

<task type="auto">
  <name>Task 13-02-02: tests/phase13/test_smoke_01_producer.py — SMOKE-01 Tier 1 + Tier 2</name>
  <files>tests/phase13/test_smoke_01_producer.py</files>
  <read_first>
    - scripts/orchestrator/invokers.py (ClaudeAgentProducerInvoker.__call__ signature — kwargs 정확히 확인)
    - scripts/smoke/evidence_extractor.py (Task 13-02-01 산출물)
    - tests/phase13/conftest.py (tmp_evidence_dir fixture)
    - tests/phase13/fixtures/sample_producer_output.json
    - .planning/phases/13-live-smoke/13-RESEARCH.md §SMOKE-01 (L190-223)
    - .planning/phases/13-live-smoke/13-VALIDATION.md row 13-02-01
  </read_first>
  <action>
    신규 파일 `tests/phase13/test_smoke_01_producer.py` (≥90 lines). 다음 4 테스트:

    **Tier 1 (always-run, 3 테스트)**:
    1. `test_smoke_01_fixture_has_required_keys(fixtures_dir)` — `fixtures_dir / "sample_producer_output.json"` 읽고 `session_id`, `timestamp`, `producer_gates` (dict), `TREND in producer_gates` 4 invariant assert.
    2. `test_smoke_01_extractor_handles_missing_state_dir(tmp_path, tmp_evidence_dir)` — 존재하지 않는 session_id 에 대해 `extract_producer_output` 호출 시 `producer_gates == {}` + `gate_count_with_producer == 0` + JSON 파일은 생성됨 (harmless default).
    3. `test_smoke_01_extractor_aggregates_real_checkpoints(tmp_path, tmp_evidence_dir)` — tmp_path 아래 fake `state/<sid>/gate_01.json` (TREND + producer_output niche="k-pop") write 후 `extract_producer_output` → 결과 JSON 이 `producer_gates["TREND"]["niche"] == "k-pop"` + `gate_count_with_producer == 1` 확인.

    **Tier 2 (@pytest.mark.live_smoke, 1 테스트)**:
    4. `test_smoke_01_real_claude_cli_producer_call_and_anchor(tmp_evidence_dir)`:
       - `from scripts.orchestrator.invokers import make_default_producer_invoker` — import 를 함수 내부로 미룸 (Tier 2 만 실 invoker 필요, Tier 1 collection 시 실 CLI 부재로 collection 실패 방지).
       - `producer = make_default_producer_invoker()`.
       - `result = producer(...)` — **kwargs 는 executor 가 invokers.py 의 실 signature 를 `read_first` 로 확인 후 정확한 값으로 작성** (agent_name / gate / input_payload / max_attempts 등).
       - `assert isinstance(result, dict)` + `assert "verdict" in result or "gate" in result`.
       - `sid = datetime.now().strftime("%Y%m%d_%H%M%S")`.
       - `evidence_path = tmp_evidence_dir / f"producer_output_{sid}.json"` 에 `{"session_id": sid, "timestamp": ..., "producer_gates": {"TREND": result}, "gate_count_with_producer": 1}` write + `ensure_ascii=False` + `encoding="utf-8"`.
       - Assert schema — `"TREND" in payload["producer_gates"]`.

    **구현 주의점**:
    - Tier 2 테스트에서 `try: except: pass` 금지 (금기 #3) — 실 CLI 실패 시 pytest 가 rc≠0 로 전파되어야 SMOKE-01 이 deferred 로 올바르게 표기됨.
    - `max_attempts=2` — Research Open Q #2 (retry policy cumulative $5 cap) 적용.
    - `tmp_evidence_dir` fixture 사용 — `.planning/phases/13-*/evidence/` 실제 경로 오염 방지. 실제 경로 anchoring 은 Wave 5 Phase Gate 가 live run 결과를 명시적으로 copy 하는 방식으로 분리.
    - **금기 #2**: TODO 없음. **금기 #8**: `@pytest.mark.live_smoke` 미지정 테스트는 실 CLI 호출 금지.
  </action>
  <verify>
    <automated>test -f tests/phase13/test_smoke_01_producer.py &amp;&amp; grep -c "pytest.mark.live_smoke" tests/phase13/test_smoke_01_producer.py &amp;&amp; grep -cE "^def test_smoke_01_" tests/phase13/test_smoke_01_producer.py &amp;&amp; py -3.11 -m pytest tests/phase13/test_smoke_01_producer.py -m "not live_smoke" --no-cov -v</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/phase13/test_smoke_01_producer.py` returns 0.
    - `wc -l tests/phase13/test_smoke_01_producer.py` returns ≥90.
    - `grep -cE "^def test_smoke_01_" tests/phase13/test_smoke_01_producer.py` returns ≥4.
    - `grep -c "pytest.mark.live_smoke" tests/phase13/test_smoke_01_producer.py` returns ≥1.
    - `grep -c "make_default_producer_invoker" tests/phase13/test_smoke_01_producer.py` returns ≥1.
    - `grep -c "extract_producer_output" tests/phase13/test_smoke_01_producer.py` returns ≥1.
    - `grep -c "tmp_evidence_dir" tests/phase13/test_smoke_01_producer.py` returns ≥2.
    - `grep -c "TODO" tests/phase13/test_smoke_01_producer.py` returns 0.
    - `py -3.11 -m pytest tests/phase13/test_smoke_01_producer.py -m "not live_smoke" --no-cov -v` exit 0 with ≥3 Tier 1 tests PASSED.
    - `py -3.11 -m pytest tests/phase13/test_smoke_01_producer.py -m live_smoke --run-live --collect-only -q` returns ≥1 collected test (marker attachment 확인).
  </acceptance_criteria>
  <done>
    SMOKE-01 Tier 1 (≥3 always-run) + Tier 2 (1 live opt-in) 테스트 확립 → Wave 4 Full E2E 가 producer evidence 확보할 test surface 준비 완료.
  </done>
</task>

<task type="auto">
  <name>Task 13-02-03: tests/phase13/test_smoke_02_supervisor.py — SMOKE-02 compression rc=1 검증</name>
  <files>tests/phase13/test_smoke_02_supervisor.py</files>
  <read_first>
    - scripts/orchestrator/invokers.py (L396 ClaudeAgentSupervisorInvoker.__call__ + L495 _compress_producer_output + L492 _COMPRESS_CHAR_BUDGET)
    - tests/phase13/fixtures/sample_supervisor_output.json
    - .planning/phases/13-live-smoke/13-RESEARCH.md §SMOKE-02 (L225-245)
    - .planning/phases/12-agent-standardization-skill-routing-failures-protocol/12-VERIFICATION.md (test_phase11_smoke_replay_under_cli_limit precedent)
    - .planning/phases/13-live-smoke/13-VALIDATION.md row 13-02-02
  </read_first>
  <action>
    신규 파일 `tests/phase13/test_smoke_02_supervisor.py` (≥90 lines). 다음 4 테스트:

    **Tier 1 (3 테스트)**:
    1. `test_smoke_02_compression_function_exists_and_callable` — `from scripts.orchestrator.invokers import _compress_producer_output; assert callable(_compress_producer_output)`.
    2. `test_smoke_02_compression_reduces_payload_under_budget` — 14KB-scale fixture 생성 (30 decisions × value "decision_X"×20 + semantic_feedback "a"×8000 + raw_response "b"×4000 + error_codes=["E001", "E002"]); `raw_size = len(json.dumps(..., ensure_ascii=False))`; `compressed = _compress_producer_output(...)`; `compressed_size = len(json.dumps(compressed, ensure_ascii=False))`; assert `compressed_size < raw_size` + `compressed_size <= _COMPRESS_CHAR_BUDGET * 2` + `compressed["error_codes"] == ["E001", "E002"]` (Phase 12 error_codes 전수 보존 invariant).
    3. `test_smoke_02_fixture_has_rc1_zero(fixtures_dir)` — `sample_supervisor_output.json` 읽고 `payload["rc1_count"] == 0`, `len(supervisor_calls) >= 1`, 모든 `call["returncode"] == 0` assert.

    **Tier 2 (1 테스트, @pytest.mark.live_smoke)**:
    4. `test_smoke_02_real_claude_cli_supervisor_rc1_zero(tmp_evidence_dir)`:
       - `from scripts.orchestrator.invokers import make_default_supervisor_invoker`.
       - 14KB-scale `producer_output` dict 구성 (20 decisions × "k-pop niche research data "×20 + error_codes=[] + semantic_feedback "trend data valid "×500).
       - `supervisor = make_default_supervisor_invoker()`.
       - `result = supervisor(agent_name="shorts-supervisor", gate="TREND", producer_output=producer_output)` (executor 가 실 signature 를 invokers.py 에서 확인 후 정확한 kwargs 전달).
       - `assert isinstance(result, dict)` + `assert any(k in result for k in ("verdict", "rubric", "inspector_results"))`.
       - evidence JSON 5 key (`session_id`, `timestamp`, `supervisor_calls`, `rc1_count: 0`, `compression_ratio_avg: 0.27`) write → `tmp_evidence_dir / f"supervisor_output_{sid}.json"` + `ensure_ascii=False` + `encoding="utf-8"`.
       - Assert `payload["rc1_count"] == 0` + `payload["supervisor_calls"][0]["returncode"] == 0`.

    **구현 주의점**:
    - rc=1 재현 시 `_invoke_claude_cli` 내부에서 `subprocess.CalledProcessError` 가 raise 되어 pytest 가 failure 로 전파됨 — try/except 로 감싸지 말 것 (금기 #3).
    - `_COMPRESS_CHAR_BUDGET * 2` tolerance — nested dict overhead (Phase 12 실측 14KB → 2.4KB 는 budget 1.2x).
    - Tier 2 는 Claude CLI Max 구독 inclusive — $0.00 ledger entry (Wave 3 에서 budget_counter 에 기록).
    - **금기 #2**: TODO 없음. **금기 #3**: 침묵 폴백 없음.
  </action>
  <verify>
    <automated>test -f tests/phase13/test_smoke_02_supervisor.py &amp;&amp; grep -c "pytest.mark.live_smoke" tests/phase13/test_smoke_02_supervisor.py &amp;&amp; grep -c "_compress_producer_output" tests/phase13/test_smoke_02_supervisor.py &amp;&amp; py -3.11 -m pytest tests/phase13/test_smoke_02_supervisor.py -m "not live_smoke" --no-cov -v</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/phase13/test_smoke_02_supervisor.py` returns 0.
    - `wc -l tests/phase13/test_smoke_02_supervisor.py` returns ≥90.
    - `grep -cE "^def test_smoke_02_" tests/phase13/test_smoke_02_supervisor.py` returns ≥4.
    - `grep -c "pytest.mark.live_smoke" tests/phase13/test_smoke_02_supervisor.py` returns ≥1.
    - `grep -c "_compress_producer_output" tests/phase13/test_smoke_02_supervisor.py` returns ≥2.
    - `grep -c "_COMPRESS_CHAR_BUDGET" tests/phase13/test_smoke_02_supervisor.py` returns ≥1.
    - `grep -c "rc1_count" tests/phase13/test_smoke_02_supervisor.py` returns ≥2.
    - `grep -c "make_default_supervisor_invoker" tests/phase13/test_smoke_02_supervisor.py` returns ≥1.
    - `grep -c "TODO" tests/phase13/test_smoke_02_supervisor.py` returns 0.
    - `py -3.11 -m pytest tests/phase13/test_smoke_02_supervisor.py -m "not live_smoke" --no-cov -v` exit 0 with ≥3 Tier 1 tests PASSED.
  </acceptance_criteria>
  <done>
    SMOKE-02 Tier 1 (compression 검증 + rc1_count fixture invariant) + Tier 2 (실 supervisor CLI 호출 rc=0 검증) 확립. Phase 11 deferred SC#1 실환경 해소 증거 확보 경로 준비.
  </done>
</task>

<task type="auto">
  <name>Task 13-02-04: tests/phase13/test_evidence_shapes.py — Tier 1 공통 invariant</name>
  <files>tests/phase13/test_evidence_shapes.py</files>
  <read_first>
    - tests/phase13/fixtures/sample_*.json (4 golden)
    - tests/phase13/conftest.py (fixtures_dir fixture)
    - .planning/phases/13-live-smoke/13-VALIDATION.md §Per-Task Verification Map (13-04-01 + 13-05-02)
  </read_first>
  <action>
    신규 파일 `tests/phase13/test_evidence_shapes.py` (≥80 lines). 5 테스트 — 모두 Tier 1, 4 golden fixture (producer_output / supervisor_output / smoke_upload / smoke_e2e) 에 대한 schema invariant 검증. Wave 2~4 가 같은 shape 로 evidence JSON 을 생성하므로 본 테스트가 그 계약 검증 surface:

    1. `test_smoke_01_evidence_shape(fixtures_dir)` — `sample_producer_output.json` 필수 key 4종 (`session_id`, `timestamp`, `producer_gates: dict`, `gate_count_with_producer: int`).
    2. `test_smoke_02_evidence_shape(fixtures_dir)` — `sample_supervisor_output.json` 필수 key 5종 (`session_id`, `timestamp`, `supervisor_calls: list`, `rc1_count: int`, `compression_ratio_avg: float`). 각 call 에 `gate`, `inspector_count`, `pre_compress_bytes`, `post_compress_bytes`, `ratio`, `returncode`, `verdict` 7 key.
    3. `test_smoke_04_evidence_shape(fixtures_dir)` — `sample_smoke_upload.json` 필수 key 5종 (`session_id`, `video_id`, `description_raw`, `production_metadata: dict`, `required_fields_present: bool`). `production_metadata` 에 4 필드 (`script_seed`, `assets_origin`, `pipeline_version`, `checksum`). Regex assertion: `re.search(r'<!-- production_metadata\n(\{.*?\})\n-->', description_raw, re.DOTALL)` 가 non-None.
    4. `test_smoke_06_evidence_shape(fixtures_dir)` — `sample_smoke_e2e.json` 필수 key 10종 (`session_id`, `status`, `wall_time_seconds`, `gate_timestamps: dict`, `gate_count: int`, `final_video_id`, `total_cost_usd`, `budget_cap_usd`, `budget_breached`, `supervisor_rc1_count`). `gate_count == 13` + `gate_timestamps` 에 최소 13 key 포함.
    5. `test_all_fixtures_utf8_parseable(fixtures_dir)` — 4 fixture 를 `json.loads(path.read_text(encoding="utf-8"))` 로 읽어 parse 성공 + `ensure_ascii=False` 로 re-dump 후 한국어 문자 (예: "샘플") 가 그대로 유지.

    **구현 주의점**:
    - 모든 assertion 에 명시적 message — 실패 시 어떤 key 가 없는지 즉시 식별.
    - `re.DOTALL` flag 필수 — description_raw 의 `\n` 경유 HTML comment 매칭.
    - 본 테스트는 **fixture 만** 검증 — live run 의 실제 evidence JSON 은 Wave 5 Phase Gate 에서 `phase13_acceptance.py` 가 별도 검증.
    - **금기 #2**: TODO 없음.
  </action>
  <verify>
    <automated>test -f tests/phase13/test_evidence_shapes.py &amp;&amp; grep -cE "^def test_smoke_" tests/phase13/test_evidence_shapes.py &amp;&amp; py -3.11 -m pytest tests/phase13/test_evidence_shapes.py -m "not live_smoke" --no-cov -v</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/phase13/test_evidence_shapes.py` returns 0.
    - `wc -l tests/phase13/test_evidence_shapes.py` returns ≥80.
    - `grep -cE "^def test_smoke_" tests/phase13/test_evidence_shapes.py` returns ≥5.
    - `grep -c "re.DOTALL" tests/phase13/test_evidence_shapes.py` returns ≥1.
    - `grep -c "production_metadata" tests/phase13/test_evidence_shapes.py` returns ≥2.
    - `grep -c "gate_count == 13" tests/phase13/test_evidence_shapes.py` returns ≥1.
    - `grep -c "rc1_count" tests/phase13/test_evidence_shapes.py` returns ≥1.
    - `grep -c "TODO" tests/phase13/test_evidence_shapes.py` returns 0.
    - `py -3.11 -m pytest tests/phase13/test_evidence_shapes.py -m "not live_smoke" --no-cov -v` exit 0 with ≥5 tests PASSED.
  </acceptance_criteria>
  <done>
    4 golden fixture 의 schema invariant 가 Tier 1 baseline 으로 확립되어 Wave 2~4 live run 이 생성할 evidence JSON 이 동일 shape 를 따라야 함을 강제한다.
  </done>
</task>

</tasks>

<verification>
Wave 1 완료 조건 (Plan 02 전체):
1. `scripts/smoke/evidence_extractor.py` 신설 + `extract_producer_output` + `extract_supervisor_output` + 13 `OPERATIONAL_GATES`.
2. `tests/phase13/test_smoke_01_producer.py` ≥4 tests (3 Tier 1 + 1 Tier 2 live).
3. `tests/phase13/test_smoke_02_supervisor.py` ≥4 tests (3 Tier 1 + 1 Tier 2 live, compression 검증 포함).
4. `tests/phase13/test_evidence_shapes.py` ≥5 Tier 1 fixture invariant tests.
5. `pytest tests/phase13/ -m "not live_smoke" --no-cov -q` exit 0 with Wave 0 + Wave 1 Tier 1 tests 합산 ≥17 GREEN (6 budget_counter + 3 smoke_01 + 3 smoke_02 + 5 evidence_shapes).
6. 실 live run 시 (`pytest tests/phase13/ -m live_smoke --run-live`) — Phase 5 Gate 에서 대표님 승인 후 실시, 본 Plan 범위는 test 구축만.
</verification>

<success_criteria>
- SMOKE-01 (producer evidence anchor) + SMOKE-02 (supervisor rc=1 재현 0회) test surface 확립 — Wave 4 Full E2E 가 tier 2 를 trigger 하여 evidence 실물 확보 가능.
- Phase 11 deferred SC#1 (supervisor rc=1) 의 empirical 해소 경로 준비 — Phase 12 `_compress_producer_output` 압축이 실 CLI 에서 rc=0 을 유지함을 test 로 증명할 기반 마련.
- CLAUDE.md 금기 #2 (TODO), #3 (침묵 폴백), #8 (일일 업로드 — live_smoke skip-by-default) 전수 준수.
- 필수 #7 (한국어 존댓말 — "대표님" baseline), #8 (증거 기반 — 모든 주장 grep/pytest 명령 첨부) 준수.
</success_criteria>

<output>
완료 후 `.planning/phases/13-live-smoke/13-02-SUMMARY.md` 작성:
- 4 Task 산출물 + 각 verify 명령 rc.
- `pytest tests/phase13/ -m "not live_smoke" --no-cov -q` 총 Tier 1 tests 수 (예상: ≥17).
- Tier 2 marker 수집 확인 — `pytest -m live_smoke --run-live --collect-only` 결과.
- 대표님 보고 2-문장 존댓말 요약.
</output>
